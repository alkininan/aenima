# Code spans

The column holds `` `${release}+${digest}` `` — `1.1.0+602d20db225ee669`, semver.

The fold deleted `**` and `` ` `` unconditionally.

A plain `code` span, a ${brace} outside one, and a [link](src/lib/routes.ts).

An unclosed ` backtick and an empty `` pair are literal text.
