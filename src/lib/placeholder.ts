/**
 * Placeholder utility. Exists only so the unit-test harness has something real
 * to exercise before the first genuine util lands in `src/lib`. Delete it then.
 */
export function identity<T>(value: T): T {
  return value;
}
