# T0.16 — Self-merge: a finished ticket merges itself
_2026-09-14T18:05:00Z_

T0.16 — self-merge. The board's Status is read as the select it is, so a task at Review is seen
again by the preflight and the guard; three human `merge` words that this run's own preflight
met and could not act on wait for the next. The reviewer's verdict is a file, `docs/reviews/<id>.md`,
and `gh pr merge` has a second door the guard opens on its own reading: that file ending in PASS,
the Stop gate's green on record for the very tree, a diff touching no gated path — `drizzle/`,
`.claude/`, `scripts/hooks/`, `scripts/run/`, `docs/product-spec.md`, the two dotfiles, and
`package.json` when its scripts move, listed once in `gated.mjs` — and the pull request's head
equal to the checkout's HEAD. A gated diff stays at Review with one comment naming the path, and
`merge` there is still the human's word; everything else lands and deploys at close, Done and the
Release row in the same run. The guard and the gate now run from `origin/main`'s copy of
`scripts/`, extracted per call, so a run editing its own boundary changes nothing until a human
merges it — the fixture test found `mktemp` answering a symlink that the hook's own entry check
could not match. Once per commit of main the next run asks the live site two questions from
outside and, on a wrong answer, reverts the merge at the tip with one commit the guard lets
through as the one push to main. Guidelines v1.8, build-guide v2.6. Three review passes: the
report and the log, the order of gate and merge — the gate now runs again before a self-merge,
main's copy, and the guard reads its record — and, past the cap, the verdict rule that would have
shut the door on every review with a Should. This pull request is gated by its own rule and waits
for the human's word — the last one.
