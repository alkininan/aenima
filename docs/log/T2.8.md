# T2.8 — Sufficiency probes
_2026-09-14T09:32:34Z_

T2.8 — sufficiency probes. **The five checks whose verdicts moved in T2.7 hold one state across
eleven runs.** T2.7 had scored `sample-juno-feature.md` eleven times on identical bytes and found
the six moving checks were exactly the ones asking whether what is present is *enough* — a check
that asks "is it there?" answered a clean absence every time, and a check that asks "is it
enough?" had no settled reading of enough. This ticket gives those five that reading, in §5's
own shape: "behind each check sits an open-ended probe library of follow-up questions". A probe
is plain text on a check — no points, no tag, no id, so "a probe never becomes a check" is the
type — rendered under its check in the prompt, and the protocol says once what a probe is: a
question answered from the artifact alone, a check satisfied only when every one of its probes
is, the first unanswered probe named as the gap. Pack 1.0.0 → 1.1.0, protocol 1.1.0 → 1.2.0,
`1.2.0+6476334ca2123595`, one re-baseline.

Measured the same way, eleven runs of the shipped scorer on the same bytes: spread **18.7 points →
0.3**, checks holding one state **14 of 20 → 19 of 20**, and each of `prd-12`, `prd-14`,
`prd-16`, `prd-18`, `prd-19` at fail 11 — the two coin flips settled without the majority vote
the ticket held in reserve. The 0.3 that remain are `prd-15`'s condition, §4's applicability
answer, held in 3 of 11 as it was held in 4 of 11 before; a condition carries no probes and
whether it may is a spec question, owner T-next. The settled answer is fail, and the notes now
name the unanswered probe rather than restating the check — "a cap on distinct partners per
week", quoted, "stated without a number or threshold"; "offline and degraded … never addressed at
all" — which reads as right on this document and is the golden set's to grade. One call in twelve
was refused by the fabrication guard for a quote that was not verbatim, wrote nothing, and was
replaced; $0.72 over twelve calls. Every stored meter re-scores on its next open and, on documents
like this one, lands lower — §5's re-baseline as written, with this entry as the explanation.
