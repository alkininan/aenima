# T1.5 — Row roving and freshness
_2026-09-16T14:24:37Z_

T1.5 — row roving and freshness. **Arrow keys walk the list's rows, and the row reads the scoring
clock.** Two of the three things T1.2 left on the list surface, in build-log open question 6's
words. Design-spec §11's arrow keys now walk the rows: one client island, `RowWalker`, wraps the
three buckets. So Down and Up cross from Your move into At risk and Flowing in visual order and
wrap at the ends, and Home and End reach the first and last row, over the `nextRovingIndex` the
menus already use. The rows stay Server Components. The stop is an attribute on the row's link,
and its name lives in a plain module because a value imported across the client boundary arrives
as a reference, not the string. The row's freshness reads the item page's clock: "scored 6h ago",
and "scored 6h ago — retrying" with the `--warning` dot while §5's queue holds a retry. A row
nothing has scored keeps last activity. Open question 6 had put this off as a second read across
the workspace. It turned out to be an embed on the request the list already makes, ordered
newest-first and capped at one *on the embed*, so open question 7's rule holds and nothing caps
the list. A read-only probe against PostgREST found the shape accepted and the newest of 46 runs
chosen.

Park was the third thing, and it is not here. Two claims stopped on where a parked item is stored:
`item` has no column for it, and a column is not something a run invents. The addendum of
2026-09-16 split it out to T1.6 Park move, which owns the storage and the migration, and this
ticket closed on the two finished pieces. The criteria were no migration or schema file, the tests
green on merged main, and no park in the code diff, each observed red first. The "Park?" chip
still renders and does nothing until T1.6. The review was one pass on `opus`: the pinned `fable`
was out of credits, and the pass stopped at its turn limit and was resumed once. It found no Must
and five Shoulds. Two red-first rows were re-reddened by mutations aimed at their claims. The stale
lines about unscored rows, and the row meters design-spec §10 says "appear when the scores do",
went to a Backlog Fix, "Show row meters once scores exist".
