# T0.23 — Partial reviews and mirror verification
_2026-09-16T13:03:00Z_

Three things a run met half-done, each now ending in the truth. A reviewer pass that stops at its
30-turn limit comes back as a result, not an error, so `review-model.mjs` never saw it and every
run that met one improvised a resume. It now reads Claude Code's own note — only where a result
begins with it, since a finished review may quote it — and answers `resume: true` once, on the
model the pass ran on, and a stop the second time. The report's reviewer table gains `resumed`,
and `report-check.mjs` refuses a row whose verdict is not PASS or FINDINGS: a partial review is
never one. §3's In progress → Decision row now names the review that could not run. The mirror
step reads each page back over the API after every chunk. Exact equality was measured first and
refused: Notion drops an HTML comment in one place and keeps it in another, and eats the digit of
a bolded numbered line, so the eight mirrors read back 0 to 7 letters apart. `verify` compares
letters and digits within 64, either way, and the last 48 sent — a write cut off mid-chunk, the
failure two chunks met on 2026-09-16, loses its ending whatever its size — and a page that reads
wrong is rewritten once from its sentinel and left under it the second time. And when the
connector's query has hit the workspace's usage limit, step 0 asks the same questions over the
token with `rows.mjs`, which is what the run that met the limit did by hand.

**The ticket met its own rule during its own review.** Both passes found Fable out of credits and
fell back to Opus by T0.22's script. Pass 1 returned PASS with seven Shoulds; four were small code
changes worth making — the note unanchored, a count that is not a number allowing one more resume,
a page that reads long passing as whole, a read-back that fails left unsaid — so they were fixed
with tests red first and sent to pass 2 rather than landed unreviewed. Pass 2 then stopped at its
turn limit with no report. The run asked the script, got `turn-limit` and `resume: true`,
continued the same session with one line, and the pass finished: PASS, marked resumed in the
table the same ticket added. AC4 needed no code — `decide` has skipped a current page since T0.12
— and got a test that holds it for all eight pages.
