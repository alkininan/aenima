# T0.14 — Ignore worktrees in lint
_2026-09-13T18:41:28Z_

T0.14 — ignore worktrees in lint. `eslint .` from a checkout walked into `.claude/worktrees/`,
where every other session's worktree lives, and linted their generated `.next/types`: the
gate from the primary went red whenever a worktree had built. Found at the T0.11 merge and
refused unticketed there, as it should have been. The fix is one pattern in the root config's
`globalIgnores`, `.claude/worktrees/**`, mirroring `.gitignore:57`; `.next/**` stays anchored,
since the worktree line covers every nested build there is. One test loads the config as the
gate does and checks a worktree's build output is ignored while the repository's own code is
still linted. One review pass, no Must. The run read no board comments: `NOTION_TOKEN` in
`.env.local` is a placeholder the API refuses, which the report records as its first open
question.
