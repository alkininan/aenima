# T0.20 — Cap counts clarifying rounds
_2026-09-15T18:17:37Z_

T0.20 — the two-round cap counts clarifying rounds and nothing else. It had counted every
pipeline comment after the first, so a task worked over several runs went mute: this run's own
preflight read T0.12's `merge` and T0.13's `merge` then `default` as capped and could say nothing
on either. A comment now carries its kind in its own words — the fixed part of the sentence
`compose` writes — and `kindOf` reads it back, from the comments already on the board as much as
from new ones. `readThread` counts the clarifying rounds since the pipeline last asked or
answered anything else, reading through the board's notices, so a reply resets the count once the
run has answered it as anything but unclear. `mayPost` in `comments.mjs` is the one place that
says whether a comment of a kind may post: the preflight reads it as `mayClarify`, and the guard
reads the page's thread over the API before any ⟡ comment goes up, holding back a third
clarifying round on one question and a second comment of a kind in one claim. Every other comment
posts, and a refusal always does: the new `refused` kind reports a merge GitHub turned down after
the guard let it through — naming the files `conflicts.mjs` finds with `git merge-tree` — a
failed apply, and a claim that could not go on. On the real board that script names
`docs/build-log.md` and `docs/guidelines.md` as what stands between T0.13 and main.

Guidelines v1.11 (§4, and §5's step 4, token paragraph and hard boundaries). Three review passes
on Opus, the reviewer's own model being out of credits: pass 1 found a board notice resetting the
count and a claim's second `default` walking into the new refusal, pass 2 a refusal hiding the
migration question from the next `apply` and comments posted after the marker was released, and
pass 3 passed with six Shoulds left for the human. The same preflight refreshed all six document
mirrors for the first time since T0.12 landed. The diff is gated by `.claude/` and `scripts/`, and
waits for the human's word.
