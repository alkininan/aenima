# T0.13 — Restrict Vercel's database role: the boundary reaches production
_2026-09-15T20:15:00Z_

T0.13 — Vercel's Production `DATABASE_URL` now names `aenima_pipeline`, the restricted role every
local and pipeline connection has used since T0.10, so the admin credential is on no path at all —
not a run's and not the deployed app's — and `.env.migrate` in the primary checkout is its only
home. The change itself was the human's, made by hand on 2026-09-13 after the run stopped for it
under §4: no Vercel CLI on the machine, no login, and a public surface a wrong guess would touch.
The run's part was the two ends of it — the stop with one comment, and on the answer the checks
from the outside (`/sign-in` 200, `/app` 307 to `/sign-in`, the signed-in load as the human's word
on the thread) and the sentence in guidelines §5, written only once the change was live.

It waited at Review long enough for five tickets to land ahead of it, each bumping the guidelines
header and adding a build-log line, so its `merge` met conflicts and came back as an addendum: main
merged in, main's text kept in both files, the one §5 sentence re-applied, the header taken to
v1.12 and the list regenerated rather than hand-merged. No code, no schema, no test file: the
red-first record holds observed commands and the log-index test rather than new tests.
