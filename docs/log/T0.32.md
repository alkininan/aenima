# T0.32 — Current state generated from the repo
_2026-09-23T04:45:47Z_

The build log's Tickets done list has been a function of `docs/log/` for several tickets, and
Current state above it was still typed by a person — so it had drifted exactly the way §5 says a
stamp drifts. It named product-spec v1.5 and design-spec v2.17 where the repo held v1.8 and v2.22,
and a next ticket, T3.1, that was already done and had its own log entry. `log-index.mjs` now
writes that block too: the version each of product-spec, design-spec and guidelines declares in
its own header, read through `version-drift.mjs`'s `parseHeaderVersion` rather than a second copy
of it, and the newest entry in the directory with its date and commit. The remote and the deploy
stand as `STANDING`, because nothing in the repo derives them.

Phase and next ticket are gone rather than generated. The board owns the queue and the roadmap
owns the phases, and a copy here could only ever disagree with them — which is the same argument
one layer up from the one the block itself makes.

What Current state said and no longer does is held by the entries it summarized. product-spec
v1.4's two rulings, and its landing just ahead of T2.2, went to `T2.2.md`. The form language's two
runs against the sign-in flow, what the deep ramp and the aero materials came to in tokens, and
the helper line narrowed to its own field's errors went to `design-spec-v2.7-v2.12.md`. Where
v2.13–v2.15 came from — the first surfaces, not a step in a flow — went to
`design-spec-v2.13-v2.15.md`. Every item of the v2.3–v2.6 list was already in T0.5 and T0.6.

One clause is recorded here because no other file holds it: **at the versions Current state last
stamped by hand — product-spec v1.5 and design-spec v2.17 — both documents stood complete and
closed and the code matched them.** The design spec still says the first half of that of itself,
at its own current version; the product spec has never said it, and nothing in the repo has ever
said the second half. That is the reason it is written down once, as a fact about a moment, rather
than carried forward as a standing claim: whether the code matches a specification today is not
something the repo can check, and a sentence that says it anyway is the stamp this ticket removed.

`.claude/commands/ticket-done.md` step 1 told a session to set the next-ticket line and mark a
phase complete. That instruction was right until this change and hand-edits a generated block
after it, so it went in the same diff — it now writes the log entry and runs the script, which is
what §5 step 8 has done since T0.10.

One thing the first generated block showed immediately: Newest entry names T0.31 rather than this
ticket, because `T0.31.md`'s stamp reads `2026-09-23T06:58:00Z` and its commit landed at
`03:54Z` — local time written with a `Z`, against CLAUDE.md's "store timestamps in UTC". The
script is right and the stamp is wrong; correcting a committed entry is not this ticket's, so it
is filed at Backlog instead. Reading the stamps for *newest* is what made a three-hour error
visible after the list had sorted on it for a ticket.
