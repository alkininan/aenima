# T2.9 — Applicability stability
_2026-09-16T10:23:02Z_

T2.9 — applicability stability. **The three applicability conditions hold one state across eleven
runs.** T2.8 had settled the verdicts and left §4's applicability answer moving — `prd-15`'s
condition held in 3 of 11 on identical bytes, and open question 22 had seen the safety layer enter
one run's denominator and not the next's, so a Must was asked or not asked on the scorer's mood and
a worse document could score higher. The ticket's Rules called it the scorer having no settled
answer, which a probe addresses and a vote cannot, and this ticket gives a condition what T2.8 gave
a check: probes, plain text on the condition, rendered beneath it in the prompt's CONDITIONS block,
and one protocol paragraph saying how a probed condition is decided — by its probes and by nothing
else, holding when the artifact answers yes to at least one. Product-spec §4 records it at v1.8,
the spec question T2.8 left open. Pack 1.1.0 → 1.2.0, protocol 1.2.0 → 1.3.0,
`1.3.0+1ef28b5d617ff4a0`, one re-baseline; the answer schema, the stored run and the denominator
arithmetic are untouched.

Measured the same way, eleven runs of the shipped scorer on the same bytes: spread **0.3 → 0.0**,
denominator **99 in 8 and 105 in 3 → 105 in 11**, checks holding one state **19 of 20 → 20 of 20**.
The list condition settled to yes — the scorer names the venue search as search results drawn from
data, every run — where arm P had leaned no eight times in eleven; whether yes is right is the
golden set's question, and it now has one answer to grade. Built on the `t2-8` branch rather than
`main`, because the probe mechanism is T2.8's and T2.8 sits at Review with `merge` said: the run's
own preflight found the guard refusing that merge because the board's Status column is a `select`
the run reads as a `status`, and filed the fix at Backlog. $0.73 over eleven calls, none refused.

Sent back from Review on 2026-09-16 to be brought up to `main` and closed, the measurement standing.
T2.8 had landed on `main` the day before, so `origin/main` was merged into `t2-9` and pull request
#13 retargeted from `t2-8` to `main` by hand — `t2-8` was still on origin, so GitHub had not done
it. Two files conflicted. The build-log list was regenerated rather than merged. The product spec
had a real collision: T0.19 had taken v1.7 on `main` for the comment path, so main's line stayed
whole and this ticket's became v1.8 — the renumbering T0.12 and T0.13 did when `main` took their
number first, read against the addendum's "stays at v1.7" as the record staying rather than the
digit. The fresh-context review passed on the second model: the reviewer's pinned Fable was out of
credits, the case T0.22 is Ready to fix, and the Opus pass needed one nudge past its turn limit.
