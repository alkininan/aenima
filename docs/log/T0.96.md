# T0.96 — Smoke D: a package script and its test, on the schedule
_2026-09-13T13:16:31Z_

T0.96 — Smoke D: `pnpm board:ids`, the keys of `.claude/board.json` one per line, and a test
that runs the command over the committed file. The first ticket a scheduled run took end to end
in a Desktop worktree under guidelines v1.5: the worktree pruned and stamped at preflight, the
marker in the shared `.git`, a one-line body expanded at claim, a default taken on the board for
what the body did not say, three tests seen red under two deliberate skeletons before the
implementation, a cold review whose one real finding was the branch base — `AENIMA_RUN_BASE=HEAD`
still set so the run could use T0.10's own scripts — and the log entry written as this file.
