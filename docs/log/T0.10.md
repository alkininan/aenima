# T0.10 — Schedule: nobody types /ticket
_2026-09-13T13:38:22Z_

T0.10 — the schedule. A Desktop scheduled task runs `/ticket` hourly in a worktree of its own;
the run marker and the gate's green fingerprint moved into the repository's shared `.git`
directory so a run in any worktree sees a run in any other, and `prune.mjs` removes the
worktrees earlier runs left. The credential a run is handed, `aenima_pipeline`, reads and writes
every row and cannot change schema; the admin URL lives in `.env.migrate`, on no path a run
follows, and migration 0014 gives the db tests their users through two definer functions. Each
ticket's log entry is its own file under `docs/log/` and the Tickets done list is generated from
the directory. Guidelines v1.5, build-guide v2.4. Two idle runs on an empty queue cost about
two minutes each; the fixture, T0.96, went from Ready to Review with a pull request on the 4:08
tick with nobody at the keyboard.
