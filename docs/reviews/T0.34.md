# T0.34 — review

Commit reviewed: `bd5f20f` (pass 3)

Read cold against `docs/tickets/T0.34.md` and the sections its Spec line cites at the versions the
repo holds — guidelines §4, §5, §6 at v1.20 on `origin/main` (the ticket's own note says build
against the repo), build-guide §2 at v2.7, product-spec §12 at v1.8.

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: nine files — `scripts/run/claims.mjs` and its test,
  `docs/guidelines.md`, `.claude/skills/ticket/SKILL.md`, `docs/build-guide.md`,
  `scripts/run/README.md`, `scripts/run/next-id.test.mjs`, the ticket pack, the earlier passes'
  verdict file. `git status --porcelain` clean. No hook, guard, gate or detector touched, no
  migration, `.claude/settings.json` untouched, `CLAUDE.md` untouched — so this diff would merge
  itself on a PASS.
- `node scripts/run/review-scope.mjs docs/tickets/T0.34.md` → `scripts/run/claims.test.mjs`,
  `scripts/run/next-id.test.mjs`, nothing `missing`. `pnpm vitest run` on both: 2 files, 61 tests,
  all green, 634 ms (claims.test.mjs alone: 34).
- Criteria ↔ Tests, both directions. AC1 → TC1 (`claims.test.mjs:17-21` over the fixture,
  `:192-197` over a real git reference); AC2 → TC2 (`:289`), AC3 → TC3 (`:297`), AC4 → TC4
  (`:305`), AC5 → TC5 (`:312`), AC6 → TC6 (`:323`), AC7 → TC7 (`:334`). Every `section()` anchor
  the greps slice on exists — `## 3. Rules that keep this from going wrong`, `## 7. Names`,
  `3  Branch`, `9  Close`, `## 3 Branch`, `## 9 Close` — so no slice silently runs to the end of
  its document. No criterion without a test; the rewritten `next-id.test.mjs:264-273` names T0.28
  in its own comment and belongs to T0.28's criteria.
- AC1 against real input, not the fixture: `claims.mjs` over all 36 files in `docs/tickets/`
  returns 42 `absent` entries, 33 of which are names that first appear below the file's
  `## Cited` line — finding 1.
- The version arithmetic, against the repo rather than the header. `git show <branch>:docs/…`
  over every remote branch: `t0-26` and `t0-30` both carry guidelines `v1.21`, and `t0-26` carries
  build-guide `v2.8`. So two branches at one number claim `v1.21` and a `v1.22`, and `v1.23` and
  `v2.9` are the next free numbers. Pass 2's Should 1 is settled, and so is its Should 2 —
  `claims.test.mjs:339` now splits on `/^\s{5}v\d+\.\d+ · /m`, which cuts on an entry's opening
  and on nothing else.
- Pass 2's Must is half settled: `ownSections` now keeps a section below `## Cited`, and
  `docs/tickets/T0.12.md`'s and `T0.19.md`'s addenda are read. What the same change introduced is
  finding 1.
- `scripts/run/readme.test.mjs`, which governs a file this diff edits: 3 tests, green.

`docs/reports/T0.34.md` and `docs/log/T0.34.md` do not exist. Under `docs/guidelines.md` §5 the
report is step 8 and this review is step 5, so their absence is the order of the run and not a
finding; `report-check.mjs` owns the refusal. It does mean nothing on file records a red-first
mutation for any test in this diff — see Should 3.

---

## Findings

**1. Must — the Cited section is cut only as far as the first `## ` heading inside the quotation,
so on 21 of the 34 ticket files on main the quoted spec is read as the ticket's own claims.**

`scripts/run/claims.mjs:78`, in `ownSections`:

    if (/^##\s/.test(lines[i])) {
      after = i;
      break;
    }

The scan ends the Cited section at the next `## ` heading outside a fence. But a ticket's cited
text is pasted as markdown, and most of it on main is pasted *unfenced*, so the first `## ` the
scan meets is a heading of the quotation itself. `docs/tickets/T0.12.md:65` is `## Cited`;
`:72` is `## 2. Databases`, the first line of the quoted guidelines §2 — and `ownSections` drops
seven lines of 471, keeping the whole 400-line quotation as the ticket's words. Measured over the
34 ticket files that carry a Cited section, 21 leak more than three lines of it and most leak
nearly all of it: T0.19 612 lines of 616, T0.21 730 of 733, T0.24 406 of 409, and T0.33 — the
newest ticket on main — 14 of 18.

The consequence is the one the module's own comment (`claims.mjs:47-51`) says the exclusion exists
to prevent. Of the 42 `absent` entries `claims.mjs` reports across `docs/tickets/*.md`, 33 first
appear below the `## Cited` line: `.claude/settings.local.json` on eleven tickets and
`drizzle/0013_….sql` on six, both out of quoted guidelines §4/§5; eleven `run/*.test.mjs` and
`hooks/*.test.mjs` on T0.21 out of a quoted T0.16 report; `plan.md`, `spec.md`, `tasks.md`,
`docs/events.md` and `docs/design-api.md` on T0.33 out of quoted product-spec §9. At pass 2's
commit the same sweep returned 9. Four fifths of what step 2 would put in front of the skill is
another document's claim, and the run is asked to judge each one as "creates or drift".

It is also a claim three documents in this diff make and the code does not keep:
`docs/guidelines.md:485` "the Cited section / left to the document it quotes",
`.claude/skills/ticket/SKILL.md:303-304` "the `## Cited` section is left to the document it
quotes", `scripts/run/README.md:134` the same sentence. AC5 asks the skill's step 2 to match the
guidelines; both match each other and neither matches the script.

TC1 covers a heading *inside a fence* (`claims.test.mjs:67`) and nothing covers a heading in
unfenced quoted text, which is the shape 21 of the 34 files on main hold — so AC1 is green on a
fixture whose Cited section is fenced and untested on the input step 2 actually meets.

Smallest correction: end the cut at the one heading a ticket can carry after Cited rather than at
any heading — `claims.mjs:78` to `/^##\s+Addendum\b/` — which keeps pass 2's fix (T0.12's
`## Addendum` at `:415` and T0.19's at `:253` are still found) and stops the quotation ending its
own section. Add a TC1 case for it: a fixture whose Cited section quotes an unfenced `## ` heading
and names an absent path under it, followed by `## Addendum` naming a different absent path —
the quoted one out of `absent`, the addendum's in.

---

## Shoulds

These are recorded and do not block.

1. **Step 8's new instruction still has nowhere to put a rule about the run scripts.** No file
   under `.claude/rules/` carries a `paths:` glob reaching `scripts/run/**`: the seven cover
   `src/lib/ai`, `src/db`, `drizzle`, `src/db/schema`, `src/packs`, `src/lib/scoring`, the
   surfaces, and — in `tests.md:4` — `scripts/**/*.test.{mjs,mts,ts}` and nothing else. A ticket
   that learns something about the harness, which is most of phase 0 and is this ticket, finds the
   destination `docs/guidelines.md:531-538` names does not exist for it. Unchanged from passes 1
   and 2; a `harness.md` area file, or one Backlog task, would close it.

2. **The build log's Current state is stale against this diff and its test is red on it.**
   `docs/build-log.md:11` reads `guidelines v1.20`; `docs/guidelines.md:1` reads `v1.23`.
   Measured: `pnpm vitest run scripts/run/log-index.test.mjs` fails 2 of 29 at this commit. Step
   8's `node scripts/run/log-index.mjs` regenerates the block and the Stop gate is the backstop,
   so this is the order of the run rather than an unmet criterion — named again, unchanged from
   passes 1 and 2, so the run does not reach step 9 without it.

3. **Nothing on file yet records the red-first evidence.** What is owed at step 8 is a row per
   test — the mutation that reddened it and the count that went green — for all 34 tests in
   `claims.test.mjs`, and a line saying why `next-id.test.mjs:264-273`'s assertion changed from
   `toBe("1.20")` to T0.28's line plus a floor, since no TC of this ticket covers it. Read on its
   own terms that rewrite is sound: it asserts more than it did about what T0.28 claimed and less
   only about a number no criterion of T0.28 owns, which is `.claude/rules/tests.md`'s standing
   "pin the rule, not the pair of numbers that currently satisfy it".

FINDINGS
