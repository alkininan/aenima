# T0.34 — review

Commit reviewed: `b1c8f07` (pass 6 — the addendum round's third cold read, after pass 5's two Musts at `4a1cfed`)

Read cold against `docs/tickets/T0.34.md` — Build 1–7, the five Rules, AC1–AC7, TC1–TC7 and the
2026-09-23 Addendum — and the sections its Spec line cites, at the versions the repo holds:
guidelines §4, §5, §6 at v1.26 on `origin/main` (the ticket says build against the repo),
build-guide §2 at v2.7, product-spec §12 at v1.8.

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: twelve files, 1704 insertions, 11 deletions.
  Merge-base is `c989a55`, which is `origin/main` itself, so the three-dot diff is exactly this
  branch's work. `90432e4` is a merge commit whose second parent is `c989a55` — main taken in by
  merge, as the Addendum asks. No hook, guard, gate, `.claude/settings.json`, `CLAUDE.md` or
  migration touched. `scripts/run/next-id.test.mjs` is not in the diff — main's copy kept, as the
  Addendum asks — and it passes.
- `b1c8f07` itself: three files. `docs/log/T0.34.md:13-16` gained the argument-in-an-array
  sentence, so `harness.md:16`'s citation now holds — pass 5's Must 1 is settled. `claims.mjs:50-52`
  now gives `.claude/settings.local.json` as the example of what checking the Cited section would
  report: 17 ticket files on main quote that §5 text, the path is gitignored (`.gitignore:54`), and
  the sweep reports it absent on the two tickets that name it in their own sections — the example
  is true as written. `claims.mjs:114-120` says a leading-dot single-segment name is skipped and
  why — pass 5's Must 2 answered in the comment rather than the code; Should 2 below says how I
  read that.
- `node scripts/run/review-scope.mjs docs/tickets/T0.34.md` → `scripts/run/claims.test.mjs`,
  nothing `missing`. It did not list `next-id.test.mjs`, which the Addendum names without its
  path, so I ran both: `pnpm vitest run scripts/run/claims.test.mjs scripts/run/next-id.test.mjs`
  → 2 files, 62 passed (35 + 27), 629 ms.
- Criteria ↔ Tests, both directions: AC1 → TC1 (`ownSections` · `codeSpans` · `classify` ·
  `claims` on the fixture, `gitLook` on a real reference, headed "TC1 → AC1" in both halves);
  AC2–AC7 → TC2–TC7, one `it` each under the protocol `describe`, each commented with its AC.
  Every `section()` anchor exists (`docs/guidelines.md:497,506,543,555,758,776`;
  `SKILL.md:303,330,433,466`; `docs/build-guide.md:61,151`), so no slice runs silently to the end
  of a file.
- Red-first record: `docs/reports/T0.34.md:21-43` — every one of the 35 tests has a row, sixteen
  under the one import-failure row (Should 5), and one row for a file the diff no longer touches
  (Should 1).
- The sweep the report cites, `claims.mjs` over every `docs/tickets/*.md` against `origin/main`
  (39 files on main plus this ticket's): ten absent entries — `.claude/worktrees/` (gitignored,
  `.gitignore:57`), `.claude/settings.local.json` twice (gitignored), `drizzle/0016` (main has
  `drizzle/0016_opportunity_keys.sql`), `sample-juno-feature.md` three times (untracked), and the
  three files this ticket creates. Every one is a true statement about `origin/main`; nothing
  present is reported absent. AC1's "none that it has" holds on the corpus.
- `claims.mjs` over `docs/tickets/T0.34.md`: absent `scripts/run/claims.mjs`, `claims.mjs`,
  `docs/reports/T0.34.md` — the three this ticket creates; present `CLAUDE.md`, `.claude/rules/`;
  `origin/main`, `git grep`, `/ticket`, the placeholders and the Done command skipped.
- `node scripts/rules.mjs check` over the area files with `harness.md` in place: exit 0, no
  problems. Both globs reach tracked files; lines 13, 15 and 16 cite `docs/log/T0.34.md`, which
  exists and now carries all three rules (`log:11-12`, `log:13-16`, `log:16-18`); line 14's quote
  is in `docs/build-log.md`. The skill's sentence at `SKILL.md:463` — `scripts/rules.mjs` refuses
  a line with no source — is `rules.mjs:119`.
- Version arithmetic, every remote branch read: `origin/main` guidelines v1.26, `t0-26` v1.25,
  `t3-2` v1.20, nothing at v1.27; this branch v1.27 with one new header entry naming §5 step 2,
  step 8 and §6 (`docs/guidelines.md:1-7`) — the next free number, as the Addendum asks.
  Build-guide: main v2.7, `t0-26` v2.8, this branch v2.9, the title line brought to match.
  `docs/build-log.md:11` reads `guidelines v1.27`.
- AC2–AC6 read in the repo, not the ticket's inlined v1.17: §5 step 2 (`guidelines.md:497-505`)
  runs `claims.mjs` and sends an absent name through §4; step 8 (`:549-553`) files an area rule
  as one line ending in `docs/log/<id>.md` and sends a rule for everywhere to open questions,
  never into `CLAUDE.md`; §6 (`:766-770`) adds "and is not already standing" and keeps *Rules*
  the section apart from `.claude/rules/` the directory, as the ticket's fifth Rule asks; the
  skill's steps 2 (`SKILL.md:316-329`) and 8 (`:457-465`) say the same clause for clause;
  build-guide §2 (`build-guide.md:132-139`) names `.claude/rules/<area>.md` for one area of the
  code and `CLAUDE.md`, which a run never edits, for everywhere.

---

## Findings

No Must stands. The diff does what the ticket says: the script finds names and calls nothing an
error, reads `origin/main` and never the worktree, hands every name to git as an argument, and the
five documents say when it runs and where a rule goes.

## Shoulds

Recorded; none blocks. Numbered by how much a PASS makes them matter.

1. **The report and the log entry say things that stopped being true at `90432e4`, and a PASS
   makes two of them false on main** (pass 4's Should 2 and pass 5's Should 2, not taken).
   `docs/reports/T0.34.md:49-51`: the passes table ends at pass 3; passes 4 (`90432e4`), 5
   (`4a1cfed`) and this one have no row, and §5 step 8 refuses a report "without ... one table
   row a pass". `:103-107`: "no review pass has read the fix ... the ticket waits at Review for a
   human's word rather than merging on the run's" — passes 4, 5 and 6 have read it, and on this
   verdict the ticket merges itself. `:96`: "the repo is at v1.20" — `origin/main` is v1.26.
   `:35`: a red-first row for `scripts/run/next-id.test.mjs`, which this diff does not touch.
   `:62` and `:88`: "36 files" — main holds 39; `:89`: "four gitignored paths" — the sweep shows
   three. `docs/log/T0.34.md:31-33`: "no cold reading has seen it. The verdict on file says
   `FINDINGS` and the ticket waits at Review" — the first half untrue since pass 4, the second
   untrue on this file. At close, before the report is mirrored and the branch pushed: rows for
   passes 4, 5 and 6 with their models; open question 1 closed; `:96`, `:62`, `:88-89` restated;
   `:35` dropped; `log:31-33` rewritten to what happened — the fix was read by three later passes
   and the ticket merged on the sixth's PASS.

2. **A root dotfile is skipped under a reason that is false of it, and the skip is said in the
   code but not in the report.** I read this differently from pass 5, which tagged it Must: the
   ticket says "path-like tokens" and leaves the definition to the script, `claims.mjs:114-120`
   now writes the choice down with its reason, the miss is in the conservative direction, and
   reporting `.env.local` as absent on every ticket that names it would be the noise the Cited
   exclusion exists to prevent — so a Should, not a Must. Two things still stand. First,
   `claims.mjs:147`:

       return skip("not a path or an identifier");

   is the `why` every dotfile carries in the JSON, and `.gitignore` is a path. Measured over the
   corpus: ten of the forty ticket files name one in their own sections — `.env.migrate` ×5,
   `.env.local` ×4, `.worktreeinclude` ×3, `.gitignore` ×2, `.env.example` ×2, `.next`, `.git`,
   `.gate-count` — all under that reason, which the skill reading `skipped` cannot tell from
   `/ticket`'s. Second, the choice is inconsistent with itself on the same fact: a gitignored path
   with a slash (`.claude/settings.local.json`, `.claude/worktrees/`) is reported absent, a
   gitignored path without one (`.env.local`) is skipped. Smallest correction: a named skip before
   `:138` — `if (segments.length === 1 && name.startsWith(".")) return skip("a dotfile, not looked
   up")` — so the reason says what the code does, and one line in the report's open questions
   naming the choice, beside question 2 which already defends its direction. Or the rule pass 5
   proposed, with one `classify` case in TC1.

3. **`claims.mjs` exits 0 on an unreadable reference** (pass 5's Should 1, not taken).
   `scripts/run/claims.mjs:237` emits `{ error: "cannot read <ref>" }` and sets nothing; observed
   `node scripts/run/claims.mjs docs/tickets/T0.34.md no-such-ref` → exit 0. The usage branch at
   `:234` sets `process.exitCode = 1`. Set it whenever the result carries `error`.

4. **`harness.md:14` is not a rule this ticket established** (pass 4's Should 1 and pass 5's
   Should 3, not taken). Its source is T2.3's fold lesson in the build log; §5 step 8 as this diff
   writes it files "a rule the ticket established ... ending in docs/log/<id>.md", and
   `docs/reports/T0.34.md:129` says the file "is this ticket using its own step 8". Say in the
   report that one line is a standing lesson filed alongside the ticket's own, or leave it to the
   phase-0 sweep open question 4 names.

5. **One red-first row stands for sixteen tests** (pass 5's Should 4). `docs/reports/T0.34.md:23`:
   "`claims.mjs` absent, so the import threw". A missing module reddens every test at once and
   tells nothing about any one of them — the fence skip, the doubled backtick, each `classify`
   kind, the dedupe. `.claude/rules/tests.md` asks for the named assertion and only that
   assertion. T0.16's report set the precedent, so recorded and not blocking.

6. **A ticket whose quoted sections are not headed `## Cited` is read whole** (pass 5's Should 5).
   `claims.mjs:23` matches `## Cited` exactly; `docs/tickets/T0.29.md` quotes the design spec under
   its own headings and has no such line, so its quoted text is its own claims. Nothing absent
   today; the shape to know about.

PASS
