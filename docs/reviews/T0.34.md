# T0.34 — review

Commit reviewed: `90432e4` (pass 4, the addendum round; the first cold read of the fix made after pass 3's cap)

Read cold against `docs/tickets/T0.34.md` — its seven Build steps, five Rules, AC1–AC7, TC1–TC7 and
the 2026-09-23 Addendum — and the sections its Spec line cites at the versions the repo holds:
guidelines §4, §5, §6 at v1.26 on `origin/main`, build-guide §2 at v2.7, product-spec §12 at v1.8.

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: twelve files. Merge base is `origin/main` (`c989a55`),
  so main is taken in as the Addendum asks. No hook, guard, gate or detector touched, no migration,
  `.claude/settings.json` and `CLAUDE.md` untouched.
- `node scripts/run/review-scope.mjs docs/tickets/T0.34.md` → `scripts/run/claims.test.mjs`, nothing
  `missing`. `pnpm vitest run scripts/run/claims.test.mjs`: 35 passed, 672 ms.
- Criteria ↔ Tests, both directions. AC1 → TC1 (`claims.test.mjs:17-21` over the fixture, `:222-227`
  over a real git reference); AC2 → TC2 (`:320`), AC3 → TC3 (`:328`), AC4 → TC4 (`:336`), AC5 → TC5
  (`:343`), AC6 → TC6 (`:354`), AC7 → TC7 (`:365`). No criterion without a test, no test without a
  criterion. Every `section()` anchor exists.
- Red-first record: `docs/reports/T0.34.md:21-43` carries a row for every one of the 35 tests —
  sixteen of them under one row (Should 3) — and one row for a file the diff no longer touches
  (Should 2).
- Pass 3's Must, fixed after the cap and unread until now: `claims.mjs:65-75` resumes the ticket only
  at `## Addendum`. `claims.mjs` over all 40 files in `docs/tickets/` returns ten absent names, every
  one true of `origin/main`: `.claude/worktrees/`, `.claude/settings.local.json` ×2 (gitignored),
  `drizzle/0016` (a migration named without its number), `sample-juno-feature.md` ×3 (untracked),
  and this ticket's `scripts/run/claims.mjs`, `claims.mjs`, `docs/reports/T0.34.md`. Over
  `docs/tickets/T0.34.md` itself: those three absent, `CLAUDE.md` and `.claude/rules/` present, and
  `docs/tickets/<id>.md`, `origin/main`, `git grep`, `.claude/rules/<area>.md`, `docs/log/<id>.md`,
  `/ticket`, the Done command skipped with the right reason each. No citable document on
  `origin/main` carries a `## Addendum` heading line, so the resume cannot fire inside a quotation.
  TC1's `:70-92` and `:96-118` cover the unfenced and fenced shapes. The fix holds.
- The version arithmetic, against the branches rather than the header: `origin/main` guidelines
  v1.26, `origin/t0-26` v1.25, no other branch above v1.23; the branch takes v1.27, and its header
  says so (`docs/guidelines.md:1-7`). Build-guide: main v2.7, `t0-26` v2.8, branch v2.9. The v1.13
  line (`:50-53`) records a renumbering the same way, as the header claims. `docs/build-log.md:11`
  reads `guidelines v1.27` and the Tickets done list holds T0.34 between T0.31 (06:58Z) and T0.35
  (07:33Z) by its 07:12Z stamp — premerge's regeneration, as the Addendum says.
- `scripts/run/next-id.test.mjs` is byte-identical to `origin/main`'s; its assertion is
  `minor >= 20` (`:268-274`), which v1.27 satisfies. Main's rule kept, as the Addendum asks.
- `scripts/rules.mjs`, which the skill's step 8 now names, exists on `origin/main` and its `check`
  (`:145-191`) accepts `harness.md`: both globs reach tracked files, every line ends in a
  `docs/log/` path that exists or a quote `docs/build-log.md:448-449` carries. It checks that a
  cited log *exists*, not that it says the rule — which is Must 1.

---

## Findings

**1. Must — `harness.md` files a rule under a source that does not hold it.**

`.claude/rules/harness.md:16`:

    - Every run script exports its logic and guards its CLI with `isMain`, because a test that imports the module would otherwise run the command and block on stdin. — `docs/log/T0.9.md`

`docs/log/T0.9.md` records the guard's parser, the marker, stale recovery, §4's stop rule, the
reviewer's scope, the red-first refusal and the sign-in clock — and not this. `git grep isMain
origin/main -- docs/log/` returns nothing: no log entry on main records the `isMain` shape.
`scripts/run/cli.mjs:7-10` attributes it to "T0.7's gate hook ... and its cold review", and
`docs/log/T0.7.md` does not carry it either. `rules.mjs:181` checks only that the cited path
exists, so `scripts/rules.test.mjs` stays green on a citation that sends the next session to a
document that does not say the thing.

This is a file this diff adds, and it loads into every session that opens `scripts/**/*.mjs` or
the skill. The ticket's Build 3 files "a rule the ticket established"; `isMain` is not T0.34's.
The skill text this same diff writes (`.claude/skills/ticket/SKILL.md`, step 8) says a line with
no source is refused "and an invented one is worse than an absent one" — a source that exists and
does not hold the rule is the invented kind the checker cannot see.

Smallest correction: remove the line. The report's open question 4 already says the phase-0
harness sweep is a job to do deliberately, and this line is that sweep done in passing with a
wrong citation. If it stays, it ends in a source that holds it — and no `docs/log/` entry does
today, so "stays" means writing one, which is the sweep.

---

## Shoulds

These are recorded and do not block.

1. **Two of `harness.md`'s five lines are not rules this ticket established.** `:14` (the corpus
   measurement, sourced to T2.3's fold lesson in the build log) and `:16` (Must 1). §5 step 8 as
   this diff writes it (`docs/guidelines.md`, step 8: "a rule the ticket established ... ending in
   docs/log/<id>.md") and the report's open question 4 both say the file is for T0.34's own rules
   and the rest is a deliberate sweep. `:14` is a true line with a source `rules.mjs` accepts, so
   it is a scope note rather than a defect; if it stays, say in the report that the file carries
   two standing lessons besides the ticket's three.

2. **`docs/reports/T0.34.md` predates the addendum round and says three things that are no
   longer so.** `:35` records a mutation of `scripts/run/next-id.test.mjs`, which the diff no
   longer touches — main's copy is kept, as the Addendum asked, so the row describes a test that
   is not in this diff. `:96-99` says the repo is at v1.20, "three versions on"; `origin/main` is
   at v1.26 and this round took v1.21–v1.26 in (T0.30, T0.33, T0.35, T0.36). `:103-107` open
   question 1 says the ticket waits on pass 3's `FINDINGS` with no cold read of the fix; this
   pass is that read. Drop the row, restate the paragraph against v1.26, add this pass to the
   table at `:47-51`.

3. **One red-first row stands for sixteen tests.** `docs/reports/T0.34.md:23`: "`claims.mjs`
   absent, so the import threw · 1 file failed → 16 passed". A missing module reddens every
   test at once and says nothing about any one of them: the fence skip (`claims.test.mjs:126`),
   the doubled backtick (`:139`), each `classify` kind (`:145-187`) and the dedupe (`:216`) have
   no mutation of their own on record. `.claude/rules/tests.md`: "reintroduce the defect, watch
   the named assertion and only that assertion go red". `docs/reports/T0.16.md` set the
   precedent and the later rows carry real mutations, so this does not block.

4. **A ticket whose quoted sections are not headed `## Cited` is read whole.**
   `claims.mjs:24` matches `## Cited` exactly; `docs/tickets/T0.29.md` carries its design-spec
   quotations under `## design-spec §2 Color system`, `## 2. Color system` and so on, with no
   `## Cited` line, so 220 lines of quoted spec are its own claims. The sweep reports nothing
   absent for it today, and the ticket's own words are "the Cited section", so this is the shape
   to know about rather than a criterion unmet.

FINDINGS
