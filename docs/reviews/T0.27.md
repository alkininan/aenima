# T0.27 — review

Reviewed at `1254a9f` (branch `t0-27`), pass 1, diff read as `git diff origin/main...HEAD`. The
Spec line cites Claude Code's docs rather than a repo section; I read the three entries as the
ticket quotes them under Cited (fetched 2026-09-22) and guidelines §5 at v1.18 on `origin/main`
and v1.19 in the diff. `review-scope.mjs` lists one file, `missing` empty; it runs green here:
`scripts/run/effort.test.mjs`, 3 passed.

What holds. The diff is five files and does what the ticket's Build names and nothing else:
`effort: xhigh` added to `.claude/skills/ticket/SKILL.md` (AC1) and to `.claude/agents/reviewer.md`
(AC2) with `model: fable`, `maxTurns: 30` and `.claude/settings.json`'s `fallbackModel` untouched;
no `effortLevel` or `CLAUDE_CODE_EFFORT_LEVEL` in any settings file; guidelines §5 carries one
sentence naming the effort and the two files that set it, header bumped v1.18 → v1.19, and no
other file pins the version. Both frontmatter values are ones the cited model-config table lists
for Fable 5.1 and Opus 5, so `xhigh` holds on the pinned model and on the fallback alike. Criteria
↔ Tests map both ways: AC1 ↔ TC1 (one test), AC2 ↔ TC2 (two tests), each `it` tagged with its
criterion. The `frontmatter()` parser in the test reads flat `key: value` lines and handles
`disable-model-invocation: true` and the long `description:` line correctly.

## Findings

1. **Must — no red-first record on file.** `docs/reports/T0.27.md` does not exist at the reviewed
   commit, so the table under *Tests written* — per test, the mutation that made it red and the
   count that went green — is absent, and all three tests in `scripts/run/effort.test.mjs` are
   unverified as written-red-first. The ticket's Tests line (`docs/tickets/T0.27.md:47–49`, "each
   reddened by removing the field") is the plan, not the record; guidelines §5 step 4 says the
   mutation and the count are recorded per test, and T0.18's pass 1 raised the same absence as
   its Must 1. Fix: write `docs/reports/T0.27.md` with one row per test — "sets xhigh in the
   /ticket skill's frontmatter", "sets xhigh in the reviewer's frontmatter", "leaves the
   reviewer's model, fallback and turn cap as they were" — naming the mutation (the `effort`
   line removed from each file; for the third, `maxTurns` or `model` changed) and the failed/
   passed counts, then re-invoke.

2. **Should — the "nothing in settings" constraint has no test.** The ticket's Build
   (`docs/tickets/T0.27.md:30–31`) and the new guidelines sentence both say the effort is never
   set in the repository's settings, and the test's own comment repeats it
   (`scripts/run/effort.test.mjs:10–11`), but no assertion reads `.claude/settings.json` for
   `effortLevel`. A future session adding one would pass every test on file. Fix: in the third
   `it`, or a fourth, `expect(settings.effortLevel).toBeUndefined()` on the parsed settings the
   test already reads.

FINDINGS
