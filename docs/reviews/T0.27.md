# T0.27 — review

Reviewed at `6c6fe71` (branch `t0-27`), pass 2, diff read as `git diff origin/main...HEAD` against
`origin/main` at `1243eed`. The Spec line cites Claude Code's docs, which carry no version; I read
the three entries as the ticket quotes them under Cited (fetched 2026-09-22) and could not check
them against code.claude.com from this checkout — the ticket says as much. The quotes are
consistent with each other: both frontmatter references carry the same `effort` field with the
same five options, and the model-config table lists `xhigh` for Fable 5.1 (the reviewer's pinned
model) and Opus 5 (its fallback), so the level holds on every model `readChain` reads. Guidelines
§5 read at v1.18 on `origin/main` and v1.19 in the diff. `review-scope.mjs` lists one file,
`missing` empty; run here: `scripts/run/effort.test.mjs`, 4 passed.

What holds. The diff is seven files and does what the Build names and nothing else: `effort: xhigh`
added to `.claude/skills/ticket/SKILL.md`'s frontmatter (AC1) and to `.claude/agents/reviewer.md`'s
(AC2), with `model: fable`, `maxTurns: 30` and `.claude/settings.json` untouched — the settings
file is not in the diff at all, and no hook, guard or run script is. Guidelines §5's opening
paragraph carries one sentence naming the effort and the two frontmatters that set it; the header
bumps v1.18 → v1.19 and keeps the running history the header has always kept; nothing else in
`scripts/` or `docs/` pins v1.18. Criteria ↔ Tests map both ways: AC1 ↔ TC1, AC2 ↔ TC2, every `it`
tagged. Pass 1's Must is settled: `docs/reports/T0.27.md` holds one row per test with the mutation
and the failed/passed counts, and I checked each mutation against the assertions it would trip
(M1 → test 1 only, M2/M3 → test 2 only, M4 → test 3 only, M5–M7 → test 4 only), so the counts are
the ones those mutations produce. Pass 1's Should is settled by the fourth test, reddened by M2 and
M3. `scripts/run/readme.test.mjs` exempts `.test.mjs` files, so the new file needs no README line.

## Findings

1. **Should — one sentence in the report contradicts its own table.** `docs/reports/T0.27.md:32`
   says "Every count in the table is for the file at four tests", but the `before:` counts in
   rows 1 and 3 (`:36`, `:38` — "2 failed / 1 passed at three tests", "1 failed / 2 passed") and
   the M5–M7 counts in row 4 (`:39` — "each 1 failed / 2 passed → 3 passed at three tests") are
   explicitly at three. The record itself is complete; the summary of it is stale. Fix: replace
   the sentence with "M1–M4 were run at four tests; the `before:` counts and M5–M7 are at three,
   and the table says which."

2. **Should — the fourth test names a criterion that does not cover it.** I am unsure this merits
   a finding. `scripts/run/effort.test.mjs:35` tags "sets no effort in the repository's settings"
   as `T0.27 TC1 → AC1`, but AC1 (`docs/tickets/T0.27.md:41`) claims the skill's frontmatter sets
   `xhigh`, and this test asserts the settings file sets nothing — the Build's constraint at
   `docs/tickets/T0.27.md:30–31`, which the ticket's Tests line (`:47–49`) does not name. A
   reader retiring AC1's tests would take a settings test with them. Fix: point the comment at
   the Build line — `// T0.27 Build "nothing in settings" — the frontmatter is the only place` —
   rather than at a criterion, or name the test in the ticket's Tests section.

Not a finding, so the run does not skip it: `docs/log/T0.27.md` and the build log's regenerated
list are not in the diff. They are step 8's, and T0.18 committed them after its review pass in the
same way; `log-index.mjs`'s test refuses a stale list, so step 8 must still write both before close.

PASS
