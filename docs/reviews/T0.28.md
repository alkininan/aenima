# T0.28 — review

Commit reviewed: `7177853`

Read cold against `docs/tickets/T0.28.md` and guidelines v1.19 §5 step 1 and §7. The ticket's
*Cited* §5 and §7 are byte-identical to `docs/guidelines.md` at `origin/main` (the only
differences are the ticket's own blank line and `---` between sections), so the tests are not
reading a document the branch edited to fit them.

What I checked myself rather than taking from the branch's summary:

- `node scripts/run/review-scope.mjs docs/tickets/T0.28.md` lists one file,
  `scripts/run/next-id.test.mjs`, and nothing `missing`. `pnpm vitest run
  scripts/run/next-id.test.mjs`: **1 file, 21 tests, all passing.**
- The CLI end to end in this checkout: `echo '{"epic":"E0.2 Pipeline","tasks":[]}' | node
  scripts/run/next-id.mjs` answers `T0.26` — not `T0.1` — so `repoTaken()` really is wired into
  `main()` and really reads this repository. `docs/` carries T0.1–T0.14, T0.16–T0.25, T0.27,
  T0.28, T0.40, T0.96–T0.99; `origin/t0-15` and `t0-15` carry the one number `docs/` does not.
  T0.26 is free of both, which is the right answer to an empty board list.
- The four criteria against the four test cases. AC1: `nextId("E0.4 …", BOARD)` → `T0.28` over a
  fixture holding T0.1–T0.27, T0.40 and T0.96. AC2: `nextId("E0.1 Foundation", BOARD)` → `T0.28`,
  not `T0.7`. AC3: the `takenIds` path (`BOARD + ["T0.28"]` → `T0.29`, `+ ["T0.28","T0.29"]` →
  `T0.30`) and the `repoTaken` block, which builds a real temp tree with `docs/log/T0.28.md` and a
  stubbed `git branch` listing. AC4: `§7` contains the three substrings and `parseHeaderVersion`
  reads `1.20`. Every criterion has a test; every test carries a `TC → AC` marker (see Should 3
  for two that carry the wrong one).
- The parsers on the inputs this repository actually holds: `origin/HEAD -> origin/main`, the bare
  `origin` ref and the 48 `claude/<name>` worktree branches all read as no ID; `t0-8-close` and
  `t0-97-stale-1607` keep their numbers; `docs/log/T0.97.md` is on main, so §7's and the
  docstring's example is true of this checkout.
- No other test reads the changed prose. `version-drift.test.mjs` and `mirror.test.mjs` assert
  guidelines versions against their own fixtures (v1.2, v1.7), `mirror.test.mjs:460` matches §2's
  Documents block, and `effort.test.mjs` reads the skill's frontmatter — none touches §5 step 1,
  §7 or the claim bullet. `next-id.test.mjs` is the only file pinned to the real v1.20.
- The three prose surfaces agree with the code and with each other, and nothing else in the
  repository still states the per-epic rule: `grep` finds `next-id` named only in
  `.claude/skills/ticket/SKILL.md:262`, `docs/guidelines.md:3,469`, `scripts/run/README.md:116`
  and the test, all updated, plus historical reports which are records and not rules.

## Finding

1. **Must — no test on this branch is recorded as observed failing first.** `docs/reports/T0.28.md`
   does not exist; `docs/tickets/T0.28.md:56` still reads `_Written at step 8 into
   `docs/reports/T0.28.md`._`, and there is no red-first record anywhere else on the branch —
   not in the ticket, not in `docs/log/` (which has no `T0.28.md` either). Guidelines §5 step 4
   asks for "new logic has tests observed failing first, the mutation and the count recorded per
   test", and this diff adds or rewrites seventeen tests: six under `nextId`, three under
   `branchIds`, three under `docsIds`, three under `repoTaken` and two under
   `docs/guidelines.md`. Twenty-one green tests are evidence about the suite, not about the
   tests: Should 1 below is a test that passes and cannot fail for the reason its name gives,
   which is exactly what a red-first row catches. *Smallest correction:* write
   `docs/reports/T0.28.md` with the *Tests written* table — one row per test, the mutation that
   made it red and the count that went green — as T0.40's report does.

## Shoulds

1. **A rewritten test can no longer fail for the reason its name gives.**
   `scripts/run/next-id.test.mjs:64-66` is named "reads a two-digit number as one number, not as
   its first digit" and asserts `expect(nextId("E0.1 Foundation", ["T0.98 Smoke A"]).id).toBe("T0.1")`
   and `expect(nextId("E0.1 Foundation", ["T0.1 a", "T0.98 Smoke A"]).id).toBe("T0.2")`. Under the
   old highest-plus-one rule, reading `98` as `9` gave `T0.10` instead of `T0.99` and the test
   caught it. Under lowest-free it cannot: whether `T0.98` is read as 98, as 9 or as 8, the lowest
   free number is `T0.1` in the first line and `T0.2` in the second. The property survives only in
   `docsIds(["log/T0.40.md"])`. *Smallest correction:* assert the case that still discriminates —
   a phase whose numbers run up to the two-digit one, e.g. `nextId("E0.1 Foundation", [...1..9,
   "T0.98"]).id` is `T0.10` and not `T0.99` — or move the claim onto `idOf` in "the parsers under
   it" and rename this one for what it now shows.

2. **The one line that makes the feature real in production is not under test.**
   `scripts/run/next-id.mjs:131` is `emit(nextId(input.epic, input.tasks ?? [], repoTaken()));`.
   Delete `repoTaken()` from it and all 21 tests stay green — `nextId` and `repoTaken` are each
   covered, their composition is not, and the ticket's TC3 is written at the CLI's level ("with
   T0.28 carried only by `docs/log/T0.28.md`, `next-id` answers T0.29"). I checked the wiring by
   hand instead, above. `release.test.mjs:15` shows the house pattern for it — `spawnSync("node",
   [script, …])`. *Smallest correction:* one test that spawns the script in a temp cwd holding
   `docs/log/T0.28.md` and asserts `T0.29` on stdout.

3. **Two tests are tagged for a criterion they do not cover.** `scripts/run/next-id.test.mjs:57-58`
   marks "refuses to invent a phase when the epic name carries none" as `// TC1 → AC1`, and
   `:63-64` marks the two-digit test the same way. AC1 is "a claim in an epic with no numbered task
   answers the lowest free number in its phase"; neither test makes that claim, and both are
   pre-existing behaviour this ticket preserves rather than criteria it introduces. The mapping is
   the thing a later run reads to know what it may retire. *Smallest correction:* leave them
   unmarked, as "the parsers under it" is, or mark them as preserved behaviour.

4. **`repoTaken` narrows in silence, and narrow is the failure this ticket exists to stop.**
   `scripts/run/next-id.mjs:86-93`: a non-zero `git branch` drops every branch-carried number, and
   `catch { entries = []; }` drops every docs-carried one, both without a word in the emitted JSON.
   The docstring calls that "a narrower answer, never a wrong one", but §7 as this diff writes it
   says a number "is never used twice" and holding it includes "a branch or a file under `docs/`" —
   under a narrowed read the script can answer a number a `docs/` file still carries, which is the
   repeat the ticket is about, and the run cannot tell it happened. *Smallest correction:* carry
   the two reads' success in the result — `{ id, phase, n, branches: true, docs: true }` — so the
   report line can say the board alone was counted.

5. **Step 1 now needs a larger board read than any script gives it.**
   `.claude/skills/ticket/SKILL.md:262` asks for `next-id.mjs` "over **every** task name on the
   board, whatever its Epic and whatever its Status", and `docs/guidelines.md:468-470` states the
   same. Nothing under `scripts/run/` prints that list: `pick-next.mjs` reads every row over the
   token (`api.tasks(board.tasks_ds)`) but emits only `pick`, `queue`, `blocked`, `cycles` and
   `notices`. So the widest input the new rule depends on is a model's connector read, and a task
   it misses is held only by a branch or a docs file — which a Backlog or Ready task does not yet
   have. *Smallest correction:* have `pick-next.mjs` emit the names it has already read, and point
   the bullet at that field.

6. **§7 carries history the ticket asked to keep to a sentence.** `docs/guidelines.md:747-748`
   ends the new paragraph with "Counting within the epic is what answered T0.1, the scaffold
   ticket's, to E0.4's first task and T0.7, E0.2's Setup, to E0.1's next (T0.28)." The ticket's
   Build says "`docs/guidelines.md` §7 Names states the rule in one sentence", and the same account
   is already in `scripts/run/next-id.mjs:9-13` and belongs in `docs/log/T0.28.md`. §7's other
   entries are one line each. *Smallest correction:* keep the bold sentence and the rule under it,
   and let the log hold what went wrong before.

FINDINGS
