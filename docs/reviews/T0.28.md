# T0.28 — review

Commit reviewed: `0c8f15d` (pass 3)

Read cold against `docs/tickets/T0.28.md` and the guidelines v1.19 §5 step 1 and §7 its *Cited*
section carries.

What I checked myself rather than taking from the branch's report:

- Every hunk of `git diff origin/main...HEAD`: ten files, and every one of them is either the
  ticket's Build (`next-id.mjs`, `guidelines.md` §5 step 1 and §7, the header, `SKILL.md` step 1,
  `README.md` step 1), the plumbing that Build needs (`pick-next.mjs`'s `names`), or the ticket's
  own documents. Nothing in the diff is outside the ticket. `git status --porcelain` is clean, so
  the diff is the whole change.
- `node scripts/run/review-scope.mjs docs/tickets/T0.28.md` lists `scripts/run/next-id.test.mjs`
  and `scripts/run/pick-next.test.mjs`, nothing `missing`. `pnpm vitest run` over the two:
  **2 files, 51 tests, all passing** (27 and 24) — the report's "Twenty-seven" is this file's own
  count.
- Criteria against tests in both directions. AC1 → "answers the lowest free number in the phase
  for an epic holding none" (T0.28 over T0.1–T0.27, T0.40, T0.96), "fills a hole the board left",
  "starts at 1 in a phase with no numbered task anywhere". AC2 → "skips a number another epic in
  the phase holds" (T0.28, not T0.7) and the two `readPick` `names` tests, both now carrying the
  marker. AC3 → the `takenIds` arms, `branchIds` (3), `docsIds` (3), `repoTaken` (6), and the CLI
  tests against real `git init` checkouts. AC4 → the two `docs/guidelines.md` tests, §7's rule in
  one sentence and `parseHeaderVersion` at 1.20. No criterion is unmatched and no test is
  unclaimed; the pre-existing `phaseOf`/`idOf` pair carries no marker and needs none.
- Pass 2's Must 2 is fixed at the root, not papered over: `echo '{"epic":"E0.9 New","tasks":[]}' |
  node scripts/run/next-id.mjs` answers `T0.26` with `unread: []` from the checkout root **and**
  from `scripts/`.
- Pass 2's Must 1, the red row it corrected, I re-derived rather than trusted. Under `idOf`'s
  `(\d+)\b` → `(\d)` exactly seven of the 27 tests go red — the three `nextId` arms over `BOARD`,
  the two-digit arm, both `docsIds` two-digit arms and `repoTaken`'s "branches and the docs tree
  together" — which is the "7 failed / 20 passed → 27 passed" the report records. The 14/5 → 19
  row reconciles the same way against the shipped per-epic count. The record is an observation.
- The old rule is stated nowhere any more: no "highest … within that epic" survives in
  `guidelines.md`, `SKILL.md` or `scripts/run/README.md`, and `nextId` has no caller but its own
  test and `main()`.

## Shoulds

1. **The skill calls the input `names`; the CLI's key is `tasks`, and a missing one is silent.**
   `.claude/skills/ticket/SKILL.md:262` reads "`node scripts/run/next-id.mjs` with the Epic's name
   and `names` from the pick above", and no command shape follows it, where step 1's other scripts
   are shown as the line to type. `scripts/run/next-id.mjs:152` is
   `emit({ ...nextId(input.epic, input.tasks ?? [], ids), unread })`: hand it `{"epic":…,"names":…}`
   and it answers over the repository alone, with `unread` empty — here that is `T0.26`, a number
   the `docs/` tree does not carry, which is the silent narrow answer the ticket exists to stop.
   The script's own docstring maps the two names; the skill, which is what a run reads, does not.
   *Smallest correction:* show the line in the skill —
   `echo '{"epic":"<Epic>","tasks":<names>}' | node scripts/run/next-id.mjs` — or refuse in
   `main()` when `tasks` is absent rather than defaulting to `[]`.
2. **`repoTaken`'s docstring and the report claim an `unread` rule the code does not have.**
   `scripts/run/next-id.mjs:84` says "a root git could not name is unread whatever is there", and
   `docs/reports/T0.28.md:103` says the fix "puts `docs` in `unread` whatever is found". The push
   lives only in the catch (`scripts/run/next-id.mjs:108`), so a null root whose `<cwd>/docs`
   happens to read leaves `unread` empty. It never fires in practice — a root git cannot name is a
   root `git branch` cannot read either, so `unread` already carries `branches` — which is why this
   is a Should and not a Must. *Smallest correction:* either push `docs` once on `root === null`
   outside the try, or reword the docstring, the report line and the test name at
   `scripts/run/next-id.test.mjs:179` to what the code does: unread when the read itself failed.
3. **The report says two prose lines were re-wrapped; one was not touched and the other is still
   long.** `docs/reports/T0.28.md:109` — "The two prose lines appended to rather than re-wrapped
   are re-wrapped." `.claude/skills/ticket/SKILL.md:243` is still the 166-character line pass 2
   named, unchanged since pass 1, and `scripts/run/README.md:123` is 126 characters in a paragraph
   that otherwise sits at ~90. Prettier's `proseWrap` is `preserve`, so lint is green either way.
   *Smallest correction:* re-wrap both paragraphs, or say in the report which one was left.
4. **One red-first row records no reddening mutation where one exists.**
   `docs/reports/T0.28.md:35`, `nextId` "counts only the phase's own numbers", is filled with "the
   per-epic count answered T3.1 too; kept as the phase filter's own test". The test's assertion is
   new on this branch, so it is worth a mutation of its own: dropping `parsed.phase === phase`
   (`scripts/run/next-id.mjs:131`) makes it answer `T3.3`. *Smallest correction:* record that
   mutation and its count in the cell.
5. **`docs/log/T0.28.md` is not on the branch, though `docs/reports/T0.28.md` is.** §5 step 8 owes
   the log entry and the build log's list regenerated from the directory (`log-index.mjs`) beside
   the report. Step 8 runs after this review, so nothing is broken yet — this is a reminder that
   the report landing early does not carry the log with it.

PASS
