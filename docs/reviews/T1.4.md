# T1.4 — review

Reviewed commit `d0aa0cc` (branch `t1-4`), pass 2. I read `docs/tickets/T1.4.md` and every hunk of
`git diff main...HEAD` (26 files), `product-spec v1.6 §2` and `§13` and build-log open question 9 as
the Spec line cites them, `design-spec v2.17 §3`, `§4`, `§10` and `§12` for the frame the Build
section claims, and `docs/guidelines.md §5` for what steps 4, 6 and 8 owe.

`node scripts/run/review-scope.mjs docs/tickets/T1.4.md` lists seven files. Its `missing` array holds
the same five prose-and-`[key]` artefacts pass 1 read: `item-key.db.test.ts` and `item.test.ts` are
prose references to files that exist, and the three `/…dom.test.tsx` entries are the script reading
past the `[key]` segment. All seven real files ran:

    6 passed | 1 skipped (7 files) · 49 passed | 6 skipped (55 tests)

The skip is `src/db/opportunity-key.db.test.ts`, with the loud stderr banner naming
`drizzle/0015_opportunity_keys.sql` as unapplied — the arrangement the ticket's Tests section states.

Pass 1's four Musts are settled. `ItemsSection.tsx` and its dom test hold AC5's both arms;
`getOpportunityByKey` orders the embed (`src/db/queries/opportunity.ts:110`) and
`opportunity.test.ts:170` asserts `referencedTable`; `OpportunityHeader.tsx:41` carries `truncate`,
which is §4's subtitle rule and what `ItemHeader` already read; `docs/reports/T1.4.md` exists with a
red-first table. The migration is `drizzle/0005_item_keys.sql` one table over, statement for
statement, and `src/db/schema/tables.ts:188`/`:196` carry the same constraint names. Build-log open
question 9 is struck through in item 11's form and 40 records the skip pattern with an owner.

## Findings

1. **Must — a test in the repo now asserts the opposite of criterion 6.** `e2e/item.spec.ts:184`
   is `test("shows the opportunity as text, with no link to a page that does not exist")`, and its
   body reads:

       expect(await lineage.evaluate((node) => node.closest("a") !== null)).toBe(false);
       await expect(page.locator('main a[href^="/o/"]')).toHaveCount(0);

   Criterion 6 is "The item page's opportunity line is a link to `/o/<key>`". Those specs drive
   `/dev/item` (`e2e/item.spec.ts:6`, `src/app/dev/item/page.tsx:117`), which renders `ITEM_HEADER`
   — and this diff gave `ITEM_HEADER` an opportunity (`src/app/dev/item-fixture.ts:53`), so
   `ItemHeader.tsx:61` wraps that exact title in a `<Link href="/o/soc-2">` inside the page's
   `<main>` (`src/app/dev/item/page.tsx:110`). Both assertions are now false, at each of the three
   viewports the `for` loop runs. The test's own comment at `:178` — "`/o/<key>` is reserved and
   unbuildable — opportunities have no key column" — is the sentence this ticket exists to retire,
   and it survives in the tree beside the change that falsified it. AGENTS.md: "never preserve
   backward compatibility in code … remove the old path in the same change." I did not run
   `pnpm e2e` — §5 step 5 keeps this review to the vitest files — so this is read from the source
   rather than observed red; the reading is direct and I am confident of it.
   Smallest fix: rewrite that test to assert what now holds — the lineage title's `closest("a")`
   has `href` `/o/soc-2` — and rewrite its name and its `:174`–`:182` comment, which state a rule
   that no longer exists. If it is re-tested rather than deleted, say so in the report's test table.

2. **Must — the red-first record is per file, not per test, so most new tests are unverified.**
   `docs/guidelines.md §5` step 4 asks for "the mutation and the count recorded per test", and step
   8 refuses the report without "per test, the mutation that made it red and the count that went
   green". `docs/reports/T1.4.md:26`–`:35` records seven rows, each naming a file and a test count,
   and each row's own count shows a single test reddening: `src/db/queries/opportunity.test.ts` —
   "8 tests" · `1 failed / 7 passed → 8 passed`; `ItemHeader.dom.test.tsx` — "3 tests" ·
   `1 failed / 2 passed`; `OpportunityHeader.dom.test.tsx` — "4 tests" · `1 failed / 3 passed`;
   `ItemLine.dom.test.tsx` — "2 tests" · `1 failed / 1 passed`; `ItemsSection.dom.test.tsx` —
   "3 tests" · `1 failed / 2 passed`. Six mutations, six tests seen red, twenty-one new passing
   tests in the tree. Fifteen of them have no row that names them and no mutation that moved them,
   which is the thing the record exists to state — `docs/reports/T0.20.md` shows the house form,
   where every row names the test or tests it reddened. This is pass 1's finding 1 answered at the
   wrong grain rather than left undone.
   Smallest fix: give each of the fifteen a row — the test named, the mutation that reddened it,
   and the named file's own red → green count — or, where several fell to one mutation, name all of
   them on that mutation's row as T0.20's table does.

## Shoulds

3. **Should — the ticket's Tests section names five files where six now exist.**
   `docs/tickets/T1.4.md:71`–`:85` does not name `src/app/o/[key]/ItemsSection.dom.test.tsx`, which
   pass 1's finding 2 added and which is where criterion 5 is held. The report's AC table
   (`docs/reports/T1.4.md:18`) does map it to AC5 and AC7, so nobody reading the record can lose it;
   the pack the next reviewer opens is the copy that is short one line.

4. **Should — one test asserts a shape where it means an absence.**
   `src/app/o/[key]/OpportunityHeader.dom.test.tsx:38`–`:44` proves the missing summary by counting
   `<p>` elements: `expect(paragraphs).toHaveLength(1)`. It passes today for the right reason, but
   it would pass just as well if the product eyebrow became a `<span>` and the summary line stayed —
   and it would redden on a change that added a paragraph and touched nothing about summaries.
   `expect(screen.queryByText("Retention drops sharply between day 3 and day 7.")).toBeNull()`
   states the branch itself.

5. **Should (no action owed here) — criteria 1 and 2 are still unobserved, and the record says so.**
   `src/db/opportunity-key.db.test.ts` skipped in this run, as the ticket's Tests section says it
   will while `drizzle/0015` is unapplied; the report's row says the six have never been green, and
   build-log open question 40 carries the pattern with an owner and a decision point. That is §5
   step 6 working, not a gap to close in this pass — it is named here so the verdict does not read
   as though the trigger had been seen to work.

FINDINGS
