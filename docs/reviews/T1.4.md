# T1.4 — review

Reviewed commit `ce5a94f` (branch `t1-4`, the re-claim merge of `origin/main` on 2026-09-20),
pass 4, cold. I read `docs/tickets/T1.4.md`, its Cited block against the current `product-spec`
§2 and §13 and build-log open question 9, `design-spec` §3 and §4 for the header and the frame,
`drizzle/0005_item_keys.sql` statement by statement against `drizzle/0015`, `docs/guidelines.md`
§4 and §5, and every hunk of `git diff main...HEAD` (28 files). Seven of those files main also
changed since pass 3 — `docs/build-log.md`, `e2e/item.spec.ts`, `src/app/dev/item-fixture.ts`,
`src/app/i/[key]/page.tsx`, `src/db/queries/item.ts`, `src/db/queries/item.test.ts`,
`src/i18n/en.ts` — and only `build-log.md` conflicted; the ticket's hunks in each are intact
(`ITEM_PAGE_TREE` still embeds `opportunity(key, title)`, the old `opportunityTitle` path is
gone from `src/`, `e2e/` and the fixture).

`node scripts/run/review-scope.mjs docs/tickets/T1.4.md` lists eight files. Its `missing` array
holds two prose references (`item-key.db.test.ts`, `item.test.ts`) and four entries the script
truncated at the `[key]` segment; every one exists on disk. The seven vitest files ran:

    Test Files  6 passed | 1 skipped (7)
         Tests  53 passed | 6 skipped (59)

The six skips are `src/db/opportunity-key.db.test.ts`, with the stderr banner naming
`drizzle/0015_opportunity_keys.sql` as unapplied — observed, not taken from the report. The four
tests over pass 3's 49 are main's, in `item.test.ts`. `e2e/item.spec.ts` is Playwright's and was
not run here.

**Criteria ↔ tests.** All seven criteria carry a named test in the report's AC table, and every
new or changed vitest test is named by a row of the mutation table — checked both ways. AC1 and
AC2 are the exception in kind (Should 1): their row exists and says, honestly, "never green".

**The migration.** `drizzle/0015` is `0005` one table over: added nullable, backfilled with
`row_number()` over `(created_at, id)` per product, `SET NOT NULL`, `UNIQUE (workspace_id, key)`,
the same shape CHECK, `app.assign_opportunity_key()` with the same `search_path`, overwriting
`NEW.key` unconditionally on `BEFORE INSERT`. Constraint names agree between the SQL and
`src/db/schema/tables.ts`. `item`'s one FK to `opportunity` is the composite
`item_opportunity_fk`, so the reverse embed `item(...)` from `opportunity` is unambiguous — the
same path `opportunity(key, title)` already rides from `item`. `0001`'s grants are table-level,
so the new column is readable through PostgREST without a grant of its own. The trigger fires
before the CHECK, so the seed's `key: ""` never meets the constraint.

**The surfaces.** `/o/[key]/page.tsx` re-checks the session, 404s on no workspace and on no row
by one `notFound()` path, and never redirects on a key; `/o` is not in the proxy's
`PUBLIC_PREFIXES`, so the fixture's comment about `/dev` linking into a protected page is right.
Both `.order` calls on the embed name `referencedTable: "item"` and supabase-js appends the second
to the same `item.order` param. Every class the three new components name resolves to a token
in `globals.css`; every string reads from `t` or is the opportunity's own data.

## Shoulds

1. **ACs 1 and 2 are unobserved, and this verdict does not assert the trigger works.**
   `src/db/opportunity-key.db.test.ts:136` skips the whole suite behind
   `describe.skipIf(OFFLINE || !APPLIED)`, so the per-product counter, the no-gap numbering, the
   refusal of a client-supplied key and the shared key space have been read against `0005` and
   never run — the mutation table's row says "never green", which is the truth and is why this
   is not a Must. The ticket's Tests section asks for exactly this, `docs/guidelines.md` §5
   step 6 is why, and the diff is gated on `drizzle/` so it cannot merge itself on this PASS. The
   run that applies `0015` owes: this file to green, `src/db/database.types.ts` regenerated and
   diffed against the hand-written block at lines 543/553/563, and a reseed.

2. **`docs/reports/T1.4.md` "Changed since this ticket was cut" is now false.** It reads
   "nothing drifted — the repo is at v1.6"; the re-claim merge brought `docs/product-spec.md` to
   v1.8, and `node scripts/run/version-drift.mjs "product-spec v1.6 §2, §13"` answers
   `drifted: true`. The cited text has not moved — I compared the ticket's Cited block to the
   current §2 and §13 line for line; v1.7 patched §11 and v1.8 patched §4/§5 — so nothing in the
   build is wrong, but §5 step 1 asks the claim to note drift and commit `ce5a94f` noted none.
   Smallest correction: one sentence in that section — cited v1.6, repo v1.8, §2 and §13 unchanged
   between them.

3. **The report names no reviewer model for any pass.** `docs/guidelines.md` §5 step 8 (line
   466) now refuses a report "without the model of every reviewer pass and whether it was
   resumed, one table row a pass". That rule arrived with the merge — it is absent at `41047a7` —
   so the three passes were recorded under the older rule, and the run will add a row for this
   pass regardless. Smallest correction: the `pass · commit · model · resumed · verdict` table
   `docs/reports/T1.5.md:98` uses, with all four rows.

4. **`docs/schema.md`'s migration list stops at `0013`.** Lines 242–337 describe `0000` through
   `0013` one paragraph each, and line 262 names `item.key` under `0005`; `0015` adds a keyed
   column to a table the same file lists at line 156 and has no paragraph. `0014` is also absent,
   which is not this ticket's to fix but is why this is a Should and not a break in the pattern.
   Smallest correction: a three-line paragraph in `0005`'s form — the column, the trigger, the
   shared key space — after `0013`'s.

No Must stands. The seven criteria are built where the report says they are built, the old
plain-text lineage path is removed rather than kept beside the link, the two 404 paths are one,
the e2e assertion inverted with the thing it guarded, and the re-claim merge dropped nothing of
either side.

PASS
