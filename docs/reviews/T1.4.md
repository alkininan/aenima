# T1.4 — review

Reviewed commit `d12218e` (branch `t1-4`), pass 3, cold. I read `docs/tickets/T1.4.md` and every
hunk of `git diff main...HEAD` (27 files), `product-spec v1.6 §2` and `§13` and build-log open
question 9 as the Spec line cites them, `design-spec §3`, `§4` and `§8` for the frame the Build
section claims, `drizzle/0005_item_keys.sql` statement by statement against `drizzle/0015`, and
`docs/guidelines.md §4` for what a gated diff owes.

`node scripts/run/review-scope.mjs docs/tickets/T1.4.md` lists eight files. Its `missing` array
holds only prose references (`item-key.db.test.ts`, `item.test.ts`) and three entries the script
truncated at the `[key]` segment; all of them exist on disk, so nothing the ticket cites is absent
from the checkout. The seven vitest files ran:

    Test Files  6 passed | 1 skipped (7)
         Tests  49 passed | 6 skipped (55)

The six skips are `src/db/opportunity-key.db.test.ts`, with the stderr banner naming
`drizzle/0015_opportunity_keys.sql` as unapplied — the behaviour the ticket's Tests section asks
for, observed rather than taken on the report's word. The per-file totals match the report's
red→green counts exactly (9 · 3 · 4 · 2 · 3 · 28 = 49). `e2e/item.spec.ts` is Playwright's and was
not run here.

**Criteria ↔ tests.** All seven criteria carry a named test, and every new or changed vitest test
is named by at least one row of the report's mutation table — I checked both directions test by
test. AC1 and AC2 are the exception in kind, not in bookkeeping: their tests exist, are correct,
and skip until a human applies the migration (Should 1).

**The migration.** `drizzle/0015` is `0005` one table over: added nullable, backfilled with
`row_number()` over `(created_at, id)` per product, `SET NOT NULL`, `UNIQUE (workspace_id, key)`,
the same `^[a-z][a-z0-9]{1,7}-[0-9]+$` CHECK, and `app.assign_opportunity_key()` on `BEFORE INSERT`
overwriting `NEW.key` unconditionally. Constraint names agree between the SQL and
`src/db/schema/tables.ts`. The hand-written `opportunity.key` block in `src/db/database.types.ts`
is `item.key`'s shape character for character — required on Row and Insert, optional on Update.
No snapshot is owed: `drizzle/meta/` has carried only `0000` and `0001` since the hand-written
migrations began.

## Shoulds

1. **ACs 1 and 2 are unobserved, and this verdict does not assert the trigger works.**
   `src/db/opportunity-key.db.test.ts:136` skips the whole suite behind
   `describe.skipIf(OFFLINE || !APPLIED)`, so the per-product counter, the no-gap numbering and the
   refusal of a client-supplied key have been read against `0005` and never run. That is what the
   ticket asks for and what `docs/guidelines.md §4` requires of a migration — its word is `apply`,
   and the diff is gated on `drizzle/`, so it cannot merge itself on this PASS. The run that
   applies `0015` owes three things, all already written down in the report's open question 1:
   rerun this file to green, regenerate `src/db/database.types.ts` and diff it, and reseed.

2. **`src/app/o/[key]/ItemLine.tsx:37` — "so the taxonomy on the right stays selectable text" is
   not what the pattern does.** The stretched `after:absolute after:inset-0` on line 40 covers the
   whole 56h row, the taxonomy included; that text is not selectable and nothing in the component
   raises it above the overlay. `src/app/app/ItemRow.tsx:104-106` states the real reason for the same
   markup — the overflow menu and the chips "stay clickable in their own right rather than being
   swallowed by an outer anchor", i.e. no nested interactives. Smallest correction: take
   `ItemRow`'s sentence, since this row has neither a menu nor chips and the pattern is here for
   consistency alone.

3. **`src/db/queries/opportunity.ts:114` orders the embed on `created_at` alone, which is not a
   total order.** `drizzle/0015`'s own backfill tie-breaks with `ORDER BY o.created_at, o.id`
   because rows written by one statement share `now()`. Any future multi-row insert of items gives
   the list ties, and `OpportunityPageDetail.items`' promise at line 57 — "In creation order, which
   is the order they were bet on" — goes back to being something Postgres may reshuffle between
   two reads, which is the defect pass 1's finding 3 was about. Today's seed writes items one
   statement at a time, so nothing is wrong on current data. Smallest correction: chain a second
   `.order("key", { ascending: true, referencedTable: "item" })`, and extend the existing embed-order
   test to assert both.

4. **`type CountRow` and `toArtifacts` at `src/db/queries/opportunity.ts:76-84` are a verbatim copy
   of `src/db/queries/item.ts:78-86`.** Both encode the same reading of PostgREST's `count` embed
   (`[{ count: n }]`), and two copies can drift into two readings. Smallest correction: export one
   from `item.ts` — or from a small shared module — and import it here.

5. **Three tests in `src/db/queries/opportunity.test.ts` name no criterion in the report's AC
   table**: "asks for the items in creation order, on the embed", "derives each item's stage and
   asks for none", and "returns an empty item list rather than failing when nothing is linked".
   Product-spec §7.5's no-orphan rule runs both ways, and it is the standard pass 1 applied to the
   `listOpportunities` test. Smallest correction: name the first two under the Rules line they hold
   (derived stage, stable order) and the third under AC5 in `docs/reports/T1.4.md`.

No Must stands. The seven criteria are built where the report says they are built, the item
header's old plain-text path is gone rather than kept beside the new one, every string on both
surfaces reads from `src/i18n`, the two 404 paths are one, and the e2e assertion that used to
guard the absence of the link now guards its presence.

PASS
