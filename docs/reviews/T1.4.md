# T1.4 — review

Reviewed commit `69d4ccf` (branch `t1-4`, pass 7, cold, the apply round). I read
`docs/tickets/T1.4.md` with its Addendum and Cited block, `product-spec` §2 and §13 at v1.8 against
the v1.6 text the ticket cites (both sections read identically — the drift is the header's, as the
report says), build-log open question 9 as it now stands, `docs/guidelines.md` §4's lines on the
guard, and every hunk of `git diff main...HEAD`. Local `main` and `origin/main` are both `ed0c5b8`
this time, so the diff is exactly this ticket's: 30 files, code and record.

`node scripts/run/review-scope.mjs docs/tickets/T1.4.md` lists nine files and calls six names
`missing`; every one of the six is on disk — two are precedents named in prose
(`src/db/item-key.db.test.ts`, `src/db/queries/item.test.ts`) and four are paths the script cut at
`[key]`. That is pass 6's Should 3 and open question 6, already filed; nothing new. I ran the eight
vitest files the list names (`e2e/item.spec.ts` is Playwright's and was not run here):

    Test Files  8 passed (8)
         Tests  63 passed (63)

No skips. `src/db/opportunity-key.db.test.ts` ran its six tests against the live database rather
than printing its not-applied banner — observed here, not read off the report — so AC1 and AC2 are
finally green in a reviewer's own run, which every pass before this one had to say it could not
assert.

**The apply round's diff** (`69d4ccf`) is three files: `src/db/database.types.ts` changes only its
header note, from asking for a regenerate-and-diff to recording one; the three `key` lines at 551,
561 and 571 are unchanged. `docs/reports/T1.4.md` and `docs/build-log.md` record the apply and
close open question 1 and OQ40's types paragraph. No logic moved, no test moved, which is what the
round claims.

**Criteria ↔ tests, both directions.** AC1 → "numbers from one, without gaps", "counts per
product, not per workspace", "refuses two opportunities sharing a key in one workspace" (unique per
workspace — the report's row 1 names only the first two; the third is the constraint's and sits in
the same file). AC2 → "ignores a key the client supplies", "keeps its own count after an insert
tried to name itself". AC3 → the header's four, `ItemLine`'s two, `ItemsSection` "lists every
item…", `opportunity.test.ts` "carries the opportunity's own name and its product" and "fetches
the items and their artifacts with the opportunity". AC4 → "returns null for a key it cannot see,
as for one that does not exist", and `src/app/o/[key]/page.tsx:36–43` is `src/app/i/[key]/page.tsx`'s
guard sequence line for line, so the page half is the item page's code path. AC5 → "says so in
words when nothing is being worked on yet". AC6 → `ItemHeader.dom.test.tsx`'s first two. AC7 → the
three "from the dictionary" tests. AA1/AA2 → `migrations.test.ts`'s four, two each. AA3 → the Stop
gate. Every test on the branch names a criterion, a Rule, or (the sixth trigger test and
`listOpportunities`'s one) the thing it holds instead, in a comment — none is orphaned.

**Red-first.** Every vitest test in the diff has a row in the report's table with a mutation and
both counts (M1–M27); the e2e change has its own row, observed in chromium. The six trigger tests
share one row and one red — see Should 1.

**The migration** reads as `0005_item_keys.sql` one table over, statement for statement: nullable
add, `row_number()` backfill over `(created_at, id)` partitioned by product, `SET NOT NULL`,
`UNIQUE (workspace_id, key)`, the same shape regex as a CHECK, `app.assign_opportunity_key()` on
`BEFORE INSERT` overwriting `NEW.key` unconditionally. The DSL in `src/db/schema/tables.ts:178–195`
names the same two constraints by the same names. `scripts/seed.ts` passes `key: ""` for the three
seed opportunities because the DSL column is `notNull()` and the trigger overwrites it before the
CHECK runs — `seedItem`'s existing shape, and said in the comment. Journal entry `idx: 16`, stamped
after `0015`'s.

**The surfaces.** `page.tsx` holds §4's frame as the item page does (back link, header, one
section, the 380 column reserved). `OpportunityHeader` takes no dictionary and renders only the
row's own strings; `ItemsSection` and `ItemLine` read every label from `t`; `en.ts` carries the
three new strings under `opportunity`. `ItemHeader` links through `opportunityHref` and renders
nothing for `opportunity: null`; `item.ts` embeds `opportunity(key, title)`. `proxy.ts`'s matcher
covers `/o/*`, so the fixture comment's "`/o` is not public" holds, and the page re-checks the
session itself. Nothing in the diff is outside the ticket: the seed and the `/dev` fixture follow
from the column and the type change, `ItemsSection.tsx` is named by the Tests section, and the rest
is record.

## Findings

1. **Should — the six trigger tests' one red is the column's absence, not a mutation of the
   trigger.** `docs/reports/T1.4.md:172` records `column "key" does not exist` → `6 passed`. That
   red says the tests need `0016`; it does not say each assertion would catch a wrong trigger — a
   `COALESCE(NEW.key, …)` that honoured a supplied key would still have gone from that red to green
   on four of the six. The report is honest about it (`:212–217`: the second red was attempted and
   the pipeline role cannot set `session_replication_role`), and the assertions are direct value
   checks now observed green against the real trigger, so this does not block. What would settle it
   the day the credential allows: one mutation of `app.assign_opportunity_key()` inside the
   rolled-back transaction, with the count that reddens. Recorded so the next migration ticket
   knows the gap is the credential's and not the file's.

2. **Should — `src/db/queries/opportunity.test.ts:827` and its comment quote a promise the source
   no longer makes.** The test is named "asks for the items in creation order, on the embed" and
   the comment at `:819–826` says `OpportunityPageDetail.items` "promising 'creation order'" would
   be lying. Pass 6's Should 2 rewrote that promise — `src/db/queries/opportunity.ts:43–48` now says
   "Oldest first, and stably … it is not the order they were bet on" — so the test names an order
   the type stopped claiming. Same staleness in `docs/reports/T1.4.md:131` ("`OpportunityPageDetail.items`'
   creation order"). Smallest correction: rename the test "asks for the items oldest first and
   stably, on the embed" and drop the quoted phrase from its comment and the report row. No
   assertion changes.

No Must stands. The diff does what the ticket and its Addendum say — the column, the trigger, the
page, the link, the renumbering — and the apply round observed the two criteria the earlier passes
could only read. The diff adds a migration, so under `docs/guidelines.md` §4 this PASS does not let
the run merge its own work: `merge` stays the human's word.

PASS
