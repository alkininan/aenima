# T1.4 — review

Reviewed commit `db6d0c0` (branch `t1-4`, the addendum round: `origin/main` merged in and the
migration renumbered `0015` → `0016`), pass 5, cold. I read `docs/tickets/T1.4.md` with its
Addendum and Cited block, `docs/guidelines.md` §4 and §5 steps 4–9 and the §7.5 line of the body
template, and every hunk of `git diff origin/main...HEAD` (29 files). Local `main` is at `c310b30`,
behind `origin/main` at `ed0c5b8` which this commit merged, so `main...HEAD` also lists T0.24's and
T3.1's files; those are main's and were not reviewed here. This round's own branch-side changes
(`git diff c67891b db6d0c0` on the ticket's files) are: the file rename, the journal entry
renumbered and restamped, `0015` → `0016` in nine comments and the ticket's Build and Tests lines,
the Addendum section, the log's four number substitutions, and one new test file,
`src/db/migrations.test.ts`.

`node scripts/run/review-scope.mjs docs/tickets/T1.4.md` computes `touched` against the stale
local `main`, so its list sweeps in T3.1's suites; its `missing` array holds two prose references
and four paths the script cut at `[key]`, every one on disk. I ran the ticket's eight vitest files:

    Test Files  7 passed | 1 skipped (8)
         Tests  57 passed | 6 skipped (63)

The six skips are `src/db/opportunity-key.db.test.ts` behind its stderr banner, which now names
`drizzle/0016_opportunity_keys.sql` — observed, not read from the report. The four tests over pass
4's 53 are `src/db/migrations.test.ts`. `e2e/item.spec.ts` is Playwright's and was not run here.

**AA1 — one migration, numbered `0016`, journal `idx` matching.** Holds. Against `origin/main` the
branch adds exactly one `.sql`; the journal's last entry is `idx: 16`, tag `0016_opportunity_keys`,
`when` 1789926529521 (2026-09-20T17:48Z), later than `0015`'s 1789644802697 — restamped as well as
renumbered, which the commit message and `apply.mjs`'s `blockedOf` both say is the point.

**AA2 — `0015` on main untouched.** Holds. `git diff origin/main HEAD --
drizzle/0015_refinement_round.sql` is empty and the journal's `0015` row is main's, field for field.

**AA3 — the merged tree green.** The Stop gate's; this ticket's files are green above.

## Findings

1. **Must — `src/db/migrations.test.ts` has no red-first record and the ticket's Tests section
   does not name it.** The Addendum says `TA1 → AA1 · TA2 → AA2 · TA3 → AA3` and nothing on the
   ticket says what TA1–TA3 are; the one test file this round adds — four tests at
   `src/db/migrations.test.ts:79`, `:86`, `:94`, `:103` — appears in neither
   `docs/tickets/T1.4.md` § Tests nor the table under *Tests written, each observed red first* in
   `docs/reports/T1.4.md` (lines 40–66, whose last row is still the e2e test from pass 2). Every
   other test on this branch has a row; these four have none, so AA1 and AA2 rest on tests never
   seen red. `docs/guidelines.md` §5 step 4 (line 458: "new logic has tests observed failing first,
   the mutation and the count recorded per test") and step 8 (line 487: the report is "refused
   without the red-first record: per test") are the rules; §7.5 (line 124: "every TC names its AC,
   no orphans") is why the Tests section owes the name. Smallest correction: add to the ticket's
   Tests section one line naming `src/db/migrations.test.ts` as TA1 (the file/entry, index and
   stamp-order tests → AA1) and TA2 ("keeps 0015 as T3.1's…" → AA2), with TA3 as the gate's own run;
   then four rows in the report's table, one mutation each — the journal's last `idx` set to 15, the
   `0016` file renamed, its `when` set below `0015`'s, `0015`'s `when` changed — each with the
   count that went red and the count that went green.

2. **Must — `docs/reports/T1.4.md` contradicts the tree on the one thing this round changed.** The
   commit message says "Every reference on this branch to its own migration follows the file"; the
   report was not touched this round and still reads `drizzle/0015_opportunity_keys.sql` at
   lines 5, 22, 204 and 224, `drizzle/0015` at 23 and 65, and open question 5 (lines 223–232)
   ends "Nothing was renumbered this round … Owner: you, in the same breath as `apply` — say which
   order" — a question the Addendum quotes the human answering, put back to them as open. §5
   step 8 mirrors this report into the board's Report section, so the human doing `apply` reads a
   filename that no longer exists and is asked a settled question again. Smallest correction: the
   substitutions the log received, plus open question 5 closed in the past tense (answered on the
   board 2026-09-20, renumbered at `db6d0c0`), plus the AA1–AA3 rows in the AC table and a pass-5
   row in the reviewer table.

3. **Should — `docs/schema.md` can now take `0016`'s paragraph; the reason for deferring it is
   gone.** Pass 4's Should 4 and the report's open question 4 held the paragraph back because the
   number was unsettled. It is settled, and `0015_refinement_round` already has its paragraph at
   `docs/schema.md:383`, so `0016` is now the one migration in the tree with none. Smallest
   correction: three lines in `0005`'s form after line 397 — the column, the trigger, the shared
   key shape with independent counters.

4. **Should — `docs/log/T1.4.md` records no addendum round.** This round's branch-side change to the
   log is four number substitutions; the last paragraph (lines 95–102) still ends with the collision
   "in the stop's comment" and says the run "left it alone", which was true at `c67891b` and is what
   this round undid. One paragraph, dated, saying the answer came and what the renumber-and-restamp
   did — the log is written at close (§5 step 8), so this may simply be pending.

No finding stands against the renumbering itself: the file, the index and the stamp moved
together, `0015` is main's untouched, and every comment on the branch that named its migration
names `0016`. What stands is the record — a test file with no red seen and a report that still
describes the collision as open.

FINDINGS
