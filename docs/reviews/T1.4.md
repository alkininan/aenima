# T1.4 — review

Reviewed commit `e738e7b` (branch `t1-4`, pass 6, cold, after pass 5's two Musts and two Shoulds
were taken). I read `docs/tickets/T1.4.md` with its Addendum and Cited block, `docs/guidelines.md`
§4 and §5 steps 4–9, and every hunk of `git diff origin/main...HEAD` (30 files, code and
record). Local `main` is at `c310b30`, behind `origin/main` at `ed0c5b8` which this branch merged,
so `main...HEAD` also lists T0.24's and T3.1's files; those are main's, reviewed under their own
tickets, and were not reviewed here.

`node scripts/run/review-scope.mjs docs/tickets/T1.4.md` — its `missing` array holds six names,
none of them missing: `item-key.db.test.ts` and `item.test.ts` are precedents the Tests section
cites in prose and sit at `src/db/item-key.db.test.ts` and `src/db/queries/item.test.ts`; the four
`/…dom.test.tsx` entries are paths the script cut at `[key]`, all on disk under `src/app/o/[key]/`
and `src/app/i/[key]/`. I ran the ticket's seven vitest files plus `src/db/migrations.test.ts`,
the one test file the diff adds beyond the Tests section:

    Test Files  7 passed | 1 skipped (8)
         Tests  57 passed | 6 skipped (63)

The six skips are `src/db/opportunity-key.db.test.ts` behind its stderr banner, which names
`drizzle/0016_opportunity_keys.sql` — observed, not read from the report. `e2e/item.spec.ts` is
Playwright's and was not run here.

**AA1 — one migration, numbered `0016`, journal `idx` matching.** Holds. Against `origin/main` the
branch adds exactly one `.sql`; the journal's last entry is `idx: 16`, tag `0016_opportunity_keys`,
`when` 1789926529521, later than `0015`'s 1789644802697. `migrations.test.ts` "names every
migration file…" and "gives each entry the index…" hold it; report rows M24 and M25 show each red.

**AA2 — `0015` on main untouched.** Holds. `git diff origin/main HEAD --
drizzle/0015_refinement_round.sql` is empty; the journal's `0015` row is main's field for field.
"keeps 0015 as T3.1's…" and "stamps them in index order…" hold it; rows M27 and M26 show each red.

**AA3 — the merged tree green.** The Stop gate's; this ticket's files are green above.

**Pass 5's Musts.** Both settled: the Addendum names TA1–TA3 with the tests each maps to, the
report's table carries M24–M27 with the count that went red and the count that went green, the AC
table has AA1–AA3 rows, the reviewer table a pass-5 row, and open question 5 is closed in the past
tense with the restamp explained. Both Shoulds too: `docs/schema.md:399` carries `0016`'s
paragraph in `0005`'s form, and `docs/log/T1.4.md` records the addendum round.

**The seven original criteria,** re-read on the merged tree: 3–7 are built and their tests name
them in both directions; `drizzle/0016` mirrors `0005` statement for statement (nullable add,
`row_number()` backfill over `(created_at, id)`, `NOT NULL`, `UNIQUE (workspace_id, key)`, the
`item_key_shape` regex, `BEFORE INSERT` overwriting `NEW.key` unconditionally);
`src/app/o/[key]/page.tsx:36–43` is `src/app/i/[key]/page.tsx:48–55`'s guard sequence line for
line, so AC4's one code path is the item page's; `ItemHeader` takes `opportunity: { key, title }
| null` and `item.ts` embeds `opportunity(key, title)`. Every vitest test on the branch has a
mutation row in the report.

## Findings

1. **Should — AC1 and AC2 are unobserved; the six trigger tests have never been green.** The
   report says so plainly (`docs/reports/T1.4.md:77`: "Never green and cannot be until the
   migration is applied", `6 skipped → 6 skipped`), and the ticket's Tests section is what asks
   for the skip. This verdict does not assert the trigger works — it asserts `0016` reads as
   `0005` does. The diff adds a migration, so the guard will not self-merge on this PASS and
   `apply` then `merge` stay the human's words (§5 step 6, step 9). What the apply run owes,
   already in open question 1: `opportunity-key.db.test.ts` green, and `src/db/database.types.ts`
   regenerated and diffed against the hand-written block at lines 554, 564 and 574.

2. **Should — `src/db/queries/opportunity.ts:110–114` overclaims what the second sort key buys.**
   The comment says "the second key is what keeps 'creation order' true the day something inserts
   a batch", and `OpportunityPageDetail.items` (line 60) promises "creation order". `.order("key",
   …)` on the embed is a *text* sort: once one batch reaches ten items sharing a `created_at`,
   `soc-10` sorts before `soc-2`, which is the opposite of creation order. The tie-break makes the
   list *stable* between two reads — the thing pass 1's Must 3 actually asked for — and nothing
   PostgREST can order by here makes it creation order inside a batch. Smallest correction: say
   "stable" rather than "creation order" in both comments, and note that the numeric half is what
   would order a batch and cannot be asked for. No test changes; the assertion in
   `opportunity.test.ts:163–174` holds either way.

3. **Should — harness, not this ticket's: `scripts/run/review-scope.mjs` cuts a Tests-section path
   at `[key]` and reports a precedent named in prose as missing.** Every one of the six names it
   called `missing` this pass are on disk. The report's open question 6 already files the
   `origin/main` default; the `[key]` cut is a second defect in the same script worth the same Fix
   task. Smallest correction: match the whole backticked path rather than stopping at `[`, and
   only count a name as cited when it resolves to a file the section lists as this ticket's.

No Must stands. The diff does what the ticket's Addendum says — the file, the index and the
stamp moved together, `0015` is main's untouched, and the record now matches the tree — and
nothing in it is unaccounted for.

PASS
