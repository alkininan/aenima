# T0.24 — review

Reviewed commit `0db8515` (branch `t0-24`), pass 1. `main` is `d79f49a` and an ancestor of HEAD.
Read `docs/tickets/T0.24.md` with its Cited text (guidelines §4 and §5 at v1.15, the capability
boundary inside §5), then every hunk of `git diff main...HEAD` (10 files): the skill's step 0a and
step 6, guidelines v1.16, `guard.mjs` rule (b) and its new predicate, `apply.mjs` whole,
`comments.mjs`'s `readThread`, the README, and the three test files. Also read what the diff
leans on: `permission.mjs` (`verify` reads the thread with the same `readThread`/`shapeOf`, so the
"a refusal consumes nothing" change reaches the guard's own read — AC5 holds on both sides),
`branch.mjs`'s reuse path, `repo.mjs`, drizzle-orm's `migrate()` and `readMigrationFiles`, and
`origin/t3-1` / `origin/t1-4` — the two migration stops the Spec line names.

Scope and runs, done here:

    node scripts/run/review-scope.mjs docs/tickets/T0.24.md   -> 3 files, missing: []
    pnpm vitest run scripts/hooks/guard.test.mjs scripts/run/apply.test.mjs \
      scripts/run/comments.test.mjs                            -> 3 files, 169 passed

By hand: `decide` refuses `node scripts/run/apply.mjs`, `pnpm node …/apply.mjs`,
`npx tsx …/apply.mjs`, `node --env-file .env.migrate x.mjs` and allows the neighbours;
`readAnswer` parses `emit`'s pretty form; `primaryCheckout()` from this worktree is
`/Users/alkininan/dev/aenima`; `workerSpec` drops `DATABASE_URL` and points `--env-file` at the
primary's `.env.migrate` with the worktree's own `apply.mjs` as the script.

Criteria ↔ Tests: TC1–TC5 each head a `describe` in `apply.test.mjs` naming its AC; the three
new `comments.test.mjs` cases sit under a `T0.24 TC5 → AC5` comment. The five new `guard.test.mjs`
cases name no criterion (see Should 6). `docs/reports/T0.24.md` does not exist at this commit, so
no new test has a red-first row: all are unverified at this pass (step 8 work; see Should 6).

## Findings

1. **Must** — The apply cannot run for the two tickets the Spec line names. Step 0a in
   `.claude/skills/ticket/SKILL.md:107-111` says: check the ticket's branch out with
   `node scripts/run/branch.mjs <id>`, "and only then `node scripts/run/apply.mjs`".
   `branch.mjs:93` does `git checkout -B <branch> origin/<branch>`, no merge of main, and
   `git ls-tree origin/t3-1 scripts/run/apply.mjs` and the same for `origin/t1-4` return nothing:
   both branches predate this diff and carry no such file. So the next run claims T3.1, swaps the
   worktree onto `t3-1`, and `node scripts/run/apply.mjs` fails at the shell with no JSON — the
   ticket's Objective ("T3.1's *apply* has sat granted … with nothing able to act on it") is
   reproduced one step later, and the Done line's end-to-end observation cannot happen through
   the skill as written. Every ticket branched *after* this merges is fine; the two this ticket
   exists for are not. Smallest correction: in step 0a, after `branch.mjs`, merge `origin/main`
   into the reused branch (`git merge --no-edit origin/main`, which T1.5's re-claim already did by
   hand) and say so in the step — the branch needs main to carry on anyway, and the worker's
   `script = SELF` (`apply.mjs:908`) then names a file that exists. An alternative is to invoke the
   primary checkout's copy by absolute path (the guard's `nodeScript` matches the basename, so it
   is still gated), but the primary sits on `main` only once this diff has merged, and the merge
   is the plainer rule.

2. **Must** — The half of `apply.mjs` that touches the database has no test. `work()`
   (`scripts/run/apply.mjs:1064-1098`) takes `deps.postgres`, `deps.drizzle`, `deps.migrate` for
   exactly this and nothing in `apply.test.mjs` calls it: not the `lastApplied` bracket that
   produces AC4's `idx`/`tag` in real use, not the catch at `:1091-1094` that produces AC5's
   "database's own error quoted" (TC5 hands the parent a `why` already written). `readAnswer`
   (`:965`) says "observed red first" and no test names it; `primaryCheckout` and `workerSpec`
   are reached only through `apply`. CLAUDE.md "New logic has tests"; §5 step 4 "new logic has
   tests observed failing first, the mutation and the count recorded per test". Smallest
   correction: one `describe` under TC4/TC5 that hands `work` a fake `postgres` (a tagged-template
   function answering `[{ present: true }]`, `[{ last: "1400" }]`, then `[{ last: "1500" }]`, with
   `.end()`), a pass-through `drizzle`, and a `migrate` that resolves → `applied` is
   `[{ idx: 15, tag: "0015_refinement_round" }]`; then a `migrate` that rejects with
   `new Error('relation "x" already exists\n  at … postgresql://u:p@h/db')` → `ok: false`, `why`
   is the first line and holds no URL; and one `readAnswer` case on `emit`'s pretty output whose
   last value is an array. Record each red in the report.

3. **Should** — `scripts/hooks/guard.mjs:698`: "`pnpm db:baseline` is in it for the same
   reason". It is not: `decide` on `pnpm db:baseline` returns null and `wanted` gives `[]` (the
   guard reads the command as typed, and `package.json`'s expansion is text it never sees). The
   guidelines' hard-boundaries line now reads "anything handed `.env.migrate`", which a reader
   would take to cover it. Never guarded on `main` either, so nothing loosened. Either add
   `invocation(argv, "db:baseline") !== null` to `appliesMigration` and a guard test, or drop
   the sentence and narrow the guidelines wording to "any command handed `.env.migrate` on its
   own line".

4. **Should** (uncertain how much it matters) — A word that outlives a refusal is retried every
   hour, with a comment every hour. `readThread` (`comments.mjs:78`) leaves `apply`/`merge`
   unanswered past a `refused`; `mayPost`'s one-of-a-kind is per claim (`permission.mjs:176`), and
   each scheduled run is a new claim. A migration with a syntax error is attempted hourly (each
   a rolled-back transaction, harmless) and posts a `refused` hourly until the human fixes the
   branch. `docs/guidelines.md:77-80` says "the next run makes it again once the thing in the way
   is settled" — the run cannot tell when it is settled. §5 step 1 already has the rule for this
   shape: "words already on the thread are not said again". Smallest: skip the `refused` when the
   newest pipeline comment is a `refused` with the same `why`, and say so in the guidelines line.

5. **Should** — `work()` returns `pending` (`apply.mjs:1090`) and `pendingAfter` is exported and
   tested ("says what is pending before anything is run"), but `apply()` at `:1043-1049` drops it
   and nothing reads it. Surface it in the answer the run posts (a `refused` could say which
   entries were pending and did not apply) or delete `pendingAfter` and its test.

6. **Should** — Records the report must carry, none of which exist at `0db8515`:
   `docs/reports/T0.24.md` (so no red-first row for any of the 23 new tests, and no reviewer-pass
   row), `docs/log/T0.24.md`, the regenerated index. The five new `guard.test.mjs` cases
   ("refuses the run's own apply script" through "counts the word it would need") name no
   criterion; tag them AC2 / Rules bullet 1 so the table can name what each covers. And the Done
   line's "T3.1's outstanding *apply* observed consumed end to end": inside this session the
   marker names T0.24, so `verify("apply")` reads T0.24's thread and the new guard would refuse
   an apply for T3.1 — while `main`'s current guard does not read `apply.mjs` at all. The honest
   observation is the next scheduled run's step 0a claiming T3.1 (after finding 1); if the run
   applied it by hand here instead, the report must say so and under which guard.

7. **Should** (cosmetic) — `scripts/run/apply.test.mjs:1396` fixture text "Fix the migration on
   the branch and say apply again." contradicts the rule the same test asserts (the human does
   not say it again). Reword to what the composer would write.

What the diff does otherwise matches the ticket: the credential is read by node in a child whose
cwd is the primary checkout, `DATABASE_URL` is dropped from what it inherits, both sides scrub
connection strings, the guard's rule (b) reads the script and any `--env-file` naming
`.env.migrate` at the position an interpreter would read them, and an `applied` comment consumes
the word while a `refused` one does not — at the preflight and at the guard alike.

FINDINGS
