# T0.24 — review

Reviewed commit `9dc198e` (branch `t0-24`), pass 3. `main` is `d79f49a` and an ancestor of HEAD.
Read `docs/tickets/T0.24.md` with its Cited text (guidelines §4 and §5 at v1.15, the capability
boundary inside §5), then every hunk of `git diff main...HEAD` (12 files, 1887+/48−): the skill's
step 0a and step 6, guidelines v1.16, `guard.mjs` rule (b) with `appliesMigration`, `nodeScript`
and `readsAdminEnv`, `apply.mjs` whole, `comments.mjs`'s `readThread`, `mayPost`, the `applied`
signature and the `migration`/`applied` composers, `permission.mjs`'s `postable`, the README, and
the three test files. Also read what the diff leans on: `package.json`'s `db:*` scripts
(`db:migrate` is `drizzle-kit migrate`; `db:baseline` hides `--env-file=.env.migrate` inside the
script, so naming it in `appliesMigration` is right), `scripts/db-baseline.ts` (writes the
journal's `when` as `created_at`, so `pendingOf`/`blockedOf` read baselined rows correctly),
drizzle-orm's skip rule at `node_modules/drizzle-orm/pg-core/dialect.js:62`
(`Number(lastDbMigration.created_at) < migration.folderMillis`, which `blockedOf`'s `<= newest`
mirrors), `repo.mjs`'s `commonDir`, `claim.mjs` (writes no status), the skill's `merge`/`apply`/
`ready` shapes, and the journals of `main` (ends 0014 at 1788982026723), `origin/t1-4` (0015 at
1788982026724) and `origin/t3-1` (0015 at 1789644802697; the branch carries no `apply.mjs`).

Scope and runs, done here:

    node scripts/run/review-scope.mjs docs/tickets/T0.24.md   -> 3 files, missing: []
    pnpm vitest run scripts/hooks/guard.test.mjs scripts/run/apply.test.mjs \
      scripts/run/comments.test.mjs                            -> 3 files, 188 passed

Pass 2's Must 1 is settled: `blockedOf` names a pending entry stamped no later than the ledger's
newest row, `work()` returns `ok: false` naming it before `migrate` is called, and the test
"refuses before running the migrator" asserts `migrated === false`; `compose("applied", { file:
null })` says "Applied nothing" and `kindOf` still reads it as `applied`; the skill posts no
`file` when `applied` is empty. Pass 2's Should 2 is taken (the `migration` comment now says
"Say \"apply\""). For T3.1 on today's ledger nothing is blocked (1789644802697 > 1788982026723) and
the apply proceeds; for T1.4 it is refused with the regenerate sentence rather than reported
applied.

Criteria ↔ Tests: TC1–TC5 each head a `describe` in `apply.test.mjs` naming its AC, and the
`exportMigrations`, `work` and `readAnswer` describes now name TC1/AC1, TC4+TC5 and TC4/AC4;
the five new `comments.test.mjs` cases under `shapeOf`/`mayPost` are tagged `T0.24 TC5 → AC5`;
the six new `guard.test.mjs` cases are tagged `T0.24, AC2`. Every criterion has a test naming it
and every new test names a criterion, except the `kindOf` variant and the two `compose`
assertions in `comments.test.mjs`, tagged `T0.24` without a TC (Should 4).

Red-first record: `docs/reports/T0.24.md` does not exist at `9dc198e`, so no new test has a row
naming the mutation that reddened it. All 43 new tests — 32 in `apply.test.mjs`, 6 in
`guard.test.mjs`, 5 in `comments.test.mjs` — are unverified at this pass. Step 8 comes after this
review, so this is a record the report must carry rather than a defect in the diff (Should 4).

## Findings

1. **Should** — `scripts/run/apply.test.mjs`, describe "apply — TC3 · AC3 the word is consumed
   once", the `answered` fixture: `"⟡ Applied drizzle/0015_refinement_round.sql to the shared
   database. The ticket picks up where it stopped."` is not the sentence the composer writes
   (it lacks "from"), and `kindOf` returns `null` for it against the anchored signature at
   `scripts/run/comments.mjs:267`. The test passes because `readThread` treats any non-`refused`
   pipeline comment as an answer, not because an `applied` comment consumes the word — the
   claim in its name. The same criterion is properly covered by "consumes the word once the run
   has said it applied" in `comments.test.mjs`, which uses `said("applied", …)`. Smallest: build
   the fixture from `compose("applied", { file: "drizzle/0015_refinement_round.sql" }, "⟡ ")`.

2. **Should** — `scripts/run/comments.mjs:123`, in `mayPost`:
   `const standing = (thread?.pipeline ?? []).findLast((comment) => comment.kind === "refused");`
   reads the newest refusal anywhere on the thread, including one older than a later answer
   (`readThread`'s own `answered`). A thread that went apply → refused (error X) → human fixes →
   apply → applied → later, on an addendum round, a second migration → apply → error X again
   would have its new refusal held back as "already the newest one … word for word" although
   a run has answered since and the human has asked afresh. Rare on today's board; the
   correction is one clause: consider only refusals newer than the last non-`refused` pipeline
   comment (or newer than the newest human reply). One case under TC5.

3. **Should** — `scripts/run/README.md`, the `migration-check.mjs` paragraph now reads
   "…that script, or anything handed `.env.migrate` —\n— `db:baseline` too — through only
   when…": a doubled dash across a line break and a parenthetical stranded from the list it
   belongs to. Fold `db:baseline` into the list: "`db:migrate`, `drizzle-kit migrate`,
   `db:baseline`, that script, or anything handed `.env.migrate` — through only when".

4. **Should** — Records the report must carry, none of which exist at `9dc198e`:
   `docs/reports/T0.24.md` with a red-first row per new test (43 rows) and the reviewer-pass
   table (three passes, this one included, with each pass's model), `docs/log/T0.24.md`, and
   the regenerated index. Tag the `kindOf` variant `["applied", { file: null }]` and the two
   `compose` assertions in `comments.test.mjs` with a TC (TC4/AC4 for the `applied` wording,
   TC3/AC3 for the `migration` wording) so the table can name what each covers. And the Done
   line's "T3.1's outstanding *apply* observed consumed end to end" cannot be observed from this
   session — the marker names T0.24, so `verify("apply")` reads T0.24's thread, and `main`'s
   guard does not yet read `apply.mjs` as an apply — so the report should say the observation
   is the next scheduled run's step 0a claiming T3.1, or say under which guard it was made if
   it was made by hand.

5. **Should** — `scripts/run/apply.test.mjs`, "refuses before running the migrator":
   `const sql = () => [{ present: true }, { created_at: "1300" }, { created_at: "9999" }];`
   answers every query with the same array, so `ledgerStamps` reads `present` off row 0 and the
   stamps off the same rows, `NaN` filtered out by `isFinite`. It holds today by the shape of
   that filter. The `fakePostgres` helper two describes down answers each query in turn; use it
   here too.

What the diff does matches the ticket. AC1: the apply is one child whose cwd is the primary
checkout and whose `--env-file` is that checkout's `.env.migrate`, handed the ref's `drizzle/`
from `git archive` without checking the branch out, the export removed whatever the answer, and
a migration drizzle would pass over is refused by name rather than reported applied. AC2: the
child's `DATABASE_URL` is dropped from what it inherits, the worktree's decoy `.env.migrate` is
never named, both sides scrub connection strings, and the guard's rule (b) reads every shape —
`db:migrate`, `drizzle-kit migrate`, `db:baseline`, `apply.mjs` at the interpreter's script
position past its options, and any `--env-file` naming `.env.migrate` — while letting the
tests, the other run scripts, `cat` and `grep` through; `wanted` counts the word for each.
AC3: an `applied` note is the last answering comment, so the word is gone from `unanswered`
and `shapeOf` is `assess`. AC4: `work()` brackets the ledger's `created_at` set around
`migrate` and names `{ idx, tag }` for every row gained, and the skill's step 8 names them.
AC5: `ok: false` carries the database's first line, scrubbed; `claim.mjs` writes no status, so
the task stays at Decision; a `refused` comment answers nothing in `readThread`, at the
preflight and at the guard's `verify` alike, so the word stands; and the same refusal is not
posted twice. The credential boundary does not move: nothing in the diff reads `.env.migrate`
from a worktree, and the guard names the script so it is not a way round the rule.

PASS
