# T0.24 — review

Reviewed commit `244a2d8` (branch `t0-24`), pass 2. `main` is `d79f49a` and an ancestor of HEAD.
Read `docs/tickets/T0.24.md` with its Cited text (guidelines §4 and §5 at v1.15, the capability
boundary inside §5), then every hunk of `git diff main...HEAD` (12 files, 1769+/44−): the skill's
step 0a and step 6, guidelines v1.16, `guard.mjs` rule (b) and `appliesMigration` with its three
new predicates, `apply.mjs` whole, `comments.mjs`'s `readThread` and `mayPost`, `permission.mjs`'s
`postable`, the README, and the three test files. Also read what the diff leans on and what the
Spec line names: `drizzle.config.ts` (no custom migrations table or schema, so `lastApplied` and
drizzle-orm's `migrate()` read the ledger `drizzle-kit migrate` writes), `prune.mjs` (fetches at
step 0, before 0a reads `origin/t<id>`), `claim.mjs` (writes no status, so a failed apply leaves
the task at Decision), `mentions` (word-bounded: "applied it" is not `apply`), drizzle-orm's
`migrate()` at `node_modules/drizzle-orm/pg-core/dialect.js:62`, and the journals of `origin/t1-4`
and `origin/t3-1` — the two migration stops the Spec line names.

Scope and runs, done here:

    node scripts/run/review-scope.mjs docs/tickets/T0.24.md   -> 3 files, missing: []
    pnpm vitest run scripts/hooks/guard.test.mjs scripts/run/apply.test.mjs \
      scripts/run/comments.test.mjs                            -> 3 files, 185 passed

By hand, from this worktree: node is v26.7.0 (`--env-file=` is native); `primaryCheckout()` is
`/Users/alkininan/dev/aenima`; `git archive --format=tar -o … origin/t3-1 drizzle` then `tar -xf`
into the scratchpad yields `drizzle/meta/_journal.json` ending at `0015_refinement_round`, where
`main`'s ends at `0014_test_users` — so `--ref origin/t3-1` would hand the worker exactly the
migration T3.1 waits on, without checking the branch out. `.env.migrate` was not read.

Pass 1's two Musts are settled: Must 1 by `exportMigrations` and the skill's "before the branch is
checked out" (`.claude/skills/ticket/SKILL.md:106-113`); Must 2 by the `work` describe in
`apply.test.mjs` (six cases through a fake `postgres`, the bracket and the catch both reached)
and the `readAnswer` cases. Shoulds 3, 4, 5 and 7 are taken (`db:baseline` in
`appliesMigration`; the word-for-word `refused` held by `mayPost`; `pending` carried through
`apply()`; the fixture reworded).

Criteria ↔ Tests: TC1–TC5 each head a `describe` in `apply.test.mjs` naming its AC; the `work`
describe names TC4 and TC5 in its comment; the five new `comments.test.mjs` cases sit under
`T0.24 TC5 → AC5`; the six new `guard.test.mjs` cases are tagged `T0.24, AC2`. The
`exportMigrations` and `readAnswer` describes name no criterion (Should 3). `docs/reports/T0.24.md`
and `docs/log/T0.24.md` do not exist at this commit, so no new test has a red-first row: all 40
are unverified at this pass (step 8 work; Should 3).

## Findings

1. **Must** — For T1.4, the second of the two tickets the Spec line names, the apply would run,
   apply nothing, and the run would post that it applied the file. drizzle-orm's migrator applies
   an entry only when `Number(lastDbMigration.created_at) < migration.folderMillis`
   (`node_modules/drizzle-orm/pg-core/dialect.js:62`). `origin/t1-4`'s journal holds
   `0015_opportunity_keys` at `when` 1788982026724; `origin/t3-1`'s holds `0015_refinement_round`
   at 1789644802697. Once T3.1's apply lands — the first thing this ticket unblocks — the ledger's
   newest `created_at` is 1789644802697, and T1.4's entry is older than it: the migrator skips it,
   `pendingAfter(entries, before)` is `[]`, `appliedBetween` is `[]`, and `work()` returns
   `return { ok: true, applied: appliedBetween(entries, before, after), pending, why: null };`
   (`scripts/run/apply.mjs`, the `try` in `work`). The skill then reads
   "`ok: true` → post one `applied` comment naming the file, set the task `In progress`"
   (`.claude/skills/ticket/SKILL.md:120`): the thread says *Applied drizzle/0015_opportunity_keys.sql
   to the shared database*, the `applied` note consumes the word, and T1.4 carries on against a
   database without its table. That is AC1 not met ("results in that migration being applied"),
   AC4 naming a migration that did not apply, and the Rules' "never silently dropped". The
   collision is drizzle's — `pnpm db:migrate` on that branch would skip it the same way, and the
   branch has to regenerate its migration either way — but the script's answer to it is this
   ticket's, and `pending` was added for exactly this reading. Smallest correction: `lastApplied`
   returns the ledger's `created_at` values as well as the max (`select created_at from
   "drizzle"."__drizzle_migrations"`); in `work()`, an entry whose `when` is not in the ledger
   and not greater than the ledger's newest is one the migrator will skip → return `ok: false`
   with `why` naming it: *the migrator would skip 0015_opportunity_keys (idx 15): its journal time
   is older than the ledger's newest; regenerate it on the branch*. Then `ok: true` with
   `applied: []` means only "nothing left to apply", and step 0a should say that in its comment
   rather than "Applied". One test under TC1/TC5: a fake `postgres` answering
   `[{ present: true }]`, `[{ last: "1789644802697" }]` and the `created_at` rows, against a journal
   whose entry 15 has `when` 1788982026724 → `ok: false`, `why` contains the tag, `migrate` never
   called. Record the red.

2. **Should** — The comment that asks for the word tells the human to apply by hand instead.
   `scripts/run/comments.mjs:355`, case `migration`, ends: *Once you've applied it, say so on this
   thread and the next run picks the ticket back up.* — pinned by `comments.test.mjs:457`. §4's
   own quotation of this comment stops at "stopped here", and §4 says the reply is `apply`. A
   human who follows the sentence applies by hand and replies "applied", which `mentions` reads
   as an answer, not the word — Ready, and the ticket continues, so nothing breaks; but the
   machinery this ticket builds is bypassed by the pipeline's own instruction, and the ticket's
   Build candidate 3 asks that the terminal step be told plainly. Not in the diff, so this is
   pre-existing; the diff touched the composer and the guidelines both. Smallest: end the comment
   with *Say `apply` on this thread and the next run applies it and picks the ticket back up.*,
   and update the test's fixture.

3. **Should** — Records the report must carry, none of which exist at `244a2d8`:
   `docs/reports/T0.24.md` (no red-first row for any of the 40 new tests — 29 in
   `apply.test.mjs`, 6 in `guard.test.mjs`, 5 in `comments.test.mjs` — and no reviewer-pass table),
   `docs/log/T0.24.md`, the regenerated index. The `exportMigrations` and `readAnswer` describes
   in `apply.test.mjs` name no criterion; tag them TC1/AC1 and TC4/AC4 so the table can name what
   each covers. And the Done line's "T3.1's outstanding *apply* observed consumed end to end":
   inside this session the marker names T0.24, so `verify("apply")` reads T0.24's thread and the
   guard would refuse an apply for T3.1 — while `main`'s current guard does not read `apply.mjs`
   at all. The honest observation is the next scheduled run's step 0a claiming T3.1; if the run
   applied it by hand here instead, the report must say so and under which guard.

What the diff does otherwise matches the ticket: the credential is read by node in a child whose
cwd is the primary checkout, `DATABASE_URL` is dropped from what it inherits, both sides scrub
connection strings, the migrations come from `git archive` of the ref and the export is removed
whatever the answer, the guard's rule (b) reads the script, `db:baseline` and any `--env-file`
naming `.env.migrate` at the position an interpreter would read them and lets the neighbours
through, an `applied` comment consumes the word while a `refused` one does not — at the preflight
and at the guard alike — and the same refusal is not said twice.

FINDINGS
