# T2.10 — review

Reviewed commit `9d964f2` (branch `t2-10`), pass 3, over the final diff. I read every hunk of
`git diff main...HEAD` (17 files, base `6526f96`) and the delta since pass 2 (`6015d67..HEAD`,
6 files), plus `docs/tickets/T2.10.md`, `docs/reports/T2.10.md`, `docs/log/T2.10.md` and build-log
open question 14 at its source.

Scope and tests, run here:

    node scripts/run/review-scope.mjs docs/tickets/T2.10.md
    pnpm vitest run "src/app/i/[key]/CheckList.dom.test.tsx" \
      "src/app/i/[key]/ReadinessPanel.dom.test.tsx" "src/app/i/[key]/GapList.dom.test.tsx" \
      src/db/queries/activity.test.ts src/lib/scoring/run-view.test.ts

5 files passed, 80 tests passed — pass 2's 79 plus the unparseable-stamp case.
`node scripts/run/report-check.mjs docs/reports/T2.10.md` answers `ok: true`, 16 rows, no problem.
`node scripts/run/log-index.mjs --check` answers `stale: false`, so the Tickets done list matches
the directory. `e2e/item.spec.ts` is Playwright and was not run here. `review-scope.mjs` still
answers `missing` for the two files under `src/app/i/[key]/`; both exist and both were run.

**Every criterion is met, and every claim has a test with a red on file.**

- AC1 — the condition line (`CheckList.tsx:200–204`) is followed by the quoted evidence and the
  question (`:221–222`, rendered at `:249–256`), and `CheckList.dom.test.tsx:414` pins the whole
  `li`'s text in that order rather than merely the presence of both.
- AC2, AC4 — the notice is gated on the line's own `not-asked` state (`CheckList.tsx:221`) and on
  the map holding that check id (`:250`); `CheckList.dom.test.tsx:473` and `:504` hold both halves.
- AC3 — the reason is asked of the ledger, never inferred: `src/db/queries/activity.ts:142–167`
  filters workspace, `subject_table`, the item's gap ids, `action = "gap.closed"` and
  `metadata->>reason` against the exported constant, which is the string `writeRun` writes
  (`src/db/queries/scoring.ts:438–441`). `activity.test.ts:220` and the `prd-19` control in
  `e2e/item.spec.ts:616` hold it.
- AC5 — `gapHasCard` still answers false for `closed`, `GapList.dom.test.tsx`'s untouched guard is
  green, and `/dev/item`'s three gap cards are still three (`e2e/item.spec.ts:144`).
- AC6 — scope, subject table, ids, action, reason, the mapping back, and no request at all for an
  item with nothing to ask about: `activity.test.ts:194–241`.
- The Rule — `CheckList.dom.test.tsx:447` says it of the dictionary entry itself and then of the
  page; the copy is a question, and no Danger or Warning is near it.
- The fixture — `run-view.test.ts:435` holds `/dev/item` to staging only closures `reconcileGaps`
  could have written, with `prd-19`'s passed closure kept out.

Pass 2's five Shoulds:

1. Step 8 is whole. `docs/log/T2.10.md` is written in the shape `log-index.mjs` demands — title,
   then the UTC stamp — and the generated list is fresh.
2. The builder's cases are mapped to AC1 in the report's table (`docs/reports/T2.10.md:20`), so
   nothing tests a claim no criterion owns.
3. The ordering is parsed rather than compared as text (`CheckList.tsx:71–72`), the test stamps are
   in the column's own `+00:00` shape, and a fourth case pins that an unparseable stamp sorts as
   oldest rather than becoming a `NaN` the first row wins behind (`CheckList.dom.test.tsx:593`). I
   traced the guard by hand: a `NaN` from `Date.parse` and a null both reach
   `Number.NEGATIVE_INFINITY`, so `held.resolvedAt <= at` is false against any real stamp in either
   arrival order.
4 and 5 were notes rather than requests; the report now states the filing and the counts.

## Shoulds

1. **The report announces my verdict before I gave it.** `docs/reports/T2.10.md:101` reads
   "**Pass 3 — PASS**, over the final diff", written in the commit the pass-3 review was invoked
   on. It happens to be right, and the guard reads `docs/reviews/T2.10.md` rather than the report,
   so nothing was granted by it — but guidelines §4 puts the verdict in the reviewer's hands
   precisely so the record is not the run's claim about its own work. Smallest correction: write
   that line after the verdict file exists, or cite it — "Pass 3 — see `docs/reviews/T2.10.md`."
2. **A tie still resolves by arrival order.** Two closures on one check id with the same
   `resolved_at`, or two unparseable stamps, take the last row, because the comparison at
   `CheckList.tsx:74` is `<=`. Unreachable today — a run closes at most one gap per check — and
   harmless when it is not, but the docstring states the null rule and not this one; one clause
   beside it would finish the sentence.
3. `review-scope.mjs` still answers `missing` for both files under a bracketed route segment.
   Filed at Backlog and on a gated path — a note for the next reviewer, who should run those files
   anyway rather than believe the list.
4. Two claims I cannot check from here, said plainly so nobody reads this PASS as covering them:
   the Backlog task and its link (`docs/reports/T2.10.md:76`), since this review reads no board,
   and the whole-suite and e2e counts (`:51`), which the Stop gate owns.

PASS
