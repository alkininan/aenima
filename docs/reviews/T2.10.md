# T2.10 — review

Reviewed commit `6015d67` (branch `t2-10`), pass 2. I read every hunk of `git diff main...HEAD`
(16 files, base `6526f96`) and the delta since pass 1 (`2734137..HEAD`, 7 files), plus
`docs/tickets/T2.10.md`, `docs/reports/T2.10.md` and build-log open question 14 at its source.

Scope and tests, run here:

    node scripts/run/review-scope.mjs docs/tickets/T2.10.md
    pnpm vitest run "src/app/i/[key]/CheckList.dom.test.tsx" \
      "src/app/i/[key]/ReadinessPanel.dom.test.tsx" "src/app/i/[key]/GapList.dom.test.tsx" \
      src/db/queries/activity.test.ts src/lib/scoring/run-view.test.ts

5 files passed, 79 tests passed — pass 1's 76 plus the three `noLongerApplicableByCheck` tests.
`node scripts/run/report-check.mjs docs/reports/T2.10.md` answers `ok: true`, 15 rows, no problem.
`e2e/item.spec.ts` is Playwright and was not run here. The `missing` list still names
`/CheckList.dom.test.tsx` and `/GapList.dom.test.tsx`; both exist (Should 4).

Pass 1's Must is settled. `docs/reports/T2.10.md:21–39` holds the red-first record: a row per new
test with the mutation that reddened it and the count that went green, including the three tests
added this pass and the changed fixture assertion. No new test is without a row, and no row has an
empty cell — `report-check.mjs` agrees.

Pass 1's six Shoulds are settled:

1. AC1's order is pinned. `CheckList.dom.test.tsx:433–438` compares the whole `li`'s text, so the
   condition, the quote and the question hold their places and moving the notice reddens it.
2. The collision is a decision rather than an accident. `noLongerApplicableByCheck`
   (`src/app/i/[key]/CheckList.tsx:52–73`) keeps the newest closure, both arrival orders are staged
   (`CheckList.dom.test.tsx:562`), and a null `resolved_at` sorts as oldest rather than deciding
   the page (`:576`). The page reads it in one line (`src/app/i/[key]/page.tsx:162`), and
   `item.gaps` carries `resolvedAt` (`src/db/queries/item.ts:48`), so the shape fits.
3. The second unindexed scan is said where a reader meets it
   (`src/db/queries/activity.ts:135–140`) and filed as open question 1 in the report.
4. The `review-scope.mjs` path bug is out of scope and on a gated path; the report records it as
   filed at Backlog.
5. Open question 14 is struck through and answered in place (`docs/build-log.md:719–734`), in the
   convention question 29 uses, with why the check line won over the gap card.
6. The tone assertion speaks about the dictionary entry first and keeps the page-wide scan after
   it (`CheckList.dom.test.tsx:461–463`).

What still holds from pass 1: the read matches `writeRun`'s own write (`gap.closed`,
`subject_table` `gap`, `metadata->>reason` against the exported constant); the notice is gated on
the run's own `not-asked` state; the gap list, its anchors and `/dev/item`'s three cards are
unchanged; Criteria ↔ Tests map both ways for AC1–AC6 and the Rule; tokens, i18n and the
no-migration rule are obeyed. The diff touches no gated path, and the committed copy of this file
is byte-identical to the verdict I wrote at pass 1.

## Shoulds

1. **Step 8 is half-written.** `docs/log/T2.10.md` does not exist, so the build log's Tickets done
   list (`docs/build-log.md:46–48`) never names T2.10. That list is generated from the directory,
   so a missing entry reddens nothing, and step 9 merges after step 8 — this is the last place it
   would be noticed. Every ticket from T0.1 to T2.8 has an entry. Smallest correction: write
   `docs/log/T2.10.md` — title line, UTC timestamp in italics with the commit after a middle dot —
   and re-run `node scripts/run/log-index.mjs` before close.
2. **Three tests with no TC.** `noLongerApplicableByCheck`'s three cases
   (`CheckList.dom.test.tsx:543`, `:562`, `:576`) are named by no line in the ticket's Tests
   section, which runs TC1–TC8; §7.5's no-orphan rule cuts both ways, and an unmatched test is one
   nobody can retire. One row in the report's AC table mapping them to AC1 — the criterion whose
   "that gap's quoted evidence" they decide — settles it.
3. **The newest closure is chosen by string comparison** (`CheckList.tsx:67`). That is right only
   because both values come from one PostgREST column in one format; the tests feed
   `2026-03-01T00:00:00Z` while the column answers `+00:00`, so the format the page actually sees
   is not the one under test. Either say that in the docstring beside the null rule, or compare
   `Date.parse` and let the format stop mattering.
4. `review-scope.mjs` still answers `missing` for both files under `src/app/i/[key]/`. The Fix is
   filed and the path is gated, so this is a note rather than a request: the next reviewer of a
   file under a bracketed route segment will be told the checkout lacks it, and should run those
   files anyway.
5. Two claims in the report I cannot check from here, said plainly so nobody reads my PASS as
   covering them: the Backlog task and its link (`docs/reports/T2.10.md:74`) — this review reads no
   board — and the whole-suite and e2e counts (`:49`), which the Stop gate owns.

PASS
