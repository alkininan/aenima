# T2.10 — review

Reviewed commit `2734137` (branch `t2-10`), pass 1. I read `docs/tickets/T2.10.md`, the sections
it cites as inlined there (product-spec v1.6 §4, §5) and build-log open question 14 at its source
(`docs/build-log.md:719–725`), then every hunk of `git diff main...HEAD` — 13 files, base
`6526f96`.

`node scripts/run/review-scope.mjs docs/tickets/T2.10.md` listed five files and put
`/CheckList.dom.test.tsx` and `/GapList.dom.test.tsx` under `missing`; both exist in the checkout
(see Should 4). I ran the four vitest files the scope names plus `GapList.dom.test.tsx`, which the
ticket names for TC6:

    pnpm vitest run "src/app/i/[key]/CheckList.dom.test.tsx" \
      "src/app/i/[key]/ReadinessPanel.dom.test.tsx" "src/app/i/[key]/GapList.dom.test.tsx" \
      src/db/queries/activity.test.ts src/lib/scoring/run-view.test.ts

5 files passed, 76 tests passed. `e2e/item.spec.ts` is Playwright and was not run here.

What holds:

- The read matches the writer. `writeRun` logs `logActivity("gap.closed", "gap", …)` with
  `reason: … "no longer applicable"` (`src/db/queries/scoring.ts:438–441`), and
  `listGapsClosedAsNoLongerApplicable` (`src/db/queries/activity.ts:135`) filters
  `workspace_id`, `subject_table = "gap"`, `subject_id in (…)`, `action = "gap.closed"` and
  `metadata->>reason` against the exported constant. That is AC6, including the request not sent
  for an empty id list.
- AC1, AC2 and AC4 are read off the line's own state: the notice is gated on
  `check.state === "not-asked"` (`src/app/i/[key]/CheckList.tsx:170–172`) and renders nothing when
  the map holds no entry for that check id (`:198–199`).
- AC3 holds by construction as well as by the filter — the map the page builds
  (`src/app/i/[key]/page.tsx:160–162`) can only hold ids the ledger returned for that one reason,
  and the fixture keeps `g5` (`prd-19`, closed by a pass) out of `ITEM_NO_LONGER_APPLICABLE`.
- AC5: the gap list is untouched. `GapList.dom.test.tsx`'s "renders a closed gap nowhere, in any
  tag" is unchanged and green; the new fixture gap `g6` is `closed`, so `/dev/item`'s three gap
  cards stay three (`e2e/item.spec.ts:144`), and the notice wears no `gap-<id>`.
- Criteria ↔ Tests map both ways. AC1→TC1, AC2→TC2, AC3→TC5, AC4→TC3, AC5→TC6, AC6→TC7, the Rule
  →TC4, the fixture→TC8. Every new test belongs to one of those; no orphan either way.
- Design and copy: `opacity-60` is design-spec §0 law 7's "Dim/park | element opacity .60", the
  same treatment `GapList.tsx:189` gives a settled card; the question goes through
  `src/i18n/en.ts` and TR/NL resolve to EN through `src/i18n/index.ts`. No hex, radius, duration
  or font size written by hand. No new column, no migration.

## Findings

**1 · Must — no red-first record on file, so eleven new tests are unverified.**
`docs/tickets/T2.10.md:79` says "New logic has tests, each observed failing before it passed", and
line 84 reserves the Report; there is no `docs/reports/T2.10.md` in the tree and the ticket's
Report section is empty. `scripts/run/report-check.mjs` is the shape that record takes: one table
under `## Tests written…` with a `reddened by` cell and a `red → green` cell per test. Nothing on
file says any of these was observed red: four in `CheckList.dom.test.tsx` (`:414`, `:437`, `:460`,
`:491`), five in `activity.test.ts` (`:194`, `:207`, `:220`, `:227`, `:238`), one in
`run-view.test.ts` (`:435`), one in `e2e/item.spec.ts` (`:616`), plus the changed expectation in
`run-view.test.ts:411`. Smallest correction: write `docs/reports/T2.10.md` with that table, a row
per test naming the mutation that reddened it and the count that went green, and come back. I
raise this at pass 1 knowing the report is step 8's; it is the one claim in this ticket the diff
cannot answer, and T0.17 and T0.20 both settled it at pass 1.

## Shoulds

1. AC1 says the evidence and the question render "beneath the condition line", and nothing asserts
   the order. `CheckList.dom.test.tsx:414` checks both are present; `e2e/item.spec.ts:624–625`
   checks they are inside the same `li`. One `compareDocumentPosition`, or reading the `li`'s
   `textContent` and asserting the condition sentence comes first, would pin it.
2. `src/app/i/[key]/page.tsx:161` keys the map by check id, so two gaps closed as no longer
   applicable on one check id collapse to whichever `getItemByKey` returned last — the page then
   quotes one closure and silently drops the other. A check can leave the denominator, come back
   and fail, and leave again, so the state is reachable. Either say in the docstring which one is
   meant to win, or key by gap and render each.
3. `src/db/queries/activity.ts:18–22` already records that `(subject_table, subject_id)` is
   unindexed and that one feed read is therefore a workspace-scoped scan. The new read adds a
   second such scan to every item page holding a closed gap, and its docstring (`:113–133`) does
   not mention it. One sentence there, or an open question with a phase owner, files the index
   once rather than having it found twice.
4. `scripts/run/review-scope.mjs` reported `"missing": ["/CheckList.dom.test.tsx",
   "/GapList.dom.test.tsx"]` for paths the ticket writes in full; both files exist. Its path reader
   appears to stop at the `[key]` segment, so every future review of a file under a bracketed route
   directory is told the checkout lacks it. Not this ticket's code — a Backlog Fix.
5. `docs/build-log.md:719` still states open question 14 in the present tense while this ticket
   answers it. The convention two questions down (`:998`) is to strike the question through and
   state the answer; the log entry step 8 writes is where to do it.
6. `CheckList.dom.test.tsx:449` asserts `document.body.textContent` matches no
   `/fail|violation|error|warning/i` over a render the test itself populated, so it can only redden
   on the new copy or on an existing label. Asserting against the notice's own node and the
   dictionary entry would say the same thing about the thing under test.

FINDINGS
