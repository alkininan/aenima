# T0.40 — review

Commit reviewed: `dabdf2a`

Read cold against `docs/tickets/T0.40.md` and design-spec v2.21 §2, §3, §5, §6, §17 (C-05, C-39),
at the repo's own header (v2.21 — no drift). The ticket's *Cited* sections are verbatim: §2, §3,
§5 and §6 in the ticket are byte-identical to the document at this commit.

What I checked myself, rather than taking from the diff's own words:

- Every custom property in **all** css blocks of §2, §5 and §6 (not only the first per section)
  exists in `src/app/globals.css`'s `:root` with the document's value, and the only properties
  `:root` carries beyond them are §4's seven `--z-*` — which are neither colour, radius, duration
  nor easing, so C-05's second direction holds.
- `src/lib/motion.ts` holds exactly §6's six script figures, each under the section that names it;
  the three components that used to carry one (`useCooldown.ts`, `useFieldValidation.ts`,
  `Toast.tsx`) now import it, and no `export const …_MS` outside `motion.ts` duplicates one.
- `scripts/fonts/check_fonts.py` over the two vendored faces in the repo: DM Sans 359 code points
  `tnum=True`, JetBrains Mono 438 code points, exit 0 — so ğĞşŞİıĳĲ per face and ⌘ ⇧ in the mono
  are really there, and the tnum clause is asked only of the two faces C-39 asks it of.
- `pnpm vitest run` over the six files `scripts/run/review-scope.mjs` lists (nothing `missing`):
  **5 files, 110 tests, all passing** (the sixth is the Playwright spec, not a vitest file).
- `next/font/local`'s loader in the installed Next 16.3.1 does accept a `font-family` declaration
  (`hasCustomFontFamily` in `dist/compiled/@next/font/dist/local/loader.js`), so the two-calls-one-
  family shape in `src/app/fonts.ts` is supported, not a guess.

## Findings

1. **Must — there is no `docs/reports/T0.40.md` at this commit, so no test in this diff has a
   red-first record.** Guidelines §5 step 8: the report is "refused without the red-first record:
   per test, the mutation that made it red and the count that went green". The diff adds
   `src/app/tokens.test.ts` (four C-05 assertions, two C-39 describes, 21 tests), one new
   `describe("cardClasses")` in `src/components/ui/composites.test.ts:359`, and
   `e2e/fonts.production.spec.ts` — 110 green tests here are evidence about the tests, not about
   the code. *Smallest correction:* write the report with one table row per new test naming the
   mutation and the counts, and — because the e2e spec is not in the Stop gate and I did not run
   it — say in the same table that `pnpm e2e`'s `production` project was run and what
   `check_fonts.py` printed through it. (Precedent: T0.27's pass 1 raised the same Must; this is
   the ordinary pass-1 finding, not a claim the work is wrong.)

2. **Must — the ticket's third Build bullet is half built, and the deferral is recorded only in a
   source comment.** The bullet reads "each face's Latin subset preloaded, and its Latin Extended
   subset too when the locale is TR or NL" (`docs/tickets/T0.40.md:32-33`). All four Latin Extended
   calls in `src/app/fonts.ts` carry `preload: false`, and the reason stands in that file's own
   docblock: "`preload` is read off the source the same way, so it cannot ask which locale is
   rendering — and nothing renders TR or NL today". I confirmed the premise — `src/i18n/` holds
   `en.ts` alone and `getDictionary()` defaults to `DEFAULT_LOCALE` — so the deferral is right.
   But AGENTS.md: "A stopgap is legal only when it is recorded in the build log as an open question
   with a phase owner. An unrecorded stopgap is a bug." *Smallest correction:* record it as an open
   question in the report and in `docs/log/T0.40.md`, naming §3's per-locale preload and the phase
   that owns per-request locale resolution. No code change.

## Shoulds

1. `src/app/tokens.test.ts:38-41` — `cssBlock()` takes "the first fenced css block in a section",
   while C-05 reads "the CSS blocks of §2, §3, §5 and §6". §5 has two; the second is the glass
   recipe, which declares no custom property, so nothing is missed today and the test is not
   vacuous. A token added to a section's second block would pass silently. `matchAll` over every
   block would close it.
2. `src/app/fonts.ts:98` and `src/app/globals.css:179-181` both say Space Grotesk's family name "is
   generated at build time", and that is why `--font-display` goes through `var(--font-space-
   grotesk)`. In the installed Next 16.3.1 that is not so: `dist/build/webpack/loaders/next-font-
   loader/postcss-next-font.js` keeps the family from Google's CSS verbatim and hashes only the
   `className`/`variable` idents. The indirection still resolves, so nothing is broken — but
   `e2e/fonts.production.spec.ts:60-65` asserts the family is literally `"Space Grotesk"`, which is
   the opposite claim. Both cannot be true; correct the comment to what the loader does.
3. `src/components/ui/variants.ts:395` — `export const TOOLTIP_SHOW_DELAY_MS = 500;` now duplicates
   §6's new `--delay-tooltip: 500ms`, which `globals.css:94` declares and nothing reads. The delay
   is a `setTimeout` in `Tooltip.tsx:50`, so it cannot read the token directly, and putting it in
   `motion.ts` would break that module's "no timer §6 does not name" direction — which is why this
   is a Should and not a Must. Worth naming for the tooltip's own ticket or C-06.
4. AC2 has two clauses; TC2 names only the fontTools half. The type-token half is covered — by
   `src/app/tokens.test.ts`'s `describe("C-39 · every §3 type token is in the scale")`, whose
   17-row `toEqual` guard keeps it from passing vacuously — but no TC names that test. Name it in
   the report's AC-to-test table so the mapping reads both ways.
5. Out of scope, for a Backlog Fix rather than this ticket: `src/app/globals.css:521-529`'s `.glass`
   carries `backdrop-filter` without the `-webkit-backdrop-filter` twin that §5's recipe states
   first ("the prefixed twin first, for the WebKit builds that still need it"). Pre-existing on
   `origin/main` and untouched by this diff, which only renamed the fallback token beneath it.

FINDINGS
