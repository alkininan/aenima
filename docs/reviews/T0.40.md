# T0.40 — review

Commit reviewed: `9ff3172`

Read cold against `docs/tickets/T0.40.md` and design-spec v2.21 §2, §3, §5, §6, §17 (C-05, C-39).
The document is unchanged on this branch and the ticket's *Cited* §2, §3, §5 and §6 are byte-identical
to it. This is pass 2; pass 1's two Musts are settled — `docs/reports/T0.40.md` now exists with one
row per new test, and the Latin Extended preload is open question 1 in the report and named in
`docs/log/T0.40.md` with the phase that owns it.

What I checked myself rather than taking from the report:

- `pnpm vitest run` over the six files `scripts/run/review-scope.mjs` lists (nothing `missing`):
  **5 files, 110 tests, all passing**; the sixth is the Playwright spec. Per-file counts match the
  report's red→green columns exactly — `src/app/tokens.test.ts` 24, `src/components/ui/composites.test.ts` 44.
- `scripts/fonts/check_fonts.py` over the two vendored faces in the repo: `DM Sans: 2 file(s), 359
  code points, tnum=True`, `JetBrains Mono: 2 file(s), 438 code points`, exit 0. The `fvar` tables
  agree with the declared ranges — DM Sans `wght 100–1000` against `weight: "100 1000"`, JetBrains
  Mono `wght 100–800` against `"100 800"` — and the name tables read 4.004 and 2.305, the versions
  `scripts/fonts/vendor.py` pins.
- `:root` occurs once in `src/app/globals.css`, so TC1's both-directions check cannot be dodged by a
  second block; outside it the only literals left are `0 0 #0000` (a null shadow), the `0ms` §6
  itself mandates, the autofill hack's `9999s`, and the floating label's two type sizes, which are
  open question 3.
- `hasCustomFontFamily` is real in the installed Next 16.3.1
  (`node_modules/next/dist/compiled/@next/font/dist/local/loader.js:33`), so the two-calls-one-family
  shape in `src/app/fonts.ts` is supported. No reference to the retired `--shadow-dropdown`,
  `--glass-fill-fallback`, `--texture-dot`, `--font-dm-sans` or `--font-jetbrains-mono` survives
  anywhere in `src/` or `e2e/`.

## Findings

1. **Must — AC2 asks that "every shipped woff2 covers ğĞşŞİıĳĲ" and the check reads per face, not
   per file; the reinterpretation is recorded only in a Python docstring.**
   `docs/tickets/T0.40.md:48-49` copies C-39 verbatim: "every shipped woff2 covers ğĞşŞİıĳĲ".
   `scripts/fonts/check_fonts.py:7-9` decides otherwise — "Per *face*, not per file: §3 splits every
   face into a Latin and a Latin Extended subset … so a file-by-file reading would fail the Latin
   file of a face that is entirely correct" — and `coverage()` unions the two files before testing.
   The reading is right: `src/app/fonts/dm-sans-latin.woff2` carries 221 code points and none of
   ğĞşŞİĳĲ, and it is §3's own instruction that put them in the other file, so C-39's sentence and §3
   cannot both be taken literally. But that makes it a Decision, and the ticket's Rules line says so:
   "one the build needs and cannot find there is a Decision, never a pick." `docs/reports/T0.40.md:11`
   reports AC2 as implemented without naming the divergence, `docs/log/T0.40.md` does not mention it,
   and nothing asks for C-39's wording to be reconciled. As it stands the conformance list says one
   thing and the check that proves it does another, with nobody having agreed.
   *Smallest correction:* a paragraph in the report — an open question, owner named, the way open
   questions 1–4 are already written — and a sentence in `docs/log/T0.40.md`, saying that C-39's
   "every shipped woff2" is read as "every shipped face" because §3 puts the letters in the Latin
   Extended subset, and that C-39's wording is the thing to amend. No code change.

## Shoulds

1. `src/app/tokens.test.ts:82` still carries the claim the report lists as fixed: "Space Grotesk's
   family name is generated at build time". `docs/reports/T0.40.md:67-71` says Should 2 of pass 1 was
   fixed "in `fonts.ts` and `globals.css`" — both were, and this third copy was not. It is the
   comment explaining why `familySpelling()` exists, so it is the one a reader reaches first.
   `e2e/fonts.production.spec.ts:57-61` asserts the family is literally `"Space Grotesk"`, which is
   the opposite claim.
2. `docs/build-log.md:111` records "`next/font/google` accepted as self-hosted per design spec v2.1
   §3; DM Sans preloaded alongside Space Grotesk." Neither clause is false after this diff, but both
   have stopped being decisions: v2.21 §3 names `next/font/google` as satisfying self-hosting outright,
   and requires every face's Latin subset preloaded, so DM Sans' preload is no longer the deviation
   that line was agreeing to. The Decisions section is the human's to edit; the report is what
   prompts it, and it says nothing. One line in the report would let the entry be retired.
3. TC1's §3 direction cannot see a font variable that nothing defines. `familySpelling()`
   (`src/app/tokens.test.ts:86-90`) reduces any `var(--font-x)` to `x`, so `--font-ui: var(--font-dm-sans)`
   would still equal §3's `'DM Sans'` — and `src/app/fonts.ts:133` names exactly that as the trap it
   is avoiding ("nothing later reaches for `--font-dm-sans` and gets a family that does not exist").
   Only `e2e/fonts.production.spec.ts`, which is outside the Stop gate, would catch it. Reading the
   variables `fonts.ts` actually declares would close it.
4. The Build's fourth bullet — "A component whose look changes with a token … changes with it and
   nothing more" (`docs/tickets/T0.40.md:34-36`) — has no Criteria line, so the test that covers it,
   `describe("cardClasses")` at `src/components/ui/composites.test.ts:358-364`, maps to no TC. It has
   a red-first row and is accounted for in the report's test table; the gap is in the ticket's
   Criteria, not in the work. Worth a criterion next time a ticket moves component literals onto tokens.
5. Two comments in `src/app/globals.css` restate the literals this diff just replaced with tokens:
   line 494, "§8 spinner — 2px stroke, 800ms linear rotation", over `animation: aenima-spin var(--t-spin) …`,
   and lines 502-503, "surface-2 base, rgba(255,255,255,.04) highlight, 1.2s linear loop", over
   `var(--hover-overlay)` and `var(--t-skeleton)`. They are true today and are the next thing to drift.

FINDINGS
