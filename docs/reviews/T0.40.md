# T0.40 — review

Commit reviewed: `f6a6879`

Read cold against `docs/tickets/T0.40.md` and design-spec v2.21 §2, §3, §5, §6, §17 (C-05, C-39).
This is pass 3. The ticket's *Cited* §2, §3, §5 and §6 are byte-identical to `docs/design-spec.md`
at this commit, and C-05 and C-39 are quoted verbatim; the spec is not in the diff, so the tests
are not reading a document this branch edited to fit them.

What I checked myself rather than taking from the report:

- `node scripts/run/review-scope.mjs docs/tickets/T0.40.md` lists six files and nothing `missing`.
  `pnpm vitest run` over the five vitest files: **5 files, 110 tests, all passing**. The sixth is
  the Playwright spec, which the Stop gate does not run.
- AC2's font clause, independently of the e2e. `scripts/fonts/check_fonts.py` over the committed
  subsets exits 0 — `DM Sans: 2 file(s), 359 code points, tnum=True`, `JetBrains Mono: 2 file(s),
  438 code points` — and per file: `dm-sans-latin-ext` and `jetbrains-mono-latin-ext` carry
  ğĞşŞİıĳĲ, `jetbrains-mono-latin` carries ⌘ and ⇧, `dm-sans-latin` carries `tnum`. I fetched
  Google's Space Grotesk myself, which only a build otherwise produces: its latin file carries
  `tnum` and its latin-ext file carries ğĞşŞİĳĲ (ı is in latin), so the third face passes the same
  check the e2e runs. AC2 holds in substance, not only by the report's word.
- The two Next behaviours `src/app/fonts.ts` rests on, in the installed 16.3.1: `font-family` in
  `declarations` is honoured and suppresses the generated family
  (`.../@next/font/dist/local/loader.js:33`), and `next/font/google` builds its CSS URL with no
  subset filter and downloads every subset, passing `subsets` only to `findFontFilesInCss` for
  preload (`.../google/loader.js:102`, `find-font-files-in-css.js:9`). So Space Grotesk's Latin
  Extended is served though only Latin is preloaded, which is what §3 asks for an EN page.
- Both directions of C-05 are real, not decorative: `:root` occurs once in `globals.css`, the
  reverse assertions cover `:root` and `@theme static`, and outside them the only literals left
  are `0 0 #0000`, the `0ms` §6 mandates, the pre-existing autofill `9999s`, and the floating
  label's two type sizes (open question 3).
- No reference to `--shadow-dropdown`, `--glass-fill-fallback`, `--texture-dot`, `--font-dm-sans`
  or `--font-jetbrains-mono` survives in `src/` or `e2e/`, and every new `@theme` key a component
  needs (`--radius-panel` for `rounded-panel`) is declared.
- Pass 1's and pass 2's Musts are settled: the red-first table has a row per new test with the
  mutation and the counts, the Latin Extended preload is open question 1 with a phase owner, and
  C-39's per-face reading is open question 5 with its owner and a paragraph in `docs/log/T0.40.md`.

No Must stands. Six Shoulds, ranked.

## Shoulds

1. **The card docstring quotes §5 at a version the diff was raising it from.**
   `src/components/ui/variants.ts:930-932` reads: `§5: "`--surface-1`, `--r-sm`, padding 16–20,
   optional `--glass-border`; cards also carry the inset edge highlight in `--edge-highlight-card`
   — quieter than glass."` The diff rewrote the tail of that quotation to v2.21 and left its middle
   at the old wording: v2.21 §5 says "padding 20 when the card stands on the page by itself … and
   16 when it sits inside another surface". Line 939 then restates the retired rule as fact — "§5
   gives padding as a range, so the two ends are both offered: 16 for a dense card in a list of
   them, 20 for one carrying a paragraph" — which is a different rule from the document's, and the
   one a reader picking a padding will follow. *Smallest correction:* quote v2.21's sentence and
   let the paragraph below it say standalone 20, nested 16.

2. **`--glass-border-hover` arrives as a token and as a Tailwind colour, and the one place that
   draws it keeps the literal.** `src/components/ui/variants.ts:69` still reads
   `"hover:not-disabled:border-[rgba(120,126,136,.72)]"`, under a comment that names it as "§7's
   glass hover border" — the exact value this diff added to `:root` as `--glass-border-hover` and
   mapped as `--color-glass-border-hover` in `@theme inline`. Nothing reads that map. The ticket
   defers component literals to C-06, which is why this is a Should and not a Must, but the diff
   made the opposite call one screen away for the card's edge highlight. *Smallest correction:*
   `hover:not-disabled:border-glass-border-hover`, which is what the new map exists for.

3. **Two comments outside `globals.css` still restate the shimmer's literals.** Pass 2's Should 5
   was fixed in the stylesheet only: `src/components/ui/Skeleton.tsx:12-13` ("a moving
   rgba(255,255,255,.04) highlight on a 1.2s linear loop") and
   `src/components/ui/variants.ts:798-799` ("surface-2 base, rgba(255,255,255,.04) highlight, 1.2s
   linear loop") describe a `.shimmer` that now reads `--hover-overlay` and `--t-skeleton`. True
   today, and the next things to drift. Name the tokens instead.

4. **`pnpm e2e` now needs a Python toolchain nothing outside a docstring mentions.**
   `e2e/fonts.production.spec.ts:75` shells out to `python3 scripts/fonts/check_fonts.py`, which
   imports fontTools and needs brotli to read woff2. The ticket's Done line is
   `… && pnpm e2e`, CLAUDE.md's Commands block lists `e2e pnpm e2e` with no prerequisite, and
   README has none either; a machine without fontTools gets a ModuleNotFoundError out of
   `execFileSync`. Failing loudly is the right choice — this is about saying where the dependency
   is, one line beside the command or in the report's open questions.

5. **A changed assertion with no row in the red-first record.**
   `src/components/ui/composites.test.ts:260-262` now asserts `rounded-panel` and
   `[--glass-elevation:var(--shadow-float)]` where it asserted `rounded-[12px]` and
   `--shadow-dropdown`. `docs/reports/T0.40.md:34-39` accounts for the diff's other touched test
   files as import-only, and the table carries the new `cardClasses` row, but this test's moved
   expectations appear nowhere. Nothing is at risk — both renames carry the same value — but the
   record claims to account for every test file the diff touches.

6. **The report's open questions run 1, 2, 3, 5, 4.** `docs/reports/T0.40.md:113-152`. Open
   question 5 is the one `docs/log/T0.40.md` points a reader at by number, so the ordering is worth
   a minute.

Standing from pass 2, unchanged and still correct to record: `docs/build-log.md:111`'s
`next/font/google` decision has stopped being a decision (the human's section to edit), and
`describe("cardClasses")` covers a Build bullet that has no Criteria line, so it maps to no TC.

PASS
