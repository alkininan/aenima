# T0.19 — review

Commit reviewed: `0c47b32` · branch `t0-19` · diff `main...HEAD`. Review pass 2.

Read: `docs/tickets/T0.19.md` and its four inlined Cited blocks against their sources —
product-spec §11 and §8 at `main`, guidelines §3 and §4 at the repo's v1.11 — all four match
their sources word for word (diffed, only the trailing `---` separators differ). Read every hunk
of `git diff main...HEAD`: three files, `docs/product-spec.md`, the ticket pack, and the pass-1
verdict this file replaces. `docs/product-spec.md` changes at three places and only three —
line 1 (title), line 9→9-10 (header comment), line 295→296 (§11's first paragraph).

`node scripts/run/review-scope.mjs docs/tickets/T0.19.md` lists no test files
(`{"files":[],"named":[],"touched":[],"missing":[]}`), so there was nothing to run and nothing is
missing from the checkout: this ticket verifies by grep and the report those greps live in is
step 8, after this review (guidelines §5). No vitest file is added or touched by the diff, so no
red-first record is owed. Criteria ↔ Tests maps both ways — AC1/TC1, AC2/TC2, AC3/TC3, no orphan
on either side.

Verified against the criteria:

- **AC1** — `docs/product-spec.md:296` states the comment path ("The confirm is a click or a
  word: a reply on the ticket that resolves to *ready* moves it") and names the three words with
  their verification ("*ready* … *apply* and *merge* … each is verified against the backlog
  itself: the reply is read from the database before the move is made, never from the model's
  account of the thread"). §8's ticket-generation clause is untouched.
- **AC2** — pass 1's Must is settled. Line 9's trailing ` -->` moved to the end of line 10, so
  the v1.7 line is inside the header comment: stripping HTML comments leaves the body between
  the title and `## 0.` empty, and the v1.7 text does not survive the strip. The line names the
  change and its sections, `(§11)` and `(§17)`. Its two outward claims check out — §17 carries
  "once aenima itself drives a coding agent from the backlog" and puts it at v2, and guidelines'
  own header records `ready` as a comment word landing at its v1.9.
- **AC3** — no other section changed; no renumbering; no correction history in the body. No test
  reads the real `docs/product-spec.md` (all three touching it use in-memory fixtures), so the
  title bump breaks nothing.

The diff touches `docs/product-spec.md`, which `scripts/run/gated.mjs` lists in `GATED_FILES`, so
this ticket waits at Review for the human's `merge` whatever this verdict says. The committed
copy of this file at `0c47b32` is pass 1's FINDINGS; the close must carry this pass's version.

## Should

1. **"The rest" lost its antecedent.** `docs/product-spec.md:296` reads "…never from the model's
   account of the thread. The rest are made by whoever does the work…". "The rest" means the rest
   of the *transitions*, but two sentences about three *words* now sit between it and the status
   set it refers to, so it reads first as "the rest of the words". Smallest fix: "The remaining
   transitions are made by whoever does the work", or put the three-words sentence after it.

2. **"resolves to *ready*" is looser than the rule it carries.** `docs/product-spec.md:296` says
   a reply "that resolves to *ready*" moves the ticket, one clause before "never from the model's
   account of the thread". On the dev board the test is literal — guidelines §3, "your newest
   reply begins with `ready`", and §4, "your *newest* reply … *begins* with the word". A reader
   building §11 from this paragraph could build the assessed version. This is the ticket's own
   wording (Build, `docs/tickets/T0.19.md:17`), so it is not a defect against the ticket — worth
   "a reply whose newest word is *ready*" next time the paragraph is open.

3. **§17 dates *apply* and *merge* but never defines them.** `docs/product-spec.md:296` bounds
   them to "once aenima drives a coding agent from the backlog (§17)". The citation is sound for
   the timing — §17 uses that phrase and puts it after v1 — but §17 is about run telemetry, so a
   reader who follows it never learns what *apply* applies or what *merge* merges. Naming two
   words with consequences the spec does not define is a claim worth the human's eye at `merge`,
   and worth defining when v2's development stage is written.

4. **`docs/guidelines.md:623` under-reports the carry.** The heading reads "## 9. Carried into the
   product spec (v1.6)" over six rulings; this is the seventh and it landed at v1.7. Precedent
   ties the two together — `4d5b714` wrote that section in the same commit that cut v1.6 — but
   this ticket's Build names `docs/product-spec.md` only, so fixing it here would be scope the
   ticket does not carry. Per guidelines §5 step 5, a Backlog task (Type Fix, Epic inherited).

5. **Done names four greps, Tests names three cases.** `docs/tickets/T0.19.md:42` asks for "the
   four greps in the report" while line 38 maps TC1–TC3 to AC1–AC3. The fourth check has no
   criterion — the orphan §7.5 rules out one level down. The report should say which grep
   verifies which AC, and drop or attach the fourth.

6. **§1 law 4 still says confirms are taps.** "One-tap confirms, always undoable"
   (`docs/product-spec.md`, §1 law 4) now sits beside a §11 confirm that is a word on a thread
   and says nothing about undoing it, and §1 says the law wins where a conflict appears. The
   ticket's Rules forbid rewriting paragraphs outside the one named, so this is not a defect in
   the diff — one clause in law 4, or one in §11, would settle it next time either is open.

PASS
