# T0.19 — review

Commit reviewed: `7ac95e1` · branch `t0-19` · diff `origin/main...HEAD` (`origin/main` at `077cda4`, confirmed current with `git ls-remote`). Re-close round, review pass 1, cold read.

## What was read and run

`docs/tickets/T0.19.md` whole, the Addendum included. I checked its Cited blocks against the sources: product-spec §11 and §8 against `origin/main`, and the addendum's guidelines §4 and §5 against `origin/main`'s `docs/guidelines.md` at v1.13. They match word for word; only the `---` separators differ.

`git diff main...HEAD` also shows T0.13's ticket, report, review and log, plus guidelines hunks. The worktree's `main` (`929e336`) is one merge behind `origin/main`. `git diff origin/main HEAD --stat` names six files, all T0.19's, so those extra hunks come from main and not from this branch. The merge at `e18e067` dropped nothing from main. I read every hunk of the six: `docs/product-spec.md`, `docs/build-log.md`, `docs/log/T0.19.md`, `docs/reports/T0.19.md`, `docs/reviews/T0.19.md` (which this file replaces) and the ticket pack.

What I ran:
- `node scripts/run/review-scope.mjs docs/tickets/T0.19.md` returns `{"files":[],"named":[],"touched":[],"missing":[]}`. There is no vitest file to run and none missing.
- `docs/product-spec.md` is byte-identical to the first round's `cf01984`.
- G1–G4 on HEAD: 1 · 1 · 2 · 0, with G4 at both `main` and `origin/main`.
- H (`git diff -U0 origin/main -- docs/product-spec.md | grep -c '^@@'`) is 3. The hunks are `@@ -1 +1 @@`, `@@ -9 +9,2 @@` and `@@ -295 +296 @@`, and line 296 is inside §11 (lines 287–303).
- With HTML comments stripped, nothing is left between the title and `## 0.`, so the v1.7 line is still inside the header comment.
- `git merge-tree --write-tree --name-only HEAD origin/main` is clean. The same command from `cf01984` conflicts on `docs/build-log.md`, as TA1's row says.
- `node scripts/run/log-index.mjs --check` returns `stale: false`.
- `node scripts/run/gated.mjs` (range `origin/main...HEAD`, calling `loosenedBy`) returns `ok: true`, `gated: []`, `reasons: []`. `cf01984`'s `gated.mjs` listed `docs/product-spec.md` in `GATED_FILES`, as TA3's row says.
- `node scripts/run/report-check.mjs docs/reports/T0.19.md` returns `ok: true` over 7 rows.
- I re-ran TA2's mutation on a scratchpad copy: G4 goes to 2 and H to 4.

Criteria ↔ Tests map both ways: AC1/TC1 (G1, G2), AC2/TC2 (G3), AC3/TC3 (G4), AA1/TA1, AA2/TA2, AA3/TA3. No orphan on either side. Every test has a row with all three cells filled.

## Against the criteria

- **AC1–AC3 — met, unchanged from the first round.** The spec file is identical to `cf01984`. §11 carries the comment path and the three words with their verification, and §8's ticket-generation clause is untouched.
- **AA1 — met.** The branch merges cleanly into current main, and the four greps pass on the merged tree.
- **AA2 — met.** There are three hunks: the title, the header comment, and §11's first paragraph.
- **AA3 — met as far as it can be before the close.** The diff adds no migration and weakens no restraint by main's measurement, and this file supplies the PASS. The self-merge, Done and the Release row are the close itself, and no review can read them before they happen.

## Should

1. **The log entry still says this ticket is gated.** `docs/log/T0.19.md:35-36` reads "The diff touches `docs/product-spec.md`, so it is gated: the run does not merge its own spec change. It waits at Review for the word." The Addendum says the opposite, and this merge would put the sentence on main. `docs/reports/T0.19.md:29` also counts "the log entry" among this round's additions, but the file is byte-identical to `cf01984`. Nothing re-reads either file after this pass. Smallest correction, at step 8 before the close commit: replace or follow that paragraph with the re-close (ungated under T0.21, merged on the reviewer's door). Failing that, drop "the log entry" from report line 29.

2. **The AA3 row points at a section the report does not have.** `docs/reports/T0.19.md:41` ends "…is the close itself and is recorded under *Close* below". The report's sections end at `## Open questions`. Smallest correction, at step 8: add the `## Close` section with what can be on record before the merge commit (the gate's green for the tree, the PASS on file, `gated.mjs` `ok: true`), or reword the row to name where the rest lands (the board's Done, the Release row).

3. **TA3's red comes from a different detector, not from a mutation main's detector catches.** At `docs/reports/T0.19.md:71`, the `ok: false` is `cf01984`'s pre-T0.21 `gated.mjs`, and the guard never runs that copy. It shows the rule changed, not that the check judging this close can go red on this branch. A stronger red: a scratch commit adding `drizzle/9999_probe.sql`, or `node -e` over `gatedDiff` with that file in `files`, gives `ok: false` naming the migration, and `ok: true` without it.

4. **G4 sees heading lines only.** `docs/reports/T0.19.md:60`: a one-word body edit reads G4 0 and H 4. H with its hunk addresses is what proves AC3. TA2 records that, but TC3's row names only G4. The code block also still diffs against `main`, while the Addendum reads the base as `origin/main`. Smallest correction: name H under TC3 as well, and write `origin/main` in the code block.

5. The first round's Shoulds 1–4 (report, "Shoulds standing") still stand as recorded. This round changes no line they are about.

PASS
