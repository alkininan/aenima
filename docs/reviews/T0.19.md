# T0.19 — review

Commit reviewed: `f7e7e37` · branch `t0-19` · diff `main...HEAD`.

Read: `docs/tickets/T0.19.md`, its four inlined Cited blocks against their sources
(product-spec §11 and §8 at `main`, guidelines §3 and §4 at the repo's v1.11) — all four match
their sources word for word — and every hunk of `git diff main...HEAD`. The diff is two files:
`docs/product-spec.md` (the title line, one header line, one paragraph of §11) and the ticket
pack itself.

`node scripts/run/review-scope.mjs docs/tickets/T0.19.md` lists no test files
(`{"files":[],"named":[],"touched":[],"missing":[]}`), so there was nothing to run: this ticket
verifies by grep, and the report those greps live in is step 8, after this review. Nothing is
missing from the checkout. Criteria ↔ Tests maps both ways — AC1/TC1, AC2/TC2, AC3/TC3, no
orphan on either side. The diff touches `docs/product-spec.md`, which `scripts/run/gated.mjs`
lists in `GATED_FILES`, so this ticket waits at Review for the human's `merge` whatever this
verdict says.

## Must

**1. The v1.7 header line is outside the header comment — it renders as body prose.**
`docs/product-spec.md:9` still ends with the comment's close:

    ...v1 reads completion back and never runs the agent (§17). -->

and the new line was appended after it, `docs/product-spec.md:10`:

    v1.7: Backlog→Ready gains its comment path. ... has run this path since its v1.9.

The comment opens at line 3 and closes at the end of line 9, so line 10 sits outside it and
renders as a visible paragraph between `# aenima — Product Specification v1.7` and
`## 0. What aenima is` — in the file and in the Notion mirror `mirror.mjs` writes. That is not
the ticket's "one header-comment line in the existing style" (Build, line 22) and it is the one
thing the same line forbids, "No correction history in the body". AC2 does not stand. The
existing style is visible in how v1.6 landed (`4d5b714`): the previous line's ` -->` was removed
and the new line carried it.

Smallest correction: drop the trailing ` -->` from line 9 and append ` -->` to line 10.
The check that would have caught this is not "the v1.7 line exists" but "the v1.7 line is inside
the comment" — e.g. the last `-->` in the header block falls on the v1.7 line, or line 10 does
not appear in the rendered body.

## Should

1. **"resolves to *ready*" leaves the reading to an assessment the next clause denies.**
   `docs/product-spec.md:296` reads "a reply on the ticket that resolves to *ready* moves it",
   and the sentence after it says the word is read "never from the model's account of the
   thread". The rule this carries is literal on the dev board — guidelines §3, "your newest
   reply begins with `ready`", and §4, "your *newest* reply since the pipeline's last comment on
   that thread *begins* with the word". A reader building §11 from this paragraph could build the
   assessed version. The ticket's own Build line says "resolves to *ready*", so this is the
   ticket's wording and not a defect against it — worth "a reply whose newest word is *ready*"
   next time the paragraph is open.

2. **The §17 clause is an addition the Build lines did not ask for.** `docs/product-spec.md:296`
   says *apply* and *merge* "land work once aenima drives a coding agent from the backlog (§17)".
   The citation is sound — §17 uses that exact phrase and puts it after v1 — and bounding the two
   words to post-v1 is more honest than leaving them undated. But Build line 19 asked only that
   the three words be named and said to be verified against the backlog; that aenima will act on
   *apply* and *merge* at all is a new product claim in a gated document. Worth the human's eye
   at `merge`.

3. **The header line claims for v1 what the body qualifies.** Line 10 says the three words are
   named and "each is verified against the backlog itself" with no mention that two of them wait
   on §17, while line 296 says they do. One clause in the header line would settle it.

4. **`docs/guidelines.md:623` still reads "Carried into the product spec (v1.6)"** and lists six
   rulings; this is the seventh carry and it landed at v1.7. Out of this ticket's scope — its
   Rules name only the one product-spec paragraph — so a Backlog task, not a change here.

5. **Done names "the four greps", Tests names three cases.** The fourth check has no criterion,
   which is the orphan guidelines §6 rules out one level down. The report should say which grep
   verifies which AC.

FINDINGS
