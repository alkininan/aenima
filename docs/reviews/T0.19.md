# T0.19 — review

Commit reviewed: `e1fe317` · branch `t0-19` · diff `main...HEAD`. Review pass 3, cold read.

## What was read and run

`docs/tickets/T0.19.md` and all four of its Cited blocks, diffed line by line against their
sources — product-spec §11 and §8 at `main`, guidelines §3 and §4 in the repo (v1.11). All four
match word for word; only the trailing `---` separators differ. Every hunk of
`git diff main...HEAD`: three files, `docs/product-spec.md`, the ticket pack, and the pass-2
verdict this file replaces. `docs/product-spec.md` changes at three places and only three —
line 1 (title), line 9 → lines 9-10 (header comment), line 296 (§11's first paragraph): four
insertions, three deletions, which is the whole of the file's `7 +-`.

`node scripts/run/review-scope.mjs docs/tickets/T0.19.md` returns
`{"files":[],"named":[],"touched":[],"missing":[]}` — the ticket names no test file and the diff
touches none, so there was nothing to run and nothing the checkout is missing. No vitest test is
added, so no red-first record is owed (guidelines §5 step 4); the report that carries the greps is
step 8, downstream of this review. Criteria ↔ Tests maps both ways: AC1/TC1, AC2/TC2, AC3/TC3,
no orphan on either side.

Nothing in the repo reads the spec's version at runtime: `scripts/run/version-drift.mjs` takes the
first `vN.N` in the first six lines, which is still line 1's, and every test that mentions
`product-spec` (`version-drift.test.mjs`, `mirror.test.mjs`, `spec-sections.test.mjs`,
`gated.test.mjs`) uses an in-memory fixture. No section was renumbered, so the ~30 source comments
citing `product-spec.md §N` still land where they point.

## Against the criteria

- **AC1 — met.** `docs/product-spec.md:296` states the comment path — "The confirm is a click or
  a word: a reply on the ticket that resolves to *ready* moves it" — and names the three words
  with their verification: "*ready*, which confirms a ticket, and *apply* and *merge* … each is
  verified against the backlog itself: the reply is read from the database before the move is
  made, never from the model's account of the thread." §8's ticket-generation clause
  (`docs/product-spec.md:247`) is untouched, as the Build requires.
- **AC2 — met.** The v1.7 line is line 10, inside the header comment: line 9's trailing ` -->`
  moved to the end of line 10, so nothing between the title and `## 0.` survives stripping HTML
  comments. The line names the change and cites `(§11)` and `(§17)` in the existing style, and
  its two outward claims hold — §17's v2 bullet does carry "once aenima itself drives a coding
  agent from the backlog", and guidelines' own header at v1.9 records "§3 ready as a comment
  word, Backlog → Ready on it; §4 three words verified in code".
- **AC3 — met.** No other section changed, no renumbering, no correction history in the body.
  The Rules hold: "Decision" is still a ticket state and §11's Decision paragraph is byte-identical.

The diff touches `docs/product-spec.md`, which `scripts/run/gated.mjs` lists in `GATED_FILES`, so
this ticket stays at Review for your `merge` whatever this verdict says (guidelines §5 step 9).

## Should

1. **Done names four greps; Tests names three cases.** `docs/tickets/T0.19.md:42` asks for "the
   four greps in the report" while line 38 maps TC1 → AC1 · TC2 → AC2 · TC3 → AC3. If the report
   lands with four greps against three criteria, the fourth is a test case that names no
   criterion — the orphan §7.5 rules out one level down. Smallest correction, at step 8: say which
   grep verifies which AC, and drop or attach the fourth. The pack also leaves TC1–TC3 as bare
   identifiers with no stated procedure, so the report is the only place the verification will
   exist; I have verified all three criteria by reading the file and the diff.

2. **"resolves to *ready*" is looser than the clause beside it.** `docs/product-spec.md:296` says
   a reply "that resolves to *ready*" moves the ticket, one clause before "never from the model's
   account of the thread". "Resolves to" is an assessment; the rule the ticket carries over is
   literal — guidelines §3, "your newest reply begins with `ready`", and §4, "your *newest* reply
   … *begins* with the word". As written, §11 says where the reply is read from but not that the
   test is mechanical, which is the half the objective wanted tight. This is the ticket's own
   wording (`docs/tickets/T0.19.md:17`), so it is not a defect against the ticket — worth "a reply
   whose newest word is *ready*" next time the paragraph is open.

3. **The three-words sentence sits in front of a sentence it strains.** `docs/product-spec.md:296`
   runs "…never from the model's account of the thread. The remaining transitions are made by
   whoever does the work, a developer on their own board or a coding agent working from §9's
   pack…". Two of those remaining transitions are the ones *apply* and *merge* land, which the
   previous sentence just put on the human's word, so "made by whoever does the work" reads as
   contradicting it. Smallest correction: move the three-words sentence to the end of the
   paragraph's transition run, after "…read back the way completion already is."

4. **§17 dates *apply* and *merge* and never defines them.** `docs/product-spec.md:296` bounds
   them to "once aenima drives a coding agent from the backlog (§17)". The citation is sound for
   the timing — §17's v2 bullet uses that phrase — but §17 is about run telemetry, so a reader who
   follows it never learns what *apply* applies or what *merge* merges in the product. Two words
   with consequences and no definition is a claim worth your eye at `merge`, and worth defining
   when v2's development stage is written. The v1/post-v1 split itself is a reading the ticket did
   not ask for (`docs/tickets/T0.19.md:19-21` says only "name the three words"); it is the right
   call, since naming them unqualified would have claimed a v1 aenima that merges and applies, but
   the report should carry it as a default taken (§4) rather than leave it silent.

5. **`docs/guidelines.md:623` under-reports the carry.** The heading reads "## 9. Carried into the
   product spec (v1.6)" over six rulings; this is the seventh and it lands at v1.7. Precedent ties
   the two together — `4d5b714` wrote that section in the commit that cut v1.6 — but this ticket's
   Build names `docs/product-spec.md` only, so amending it here would be scope the ticket does not
   carry. Per guidelines §5 step 5, a Backlog task (Type Fix, Epic E0.2 inherited).

6. **§1 law 4 still says confirms are taps.** "One-tap confirms, always undoable"
   (`docs/product-spec.md:44`) now sits beside a §11 confirm that is a word on a thread and says
   nothing about undoing it, and §1 says the law wins where a conflict appears. The Rules forbid
   rewriting paragraphs outside the one named, so this is not a defect in the diff — one clause in
   law 4, or one in §11, would settle it next time either is open.

PASS
