# T0.39 — review

Commit reviewed: `0e20e9a` (pass 1)

Read cold against `docs/tickets/T0.39.md` — Build, Rules, AC1–AC4, TC1–TC4 — and what its Spec
line names at the versions the checkout holds: `scripts/run/health.mjs`, `.claude/skills/ticket/SKILL.md`
step 0 **e. The deploy.** (`:200-227`), and guidelines §5 at v1.27 on `origin/main` (the ticket's
Cited section) and at v1.28 on this branch.

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: six files, 501 insertions, 17 deletions, one commit
  past `origin/main`. `health.mjs` (`assess` gains `outcome` and `unanswered`; `health()` returns
  before writing the record on unknown), `health.test.mjs`, the skill's step e, guidelines header
  and two §5 passages, `scripts/run/README.md`, and the ticket file. No hook, guard, gate,
  `.claude/settings.json` or migration touched. No `docs/reports/T0.39.md`, no `docs/log/T0.39.md`.
- `node scripts/run/review-scope.mjs docs/tickets/T0.39.md` → `scripts/run/health.test.mjs`,
  nothing `missing`. `pnpm vitest run scripts/run/health.test.mjs` → 1 file, 16 passed, 289 ms.
- `revert.mjs`, `gated.test.mjs` and `loosening.test.mjs` name `health.mjs` only as a path or in a
  comment; nothing outside this file reads `assess`'s shape. The skill is the one consumer of the
  CLI's output, and it now reads `outcome` (`SKILL.md:207-211`); no `ok` from `health.mjs` is read
  anywhere (grep over `scripts/`, `.claude/`).
- Criteria ↔ Tests: AC1 ← TC1 (`health.test.mjs:41`, `:142`); AC2 ← TC2 (`:30`, `:129`); AC3 ← TC3
  (`:142` — record absent after unknown, same commit asked again, `asked` is 4); AC4 ← TC4 (`:209`,
  `:215`, reading the two documents through anchors that exist — `SKILL.md:200`/`:229`,
  guidelines **The deploy check.**/**The marker** — and fail loudly if an anchor goes). Every
  criterion has a test. One new test names no criterion (Should 4).
- Ran `health()` against the waiting branch myself (`now` a minute after the commit): it returned
  `{"outcome":"up","ok":null,"waiting":true,"changed":true}`. Must 1.
- The ticket's Build says `probe` keeps a silence as `status: null` — unchanged code, covered by the
  T0.16 probe test at `:71`, still true.

## Findings

1. **Must** — A result that asked nothing says `outcome: up`. `scripts/run/health.mjs:140-146`, the
   deploy-window branch, returns `{ ...first, ok: null, waiting: true }`, and `first` is
   `assess({ commit, checked, results: [] })` with `changed` true — no wrong answers, no silences,
   so `assess` calls it `"up"` (`:60-66`), and only `ok` is put back to null. The ticket defines up
   as "every check answered as expected"; none was asked. The doc comment at `:53` — "`outcome`
   and `ok` are null when nothing was asked" — is false for this branch. A run reading the skill's
   order (`SKILL.md:205-207`, `waiting` before `outcome`) reverts nothing, so the consequence today
   is a false `up` in the emitted record and in any report line that quotes it; a reader who takes
   `outcome` first is told the deploy is up without a probe. Smallest correction: `outcome: null`
   beside `ok: null` at `:143`, and `outcome: null` in the waiting test's `toMatchObject` at
   `health.test.mjs:171` so it stays that way.

2. **Must** — The guidelines' step-0 block now says unknown files a Fix task and moves the ticket
   back. `docs/guidelines.md:471-475`:

       a wrong answer (down) reverts the merge at the tip
       (revert.mjs, then the one push to main the guard lets through, HEAD:main),
       and no answer (unknown) reverts nothing and is asked again next run,
       files one Fix task at Backlog, puts the reverted ticket back at Backlog, one
       comment

   Before this diff "files one Fix task … puts the reverted ticket back … one comment" listed what
   a wrong answer does. The inserted clause now stands between that list and its subject, so read
   as written the tail hangs off "no answer (unknown)". AC4 asks the guidelines to state the rule;
   this passage states its opposite for the Fix task and the ticket's status. The skill
   (`SKILL.md:208-211`) and the prose paragraph (`guidelines.md:615-622`) are right, and the run
   follows the skill, which is why I am not sure how much this reaches — but the guidelines are
   the protocol (`.claude/rules/harness.md`), and the mirror will carry this sentence. Smallest
   correction: move the clause after the list — "… one comment; no answer (unknown) reverts
   nothing and is asked again next run · then refresh …".

3. **Must** — No red-first record. The ticket's Tests line promises "each reddened by a named
   mutation" and its Report line names `docs/reports/T0.39.md`; neither the report nor its *Tests
   written* table exists at `0e20e9a`. The tests the diff adds or changes — `health.test.mjs:24`,
   `:30`, `:41`, `:129` (assertions changed), `:142`, `:209`, `:215` — have no row naming the
   mutation that made each red and the count that went green, so each is a passing test and
   nothing more. I know step 8 follows step 5 in §5, and T0.40's first pass raised the same thing
   and it was settled by its third; it stands until the record is on disk. Smallest correction:
   write the report with one row per test above before the next pass.

4. **Should** — One case asserted twice, one of them unlabelled. `health.test.mjs:19-27`: "is ok
   when every check answered what it should" and "is up when every check answered what it should"
   run the same `results(200, 307)` and split one result's fields across two tests; the second
   carries no TC. Fold `outcome: "up", unanswered: []` into the first, or label the second — the
   ticket's mapping runs both ways.

5. **Should** — `ok` is now a second spelling of `outcome`. `health.mjs:67` derives it, `:54`
   documents it as "true for up, false for down and null for unknown", and nothing reads it: the
   skill moved to `outcome` in this same diff. AGENTS.md's first build principle says remove the
   old path in the same change. Not a Must because `ok` gives no wrong answer — false only on down
   — but `ok: null` now means three things (not asked, waiting, unknown), and the T0.16 tests that
   still assert on it (`:122`, `:125`, `:138`, `:171`) are what keeps it alive.

6. **Should** — A stale comment. `health.mjs:72-74`: "One silence … is asked again before it
   counts: a revert on a blip is a merge nobody wanted undone." After this ticket a silence never
   counts toward a revert; the second attempt now buys an answer rather than protects against a
   wrong one. One clause to reword.

FINDINGS
