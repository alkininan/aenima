# T0.39 — review

Commit reviewed: `81fbfc3` (pass 3)

Read cold against `docs/tickets/T0.39.md` — Objective, Build, Rules, AC1–AC4, TC1–TC4 — and what
its Spec line names: `scripts/run/health.mjs`, `.claude/skills/ticket/SKILL.md` step 0 **e. The
deploy.** (`:200-227`), and guidelines §5 at v1.27 (the ticket's Cited section) and at v1.28 on
this branch (header, step-0 row `:466-478`, *The deploy check* `:610-623`).

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: twelve files, four commits past `origin/main`.
  `health.mjs` — `assess` returns `outcome` and `unanswered`, `failed` holds wrong answers alone
  (`:57`), `ok` is gone; `health()` returns before the record write on unknown (`:149-156`) and
  the waiting branch says `outcome: null` (`:141`). `health.test.mjs`, `claims.test.mjs` (T0.34's
  TC7 finds its entry by ticket rather than taking the newest), the skill's step e, the
  guidelines' header, step-0 row and deploy-check paragraph, `scripts/run/README.md`,
  `.claude/rules/harness.md` (one line, ending in `docs/log/T0.39.md`, which exists),
  `docs/build-log.md` (Specs v1.28, newest entry, list line), the log entry, the ticket, the
  report, pass 2's verdict. No hook, guard, gate, settings or migration touched. Every file is
  named in the ticket or the report; nothing is unaccounted for.
- `node scripts/run/review-scope.mjs docs/tickets/T0.39.md` → `scripts/run/claims.test.mjs`,
  `scripts/run/health.test.mjs`; nothing `missing`. `pnpm vitest run` on those two → 2 files,
  50 passed: 15 in health, 35 in claims — the totals every row of the report's red-first table
  goes green to.
- Criteria ↔ Tests, both directions. AC1 ← TC1 at `health.test.mjs:19`, `:37`, `:138`, `:158`;
  AC2 ← TC2 at `:25` (down with a silent sibling, `failed` holds `/sign-in` alone) and `:124`
  (record written, `failedText` names the wrong answer, the same commit is `changed: false` next
  run); AC3 ← TC3 at `:138` (record absent after unknown, the same commit asked again, `asked` is
  4, recorded on up); AC4 ← TC4 at `:203` and `:209`, through anchors that exist (`**e. The
  deploy.**` at `SKILL.md:200`, `**f. ` at `:229`; `**The deploy check.**` and `**The marker**` in
  the guidelines) and leave the slice empty if they go, so the test fails loudly. Every
  T0.39-labelled test — nine, the claims fix included — has a row in the *Tests written* table
  with `reddened by` and `red → green` filled; I traced each mutation to the tests it leaves red
  and the counts hold (row 2's three are `:19`, `:115`'s `first` and `:138`'s `second`; row 4's
  two are `:25` and `:124`; row 5's one is `:138`'s `why` and record).
- Nothing outside `health.mjs` reads `assess`'s shape: grep over `scripts/`, `.claude/skills`,
  `.claude/agents`, `scripts/hooks`. The skill branches on `outcome` (`SKILL.md:207-212`); the
  `ok: false` at `SKILL.md:222` is `revert.mjs`'s return (`revert.mjs:35-36`).
- The header's claims: `origin/main` holds v1.27, `origin/t0-26` holds v1.25 — both read from
  git. The step-0 row's widest line is 93 columns.
- The Rules line — a change to the pipeline's scripts lands on the human's `merge` — is
  `gated.mjs`'s, and `gated.test.mjs:79` uses `scripts/run/health.mjs` itself as its fixture, so
  this diff waits for the word whatever this file says.

## Findings

No Must stands. Four Shoulds:

1. **Should** — `assess`'s contract and its behaviour part over an empty `results`.
   `health.mjs:53` says "`outcome` is null when nothing was asked"; `health.mjs:59-65` reads
   `assess({ commit: "a", checked: "b", results: [] })` as `"up"` — nothing failed and nothing
   was unanswered, so the last branch wins. `health.mjs:130` builds exactly that value as `first`,
   and the waiting branch at `:141` overrides it to null by hand — which is how pass 1's Must was
   settled, at the caller rather than in the function whose comment makes the promise. No caller
   observes the "up", so no wrong result reaches the skill. Smallest correction: in `assess`,
   `const outcome = !changed || results.length === 0 ? null : …`; the override at `:141` then
   says nothing the function did not already, and `:158-170` still holds.

2. **Should** — `describeFailed` now describes silences. Its comment (`health.mjs:98`) says "The
   failures as one clause", and `:154` hands it `result.unanswered`, so the unknown `why` opens
   "/app answered nothing rather than 307" — a shortfall against an expectation, in the one line
   whose point is that the silence is no shortfall. One phrasing for silences ("/app never
   answered") or one word in the comment saying it names silences too.

3. **Should** — On down with a silent sibling, `failedText` (`health.mjs:162`) names the wrong
   answer alone, so the Fix task's `reason` and the `reverted` comment's `failed`
   (`SKILL.md:225-227`) read as if the other check answered well. The wrong answer is the right
   revert signal and the ticket asks for nothing more; the record would be truer with the
   unanswered clause appended when `unanswered` is not empty.

4. **Should** — `docs/reports/T0.39.md:56` says "Pass 3 reviewed that change." and the passes
   table (`:35-38`) holds passes 1 and 2. This pass is the third; §5 step 8 wants one row a
   pass, and `report-check.mjs` refuses the report without it. One row: `3 · 81fbfc3 · fable ·
   no · PASS`.

PASS
