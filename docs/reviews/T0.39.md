# T0.39 — review

Commit reviewed: `341832e` (pass 2)

Read cold against `docs/tickets/T0.39.md` — Build, Rules, AC1–AC4, TC1–TC4 — and what its Spec
line names: `scripts/run/health.mjs`, `.claude/skills/ticket/SKILL.md` step 0 **e. The deploy.**
(`:200-227`), and guidelines §5 at v1.27 (the ticket's Cited section) and at v1.28 on this branch.

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: eight files, two commits past `origin/main`.
  `health.mjs` (`assess` gains `outcome` and `unanswered`, `ok` is gone; `health()` returns
  before writing the record on unknown; the waiting branch says `outcome: null`),
  `health.test.mjs`, the skill's step e, the guidelines' header, step-0 row and *deploy check*
  paragraph, `scripts/run/README.md`, the ticket, the report and pass 1's verdict file. No hook,
  guard, gate, `.claude/settings.json` or migration touched. No `docs/log/T0.39.md` yet — step 8's,
  after this pass.
- `node scripts/run/review-scope.mjs docs/tickets/T0.39.md` → `scripts/run/health.test.mjs`,
  nothing `missing`. `pnpm vitest run scripts/run/health.test.mjs` → 1 file, 15 passed, 393 ms.
  Fifteen is the total every row of the report's red-first table sums to.
- Nothing outside `health.mjs` reads `assess`'s shape (grep over `scripts/`, `.claude/`,
  `docs/guidelines.md`); the skill is the one consumer of the CLI's output and it branches on
  `outcome` (`SKILL.md:207-211`). The `ok: false` at `SKILL.md:222` is `revert.mjs`'s, not health's.
- Pass 1's three Musts, checked in the code and not in the report: the waiting branch now returns
  `outcome: null` (`health.mjs:141`) and the waiting test asserts it (`health.test.mjs:167`); the
  step-0 clause sits after "one comment" and says unknown "reverts nothing, files nothing, records
  nothing" (`guidelines.md:474-475`); the report holds a *Tests written* table with `reddened by`
  and `red → green` columns, every cell filled, the test file named — the shape
  `report-check.mjs` reads.
- Criteria ↔ Tests: AC1 ← TC1 (`health.test.mjs:19`, `:37`, `:138`); AC2 ← TC2 (`:25`, `:124` —
  down whether or not another check was silent, `failedText` names the wrong answer alone, the
  record is written and the same commit is `changed: false` next time); AC3 ← TC3 (`:138` — record
  absent after unknown, the same commit asked again, `asked` is 4, then recorded on up); AC4 ← TC4
  (`:203`, `:209`, through anchors that exist and fail loudly if they go). Every criterion has a
  test; the two gaps in the other direction are Shoulds 1 and 2.
- Red-first: rows 1, 3 and 4 are mutations of `assess`'s filter, `assess`'s down branch and
  `health()`'s unknown return; I traced which tests each leaves red and the counts hold (row 1's
  three are `:37`, `:138` and `:25`'s `both` case, whose `failed` would hold two paths). Rows 5 and
  6 redden the two document tests by the exact words they assert.
- The `why` on unknown reads "/app answered nothing rather than 307: the site could not be reached
  from this machine, which is no verdict on the merge; asked again next run" — what the ticket's
  third Build bullet asks the report line to say, and what the skill tells the run to quote.

## Findings

No Must stands. Four Shoulds:

1. **Should** — One TC1 test has no red-first row. `health.test.mjs:19-23`, "is up when every check
   answered what it should", is labelled `T0.39 TC1 → AC1` and listed under TC1 in the report's AC1
   row (`docs/reports/T0.39.md:12`), and no row of the *Tests written* table names a mutation that
   reddens it: rows 1, 3 and 4 all leave `results(200, 307)` at `up`. Its only recorded red is the
   preamble's aggregate run against the old code (`:19-20`), a red for a missing key rather than a
   wrong outcome. Smallest correction: one row — `"up"` swapped for `"unknown"` in `assess`'s last
   branch (`health.mjs:65`), 1 failed / 14 passed → 15 passed.

2. **Should** — A test the report claims is unlabelled in the file. `health.test.mjs:158`, "waits,
   asking and recording nothing…", is the second TC1 row of the red-first table (`report:25`) and
   TC1 evidence in the AC1 row, and carries no `T0.39 TC1 → AC1` comment; the ticket's mapping runs
   both ways. One comment line above it.

3. **Should** — `docs/guidelines.md:475` is 108 columns in a step block hand-wrapped at 93 or
   fewer, and the mirror renders the block as code. Break before "· then refresh".

4. **Should** — `docs/reports/T0.39.md:47` says "The ticket cites `docs/guidelines.md` with no
   version"; the ticket's Spec line (`docs/tickets/T0.39.md:4`) cites "guidelines v1.27 §5". One
   clause: the ticket cited v1.27, the repo was at v1.27 at claim, and this change takes it to v1.28.

PASS
