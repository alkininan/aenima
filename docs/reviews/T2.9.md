# T2.9 — review

Commit reviewed: `2ae25f6` (branch `t2-9`, addendum round; `main` and `origin/main` at `46c7f7c`, which is also the merge base).

## What was checked

- The ticket file, including the addendum and its three tests. The cited product-spec v1.6 §4 and §5 match the repo's §4 and §5 before this diff; T0.19's v1.7 changed only §11. Build-log open question 22 is quoted in full, as it read before the ticket.
- `git diff main...HEAD`, every hunk: 12 files. The merge base is main's tip, so the two-dot and three-dot diffs are the same. Nothing of main's is removed. `docs/product-spec.md` keeps main's v1.7 line whole, adds a v1.8 line inside the header comment (the `-->` moved to it), and adds one §4 sentence. `docs/build-log.md` adds one list line and the answer to open question 22. `schema.ts`, `answer.ts` and `run.ts` are untouched, so the answer schema and the stored run did not change, as Build says. `run.ts:283` stores `condition.when` only.
- Build and Rules: a condition's probes are `string[]`, with no points, no id and no verdict. `renderPack` puts them under the condition, and a condition without probes renders `id: when` as before. The protocol paragraph decides a probed condition by its probes alone, holding on at least one yes. The `FINGERPRINT_PACK` condition carries a probe, so the digest moves if condition-probe rendering changes. The five check probe sets are byte-identical to main's.
- Criteria and Tests match both ways: AC1–TC1, AC2–TC2, AC3–TC3, AC4–TC4, AA1–TA1, AA2–TA2, AA3–TA3.
- Red-first record (report, *Tests written*): five rows, no empty cell. This round adds no test. Its test changes are comment edits, v1.7 to v1.8.
- `review-scope.mjs`: three files, none missing. `pnpm vitest run src/lib/scoring/prompt.test.ts src/packs/feature-prd.test.ts src/packs/validate.test.ts`: 3 files, 37 passed. TA2's `scripts/run/log-index.test.mjs` was run as well: 12 passed.
- TC4: `version-drift.mjs "product-spec v1.6 §4, §5 · build-log OQ22"` gives cited 1.6, repo 1.8, `drifted: true`. The §4 sentence is present and says what AC4 asks.
- TA1: `git merge-tree --write-tree --name-only origin/main HEAD` exits 0 with no conflicted file. `gh pr view 13` gives base `main`, state OPEN, and head `2ae25f6`, which is HEAD. `gated.mjs` gives `ok: true` with `reasons: []`. `report-check.mjs docs/reports/T2.9.md` gives `ok: true` over 5 rows.
- Not yet evidenced, by design: AA2's gate green for this tree and AA3's close. Both happen at step 9, after this verdict. The gate record beside the marker is from 12:40, before the merge commit.

## Shoulds

1. **Should — the report and the log entry still describe round one. Settle this at step 8: nothing reviews what lands after this PASS.** On main, v1.7 is now T0.19's comment path, so these lines send a reader to the wrong ruling:
   - `docs/log/T2.9.md:12`: "Product-spec §4 records it at v1.7".
   - `docs/reports/T2.9.md:78` (the AC4 row): "`cited 1.6, repo 1.7, drifted: true`". The run today prints repo 1.8.
   - `docs/reports/T2.9.md:5, :57, :64, :138, :152` also say v1.7.
   - `:11` and `:156–160` (*Changed since this ticket was cut*) still say the pull request "is opened against `t2-8`" and that the diff "carries T2.8's six source files".
   - The report has no AA1–AA3 rows and no addendum-round section under *Reviewer passes and findings*.
   - Smallest fix:
     - Name v1.8 on each of those lines, the way `docs/log/T0.12.md:17` does ("v1.7 at the first round, v1.8 after the addendum").
     - Rewrite *Changed since* for the merge of `origin/main` and PR #13's base `main`.
     - Add AA1–AA3 with TA1–TA3 as run, and a section recording this pass.
     - In the red-first table, write the first four rows' green as "1 failed / 36 passed". 4 failed + 33 passed is 37 tests, so "36 passed" leaves out the pin that was still red.
2. **Should — v1.8 departs from the words of a standing decision.** The addendum says "product-spec §4 recording that a condition may carry probes stays at v1.7". `docs/tickets/T2.9.md:108–115` reads that as a decision about §4 recording the rule, not about the number, and takes v1.8 because main's v1.7 was T0.19's by the time the branch merged. I agree with that reading. Keeping v1.7 would have meant editing main's changelog line, which the addendum's "keeping main's text" rules out. T0.13 set the precedent. The Owner should still see it. Record it in the report's defaults, where the claim's `default` comment already puts it.
3. **Should — `src/packs/feature-prd.test.ts:116–118` pins the pack version a second time, under a stale comment.** The test "carries probes on the five sufficiency checks and on no other" still ends with `expect(featurePrdPack.version).toBe("1.2.0")`. The comment above it says "the probes are a rubric edit, and the version is what makes the re-baseline findable", which is about T2.8's check probes, but 1.2.0 is T2.9's condition probes. The new test at `:140` pins the same value, so the next version bump has to edit both. Smallest fix: drop the pin and its comment from the older test, and leave the version to the test at `:140`.
4. **Should (not a problem in this pack today) — `src/packs/validate.ts:78` validates only the first occurrence of each condition id.** `packConditions` dedupes by id and keeps the first. Suppose a later check carries the same condition id with an empty probe, or with different probes. That pack loads without complaint, and its later probes never reach the prompt. The Feature PRD pack shares one constant per condition, so nothing is affected now. A hand-edited pack could diverge silently. Smallest fix, next time this file is touched: check every `appliesWhen` instance, and refuse a condition id that appears with different `when` or `probes`.
5. **Should — `docs/build-log.md:887–888` breaks a code span across a line: `` `list-rendering-`` then `` surface` ``.** Markdown turns the line break into a space, so the page shows "list-rendering- surface". Smallest fix: move the whole `` `list-rendering-surface` `` onto one line.
6. **Should (outside this ticket) — `scripts/run/review-scope.mjs:24` reads only `## Tests`.** It did not list the addendum's `scripts/run/log-index.test.mjs`, and the reviewer ran that file by hand. An addendum round that names test files outside `## Tests` gets a scope that misses them. If wanted, file a Backlog Fix under the harness epic.

PASS
