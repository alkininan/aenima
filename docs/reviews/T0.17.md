# T0.17 — review

Reviewed at `8e6087c` (branch `t0-17`), pass 2. I read the diff as `git diff origin/main...HEAD`,
because the local `main` ref sits at `1544c5f`, two merges behind, as the ticket says. I ran the
seven test files this diff touches: guard, hooks, comments, notion, permission, pick-next and
threads, 197 passed. That matches the report's per-file counts (21 + 39 + 14 + 21 + 7 + 89 + 6).
`review-scope.mjs` also lists four more files (eslint-ignores, prompt, feature-prd, validate). They
show up only because it diffs against the stale local `main`. I ran them too: 36 passed.
`missing` was empty.

Pass 1's Musts are settled.
- **Must 1:** `guard.mjs:850-866` now refuses every status write on a task read at Backlog except
  Ready on the word. It refuses a status write when it could not read the task, and it refuses a
  task created at any status but Backlog. `guard.test.mjs` "refuses any move out of Backlog but
  Ready…" tries the two-step path. I checked the chain by hand: Backlog → X, null status → X, and
  Done → Backlog → Ready all end at the word.
- **Must 2:** `docs/reports/T0.17.md` carries the red-first table.

The session transcript shows the connector's real `tool_input` shape: `page_id`, `properties.Status`
and `pages[].properties`. That is the shape rule (h) reads, so the guard sees the writes it claims
to see.

## Shoulds

1. **Should — five tests have a red that did not come from their own assertion.**
   - `scripts/hooks/guard.test.mjs:211` "allows Backlog → Ready on the word". The report says the
     rule (h) tests were red because the rule was missing: "6 failed / 82 passed". With the rule
     missing, `decide` returns null for every connector call. So this test, which expects null,
     was one of the tests that passed. No later mutation (G1–G12) turns it red.
   - `scripts/run/pick-next.test.mjs:91`, `:103`, `:144` and `:367`. Their only row is "no tests
     collected". That was a collection error from `compose("waiting")`, not these assertions
     failing. `:103` "leaves the caller's rows alone" would also have passed against the old
     picker.
   - The behaviour itself is covered elsewhere: the allow path by the `judge` test under G6 and
     by `permission.test.mjs` under PM1, PM2 and C3. But `:144` is the only test of Build 4's
     "incomplete".
   - Fix: record one mutation per test. Suggested:
     - `:211`: refuse a Ready write at Backlog even when `granted.ok`.
     - `:144`: build `dependents` from `rows` instead of `pending`.
     - `:91`: let a Backlog row into `queue`.
     - `:103`: sort `rows` in place.
     - `:367`: drop the token check.

2. **Should — the stale-task step still points at a picker input that no longer exists.**
   `.claude/skills/ticket/SKILL.md:137-138` says: "With several stale tasks, order them with
   `pick-next.mjs` over those rows alone". This diff removed `--file` and stdin. The CLI now reads
   the whole board, and `pickNext` only claims rows at Ready, so In progress rows give `pick: null`.
   The old picker also skipped non-Ready rows, so this step already could not work. Now the
   command cannot even be given those rows. Fix: reword to "order them the way the picker would:
   Priority, then Epic name, then ID", or name a script that can take rows.

3. **Should — the token can set Status directly, and neither the open question nor the filed task
   says so.** The report's live check moved the fixtures to the trash "with the token". So the
   integration can update pages, and a Bash `fetch` to `PATCH /v1/pages/<id>` can set a Backlog
   task Ready without the connector, so rule (h) never sees it. Open question 1 and the filed
   *Guard the token and duplicate routes* only name the fake-comment route and the
   duplicate/move route. §5's hard boundaries (`docs/guidelines.md:504`, "no Backlog task set
   Ready until it has read your `ready`") read as if the guard enforces this completely. Fix: add
   the direct property write to that Backlog task's criteria and to open question 1. Outside this
   ticket otherwise.

4. **Should — an old comment was left behind, and a few tags and counts are out of date.**
   - `scripts/hooks/guard.mjs:745` still has the old JSDoc "True when an update-page call sets
     Status to Ready." stacked above the new `movesStatus` comment. Delete it.
   - `scripts/hooks/guard.test.mjs:869-871`: the T0.11 comment is now separated from
     `describe("wanted …")` by a blank line.
   - `scripts/run/threads.test.mjs:36`, `:47` and `:64` gained a T0.17 `ready` row under a
     `T0.11 Build 1` tag, with nothing naming TC3.
   - The report's AC3 row says "`guard.test.mjs` rule (h) (8)", but there are nine since pass 1.

PASS
