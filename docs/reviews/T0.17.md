# T0.17 — review

Reviewed at `5929aa9` (branch `t0-17`), pass 3. I read the diff as `git diff origin/main...HEAD`,
because the local `main` ref sits at `1544c5f`, two merges behind, as the ticket says.
`review-scope.mjs` listed eleven files, with `missing` empty. Seven are this ticket's. The other
four (eslint-ignores, prompt, feature-prd, validate) show up only because the script diffs against
the stale local `main`. I ran all eleven: 234 passed. The seven come to 198, which matches the
report's per-file counts (22 + 39 + 14 + 21 + 7 + 89 + 6).

What I checked:
- **Criteria and Tests.** AC1 to AC6 each have a TC, and every new test names a TC or a Build item.
- **Red-first record.** Every new test has a row in the report's table, including the five pass 2
  asked for (G13, PN18 to PN21) and PN22 for `--among`.
- **Pass 2's Shoulds.** All four are settled:
  - Each of the five tests now has its own red.
  - The skill's stale step uses `--among`.
  - The token's status write is named in open question 1, in the hard boundaries and in the filed
    Fix.
  - The stale comment and the tags are fixed.
- **Live claims.** I checked them against the session transcript:
  - `pick-next.mjs` on the real board picked Fixture B `as: High`.
  - The waiting notice was printed once and not on the next read.
  - The checkout's guard exited 2 on a Ready write with no word and on an unprefixed comment, then
    exited 0 once `threads.mjs` shaped C's reply `ready`.
  - The connector's real `tool_input` (`page_id`, `properties.Status`, `pages[].properties`,
    `markdown`) is what rule (h) reads.
- **The guard's status-write logic.** I traced it: Backlog to X, empty or unreadable status to X,
  and a task created at a status other than Backlog all end at the word or at a refusal. Decision,
  Review and In progress to Ready, and anything to Backlog, go through as §3 has them.

No Must stands.

## Shoulds

1. **Should — the Urgent notice can repeat for the same batch of Urgent work.**
   - `scripts/run/pick-next.mjs:205` picks the newest Urgent Ready task again on every run.
     `unposted` (`:248`) only checks that task's own thread.
   - Example with four Urgent tasks, where the newest is also first in roadmap order:
     - Run 1 posts the notice on it and claims it.
     - Run 2 still has three Urgent tasks. Their newest is another task, whose thread has no notice
       yet, so it gets a second "3 tasks are Urgent" comment.
   - AC4 at exactly three still holds, because the second run has two left.
   - Fix: drop an `urgent` notice when any Ready Urgent task's thread already holds one, with a test
     of four tasks where the newest sorts first. Or record it as an open question.

2. **Should — "The connector's route is now closed" claims more than the matcher covers.**
   - `docs/reports/T0.17.md:186` says so, but `.claude/settings.json` guards three connector tools.
   - The same connector offers `notion-update-data-source` (it appears in this session's tool
     list), which can rename the Status select's options and so move every Backlog row at once.
     `notion-move-pages` and `notion-duplicate-page` are the routes already filed.
   - Fix: name the data-source update in open question 1 and in *Guard the token and duplicate
     routes*, and soften the sentence.

3. **Should — the guard's prefix check and `readThread` read different text for a rich-text
   comment. I am not sure this is reachable:** the session only shows the connector taking
   `markdown`.
   - `scripts/hooks/guard.mjs:761` `commentText` joins `text.content ?? plain_text`, so a
     non-text part (an equation or a mention) counts as empty.
   - `notion.mjs` `comment()` reads the API's `plain_text`, where an equation part is its
     expression.
   - Example: `rich_text` of an equation `ready` followed by a text part `⟡ x` passes the guard.
     The API would return it as a human reply that begins with "ready".
   - Fix: refuse a `rich_text` comment unless its first part is a text part that begins with the
     prefix.

4. **Should — §5's token paragraph says less than the guard does.**
   - `docs/guidelines.md:457-458`: "a gated merge, an apply or a Ready write is refused on that
     ground".
   - Rule (h) refuses every status write except to Backlog when it cannot read the task. §4 says
     so, and the report's open question 5 records the lost Done writes.
   - Fix: "a gated merge, an apply, or any status write but Backlog".

5. **Should — `unposted` has a second condition that no document states.**
   - `scripts/run/pick-next.mjs:248` also drops a notice while the thread holds an unanswered human
     reply.
   - `docs/guidelines.md:329` only says "words already on the thread are not said again".
   - So a Ready task whose thread has stopped at the two-round cap never gets its notice.
   - Fix: one clause in §5 step 1.

PASS
