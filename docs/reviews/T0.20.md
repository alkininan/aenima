# T0.20 — review

Reviewed commit `b60dcc0` (branch `t0-20`), pass 3. I read every hunk of `git diff main...HEAD`
(15 files; local `main` is `1667964`, the base the ticket names) and the delta since pass 2
(`f4be859..HEAD`, 8 files). `review-scope.mjs` listed five test files (guard, comments,
conflicts, permission and threads tests), with `missing` empty. Those five ran green: 180 passed,
which is pass 1's 177 plus pass 2's three. `report-check.mjs` accepts the report (24 rows). I
could not check TC6's fixture against T0.13's thread on the board, because this review reads no
board. I also ran four read-only `node -e` probes against `comments.mjs` and `conflicts.mjs`. One
of them used a stub runner, so nothing was fetched and no object was written.

What holds:
- Pass 2's three Musts are corrected:
  - Step 9 says what to do when the guard lets a merge through and GitHub refuses it: run
    `conflicts.mjs`, post one `refused` comment before `release.mjs`, and leave the task at Review
    (`.claude/skills/ticket/SKILL.md:370–374`).
  - `awaitingMigration` now reads past a `refused` comment (`scripts/run/comments.mjs:169–172`).
    On the thread `migration, "apply", refused, "apply"`, the shape is `apply`. The first `apply`
    stays used up, because `unanswered` still starts after the refusal, and `verify` reads the
    same `shapeOf`.
  - Steps 4 and 6 post the `default` and `decision`/`migration` comments before `release.mjs`
    (`SKILL.md:269–276`, `:305–307`, and §5 row 4). Every other place the skill posts a comment
    under a claim also posts before release: step 0b merge and apply, step 3's `ok: false`, and
    step 9's `gated`, `setup` and `refused`.
- Pass 2's Shoulds are settled:
  - 4, a `default` over several lines: `[\s\S]+`, M20.
  - 5, a comment `kindOf` cannot place: now open question 2, with an owner.
  - 6, a failed fetch: `conflictsFor`, M21. A stub-runner probe shows its success path fetches,
    then runs `merge-tree` on `origin/main` and `origin/<branch>`.
  - 7, the T0.16 header: moved.
  - 8, M1's row: names its four tests.
- Criteria and Tests map both ways. TC1–TC6 each have tests tagged `T0.20 TCn → ACn`. Every
  other new test is tagged `T0.20 Build 2`, or is an existing test the diff rewrote or moved, and
  the report names those.
- Every new test has a red-first row with no empty cell.
- `mayPost` is the one function behind `threads.mjs`'s `mayClarify` and the guard's `postable`.
  Earlier sessions' transcripts show the connector's create-comment calls always carry `page_id`
  and `markdown`, the two fields the guard reads, even when they also carry a `discussion_id`.

## Shoulds

1. **Should: a composed comment sent with trailing whitespace reads as no kind wherever the
   signature is anchored at its end.**
   - `scripts/run/comments.mjs:256` reads `const body = whole.slice(prefix.length);`. The
     `default`, `cycle` and `urgent` signatures end in `$`.
   - Probe: `kindOf(compose("default", …) + "\n")` is `null`.
   - The guard reads the markdown as the run sends it. A second `default` in one claim with a
     trailing newline is therefore checked as kind `null` and posts, so AC4 does not hold for it.
     The cap is not affected, because the `clarifying` signature is anchored at the start only.
   - Fix: use `whole.slice(prefix.length).trimEnd()`, and add a trailing-newline variant to
     "reads back the kind of every composed comment, every variant".
2. **Should: `conflictsFor` has no test for a fetch that succeeds.**
   - "says so, rather than naming nothing, when origin could not be fetched"
     (`scripts/run/conflicts.test.mjs:74`) runs in a repository with no `origin`.
   - So M21 reddens it only through `why`. Once the fetch is ignored, `ok: false` and
     `files: []` come from `conflictsOf` anyway.
   - Fix: give the temporary repository a local bare remote as `origin`, push `main` and `t9-1`
     to it, and assert that `conflictsFor("t9-1", run)` names `README.md` and `guidelines.md`.
3. **Should: `scripts/run/conflicts.mjs:3` still says "Step 0b — which files stand in the way of
   a merge GitHub refused".** Since pass 2, step 9 runs it too. Change it to "Steps 0b and 9".
4. **Should: the skill says nothing about `conflicts.mjs` answering `ok: false`.**
   - `SKILL.md:86–87` and `:372–373` say "`files` its `files`", and nothing more.
   - On a failed fetch the comment would still post with no files, which is right: a refusal
     always reports.
   - Fix: one clause, "with `ok: false`, quote its `why` in the report and post the refusal
     without files", so a run does not have to work that out alone.
5. **Should: the report falls behind the diff in two places.**
   - AC3's row (`docs/reports/T0.20.md:22`) cites the skill's "step 0b's merge and apply shapes
     and step 3", not step 9. Its Tests cell leaves out two tests tagged TC3 → AC3: "is apply
     again after a refusal reported the failed apply" and "says so, rather than naming nothing,
     when origin could not be fetched".
   - `:42–44` names two files for pass 2's "3 failed / 75 passed → 78 passed over those three
     files". The 78 is comments, conflicts and permission together (52 + 4 + 22). Name the third
     file.
6. **Should: "holds one claim to one comment of a kind on its task's thread" can leave its marker
   behind.** `scripts/hooks/guard.test.mjs:1118` releases the `s20` marker only as the test's
   last line. If an earlier assertion fails, the marker stays for every test after it, and one
   red becomes several. Put the release in a `try … finally`.

PASS
