# T0.32 — review

Commit reviewed: `29c05e3` (pass 1)

Read cold against `docs/tickets/T0.32.md`. Its `Spec` line cites product-spec v1.8 §5 (read at
v1.8 — the header carries it; §5's last law, "a stamp a human maintains is a stamp that is
eventually wrong", is the ticket's whole argument), the build log's Current state, and
`scripts/run/log-index.mjs` and `version-drift.mjs`. Also read: `docs/guidelines.md` §5 step 8 and
its ticket-body template (§ the Tasks table, line 136 — the no-orphan rule), `.claude/skills/ticket/SKILL.md`
step 8, and `.claude/commands/ticket-done.md`, which is the other writer of the block this diff
generates.

What I checked myself rather than taking from the branch:

- Every hunk of `git diff origin/main...HEAD`: eight files — the ticket, `docs/build-log.md`
  (Current state), three `docs/log/` entries, `scripts/run/README.md` step 8,
  `scripts/run/log-index.mjs`, `scripts/run/log-index.test.mjs`. `git status --porcelain` is clean,
  so the diff is the whole change.
- `node scripts/run/review-scope.mjs docs/tickets/T0.32.md` lists `scripts/run/log-index.test.mjs`,
  nothing `missing`; `named` is empty, because the ticket's *Tests* section names no path. I ran
  `pnpm vitest run scripts/run/log-index.test.mjs scripts/run/version-drift.test.mjs` — the second
  file because TC5 claims "both scripts' tests": **2 files, 42 tests, all passing**.
- `node scripts/run/log-index.mjs --check` → `{"stale": false}`, so the committed block is the
  generated one outside vitest too.
- AC3 measured rather than read, in a throwaway copy of HEAD: `**Specs:** product-spec v1.8` →
  `v1.7` reddens two tests ("matches docs/log/ and the documents' own headers" and "names
  product-spec, design-spec and guidelines at their header versions"); `[T0.31` → `[T0.30` on the
  Newest entry line reddens two ("matches …" and "names the newest docs/log/ entry"). AC3 holds.
- AC5 measured: `parseHeaderVersion`'s regex narrowed to the major only reddens 8 tests — 6 in
  `version-drift.test.mjs`, 2 in `log-index.test.mjs`. One parser, and both scripts feel it.
- Red-first derived independently: HEAD's test file over `origin/main`'s tree — 15 failed, 14
  passed. The four rows that are not "red on main" are in Should 2.
- `loosening.mjs` → `{"ok": true}`, `gated.mjs` → `"gated": []`, `migration-check.mjs` →
  `"waiting": false`. A PASS here self-merges, which is why Must 1 is a Must.

Markdown is Prettier-ignored (`.prettierignore`: `*.md`), so the unwrapped intro line in
`docs/build-log.md:9` is not a lint problem.

## Findings

1. **Must — `.claude/commands/ticket-done.md:9-10` still tells a session to write the two lines
   AC2 removes.**

   > 1. Current state → set "Next ticket" to the next ticket in the build
   >    guide's order. Mark the phase complete if this was its last ticket.

   On `origin/main` that instruction was correct: `docs/build-log.md:9-10` held `**Phase:**` and
   `**Next ticket:**`. This diff deletes them and makes the block generated, so following step 1
   now hand-edits a generated section — and I measured that a hand-edit of that block reddens the
   suite (two tests per edit, above). AGENTS.md: "never preserve backward compatibility in code.
   No compatibility layers, no fallbacks, no legacy paths — remove the old path in the same
   change." The old path is the writer, and it is still here. Smallest correction: delete steps 1
   and 2 of that file and put in their place what SKILL.md step 8 does — write
   `docs/log/<id>.md`, then run `node scripts/run/log-index.mjs`, never edit the generated
   sections. (Step 2, "Tickets done → add one line", was already stale before this diff, since
   the list has been generated for several tickets; it is the same edit, so it costs nothing to
   settle both, but only step 1 is this ticket's doing.)

2. **Must — one removed sentence is held by no file, and the test that claims it asserts something
   else.** `origin/main`'s `docs/build-log.md:14-15`:

   > `docs/design-spec.md` is **v2.17** and `docs/product-spec.md` is **v1.5**, both complete and
   > closed, and the code matches.

   AC4: "Every narrative sentence removed from Current state is present in a `docs/log/` entry or
   a spec's own header comment." The versions are now generated, and design-spec says its half for
   itself (`docs/design-spec.md:669`, "v2.22 — complete and closed: no open items"). Nothing in
   the repo says product-spec is complete and closed — `grep -rn "complete and closed" docs/*.md`
   returns that one line, and product-spec's own closing line says only "End of specification" —
   and nothing anywhere says the code matches the specs: `grep -rn "the code matches" docs/`
   returns nothing. Build step 3's "nothing is dropped that no other file holds" is not met.
   The test for it, `scripts/run/log-index.test.mjs:277-279`:

   > it("leaves the closed-and-matching claim with the document that makes it", () => {
   >   expect(read("docs/design-spec.md")).toContain("complete and closed: no open items");

   names *matching* and checks only closure, so TC4's "one grep per moved sentence" has no grep
   for this one. Smallest correction: carry the clause in `docs/log/T0.32.md` — the entry this
   ticket writes at step 8 — as one sentence saying both documents stood complete and closed and
   the code matched them at the versions Current state last stamped by hand, and grep that entry
   in the test beside the design-spec assertion. If instead the clause is meant to be dropped
   because "the code matches" is exactly the hand-kept stamp §5 warns about, that is a change to
   AC4 and belongs in the report as an open question with the human's word on it, not in a test
   name. (Lesser half of the same finding: design-spec:669 is the document's closing line, not "a
   spec's own header comment" as AC4 words it.)

## Shoulds

1. **AC3 names no test in the file, and "TC2 → AC2" now means two different things in it.**
   `scripts/run/log-index.test.mjs:30` still carries the marker of the ticket that created the
   file — "TC2 → AC2. The Tickets done list in docs/build-log.md is a function of docs/log/" —
   above the very test this ticket extends into AC3's work, while line 213 carries T0.32's own
   "TC2 → AC2" over the Phase/Next-ticket test. So a reader mapping this ticket's criteria to
   tests finds TC1, TC2, TC4, TC5 and no TC3. Smallest correction: put "TC3 → AC3" over the
   equality test at line 32 and date the old marker to its ticket.

2. **The red-first table owes four rows that are not "red on main".** `docs/reports/T0.32.md` does
   not exist yet and step 8 runs after step 5, so nothing is broken. HEAD's test file over
   `origin/main`'s tree: 15 red, 14 green. Eleven new tests are simply red on main. These four are
   not, and each needs the mutation that reddens it (all measured here):

   | test (`log-index.test.mjs`) | on main | mutation that reddens it |
   |---|---|---|
   | matches docs/log/ and the documents' own headers (33) | green — main's `generate` ignores the extra `root` key | `v1.8` → `v1.7` in the block; or `[T0.31` → `[T0.30`; or drop the Deployed line from `STANDING` |
   | keeps the two lines the repo cannot derive (208) | green | delete the Deployed line from `docs/build-log.md` — removing it from `STANDING` alone leaves this test green and reddens only 33 |
   | keeps every v2.3–v2.6 item across T0.5 and T0.6 (242) | green | remove `48h fields, ` from `docs/log/T0.5.md` |
   | leaves the closed-and-matching claim (277) | green | `complete and closed: no open items` → `closed` in `docs/design-spec.md` |

   The modified existing test "refuses the whole run when one file is malformed" (161) is also
   green on main; its row is the one it already had.

3. **TC5's own test cannot fail on a change to the shared function.**
   `scripts/run/log-index.test.mjs:294-300` compares `versions(...)` against
   `parseHeaderVersion(...)` computed in the test, so a mutation moves both sides together: under
   the major-only mutation above it stayed green while 8 other tests reddened. What actually
   carries AC5 is the source assertion at line 302-309 and the file-equality test. Smallest
   correction: assert the three versions literally (`1.8`, `2.22`, `1.20`) in that test, so it
   answers about the parser rather than about itself.

4. **The test file grows the second version parser three lines from the assertion that forbids
   one.** `scripts/run/log-index.test.mjs:181-185` hardcodes `/\bv(\d+\.\d+)\b/` and the six-line
   header window, which is `parseHeaderVersion`'s body, while line 306 asserts
   `expect(source).not.toContain("\\d+\\.\\d+")` of `log-index.mjs`. AC5 is about the two scripts,
   so this is legal — but it is the copy AC5 exists to prevent, one file over, and it drifts
   silently if the header window ever changes. Smallest correction: `header()` calls
   `parseHeaderVersion(readFileSync(join(root, path), "utf8"))`; the check stays real because the
   committed text is static.

5. **`scripts/run/log-index.mjs:15-16` states a drift that measurement does not support.**

   > Current state is the same kind of thing, for the same reason: it was four versions and a
   > finished phase out of date when this was written

   Against the repo — the record this ticket insists on — design-spec had moved five versions
   (v2.17 → v2.22, its header lists v2.18 through v2.22) and product-spec three (v1.5 → v1.8);
   neither is four. And phase 3 is not finished: `docs/build-log.md:612` still defers an open
   question to Phase 3 and the ticket's own Objective says T3.2 is Ready, so what was stale was
   the next-ticket line — T3.1 is done, `docs/log/T3.1.md` — not the phase. Smallest correction:
   "five versions and a finished ticket out of date".

6. **The instruction the run actually follows still describes one generated section.**
   `.claude/skills/ticket/SKILL.md:416-421` says `log-index.mjs` regenerates "the list in
   `docs/build-log.md`" and closes with "Never edit that list by hand; its test refuses a stale
   copy." `scripts/run/README.md` was updated to say two sections; the skill was not, and it is
   the one a run reads. The action it prescribes is still right, which is why this is not a Must.
   Smallest correction: "the two generated sections in `docs/build-log.md`", and the warning in
   the plural.

7. **AC1's "and commit" is dead text under the entry convention.** `docs/log/T0.31.md:2` is
   `_2026-09-23T06:58:00Z_` with no commit, because SKILL.md:415 tells every run to leave it off
   ("the commit is the board row's"). So the Newest entry line renders without a commit today and
   will for every entry the pipeline writes from here; only the migrated entries carry one. The
   code is right — `stamp()` prints the commit where an entry has one — but the criterion reads
   as though the line will show one. Worth saying in the report rather than changing.

8. **Three exports and one parameter with no caller.** `scripts/run/log-index.mjs:71`, `:77`,
   `:82` export `ordered`, `link` and `stamp`, which nothing imports — the test imports `newest`,
   not these — and `:109` `versions(readDoc, docs = CURRENT_DOCS)` is never called with a second
   argument. AGENTS.md: "Abstractions the spec doesn't name are speculation — don't build them."
   Smallest correction: drop the three `export` keywords and the parameter.

FINDINGS
