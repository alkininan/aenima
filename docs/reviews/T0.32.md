# T0.32 — review

Commit reviewed: `f314805` (pass 2)

Read cold against `docs/tickets/T0.32.md`. Its `Spec` line cites product-spec v1.8 §5 — read at
v1.8, the header carries it; the law the ticket leans on is §5's last one, "a stamp a human
maintains is a stamp that is eventually wrong" — plus the build log's Current state and
`scripts/run/log-index.mjs` and `version-drift.mjs`, both read whole. Also read:
`docs/guidelines.md` §5 steps 4, 5 and 8, `.claude/skills/ticket/SKILL.md` step 8,
`.claude/commands/ticket-done.md`, `docs/tickets/T0.8.md` and `T0.10.md` (the other owners of the
markers in the test file), and every destination this diff moves narrative into.

What I measured rather than took from the branch:

- Every hunk of `git diff origin/main...HEAD`: twelve files, all accounted for by the ticket —
  the pack, `docs/build-log.md`, four `docs/log/` entries, `scripts/run/README.md`, SKILL.md step
  8, `.claude/commands/ticket-done.md` (the block's other writer, removed in the same change),
  the script, its test, and pass 1's verdict file. `git status --porcelain` is clean.
- `node scripts/run/review-scope.mjs docs/tickets/T0.32.md` → `scripts/run/log-index.test.mjs`,
  nothing `missing`. `pnpm vitest run scripts/run/log-index.test.mjs
  scripts/run/version-drift.test.mjs` — the second because TC5 claims "both scripts' tests" —
  **2 files, 42 tests, all passing**.
- AC3 measured, not read: eleven hand edits fed through `generate` in memory against the real
  `docs/log/` and the real document headers. Each of the ten mutations is red (each spec version
  in the block, the newest entry's id, its date, either standing line deleted, the intro
  reworded, a `**Phase:**` line put back, and a document header moving under an unchanged block);
  the unmutated file is green. `node scripts/run/log-index.mjs --check` → `{"stale": false}`.
- AC5 measured: with `parseHeaderVersion`'s regex narrowed to the major in a throwaway copy,
  `versions()` answers `1`, `2`, `1` where the test asserts `1.8`, `2.22`, `1.20` — so TC5's test
  now moves with the shared function instead of with itself, which was pass 1's Should 3.
- AC4 read sentence by sentence against `origin/main:docs/build-log.md`, not only by the greps:
  every item of the v2.3–v2.6 and v2.13–v2.15 lists, all five v2.7–v2.12 clauses, v1.4's two
  rulings, and the closed-and-matching clause are each in a `docs/log/` entry.
- The entry's timestamp claim: `docs/log/T0.31.md:2` reads `_2026-09-23T06:58:00Z_` while commit
  `6c14051` landed at `2026-09-23T03:54:17Z` — local time written with a `Z`, as the entry says.
  T0.32's own stamp, `04:45:47Z`, sits between its two commits (`04:34Z`, `04:48Z`) and is UTC.
- `loosening.mjs` → `{"ok": true}`, `gated.mjs` → `"gated": []`, `migration-check.mjs` →
  `"waiting": false`: this PASS self-merges, so I read the diff with that in mind.

Both of pass 1's Musts are settled. `.claude/commands/ticket-done.md` no longer writes the
generated block, and the closed-and-matching clause is carried once in `docs/log/T0.32.md` with a
grep on it. No Must stands.

## Shoulds

1. **`scripts/run/log-index.test.mjs:30` cites the wrong ticket for the marker it dates.**

   > // T0.8's TC2 → its AC2, and T0.32's TC3 → its AC3.

   That describe block is T0.10's: `docs/tickets/T0.10.md:86` — "AC2 log index generated from
   docs/log/ matches the migrated entries" — and `:101` — "TC2 → AC2: `scripts/run/log-index.test.mjs`
   'the committed build log is the generated one' (2)", which names this block and its count.
   `log-index.mjs` and its test were both added by T0.10 (`eeaec95`). T0.8 does have a TC2 → AC2
   pair, `docs/tickets/T0.8.md:140`, but it is "Version drift is detected from real header
   lines" — so the citation points at a real pair that this test does not cover, which is worse
   than a dangling one. Smallest correction: "T0.10's TC2 → its AC2".

2. **`docs/guidelines.md:519-520` still describes one generated section.** §5 step 8 reads "write
   docs/log/<id>.md and regenerate the build log's list from the directory (log-index.mjs)".
   `scripts/run/README.md` and SKILL.md step 8 were both corrected in this diff; the document the
   protocol actually lives in — and the one `mirror.mjs` pushes to the board — was not. The
   action it prescribes is still right, which is why this is not a Must. Smallest correction:
   "regenerate the build log's two generated sections from the repo (log-index.mjs)". It costs a
   guidelines version bump, which the generated Current state then picks up by itself.

3. **The step-8 report owes a mutation row for two new tests that are green on an untouched
   tree.** `docs/reports/T0.32.md` does not exist yet and step 8 runs after step 5, so nothing is
   broken — but the red-first record cannot be checked at this pass, and these are the rows that
   will not read "red on main":

   | test (`log-index.test.mjs`) | why it is green untouched | mutation that reddens it |
   |---|---|---|
   | keeps the two lines the repo cannot derive (209) | both lines were already in the block on main | delete the `**Deployed:**` line from `docs/build-log.md` |
   | keeps every v2.3–v2.6 item across T0.5 and T0.6 (243) | this diff does not touch `T0.5.md` or `T0.6.md` | remove `48h fields, ` from `docs/log/T0.5.md` |
   | matches docs/log/ and the documents' own headers (35) | pre-existing test, extended | any of the ten block edits I measured above |

   The other seventeen new tests are red without the feature — including "keeps the
   closed-and-matching claim" (278), which pass 1's fix moved onto `docs/log/T0.32.md` and which
   is therefore now genuinely red on main.

4. **`docs/build-log.md:11` names the second-newest entry.** The shipped block says
   `**Newest entry:** [T0.31 — Read bundled shell -c flags](log/T0.31.md) · 2026-09-23`, while
   T0.32's entry is the newer one in real time; the cause is `docs/log/T0.31.md:2`'s local-time
   stamp, which I confirmed above. The script is right and the stamp is wrong, the entry records
   that, and §5 step 5 sends an out-of-scope finding to Backlog — I cannot read the board, so the
   filing is the run's claim. Worth the report's open questions either way: until that stamp is
   corrected, AC1's line points a fresh chat one entry back.

5. **`.claude/commands/ticket-done.md:9-10` gives the entry a title shape the repo does not
   use.** It says the first line is `# <title>`; SKILL.md:414 says `# <id> — <title>`, and all 56
   files in `docs/log/` carry the ID. `readEntry` accepts either, so an entry written from that
   command produces a Tickets done line with no ID and no test notices. Smallest correction:
   `# <ticket ID> — <title>`.

6. **AC4's greps cover one of the two moved lists unevenly.**
   `scripts/run/log-index.test.mjs:259-270` greps only the five v2.7–v2.12 clauses the entry did
   not already carry, while `:243-257` greps all eight v2.3–v2.6 items including ones that were
   already in T0.5 and T0.6. The unguarded half — "the neutral variant", "the derived press value
   and the brand hexes", "the resend cooldown", "field state to the leading icon", "the 24h label
   zone", "one step-chrome variant" — could leave `design-spec-v2.7-v2.12.md` later with the
   suite green, and AC4 is a claim about the whole sentence. (Also: main's "centred step chrome"
   survives only as the entry's "step alignment", which is the same commit said differently — I
   read that as kept, not dropped.)

7. **`scripts/run/log-index.test.mjs:282` couples an AC4 test to a spec's closing line.**
   `expect(read("docs/design-spec.md")).toContain("complete and closed: no open items")` reddens
   if `docs/design-spec.md:669` is ever re-worded at v2.23, which is not what AC4 is about — the
   clause AC4 needs is carried by the `docs/log/T0.32.md` assertion three lines below. Also worth
   noting in the report: that line is the document's closing line, not "a spec's own header
   comment" as AC4 words it, so the entry is the branch of AC4 that is actually met.

8. **The committed verdict file is pass 1's.** `docs/reviews/T0.32.md` at `f314805` ends
   `FINDINGS`. `permission.mjs` reads the checkout's copy, so the merge reads this file, not that
   commit — but unless the close commits this pass's copy, the merged history records a verdict
   the merge contradicts.

PASS
