# T0.21 — review

Reviewed commit `dd402d5` (branch `t0-21`), pass 3, cold. I read `docs/tickets/T0.21.md` in full —
its Cited section carries guidelines §4 and §5 at v1.11 and the T0.16 report, which is what the
Spec line names — and every hunk of `git diff main...HEAD`, all seventeen files, including the
four the summary passes over (`scripts/hooks/guard.mjs`, `scripts/run/permission.mjs`,
`scripts/run/threads.test.mjs`, `docs/reviews/T0.21.md`, which carries pass 2's verdict).

Scope and tests, run here:

    node scripts/run/review-scope.mjs docs/tickets/T0.21.md
    pnpm vitest run scripts/hooks/guard.test.mjs scripts/run/comments.test.mjs \
      scripts/run/gated.test.mjs scripts/run/loosening.test.mjs scripts/run/threads.test.mjs

5 files passed, 203 tests passed, nothing `missing`. `node scripts/run/gated.mjs` on this branch
answers `ok: false` with one reason, `the diff edits the loosening detector` — the Done line's
claim that this ticket's own pull request is gated holds — in 0.4 s, well inside the Bash hook's
sixty.

Every criterion has a test naming it: TC1 and TC2 in `gated.test.mjs` "lets the specs through"
and "lets a routine harness change through" and in `loosening.test.mjs` "reads a diff that
weakens nothing as ok"; TC3 in "gates a diff that deletes a guard rule, and names it" and
`comments.test.mjs`; TC4 in "gates a diff that narrows a matcher"; TC5 in "stops a migration at
Decision and gates the same file at Review"; TC6 in "gates a diff that edits the detector" and
"…that renames the detector away". TC3 and TC4 are over temporary repositories with real
before/after pairs, as the Tests line asks. Pass 2's two Musts are fixed and I re-measured both
rather than reading them: with `--no-renames` the renamed detector is listed and gated, and
`reviewed()` returning `{ ok: true }` at the top now answers with four door reasons. Pass 2's
Shoulds 3, 4 and 6 are settled in this diff. The finding below is the same wall one course lower.

## Findings

1. **Must — the door's own wiring to `gated.mjs` is measured by nothing, so a one-line diff that
   disconnects the detector from the merge self-merges.** `DOOR_CORPUS`
   (`scripts/run/loosening.mjs:157`) measures `reviewed()` with all four effects injected —
   `DOOR_OPEN` at `:218` hands it `diff: () => ({ ok: true, gated: [] })` and each entry withholds
   exactly one — so no entry ever lets `reviewed` compute its own answer. The line that computes
   it, `scripts/run/permission.mjs:248`,

       const diff = deps.diff ? deps.diff() : gatedDiffOf({ cwd: dir });

   is therefore outside the measurement, and `scripts/run/permission.test.mjs` injects `diff` in
   every one of its `reviewed` cases too, so the suite does not hold it either. Measured over a
   temporary repository in this checkout's own `repoWith` shape, one commit whose only change is
   that line rewritten to `: { ok: true, gated: [] }`: `loosenedBy` answers
   `{"ok":true,"reasons":[]}`. The same hole from the other end, `scripts/run/gated.mjs:62`, the
   `...weakened,` spread deleted from `gatedDiff`: `{"ok":true,"reasons":[]}` again — caught there
   by `gated.test.mjs`, but by the suite and not by the detector the Rules line says does the
   detecting. Either diff lands on main on the reviewer's PASS, and from main every diff after it
   is judged by the gutted copy, which is the one thing the Objective keeps for the human.
   Smallest correction: one more `DOOR_CORPUS` entry that withholds `diff` rather than supplying
   it —

       { name: "a merge whose diff nobody measured", door: "reviewed",
         refused: ({ reviewed }) =>
           !reviewed({ dir: "/nowhere", deps: { ...DOOR_OPEN, diff: undefined } }).ok }

   — which catches both mutations: on an unmutated side `reviewed` falls through to `gatedDiffOf`,
   `scripts/` cannot be read from `/nowhere`, and it refuses in 5 ms with no child process (I ran
   it); on either mutated side it opens. Plus its row in the red-first record.

2. **Must — there is no report, so no test in this diff has a red-first record.** §5 step 8 wants
   `docs/reports/T0.21.md` with, per test, the mutation that made it red and the count that went
   green, and the ticket's Done line says "TC1–TC6 observed". Neither `docs/reports/T0.21.md` nor
   `docs/log/T0.21.md` is in the tree at `dd402d5`, and the build log's Tickets done list is not
   regenerated, so the 32 `loosening.test.mjs` cases, the rewritten `gated.test.mjs` and the
   changed `comments.test.mjs` case are all unverified from here: a passing test is evidence about
   the test. Step 8 falls after step 5 in the run protocol, so this is plausibly work not yet done
   rather than work skipped — but it is pass 1's Must 3 and pass 2's Must 3 unchanged, and the
   claim the ticket rests on is still not on file at the commit I read.

## Shoulds

1. Some twenty cases in `scripts/run/loosening.test.mjs` name no criterion — the last two of
   `describe("the corpus")`, the three `doorsLoosened` cases, the five `hooksOf and hooksLoosened`
   cases, the four `gateLoosened` cases, the four `testsLoosened` cases, `loosenings`'s second
   case and the last three repository cases. They cover Build item 2's hook, gate and deleted-test
   bullets, which no AC names, so nobody can retire them against a criterion. An AC7 in the
   ticket, or a tag on each, would settle it. Pass 1's Should 5 and pass 2's Should 1, standing.
2. `.worktreeinclude` and `.gitignore` are ungated now, as Build item 2 asks, and nothing measures
   them — a commit appending `.env.migrate` to `.worktreeinclude` answers `ok: true`, while §5's
   capability boundary rests on that file not carrying it. Guard rule (b) stays as the second
   layer, so this does not block. Pass 1's Should 3 and pass 2's Should 2, standing.
3. `scripts/run/permission.test.mjs:250` still frames the door as "the diff against origin/main
   touches no gated path" and `:288` is named "refuses a diff on a gated path even with the PASS,
   and names the path". Both pass, because they assert with `toContain`, and the file is not in
   the diff — so the module's vocabulary moved and its tests' did not. One sentence and one test
   name settle it, in the same place the `diff`-withholding entry of Must 1 would go.
4. `CRITERION` (`scripts/run/loosening.mjs:58`, used at `:488–493`) is not ticket-scoped, so a
   deleted test file naming `AC1` reads as replaced by any added file naming `AC1` — every ticket
   numbers from AC1. Scoping the token to the ticket id, or to the same directory, tightens it.
   Pass 2's Should 5, standing.
5. `PROBE_TIMEOUT_MS = 60_000` (`scripts/run/loosening.mjs:52`) is exactly the Bash PreToolUse
   hook's own `"timeout": 60` in `.claude/settings.json`, and `loosenedBy` runs two probes behind
   two `git archive`s — so the probe's conservative "unmeasured is gated" branch cannot be reached
   inside the guard: the hook is killed first, and a killed PreToolUse hook refuses nothing. The
   measured cost today is 0.4 s, so nothing is at risk now; a probe timeout well under the hook's
   would make that branch reachable. I am less sure of the runtime's behaviour on a hook timeout
   than of the two numbers, which is why this is a Should.

FINDINGS
