# T0.21 — review

Reviewed commit `070b195` (branch `t0-21`), pass 2, cold. I read `docs/tickets/T0.21.md` in full —
its Cited section carries guidelines §4 and §5 at v1.11 and the T0.16 report, which is what the
Spec line names — and every hunk of `git diff main...HEAD` (14 files, 2206 insertions), including
the two the summary does not dwell on (`scripts/hooks/guard.mjs` and `scripts/run/permission.mjs`,
comment and refusal-text changes only).

Scope and tests, run here:

    node scripts/run/review-scope.mjs docs/tickets/T0.21.md
    pnpm vitest run scripts/run/comments.test.mjs scripts/run/gated.test.mjs \
      scripts/run/loosening.test.mjs

3 files passed, 93 tests passed, nothing `missing`. `node scripts/run/gated.mjs` on this branch
answers `ok: false` with one reason, `the diff edits the loosening detector` — the Done line's
claim that this ticket's own pull request is gated holds.

Every criterion has a test naming it, and I re-measured pass 1's two Musts over temporary
repositories of my own rather than reading the fix: `"test": "true"` in `package.json` now answers
`the gate's test command is no longer main's`, and deleting the `release.mjs` hook from the
two-hook `SessionEnd *` group answers `the SessionEnd * hook that runs scripts/run/release.mjs no
longer carries main's command`. Both are fixed. A narrowed matcher (`vercel --prod`) and a deleted
test file are still caught. The findings below are three more holes in the same wall, found the
same way.

## Findings

1. **Must — a rename takes the detector out of the diff, so AC6 has a hole the measurement cannot
   see.** `scripts/run/gated.mjs:84` and `scripts/run/loosening.mjs:512–517` both list the diff
   with `git diff --name-only <range>`, which prints only the *destination* path of a rename, and
   `detectorLoosened` (`scripts/run/loosening.mjs:398`) asks `files.includes(DETECTOR)` against
   that list. Measured over a temporary repository whose one commit does
   `git mv scripts/run/loosening.mjs scripts/run/loosen.mjs`, renames the test with it, updates
   `gated.mjs`'s import and rewrites the `DETECTOR` constant: `git diff --name-only main...HEAD`
   lists `scripts/run/gated.mjs`, `scripts/run/loosen.mjs`, `scripts/run/loosen.test.mjs`,
   `detectorLoosened` answers `[]`, and `gatedDiffOf` answers `{ ok: true, gated: [] }`. The one
   file the design says cannot mark its own homework can be replaced wholesale by a diff that
   merges itself. Smallest correction: `--no-renames` on both `git diff --name-only` calls, so a
   rename lists the old path as well as the new (verified: with the flag the same repository lists
   `scripts/run/loosening.mjs` too, and the detector rule fires) — plus a case in
   `loosening.test.mjs` whose mutation is the rename, with its row in the red-first record.

2. **Must — the two doors the merge actually rests on are in no corpus, so a diff that guts either
   self-merges.** `PROBE_DEPS` (`scripts/run/loosening.mjs:69–79`) hands `decide` a fixed
   `verdict: () => ({ ok: false })` and `permission: () => ({ ok: false })`, so what the corpus
   measures is the rule table's reaction to an answer, never the code that computes the answer —
   and that code, `reviewed()` in `scripts/run/permission.mjs:246`, left the gated set with this
   diff. `gateLoosened` (`:319`) has the same shape for the Stop gate: it compares `STEPS`,
   `MAX_RED` and `package.json`'s commands, never `gate.mjs`'s own decision. Measured over
   temporary repositories, one mutation each: `reviewed()` returning `{ ok: true }` at the top of
   the function answers `ok: true, reasons: []`; `decide()` in `scripts/hooks/gate.mjs` returning
   `{ close: true, block: false }` answers `ok: true, reasons: []`. Either merges itself, and the
   next run's hooks then run the gutted copy from `origin/main` — which is the one thing the
   Objective keeps for the human. I am uncertain whether Build item 2's four bullets were meant as
   the whole of "weakens a restraint" or as examples of it, so the smallest correction may be
   either: a second corpus in the same shape as `GUARD_CORPUS` — `gate.mjs`'s `decide` over a
   state where a red must block, `permission.mjs`'s `reviewed` and `wordFor` over injected effects
   where each must refuse, compared side to side — or, if that is more than this ticket wants, the
   gap recorded in `docs/reports/T0.21.md` and `docs/log/T0.21.md` as an open question with a
   phase owner, which is what AGENTS.md asks of a stopgap.

3. **Must — there is no report, so no test in this diff has a red-first record.** §5 step 8 wants
   `docs/reports/T0.21.md` with, per test, the mutation that made it red and the count that went
   green; the ticket's Done line says "TC1–TC6 observed". Neither `docs/reports/T0.21.md` nor
   `docs/log/T0.21.md` is in the tree at `070b195`, and the build log's Tickets done list was not
   regenerated, so `report-check.mjs` has nothing to read and the 30 `loosening.test.mjs` cases,
   the reworked `gated.test.mjs` and the changed `comments.test.mjs` case are all unverified from
   here — a passing test is evidence about the test. Step 8 falls after step 5 in the run
   protocol, so this may be work not yet done rather than work skipped; either way the record is
   what the claim rests on and it is not on file at the commit I read. This is pass 1's Must 3,
   unchanged.

## Shoulds

1. Nineteen tests in `scripts/run/loosening.test.mjs` still name no criterion — the last three of
   `describe("the corpus")`, `ruleLetters`, `pathsLoosened`, the five `hooksOf and hooksLoosened`
   cases, the four `gateLoosened` cases, the four `testsLoosened` cases and "gates a diff whose
   before side cannot be read". They cover Build item 2's hook, gate and deleted-test bullets,
   which no AC names, so nobody can retire them against a criterion. An AC7 in the ticket, or a
   tag on each, would settle it. Pass 1's Should 5, standing.
2. `.worktreeinclude` and `.gitignore` are ungated now and nothing measures them, while guidelines
   §5's capability boundary rests on both. Measured here: a commit that appends `.env.migrate` to
   `.worktreeinclude` answers `ok: true`. Guard rule (b) stays as the second layer, so this does
   not block; a rule that reads the after side for an `.env` name the before side did not carry
   would make it a measurement rather than a hope. Pass 1's Should 3, standing.
3. `.claude/agents/reviewer.md:59` still says the guard "lets a ticket whose diff touches no gated
   path merge itself on `PASS` (`docs/guidelines.md` §4)". §4 no longer has gated paths. The skill
   and `scripts/run/README.md` were brought over in this diff and the reviewer's own definition
   was not; one sentence — adds no migration and weakens no restraint — settles it.
4. `docs/guidelines.md:342` and `:345` now say the same thing twice: "Everything else lands on
   main at close: …" and, three lines later, "Everywhere else a finished ticket merges itself at
   close: …". The second is the seam the edit left; deleting "Everywhere else" and joining the
   sentence to the first reads clean.
5. `CRITERION` (`scripts/run/loosening.mjs:58`, used at `:371–375`) is not ticket-scoped, so a
   deleted test file naming `AC1` is read as replaced by *any* added file naming `AC1` — including
   one from another ticket, since every ticket numbers from AC1. Scoping the token to the ticket
   id, or requiring the replacement to sit in the same directory, would tighten it.
6. Two fixtures still compose the `gated` comment with the field it no longer reads —
   `scripts/run/threads.test.mjs:102` and `scripts/hooks/guard.test.mjs:1111` both pass
   `{ paths: … }`. They pass, because the signature is matched on the sentence's opening, but the
   sentence they build now reads "…only your word merges: . It stays at Review", which is what a
   later reader would copy.

FINDINGS
