# T3.2 — review

Reviewed commit `c3347f4` (branch `t3-2`, `git diff origin/main...HEAD`, 21 files, every hunk
read). The first pass of the round after `apply` and the split, cold: three commits past the
last review — `dbe0979` (merge of main), `5154980` (the apply's aftermath: types regenerated, the
database test corrected), `c3347f4` (the split addendum, ticket file only).

**Read against.** `docs/tickets/T3.2.md` as it stands after the 2026-09-23 split — the ticket's
criteria are AA1, AA2 and AA3, its tests TA1, TA2 and TA3, plus the database half of AA1 and AA3
in `src/db/refinement.db.test.ts`; AC1–AC7 and AB1–AB8 moved to two Backlog tasks and are not
judged here. The sections the addendum leans on, in the repo's files: product-spec §1 laws 3 and
7, §5 (results cache per artifact version), §6 (the two-round cap); design-spec §4, §6, §8.32–
§8.34, §11, §17 for what the moved criteria will need.

**Tests, run here.** `node scripts/run/review-scope.mjs docs/tickets/T3.2.md` listed 5 files,
`named` = `src/db/refinement.db.test.ts`, `missing` empty. `pnpm vitest run` on those 5:
**5 files passed, 98 tests passed, none skipped** — loop 27, rounds 22, sections 22, run 12, and
`refinement.db.test.ts` 15 of 15 against the live schema, which is the observation the ticket
says settles the Must the last three passes carried.

**Traced by hand.** `0017` against the Drizzle table, both agreeing on the key
(artifact, section, check, cycle, round), `refinement_round_cycle`, `refinement_round_base_hash_len`
over `sectionHash`'s 64 hex characters, the four-armed `refinement_round_shape` with an early
surfacing only on `evidence_truncated and author_position is null`; `database.types.ts` has
the four new columns and the nullable position on Row, Insert and Update and nothing else moved.
`currentCycle` reads every row of the newest cycle and turns only on a non-null baseline that
differs; a revert to older text lands on a new cycle number rather than colliding with the one it
spent. `nextMove` on a turned cycle sees no rows and revises at round 1. In `loop.ts`
`admitObjections` runs before `fit`, so the occurrence guard reads the whole quote and the cut is a
prefix; the truncated-quote path surfaces with `authorPosition: null` and `continue`s without
setting `retest`; the round-3 surfacing carries the cut flags through `base`. `readVersionConditions`
and `readHumanBaseline` both filter `workspace_id`; `authored_by_kind = 'human'` is an enum
value the `artifact_version_actor_shape` check names; `readLatestConditions` has no caller in code.
`writeRound` puts both truncation flags in the activity row's metadata. The corrected database
read (`refinement.db.test.ts:419-421`) filters to its own artifact inside a rolled-back
transaction, so the twelve standing rows from T3.1 no longer answer it.

**What this diff is.** AA1, AA2 and AA3, the migration that carries AA1 and AA3, the types and
the test correction — what the split says the ticket closes on, and nothing outside it. The
shipped key deviates from AA1's literal "(artifact version, section, check)"; the report's
*Defaults taken* gives the terminating argument, AA1's own trigger sentence is what shipped, and
the owner's reply takes the diff as it stands. Recorded, and right.

## Findings

No Must. Four Shoulds, on the record rather than the code.

### Shoulds

1. **The report's drift line is false at HEAD.** `docs/reports/T3.2.md:190`: "Nothing.
   `version-drift.mjs` reads product-spec at v1.8 and design-spec at v2.21, exactly as the
   ticket's Spec field cites them." Run on the ticket's Spec line it reports `drifted: true` for
   both: product-spec `1.8 → 1.9`, design-spec `2.21 → 2.22`, brought in by the merge at
   `dbe0979`. Neither moved a section the narrowed criteria lean on — v1.9 changes §9, §12, §8,
   §17 and Appendix D; v2.22 changes the dock's §8.33 and §11 among others, which are now the
   surface task's reading. Product-spec §11's own rule is to surface "changed since this ticket
   was cut" rather than say nothing moved. Smallest correction: rewrite that section to name the
   two bumps and say the sections AA1–AA3 cite did not move; the Spec line stays as cited or is
   bumped in the same edit.

2. **The report and the log were not carried through the split.** `docs/reports/T3.2.md:3`
   ("the dock itself is not built"), `:17` ("It stopped again, at Decision, before the dock"),
   `:161-179` ("This is the ticket's remaining scope … whether the remainder is re-cut … is left
   as an open question"), open question 3 at `:206` ("Not decided here"), and `:48` ("Full
   suite at the last green: 1714 passed, 15 skipped" — the 15 now run). `docs/log/T3.2.md:57`
   says fifteen tests "skip until `drizzle/0017_refinement_cycles.sql` is applied" and `:65` that
   the re-cut "is the owner's call". The ticket's split addendum (`docs/tickets/T3.2.md:134-169`)
   answered all of it, and `c3347f4` touched the ticket file alone. Smallest correction: strike
   open question 3 as settled the way 1 and 4 are, point *What is not built* at the two Backlog
   tasks by their links, refresh the suite count, and add a dated line to the log saying the
   database half ran and the dock moved.

3. **Four of the six new database cases have no red observation.** `docs/reports/T3.2.md:45`
   still reads "Not observed red, and not observed green either … skipped" for all six, and `:46`
   records the one mutation, which reddened two (`refinement.db.test.ts:394` *stores the cut*,
   `:406` *takes a surfacing the author never answered*). No mutation and no count stand for
   `:233` *lets a reopened check start again at round one on the next cycle*, `:251` *refuses a
   round with no cycle at all*, `:422` *still requires a position of every outcome the author
   answered*, `:431` *refuses a surfacing past round three*. Three of the four assert the
   constraint's name in the error, so a vacuous pass is unlikely — which is why this is a Should.
   Smallest correction: either name a mutation and its count (e.g. `cycleNo: 1` in place of `0`
   at `:256`, or `roundNo: 3` at `:437`, each of which should turn the case green wrongly and
   prove it can fail), or say in the row that these four were not reddened, as the `fit` row at
   `:43` already does for its two.

4. **The three AA3 loop cases do not name their criterion.** `src/lib/authoring/loop.test.ts:353`,
   `:372`, `:394` sit under "refineSection — the doors before the author" with one comment above
   the first (`:351`) that says AA3; the titles carry no `TA3 → AA3`, while `fit`'s describe at
   `:584` does. The ticket's rule (product-spec v1.6's carry: every test case names its criterion)
   and the report's own table both call these the TA3 cases. Smallest correction: prefix the three
   titles with `T3.2's TA3 → AA3:` or wrap them in a describe that carries it.

PASS
