# T0.41 — review

Commit reviewed: `3ad63b9` (branch `t0-41`, merge-base with `origin/main` `a10159c`). Reviewer: Fable 5.1, pass 1.

## What I read

- `docs/tickets/T0.41.md` — Build, Criteria, Tests and the cited §6 and §17 of design-spec v2.22 — and T0.37's Decision (`docs/tickets/T0.37.md:73-89`) and report (`docs/reports/T0.37.md`), which the ticket names as the source the wording must follow.
- Every hunk of `git diff origin/main...HEAD`. Two files: `docs/design-spec.md` (title, the v2.22 changelog line losing its ` -->`, the v2.23 line added and closing the comment, the §6 trigger sentence, the §6 focus sentence, the footer) and `docs/tickets/T0.41.md`, the pack step 2 writes. `git status --porcelain` is clean. Nothing else in the diff, and nothing in the diff the ticket does not account for.
- `src/components/ui/Panel.tsx:149-218` for AC2 — the code the focus sentence's reason must match.

## What I checked myself

- **AC1.** `grep -n "visibility" docs/design-spec.md` finds only lines 296 and 297: the new "never by `visibility`" clause and the mechanism's existing "never `position-visibility`". No `visibility: hidden` remains. Line 296 reads "hidden by `opacity: 0` and `pointer-events: none` for the panel's lifetime, its box reserved, and returns with focus on close". §7's state table (line 318) says "the trigger is hidden inside the panel's footprint (§6 Morph)" without naming how, which still holds.
- **AC1's clause of why against T0.37.** The Decision (`docs/tickets/T0.37.md:75-80`): "In Chromium an anchor hidden by `visibility` makes its anchor-positioned panel stop taking pointer events under the default `position-visibility`". The spec now says the same thing in the same terms and adds "which the mechanism below never sets", which is what line 297 already says. `.claude/rules/surfaces.md:17` and `src/components/ui/Select.dom.test.tsx:204` agree.
- **AC2.** `src/components/ui/Panel.tsx:178-183`: the trigger is hidden by `style.opacity = "0"` and `style.pointerEvents = "none"`, and the very next statement focuses the panel's first item, under the comment "an opacity-hidden trigger keeps focus, so a keystroke in the gap would land on a trigger nobody can see". The spec's new reason at line 297 — "because an opacity-hidden trigger keeps focus and a keystroke in the gap would land on a trigger nobody can see" — is that comment. `focus-fixup` appears nowhere in the document now. The placement itself is unchanged ("inside the update callback, straight after `showPopover()`"), as the ticket asked.
- **AC3, C-01 to C-04 by script** (no script in the repo implements them; I wrote one over the file, C-01 whole and C-02 to C-04 over the four changed lines, the only lines that could newly fail): title, last changelog line and footer all `2.23`; one `-->` in the file, on the v2.23 line; 24 changelog lines, v2.0 through v2.23, one per version, no duplicates. No hedge before a number on lines 27, 296, 297 or 670. Every custom property those lines name in prose (`--r-panel`, `--t-med`, `--ease`, `--ease-spring`, `--t-fast`, `--shadow-float`, `--morph-r0`) is declared or excepted. Every bare section reference on them (§0, §4, §6, §17) resolves. No trailing whitespace on any edited line; line 26 now ends `(§17).` exactly.
- **§17.** The Build's "any check that names the trigger's `visibility` matches" is a no-op: no check does — C-14 and C-17 describe the transition and where focus lands, not how the trigger hides. Nothing to change, and the diff changes nothing there.
- **Tests.** `node scripts/run/review-scope.mjs docs/tickets/T0.41.md` lists no files, none `named`, none `touched`, none `missing`. The diff touches no test file, so there is no new test and nothing to have been observed red first. `pnpm vitest run src/app/tokens.test.ts` — the one suite that reads `docs/design-spec.md`, the file T0.29's bump ran for the same reason — 24 passed; the change touches no CSS block, so it is invisible to that suite, as expected.
- **The report.** `docs/reports/T0.41.md` is not in the checkout. That is the protocol's order, not a defect: guidelines §5 writes the report at step 8, after step 5's review, and T0.37's report landed in its closing commit. Should 3 says what it must carry.
- **`docs/build-log.md:11`** still says `design-spec v2.22`. Correct to leave: `scripts/run/log-index.mjs:110-123` generates that line from each document's header at close.

## Findings

No Must stands.

1. **Should** — `e2e/panel-geometry.spec.ts:90-91` reads "§6 hid the trigger with `visibility: hidden`", and from v2.23 §6 says the opposite, so the sentence points a reader at a line that is no longer there. Smallest correction: "v2.22's §6 hid the trigger …". The same shape at `src/components/ui/Panel.tsx:174-177`, which cites "T0.37's Decision, answered *default*" as the home of the reason; since v2.23 the reason lives in §6 itself and the comment can cite it. Both are outside the ticket's Build, which names the spec alone — recorded, not blocking.

2. **Should** — `docs/design-spec.md:296`, the new trigger sentence: "… hidden by `opacity: 0` and `pointer-events: none` for the panel's lifetime, its box reserved, and returns with focus on close — never by `visibility`, because …". On a first read "never by `visibility`" hangs off "returns with focus on close" rather than "hidden by". Smallest reorder: move the "— never by `visibility`, because … never sets —" clause to sit directly after `pointer-events: none`, before "for the panel's lifetime". A preference on prose; it changes nothing the sentence says.

3. **Should** — for the report (guidelines §5 step 8): the ticket's TC1 and TC3 are "by script over `docs/design-spec.md`", and no test file exists for them, so the Tests written table is the only record. It should carry them the way `docs/reports/T0.29.md` did — counts against `origin/main`: `v2.23` in title/footer/last changelog line 0 → 1, `visibility: hidden` 1 → 0, `focus-fixup` 1 → 0, `-->` 1 → 1 — and name TC2's method, which the ticket's Tests line leaves unnamed: the read of `src/components/ui/Panel.tsx:180-183` against §6's focus sentence. It should also say in one line that §17 names no check on the trigger's `visibility`, so that Build line is answered rather than silent.

PASS
