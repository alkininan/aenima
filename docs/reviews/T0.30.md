# T0.30 — review

Commit reviewed: `9b1f641` (pass 2, over `git diff origin/main...HEAD` whole — the key commit
`3a1148b` and the Shoulds commit `9b1f641` together).

Read cold against `docs/tickets/T0.30.md` and the two sections its *Cited* block carries
(`docs/guidelines.md` §2 and §7). The ticket has no `Spec` line; the *Rules* line names
`CLAUDE.md`, `AGENTS.md` and `docs/guidelines.md`, and §5 steps 4, 5, 8 and 9 are what this pass
holds the branch to.

What I checked myself:

- Every hunk of the diff. Nine files: `scripts/run/runs.mjs` (`startMinute`, `runKey`, the key
  check in `post`, the module docstring), `scripts/run/notion.mjs` (the `run` shaper, `runs()`,
  `rows()` lifted out of `tasks()`), the three test files, `docs/guidelines.md` (header to v1.21
  and a §2 paragraph), `scripts/run/README.md`, `docs/tickets/T0.30.md` and `docs/reviews/T0.30.md`
  — pass 1's verdict, which this file replaces. `git status --porcelain` is clean.
- `node scripts/run/review-scope.mjs docs/tickets/T0.30.md`: `named` empty, `touched` and `files`
  are `next-id.test.mjs`, `notion.test.mjs`, `runs.test.mjs`, nothing `missing`. `pnpm vitest run`
  over those three: **3 files, 89 tests, all passing**.
- The key against the shapes it meets: `runKey` strips dashes from both sides, `startMinute`
  floors to the minute in UTC through `Date.parse`, so a `+00:00` offset and a `Z` read alike; a
  row with no `Started` keys `null` and matches nothing; two tasks in one minute stay two runs
  (`runs.test.mjs:628`), and an idle run keys on its minute alone.
- AC1's other three doors, which no test closes: `.claude/skills/ticket/SKILL.md:477` on
  `origin/main` already says "The Runs row is not yours to write", and `runs_ds` is named in code
  only at `scripts/run/runs.mjs:389` and `:400`. The script path is the one the key now closes,
  and pass 1's transcript forensics found no connector write into Runs. I did not re-run those.
- AC2 and TC2 are board work and are not mine to query. Nothing in the diff records them except
  the sentence Must 2 is about.
- No `docs/reports/T0.30.md` and no `docs/log/T0.30.md`, and the ticket's own **Report** section
  (`docs/tickets/T0.30.md:42`) is empty. §5 puts both at step 8, after this review at step 5, so
  there is no red-first table to read yet: **no new test on this branch is yet recorded as
  observed failing first**, and `report-check.mjs` owns that record at close. Not a finding —
  there was nothing to read — but Shoulds 2, 3 and 4 below each name a line that record must carry.

Two Musts stand, then four Shoulds. Ranked.

## Musts

1. **The branch contradicts itself about `/clear`, and the wrong half is the one in law.**
   `docs/guidelines.md:215-217` now reads "SessionEnd fires again when a session is cleared,
   resumed or exited, and every firing reads the same transcript from the same first timestamp,
   so the row is keyed on its Task and the minute of its Started and a second post writes
   nothing". `docs/reviews/T0.30.md:50-51` — pass 1's verdict, committed in this same diff —
   reads "`/clear` starts a new transcript with a new first timestamp and so keys anew". Both
   are on this branch and they cannot both hold. A cleared session's next firing reads a
   *different* transcript: its first timestamp is later, and the cleared half carries no claim
   command, so `own` is null and the row keys on the minute alone — a second row, not a no-op.
   §2 is the document every run reads, so the over-claim there is the one that costs. The same
   sentence is at `scripts/run/README.md:243` and, as the cause the fix rests on, at
   `scripts/run/runs.mjs:13` and `:341`. *Smallest correction:* drop "cleared" from the list in
   all four places — "resumed or exited" are the firings that re-read the same transcript, and
   they are what the key joins — or, if a clear does append to the same file, say so with what
   was observed and correct the line in this file.

2. **`docs/guidelines.md:218` puts a board count in law that no record on the branch supports,
   and it is 18 copies larger than AC2.** The line: "Thirty-seven rows described fifteen runs
   before that key, every copy repeating its original's Tokens and Findings into the analytics."
   Thirty-seven rows for fifteen runs is twenty-two copies. `docs/tickets/T0.30.md:29-30` (AC2)
   and `:19` (Build, third bullet) name four — "Remove R-0055, R-0057, R-0058 and R-0061,
   keeping the first row of each run" — and AC2's first clause is "The Runs database holds one
   row per run". So either the board held twenty-two copies, in which case removing four leaves
   AC2 unmet and eighteen rows standing, or the count is wrong in the document the pipeline runs
   on. Nothing on the branch says which. I am uncertain which way it falls, and it is a Must
   either way. *Smallest correction:* the step 8 report names the rows actually deleted and the
   row count left behind, and §2's sentence either cites that record or is struck; if only the
   four named rows went, the rest of the copies go too before AC2 can be claimed.

## Shoulds

1. **The row is written with its seconds and read back without them, so the key rests on an
   assumption about Notion nothing here tests.** `scripts/run/runs.mjs:365` writes
   `{ date: { start: summary.started } }` — `2026-09-13T11:22:30.874Z` — while
   `scripts/run/runs.mjs:332-336` floors the read side to the minute, and
   `scripts/run/notion.mjs:109` states the assumption flat: "Started comes back as Notion stored
   it, which is to the minute". Every test encodes it rather than checking it —
   `runs.test.mjs:587` truncates with a regex. If the API ever rounds rather than truncates,
   half the runs key wrong and every one of these tests still passes. *Smallest correction:*
   write the floored value — `properties.Started = { date: { start: startMinute(summary.started) } }`
   — so both sides are the same string whatever the API does; Notion keeps no finer precision and
   Duration carries the rest.
2. **Pass 1's Should 3 was not taken, and the claim it was about is now in §2.** The cause —
   the hook re-firing — is still asserted rather than shown, at `scripts/run/runs.mjs:13-15` and
   `:341-343`, and `docs/tickets/T0.30.md:13-16` still reads the other way ("the step that closes
   a merged task, not at the run's own step 9"). *Smallest correction:* the report carries the
   observation pass 1 made — no connector create into the Runs data source in any local
   transcript, and none of the four copies named in one — so the branch holds evidence for what
   §2 now states.
3. **First write wins, so a resumed run keeps its shorter row.** `scripts/run/runs.test.mjs:613`,
   "writes nothing for a longer read of the same session", asserts it: the later read carries a
   further Status and more tokens and is dropped, so the row can hold an Outcome the run moved
   past and Tokens below what it spent — in the data §2 keeps for park rate and the weight
   tuning. The ticket chose this (`docs/tickets/T0.30.md:18`), so it does not block. *Smallest
   correction:* one open question with a phase owner in the report; an unrecorded stopgap is a
   bug (`AGENTS.md`).
4. **AC2 and TC2 have no record anywhere I can read**, as in pass 1. *Smallest correction:* the
   report's AC table gives AC2 the deletions by name and TC2 its count, beside the red-first rows
   for TC1.
5. **The §2 the ticket's pack quotes is no longer the §2 on the branch.**
   `docs/tickets/T0.30.md` carries §2 at v1.20 under *Cited*, and the diff moves the document to
   v1.21 with a new paragraph in that very section; `scripts/run/next-id.test.mjs:265` — T0.28's
   `TC4 → AC4` — was edited from `"1.20"` to `"1.21"` to keep the suite green. Both are right as
   far as they go, and neither is named in the ticket's Build. *Smallest correction:* one line in
   the report saying §2 moved and why, so the next reader of the pack knows it reads the older
   text.

FINDINGS
