# T0.30 — review

Commit reviewed: `a88ad4b` (pass 3, over `git diff origin/main...HEAD` whole — the key commit
`3a1148b`, the Shoulds commit `9b1f641` and the Musts commit `a88ad4b`).

Read cold against `docs/tickets/T0.30.md` and the two sections its *Cited* block carries
(`docs/guidelines.md` §2 and §7, now regenerated at v1.21 so the pack and the branch agree). The
ticket has no `Spec` line; *Rules* names `CLAUDE.md`, `AGENTS.md` and `docs/guidelines.md`, and §5
steps 4, 5, 8 and 9 are what this pass holds the branch to.

What I checked myself:

- Every hunk of the diff. Nine files: `scripts/run/runs.mjs` (`startMinute`, `runKey`, the key
  check in `post`, the module docstring), `scripts/run/notion.mjs` (the `run` shaper, `runs()`,
  `rows()` lifted out of `tasks()`), the three test files, `docs/guidelines.md`,
  `scripts/run/README.md`, `docs/tickets/T0.30.md` and `docs/reviews/T0.30.md` — pass 2's verdict,
  which this file replaces. `git status --porcelain` is clean, so the diff is the whole change.
- `node scripts/run/review-scope.mjs docs/tickets/T0.30.md`: `named` empty, `touched` and `files`
  are `next-id.test.mjs`, `notion.test.mjs`, `runs.test.mjs`, nothing `missing`. `pnpm vitest run`
  over those three: **3 files, 90 tests, all passing**.
- The key against the shapes it meets. `runKey` strips dashes from both sides, `startMinute`
  floors to the minute in UTC through `Date.parse`, so an offset Notion returns and the `Z` the
  script wrote read alike; a row with no `Started` keys `null` and matches nothing; two tasks in
  one minute stay two runs (`runs.test.mjs:637`); an idle run keys on its minute alone. The one
  round trip the key would not survive is an API that *rounds* a written `:30` up to the next
  minute rather than truncating it — pass 2 raised that and the run declined it with its reason in
  `a88ad4b`'s message. No objection: floors on both sides cover truncation and full precision
  alike, and rounding up is not what Notion does.
- `post` is still the only writer of a Runs row in code: `runs_ds` is named at
  `scripts/run/runs.mjs:395` and `:406` and nowhere else, and `.tasks(` is called only on
  `tasks_ds`, `epics_ds` and `releases_ds`.
- AC1's other three doors, which no unit test can close. I searched every transcript under
  `~/.claude/projects` for `R-0054`, `R-0055`, `R-0060` and `R-0061`: the only files that name
  them are this session's own and its subagents'. A row a model had written through the connector
  would carry its Name in the call that wrote it. So the script path is the path that wrote the
  copies, and it is the path the key closes. Recorded here because the repo holds this evidence
  nowhere else.
- No `docs/reports/T0.30.md` and no `docs/log/T0.30.md`, and the ticket's **Report** section
  (`docs/tickets/T0.30.md:42`) is empty. §5 puts both at step 8, after this review at step 5, so
  there is no red-first table to read yet: **no new test on this branch is yet recorded as
  observed failing first**, and `report-check.mjs` refuses the report without it. Not a finding —
  there is nothing to read — but Should 4 names what that record owes.

One Must stands, then five Shoulds. Ranked.

## Must

1. **AC2 and TC2 are not met at this commit, and nothing on the branch says so.**
   `docs/tickets/T0.30.md:19` — "Remove R-0055, R-0057, R-0058 and R-0061, keeping the first row
   of each run" — and `:29-30`, "The Runs database holds one row per run", are Build and Criteria,
   not notes. No hunk of the diff removes a row, and none could: the connector exposes no archive
   and `update-page` no `in_trash`, and guidelines §5 refuses a write to the Notion API from a
   command line, so the deletion cannot be made from this side at all. The branch's own
   `scripts/run/runs.mjs:13-14` puts the duplicate set at "thirty-seven rows in Runs described
   fifteen runs" — twenty-two copies where AC2 names four — so even on the reading most
   favourable to the branch, AC2's "one row per run" is not shown and TC2's query
   (`docs/tickets/T0.30.md:36`, "finds no two rows with the same task and start") would still find
   pairs. I did not query the board, so I cannot report its present state; that is the finding's
   second half — a reader of this branch cannot either. The ticket's Report is empty, there is no
   addendum, and the diff records nothing about the rows. A PASS here would open the guard's
   self-merge door on a ticket half of whose criteria stand open, and the analytics the Objective
   names would stay wrong with the board marked Done.
   *Smallest correction:* say on the branch what is true — an addendum section in
   `docs/tickets/T0.30.md`, or the step 8 report's AC table, naming AC2 and TC2 as blocked on a
   hand that can delete a Notion page, listing the rows to go by name and the count behind the
   four the ticket knew about — and leave the close to the human's word rather than the run's own
   merge. AC1 and TC1 are done and are not in doubt; it is the second criterion that needs the
   door held open.

## Shoulds

1. **The branch still asserts, in one place, the cause it disowns everywhere else.**
   `scripts/run/runs.test.mjs:603`: "The SessionEnd hook fires again when a session is cleared,
   resumed or exited, and reads the same transcript from the same first timestamp."
   `scripts/run/runs.mjs:15` now reads "What fires the hook again is not established here", and
   §2 and the README were cut to match. `a88ad4b`'s message says the four places that named it
   "now say only what the branch can show"; there were five. *Smallest correction:* that comment
   opens "A second read of the same transcript — the duplicate R-0055, R-0057, R-0058 and R-0061
   are — writes nothing", with no cause in it.
2. **The board count in `scripts/run/runs.mjs:13-14` has no source a reader can follow.** It is
   true — the run read the board to get it — but the branch says so nowhere, and it is the one
   number that tells a reader how much larger the duplicate set was than the ticket's four.
   *Smallest correction:* the report carries the board read that produced it, with its date, and
   the docstring cites the report rather than standing alone.
3. **Two new tests name the ticket but not the criterion.** `scripts/run/notion.test.mjs:320`
   heads `describe("run")` with "T0.30 —" where `:261` writes "T0.30 TC1 → AC1" over the test
   beside it. §7.5 wants every test naming its AC. *Smallest correction:* the same marker on
   `:320`.
4. **The red-first record owes eight rows.** `runs.test.mjs:597`, `:606`, `:622`, `:637`, `:648`,
   `:663`, `:671`, `:677`, `:686` and `notion.test.mjs:262`, `:323`, `:343` are the new tests;
   `next-id.test.mjs:265` is an edit, not a new test, and the report should say why it moved (the
   header bump to v1.21 carried T0.28's version assertion with it). *Smallest correction:* one
   table row per test, mutation and count filled, as `report-check.mjs` requires.
5. **First write wins, so a resumed run keeps its shorter row.** `runs.test.mjs:622`, "writes
   nothing for a longer read of the same session", asserts it: the later read carries a further
   Status and more tokens and is dropped, so the row can hold an Outcome the run moved past and
   Tokens below what it spent — in the data §2 keeps for park rate and the weight tuning. The
   ticket chose this (`docs/tickets/T0.30.md:18`), so it does not block. *Smallest correction:*
   one open question with a phase owner in the report; an unrecorded stopgap is a bug
   (`AGENTS.md`).

FINDINGS
