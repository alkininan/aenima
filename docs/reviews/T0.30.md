# T0.30 — review

Commit reviewed: `a9c6432` (pass 4, the addendum round — `9d00500` the addendum recorded,
`fdf9a8d` main taken in by hand, `a9c6432` the two written files settled — over
`git diff origin/main...HEAD` whole, `origin/main` at `3bdf6e9`).

Read cold against `docs/tickets/T0.30.md`, its Addendum of 2026-09-23 included, and the two
sections its *Cited* block carries (`docs/guidelines.md` §2 and §7, as this branch leaves them).
No `Spec` line; *Rules* names `CLAUDE.md`, `AGENTS.md` and `docs/guidelines.md`, and §4 and §5
steps 4, 5, 8 and 9 are what this pass held the branch to.

What I checked myself:

- Every hunk. Eleven files: `scripts/run/runs.mjs` (the module docstring, `startMinute`,
  `runKey`, the key check in `post`, the Started comment in `rowProperties`),
  `scripts/run/notion.mjs` (the `run` shaper, `runs()`, `rows()` lifted out of `tasks()`), the two
  test files, `docs/guidelines.md` (header to v1.26, §2's closing sentence),
  `scripts/run/README.md`, `docs/build-log.md` (Specs line to v1.26, the T0.30 list line), the
  ticket, the report, the log entry, and pass 3's verdict, which this file replaces.
  `git status --porcelain` is clean, so the diff is the whole change, and nothing in it is outside
  the ticket: `rows()` is the lift that lets `runs()` share `tasks()`'s paging.
- `node scripts/run/review-scope.mjs docs/tickets/T0.30.md`: `files` are `notion.test.mjs` and
  `runs.test.mjs`, `named` empty, `missing` empty. `pnpm vitest run` over the two: **2 files, 63
  tests, all passing** — the count the report gives.
- Criteria ↔ Tests both ways: AC1 ↔ TC1, AC2 ↔ TC2. The twelve new tests all carry
  `T0.30 TC1 → AC1`. TC2 is a board query, not a file — Should 1.
- The red-first record: twelve new tests, twelve rows under *Tests written*, each with its
  mutation (M1–M6) and its count. The two edited `post` cases (`tasks` → `runs`) are edits, not
  new tests, and owe no row. `next-id.test.mjs` is not in the diff, as the report says.
- The key against the code around it. `parseTranscript` returns `page` and `started`
  (`runs.mjs:278`, `:292`), the two `runKey` reads; `related` and `plain` exist in `notion.mjs`
  (`:75`, `:60`); `rows` is a hoisted declaration inside `client()`, so `runs()` reaches it.
  `runs_ds` is named at `runs.mjs:396` and `:407` and nowhere else under `scripts/` or
  `.claude/`, and `.claude/skills/ticket/SKILL.md:511` tells the model the row is not its to
  write — `post` is the one writer, so AC1's three doors (preflight, merge, Done) all pass through
  this key. Both sides floor to the minute through `Date.parse`, so an offset the API returns and
  the `Z` the script wrote read alike; a row with no Started keys `null` and matches nothing; two
  tasks in one minute stay two runs; an idle run keys on its minute alone.
- The SessionEnd hook is `node "${CLAUDE_PROJECT_DIR}/scripts/run/runs.mjs" --hook`
  (`.claude/settings.json:67`) — the checkout's copy, as the report's TC2 section says, which is
  why runs standing at main went on writing copies after the 22 were cleared and stop once this
  lands.
- v1.26: `origin/main`'s header is v1.24, `origin/t0-26`'s is v1.25, and the header's own line
  says so. The build log's Specs line reads v1.26, and `log-index.mjs` sorts by timestamp then id,
  so T0.30 at 05:50Z sits rightly between T0.32 (04:45Z) and T0.33 (06:01Z).
- AC2 I could not check myself: this session has no board read, and Bash here is for the diff,
  the tests and this file. The Addendum on the ticket — the human's word — says AC2 is met and the
  22 copies gone, and the report says R-0055, R-0057, R-0058 and R-0061 return nothing and
  R-0054, R-0056 and R-0060 stand. I take the board's state from those two, and say so here.

No Must stands. Four Shoulds, ranked.

## Shoulds

1. **TC2 is red on the board and the AC table says otherwise.** `docs/reports/T0.30.md:8` —
   "TC2: the query below, green for the rows AC2 names" — while `:75–99` says the query still
   finds seven copies (R-0077, R-0079, R-0081, R-0082, R-0083, R-0085, R-0086), every one written
   by main's `runs.mjs` on 2026-09-23 after the branch was cut. Not a Must: the ticket's Addendum
   (`docs/tickets/T0.30.md:51`) says AC2 is met and to land it, the seven are the residue of this
   fix not yet being where the hook runs, and nothing a run holds can remove a Notion row — a Must
   here would hold back the one change that stops the set growing and hand the run nothing it can
   do. *Smallest correction:* the AC2 row's Test cell reads "TC2 red on seven post-cut copies,
   named below for the hand that cleared the 22".
2. **The log entry ends where the first round ended.** `docs/log/T0.30.md:40–45` — "The
   twenty-two copies are still there … the ticket waits at Review for a hand that can do it" —
   stands in the present tense under a 05:50Z stamp, and the addendum round has since cleared
   them and the human said to land it. The build log is the record that outlives the thread.
   *Smallest correction:* one closing paragraph dated to the addendum round — the 22 gone by
   hand, seven more from runs at main until this lands, AC2 met on the human's word.
3. **Two bullets in the report contradict each other about the Spec field.**
   `docs/reports/T0.30.md:182` says the field cites `guidelines v1.21` and drifted; `:190` says
   "No spec named in the Spec field, so `version-drift.mjs` had nothing to check; nothing
   drifted." One is stale. *Smallest correction:* drop `:190`, or fold it into `:182`'s account of
   why the script read the field as empty.
4. **A pure-function test sits under `describe("post")`.** `scripts/run/runs.test.mjs:597`
   "matches a row written with seconds against the minute the board reads back" calls
   `rowProperties` and `runKey` and never `post`, and `:671` "keys the row written with seconds
   and the row read back without them alike" makes the same claim, less the `rowProperties`
   step. *Smallest correction:* move `:597` under `startMinute and runKey`, or fold its
   `rowProperties` line into `:671`.

PASS
