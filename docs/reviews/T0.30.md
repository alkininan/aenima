# T0.30 — review

Commit reviewed: `3a1148b`

Read cold against `docs/tickets/T0.30.md` and the two sections its *Cited* block carries. Both are
byte-identical to `origin/main:docs/guidelines.md` — §2 (lines 109–228 there, 48–167 in the pack)
and §7 (737–746 / 173–182) — so the ticket's reading is the document's, not the branch's.

What I checked myself rather than taking from the branch:

- Every hunk of `git diff origin/main...HEAD`. Five files: `scripts/run/notion.mjs` (the `run`
  shaper, a `runs()` method, `rows()` lifted out of `tasks()` so both share the paged query),
  `scripts/run/runs.mjs` (`startMinute`, `runKey`, the key check in `post`, the module docstring),
  the two test files, and `docs/tickets/T0.30.md`, the pack §5 step 2 owes. Every hunk is the
  Build's second bullet or the plumbing it needs; nothing in the diff is outside the ticket.
  `git status --porcelain` is clean, so the diff is the whole change.
- `node scripts/run/review-scope.mjs docs/tickets/T0.30.md` lists `scripts/run/notion.test.mjs`
  and `scripts/run/runs.test.mjs`, nothing `missing`. `pnpm vitest run` over the two: **2 files,
  61 tests, all passing** — 17 and 44, against 15 and 33 on `origin/main`. No test was deleted;
  the two `post` tests the diff edits swap the `tasks` stub for a `runs` one and keep their
  assertions.
- That `post` is the only writer of a Runs row in code. `scripts/run/runs.mjs` is named in exactly
  one command, `.claude/settings.json:67`'s SessionEnd hook; `.tasks(` is called only on
  `tasks_ds`, `epics_ds` and `releases_ds` (`pick-next.mjs`, `threads.mjs`, `rows.mjs`), so moving
  the Runs read to `runs()` reaches nothing else.
- AC1's other three doors, which no test can close: a run's preflight, its merge and the step that
  marks a task Done are the model's own moves through the connector, and `scripts/hooks/guard.mjs:1304`
  lets a `create-pages` call through whenever no `Status` is set — a Runs row has none. So I looked
  for one: across the 33 transcripts under `~/.claude/projects/-Users-alkininan-dev-aenima/`, no
  line carries a `notion-create-pages` call naming the Runs data source (`dff7570e…`), and none of
  `R-0055`, `R-0057`, `R-0058`, `R-0061` appears in any transcript at all. A row a model wrote
  would carry its own Name in the call that wrote it. The copies came from the script, which is
  the one path the key now closes — see Should 3.
- The key against the shapes it meets. `runKey` strips dashes from both sides, so the claim
  command's page id and the relation's dashed id key alike; `startMinute` floors to the minute in
  UTC through `Date.parse`, so an offset Notion returns (`+02:00`) and a `Z` the script wrote read
  the same. A row with no `Started` keys to `null` and matches nothing; a run with no start is
  written rather than dropped, and says so at `scripts/run/runs.mjs:344-346`.
- The branch carries no `docs/reports/T0.30.md` and no `docs/log/T0.30.md`. Guidelines §5 puts
  both at step 8, after this review at step 5, so there was no red-first table to read: **no new
  test on this branch is yet recorded as observed failing first**, and `report-check.mjs` owns
  that record. Not a finding here — there was nothing to read. Should 2 names what it must carry.

No Must stands. Six Shoulds, ranked.

## Shoulds

1. **First write wins, so a resumed run keeps its shorter row — and the diff's own test says so.**
   `scripts/run/runs.test.mjs:613` is "writes nothing for a longer read of the same session": the
   later read carries a further Status and more tokens, and it is dropped. `/clear` starts a new
   transcript with a new first timestamp and so keys anew, but a resumed session appends to the
   same file, and its row then holds the Outcome the run had not yet moved past and Tokens below
   what it spent — in the data §2 keeps for park rate and the weight tuning. The ticket chose
   this — `docs/tickets/T0.30.md:18`, "writing a row whose key already exists changes nothing" —
   so it does not block. *Smallest correction:* one line in the report's open questions, with a
   phase owner, naming first-wins and the resumed session as what it costs; an unrecorded stopgap
   is a bug (`AGENTS.md`).

2. **AC2 and TC2 have no record anywhere I can read.** `docs/tickets/T0.30.md:29-30` — "R-0055,
   R-0057, R-0058 and R-0061 are gone; R-0054, R-0056 and R-0060 remain" — and `:36`, "a query
   over Runs finds no two rows with the same task and start", are both board work, invisible in a
   diff and not mine to query. Nothing on the branch says either was done. Step 8's report is the
   place, and it comes after this pass. *Smallest correction:* the report's AC table gives AC2 the
   deletion of the four rows by name and TC2's count, beside the red-first rows for TC1.

3. **The module docstring answers the Build's first bullet by assertion, and asserts the opposite
   of what the ticket says.** `scripts/run/runs.mjs:13-15`: "the hook fires again when a session is
   cleared, resumed or exited". The ticket at `docs/tickets/T0.30.md:13-16` reads "The copies were
   made by later runs … which points at the step that closes a merged task, not at the run's own
   step 9". Nothing on the branch settles which is true, and the fix only reaches the script path,
   so the difference decides whether the ticket fixed anything. I checked and the branch is right
   (the transcript evidence above), which is why this is a Should and not a Must. *Smallest
   correction:* the report carries the observation — no connector create into the Runs data source
   in any local transcript, and no transcript naming the four copies — and the docstring says
   "read from the transcripts" rather than stating the cause flat.

4. **§2 and the run README still describe a script that writes a row every time it runs.**
   `docs/guidelines.md:196-198` — "Written by a script at session end from the local transcript
   … Since T0.12 the script is `scripts/run/runs.mjs`" — and `scripts/run/README.md:232-244`
   both spell out what `runs.mjs` reads and writes, and neither says a run already recorded writes
   nothing. The same paragraph carries "Since T0.12" and "Since T0.23" for exactly this kind of
   change. The Build names neither file and `CLAUDE.md` forbids extending scope, so leaving them
   was right here. *Smallest correction:* one clause in §2's Runs paragraph — a row keyed on task
   and start minute, a second post writing nothing — in this ticket or the next one that touches §2.

5. **`client().runs()` has no test of its own.** `scripts/run/notion.mjs:245-247` is the new method;
   the tests cover the `run` shaper directly (`notion.test.mjs:292`) and `post` against injected
   fakes, so nothing reads the method itself — a wrong shaper or a wrong data source on that line
   would pass. `tasks()` has its pagination test at `notion.test.mjs` in the `client` describe.
   *Smallest correction:* one canned-fetch test that `client(…).runs("ds")` returns rows shaped by
   `run`, mirroring the `tasks` one.

6. **Two new markers read as another ticket's.** `scripts/run/runs.test.mjs:576` and `:652` both
   open "TC1 → AC1", and the same file already carries unqualified "TC1 → AC1" at `:140`, `:231`
   and `:534` for T0.12's criteria and at `:682` for T0.10's. `scripts/run/notion.test.mjs:290`
   names T0.30 but no criterion, where that file qualifies its markers at `:140` ("T0.16 TC1 → AC1
   (carries T0.15)"). *Smallest correction:* "T0.30 TC1 → AC1" on all three, as
   `scripts/hooks/hooks.test.mjs:124` writes it.

PASS
