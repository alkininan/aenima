# T0.12 — review

Reviewed commit `455b42d` (branch `t0-12`), addendum round, pass 3. The diff read is
`git diff origin/main...HEAD`. Local `main` is still `ec531f3`; `origin/main` is `11dc6e3`, the
same as the remote. Every hunk was read, and so was `git diff ff9dca6 HEAD` for pass 2's fixes.

- TA1: `git merge-tree --write-tree origin/main t0-12` reports no conflict. The lines the diff
  removes from main in the four conflicted files are the later-ticket placeholders and reflows
  whose words survive.
- TA2: skill 0f comes after 0e, and the README and §5 step 0 put the mirror after the deploy
  check. TA4: header v1.10 above v1.9, and §8 reads v1.6, v1.8, v1.9 and this version.
- Tests: `review-scope.mjs` lists 14 files with `missing: []`. With TA2's `readme.test.mjs` that
  is 15 files, 329 passed. `runs.test.mjs` alone gives 35 passed, and the three live T0.10
  transcripts ran rather than skipped.
- `report-check.mjs docs/reports/T0.12.md origin/main` gives `ok`, 67 rows.
- Pass 2's two Musts are settled. `runsIn` reads commands with the guard's `parse` and `target`,
  and the heredoc test reddens under the old `MERGE_PATTERN`. TA3 now names the eight tests the
  file holds.
- HEAD's `runs.mjs --dry-run` and pass 2's (`ff9dca6`) were run over the 20 most recent
  transcripts on this machine that ran `claim.mjs`. Nothing was posted. They disagree on two:
  - The parent session `eb5f836a` reads T0.12 at HEAD and T0.13 at `ff9dca6`: its heredoc edits.
  - Session `748b808b` (`objective-agnesi-49f693`) reads `T1.5, Decision` at HEAD and
    `T0.14, Stopped` at `ff9dca6`. HEAD is right. See finding 1.

## Findings

1. **Must: AA3's "or apply" half is untested. No fixture has a step-0 claim that did not merge,
   followed by step 1's claim, so two mutations that misread real runs leave all 35 tests green.**
   AA3 (`docs/tickets/T0.12.md:440–444`) says the row names the run's own task "when step 0
   claimed and released another task to merge or apply before step 1 claimed it".
   - **What the tests cover.** Every multi-claim test puts a `gh pr merge` on step 0's claim
     (`mergedInStepZero`, `scripts/run/runs.test.mjs:280–289`). That claim is dropped by the
     `merged` flag, and `others` never holds more than one claim.
   - **Not covered: a refused apply.** Skill 0b releases after a refused apply and posts
     nothing. The same goes for a merge the run only checked with `permission.mjs merge`, as
     `748b808b` did. Such a claim stays in `others`.
   - **Also not covered: two simple commands in one Bash call.** `748b808b` ran
     `claim.mjs --task T0.14 … && permission.mjs merge; release.mjs; claim.mjs --task T1.5 …`
     as one Bash command.

   Both mutations were run against a scratch copy, and each gives `Tests 35 passed (35)`:
   - (a) `scripts/run/runs.mjs:268`: fall back to the earliest unmerged claim,
     `?? others[0] ?? null`. A run is killed after a refused apply, before its own first Status.
     In the order five of six recorded runs use (`In progress` before `claim.mjs`), the row
     would name the applied task.
   - (b) `scripts/run/runs.mjs:84–86`: `break` after the first claim in a command. The scratch
     copy reads the real session `748b808b` as `T0.14, Stopped` instead of `T1.5, Decision`.

   The code at HEAD is right on both. The finding is that nothing keeps it right. I am sure the
   gap is real. I tag it Must, not Should, because it is half of a named criterion, not a
   stronger test of a covered one.

   *Correction:* add one test to the `TA3 → AA3` block, modelled on `748b808b`:
   - One Bash command
     `node scripts/run/claim.mjs --task T0.13 --page <A> --branch t0-13 && node scripts/run/permission.mjs apply; node scripts/run/release.mjs; node scripts/run/claim.mjs --task T0.96 --page <B> --branch t0-96`.
   - `In progress` on B written before that command, and no Status after it. Expect
     `T0.96, Stopped`.
   - The same with `Decision` on B after it. Expect `T0.96, Decision`.

   Name the test in TA3, and give it a red-first row with both mutations above.

## Shoulds

1. `docs/guidelines.md:154–156`, `scripts/run/README.md:178` and `scripts/run/runs.mjs:22` say
   the task is "the last claim that did not merge before writing a Status of its own". The code
   (`runs.mjs:268`) prefers the last unmerged claim that wrote a Status. It falls back to the last
   unmerged claim only when none did. Take the claims [B wrote Decision, C later wrote nothing].
   The text reads C and the code reads B. Say the preference in the three places, as the
   `parseTranscript` comment at `runs.mjs:246–247` already does.
2. `scripts/run/runs.mjs:186` declares a local `const parse` that shadows the guard's imported
   `parse` (`:43`). `:252` names its loop variable `run`, which shadows the `run` boolean at
   `:212` that the function returns. Both are correct today, and both are one rename from a
   confusing bug.
3. `scripts/run/runs.mjs:89–91` re-implements the guard's `ghWords` (`scripts/hooks/guard.mjs:531`)
   inline. The docs say claims and merges are read with the guard's own parser, and for
   `gh pr merge` the word-skipping is a copy. Exporting `ghWords` would make that literal. It
   touches `guard.mjs`, so it may be a next-time change.
4. "does not read a claim.mjs quoted in a later command's prompt as a claim"
   (`scripts/run/runs.test.mjs:232`) sits under the describe tagged `TC1 → AC1` (`:138`). TA3
   claims it too. One tag on the test itself, `TA3 → AA3`, would say which criterion retires it.
5. `docs/reports/T0.12.md:341–342` says pass 2 "ran `parseTranscript` over six real `/ticket`
   transcripts". Pass 2's review says ten, six of which claimed their own task.
6. Wrap: `scripts/run/runs.mjs:24` (141 columns), `scripts/run/README.md:184` (the paragraph
   ends "its replies. A" and then a new line), `docs/log/T0.12.md:11` (177 columns), and
   `docs/reports/T0.12.md:83–84`.

FINDINGS
