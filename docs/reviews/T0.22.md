# T0.22 — review

Commit reviewed: `fa681af` (branch `t0-22`, base `41e2d0f`; local `main` and `origin/main` both at `41e2d0f`). Pass 2.

Read: `docs/tickets/T0.22.md`, guidelines §5 as inlined (v1.12's cite, v1.13 on `main`) and at v1.14 on the branch,
`.claude/settings.json` (`"fallbackModel": ["opus"]`), `.claude/agents/reviewer.md` (`model: fable`),
`docs/reports/T0.19.md` (the re-close round and its open question), `docs/reports/T0.22.md`, the pass 1 verdict, and
every hunk of `git diff main...HEAD` (10 files).

Tests run: `node scripts/run/review-scope.mjs docs/tickets/T0.22.md` gives `named` [], `touched`
`scripts/run/report-check.test.mjs` and `scripts/run/review-model.test.mjs`, `missing` []. `pnpm vitest run` on those
two: 2 files, 26 passed (14 + 12), the counts the report records after pass 1.

Also checked by hand, read-only: `review-model.mjs` given the refusal exactly as this project's transcripts hold it
(`… (error type rate_limit, HTTP 429, request id …, model sent to the API: claude-fable-5)`) with `tried: ["fable"]`
answers `credits`, `stop: false`, `model: opus`; with `tried: ["fable","opus"]` and a lost connection it answers
`stop: true` and names the whole chain. `node scripts/run/report-check.mjs docs/reports/T0.22.md` answers `ok: true`.

Pass 1's Must is settled. Skill step 5 invokes every pass without `model` and asks the script with the models *this
pass* was called on. §5 step 5, the README, and the `nextReviewer` docstring say the same thing. TC1 holds the phrase in
both §5 and the skill. So pass 2 on a model still out of credits gives `tried: ["fable"]` → `opus`, not a stop. Pass
1's Shoulds 1–4 and 6 are fixed as the report says (M18, the heredoc, the `Build 1` tags, `detail` in the `refused`
comment, and M17 holding step 8's template). Should 5 is recorded, and the `reviewChain` docstring now says an unpinned
reviewer has no fallback.

Criteria and tests: AC1 → TC1 (three tests on §5 step 5 and the skill), AC2 → TC2 (five tests: four on the checker and
one on step 8's template), AC3 → TC3 (two tests). The four `T0.22 Build 1` tests and the five `T0.22 Rules` tests are
mapped in the report's AC section. Every new test has a row in the red-first table with its mutation and its count.
M2 survived and is recorded as such, with the strengthening commit named. The three-pass cap, Must/Should tagging and
the verdict file are untouched. The v1.14 header entry is newest-first, as the file's convention has it.

Step 8 work still to do after this pass, and not findings: the report's reviewer table needs pass 2's row, and
`docs/log/T0.22.md` is not written yet.

## Musts

None.

## Shoulds

1. **Should (uncertain about the real shape).** `scripts/run/review-model.mjs:85`:
   `/\bnot_found_error\b[\s\S]*\bmodel\b/i`. This matches the raw API body, which is the fixture at
   `review-model.test.mjs:68`. Every subagent refusal in this project's transcripts has Claude Code's other shape:
   `(error type rate_limit, HTTP 429, …, model sent to the API: claude-fable-5)`. Two consequences follow.
   (a) In that shape a 404 would most likely read `error type not_found, HTTP 404`. `not_found_error` does not match
   it, so a pinned model that does not exist would read `other` and stop. Default 3 says it falls back.
   (b) If the raw type does appear, the suffix `model sent to the API` supplies the word `model` for any 404, whatever
   was not found. No real 404 is in the transcripts, so neither case is confirmed. Smallest correction: read the
   status too, as the other branches do (`status === "404"` or `error type not_found`), and require the model to be
   what was not found rather than any later mention of `model`. Otherwise, drop the branch and list model-not-found
   as a deliberate stop in the defaults.
2. **Should.** `scripts/run/README.md:137`: "an API error with a 429, a 5xx, an overloaded model or a lost
   connection". The model-not-found case that `causeOf` also reads as availability is missing from this list, and so
   is the `causeOf` docstring (`review-model.mjs:63-67`). If Should 1 keeps the branch, name it in both. If it drops
   the branch, nothing changes here.
3. **Should.** Skill step 8 asks for "the model it ran on". `report-check.mjs` accepts only the model as the chain
   spells it: `opus` and `` `fable` `` pass, but `claude-fable-5`, the name the refusal itself prints, and
   `Opus (Fable refused)` are refused. The refusal message names the chain, so a run can correct itself. Four words in
   step 8 would save it the round-trip: "as the chain names it".
4. **Should (carried from pass 1, recorded there).** `review-model.test.mjs:40` still pins the unpinned-reviewer chain
   `["opus"]`, a shape the fallback can never use. The docstring now says so. The test could assert that a refusal
   on that chain stops, rather than pinning the chain alone.

PASS
