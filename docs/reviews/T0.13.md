# T0.13 — review

Commit reviewed: `836ad01` (branch `t0-13`, addendum round; `origin/main` at `6526f96`).

## What was checked

- Diff against main: five files, docs only (`docs/guidelines.md`, `docs/build-log.md`,
  `docs/log/T0.13.md`, `docs/reports/T0.13.md`, `docs/tickets/T0.13.md`). No code, schema,
  migration or test file, as the Rules require.
- AA1 / TA1: `git merge-tree --write-tree --name-only origin/main HEAD` exits 0 with no conflicted
  file; `origin/main` is an ancestor of HEAD. The §5 capability-boundary paragraph is the same text
  round one's `6571ed6` held (diff exit 0), set on top of main's paragraph, and main's old ending
  ("on no path a run follows") is gone, not left beside it.
- AA2 / TC3: `version-drift.mjs "guidelines v1.12 §5"` gives `drifted: false` (repo 1.12);
  `spec-sections.mjs` for guidelines §5 contains "on no path at all" once and "Production is the
  same role" once.
- AA3 / TA3: `pnpm vitest run scripts/run/log-index.test.mjs` passes, 12 tests. `log-index.mjs`
  was also run once and gave `written: false`, 38 entries: the list is already the generated one.
  The tree was still clean afterwards.
- TC2: `curl -sI https://aeni.ma/sign-in` gives `HTTP/2 200`; `curl -sI https://aeni.ma/app`
  gives `HTTP/2 307`, `location: /sign-in`.
- Criteria and Tests match both ways: AC1–TC1, AC2–TC2, AC3–TC3, AA1–TA1, AA2–TA2, AA3–TA3.
- `review-scope.mjs docs/tickets/T0.13.md` lists no files (`files: []`, `missing: []`).
- Red-first record (report, "Tests written"): every test has a row with no empty cell. TC2 has no
  red. The report says so and says why (`docs/reports/T0.13.md:105`): the unauthenticated routes
  do not touch the database. So the part of AC2 that does touch the database, and all of AC1, rest
  on the human's quoted reply. That limit comes from the ticket; it is not a gap in the run.

## Shoulds

1. **Should (unsure).** `docs/guidelines.md:533-535` and header line 2 say: "so the admin
   credential is on no path at all — not a run's and not the deployed app's". TC1's evidence covers
   Vercel's *Production* `DATABASE_URL` only. The ticket, the thread reply and the report never
   name Vercel's Preview or Development environments. T0.16's report (`docs/reports/T0.16.md:19`)
   treats a preview deployment as a real target. If Preview still names the admin role, every
   branch a run pushes gets a preview build holding the admin credential. The line would then be
   false, and the ticket's Rules forbid that ("The guidelines line states what is true"). This is
   not a Must because the diff writes the sentence AC3 asks for, and the ticket covers Production
   only. The gap is in what the ticket asked for, so §5 step 5 sends it to Backlog.
   Smallest fix: a Backlog Technical task under E0.2, or one question on the thread asking which
   role Preview's `DATABASE_URL` names, if Preview has one. If the answer is the admin role, either
   narrow the sentence to production or change Preview too.
2. **Should.** `docs/log/T0.13.md:13-14` says: "It waited at Review long enough for five tickets to
   land ahead of it, each bumping the guidelines header and adding a build-log line". Six pull
   requests merged to main after round one's close: T0.16, T0.14, T2.8, T0.17, T0.12 and T0.20.
   Four of them bumped the header (v1.8–v1.11), and the report names those four
   (`docs/reports/T0.13.md:23-24`). Smallest fix: "for six tickets to land ahead of it, four of
   them bumping the guidelines header and all of them adding a build-log line".
3. **Should.** The version numbers in the ticket and the report no longer agree:
   - `docs/tickets/T0.13.md:60`: AC3 still says "docs/guidelines.md is v1.7".
   - `docs/tickets/T0.13.md:70`: TC3 still cites `"guidelines v1.7 §5"`.
   - `docs/tickets/T0.13.md:13-14`: "since v1.6 — the one AC3 names —" went stale when round one
     moved AC3 to v1.7.
   - `docs/reports/T0.13.md:53`: the AC3 row paraphrases AC3 as "is at the next version" instead
     of quoting it.

   v1.12 is correct under the addendum (AA2, ticket lines 103-106). Smallest fix: quote AC3 as
   the ticket holds it in the report row and add that AA2 moves it to v1.12. Or bring AC3 and TC3
   to v1.12, as round one did, and drop "the one AC3 names".
4. **Should.** `docs/tickets/T0.13.md:101`: "TA1 → AA1 · TA2 → AA2 · TA3 → AA3". Only the report
   says what each test runs, and the ticket never names TA3's file, `scripts/run/log-index.test.mjs`.
   That is why `review-scope.mjs` gives this ticket an empty list. Smallest fix: the round's claim
   paragraph (`docs/tickets/T0.13.md:103-106`) names the three checks and gives TA3's file by path.
5. **Should.** The header jumps from v1.8 (`docs/guidelines.md:17`) to v1.6 (`:22`). With this
   change now at v1.12, nothing in the file says why v1.7 is missing. Only the report does
   (`docs/reports/T0.13.md:33`). Smallest fix: one clause in the v1.12 entry, e.g. "this change
   held v1.7 on its branch; that number never reached main".

PASS
