# T0.13 — review

Commit reviewed: `a7e25cc` (branch `t0-13`, re-close round under T0.21; `origin/main` and `main` at `cbdc9aa`).

## What was checked

- The ticket file, and both cited texts read at their versions: the inlined guidelines v1.5 §5
  matches `docs/guidelines.md` at `b53c4c0` (the branch base) line for line, and the inlined v1.12
  §5 matches `cbdc9aa`. The only difference in each is the `---` that closes the section.
  `docs/reports/T0.10.md:303-304` open question 6 is quoted exactly.
- Diff against main: six files, docs only (`docs/build-log.md`, `docs/guidelines.md`, and
  `docs/log`, `docs/reports`, `docs/reviews`, `docs/tickets` `T0.13.md`). No code, schema,
  migration or test file, as the Rules require. `docs/guidelines.md` has two hunks against
  `origin/main`: the header entry and the capability-boundary paragraph. That paragraph is
  byte-identical to the rebase round's (`a935d08`). Main's v1.12 entry stays whole under the new
  v1.13 entry. `docs/build-log.md` gains one list line.
- AB1 / TB1: `git merge-tree --write-tree --name-only origin/main HEAD` exits 0 with no conflicted
  file, and `origin/main` is an ancestor of HEAD. The red is confirmed too:
  `merge-tree cbdc9aa a935d08` exits 1 with `CONFLICT (content)` in `docs/build-log.md` and
  `docs/guidelines.md`.
- AC3 / TC3 / AA2: `version-drift.mjs "guidelines v1.13 §5"` gives `drifted: false` (repo 1.13).
  The §5 body from `spec-sections.mjs` contains "on no path at all" once, "Production is the same
  role" once, and main's old "on no path a run follows" zero times.
- AB2 / TB2: `gated.mjs` gives `ok: true`, `reasons: []`, over the six files above.
  `loosening.mjs` gives `ok: true`, `reasons: []`. The scratch commit `620543c` exists; its parent
  is `4cadfc8` and it changes only `.claude/settings.json`. `loosening.mjs origin/main...620543c`
  gives `ok: false` with the one Stop-hook reason the report quotes. So the detector could have
  refused this diff.
- AA3 / TA3: `review-scope.mjs` lists `scripts/run/log-index.test.mjs` (named, none missing);
  `pnpm vitest run scripts/run/log-index.test.mjs` passes, 1 file, 12 tests.
- AC2 / TC2: `curl -sI https://aeni.ma/sign-in` gives `HTTP/2 200`; `curl -sI https://aeni.ma/app`
  gives `HTTP/2 307`, `location: /sign-in`.
- AB3 / TB3: the report quotes `gated.mjs` and names this file. The rest of AB3 happens after this
  verdict and is on the board.
- Criteria and Tests match both ways: AC1–TC1, AC2–TC2, AC3–TC3, AA1–TA1, AA2–TA2, AA3–TA3,
  AB1–TB1, AB2–TB2, AB3–TB3.
- Red-first record (report, *Tests written*): every test has a row with no empty cell. TC2 has no
  red, and the report says why: an unauthenticated route does not touch the database. TC1 and the
  signed-in half of AC2 rest on the human's quoted reply, as the ticket defines them. TB3's green
  comes after the close, as the ticket defines it.
- Carried from the addendum round, not raised again: the §5 claim "on no path at all" is backed by
  Production only. Vercel's Preview environment is filed at Backlog as *Check Preview's database
  role on Vercel* and is open question 2 in the report.

## Shoulds

1. **Should.** `docs/reports/T0.13.md:78` (the AB3 row) says: "the verdict is
   `docs/reviews/T0.13.md`, written by the reviewer for this round". But *Reviewer passes and
   findings* (`:157`) has sections only for round one and the addendum round. At `a7e25cc` the file
   on the branch was still the addendum round's verdict, for `836ad01`. Smallest fix: add a
   `### Re-close round (2026-09-16)` section recording this pass, its commit and these Shoulds.
2. **Should.** `docs/reports/T0.13.md:71` (the AC2 row) says: "Observed 2026-09-14 15:28 UTC and
   again 2026-09-15 20:15 UTC, quoted below". The block quoted below is the 2026-09-16 09:13 UTC
   run. Also, `:58-61` lists `docs/reviews/T0.13.md` under *Changed*, but against `origin/main` it
   is added, like the other three `T0.13.md` files. Smallest fix: add the 2026-09-16 time to the
   row, and move the review file to *Added*.
3. **Should (outside this ticket's settlement rule).** `docs/guidelines.md:647`, §8 Seed, says:
   "`T0.21 Merge by default` (…) — this version." Once v1.13 lands this is no longer true, since
   T0.21 is v1.12. The addendum rule allows only the one §5 sentence and the header here, and
   T0.20 did not name itself in §8 either. Smallest fix: next time §8 is touched, "— v1.12" in
   place of "— this version".

PASS
