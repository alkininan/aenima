# T0.26 — review

Commit reviewed: `b31fad2` (the addendum round: origin/main taken into t0-26; fresh context).

Read: `docs/tickets/T0.26.md` with its Addendum; the whole of `git diff origin/main...HEAD`, every
hunk, and the three commits on the branch past `origin/main` (`f9c23ab`, `f45971b`, `b31fad2`)
one by one; `docs/guidelines.md` §4 and §5 as HEAD holds them beside `origin/main`'s header;
`docs/reports/T0.26.md`, `docs/log/T0.26.md`, build-log open question 40 as this diff leaves it;
the T1.4 and T3.1 reports' migration and skip passages; `origin/main:scripts/run/next-id.test.mjs`.

Tests run — the eight files `node scripts/run/review-scope.mjs docs/tickets/T0.26.md` lists,
nothing `missing`, nothing else:

    pnpm vitest run scripts/hooks/guard.test.mjs scripts/run/comments.test.mjs \
      scripts/run/gated.test.mjs scripts/run/loosening.test.mjs scripts/run/permission.test.mjs \
      src/db/opportunity-key.db.test.ts src/db/refinement.db.test.ts \
      src/test/migration-gate.test.ts

    Test Files  8 passed (8)
         Tests  322 passed (322)

The two `.db.test.ts` files were then run alone with the verbose reporter: **21 passed, none
skipped** — six in `opportunity-key`, fifteen in `refinement` (T3.2's six came in with main).
Both `drizzle/0015` and `0016` are on `origin/main`, so AC4's second half holds in the product.

## What the round owed, and what holds

The addendum asked for one thing: a branch that carries `origin/main`, settled where a script may
not settle it. The merge-base of HEAD and `origin/main` is `3bdf6e9`, main's tip. The guidelines
header carries main's v1.24 line and this ticket's own at **v1.25**, the first number free above
main's newest. `scripts/run/next-id.test.mjs` is byte for byte main's — the diff against
`origin/main` for that file is empty — so main's rule-pinning assertion is the one kept.
`src/db/refinement.db.test.ts` is main's fifteen tests over this ticket's gate, asked about
`0015` and `0017` each on its own; the `SKIP` reads `GATES.some(gate => gate.skip)` and the
offline case still resolves to `OFFLINE`. The build log's generated sections carry both sides'
entries.

**AC1–AC3.** Unchanged from pass 3 and re-traced on the merged tree. `appliedMigrations` reads
the tag out of the `applied` note and only after the human's earliest `apply`; the composer's
sentence (`comments.mjs:415`, `Applied <file> to the shared database.`) is what the new
`APPLIED_CLAUSE` reads, and the "Applied nothing" form matches nothing, which is right.
`coveredBy` matches by `migrationTag`, covers the journal and snapshots only when every migration
beside them is covered, and never on their own; `...weakened` is spread outside the filter. All
seven of `verify`'s returns carry `applied`, `judge` hands them to `reviewed` with `?? []`, and
`consumedApplies` answers `{ tags: [] }` on marker, page, token, board and thread failures. I
asked the checkout's guard whether `node scripts/run/gated.mjs` — now a script that reads the
Notion API from a branch copy the guard will read — is refused under rule (i): it is allowed, on
both argument shapes.

**AC4–AC5.** `gateOf` and `landedOnMain` unchanged; every unprovable case answers `landed: true`.
`b31fad2` is this round's one change past the merge: the AC5 assertion now reads every `file:`
the suite hands the gate with `matchAll` rather than the first, which the two-migration
`refinement.db.test.ts` needed for AC5 to still mean what it says. That is inside "the five ACs
above are what the merged tree still has to satisfy".

**Criteria ↔ Tests.** Every AC has tests naming it; the five `diffGating`/`diffsLoosened` cases
name none and are recorded as open question 3 with an owner, as before.

## Findings

**Must 1 — the record this diff writes describes the branch before the addendum, not after it.**
Three places say what the diff no longer does:

- `docs/log/T0.26.md:66` — "Guidelines to v1.21." The header this diff sets is v1.25
  (`docs/guidelines.md:1`), which the addendum asked for by name. v1.21 is a number main's own
  v1.24 line records as held by an open branch and that, after this merge, no line carries; a
  reader following the log to the version finds nothing.
- `docs/log/T0.26.md:57-63` — "One test had to change that is not this ticket's subject. T0.28
  asserted the guidelines header read `1.20` exactly … It now asserts the rule the assertion was
  reaching for." `git diff origin/main...HEAD -- scripts/run/next-id.test.mjs` is empty. The
  assertion described is main's (`origin/main:scripts/run/next-id.test.mjs:268`, "carries a
  version no older than the one this rule arrived in"), kept per the addendum; this diff changes
  nothing there.
- `docs/reports/T0.26.md:65` — a red-first row for `scripts/run/next-id.test.mjs`, a test this
  diff does not touch; `:175` "moved v1.20 → **v1.21** in this diff"; `:178-183` the same claim
  about next-id; open question 1 (`:193-200`) about a relaxed assertion this diff no longer
  relaxes.

The log entry is the durable one — `log-index.mjs` writes the build log from it — and it states
a version the diff does not set and a change the diff does not carry. Smallest correction: in
`docs/log/T0.26.md`, `v1.21` → `v1.25`, and replace the next-id paragraph with one sentence — the
branch's rewrite of that assertion was dropped for main's at the merge, since main had already
pinned the rule. In `docs/reports/T0.26.md`, drop the next-id row and open question 1, and let
"Changed since this ticket was cut" say what happened: main moved v1.20 → v1.24 under the branch,
so the line is v1.25 and `next-id.test.mjs` is main's. Step 8 rewrites the report after this
pass; the log line is what has to change either way.

**Should 1 — `docs/guidelines.md:5-6` says the opposite of the line above it.** The v1.25 line
reads "main passed it while the branch waited"; main's v1.24 line (`:5-6`) reads "Numbered past
v1.21 and v1.23, which branches still open are holding" — main stepped over v1.21 and left it
for this branch, and the addendum chose the next free number above main's newest regardless.
"main numbered past it" would agree with the line it sits above.

**Should 2 — this round's one code change has no red-first record of its own.** `b31fad2`'s
message says "a banner naming 0017 passed it. Observed, both ways", but the row at
`docs/reports/T0.26.md:66` still carries pass 2's mutation and count ("a banner restored to
`refinement.db.test.ts`", 1 failed / 9 passed → 10 passed). The mutation the `matchAll` change
exists to catch is a banner naming `0017` while `0015`'s is clean; the row should name it and
the count that went green.

**Should 3 — the counts moved with the merge.** `docs/reports/T0.26.md` (AC4 bullet),
`docs/log/T0.26.md:56` and `docs/build-log.md:1165` all say "all fifteen of their tests"; the
two suites now hold 21, T3.2's six having reached `refinement.db.test.ts` on main.

**Should 4 (carried from pass 3) — `scripts/run/gated.mjs:117-118` still says "waits for you
either way"** in the copy the human reads, and the `gated` composer (`comments.mjs:426`) closes
with 'say "merge" here', so one comment names two words for one decision. Recorded with owner
`T-next`; drop "either way".

**Should 5 (carried) — `docs/guidelines.md:401-403`** reads the spending "not off anything a run
says about it" and then names the run's own `applied` note as half of it. Open question 2.

**Should 6 (carried) — `src/test/migration-gate.ts:40` trusts the local `origin/main` ref.**
Fresh in the pipeline, where step 0 fetches; not in a stale checkout. One sentence in the doc
comment would price it.

**Should 7 (carried) — `src/test/migration-gate.test.ts:133-138` binds AC5 to a banner that
names a path.** A reworded inline skip would pass it.

FINDINGS
