# T0.26 — review

Commit reviewed: `974dbb8` (review pass 2, fresh context).

Read: `docs/tickets/T0.26.md`, its Cited transcription of `docs/guidelines.md` §4 and §5 against
`origin/main`'s copy (word for word, as the report claims), the whole of
`git diff origin/main...HEAD`, and `docs/reports/T0.26.md`.

Tests run — the nine files `node scripts/run/review-scope.mjs docs/tickets/T0.26.md` lists,
nothing `missing`:

    pnpm vitest run scripts/hooks/guard.test.mjs scripts/run/comments.test.mjs \
      scripts/run/gated.test.mjs scripts/run/loosening.test.mjs scripts/run/next-id.test.mjs \
      scripts/run/permission.test.mjs src/db/opportunity-key.db.test.ts \
      src/db/refinement.db.test.ts src/test/migration-gate.test.ts

    Test Files  9 passed (9)
         Tests  332 passed (332)

The two `.db.test.ts` files were run again on their own: 15 passed, nothing skipped, so AC4's
"does not skip once the migration is on origin/main" is true in the product and not only in the
helper's unit tests.

What holds. `coveredBy`/`migrationTag` take a migration off the gated list on its own tag and
never on another's, and the bookkeeping beside it only when every migration in the diff is
covered (AC1, AC2, AC3). `appliedMigrations` reads the spending off the thread — the human's
reply beginning with `apply`, then the run's own `applied` note naming the tag — and never off
the `migration` question, which is where the file name would otherwise be free for the taking.
Every reader fails closed: `consumedApplies` has five returns and four of them hand back no tags,
`verify` carries `applied: []` on each early exit, and `reviewed` defaults to `[]`, so a caller
that read no thread gates exactly as before. The guard's door reads the tags off the fetch
`verify` already made rather than asking the board twice. The timing holds too: SKILL.md's
`shape: apply` branch posts the `applied` comment at step 0, before the branch is checked out and
long before step 9 asks `consumedApplies`, so AC1's self-merge has its evidence on the thread by
the time it is read. `src/test/migration-gate.ts` splits the rule from the observation and every
unreadable case — ref, git, lookup — answers "landed", so nothing unproven buys a skip (AC4, the
third Rule). Both `.db.test.ts` files call the helper and neither composes its own banner (AC5).
The new top-level `await main()` in `gated.mjs` does not re-enter `loosening.mjs`'s probe
deadlock: the condition is false under `await import`, and the 60-second probe tests exercise it.

## Findings

**1. Must — the rule changed in five places' worth of prose and moved in one.** Build 3 patched
`docs/guidelines.md` §4 and §5, and the same rule is stated in four other places that still carry
the superseded version. Two of them are instructions a run and a reviewer act on, and one is the
header of the very file this ticket rewrote:

- `scripts/run/gated.mjs:12-13`, the module's own contract paragraph, still reads "a diff adding
  a migration waits for `apply`, and its ticket waits for `merge`: the schema is shared, and
  applying one is the human's call either way (§4)" — directly above `coveredBy`, which is the
  code that stopped it being true.
- `.claude/skills/ticket/SKILL.md:477`: "the reviewer's `PASS` on file over a diff that adds no
  migration and weakens no restraint". This is under **Never merge on your own word**, and it is
  what the run reads at the moment the new gate fires; `:436`'s gloss "it adds a migration, or it
  weakens" is the same sentence one screen up.
- `.claude/agents/reviewer.md:63`: "the guard reads that file at close and lets a ticket whose
  diff adds no migration and weakens no restraint merge itself on `PASS`" — the next reviewer's
  own definition of the door it is writing a verdict for.
- `docs/build-guide.md:110-113`: "a pull request that adds one, or that **weakens a restraint**
  … merges on the word `merge` at Review", and the v2.7 header line that summarises it.

Smallest correction: the qualifier guidelines already settled on — *adds no migration your
`apply` is still owed on* — in each, and a `v2.8 · in the repo` line on `build-guide.md` since
`version-drift.mjs` reads that header. I am confident about the first three; `build-guide.md` is
the one I would accept deferred, but only as a recorded open question with a phase owner
(AGENTS.md), not silently.

**2. Must — three `consumedApplies` cases carry no red-first record, and the report says they
do.** `docs/reports/T0.26.md:81` reads "Fixed with a describe over `consumedApplies`, one case
per return, each observed red by a mutation that stops it failing closed." The table records two
rows for that describe: "the three fail-closed returns", whose mutation is "the no-marker and
unreadable-thread returns handing back tags instead of none" (3 cases), and "answers with the
tags …" (1 case). That is four of the seven cases. These three have neither a mutation nor a
count anywhere in the table:

- `scripts/run/gated.test.mjs:358` "answers with nothing when the token is not there to read the
  board with" — the `readToken` return is not among the two the mutation names;
- `scripts/run/gated.test.mjs:365` "answers with nothing when the board file cannot be read" —
  same, the `readBoard` catch;
- `scripts/run/gated.test.mjs:391` "answers with nothing when the thread carries no spent apply".

Guidelines §5 step 8 refuses a report without the record per test, and pass 1's Must was that
this function's returns were the run's own half of AC1 with nothing exercising them — so the
gap is on the half the previous finding was about. Smallest correction: run the two missing
mutations (the `token === null` and board-catch returns handing back tags; `appliedMigrations`
returning a tag whatever the thread says) and add the rows with their counts — or, following
T0.29's precedent in `docs/reports/T0.29.md`, a row that says "not reddened" and why, rather
than prose that says every case was.

## Shoulds

1. `scripts/run/gated.mjs:58-60` — `JOURNAL` is built from `MIGRATIONS_DIR` and the snapshot half
   of `isBookkeeping` hardcodes `drizzle/` in its regexp. If that constant ever moves, the
   journal follows it and the snapshots silently stop matching, which gates a correctly applied
   migration's diff for a reason nobody would look for. Build the regexp from `MIGRATIONS_DIR`
   too.
2. `src/test/migration-gate.test.ts:128` — the AC5 assertion rejects only `process.stderr.write`
   calls whose text contains `drizzle/00`. A re-introduced inline skip path worded without a
   migration path ("the table does not exist yet") passes it, which is the drift the test exists
   to stop. Asserting on the absence of any second skip source — that `gate.skip` is the only
   thing `describe.skipIf` reads — would bind the rule rather than the wording.
3. `scripts/run/loosening.test.mjs:214-260` — the five `diffGating`/`diffsLoosened` cases name no
   criterion, and no Criteria line in the ticket covers the detector's new rule. Pass 1's Should 4
   was answered by relabelling the describe rather than by giving the rule a criterion, so the
   ticket's Criteria ↔ Tests map still has a group nobody can retire against it. A sixth AC, or a
   line in Build 1 that the narrowing must itself be measured, would close it.

FINDINGS
