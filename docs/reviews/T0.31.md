# T0.31 — review

Commit reviewed: `7ec8dd6` (pass 3)

Read cold against `docs/tickets/T0.31.md`. The ticket cites no spec section; its *Rules* name
`CLAUDE.md`, `AGENTS.md` and `docs/guidelines.md` as a whole and its *Cited* section says the
Spec property is empty, so the reading is those three — `docs/guidelines.md` §4 (the gated
sentence, the three words, the guard's second door) and §5 steps 4, 5, 8, 9 in particular, since
the diff moves the guard the close rests on.

What I checked myself rather than taking from the branch:

- Every hunk of `git diff origin/main...HEAD`: four files — the ticket, pass 2's verdict,
  `scripts/hooks/guard.mjs` (three hunks: `ONE_DASH_LONG` and `isCommandFlag`; the `parse()`
  loop; the `shellRun()` option split) and `scripts/hooks/guard.test.mjs` (one describe, ten
  tests). Nothing outside the ticket, and `git status --porcelain` is clean, so the diff is the
  whole change.
- `node scripts/run/review-scope.mjs docs/tickets/T0.31.md` lists `scripts/hooks/guard.test.mjs`,
  nothing `missing`. `pnpm vitest run scripts/hooks/guard.test.mjs`: **1 file, 125 tests, all
  passing**.
- AC1 measured rather than read: `origin/main`'s `decide()` and this commit's, side by side over
  6,382 shell shapes (four shells × 30 option spellings × ten payloads, bare and behind
  `env X=1` and `sudo`). 588 transitions, every one allowed → refused. The bundles the ticket
  names — `-lc`, `-ec`, `-xc`, `-cl`, `-cx`, `-lec` — now close rules (a), (d) and (i) past the
  wrappers too, and `bash --version`, `bash --color`, `bash -l`, `sh -e`, `pnpm test` stay
  allowed. Pass 2's Must 1 is settled: `bash -norc -c 'pnpm db:push'`, `-rcfile`, `-restricted`,
  `bash -norc -lc '…'` and `bash -norc post.sh` all refuse here.
- Red-first derived independently, each test's assertions run against `origin/main`'s guard and
  against `574d3c4` and `3ffeb87` — the table is in Should 3, for step 8.
- `node scripts/run/loosening.mjs origin/main...HEAD` → `{"ok": true, "reasons": []}` and
  `gated.mjs` → `"gated": []`, so a PASS here self-merges. That is why Shoulds 1 and 2 below name
  the two refused → allowed shapes the corpus does not see: neither is a new capability — both
  are doors `bash -c post.sh` already holds open on main — but the report owes them.
- `bash -protected -c 'echo A'`, `bash -norc -c …`, `bash -restricted -c …` run here, so the
  one-dash spelling is real and so is the option Should 1 names.

No Must stands. The Shoulds:

1. **`ONE_DASH_LONG` is missing `-protected`, and the comment above it says the set is
   complete.** `scripts/hooks/guard.mjs:411-413`:

   > The rest of bash's list carries a hyphen or no `c` and cannot reach the matcher below.

   `-protected` is letters-only, holds a `c`, and bash runs it — I ran
   `bash -protected -c 'echo A'`. So the sentence is not true, and the word it leaves out reaches
   `isCommandFlag` at `scripts/hooks/guard.mjs:424`. `parse()` is immune, because the loop at
   `guard.mjs:481` follows every match rather than the first —
   `bash -protected -c 'pnpm db:push'` and `bash -protected -lc 'pnpm db:push'` are both refused
   here, the second a tightening. The one consequence is the second call site,
   `guard.mjs:882` `options.some(isCommandFlag)`: measured against `origin/main` with the same
   inputs, `bash -protected post.sh` with `post.sh` writing the board is **refused on main and
   allowed here**. A Should rather than a Must because it opens nothing
   `bash -c post.sh` — allowed on main — does not already open, and because the rules AC1 names
   close on every `-protected` spelling. Smallest correction: add `"-protected"` to the set at
   `guard.mjs:415` and reword the sentence to say the set is bash's documented list plus
   `-protected`, which its man page does not carry.

2. **A bundle whose string is a bare script name loses the read main happened to have.**
   Pass 1's and pass 2's Should 1, still standing, and now measured for every shell in `SHELLS`:
   `bash -lc post.sh`, `sh -ec run.sh`, `zsh -lc deploy.sh` are refused on main and allowed here.
   Main took the `-c` string for a path and opened it; this commit hands it to `parse()`, and
   `pathRun` (`guard.mjs:892`) reads a word only when it holds a `/`. Main's read was a
   misreading — it also opened `/repo/echo hi` for `bash -lc 'echo hi'`, which is what the test at
   `scripts/hooks/guard.test.mjs:1526` deliberately ends — and `bash -c post.sh` is allowed on
   main too, so this is an exchange rather than a new hole. It is still a refused → allowed that
   `loosening.mjs` cannot see: it belongs in `docs/reports/T0.31.md` or in one Backlog Fix (Type
   Fix, Epic inherited) reading *a `-c` string that is only a script's name is never read*,
   with Should 1 folded into it.

3. **The red-first record the report owes, with the three rows that are not simply "red on
   main".** `docs/reports/T0.31.md` does not exist and the ticket's *Report* section
   (`docs/tickets/T0.31.md:39`) is empty; step 8 runs after step 5, so nothing is broken yet. I
   ran each of the ten new tests' assertions against `origin/main`'s guard and against the two
   earlier commits on this branch:

   | test (`guard.test.mjs`) | main | 574d3c4 | 3ffeb87 |
   |---|---|---|---|
   | parses the bundle's next word (1477) | RED | green | green |
   | rule (a) (1490) | RED | green | green |
   | rule (d) (1501) | RED | green | green |
   | rule (i) (1507) | RED | green | green |
   | reads the cluster wherever c sits in it (1516) | RED | green | green |
   | leaves a shell that runs no command string alone (1523) | green | green | green |
   | does not also read the command string as a script path (1533) | RED | green | green |
   | does not take a single-dash long option for the cluster (1541) | RED | RED | RED |
   | reads past an option it does not know (1554) | green | RED | RED |
   | still reads a script whose own argument looks like the cluster (1563) | green | RED | green |

   Seven rows are "red on main". Three are not, and each needs a row that says what made it red
   the way `docs/reports/T0.28.md` records its "—" rows: "leaves a shell that runs no command
   string alone" is green everywhere on this branch — a boundary test, whose reddening mutation
   is a relaxed matcher (`/^-.*c.*$/` lets `bash --color` through, measured); "reads past an
   option it does not know" reddens at `574d3c4`, not on main; "still reads a script whose own
   argument looks like the cluster" reddens at `574d3c4` only and is green again at `3ffeb87`,
   which is pass 1's Must 1 and its fix.

4. **Two spellings of the same family, both outside this ticket's sentence.** `bash -c --
   'pnpm db:push'` runs the command and is allowed on main and here alike, because the word after
   `-c` is `--`. And `INLINE_CODE` (`scripts/hooks/guard.mjs:738-740`) matches `-e`, `-p`, `-c`
   as whole words while both interpreters cluster: measured with no script reader,
   `node -e '…fetch api.notion.com…POST…'` and `python3 -c '…'` are refused while `node -pe '…'`
   and `python3 -ic '…'` are allowed, on main and here both. Neither is a regression and neither
   is a shell, so neither is this ticket's; both belong in one Backlog Fix (Type Fix, Epic
   inherited) — worth filing while the matcher is open. Pass 1's Should 2 and pass 2's Should 4,
   unchanged by this diff.

PASS
