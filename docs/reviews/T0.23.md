# T0.23 — review

Reviewed at `8a0082e` (branch `t0-23`, `git diff main...HEAD`, 16 files, every hunk read). Pass 2.

Read: `docs/tickets/T0.23.md` with its Cited text (guidelines v1.14 §3 and §5, T0.22's open
questions 1 and 2); the whole diff to `scripts/run/{mirror,notion,report-check,review-model,rows}.mjs`
and their tests, `.claude/skills/ticket/SKILL.md` (step 0 a–f, step 5, step 8), `docs/guidelines.md`
(header, §2 Documents, §3, §5 steps 0, 5 and 8, the token paragraph), `scripts/run/README.md`,
`docs/reports/T0.23.md` and pass 1's verdict. Also read, outside the diff, what the diff feeds:
`notion.mjs`'s client (`tasks`, `children`, pagination, timeout), `stale.mjs`'s `idOf` and
`merge-detect.mjs`'s row shape, which `rows.mjs` rows satisfy (`Name`, `Commit`, `url`, `id`).

Criteria and Tests match both ways. TC1 → AC1 (`review-model.test.mjs` "a pass stopped at its
turn limit" and the two step 5 document tests, `report-check.test.mjs` "a pass resumed at its
turn limit" and the step 8 test), TC2 → AC2 (`review-model.test.mjs` "guidelines §3"), TC3 → AC3
(`mirror.test.mjs` "verify", "the read-back, in the skill and guidelines §2"), TC4 → AC4
(`mirror.test.mjs` "readPlan" › "skips every page…"), TC5 → AC5 (`rows.test.mjs`,
`notion.test.mjs` "reads a Tasks row's Commit…"). Every new test sits under a `T0.23 TCn → ACn`
tag, and every one has a row in the red-first table with a mutation and a count; the four tests
pass 1's Shoulds added have rows of their own (M31–M34), and the two `Commit: ""` expectations
now have theirs.

Tests run: `node scripts/run/review-scope.mjs docs/tickets/T0.23.md` listed five files, nothing
missing. `pnpm vitest run` on those five plus `scripts/run/readme.test.mjs` (the report cites it
for `rows.mjs`): 6 files, 95 passed — mirror 33, review-model 23, report-check 15, notion 15,
rows 6, readme 3, the counts the report's last rows give.

Pass 1's Shoulds, checked against the code: 1 settled (`mirror.mjs:327`
`Math.abs(short) <= SLACK`, test "finds a page that reads long"); 2 settled (`SKILL.md:211–212`, a
read-back that fails is `next: leave`, and the step 0 test holds the sentence); 3 recorded as the
report's open question 3; 4 settled (`review-model.mjs:70` anchored, both fixture shapes still
match); 5 settled (`rewritten === 0` and `resumed !== 0`, so `NaN`, `"once"` and `null` leave or
stop); 6 and 7 settled in the report.

Checked beyond the tests:
- The turn-limit note's shape, in real transcripts: a foreground result begins with
  "NOTE: this agent stopped at its 30-turn limit before finishing." at index 0, as both fixtures
  hold, so the anchor matches it.
- The measurement: `written` over `docs/guidelines.md` at `main` is 33 409 letters and digits,
  whole and as `chunk`'s three chunks joined, the number the report gives for the Guidelines page
  read back. That number also confirms `held` dropping the first block: the header callout is one
  block with no children.
- The chunk endings `chunk` makes today for all eight mirrored files end in prose, a heading, a
  closed HTML comment or `@AGENTS.md`, which `written` and a page read the same way.
- `report-check.mjs` is read only by the skill's step 8 (grep over `scripts/` and `.claude/`), so
  the new `resumed` and `verdict` checks reach no older report.
- No other test asserts `task()`'s exact shape, so the added `Commit` field changes nothing else.

## Shoulds

1. **Should** — `.claude/skills/ticket/SKILL.md:331` "`stop: false` → the call was refused for
   credits or availability: invoke the reviewer again with `model` set to…". The script now also
   answers `stop: false` for a turn limit (with `resume: true`). A run reading this paragraph
   alone would open a fresh session where the next paragraph says to continue the same one. The
   outcome is a full review either way. Smallest correction: "`stop: false` and `resume: false` →".
2. **Should** (unsure how often it happens) — `scripts/run/review-model.mjs:70` reads the note only
   at the start of the text. This project's transcripts also hold it inside a background agent's
   notification: stoic-williams, "Review T1.4 pass 2", `<task-notification>…<result>NOTE: this
   agent stopped at its 30-turn limit…`. A run that passes that whole notification as `error` gets
   `other` and a stop where the ticket wants a resume. That errs on the safe side: it stops, and
   never takes a partial output for a verdict. The skill does say "the note verbatim". Smallest
   correction: the skill names the `<result>` text as the note, or the pattern also accepts
   `<result>` in front of `NOTE:`, with a fixture of that shape.
3. **Should** — `scripts/run/mirror.mjs:25–26` "A page that reads short is rewritten once from its
   sentinel, and left under the sentinel if it reads short again." This file-head comment predates
   pass 1's Should 1. `verify` now rewrites a page that reads long too, as its own comment at
   `:314`, the README, §2 and the skill already say. Smallest correction: "reads short or long …
   reads wrong again".
4. **Should** — `.claude/skills/ticket/SKILL.md:58` "Steps a, c and e ask the board through the
   connector's query". Step 1 still asks the connector with no fallback when the quota is spent:
   `next-id.mjs` "over the Epic's task names" (`:248`) and "the Epics list". `node
   scripts/run/rows.mjs` with no flag already returns every Tasks row with its `Epic`. AC5 and
   Build item 4 speak of the preflight, so this is not a Must. One sentence in step 1, or an open
   question beside open question 1, would settle it.
5. **Should** — `docs/reports/T0.23.md` "## Reviewer passes and findings" still reads "(written at
   review)". Step 8's `report-check.mjs` will refuse it until each pass has a row. This pass, pass
   2, stopped at its turn limit and was continued, so its row says `yes` under `resumed`.

PASS
