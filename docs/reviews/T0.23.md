# T0.23 — review

Reviewed at `6d052b7` (branch `t0-23`, `git diff main...HEAD`, 15 files, every hunk read).

Read: `docs/tickets/T0.23.md` with its Cited text (guidelines v1.14 §3 and §5, T0.22's open
questions 1 and 2); the diff to `scripts/run/{mirror,notion,report-check,review-model,rows}.mjs`
and their tests, `.claude/skills/ticket/SKILL.md`, `docs/guidelines.md` (§2 Documents, §3, §5
steps 0, 5 and 8, the token paragraph, the v1.15 header), `scripts/run/README.md` and
`docs/reports/T0.23.md`.

Criteria and Tests match both ways. TC1 → AC1 (`review-model.test.mjs` "a pass stopped at its
turn limit" and the two §5/skill tests, `report-check.test.mjs` "a pass resumed at its turn
limit"), TC2 → AC2 (`review-model.test.mjs` "guidelines §3"), TC3 → AC3 (`mirror.test.mjs`
"verify" and "the read-back, in the skill and guidelines §2"), TC4 → AC4 (`mirror.test.mjs`
"readPlan" › "skips every page…"), TC5 → AC5 (`rows.test.mjs`, `notion.test.mjs` "reads a Tasks
row's Commit…"). Every new test carries its `T0.23 TCn → ACn` tag, and every one of them has a
row in the red-first table, with a mutation and a count in each cell. TC4 and the §2 document
test were green when written; the table says so and names the mutations (M23, M24, M30) that
made them red. The per-file counts in the table (21, 15, 31, 6, 15) match the files.

Tests run: `node scripts/run/review-scope.mjs docs/tickets/T0.23.md` listed five files and
nothing missing. `pnpm vitest run` on those five plus `scripts/run/readme.test.mjs` (the
report cites it for `rows.mjs`): 6 files, 91 passed.

What was checked against the ticket:
- AC1: `causeOf` reads the turn-limit note before anything else. `nextReviewer` checks the
  chain order first, so an empty `tried` still stops. It resumes on `tried.at(-1)` once and
  stops at `resumed >= 1`. The skill's step 5 routes the note to the script and resumes with
  one line. `report-check` requires `resumed` to say yes or no and the verdict to be PASS or
  FINDINGS, and the skill's step 8 names the new columns. A pass that stops twice goes to the
  same Decision that §3 now names.
- AC3: the rewrite starts from the sentinel, not from the chunk alone. That differs from Build
  item 3's wording, and the report records it as default 1 with its reason. It stays on a
  developer surface (§4). I read the endings of the chunks `chunk` makes today for design-spec,
  build-log, schema, product-spec, guidelines and build-guide. They end in prose, headings or a
  closed fence, so no intermediate `ended` check is exposed to a construct `written` reads
  differently.
- AC4: `decide` has compared against the file's last commit on `origin/main` since T0.12, and
  TC4 holds that for all eight pages. Default 4 records that no code was needed.
- AC5: `rows.mjs` covers the three connector queries the skill makes (steps a, c, e; no other
  "Query" is in the skill). Releases' `Name` (title) and `Commit` (text), per guidelines §2, are
  what `task()` reads. `idOf` reads the whole ID, so `T0.2` never matches `T0.23`.

## Shoulds

1. **Should** — `scripts/run/mirror.mjs:323` `const ok = short <= SLACK && ended;` accepts a
   page that reads long by any amount. A chunk that landed twice would still get `continue` and
   be headed as whole. Build item 3 says "on a mismatch"; AC3 and the Rule speak only of short,
   which is why this is not a Must. I am not certain of that reading. Smallest correction:
   `Math.abs(short) <= SLACK` (schema's measured −2 fits inside it), plus a test with a chunk
   sent once and read back twice.
2. **Should** — `.claude/skills/ticket/SKILL.md:207–212` names `continue`, `rewrite` and
   `leave`, but not a read-back that throws. `readBlocks` makes one request per block with
   children at `TIMEOUT_MS` 4000. A second 429 or one slow page exits `mirror.mjs --verify`
   (`scripts/run/mirror.mjs:378`, no catch) with a stack trace and no `next`. Smallest
   correction: one sentence in step 0f saying a read-back that errors is `leave` (the sentinel
   stays and the report line says so), or catch in `main()` and emit that.
3. **Should** — a page that reads short for a reason other than a cut write follows the path
   at `SKILL.md:210` (`next: leave` … "the next preflight refreshes it first"). That covers a
   difference over 64 letters or at a chunk's last 48, for example a future link with a title,
   `[a](url "t")`, which `written` does not strip. Such a page is rewritten twice and left under
   its sentinel on every preflight from then on, and only the report line records it. That
   costs the most quota on the pages the ticket wanted cheaper. Nothing in today's documents
   triggers it; worth an open question beside open question 2.
4. **Should** — `scripts/run/review-model.mjs:69` `TURN_LIMIT` is unanchored and read first,
   so any text that quotes the note counts as `turn-limit`, including a finished review that
   quotes it. No outcome changes today, because the skill sends only failures to the script.
   `/^NOTE: this agent stopped at its \d+-turn limit\b/im` matches both fixture shapes.
5. **Should** — `scripts/run/review-model.mjs:160` `Number(input.resumed ?? 0)` and
   `scripts/run/mirror.mjs:374` `Number(args[++i])` read a non-number as `NaN`, and
   `NaN >= 1` is false. So `"resumed":"once"` resumes a second time, and `--rewritten x`
   rewrites again: a mistyped count means one more resume or rewrite instead of a stop.
   Smallest correction: treat a count that is not a finite number as a stop / `leave`.
6. **Should** — `docs/reports/T0.23.md:69`: the notion row covers only the new test. The two
   existing `toEqual` expectations that gained `Commit: ""` (`scripts/run/notion.test.mjs:93`,
   `:284`) have no row. The equivalent `resume: false` edits in `review-model.test.mjs` have one
   (`docs/reports/T0.23.md:42`).
7. **Should** — `docs/reports/T0.23.md:104` "a stricter skip than main's own head": comparing
   against the file's last commit skips more pages than comparing against main's head, not
   fewer. "A narrower comparison" or "a skip that holds while main moves" says what is meant.

PASS
