# T0.33 — review

Commit reviewed: `c475c21` (pass 1)

Read cold against `docs/tickets/T0.33.md`. Its `Spec` line cites product-spec §9 — read at v1.8 in
the ticket's Cited block and against `docs/product-spec.md`'s own §9, the seam being "nested
AGENTS.md per area … loaded only when the agent works there" — plus `CLAUDE.md`'s References, the
build log's Decisions section (92,646 characters, 95 top-level bullets), and `docs/guidelines.md`
§5 steps 4, 5 and 8.

What I measured rather than took from the branch:

- Every hunk of `git diff origin/main...HEAD`: twelve files, all accounted for by the ticket —
  seven `.claude/rules/*.md`, `CLAUDE.md` (+1 line, one hunk, no deletion), `docs/build-log.md`
  (+1 line, one hunk), the pack, `scripts/rules.mjs`, `scripts/rules.test.mjs`.
  `git status --porcelain` is clean.
- `node scripts/run/review-scope.mjs docs/tickets/T0.33.md` → `scripts/rules.test.mjs`, nothing
  `missing`. `pnpm vitest run scripts/rules.test.mjs` — **1 file, 35 tests, all passing**, 233 ms.
- AC1 measured, not read: every `paths:` glob run against `git ls-files` independently of the
  module — `src/lib/ai/**` 13, `src/db/**` 34, `drizzle/**` 20, `src/packs/**` 8,
  `src/lib/scoring/**` 14, `src/app/**` 64, `src/components/**` 43, `src/i18n/**` 3,
  `src/**/*.test.{ts,tsx}` 81, `scripts/**/*.test.{mjs,mts,ts}` 38, `e2e/**/*.spec.ts` 7.
  All seven files are tracked and none is `.gitignore`d.
- AC2 measured with my own parser, not `check`: all 47 rule lines end in a source; the two
  `docs/log/` paths exist; all 45 quoted phrases are present verbatim (whitespace folded) **inside
  the Decisions section**, not merely somewhere in the log.
- AC3 read line by line against the log paragraph each rule was distilled from: the enum rule, the
  CHECK-passes-NULL rule, `::text::jsonb`, the `Date` on the drizzle-wrapped raw client, NFC-never-
  NFKC, the computed protocol version, the fixture rule and the row-ordering rule are each in the
  file for the code they govern, and each line states what its paragraph states.
- AC4 measured against the real committed files, not only the temp-dir fixtures: `check` run with
  an in-memory mutation of `migrations.md` reports exactly one problem per defect — frontmatter
  stripped, `drizzle/**` → `drizzle/nowhere/**`, `docs/log/T1.4.md` → `T9.9.md`, the CHECK quote's
  FALSE → TRUE — and `[]` unmutated.
- The mechanism, checked rather than assumed: the Claude Code binary carries the `InstructionsLoaded`
  hook with `load_reason` `path_glob_match` and "globs (optional — the `paths:` frontmatter patterns
  that matched)", so the premise holds. `toNodeGlob` is necessary, not decoration: Node's
  `globSync` answers 0 for `src/app/i/\[key\]/*.tsx` and 15 for the translated `[[]key[]]` form.
- No rule names a symbol that has moved: `PROTOCOL_RELEASE`, `PROTOCOL_VERSION`, `RUBRIC_TOTAL`,
  `packConditions`, `applicableChecks`, `percentageOf`, `runScorer`, `validatePack`,
  `closeSharedDbClient`, `bootstrap_workspace`, `src/components/ui/icons.tsx` as the sole
  `iconoir-react` importer, `e2e/production.spec.ts`, `/dev/list` beside `/dev/primitives` — all
  resolve in the checkout.
- `vitest.config.mts`'s node project includes `scripts/**/*.{test,spec}.{ts,mts,mjs}`, so the new
  test is in the Stop gate's suite; `.prettierignore` carries `*.md`, so the rule files are not
  Prettier's to reformat.
- No `@`-import in any rule file or in `CLAUDE.md`'s new line; nothing removed from `CLAUDE.md`,
  nothing moved out of the build log.

Criteria ↔ Tests maps in both directions for the six named cases: AC1/AC2 → TC1/TC2 (`over()` over
the real dir), AC3 → TC3 (four `it`s, eight assertions, one per named rule), AC4 → TC4 (the four
defects plus two more and a sound baseline), AC5 → TC5, AC6 → TC6. Each labelled describe names its
TC in a comment.

One thing I could not check: `docs/reports/T0.33.md` does not exist yet, so there is no red-first
table to read. Under `docs/guidelines.md` §5 the report is step 8 and this review is step 5, so its
absence here is the order of the run and not a defect — step 8's refusal owns it. What I could do
instead I did: I reddened all four AC4 cases myself against the committed files, above.

Shoulds — none of these blocks the close:

1. `.claude/rules/migrations.md:1-4` scopes the migration rules to `drizzle/**`, but two of them are
   rules about DDL that is authored somewhere else. The CHECK-passes-NULL rule is about the
   expressions at `src/db/schema/tables.ts:335-336`, `:397-398` and the comment at `:456` ("The
   `is not null` guards are load-bearing"), and the composite-foreign-key rule is about the
   `foreignKey({...})` calls from `:149` on. A session editing `src/db/schema/tables.ts` matches
   `src/db/**` and so loads `database.md` alone — the two rules it most needs arrive nowhere.
   Smallest correction: add `"src/db/schema/**"` to `migrations.md`'s `paths:` list.
2. `scripts/rules.test.mjs:63` proves AC3's `Date`-on-the-raw-client rule with
   `expect(text).toContain("drizzle()")`. Any line in `database.md` mentioning drizzle satisfies
   that, including lines that say nothing about the rule; deleting the rule and leaving any other
   drizzle mention keeps it green. Pinning a clause of the rule itself — `"reaches the wire encoder
   unconverted"` — would make the assertion name what it claims to catch, which is `tests.md`'s own
   last line applied here.
3. `scripts/rules.test.mjs:139-238`: the four helper describes (`splitFrontmatter and readPaths`,
   `readSource`, `matchTracked`, `flatten`) carry no TC comment, while the six beside them do. They
   back TC1 and TC2; saying so in a comment is what lets a later reader retire them with their
   criterion.
4. `scripts/rules.mjs:52-56` reads the YAML flow-list form — `paths: ["src/**", "drizzle/**"]` —
   as the two globs `["src/**` and `drizzle/**"]`, and reports "matches no tracked file". It errs
   red, which is the safe direction, but it names the wrong cause for the next rule-writer who uses
   a form Claude Code's YAML accepts. Stripping a leading `[` and a trailing `]` from `inline`
   before the split would settle it.
5. `docs/build-log.md:93` opens "The standing rules below are distilled one per line into
   `.claude/rules/`". 47 lines are indexed against 95 top-level bullets in that section. The ticket
   prescribed this wording, so the diff did what it was told; but as it stands the sentence claims a
   completeness the index does not have, and the next writer reads it as "already done". "The
   standing rules below are distilled, one per line, into `.claude/rules/` as they prove to hold
   everywhere" would claim only what is true.

PASS
