# T0.33 — review

Commit reviewed: `e22685b` (pass 2)

Read cold against `docs/tickets/T0.33.md`, its `Spec` line's product-spec §9 (the ticket's Cited
block and `docs/product-spec.md`'s own §9 — "nested AGENTS.md per area … loaded only when the
agent works there"), `CLAUDE.md`'s References, the build log's Decisions section, and
`docs/guidelines.md` §5 steps 5 and 8.

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: thirteen files — seven `.claude/rules/*.md`,
  `CLAUDE.md` (+1 line, one hunk, no deletion), `docs/build-log.md` (+1 line, one hunk), the
  ticket, pass 1's review file, `scripts/rules.mjs`, `scripts/rules.test.mjs`. Nothing in the
  diff falls outside the ticket. `git status --porcelain` is clean.
- `node scripts/run/review-scope.mjs docs/tickets/T0.33.md` → `scripts/rules.test.mjs`, nothing
  `missing`. `pnpm vitest run scripts/rules.test.mjs` — 1 file, 36 tests, all passing, 176 ms.
- AC1 with my own parser rather than the module's: every file opens with a `paths:` list, and
  every glob reaches tracked files — `src/lib/ai/**` 13, `src/db/**` 34, `drizzle/**` 20,
  `src/db/schema/**` 3, `src/packs/**` 8, `src/lib/scoring/**` 14, `src/app/**` 64,
  `src/components/**` 43, `src/i18n/**` 3, `src/**/*.test.{ts,tsx}` 81,
  `scripts/**/*.test.{mjs,mts,ts}` 38, `e2e/**/*.spec.ts` 7.
- AC1 again through the mechanism itself, which is the only reading that matters. The Claude Code
  binary loads a rules file by `lzt(frontmatter.paths)` → strip a trailing `/**` → `ignore()
  .add(patterns).ignores(relPath)` — gitignore semantics, not Node's glob. Replaying that pipeline
  against `git ls-files` with the bundled `ignore` package: all twelve globs match, `tests.md`'s
  brace groups included (`lzt` expands `{ts,tsx}` itself before matching, so the test's
  "expands braces, as Claude Code does" is true of the mechanism and not only of Node). The
  `InstructionsLoaded` hook carries `load_reason: path_glob_match` and "globs (optional — the
  `paths:` frontmatter patterns that matched)", so the premise the ticket is built on holds.
- AC2 with my own parser: all 47 rule lines end in a source; both `docs/log/` paths exist; all 45
  quoted phrases are present verbatim in `docs/build-log.md` after folding the hard wrap.
- AC3 read against the log paragraph each line was distilled from: the enum rule
  (`migrations.md:10`), CHECK-passes-NULL (`:11`), `::text::jsonb` (`database.md:10`), the `Date`
  on the drizzle-wrapped raw client (`:11`), NFC-never-NFKC (`scoring.md:10`), the computed
  protocol version (`:11`), the fixture rule (`tests.md:11`) and the row-ordering rule (`:10`) are
  each in the file for the code they govern, and each states what its paragraph states. I checked
  the two shortest quotes against their paragraphs — `docs/build-log.md:169` and `:279` — and both
  say what the rule line says they say.
- AC4: the four defects are planted per test and each asserts an exact one-element problem list,
  so each case is reddened by its own mutation rather than by a shared fixture.
- AC5: one added line under References, no deletion anywhere in `CLAUDE.md`; the file is 77 lines,
  inside its own ~150-line ceiling. AC6: the Decisions section's first non-blank line names
  `.claude/rules/`.
- No `@`-import in any rule file (asserted per file) and none in `CLAUDE.md`'s new line. Nothing
  moved out of the build log. `vitest.config.mts:21` includes `scripts/**/*.{test,spec}.{ts,mts,mjs}`,
  so the new test is in the Stop gate's suite.

Criteria ↔ Tests both ways: AC1 → TC1 (the committed files, `matchTracked`, `splitFrontmatter`/
`readPaths`), AC2 → TC2 (`readSource`, `flatten`), AC3 → TC3 (four `it`s, eight assertions, one per
named rule), AC4 → TC4 (the four defects plus a sound baseline and two further refusals), AC5 → TC5,
AC6 → TC6. Every describe names its TC in a comment; no test is left without a criterion and no
criterion without a test.

`docs/reports/T0.33.md` does not exist yet, so there is no red-first table to read. Under
`docs/guidelines.md` §5 the report is step 8 and this review is step 5, so its absence here is the
order of the run; step 8's refusal owns it, and the record it is refused without — per test, the
mutation that reddened it and the count that went green — is still owed for all 36.

Shoulds — none of these blocks the close:

1. `scripts/rules.mjs:59` splits an inline `paths:` value on every comma:
   `readPaths('paths: "src/**/*.test.{ts,tsx}"')` answers `["src/**/*.test.{ts", "tsx}"]`, and the
   flow-list form tears the same way. `check` then reports "matches no tracked file" for a pattern
   Claude Code expands and matches — the wrong cause, which is the defect pass 1 raised about the
   brackets, one level down. The binary's own parser splits on commas at brace depth zero
   (`if(l==="{")i++ … else if(l===","&&i===0)`); tracking depth in that split would settle it. The
   committed files all use the block-list form, so nothing is mis-reported today.
2. `scripts/rules.mjs:130-140` accepts a `paths:` list whose every entry is `**`. Claude Code drops
   such a list (`if(o.length===0||o.every((f)=>f==="**"))return{content:r}`) and the file then loads
   at every launch — exactly what AC1 refuses, arriving in a shape the guard reads as sound.
   Treating an all-`**` list as no list in `check` would close it.
3. `.claude/rules/migrations.md:17` and `.claude/rules/packs.md:12` quote the build log's *Open
   questions* section — `docs/build-log.md:637` and `:806` — while Build step 1 asks for "a phrase
   quoted verbatim from the build log's Decisions section". AC2 only asks for the log, and both
   phrases resolve, so the criterion is met; but Open questions is the section that gets rewritten
   when a question closes, so those two sources are the ones most likely to go stale. Either point
   them at the Decisions paragraph carrying the same rule (`:179` for the regenerate rule) or note
   that a source may be any section of the log.

PASS
