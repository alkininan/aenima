# T0.33 — review

Commit reviewed: `9d74ce8` (pass 3)

Read cold against `docs/tickets/T0.33.md`, its `Spec` line's product-spec §9 (the ticket's Cited
block and `docs/product-spec.md` — "nested AGENTS.md per area … loaded only when the agent works
there"), `CLAUDE.md`'s References, the build log's Decisions section, and `docs/guidelines.md`
§5 steps 4, 5 and 8.

What I measured rather than read:

- Every hunk of `git diff origin/main...HEAD`: thirteen files — seven `.claude/rules/*.md`,
  `CLAUDE.md` (1 insertion, 0 deletions, one hunk), `docs/build-log.md` (2 insertions, 0
  deletions, one hunk), the ticket, the earlier passes' review file, `scripts/rules.mjs`,
  `scripts/rules.test.mjs`. Nothing in the diff falls outside the ticket; `git status
  --porcelain` is clean; no hook, guard or gate script is touched.
- `node scripts/run/review-scope.mjs docs/tickets/T0.33.md` → `scripts/rules.test.mjs`, nothing
  `missing`. `pnpm vitest run scripts/rules.test.mjs` — 1 file, 38 tests, all passing, 232 ms.
  `vitest.config.mts:21` includes `scripts/**/*.{test,spec}.{ts,mts,mjs}`, so the guard is in the
  Stop gate's suite.
- The premise, against the binary rather than the report. `/opt/homebrew/lib/node_modules/
  @anthropic-ai/claude-code/bin/claude.exe` builds `Ak(projectRoot, ".claude", "rules")` and walks
  it as a Project rule source; `xls` reads the frontmatter and returns `{content}` alone when
  `paths` is absent or normalizes to nothing; `lzt` splits an inline list at brace depth zero and
  expands `{a,b}`; the loader strips a trailing `/**` and matches with
  `ignore().add(globs).ignores(relPath)` — gitignore semantics. The `InstructionsLoaded` hook
  carries `load_reason: path_glob_match` with "globs (optional — the `paths:` frontmatter patterns
  that matched)". The mechanism the ticket is built on is real and is spelled the way the diff
  spells it.
- AC1 twice. With my own parser: every file opens with a `paths:` list. Then through the loader's
  own pipeline, replayed over `git ls-files` with the bundled `ignore@7.0.6` — every file loads:
  ai 13 files, database 34, migrations 23, packs 8, scoring 14, surfaces 111, tests 126,
  `tests.md`'s brace groups included.
- AC2 with my own parser, not the module's: all 47 rule lines across 7 files end in a source; the
  three `docs/log/` paths exist; all 44 quoted phrases are present verbatim in `docs/build-log.md`
  after folding the hard wrap — and, since pass 2, every one of them inside the Decisions section
  (lines 91–605), which is what Build step 1 asks for. The two `docs/log/` sources moved in this
  pass support their rules: `docs/log/T1.4.md:44` carries the `information_schema` skip and
  `docs/log/T2.1.md:7-10` carries the zero-sum budget enforced at load and the 100 points.
- AC3 read against the log paragraph each line was distilled from: the enum rule
  (`migrations.md:10`), CHECK-passes-NULL (`:11`), `::text::jsonb` (`database.md:10`), the `Date`
  on the drizzle-wrapped raw client (`:11`, against `docs/build-log.md:457-463`), NFC-never-NFKC
  (`scoring.md:10`), the computed protocol version (`:11`), the fixture rule (`tests.md:11`) and
  the row-ordering rule (`:10`) each sit in the file for the code they govern and each says what
  its paragraph says. I also checked every symbol the rule lines name — `closeSharedDbClient`,
  `percentageOf`, `PROTOCOL_VERSION`, `PROTOCOL_RELEASE`, `runScorer`, `packConditions`,
  `applicableChecks`, `validatePack`, `RUBRIC_TOTAL`, `iconoir-react`, `focus-modality` — and all
  still exist where the rule puts them.
- AC4: four defects, each planted in its own temp directory and each asserted as an exact
  one-element problem list, so each case reddens on its own mutation rather than on a shared
  fixture. Three further refusals beside them (no source, no rule lines, a sound baseline).
- AC5: one added line under References, no deletion anywhere in `CLAUDE.md`; the file is 77 lines,
  inside its own ~150-line ceiling; `AGENTS.md` untouched. AC6: the Decisions section's first
  non-blank line names `.claude/rules/`.
- The ticket's Rules: no `@`-import in any rule file (asserted per file) or in `CLAUDE.md`'s new
  line; one physical line per rule (a wrapped line would lose its source and redden `check`);
  no rule repeats one `CLAUDE.md` already states, and the three files whose area overlaps it say
  which ones they are leaving out; nothing removed from `CLAUDE.md`, nothing moved out of the log.
- The bracket escape the ticket prescribes is the loader's:
  `ignore().add("src/app/i/\[key\]/*.tsx").ignores("src/app/i/[key]/CheckList.tsx")` is true and
  the unescaped form is false, so `toNodeGlob` translates rather than misreports.

Criteria ↔ Tests both ways: AC1 → TC1 (the committed files, `splitFrontmatter`/`readPaths`,
`matchTracked`), AC2 → TC2 (`readSource`, `flatten`), AC3 → TC3 (four `it`s, eight assertions, one
per named rule), AC4 → TC4, AC5 → TC5, AC6 → TC6. Every `describe` names its TC in a comment; no
test is left without a criterion and no criterion without a test. TC5's second half — the diff's
hunk count on `CLAUDE.md` — is a measurement, not a test file, and I made it: one hunk, +1/-0.

`docs/reports/T0.33.md` does not exist, so there is no red-first table to read. Under
`docs/guidelines.md` §5 the report is step 8 and this review is step 5, so its absence here is the
order of the run rather than a finding; step 8's own refusal owns it, and what it is refused
without — per test, the mutation that reddened it and the count that went green — is still owed
for all 38.

Shoulds — none of these blocks the close:

1. `scripts/rules.mjs:96-98`. `dropBlanket` catches a list of literal `**` and nothing else, but
   `**` is not the only spelling of "everything": under the loader's matcher
   `ignore().add(["**/*"]).ignores("src/lib/stage.ts")` is true, and so is `["*"]`. A rules file
   scoped `paths: ["**/*"]` therefore loads at every launch — the load AC1 exists to refuse —
   while `check` reads it as sound, because Node's glob does match tracked files for it. The
   binary normalizes first (strip a trailing `/**`, drop empties) and then asks whether what is
   left is all-`**`; asking instead whether every surviving entry matches an arbitrary path would
   close `**/*` and `*` with it. Verified with the bundled `ignore@7.0.6` against `git ls-files`.

2. Forty-five tracked source files reach no rules file at all, and two of them are files the log
   has a standing rule about. `.claude/rules/surfaces.md:13` carries the focus-modality rule —
   "Focus modality is tracked on `<html>` … not gated on `:focus-visible`" — but that file's
   `paths:` are `src/app/**`, `src/components/**`, `src/i18n/**`, so it does not reach
   `src/lib/focus-modality.ts`, the module the rule names. `docs/build-log.md:165` — "Stage lives
   in TypeScript (`src/lib/stage.ts`), never SQL" — reaches `src/lib/stage.ts` from nowhere.
   `src/lib/authoring/**` (nine files) and `scripts/run/**` are uncovered the same way. Build step
   1 asks for one file per area the log's rules are about, and the ticket's own expectation list
   is met, so this is a gap rather than an unmet criterion: adding `src/lib/**` to `surfaces.md`'s
   paths, or one more area file, would close the two named cases.

3. `scripts/rules.mjs:124-130`. The guard resolves a glob with Node's `globSync` while the
   mechanism resolves it with gitignore semantics over the same string. They agree on all twelve
   committed globs — I replayed the loader's pipeline and every file loads — and `toNodeGlob`
   patches the one escape the two spell differently, but the agreement is a fact about today's
   twelve patterns, not a property. `ignore` is already in the lockfile; matching with it would
   make the guard answer the question the mechanism asks, and `toNodeGlob` could go.

4. Four rule lines end in a quote that starts mid-sentence: `.claude/rules/surfaces.md:13` ends in
   `"is not the focus split."` and `packs.md:10` in `"is the only division in the product"`, both
   fragments with the subject dropped. A log edit that keeps the predicate and changes the subject
   leaves the guard green on a rule whose source no longer says it. Quoting from the subject on —
   "`:focus-visible` is not the focus split." — costs nothing and anchors the line to its claim.

PASS
