# T0.42 — review

Commit reviewed: `639fe68` (branch `t0-42`, `git diff origin/main...HEAD` against `2e6c16d`). Reviewer: Fable 5.1, pass 2.

## What I read

- `docs/tickets/T0.42.md` — Objective, Build, Criteria, Tests and the Cited section, checked against the repo's `docs/design-spec.md` v2.23 §7, §8.1–§8.4 and §17 (C-07, C-08, C-18, C-19, C-27, C-31), with §3's type table, §4's Grid and Height lines, §6's focus-visible and reduced-motion lines and §12's +30% reserve, which the cited sections lean on. The Cited quotes match the repo.
- Every hunk of the diff, 15 files: the ticket, pass 1's review, `e2e/form-language.spec.ts`, `e2e/forms.spec.ts` (new), `src/app/dev/primitives/Composites.tsx`, `src/app/globals.css`, `src/app/sign-in/SignInForm.dom.test.tsx`, `src/app/sign-in/SignInForm.tsx`, `src/app/sign-in/page.tsx`, `src/components/ui/Button.tsx`, `Input.tsx`, `OtpInput.tsx`, `variants.test.ts`, `variants.ts`, `src/i18n/en.ts` — and the touched sources whole, plus `useFieldValidation.ts`, `useCooldown.ts`, `motion.ts`, `IconButton.tsx`, `layout.tsx`, the sink's `page.tsx` and the other caller of `inputHelperClasses` (`GapMoves.tsx`, default of 1, unchanged). `git status` clean. Every hunk is accounted for by the ticket's Objective and the sections its Build names; nothing in the diff is outside it.
- The rules on the touched paths: `.claude/rules/surfaces.md`, `.claude/rules/tests.md`.

## Pass 1's Musts, checked on the tip

1. Label box — `globals.css:804-805` now `left: 0; padding-inline: 8px;` with no `right`; the box shrink-wraps its text under `white-space: nowrap`, so the 17/13 scale from its top-left stays inside the composite. `e2e/forms.spec.ts:118-125` asserts `document.documentElement.scrollWidth === 375` at rest and floated on `/sign-in` — the assertion that reddens on `right: 0`. Green here.
2. Gutters — `page.tsx:24` `px-[16px] … lg:px-[24px]`: 16 in hand chrome (<1024), 24 from `lg`. `e2e/forms.spec.ts:195-226` adds the `[375, 812, 48, 343]` row asserting `main > div` is 343 wide. Green here.

Shoulds 4–6 of pass 1 are also settled: the "both submits" test now reaches the code step and checks Continue before Back; the `:has()` comment says what the cascade does; the sink's error demo carries `t.signIn.codeRejected`.

## Criteria ↔ Tests

AC1 ← TC1: `e2e/forms.spec.ts` C-07 ×2, C-08 ×3, §8.3 step ×3; `e2e/form-language.spec.ts` "a field does not shift…" and the two radius rows; `variants.test.ts` "forms to v2.21" ×3. AC2 ← TC2: `SignInForm.dom.test.tsx` "sign-in forms to v2.21" ×6 (C-18's submit half, C-19's shape, the request slot); the timing half of C-18 in the untouched `useFieldValidation.dom.test.ts` and the return-to-label half of C-19 in `useCooldown.dom.test.ts` and `SignInForm.dom.test.tsx:261` "returns to its normal label once the window is out". AC3 ← TC3: `e2e/forms.spec.ts` C-27 ×4 (two routes, two widths), C-31 ×1. Both directions close: no criterion without a test, no test without a criterion. C-07, C-08, C-27 and C-31 run in Chromium as §17 requires.

## Tests, run here

`node scripts/run/review-scope.mjs docs/tickets/T0.42.md` → 4 files, `missing` empty. `pnpm vitest run src/components/ui/variants.test.ts src/app/sign-in/SignInForm.dom.test.tsx` → 2 files, **90 passed**. `PLAYWRIGHT_BASE_URL=http://localhost:3143 pnpm exec playwright test e2e/forms.spec.ts e2e/form-language.spec.ts --project chromium` against a dev server started from this worktree on :3143 → **39 passed**.

The red-first record: `docs/reports/T0.42.md` is not in the checkout — the protocol writes it at step 8, after this pass (guidelines §5) — so until it lands every new test below is unverified as red-first; Should 1 names what its *Tests written* table must carry.

## Findings

1. **Should** — for `docs/reports/T0.42.md` (guidelines §5 step 8): the *Tests written* table needs a row — mutation that made it red, count that went green — for each of: the six of "sign-in forms to v2.21" (`SignInForm.dom.test.tsx:477-585`), the three of "forms to v2.21" (`variants.test.ts:387-410`), the thirteen of `e2e/forms.spec.ts` (C-07 ×2, C-08 ×3, §8.3 ×3, C-27 ×4, C-31 ×1), and the four assertions the diff rewrote (`form-language.spec.ts` two radius rows; `SignInForm.dom.test.tsx` "gives the primary the full width" and "keeps the resend beneath the primary"). Pass 2's additions have their mutations already: the 375 `scrollWidth` assertions (`forms.spec.ts:118-125`) redden on restoring `right: 0` at `globals.css:804`; the 375 step row (`forms.spec.ts:198`) reddens on restoring `px-[24px]` at `page.tsx:24`. Say so in the rows.

2. **Should** — `src/app/sign-in/SignInForm.tsx:359-360` `readableWhenDisabled` / `disabled={pending || resendCooldown.active}`. §8.4's exemption from §7's disabled tone is "while it counts down"; here it is unconditional, so once the window is out a verify in flight disables the resend on `pending` alone and paints its label `--n-secondary` where §7's row says `--n-disabled`. Smallest correction: `readableWhenDisabled={resendCooldown.active}`. (`Composites.tsx:234-235` disables on `cooldown.active` only, so it is already aligned.)

3. **Should** — `src/app/sign-in/SignInForm.tsx:280` and `:317`, `gap-[16px]` on both forms: the space between the last field block and the primary. §8.3 names 24 (title block → first label zone), none (field block → field block) and 8 (primary → tertiary), and nothing for field block → primary; the 16 predates this ticket and is a pick the spec does not name. The ticket's Rules make that a Decision, never a pick — list it in the report's open questions (with the reading that the reserved helper line is the space, i.e. 0, as the alternative) rather than leave it silent.

4. **Should** — C-27 is "(grep and browser)". The browser half runs on `/sign-in` and the sink at 375 and 1440; no grep half exists (`grep -rn "C-27"` over `src`, `scripts`, `e2e` finds only `forms.spec.ts`). A grep over the type-scale classes on `input`/`textarea` call sites, or over `globals.css` for the `touch-action` and tap-highlight declarations, would cover the routes those two pages do not render. The ticket's Tests line asks for the engine half only, so this is a gap in the check's coverage, not in the ticket.

5. **Should** — `src/components/ui/variants.test.ts:406` `expect(inputHelperClasses("error", 2)).not.toContain("field-helper-reserved ")` leans on a trailing space to tell the one-line class from the two-line one; `has(…, "field-helper-reserved")` over the split class list is the sturdier negative and reads as what it means.

6. **Should, outside this ticket's checks (carried from pass 1)** — §7 Hit areas: sm and md buttons extend their hit area to the 40/44 floor with a transparent `::before`; `.control` (`globals.css:395-426`) has no such extension, and the tertiary is now md at 34. C-10 is not among the cited checks, so this is a Backlog task, not a Must here.

PASS
