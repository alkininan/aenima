# T0.42 — review

Commit reviewed: `f20ed4a` (branch `t0-42`, `git diff origin/main...HEAD` against `2e6c16d`). Reviewer: Fable 5.1, pass 1.

## What I read

- `docs/tickets/T0.42.md` — Objective, Build, Criteria, Tests, and its Cited section — checked word for word against the repo's `docs/design-spec.md` v2.23 §6, §7, §8.1–§8.4 and §17 (C-07, C-08, C-18, C-19, C-27, C-31), plus §3's type table, §4's Grid and Height lines and §12's +30% reserve, which the cited sections lean on. The Cited quotes match the repo.
- Every hunk of the diff: 14 files — the ticket, `e2e/form-language.spec.ts`, `e2e/forms.spec.ts` (new), `src/app/dev/primitives/Composites.tsx`, `src/app/globals.css`, `src/app/sign-in/SignInForm.dom.test.tsx`, `src/app/sign-in/SignInForm.tsx`, `src/app/sign-in/page.tsx`, `src/components/ui/Button.tsx`, `Input.tsx`, `OtpInput.tsx`, `variants.test.ts`, `variants.ts`, `src/i18n/en.ts`. `git status` clean. Every hunk is accounted for by the ticket's Objective; nothing in the diff is outside it.
- The rules that govern the touched paths: `.claude/rules/surfaces.md`, `.claude/rules/tests.md`.

## Criteria ↔ Tests

AC1 ← TC1 (`e2e/forms.spec.ts` C-07 ×2, C-08 ×3 and the §8.3 step pair; `variants.test.ts` "forms to v2.21"; the radius rows of `form-language.spec.ts`) · AC2 ← TC2 (`SignInForm.dom.test.tsx` "sign-in forms to v2.21", six tests; the timing half of C-18 and the return-to-label half of C-19 stand in the untouched `useFieldValidation.dom.test.ts` and `useCooldown.dom.test.ts` / "returns to its normal label once the window is out") · AC3 ← TC3 (`e2e/forms.spec.ts` C-27 ×4, C-31 ×1). Both directions close: no criterion without a test, no test without a criterion. C-07, C-08, C-27 and C-31 run in Chromium as §17 requires.

## Tests, run here

`node scripts/run/review-scope.mjs docs/tickets/T0.42.md` → 4 files, `missing` empty. `pnpm vitest run src/components/ui/variants.test.ts src/app/sign-in/SignInForm.dom.test.tsx` → 2 files, **90 passed**. `pnpm exec playwright test e2e/forms.spec.ts e2e/form-language.spec.ts --project chromium` against a dev server on :3142 → **38 passed**. A probe of my own in the same engine (scratchpad, not the repo) confirmed the resend under cooldown: `color rgb(157,163,176)` (`--n-secondary`), `--surface-2` at .4, cursor default, clock in JetBrains Mono 12/16 tabular, width 194.78 before and after a tick.

The red-first record: `docs/reports/T0.42.md` is not in the checkout — the protocol's order (guidelines §5 writes it at step 8, after this pass), not a defect; Should 1 names what its *Tests written* table must carry, and until it does every new test below is unverified as red-first.

## Findings

1. **Must** — `src/app/globals.css:800-801` (`.field-label { left: 0; right: 0; … }`) with the at-rest rule at `:833-837` (`transform: translate(…) scale(calc(17 / 13))`). The label's box spans the composite, and scaling that box 17/13 from its top-left pushes its right edge 30% of the composite's width past the composite. Measured in Chromium at 375×812: `/sign-in` with the email at rest → `document.documentElement.scrollWidth` **490** (label box right edge 490.15 on a 375 viewport); the same on `/dev/primitives` at 375 (490). Focus the field and it returns to 375. §4 makes the auth flow "fully usable at 375", §8.2 says "floating never shifts layout" — here the page's scrollable width changes by 115 between rest and floated, and a phone pans sideways on the sign-in step the moment the field is blurred (a tap outside it, or a submit on an empty field, which validates without focusing). The overshoot is new to this diff: the old at-rest label was unscaled. Smallest correction: give the label a content-width box — replace `right: 0` with `right: auto` (or drop it); `white-space: nowrap` already shrink-wraps the text, so the scaled box stays inside the composite. Verified by injecting that one declaration in the probe: `scrollWidth` 375 at rest, glyphs at 73 (unchanged, = 24 + 49) at rest and 32 (= 24 + 8) floated; `form-language.spec.ts`'s `zonePadding` read (`label.x − pill.x`) stays 0. Add the assertion that reddens on `right: 0` to C-08's browser test: at 375, on `/sign-in` with the field blurred and on the sink, `document.documentElement.scrollWidth === 375`.

2. **Must** (confident of the value; the fix is one class) — `src/app/sign-in/page.tsx:22` `px-[24px]` at every width. §8.3: "the step is a centred column of 400 …, **the mode's gutters** outside it"; §4 Grid: "gutters 24 (**16 in hand chrome**)", hand being <1024. Measured at 375: `main` padding-left 24, column 327. §8.2 sizes the helper reservation "at 343 wide" — 375 − 2 × 16 — so the width the spec's arithmetic is done at is the one the page does not render. The ticket's Objective claims the step's "column … geometry" and its Build names §8.3 whole; C-08's step test measures only at 1440, where 24 is right. Smallest correction: `px-[16px] lg:px-[24px]` (Tailwind's `lg` is 1024, §4's hand line), and a 375 row in the §8.3 step test asserting `column === 343` — reddened by restoring `px-[24px]`.

3. **Should** — for `docs/reports/T0.42.md` (guidelines §5 step 8): the *Tests written* table needs a row — mutation that made it red, count that went green — for each of: the six tests of "sign-in forms to v2.21" (`SignInForm.dom.test.tsx:499-587`), the three of "forms to v2.21" (`variants.test.ts:403-423`), the twelve of `e2e/forms.spec.ts` (C-07 ×2, C-08 ×3, §8.3 ×2, C-27 ×4, C-31 ×1), and the four assertions the diff rewrote (`form-language.spec.ts` two radius rows; `SignInForm.dom.test.tsx` "gives the primary the full width" and "keeps the resend beneath the primary"). Three of the e2e tests measure what a mutation must move (a padding, a `min-height`, the `transition` list); say which.

4. **Should** — `src/app/sign-in/SignInForm.dom.test.tsx:518` "leaves both submits pressable while their forms are incomplete" asserts one submit ("Send code"); the code step's Continue is never rendered in it. `.claude/rules/tests.md`: a name that promises more than it delivers — either `arrive()` and assert Continue's `disabled` is false with an empty code, or rename to the one it checks.

5. **Should** — `src/app/globals.css:774-777`, the comment on `.field-pill:has(.field-input[aria-invalid]) .field-icon-leading`: "after the focus rule at equal specificity so source order decides". `:has()` takes its argument's specificity, so the rule is (0,4,0) against `.field-pill:focus-within .field-icon-leading`'s (0,3,0) — it wins wherever it sits, and the reorder changed nothing. The next comment, "Highest specificity of the four" on the `:disabled` rule, is likewise equal to the error rule's, with order deciding between those two only. Say what the cascade actually does.

6. **Should** — `e2e/forms.spec.ts:131-155`: the sink's error demo carries "That code didn't work", one line, so the check proves the 36 reservation holds and its states match, not that §12's wrong-code string at +30% stays inside it, which is the sentence C-08 cites. Put `t.signIn.codeRejected` on the sink's error demo (or refuse a code on `/sign-in`) and assert the helper's text height ≤ 36 at 343.

7. **Should, outside this ticket's checks** — §7 Hit areas: "sm and md buttons … extend [the] hit area to the floor with a transparent `::before`"; `.control` has no such extension (`globals.css:395-425`), and the tertiary is now md at 34. C-10 is not among the ticket's cited checks, so this is a Backlog task, not a Must here.

FINDINGS
