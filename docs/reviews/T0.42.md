# T0.42 — review

Commit reviewed: `bdaca4a` (branch `t0-42`, `git diff origin/main...HEAD` against `2e6c16d`). Reviewer: Fable 5.1, pass 3.

## What I read

- `docs/tickets/T0.42.md` — Objective, Build, Rules, Criteria, Tests and the Cited section. The ticket cites v2.21 and says the repo holds v2.23; the repo header reads v2.23, and every one of the 57 quoted lines in Cited is present verbatim in `docs/design-spec.md` (checked by script). Read alongside the lines the cited sections lean on: §3's type table (ui-label 13/18 Medium, ui-input 17/22, ui-button 15/20, ui-button-sm 13/18, ui-footnote 13/18, mono-readout 12/16 tabular, special-otp 22/24), §4's Grid (gutters 24, 16 in hand chrome), Layout modes (Hand <1024, Desk from 1024) and Height (16 below 600 tall), §6's focus-visible mechanism and §12's +30% reserve.
- Every hunk of the diff, 16 files: the ticket, the pass-2 review, `e2e/form-language.spec.ts`, `e2e/forms.spec.ts` (new), `src/app/dev/primitives/Composites.tsx`, `src/app/globals.css`, `src/app/sign-in/SignInForm.dom.test.tsx`, `src/app/sign-in/SignInForm.tsx`, `src/app/sign-in/page.tsx`, `src/app/touch.test.ts` (new), `src/components/ui/Button.tsx`, `Input.tsx`, `OtpInput.tsx`, `variants.test.ts`, `variants.ts`, `src/i18n/en.ts`. And the touched sources whole, plus `IconButton.tsx`, `useCooldown.ts`, `src/lib/motion.ts`, `src/test/setup-dom.ts`, the stylesheet's `.control`, ring and reduced-motion blocks, `playwright.config.ts`, `vitest.config.ts`, and the area rules `.claude/rules/surfaces.md` and `tests.md`. `git status` clean. Every hunk is accounted for by the Objective and the sections its Build names; the two review-file commits are the run committing pass 1's and pass 2's verdicts after each fix, not edits of its own.

## Pass 2's Shoulds, on the tip

- Should 2 settled: `SignInForm.tsx:359` `readableWhenDisabled={resendCooldown.active}`, and `SignInForm.dom.test.tsx:513` "gives the resend §7's disabled tone while a verify is in flight" pins it (`disabled:text-n-disabled` once the window is out and `pending` alone disables it).
- Should 4 settled: `src/app/touch.test.ts` is C-27's grep half over `globals.css` with comments stripped — the `touch-action: manipulation` rule's selector list, the root's `-webkit-tap-highlight-color: transparent`, and no `font-size` under 16 on `.type-ui-input` or `.type-special-otp`.
- Should 5 settled: `variants.test.ts:387` splits the class list before the negative.
- Shoulds 1, 3 and 6 stand, carried below as 1, 3 and 4.

## What I checked myself

- **C-07.** `variants.ts:46-50` sm/md/lg `h-[28|34|48px]` with `py-[5|7|14px]` = (height − 18|20|20) ÷ 2 against §3's line boxes; `ICON_BUTTON_SIZE_CLASSES` now declares `p-[5|7|12px]` = (box − icon) ÷ 2 and `IconButton` cannot take `readableWhenDisabled`. `e2e/forms.spec.ts:18-85` measures both in Chromium.
- **C-08.** `globals.css:796-820` — the label box is `left: 0; padding-inline: 8px`, no `right`, ui-label in both states, `transform-origin: top left`, transition on `transform` alone; at rest (`:827-843`) `translate(rest-x − 8 × (17/13 − 1), 24 + (48 − 18 × 17/13) ÷ 2) scale(17/13)`. I recomputed the offsets: text at 1 + 16 from the pill (49 behind a leading icon) and the scaled 23.54 line centred in the 48 pill. `prefers-reduced-motion` snaps it (`:1057`). `.field-helper-reserved-2 { min-height: 36px }` and `OtpInput.tsx:177` asks for 2; `Input.tsx:155` still 1 or 0; `GapMoves.tsx:265` takes the default 1. Error outranks focus: `inputFieldClasses` drops `focus-within:border-prime` on an invalid pill (`variants.ts:206`) and the icon's `:has([aria-invalid])` rule sits after the `:focus-within` one; the keyboard ring (`globals.css:893-897`) keys on `html[data-focus-modality="keyboard"]`, not on the dropped utility, so an errored field still takes the ring under keyboard modality. §8.3: `page.tsx:24` `px-[16px] … lg:px-[24px]` is §4's Hand/Desk gutter split at 1024; `py-[48px]` with `[@media(max-height:599px)]:pt-[16px]`; `SignInForm.tsx:236` header `gap-[24px]`, `:227` column `gap-[24px]` to the first label zone, `:317`/`:347` `gap-[8px]` primary to tertiary, tertiary Neutral md, the request slot after the step's last control on both steps.
- **C-18.** Neither submit ever carries `disabled` (`SignInForm.tsx:283`, `:348`); `onEmailSubmit` calls `validateNow()` and `scrollIntoView` on the input; `submitCode` scrolls to the OTP wrapper. The timing half is the untouched `useFieldValidation.dom.test.ts` (floor, 1.5s pause, blur, instant clear), which the diff does not weaken.
- **C-19.** `useCooldown` starts on "sent" (`SignInForm.tsx:127`), 60s from `RESEND_COOLDOWN_MS`; the label is one inline run with the clock in `.type-mono-readout` (12/16, tabular-nums, so the width holds); `BUTTON_DISABLED_CLASSES` fades the fill alone (`bg-surface-2/40`) and `readableWhenDisabled` swaps only the text tone to `--n-secondary`; `.control:disabled` sets cursor and edge, never colour, so nothing overrides it. Return to the plain label at zero: `useCooldown.dom.test.ts:74` and `SignInForm.dom.test.tsx:261`.
- **C-27.** `globals.css:193-212` — `html { -webkit-tap-highlight-color: transparent }` (inherited) and `touch-action: manipulation` on `button, a[href], input, textarea, select, label, summary` and the button/option/menuitem/tab/switch/combobox roles. Grep half in `touch.test.ts`; browser half over `/sign-in` and the sink at 375 and 1440.
- **C-31.** `e2e/forms.spec.ts:282-303` — autofocused field with no ring on load, none after a click, `2px` after Tab, `main` never.
- **Rules.** No bare string in JSX (the sink's demo error now reads `t.signIn.codeRejected`); no hex, radius or size outside the spec's; `resendIn` returns its parts and only `en.ts` exists, so no TR/NL dictionary is left on the old shape; fake timers name only what the code reads (`tests.md`).

## Criteria ↔ Tests

AC1 ← TC1: `e2e/forms.spec.ts` C-07 ×2, C-08 ×3, §8.3 ×3; `e2e/form-language.spec.ts` "a field does not shift…" and the two `--r-pill` rows; `variants.test.ts` "forms to v2.21" ×3. AC2 ← TC2: `SignInForm.dom.test.tsx` "sign-in forms to v2.21" ×7 (C-18's submit half, C-19's shape and tone, the request slot), with the timing half of C-18 in `useFieldValidation.dom.test.ts` and C-19's return-to-label in `useCooldown.dom.test.ts` and `SignInForm.dom.test.tsx:261`. AC3 ← TC3: `e2e/forms.spec.ts` C-27 ×4 and C-31 ×1; `src/app/touch.test.ts` ×3. Both directions close. C-07, C-08, C-27 and C-31 run in Chromium, as the Tests line and §17 require.

## Tests, run here

`node scripts/run/review-scope.mjs docs/tickets/T0.42.md` → 5 files, `named` empty, `missing` empty. `pnpm vitest run src/app/sign-in/SignInForm.dom.test.tsx src/app/touch.test.ts src/components/ui/variants.test.ts` → 3 files, **94 passed**. `PLAYWRIGHT_BASE_URL=http://localhost:3147 pnpm exec playwright test e2e/forms.spec.ts e2e/form-language.spec.ts --project chromium` against a dev server started from this worktree on :3147 → **39 passed**.

The red-first record: `docs/reports/T0.42.md` is not in the checkout — the protocol writes it at step 8, after this pass (guidelines §5) — so every new test below is unverified as red-first until it lands. Should 1 names what its *Tests written* table must carry.

## Findings

No Must stands.

1. **Should** — for `docs/reports/T0.42.md` (guidelines §5 step 8): the *Tests written* table needs a row — the mutation that made it red, the count that went green — for each of: the seven of "sign-in forms to v2.21" (`SignInForm.dom.test.tsx:477-612`), the three of "forms to v2.21" (`variants.test.ts:387-410`), the three of `src/app/touch.test.ts`, the thirteen of `e2e/forms.spec.ts` (C-07 ×2, C-08 ×3, §8.3 ×3, C-27 ×4, C-31 ×1), and the six assertions the diff rewrote (`form-language.spec.ts:111` and `:141` radius; `SignInForm.dom.test.tsx:118` and `:143`; `variants.test.ts:274` and `:291`). Pass 3's additions have their mutations ready: `readableWhenDisabled={true}` at `SignInForm.tsx:359` reddens `:513`; removing the `touch-action` block or the root's tap-highlight line at `globals.css:193-212` reddens `touch.test.ts`. Say so in the rows, and name the model of every reviewer pass.

2. **Should** — `src/app/sign-in/SignInForm.dom.test.tsx:558` `Element.prototype.scrollIntoView = scrolled;`. jsdom has no `scrollIntoView` and `setup-dom.ts` adds none, so before this line runs any test in the file that submits an empty email or an incomplete code would throw `TypeError`, and after it the mock outlives the test for the rest of the file — the suite is green by test order. Smallest correction: a no-op `Element.prototype.scrollIntoView` once in `src/test/setup-dom.ts`, and here `const scrolled = vi.spyOn(Element.prototype, "scrollIntoView")` restored in `afterEach`.

3. **Should (carried from pass 2)** — `src/app/sign-in/SignInForm.tsx:280` and `:317`, `gap-[16px]` on both forms: the space between the last field block and the primary. §8.3 names 24 (title block → first label zone), none (field block → field block) and 8 (primary → tertiary), and nothing for field block → primary; the 16 predates this ticket and is a value the spec does not name. The ticket's Rules make that a Decision, never a pick — record it in the report's open questions as the default taken, with 0 (the reserved helper line as the space, §8.3's own reading for field blocks) as the alternative, rather than leave it silent. I am uncertain whether a value the diff inherits rather than sets is the run's to answer; it is the ticket's section, so it is at least the report's to name.

4. **Should, outside this ticket's checks (carried from pass 2)** — §7 Hit areas: sm and md buttons extend their hit area to the 40/44 floor with a transparent `::before`; `.control` (`globals.css:395-426`) has no such extension, and the tertiary is now md at 34 on a real page. C-10 is not among the cited checks, so this is a Backlog task (Type Fix, Epic inherited), not a Must here.

5. **Should** — `src/app/sign-in/SignInForm.tsx:365-373` and `src/app/dev/primitives/Composites.tsx:241-249` carry the same eleven lines: the `<span>` wrapper, `t.signIn.resendIn(…)`, the `key="clock"` mono span and its comment. The sink is meant to render what the product renders; one `ResendLabel` (or a `Cooldown`-taking child) beside `useCooldown` would keep the two from drifting the next time §8.4 moves.

PASS
