# T0.38 — review

Commit c414186 (pass 2, Fable 5.1). Read against `docs/tickets/T0.38.md`, design-spec v2.23 §0 and §17 (the ticket's Cited block matches the file at HEAD line for line), §12's defaults line (562), §8.9 (393), §8.10 (395–404), §8.27 (479–481), §8.32 (511), §5's fallback (266) and §4's scrollbar mechanism (240); product-spec v1.10 Appendix G. Diff `origin/main...HEAD`, every hunk, and the delta `f769aa0..HEAD` for what moved after pass 1. Ran `node scripts/run/review-scope.mjs docs/tickets/T0.38.md` — eight files, none missing — then `pnpm vitest run` on its five vitest files (`scripts/design/laws.test.mjs`, `scripts/design/spec-checks.test.mjs`, `src/app/i/[key]/CheckList.dom.test.tsx`, `src/components/ui/variants.test.ts`, `src/lib/relative-time.test.ts`): 5 files, 111 tests, all green; `laws.test.mjs` alone holds 20. `node scripts/design/spec-checks.mjs` exits 0 over the committed spec. The three Playwright files in scope I did not run — a second suite on the machine (guidelines §5 step 5); the report records their runs.

Criteria ↔ Tests: AC1–AC5 each have a TC; TC1 sits in `spec-checks.test.mjs`, TC2–TC5 in `laws.test.mjs`, each `describe` naming its TC → AC. Both directions close.

Pass 1's three Musts are settled in the diff: `DEFAULTS` lists reopen and the two unclear chips (`laws.mjs:799-801`), `en.ts:168` and `:293` hold §12's words, `CheckList.tsx:315` reads the chip by tag, and the report's table carries the f769aa0 tests and the Playwright runs. Its Shoulds 5, 7 and 9 are fixed; 4 is rewritten; 6 and 8 stand as recorded.

## Must

1. **Must — `scripts/design/laws.mjs:780` `DEFAULTS` leaves out a §12 gap-chip default whose surface exists: accepted = "Must · {accepter}".** §12 (line 562) lists four gap-chip defaults — "Must · unclear" / "Should · unclear" / "Must · {check id}" / "Must · {accepter}" — and §8.9 (line 393) places the last: "**The chip's text carries priority and state, never colour alone** (§13): … accepted "Must · {accepter}"", with the accepted chip's recipe "`--surface-2` + `--n-secondary` + accepter name". The surface is built: `src/app/i/[key]/GapList.tsx:174-183` renders an accepted gap's `<Chip variant="gap" tone={toneFor(gap)}>{label}</Chip>` with `label = t.item.gapAccepted` — the word "Accepted" — and puts the tag in a separate mono-micro span (`:200-203`) and the name in the stamp beneath. The ticket's Decision default is "C-33 checks only the §12 defaults whose surface exists, and a string that differs is fixed to §12's words", and the ticket's own record of pass 1 already reads that rule as reaching a composed chip. The check lists two of §12's four chips and passes without looking at this one. Smallest correction: a key of §12's shape with the slot's name out — `item.gapAcceptedChip: (accepter: string) => \`Must · ${accepter}\`` (and its Should twin, since a Should can be accepted too: `GapList.dom.test.tsx:128`) — fed `actorSelf`/`actorOther`, listed in `DEFAULTS`, rendered by the chip at `GapList.tsx:181`; `GapList.dom.test.tsx` and `e2e/item.spec.ts:372` ("keeps an accepted gap named…") follow.

2. **Must — the same omission for the row: "Must · {check id}".** §8.9 (line 393): "on a row "Must · {check id}", the id in mono-readout"; §12 lists it among the gap chips. `src/app/app/ItemRow.tsx:157-159` renders `<Chip variant="gap" tone={gap.tag}>{gap.checkId}</Chip>` — the id alone, the priority left to the tone, which is the case §8.9's sentence and C-32 exist to refuse. The surface exists, the words differ, `DEFAULTS` has no row. I am sure of the surface and the words; what makes me name the exit below is geometry the diff does not touch — `ItemRow.tsx:50` still shows "max 2 + overflow" (`VISIBLE_GAPS`) where §8.27 v2.22 (line 479–481) keeps "one gap chip + an overflow count" in a 160 column with the chip at most 112 wide ("Should · PRD-07" measures 110.7), so two chips of §12's shape do not fit the column the way two bare ids did. Smallest correction: either `list.gapChip: { must: (id) => \`Must · ${id}\`, should: … }` in `DEFAULTS` and on the chip (the id in a `type-mono-readout` span, per §8.9) with §8.27's one-chip rule applied at the same time and `e2e/list.spec.ts` following; or, if the run reads the §8.27 change as another ticket's, the row's chip named in the report's open questions with a phase owner rather than left out of `DEFAULTS` in silence — AGENTS.md: an unrecorded stopgap is a bug. What is not acceptable is the check claiming "the §12 defaults whose surface exists" while not listing a surface that exists.

3. **Must — the red-first record has no row for seven of `laws.test.mjs`'s twenty tests.** The six detector tests under C-06/C-39 — `laws.test.mjs:53` "reads a colour, a radius, a duration, a shadow and a z-index…", `:70` "reads a font size or a line height…", `:78` "passes a token, one of §4's rungs…", `:103` "reads a shadow as literal only when…", `:111` "reads class strings from the syntax tree…", `:131` "does not read the token layer…" — and C-50's `:226` "finds the clash when the standard properties leave §4's gate". Row 3 of *Tests written* names "C-06/C-39 repo scan … C-50" against "the tree before the first claim's `src/` fixes", and a tree mutation cannot redden a test whose input is planted inside the test; nothing else in the table names them. Guidelines §5 step 8: the report is "refused without the red-first record: per test, the mutation that made it red and the count that went green". Pass 1 named only the last commit's tests; it should have named these. Smallest correction: one row per test — or one row per detector naming every test it reddens — e.g. `utilityLiteral`'s colour branch returning null (`:53`), `declarationLiteral`'s font-size branch (`:70`), `isTokenValue` returning false (`:78`), `isLiteralShadow`'s `var(` guard (`:103`), `isPlusChain` (`:111`), `isTokenLayer` (`:131`), `gated` (`:226`) — each with the count that went green.

## Should

4. **Should — the table's counts do not match the file.** Rows 6, 8, 9, 10 and 11 of *Tests written* end at "21 passed" for `laws.test.mjs`; the file holds 20 at HEAD and at f769aa0 (`git diff f769aa0..HEAD` does not touch it; `pnpm vitest run scripts/design/laws.test.mjs` → 20), and row 7 says 20. The counts are the record's evidence; bring them to what the file holds.

5. **Should — `laws.mjs:817` `dictionaryShapes` keeps the first string under a key.** A key holding a function with two branches (`en.ts:269` `versionCount`) is read by one branch only, so a §12 default keyed on such a function would be checked against half of it. Inert today; a comment saying so, or reading every string under a key, keeps the next row honest.

6. **Should — `laws.mjs:420` `literalOffenders` does not read inline `style` objects.** `Meter.tsx:67` (`width: \`${clamped}%\``) and `PipelineStrip.tsx:92` (`"--strip-pad": "4px"`) are the only two and neither is a C-06 literal, but a `style={{ borderRadius: "8px" }}` would pass C-06's grep, since a bare `"8px"` matches no utility pattern and a bare `"#fff"` is only tested inside brackets (`:315`). Name the blind spot in the header comment, or read `style` attributes' string values through `declarationLiteral`.

7. **Should — `laws.mjs:755` the sentence-case scan skips the word after an interpolation.** `textOf` joins a template's head and spans with spaces, so `` `${actor} accepted this` `` reads " accepted this" and `slice(1)` drops "accepted" as the segment's first word; a capital there would pass. Inert in `en.ts` today; a `{}` placeholder from `shapeOf` would count as the first word instead.

Checked and not a finding: `.scroll-thin`'s standard properties inside `@supports not selector(::-webkit-scrollbar)` is §4 line 240's own mechanism; the `@supports not (backdrop-filter …)` fallback on `.glass-blur` alone follows §5 line 266 — `.glass` carries no `backdrop-filter` and so has nothing to fall back from; `z-[calc(var(--z-content)+1)]` compiles to `calc(var(--z-content) + 1)` under the installed Tailwind 4.3.3 (compiled it) with `--z-content: 0`, so the menu's rung is the previous 1; `PRIMARY_TEXT` in `e2e/item.spec.ts:21` is `--n-primary` `#E0E5EB` resolved; `ActivityFeed.tsx`'s actor in `--n-primary` follows C-36's list and §0 law 4's "drafted or proposes"; Appendix G's words are the ones §1 law 6 and §13 name and its `archive` exclusion is reasoned in the appendix; `checkUnclear` keyed by tag is exercised for both tags by `CheckList.dom.test.tsx`; the four §12 strings the Decision named and the two pass 1 named are as §12 writes them; `c34Terms` reads fifteen terms and Appendix G has a row for each.

FINDINGS
