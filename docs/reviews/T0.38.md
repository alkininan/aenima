# T0.38 — review

Commit 121c552 (pass 3, Fable 5.1). Read against `docs/tickets/T0.38.md`, design-spec v2.23 §0 and §17 (the ticket's Cited block matches the file line for line, one trailing blank aside), §4 (lines 216–245: the z-ladder, the scrollbar mechanism), §5 (246–271: the recipe and its blur rule), §6's skeleton (301), §8.2's floated label (v2.22, 360), §8.9 (393), §8.10 (395–404), §8.24's Unclear state (462), §8.27 (479–481), §8.32 (511), §8.34 (519) and §12's defaults line (562); product-spec v1.10 §1 law 6 (line 38), §13 (327) and Appendix G. Diff `origin/main...HEAD`, every hunk, and `git show 293def6^` for the tree the red-first record's row 3 names. Ran `node scripts/run/review-scope.mjs docs/tickets/T0.38.md` — nine files, none missing — then `pnpm vitest run` on its six vitest files: 6 files, 124 tests, all green. `node scripts/design/spec-checks.mjs` exits 0 over the committed spec. The three Playwright files in scope I did not run — a second suite on the machine (guidelines §5 step 5); the report records their runs.

Criteria ↔ Tests: AC1–AC5 each have a TC; TC1 sits in `spec-checks.test.mjs`, TC2–TC5 in `laws.test.mjs`, each `describe` naming its TC → AC. Both directions close.

Pass 2's Musts: (1) the accepted chip is settled — `en.ts:349` `gapAcceptedBy` keyed by tag, `GapList.tsx:178` renders it through `actorWords`, `laws.mjs:802–803` lists both, `GapList.dom.test.tsx` covers Must-by-self and Should-by-other, and neither `GapList.tsx` nor `GapMoves.tsx` carries `"use client"`, so the new import crosses no boundary. (2) the row's chip is recorded as an open question with an owner (`T-next`, the form T0.30, T0.96 and T2.10 use) in the report and in the ticket's Decision section — the exit pass 2 named. (3) rows 14–20 of *Tests written* now cover the six detector tests and C-50's, each with a mutation inside the detector and a count.

## Must

1. **Must — `laws.test.mjs:161` "C-37 modals, sheets and the pipeline strip carry the unblurred class and not the blurred" has no row in *Tests written* that can have reddened it.** The only row naming C-37 is row 3, whose mutation is "the tree before the first claim's `src/` fixes (the fix diff reverse-applied)" and whose count is "4 failed / 11 passed". On that tree — `293def6^`, which I read — `.glass` declares `backdrop-filter` (`globals.css:524`), no `.glass-blur` exists, and `MODAL_BASE` (`variants.ts:677`), `SHEET_BASE` (`:696`) and the strip (`PipelineStrip.tsx:92`) all carry `glass`. So `classUses(root, "glass-blur")` is empty, every `blurred.has(context)` is false, every `classUses(root, "glass").some(…)` is true, and the test passes. The four reds row 3 counts are the C-06 scan, C-36, the *first* C-37 test (`.glass` outside `BLUR_ALLOWED`) and C-50 — the count itself leaves no fifth. The test existed at 293def6 (`laws.test.mjs:160` there) and no later row names it. Guidelines §5 step 8: per test, the mutation that made it red and the count that went green; a test with no such row is unverified. Smallest correction: one row — e.g. `MODAL_BASE`'s `glass` → `glass-blur` in `variants.ts`, or `SHEET_BASE` renamed — with the count that went green.

## Should

2. **Should — `src/i18n/en.ts:343` `gapAccepted: "Accepted"` has no renderer left.** `GapList.tsx:178` now reads `gapAcceptedBy`; nothing else in `src/` names `gapAccepted`. AGENTS.md: remove the old path in the same change. Delete the key.

3. **Should — `laws.mjs:772–775` states `DEFAULTS`'s rule as "whose surface exists" and applies it as "whose key exists".** `common.retry` and `errors.backToApp` are listed as "degraded pages", and no route renders either — `src/app` holds no `error.tsx` or `not-found.tsx`, and no component reads `t.errors` or `t.common.retry` — so the report's "what it found, fixed: Try again → Retry, Back to your workspace → Back to dashboard" describes strings no surface shows. The freshness ladder's fifth rung ("{n} y ago", §12) is the reverse: a surface that exists and a key that does not, left out and recorded as an open question. Neither changes a result today. Say in the comment that the list is keyed by what `en.ts` holds, and name the year rung there as the one built surface it does not reach, so the next reader does not take "surface" at its word.

4. **Should — product-spec Appendix G lists "quiz" under walkthrough, and neither spec names the word.** The appendix's own rule (line 481) admits a word "in so many words, or by an argument a section already makes"; §1 law 6 (line 38) names test, fail and violation, and "the register it refuses" points at no section. The Decision made the appendix a draft for the human's read; the row should say the word is the draft's own, or drop it until a section names it.

Pass 1's Shoulds 6 and 8 and pass 2's 5–7 stand as recorded in the report.

Checked and not a finding: `.field-label` follows §8.2 v2.22 — ui-label in both states via `type-ui-label` (13/18/500, `globals.css:324`), `transform` alone over `--t-fast` on `--ease`, scaled 17/13 about `8px 0` so the text's left edge stays at 8 + 9 = 17, the value's own x, and the translate `24 + (48 − 18·17/13)/2` centres the scaled line in the pill; `--glass-border-hover` (`globals.css:32`, `@theme:127`), `.shimmer` on `--t-skeleton` (`:504`, §6 line 279) and `--z-content: 0` (`:101`) all exist; `.scroll-thin`'s standard properties inside `@supports not selector(::-webkit-scrollbar)` is §4 line 240's mechanism verbatim; the `@supports not (backdrop-filter …)` fallback on `.glass-blur` alone follows §5 — `.glass` has no blur to lose; `panelRowClasses` puts `text-danger` on `destructive` only; `toneFor` gives an accepted gap §8.9's tones; `AGENT_ALLOWED` empty holds — no proposal card, spinner, caret or chart is built, and the activity actor is none of C-36's four; `DEFAULTS` covers every §12 default that has both a key and a surface, the row chip and the year rung recorded; `c34Terms` reads fifteen and Appendix G has a row for each; the sentence-case scan clears `en.ts` with `Product` and `Teams` as recorded; the committed `docs/reviews/T0.38.md` is pass 2's verdict unchanged.

FINDINGS
