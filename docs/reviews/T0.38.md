# T0.38 — review

Commit f769aa0 (pass 1, Fable 5.1). Read against `docs/tickets/T0.38.md`, design-spec v2.23 §0 and §17 (the ticket's Cited block matches the file at HEAD line for line) and product-spec v1.10 Appendix G. Diff `origin/main...HEAD`, every hunk. Ran `node scripts/run/review-scope.mjs docs/tickets/T0.38.md` and then `pnpm vitest run` on its four vitest files (`scripts/design/laws.test.mjs`, `scripts/design/spec-checks.test.mjs`, `src/components/ui/variants.test.ts`, `src/lib/relative-time.test.ts`): 4 files, 91 tests, all green. `node scripts/design/spec-checks.mjs` exits 0 over the committed spec. The three Playwright files in scope (`e2e/form-language.spec.ts`, `e2e/item.spec.ts`, `e2e/list.spec.ts`) I did not run — Playwright's config boots a dev server and a `next build` server, a second suite on the machine (guidelines §5 step 5) — see Must 3 for what the record says about them.

Criteria ↔ Tests: AC1–AC5 each have a TC, TC1 sits in `spec-checks.test.mjs` and TC2–TC5 in `laws.test.mjs`, each `describe` naming its TC → AC. Both directions close.

## Must

1. **Must — `scripts/design/laws.mjs:985` `DEFAULTS` leaves out a §12 default whose surface exists: reopen = "Reopen".** The Decision's default (ticket, *Decision answered*) is "C-33 checks only the §12 defaults whose surface exists". §12 lists `reopen = "Reopen"`, and §8.10 (design-spec line 400) makes it a standing Secondary sm control beside the stamp — the button `src/app/i/[key]/GapMoves.tsx:147-149` renders, whose label is `src/i18n/en.ts:289` `gapReopen: "Reopen this gap"`. The surface exists, the string differs, and the check does not list it, so C-33's "the §12 defaults appear verbatim" passes without looking at it. The run applied §12's word to the sibling control in the same form (`gapAcceptSubmit: "Looks right"`, whose own comment cites §0 law 8 against saying the move twice on one control); the reopen button is the same case. Smallest correction: add `{ key: "item.gapReopen", default: "Reopen", as: "reopen" }` to `DEFAULTS`, set `en.ts:289` to `"Reopen"`, and update the two literal names in `e2e/item.spec.ts:284` and `:375` (`GapList.dom.test.tsx` and `GapMoves.dom.test.tsx` read the key and follow on their own).

2. **Must (uncertain — said so) — the same omission for §12's gap chips: `"Must · unclear"` / `"Should · unclear"`.** §12 names them, §8.9 (line 393) and §8's unclear state (line 462) put the words on "the §8.9 gap chip in its open tones, reading priority and state", and C-32 requires the chip's text to carry priority and state. The check list's chip exists — `src/app/i/[key]/CheckList.tsx:314` renders `<Chip variant="gap" tone={check.tag}>{t.item.checkUnclear}</Chip>`, which paints "Unclear" and leaves the priority to the tone alone. The surface exists, the words differ, and `DEFAULTS` has no row. I am sure of the surface and the words; I am less sure the run reads a two-part chip string as one of the Decision's "defaults", which is why this is numbered apart from 1. Smallest correction: two keys — `item.checkUnclearMust: "Must · unclear"`, `item.checkUnclearShould: "Should · unclear"` — read by `CheckList.tsx:314` on `check.tag`, both listed in `DEFAULTS`; or, if the run reads the Decision as not reaching composed chip text, say so in the report's open questions with a phase owner rather than leaving the default unlisted in silence (AGENTS.md: an unrecorded stopgap is a bug).

3. **Must — the red-first record does not cover the tests the last commit added, and the report contradicts the diff.** `docs/reports/T0.38.md` at HEAD is the 2026-09-24 in-progress file: its *Tests written* table (lines 31-37) has no row for the two C-33 defaults tests (`laws.test.mjs:1279` "hold §12's defaults verbatim…", `:1283` "reads a default by its shape…") nor for the three C-34 tests (`:1298`, `:1302`, `:1309`), all added in f769aa0; its AC3 line (report line 20-21) still says "C-33's defaults half and C-34: **not built — the Decision**", and *Open questions* (lines 55-56) still lists the Decision as open. The same commit changed `e2e/item.spec.ts:160-166` (the agent-actor assertion inverted) and `e2e/item.spec.ts:813`, `e2e/list.spec.ts:256-268` (the freshness strings), and the report records a Playwright run for `form-language.spec.ts` only. Guidelines §5 step 4: "new logic has tests observed failing first, the mutation and the count recorded per test"; a test with no row is unverified. Smallest correction: one row per new test with the mutation that made it red and the count that went green (a planted `"Test drive"` in `en.ts` for C-34's word grep, a removed Appendix G row for its row check, a changed `en.ts` default for the defaults test), the run record for the three Playwright files, and the AC3/open-questions lines brought to what the diff holds.

## Should

4. **Should — the report's AC4 line describes a change the merged diff no longer carries.** Report lines 22-25: "`.glass` carried `backdrop-filter` everywhere; split into `.glass` … and `.glass-blur`". That was true of 293def6's base; T0.37 (14edb3e) landed the same split on main and the merge reconciled, so against `origin/main` this ticket's glass change is one hunk — the `@supports not (backdrop-filter: blur(1px))` fallback moved from `.glass` to `.glass-blur` (`globals.css:548-552`). Likewise "Passes over `docs/design-spec.md` v2.22" (line 12) and *Changed since this ticket was cut* (lines 50-51) stop at v2.22 while the file is v2.23. Step 8 rewrites the report; write it from the diff as merged.

5. **Should — `laws.mjs:942` `NAMES` exempts nothing.** `copyOffenders` replaces "Opportunity Brief" with its first word, "Opportunity", which is still capitalised and is not in `CAPITALISED`, so a mid-sentence "Opportunity Brief" would be reported, the opposite of the comment's intent. Today the only use (`en.ts:125`) is sentence-initial, so it is inert. Replace with a lowercase placeholder, or add the whole name to the set the scan consults.

6. **Should — `laws.mjs:613` files `html { font-size: 16px }` under `C06_NAMED`.** It is C-39's literal clause, not one of the literals C-06 names; §3 ("Root 16px", line 186) and §4 (line 218) are what except it. Either a `C39_NAMED` list, or `isTokenLayer` treating `html`'s root size as the type scale's root, so the constant's name and comment tell the truth.

7. **Should — `laws.mjs:1088` `steeredWord`'s suffix set misses a doubled consonant.** Appendix G says a listed word matches "with a plural or verb ending"; `quiz(?:s|es|d|ed|ing)?` does not reach "quizzes" or "quizzed". Small; note it or widen the set.

8. **Should — `laws.mjs:908` `CAPITALISED` carries "Product" and "Teams" as bare words.** The set exists for product-spec §14's role names and the Teams connector, but a bare-word set lets "Product" and "Teams" pass mid-sentence in any use, which is the case C-33's sentence-case clause exists to catch. Acceptable today (`en.ts:315` and `:324` are the role); worth a comment naming why each entry is there, or a narrower match.

9. **Should — `e2e/item.spec.ts:160-166` pins only what the colour is not.** `expect(colour).not.toBe(AGENT)` passes for any colour, `--danger` included; the rule is C-36 and the surface's own class is `text-n-primary` (`ActivityFeed.tsx:66`). Assert the value it should be (`.claude/rules/tests.md`: pin the rule, not the discrepancy).

Checked and not a finding: the `ActivityFeed.tsx` actor losing `text-agent` follows C-36's list and §0 laws 3–4 (an activity entry is neither drafted nor proposed content; the design spec names no violet for it); `z-index: calc(var(--z-content) - 1)` resolves to the previous `-1` with `--z-content: 0` in the token layer; the field label's `transform-origin: 8px 0` reads §8.2's "from its top-left" as the text's top-left inside the 8 zone padding, which is what keeps label and value at x = 17; the C-50 gate is §4's own mechanism (line 240); the four §12 strings the Decision named are as §12 writes them, and `"scored 6 h ago — retrying"` agrees with §10 line 551.

FINDINGS
