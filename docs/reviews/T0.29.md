# T0.29 — review

Commit reviewed: `e22c317`

Read cold against `docs/tickets/T0.29.md` and design-spec v2.21 §2, §3, §6 and §17 as the ticket
cites them. The ticket's *Cited* §2 is byte-identical to `origin/main:docs/design-spec.md`'s §2,
its C-39 is verbatim, and §3's "C-39 checks every shipped face for the coverage below" is already
in main — so the objective's two premises are the document's, not the branch's, and the branch did
not edit the text the checks read except where the Build says.

What I checked myself rather than taking from the branch:

- Every hunk of `git diff origin/main...HEAD`. Two files: `docs/design-spec.md` with exactly five
  changed lines and one added — title (1), the v2.21 line losing its trailing ` -->` (25), the
  v2.22 line added (26), §2's `--hero-gradient` comment (147), C-39 (650), the footer (669) — and
  `docs/tickets/T0.29.md`, the pack step 2 writes. Nothing else in the diff, and
  `git status --porcelain` is clean, so the diff is the whole change.
- The added changelog line against the ticket's fenced block: byte-identical, `-->` included, and
  line 25 now ends `(§17).` with no trailing space. The other four replacements are the ticket's
  strings exactly.
- TC1 `grep -c -- "-->"` = 1 · title, last changelog line and footer all read v2.22 · 23 changelog
  lines, v2.0 through v2.22, one per version, the comment closing on its last.
- TC2 = 0. C-03 and C-04 I resolved by hand (see Should 4): the diff names no custom property in
  prose outside the comment C-03 exempts, and every section the new line references — §0, §2–§7,
  §9, §10, §12, §14, §17 and §8.1, §8.2, §8.4, §8.23, §8.25, §8.27, §8.32, §8.33, §8.35, §8.36 —
  exists as a heading, as does the `(§6)` the new §2 comment adds.
- TC3 = 0 and 1 · TC5 = 0 and 1.
- The changelog line's claims are the body's, spot-checked where they are checkable: §8.23 "the
  fill switches to `--success` when the item is handover-ready … whatever the percentage reads",
  §8.25 "pad 8 vertical / 4 horizontal", §8.1 "padding (box − icon) ÷ 2", §4 "Firefox gets no
  hover colour", §10 "a row shows the `--warning` dot alone", §14 danger only on a deletion, §9
  four neutral series. None overstates its section.
- The one test in the suite that reads the changed file, `src/app/tokens.test.ts` (C-05 and the
  type half of C-39): **1 file, 24 tests, all passing**. Its `stripComments` drops the CSS
  comments, so the §2 edit cannot move it either way — which is also why nothing catches Should 1.
- `node scripts/run/review-scope.mjs docs/tickets/T0.29.md` lists no files and nothing `missing`:
  TC1–TC5 are greps, so there is no vitest scope and no new test to have been observed red. The
  branch carries no `docs/reports/T0.29.md` and no `docs/log/T0.29.md` yet; guidelines §5 puts the
  report at step 8, after this review at step 5, and `report-check.mjs` owns the red-first and
  reviewer-pass tables. Not a finding here — there was nothing to read.

No Must stands. Four Shoulds, ranked.

## Shoulds

1. **The stylesheet still carries the sentence this ticket retired.** `src/app/globals.css:75`
   reads `  /* hero gradient — marketing surfaces + 100%-meter shimmer only */`, the §2 line
   copied into the token layer comment and all — the lines around it are §2's verbatim. AC3 says
   "100%-meter shimmer appears nowhere"; TC3 scopes that to `docs/design-spec.md`, and the
   ticket's Rules forbid touching anything else, so the run was right not to edit it. But the two
   copies now say different things about when the shimmer fires, C-05's test strips comments and
   will never see it, and "two copies both claiming truth" is the thing this board exists to
   prevent (guidelines §1). *Smallest correction:* a follow-up task at Backlog that makes
   `globals.css:75` read the new comment, or one line in this ticket's open questions naming the
   drift and its owner — an unrecorded stopgap is a bug (AGENTS.md).

2. **§2's usage map holds a second line contradicting §8.23, and the objective calls the comment
   "the one".** `docs/design-spec.md:159` reads
   `| Passed checks, handover-ready, meter at 100, diff additions | `--success` (soft bg for diffs) |`.
   §8.23 says the fill switches to `--success` "when the item is handover-ready — every Must
   passed or accepted — whatever the percentage reads", so "meter at 100" as its own trigger says
   a 100% meter with an open Must is `--success`, which §8.23 denies — the same shape of conflict,
   twelve lines below the one this ticket settled. Out of scope by the Rules, and correctly left
   alone. *Smallest correction:* next version drops `meter at 100` from that row, or writes it
   `handover-ready (whatever the meter reads)`.

3. **TC4 cannot pass as written, and the check that would measure AC4 is a different command.**
   `docs/tickets/T0.29.md:66` reads "`git diff --stat` names one file". On this branch it names
   two, and must: `docs/tickets/T0.29.md` is the pack guidelines §5 step 2 requires, and at close
   the branch also carries `docs/reports/`, `docs/log/`, `docs/build-log.md` and this file. AC4's
   substance holds — the spec change is confined to one file, five lines changed and one added —
   but the test as phrased measures something no ticket on this board can satisfy. *Smallest
   correction:* `git diff origin/main...HEAD --stat -- docs/design-spec.md`, with the AC read as
   "one file outside the ticket's own documents".

4. **TC2's second half names a script the checkout does not hold.**
   `docs/tickets/T0.29.md:62-63`: "a script over the file finds no undeclared custom property and
   no unresolved section reference" — C-03 and C-04. `C-01`, `C-02`, `C-03` and `C-04` appear
   nowhere in this repository but `docs/design-spec.md` and this ticket; nothing under `scripts/`
   implements them, so AC2's C-03 and C-04 half is carried by hand or not at all. I carried it by
   hand above. Worth recording alongside it: C-03 exempts the changelog comment from the
   custom-property read and C-04 says nothing either way, while TC2's `sed '1,/-->/d'` deletes the
   whole comment before the C-02 grep — so the new line's twenty-odd section references sit
   outside every check that actually runs. *Smallest correction:* say in the report which of C-03
   and C-04 was checked by hand, or file the script as its own task.

PASS
