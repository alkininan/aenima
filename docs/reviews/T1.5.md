# T1.5 — review

Reviewed commit `3e5f6c6` (branch `t1-5`), pass 1. `main` and `origin/main` are both `f4efa56` and
both are ancestors of `HEAD`. I read `docs/tickets/T1.5.md`, the sections it cites at the repo's
versions (product-spec §13, design-spec §10 and §11, build-log open question 6), every hunk of
`git diff main...HEAD` (14 files), and the report as a claim to check.

Scope and runs, done here:

    node scripts/run/review-scope.mjs docs/tickets/T1.5.md    -> 4 files, missing: []
    pnpm vitest run src/app/app/ItemRow.dom.test.tsx src/app/app/RowWalker.dom.test.tsx \
      src/db/queries/item.test.ts                             -> 3 files, 41 passed
    pnpm vitest run e2e/list.spec.ts                          -> no vitest project includes it
    PLAYWRIGHT_BASE_URL=http://localhost:3457 pnpm exec playwright test e2e/list.spec.ts \
      --project chromium                                      -> 23 passed, both new cases among them

TA1: `git diff --name-only origin/main...HEAD` names nothing under `drizzle/` or `src/db/schema/`,
and not `docs/schema.md` or `src/db/database.types.ts`; `migration-check.mjs` reads
`waiting: false`. TA2: `origin/main` is an ancestor of `HEAD`, and the runs above are on that tree.
TA3: the `+`/`-` lines of `git diff -U0 origin/main...HEAD -- src e2e` contain no match for `park`,
in any case.

**What the diff does matches the ticket.**

- AC3: `RowWalker.tsx:362-389` acts only when focus is on an element with `data-row-link`, and
  leaves alone events that are already handled or carry a modifier. It walks with
  `nextRovingIndex`, which returns null for any key other than ArrowUp/ArrowDown/Home/End
  (`src/lib/roving.ts`). It wraps the three buckets on `/app` (`page.tsx:419`), so the walk crosses
  buckets. The rows stay Server Components. `row-link.ts` has no directive, and I checked the
  server/client split: rendering the server-rooted `/dev/list` puts `data-row-link=""` on all four
  row links.
- AC4: `ITEM_ROW` embeds `scoring_run(scored_at, artifact(next_scoring_attempt_at))`, sorted
  newest-first and capped at 1 inside the embed. The list query itself has no cap, so open
  question 7 still holds (`item.test.ts` asserts `calls.limit` is `[]`). The mapper picks the newest
  run, and the retry comes from that run's own artifact, as in `getLatestRunForItem`. The row uses
  the same `t.item.scoredAt` / `t.item.scoredRetrying` strings and the same `bg-warning`/`bg-prime`
  dot as `ReadinessPanel.tsx:209-217`, and never red. It shows a retry only next to a run.
  `scoring_run_item_fk` is `(workspace_id, item_id)`, so every embedded run belongs to the item's
  workspace. I did not test the embed against PostgREST myself. That rests on the report's probe
  (open question 2).
- Criteria and Tests map both ways: AC3-TC3, AC4-TC4, AA1-TA1, AA2-TA2, AA3-TA3. Every new test has
  a row in the red-first table, with no empty cells.
- The build-log open question 6 hunk says what the diff does. No mutation, no new string, no
  hardcoded token.

## Shoulds

1. **Two red-first rows prove the import, not the assertion.** `docs/reports/T1.5.md:74` and `:77`
   record "RowWalker › renders one link per row, in bucket order" and "RowWalker › leaves other keys
   alone" as reddened by "`RowWalker.tsx` moved out of the tree — the import fails to resolve", with
   "no test ran". That shows the file needs the module, not that either assertion can fail.
   "leaves other keys alone" (`RowWalker.dom.test.tsx:306-314`) would pass with no walker at all.
   Fix: redden it with a change aimed at the claim, for example the walker treating `j` as
   ArrowDown (expect `1 failed / 4 passed`). For the bucket-order test, reorder the harness's
   buckets. Record both counts.
2. **A test name and a comment are now false for the fixture this diff changed.** The fixture now
   has two scored rows (`list-fixture.ts`: `soc-12`, `soc-4`), but `e2e/list.spec.ts:156` is still
   named "a row renders no meter at all while nothing is scored". `ItemRow.tsx:67-69` still says
   "Nothing is scored until Phase 2 … The meters come back with the scores". Fix: rename the test
   and reword the comment to say what is true now. Out of scope: design-spec §10 says row meters
   "appear when the scores do", and scores exist now, but no ticket or open question owns row
   meters. This belongs in a Backlog task, not in this diff.
3. **A count in a comment is out of date.** `src/db/queries/item.ts:234` reads "Three differences
   from `ITEM_TREE`, each one paying for a bucket rule". The diff adds a fourth embed
   (`scoring_run`, documented at `:257-267`), and it pays for the row's readout, not a bucket rule.
   Fix: "Four differences", and say what the last one is for.
4. **The walk is only tested in a browser on the client-rooted preview.** `e2e/list.spec.ts:216`
   runs on `/dev/primitives`, where `Composites` is `"use client"`. The layout `/app` actually has —
   Server Component rows inside the client walker — is never walked. That is the exact difference
   `/dev/list` exists to test, after the production 500. I checked the attribute on the server side
   (Should-level only). Fix: wrap `/dev/list`'s buckets in `RowWalker` as `page.tsx` does, and point
   the walk case at `/dev/list`, or add a second case there.
5. **The report claims close-time work that is not done yet.** `docs/reports/T1.5.md:46` lists
   `docs/log/T1.5.md` as added, and `:50` says "Tickets done regenerated". Neither is in the tree at
   `3e5f6c6`, and `node scripts/run/report-check.mjs docs/reports/T1.5.md` returns `ok: false`
   ("no table under Reviewer passes"). This is step 8 work, so it is a reminder, not a defect: all
   three need to be true before close.

PASS
