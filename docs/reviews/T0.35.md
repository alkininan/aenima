# T0.35 — review

Commit `75fe61c` · `git diff origin/main...HEAD` — `docs/product-spec.md` (5 hunks) and
`docs/tickets/T0.35.md` (added). Nothing else in the diff.

## What was checked

Read the ticket and every section its `Spec` line cites at v1.8 — §9, §17, Appendix D, §8, §12 —
then every hunk of the diff.

Criteria ↔ Tests maps both ways: AC1–AC7 each have a TC, TC1–TC7 each name their AC, no unmatched
line on either side.

`node scripts/run/review-scope.mjs docs/tickets/T0.35.md` returns
`{"files":[],"named":[],"touched":[],"missing":[]}` — no vitest file is named or touched, so no
vitest run applies. This is a Content ticket verified by greps, T0.19's precedent. I ran them
against the checkout rather than reading the claim:

```
AC1  awk '/^## 9\./,/^## 10\./' docs/product-spec.md | grep -c 'CLAUDE.md. mirror'            → 0
AC1  … | grep -cE '^\*\*Delivery\.\*\* aenima never rewrites, moves or appends'               → 1
AC2  … | grep -cE 'Report back as: acceptance criteria implemented, each with its test · …'   → 1
AC3  … | grep -cE 'A \*\*departure\*\* is a place the build left the pack, with its reason'   → 1
AC4  grep -c 'No mirror — delivery follows §9.s rules\.' docs/product-spec.md                 → 1
AC5  grep -c 'hard-enforce the ticket fence in Claude Code against the files the builder …'   → 1
AC6  grep -cE '^# aenima — Product Specification v1\.9$|^v1\.9: .*\(§9, Appendix D\).*\(§17\)\. -->$'  → 2
AC7  git diff origin/main...HEAD -- docs/product-spec.md | grep -cE '^[-+]## '                → 0
AC7  hunk addresses: @@ -1,4 +1,4 @@ · @@ -8,7 +8,8 @@ · @@ -251,15 +252,17 @@ ·
     @@ -369,7 +372,7 @@ · @@ -462,7 +465,7 @@ — the title, the header comment, §9,
     §17, Appendix D, and nothing else
```

The Delivery paragraph (`:257`), the Layer 3 preamble's last sentence (`:263`), the §17 clause
(`:375`) and Appendix D's line (`:468`) carry the ticket's prescribed words verbatim. The header
comment closes on the v1.9 line — `-->` moved off the v1.8 line, T0.19's first-round defect is
not repeated.

Red-first record: `docs/reports/T0.35.md` does not exist yet, and by guidelines §5 the report is
step 8, after this review — so there is nothing to check at this point and its absence is not a
finding. When it is written it owes one row per TC1–TC7 with the mutation that reddened each grep
and the count that went green; `scripts/run/report-check.mjs` refuses it without them.

## Findings

1. **Should** — `docs/product-spec.md:462`, inside Appendix D's constitution skeleton:
   `All listed checks pass. Report: ACs implemented, tests written, open questions.` That is the
   three-entry report-back §9 `:263` just replaced with six. An emitted pack would then carry two
   report-back instructions that disagree — an instruction "loaded twice is context spent twice,
   and two copies drift apart" is the rule this same ruling writes at `:257` — and the shape §12's
   code parses would be the one the constitution contradicts. The diff is right not to have touched
   it: Build step 4 and the Rules fence Appendix D's edit to the mirror sentence alone, and AC7
   holds the rest of the file still. The correction belongs outside this diff — an open question in
   the report plus a Backlog Fix task (guidelines §5 step 5) — and is one sentence when it is cut:
   `All listed checks pass. Report back per §9's report-back.`

2. **Should** — `docs/product-spec.md:255` and `:445`. The first paragraph still reads "Emitted
   as … with the constitution as `AGENTS.md`", and Appendix D's heading still reads
   "(root AGENTS.md, per product)". Both name the channel at emission, two lines above a rule that
   says the channel "is chosen at emission from what the repository already carries … measured,
   never assumed". Build step 1 kept "as `AGENTS.md`" deliberately — the constitution is written as
   AGENTS.md content — so this is not a departure, but T5.5 reads both lines and has to decide which
   governs. "with the constitution in `AGENTS.md` form" and "(constitution content, per product)"
   would leave only one reading.

3. **Should** — `docs/product-spec.md:257`, the first of the three delivery rules. AC1 asks for
   "the three delivery rules, each with its reason". Rules two and three carry theirs — "an
   instruction file loaded twice is context spent twice, and two copies drift apart", "because
   agents change which instruction files they load from release to release". The first carries only
   its scope: "an instruction file the team owns — their `CLAUDE.md`, their `AGENTS.md`, their
   agent's own rules". I read ownership as the reason and AC1 as met, which is why this is not a
   Must; the ticket prescribed these words verbatim, so the diff did not depart from anything. Half
   a clause would close the reading — the file is theirs, and a tool that edits it hands them a
   review they did not ask for.

No Must stands: the five edits are the five the ticket names, in the ticket's words, every
criterion has a green check I ran myself, and nothing in the diff is unaccounted for.

PASS
