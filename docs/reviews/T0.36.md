# T0.36 — review

Commit `a2584be` (review pass 4, the addendum round) · `git diff origin/main...HEAD`
(origin/main at `ede2da8`, T3.2's merge, which is HEAD's second parent and the merge base) —
`scripts/run/premerge.mjs` (added, 234 lines), `scripts/run/premerge.test.mjs` (added, 356
lines), `.claude/skills/ticket/SKILL.md` (2 hunks: step 0's `merge` shape, step 9),
`docs/guidelines.md` (3 hunks: header v1.24, §5 step 0, §5 step 9), `scripts/run/README.md`
(2 hunks), `scripts/run/next-id.test.mjs` (1 hunk: `toBe("1.20")` loosened to `>= 1.20`),
`docs/build-log.md` (generated: Specs line v1.20 → v1.24, Newest entry, T0.36's list line),
`docs/log/T0.36.md` (added), `docs/reports/T0.36.md` (added), `docs/reviews/T0.36.md` (this
file), `docs/tickets/T0.36.md` (added, with the addendum). Nothing else.

## What was checked

Read the ticket, §5 steps 0 and 9 of `docs/guidelines.md` at v1.20 (`origin/main`, the version
the `Spec` line cites) and at v1.24 (this branch), and every hunk of the diff. Read
`scripts/run/log-index.mjs` for the six names `premerge.mjs` imports — `BUILD_LOG`,
`CURRENT_HEADING`, `HEADING`, `LOG_DIR`, `generate`, `replaceSection` are all exported;
`replaceSection` throws on a missing heading, which is what `mergeWritten`'s `catch` at
`premerge.mjs:83` leans on — and `scripts/run/cli.mjs` for `emit` and `isMain`. `branch.mjs`,
`route-types.mjs`, `conflicts.mjs` and `gated.mjs`, which the new step 0 and step 9 text name,
are all in the checkout.

**Since pass 3's PASS at `65470d1`, no code changed.** `git diff 65470d1 HEAD` outside what the
merge brought in from main is `docs/build-log.md` (regenerated), `docs/log/T0.36.md`,
`docs/reports/T0.36.md`, `docs/reviews/T0.36.md` and the ticket's addendum section. The
addendum's premise — merging main in changes nothing of this ticket's — holds on the diff.

**The branch's own merge, as the ticket's step would make it.** `a2584be` is a merge commit
with two parents (`896f969`, `ede2da8`), git's `--no-edit` message, and no rebase. `git
merge-tree --write-tree 896f969 ede2da8` exits 0: the two regenerated build logs joined
cleanly on their own, because main's T3.2 line landed mid-list and this branch's T0.36 line
and Specs/Newest lines sat in other hunks — so `premerge.mjs` at this commit answered its
`merged: true, resolved: []` path, not the resolution path. `node scripts/run/log-index.mjs
--check` on HEAD says `stale: false`: the build log equals a fresh regeneration and holds both
T3.2's and T0.36's entries, which is what AC1 asks of the tree that lands. The resolution path
is exercised by the fixture (below), and, at pass 3, against `origin/t0-26`, `t0-30`, `t0-34`.

**Criteria ↔ Tests, both directions.** AC1 ↔ TC1: `premerge.test.mjs:100` "is the conflict
this ticket exists for" asserts the ticket's *red without the step* on the fixture; "merges
main in and rebuilds the list from both sides' entries" (two parents, clean tree); "leaves the
build log equal to a fresh generation, holding both tickets" (`toBe(regenerated())`, both
entries present, the hand-written passage kept). AC2 ↔ TC2: "refuses a conflict in a file
nothing regenerates, and pushes nothing" — `files` and `why` name `notes.md`, HEAD unchanged,
tree clean; "refuses a build log whose conflict is outside the generated sections" covers the
ticket's "confined to" clause. AC3 ↔ TC3: "the protocol carries the step" — `premerge.mjs` in
§5's step 0 row and step 9 row, header version past 1.20, the skill naming the command on
both sides of `## 9 Close`. The `mergeWritten` block carries no TC label; it is unit coverage
of the function TC1 and TC2 exercise end to end. The `next-id.test.mjs` hunk is what AC3
forced. Every criterion has tests; every test names a criterion or is accounted for.

**AC3 on the checkout.** Header reads v1.24; `premerge.mjs` sits in the step 0 row and the
step 9 row of §5. Every open remote branch's header, read with `git show <branch>:docs/
guidelines.md`: `t0-26` and `t0-30` v1.21, `t0-34` v1.23, `t3-2` and `main` v1.20, the rest
older. Nothing but `t0-36` holds v1.24. The header's sentence "past v1.21 and v1.23, which
branches still open are holding" is true.

**The ticket's rules.** `merge --no-ff --no-edit` at `premerge.mjs:171`, never a rebase. The
generated sections are regenerated by `regenerateAt` (`:113-116`), never hand-resolved; the
written passages go to `git merge-file` (`:99-109`) in the order the command wants — ours,
base, theirs — and the index stages are read in the right order (`:2` ours, `:3` theirs, `:1`
base, `:200-204`).

**Red-first record**, `docs/reports/T0.36.md` "Tests written, each observed red first": one
row per mutation, with counts, covering 17 of the 21 tests in `premerge.test.mjs`, the
rewritten `next-id.test.mjs` assertion, and the three pre-existing tests the change reddened.
Four tests have no row that reddens them — Should 1.

`node scripts/run/review-scope.mjs docs/tickets/T0.36.md` →
`files: [scripts/run/next-id.test.mjs, scripts/run/premerge.test.mjs]`, `missing: []`.
`pnpm vitest run` on those two: 2 files, 48 tests, all green.

HEAD is two commits past `origin/t0-36` (`ae16892`): the addendum commit and the merge are
local, which is where step 5 stands before step 9 pushes. Not a finding.

## Findings

No Must stands. Shoulds, for the run to record:

1. **Should** — Four tests have no red on record. The table's first row is honest that
   under the inert module "7 failed / 4 passed", but it does not name the four or say what
   would redden them, so each is a test nobody has seen fail:
   - `scripts/run/premerge.test.mjs:100` "is the conflict this ticket exists for" — a
     fixture assertion; no mutation of `premerge.mjs` reddens it, and its row should say so
     (its red is a fixture that stops conflicting).
   - `:253` "refuses a dirty checkout rather than merging over it" — reddened by removing
     the refusal at `premerge.mjs:144` (`if (out(g(["status", "--porcelain"])) !== "")`).
   - `:273` "reads a difference in the written part, with no base to judge it by, as a real
     one" — reddened by removing `premerge.mjs:88` (`if (b === null) return null;`).
   - `:304` "reads a file missing its headings as one it cannot settle" — reddened by
     removing the `try`/`catch` at `premerge.mjs:81-85`, which then throws.
   Smallest correction: one row each, with the line removed and the count.

2. **Should** — The report is a pass behind. `docs/reports/T0.36.md:48` "Reviewer passes and
   findings" holds three rows and this is the fourth pass; `:81` "How this ticket closed" says
   the gate is red on main and the ticket waits at Review unmerged; the first open question
   says the suite is red on main. Main now carries T3.2's merge (`ede2da8`) and this branch
   carries it since `a2584be`, which is what the addendum says lands the ticket. Step 8 of
   this round rewrites those three places. (`896f969`'s message, "main taken in, the gate
   rerun, the ticket landed", was written before the merge commit after it existed; a commit
   message is not a record, so this is only a note.)

3. **Should** — `docs/tickets/T0.36.md:21` AC2 says a refused conflict "pushes nothing";
   `.claude/skills/ticket/SKILL.md:455` step 9 says "**every exit pushes**". Both are right
   under one reading — the script pushes nothing and the refused merge is never pushed, while
   step 9's push is the ticket's own commits, which pass 1's Must 2 required so the board's
   Commit names a commit that can be fetched — but the two sentences read against each other
   cold. Smallest correction: one sentence in the report's AC2 paragraph
   (`docs/reports/T0.36.md:19`) saying which push each means.

4. **Should** — Pass 3's Shoulds 3 and 4 still stand unchanged and are filed as the Backlog
   task the report names (*Read premerge's fetch and name its files*): `premerge.mjs:135` reads
   the fetch's status with nothing, and `:194` returns every conflicted path as `files` so the
   refusal names `docs/build-log.md` beside the file actually in the way. Recorded here so the
   run does not re-derive them.

PASS
