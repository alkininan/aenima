# T0.36 — review

Commit `65470d1` (review pass 3) · `git diff origin/main...HEAD` (origin/main at `dbc6f0d`, the
branch's merge base) — `scripts/run/premerge.mjs` (added, 234 lines),
`scripts/run/premerge.test.mjs` (added, 356 lines), `.claude/skills/ticket/SKILL.md` (2 hunks:
step 0's `merge` shape, step 9), `docs/guidelines.md` (3 hunks: header v1.24, §5 step 0, §5
step 9), `scripts/run/README.md` (2 hunks), `scripts/run/next-id.test.mjs` (1 hunk: the pinned
`1.20` loosened to `>= 1.20`), `docs/build-log.md` (the generated Specs line, v1.20 → v1.24),
`docs/reviews/T0.36.md` (passes 1 and 2), `docs/tickets/T0.36.md` (added). Nothing else.

## What was checked

Read the ticket and §5 steps 0 and 9 of `docs/guidelines.md` at v1.20 (`origin/main`, the
version the ticket cites) and at v1.24 (this branch), then every hunk of the diff, then
`scripts/run/log-index.mjs` (`replaceSection`, `generate`) which `premerge.mjs` leans on,
`scripts/run/branch.mjs` which the new step 0 text leans on, and the guard's rule (f).

**Criteria ↔ Tests**, both directions. AC1 ↔ TC1: `premerge.test.mjs` "is the conflict this
ticket exists for" (the ticket's *red without the step*, asserted on the fixture), "merges main
in and rebuilds the list" (a merge commit with two parents, clean tree), "leaves the build log
equal to a fresh generation, holding both tickets". AC2 ↔ TC2: "refuses a conflict in a file
nothing regenerates, and pushes nothing" — `files` and `why` name `notes.md`, HEAD unchanged,
tree clean; "refuses a build log whose conflict is outside the generated sections" covers the
ticket's "confined to" clause. AC3 ↔ TC3: "the protocol carries the step" — `premerge.mjs`
under §5 step 0 and step 9, header version past 1.20, the skill naming the command before and
after `## 9 Close`. The `mergeWritten` block and the `next-id.test.mjs` hunk carry no TC label
of their own; the first is unit coverage of the function TC1/TC2 exercise end to end, the
second is what AC3 forced — the old test pinned the header to `1.20` exactly. Both accounted.

**AC3 on the checkout**: header reads v1.24; `premerge.mjs` is named in the step 0 row and the
step 9 row of §5. Open branches hold v1.21 (`origin/t0-26`, `origin/t0-30`) and v1.23
(`origin/t0-34`); nothing holds v1.24, and the header's sentence saying so is true.

**Pass 2's Must 1 is settled.** `scripts/run/premerge.mjs:80-92` `mergeWritten` now answers
only what equality can answer and hands the rest to `git merge-file -p` over the three texts
with the generated bodies blanked (`mergeFile`, `:98-108`); `premerge` reads the index stages
in the right order (`:2` ours, `:3` theirs, `:1` base, `:198-201`). Not on the fixture only:
in a throwaway clone under the scratchpad, this branch's `premerge({ base: "origin/main" })`
run at `origin/t0-26`, `origin/t0-30` and `origin/t0-34` — the three branches the Objective
names — each answered `ok: true, merged: true, resolved: ["docs/build-log.md"]`;
`log-index.mjs --check` on each result said `stale: false`, and no `<<<<<<<` remained. The
test the fix owed ("merges written passages both sides moved, in places that do not overlap")
is in the file and asserts both passages survive and the file equals a fresh regeneration.
The comment pass 2 found wrong at `:166-168` now says what the test does.

Pass 2's Should 4 is settled: §5 step 0 now reads "where that moved the branch, gate the
result and push it", which is what the skill says.

`node scripts/run/review-scope.mjs docs/tickets/T0.36.md` →
`files: [scripts/run/next-id.test.mjs, scripts/run/premerge.test.mjs]`, `missing: []`.
`pnpm vitest run` on those two: 2 files, 48 tests, all green.

The guard's door for a merge word (`scripts/hooks/guard.mjs:1448-1482`, on `origin/main`)
reads the thread and the branch and pins nothing to HEAD; its reviewer's door pins the pull
request's head to the checkout's HEAD. Step 9's order as the skill writes it — premerge, push,
`gated.mjs`, gate on the pushed tree, merge from that commit — holds against both.
`branch.mjs` (`heldBy`, `git worktree remove`, refusing a dirty tree) does free a branch from
the worktree holding it, so step 0's new line names a script that can do what it says.

**Red-first record**: `docs/reports/T0.36.md` does not exist and the ticket's Report section
is empty. By §5 the report is step 8, after this review, so its absence is not a Must here —
but every test this diff adds is unverified at this pass (Should 1).

## Findings

No Must stands. Shoulds, for the run to record:

1. **Should** — Red-first record. The report at step 8 owes one row per test — the mutation
   that made it red and the count that went green — for the 21 tests in
   `scripts/run/premerge.test.mjs` and the rewritten `carries a version no older than the one
   this rule arrived in` at `scripts/run/next-id.test.mjs:268`. None has a row anywhere yet;
   the gate refuses a report without them (§5 step 8).

2. **Should** — What this diff does to the three pull requests the Objective names, for the
   report's open questions. Verified with `git merge-tree --write-tree --name-only` in the
   scratch clone: once this branch is main, `origin/t0-26`, `origin/t0-30` and `origin/t0-34`
   each conflict on `docs/guidelines.md` (the header every ticket prepends to on the same
   lines: v1.21, v1.21, v1.23 against this branch's v1.24) and on
   `scripts/run/next-id.test.mjs:265` (each pinned its own number on the line this diff
   loosens). `premerge` refuses those by name, within the ticket's rule, so each needs a round
   of its own; and a `merge` word acted on before this branch lands runs the old step 0 and is
   refused on the build log as before. Two things follow for the run: say in the report which
   order lands what, and file one Backlog Fix task (Type Fix, Epic E0.2) for the guidelines
   header, which is append-only on the same lines by every branch and behaves like a generated
   section that nothing regenerates.

3. **Should** — `scripts/run/premerge.mjs:135`:

       if (fetch) g(["fetch", "--quiet", "origin"]);

   The fetch's status is read by nothing. Offline, `origin/main` is whatever the last fetch
   left, the merge is made against it and reported `ok: true`, and the run finds out only
   when GitHub refuses the pull request. `branch.mjs` at least carries `fetched` in its
   result. Smallest correction: carry `fetched: fetched.status === 0` in the returned object
   so the skill's comment can say the base may be stale — or refuse outright when the fetch
   fails, since a merge against a base nobody read is the thing this script exists to
   prevent.

4. **Should** — `scripts/run/premerge.mjs:186-190`, the ungenerated refusal:

       return abort(
         `the conflict reaches ${ungenerated.join(", ")}, which nothing regenerates — …`,
       );

   `abort` returns `files` as every conflicted path, so with `notes.md` and the build log both
   conflicted the `refused` comment the skill posts from `files` names `docs/build-log.md`
   beside `notes.md`, and the human reads the build log as theirs to settle by hand when the
   next `merge` word's `premerge` would settle it itself. `why` already names only the
   ungenerated files. Smallest correction: return `files: ungenerated` on that path, keeping
   the full list under another key if the report wants it.

PASS
