# T0.36 — review

Commit `8e1fdc5` (review pass 2) · `git diff origin/main...HEAD` — `scripts/run/premerge.mjs`
(added), `scripts/run/premerge.test.mjs` (added), `.claude/skills/ticket/SKILL.md` (2 hunks),
`docs/guidelines.md` (3 hunks), `scripts/run/README.md` (2 hunks),
`scripts/run/next-id.test.mjs` (1 hunk), `docs/build-log.md` (generated Specs line),
`docs/reviews/T0.36.md` (pass 1's verdict), `docs/tickets/T0.36.md` (added). Nothing else.

## What was checked

Read the ticket and guidelines §5 steps 0 and 9 at v1.20 (`origin/main`, the version cited) and
at v1.24 (this branch), then every hunk of the diff.

Criteria ↔ Tests maps both ways: AC1↔TC1, AC2↔TC2, AC3↔TC3, each named in
`premerge.test.mjs`'s block comments. The `next-id.test.mjs` hunk is not named by the ticket
but is the consequence of AC3 — the old test pinned the header to `1.20` exactly — and is
accounted for. AC3 holds: the header reads v1.24; open branches hold v1.21 (`origin/t0-26`,
`origin/t0-30`) and v1.23 (`origin/t0-34`), none holds v1.24, and the header's sentence about
that is now true. §5 names `premerge.mjs` at step 0 and at step 9.

`node scripts/run/review-scope.mjs docs/tickets/T0.36.md` →
`files: [scripts/run/next-id.test.mjs, scripts/run/premerge.test.mjs]`, `missing: []`.
`pnpm vitest run` on those two: 2 files, 45 tests, all green.

Pass 1's Must 2 (every exit pushes) is settled at `SKILL.md` step 9; Shoulds 3, 4 and 6 are
settled. Pass 1's Must 1 is what finding 1 below is about: the merge base is read now, but it
is read as an equality, not as a merge, and the branch pass 1 named is still refused.

The guard's second door (`scripts/run/permission.mjs` `reviewed()`, on `origin/main`) reads the
verdict's last line, the gated diff, and the gate's fingerprint — HEAD plus the dirty state —
and pins the verdict to no commit, so step 9's order (premerge, push, gate on the new HEAD,
merge) holds together as the skill writes it. No git hooks are installed, so the script's
`git commit --no-edit` has nothing to fail on.

Red-first record: `docs/reports/T0.36.md` does not exist and the ticket's Report section is
empty. By §5 the report is step 8, after this review, so its absence is not a Must here — but
every one of the 18 tests in `premerge.test.mjs` and the rewritten one in `next-id.test.mjs`
is unverified at this pass (Should 2).

## Findings

1. **Must** — `scripts/run/premerge.mjs:80-84`, `sideToKeep`:

       if (o === t) return "theirs";
       if (b === null) return null;
       if (t === b) return "ours";
       if (o === b) return "theirs";
       return null;

   Three texts, compared for equality. When the branch wrote in one place of the build log's
   written passages and main wrote in another — no line in common, which git merges cleanly —
   none of the three equalities holds, the function answers `null`, and `:186-190` aborts with
   `why` reading "where both sides wrote different words". They did not write different words;
   they wrote in different places. AC1: "A branch whose only conflict with main is in generated
   files merges." Confirmed on the branch the Objective names, not on a fixture: in a scratch
   clone at `origin/t0-26`, `premerge({ base: "origin/main" })` answers
   `ok: false, files: ["docs/build-log.md"], why: "docs/build-log.md conflicts outside its
   generated sections, where both sides wrote different words …"`. Against `origin/main`,
   t0-26 conflicts on `docs/build-log.md` alone (`git merge-tree --write-tree --name-only
   origin/main origin/t0-26`), and git's markers sit at lines 88–95 of the merged file, inside
   "Tickets done" (30–97) — its only conflict is in a generated section. What moved on main's
   side is T0.33's `c475c21` ("standing rules load by path"), which changed the written
   passages after t0-26 branched; main's written passages moved in 3 of the last 14 commits
   touching the build log (T0.33 once, T1.4 twice), so a waiting branch that wrote a decision
   meets this regularly. The comment at `scripts/run/premerge.test.mjs:166-168` — "Both sides
   differ outside the generated sections, but only one of them moved from the base — which is
   the shape origin/t0-26 is actually in, and AC1 says it merges" — is wrong on both counts:
   both sides moved, and it does not merge. Smallest correction: replace the equalities with a
   real three-way merge of the stripped texts — write `withoutGenerated(ours)`,
   `withoutGenerated(base)`, `withoutGenerated(theirs)` to three temp files and run
   `git merge-file -p -L ours -L base -L theirs <ours> <base> <theirs>`; exit 0 → write its
   output to `docs/build-log.md` and call `regenerate` as now (the blanked generated bodies
   are what `generate` fills); exit > 0 → refuse as now, naming the file. With no base keep
   the two-way equality. Verified on t0-26's three texts: `merge-file` exits 0 with no
   markers. Add one test beside "keeps prose main added that the branch never touched": the
   branch and main each append a passage to a different written section, `premerge` is
   `ok: true`, both passages are in the file, and the file equals a fresh regeneration; and
   fix the comment at `:166-168`.

2. **Should** — Red-first record. The report at step 8 owes one row per test — the mutation
   that made it red and the count that went green — for all 18 tests in
   `scripts/run/premerge.test.mjs` and the rewritten `carries a version no older than the one
   this rule arrived in` in `scripts/run/next-id.test.mjs:268`; none has a row anywhere yet.
   The first test in the file (`is the conflict this ticket exists for`) is the ticket's own
   "red without the step" and needs its row like the rest.

3. **Should** — What this diff does to the three pull requests the Objective names, for the
   report's open questions. Today, against `origin/main`, `premerge` settles `origin/t0-30`
   and `origin/t0-34` (verified in the scratch clone: `ok: true, merged: true, resolved:
   ["docs/build-log.md"]`), so "T0.30's is next" comes true only if its `merge` word is acted
   on before this branch lands. Once this branch is main, `git merge-tree --write-tree HEAD
   origin/t0-26`, `…t0-30`, `…t0-34` each conflict on `docs/guidelines.md` (the header every
   ticket prepends to on the same lines) and `scripts/run/next-id.test.mjs` (the pinned line
   each of them also changed, which this diff changes too) — refused by name, within the
   ticket's rule, and each needs a round of its own. The guidelines header behaves like a
   generated section — append-only, every branch on the same lines — and may deserve a
   Backlog Fix task.

4. **Should** — `docs/guidelines.md:441-443`, §5 step 0: "take main into it (premerge.mjs),
   gate, push, gh pr merge --merge" reads as unconditional; `.claude/skills/ticket/SKILL.md:114`
   says on `merged: false` "push nothing, gate nothing", and on `ok: false` nothing merges.
   The table is a summary, but "gate and push when main came in" is one clause and would keep
   the two texts saying the same thing.

FINDINGS
