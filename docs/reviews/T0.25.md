# T0.25 — review

Reviewed commit `7368562` (branch `t0-25`, pass 1, cold). I read `docs/tickets/T0.25.md` and the
reading its Spec line names — `scripts/run/review-scope.mjs`, `scripts/run/report-check.mjs`,
`scripts/run/loosening.mjs`'s corpus rule, `docs/reports/T1.4.md` open question 6 and pass 6's
Should 3, `docs/reviews/T1.4.md`, `docs/tickets/T2.9.md`'s Addendum, and guidelines §5 steps 5
and 8 as inlined — and every hunk of `git diff origin/main...HEAD`: seven files, 663 insertions,
24 deletions. Local `main` and `origin/main` differ in this worktree and the diff against
`origin/main` is exactly this ticket's, which is the defect the ticket is about.

`node scripts/run/review-scope.mjs docs/tickets/T0.25.md` lists two files and calls nothing
`missing`. I ran them:

    Test Files  2 passed (2)
         Tests  28 passed (28)

**AC1.** `review-scope.mjs:25` exports `BASE = "origin/main"`; `touchedTests`, `reviewScope` and
the CLI's `base ?? BASE` all default to it, and `report-check.mjs:29,174` imports the same
constant rather than a second literal. TC1 stands in both test files over a real repository whose
`refs/remotes/origin/main` is one commit past local `main`: the CLI reads `mine.test.mjs` and not
`other.test.mjs`, the check accepts a report naming only the branch's test, and the library
default is asserted for callers that pass no base. Under the old literal the touched list would
hold `other.test.mjs` and both tests would go red.

**AC2.** `SEGMENT` (line 35) reads `[key]`, `[...rest]` and `[[...slug]]` whole. TC2 holds
T1.4's four bracketed paths and both catch-all shapes, and `missing` is empty over T1.4's Tests
section when every cited file is on disk. The fixture's paths match `docs/tickets/T1.4.md`'s
Tests section line for line; only the prose after each is abridged.

**AC3.** `PATH_LIKE` (line 46) now requires at least one directory, so `item-key.db.test.ts` and
`item.test.ts` are read as prose. TC3 asserts both absent from `named`. The comment at lines
38–44 records the convention and its one blind spot (a precedent written as a full path is a test
too many). No test file in the checkout sits at the repo root, so the rule loses nothing here.

**AC4.** `addendumSections` reads every `## Addendum…` section, and `namedTests` unions it with
`## Tests`. TC4 holds T2.9's shape — `## Addendum` and `## Addendum — renumber (2026-09-20)` — and
asserts the `## Cited` block after them is not read. Every Addendum heading under `docs/tickets/`
is one of those two forms; T0.20's is an `###` inside Cited and is rightly not matched.

**Criteria ↔ tests, both directions.** AC1 → TC1 (two tests in `review-scope.test.mjs`, one in
`report-check.test.mjs`); AC2 → TC2 (two tests); AC3 → TC3 (one); AC4 → TC4 (two). Every new
describe block names its TC → AC in a leading comment. No orphan on either side.

**Unnamed by the ticket, accounted for by it.** `.claude/agents/reviewer.md` steps 2 and 5 now
say `origin/main...HEAD` and "Tests and Addendum sections", and `scripts/run/README.md` follows.
The ticket names neither file, but the Objective's defect — a ref a worktree never moves — is the
one the reviewer's own step 2 carried (`docs/reviews/T1.4.md:7` diffed `main...HEAD` and had to
say the two refs happened to agree). Not scope creep; the report's "changed" section should
name it.

Shoulds:

1. **Should** — Red-first record. `docs/reports/T0.25.md` does not exist at `7368562`, so none of
   the eight new tests — seven in `scripts/run/review-scope.test.mjs`, one in
   `scripts/run/report-check.test.mjs` — has a row naming the mutation that reddened it. All eight
   are unverified at this pass. Step 8 comes after this review, so this is a record the report
   must carry rather than a defect in the diff: one row per test, and the reviewer-pass table with
   this pass's model.
2. **Should** — `scripts/hooks/guard.mjs:1017`: "`merge-base` is the one the reviewer's own
   `main...HEAD` diff rests on" is stale once `.claude/agents/reviewer.md:24` says
   `origin/main...HEAD`. One word in a comment; no rule changes.
3. **Should** — The Build section says "remove that workaround from the skill". The skill holds
   none: `.claude/skills/ticket/SKILL.md:327` and `:399` invoke both scripts with no base, so the
   hand-passed `origin/main` lived in the runs' transcripts, not the skill. The report should say
   so rather than leave the line reading as undone. While there, `SKILL.md:326` — "the tests the
   ticket names" — could say "Tests and Addendum sections" as `reviewer.md:40` now does.

No Must stands.

PASS
