# T0.18 — review

Reviewed at `c5ae54f` (branch `t0-18`), pass 2, diff read as `git diff origin/main...HEAD` with
`origin/main` at `11c946a`. The ticket cites no Spec line; I read CLAUDE.md, AGENTS.md and the two
guidelines sections it inlines at v1.17. `review-scope.mjs` listed three files, `missing` empty;
all three run green here: 162 passed (`guard.test.mjs`, `hooks.test.mjs`, `loosening.test.mjs`).

What holds. AC1 maps to TC1 and every new test is tagged `T0.18 TC1 → AC1`; the report's *Tests
written* table has a row for each of the sixteen tests in `guard.test.mjs`'s T0.18 describe, the
two in `hooks.test.mjs` and the two changed in `loosening.test.mjs`, each with the mutation that
reddened it and the count that went green. Pass 1's two Musts are settled in the diff: the report
is on file, and `./ready.sh`, `cd sub && node post.mjs`, `node - < post.mjs`, `cat post.mjs | node`
and any-case host and method are each refused, with a test red first (batch B). Probing `decide`
beyond the tests: `pnpm node`, `npx tsx`, `pnpm exec node`, `env NOTION_TOKEN=x node`, a shell
script that runs a shell script that runs a node script, `bash -s < ready.sh`, `curl -T`,
`curl -XDELETE`, a `$PAGE` in the URL, `--eval=`, a `<<<` here-string and Python's `requests.post`
and `Request(method="PATCH")` are all refused; `curl -X GET -d`, a query and a search go through.
The hook group in `.claude/settings.json` carries the Bash group's command byte for byte, and
`hooks.test.mjs` proves both the three new tools and the byte-for-byte comparison against the
extracted `scripts/` from the real command. `gated.mjs` reads this diff as gated (it edits
`loosening.mjs`), so it waits for `merge` from the human as §4 says. Nothing in the diff is
unaccounted for by the ticket or pass 1's findings.

## Findings

1. **Should — a bundled `-c` carries a write past rule (i), and every other rule too.**
   `bash -lc 'curl -X PATCH https://api.notion.com/v1/pages/p'`, `-ec` and `-xc` are allowed:
   `parse()`'s unwrapping of `sh -c "…"` (`scripts/hooks/guard.mjs:462`, `args.indexOf("-c")`)
   reads only a bare `-c`, so `shellRun` takes the command string as a script name no file holds.
   Older than this ticket — `bash -lc 'git push --force origin main'` and `bash -lc 'pnpm db:push'`
   pass rules (d) and (a) the same way on main today — so it is not this diff's to fix. Fix: in
   `parse()`, read a short bundle whose letters include `c` as `-c`; one test. It leaks every rule,
   so it merits a Fix of its own at Backlog rather than a line in *Guard the token's remaining
   routes*.

2. **Should — `node -pe '…'` is allowed.** `codeRun` (`scripts/hooks/guard.mjs`, the loop over
   `INLINE_CODE`) matches `-e` and `-p` as whole tokens, so the bundle `-pe` is skipped as an
   unknown flag and the code string is read as a script path no file holds; `node -p '…'` and
   `node -e '…'` are refused. Fix: read a short bundle containing `e` or `p` as inline code. One
   line in the Backlog task from pass 1's Should 6.

3. **Should — open question 3 in `docs/reports/T0.18.md:104` names `runs.mjs` alone.** On their
   text `scripts/hooks/guard.mjs`, `scripts/run/loosening.mjs` and `scripts/run/notion.mjs` match
   rule (i) as well, so a person's `node scripts/run/loosening.mjs` from a branch that edits it is
   refused the same way. The pipeline is not caught — the skill types `gated.mjs`, which imports
   `loosening.mjs`, and tests run under vitest, which rule (i) does not read — but the next
   detector ticket will meet it. Fix: name the four in that open question, or in §5's rule (i)
   sentence beside "a script `origin/main` carries, byte for byte, is the pipeline's".

4. **Should, unverified — `docs/guidelines.md` §5 now cites a Backlog task, *Guard the token's
   remaining routes*, that nothing in the repo shows.** The report says it was filed from pass 1's
   Should 6; I did not read the board, so I cannot say it exists. If it was not filed, the §5
   sentence points at nothing. Fix: confirm the row is on the board before close, and name its
   URL in the report's open questions.

PASS
