# T0.39 — Revert only on a wrong answer, never on silence

The deploy check read a site that never answered as a site that was down, and down reverts the merge
at main's tip. On 2026-09-24 the machine running the schedule could not look aeni.ma up, and only a
run's own judgement kept it from reverting a healthy merge. `health.mjs` now reads three outcomes —
up, down and unknown — and only down reverts; unknown records nothing and the next run asks again.

## ACs implemented

| AC | What was built | Its test |
|---|---|---|
| AC1 | `assess` keeps a silence (`status: null`) apart from a wrong status: `failed` holds wrong answers alone, `unanswered` the silences, and `outcome` is **unknown** when a check never answered and none answered wrongly. Unknown carries no revert signal — the old `ok` field, which the skill used to read `false` from, is gone, and the skill's step e reverts on `outcome: down` alone. The deploy-window wait reports `outcome: null`, since nothing was asked. | TC1 — `scripts/run/health.test.mjs`, "is unknown, never down, when a check never answered…", "is up when every check answered…", "reads a site that never answered as unknown…", "waits, asking and recording nothing…" |
| AC2 | A wrong status is **down** whether or not another check was silent; `health` records the commit, so one outage reverts one merge, and prints `failedText` for the Fix task. | TC2 — "is down on a wrong status, whether or not another check was silent", "records a failed commit too…" |
| AC3 | On unknown, `health` returns before writing `aenima-deploy-checked`, with a `why` saying the site could not be reached from this machine and is asked again next run. | TC3 — "reads a site that never answered as unknown, records nothing, and asks again next run" |
| AC4 | `.claude/skills/ticket/SKILL.md` step e branches on `outcome: up`, `outcome: unknown` and `outcome: down`; `docs/guidelines.md` §5's step-0 row and its *deploy check* paragraph name up, down and unknown and say only down reverts; v1.28 header line. `scripts/run/README.md` says the same. | TC4 — "the skill's step e reverts on down alone and asks again on unknown", "the guidelines' deploy check names up, down and unknown, and only down reverts" |

## Tests written, each observed red first

Before the build every new assertion was run against the old `health.mjs` and the old documents:
8 failed / 8 passed. Then, once green, each was reddened again by a named mutation:

| test | reddened by | red → green |
|---|---|---|
| `scripts/run/health.test.mjs` TC1 — `assess` is unknown, never down, on silence; `health` reads a mute site as unknown | `assess`'s `failed` filter back to `status !== expected`, so a silence counts as wrong | 3 failed / 12 passed → 15 passed |
| TC1 — up is up: every check answered as expected | `"up"` replaced with `"unknown"` as `assess`'s last outcome | 3 failed / 12 passed → 15 passed |
| TC1 — the wait reports no outcome | `outcome: null` removed from the waiting branch, so a probe that asked nothing reads as up | 1 failed / 14 passed → 15 passed |
| TC2 — a wrong status is down, and is recorded | `failed.length > 0` replaced with `false`, so down is never reached | 2 failed / 13 passed → 15 passed |
| TC3 — unknown records nothing and is asked again | the unknown early return made unreachable (`if (false)`), so the commit is recorded | 1 failed / 14 passed → 15 passed |
| TC4 — the guidelines state the rule | `Only down reverts.` replaced with `Down reverts.` | 1 failed / 14 passed → 15 passed |
| `scripts/run/claims.test.mjs` T0.34's TC7 — its header entry names §5 step 2, step 8 and §6 | the gate before this change: the test took the newest header entry as T0.34's, and v1.28 went above it (1 failed / 34 passed); after it, T0.34's `(T0.34)` renamed `(T0.33)` so no entry is found | 1 failed / 34 passed → 35 passed |
| TC4 — the skill states the rule | `so unknown never reverts` replaced with `so unknown reverts` | 1 failed / 14 passed → 15 passed |

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `0e20e9a` | fable | no | FINDINGS |
| 2 | `341832e` | fable | no | PASS |
| 3 | `81fbfc3` | fable | no | PASS |

Pass 1's three Musts, all fixed: the waiting branch returned `outcome: "up"` over a probe that asked
nothing — now `outcome: null`, with the waiting test asserting it; the clause added to §5's step-0
row sat before "files one Fix task…", so it read as unknown filing a Fix task — moved after "one
comment" and made to say unknown files nothing; and no red-first record existed — this report is it.
Its Shoulds, all taken: the duplicate "is ok" test merged into "is up"; `ok`, a derived duplicate of
`outcome` nothing read, removed in the same change (AGENTS.md); and `probe`'s comment no longer says
a silence "counts".

Pass 2 found no Must and four Shoulds, all taken after its PASS: a red-first row for "is up when
every check answered…" (`"up"` → `"unknown"`); a TC1 label on the waiting test; §5's step-0 row
rewrapped so no line runs past the block's width; and this section's claim that the ticket cited
the guidelines with no version, which it did not.

The close's gate then went red outside this ticket's own tests: T0.34's TC7 in
`scripts/run/claims.test.mjs` read the *newest* guidelines header entry as T0.34's, which held
only until the next version bump — this one. It now finds T0.34's entry by its ticket and checks
the same three section names in it. Pass 3 reviewed that change and passed it,
with four Shoulds. The row above is the fourth. The other three are recorded here and not built,
because a change after the third pass would go unreviewed:

- `assess` over an empty `results` with `changed: true` returns `"up"`; `health` overrides it to
  null on the wait, so no caller sees it, but the function's own comment promises null.
- The unknown `why` borrows `describeFailed`'s wording — "/app answered nothing rather than 307" —
  a shortfall phrasing in the line whose point is that silence is no shortfall.
- On down with a silent sibling, `failedText` names the wrong answer alone, so the Fix task and the
  `reverted` comment do not mention the check that never answered.

## Changed since this ticket was cut

Nothing. The ticket cites guidelines v1.27 §5, which is what the repo held at claim; this change
takes it to v1.28, numbered past v1.25, which `t0-26` still holds.

## Open questions

None. The three Shoulds above are small enough for a Fix task if you want them; none gives a
wrong revert.
