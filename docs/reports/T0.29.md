# T0.29 — Design spec v2.22

**Branch:** `t0-29` · **Commit:** `e22c317`
**Spec:** design-spec v2.21 → v2.22 · no drift at claim (`version-drift.mjs`: cited 2.21, repo 2.21)

The document moves to v2.22 on five replacements and one added line, in one file. Two of them
settle contradictions the check of v2.21 against v2.17 found; the added line records what
v2.19–v2.21 changed without one.

## ACs implemented

- **AC1** C-01 — the title, the last changelog line and the footer read v2.22; the comment holds
  one line per version, v2.0 through v2.22, and closes on its last. **TC1.**
- **AC2** C-02, C-03 and C-04 still hold. **TC2.**
- **AC3** "100%-meter shimmer" appears nowhere; the new §2 comment appears once. **TC3.**
- **AC4** The diff is the five replacements and the added line, in `docs/design-spec.md` alone.
  **TC4.**
- **AC5** C-39 reads the coverage across each face's subset files; `every shipped woff2` appears
  nowhere. **TC5.**

## Tests written, each observed red first

No test file is added or changed: `review-scope.mjs` lists no files and nothing `missing`. The
ticket's TC1–TC5 are greps over the changed document, and the record below is the count each one
gave before the change and after it, run on the working tree. The one suite that reads
`docs/design-spec.md`, `src/app/tokens.test.ts`, was run whole: 24 passed, unchanged either side —
its comparison strips CSS comments, so the §2 line is outside what it reads.

| test | reddened by | red → green |
|---|---|---|
| TC1 → AC1 · the three version strings and the closing arrow | the document at `origin/main`: title v2.21, footer v2.21 | title 0 / footer 0, arrows 1 → title 1 / footer 1, arrows 1 |
| TC2 → AC2 · the C-02 hedge grep, and C-03 and C-04 by script | not reddened — a regression guard, green on both sides; recorded as such rather than claimed as red-first | 0 → 0 (hedges); 0 unresolved references, 0 undeclared properties, both sides |
| TC3 → AC3 · the §2 comment | the document at `origin/main`, whose comment still read `100%-meter shimmer only` | old 1 / new 0 → old 0 / new 1 |
| TC4 → AC4 · the shape of the diff | no diff at all before the change — `git diff` was empty | 0 files → 1 file, 6 insertions / 5 deletions |
| TC5 → AC5 · C-39's coverage sentence | the document at `origin/main`, whose C-39 still read `every shipped woff2 covers ğĞşŞİıĳĲ` | old 1 / new 0 → old 0 / new 1 |
| `src/app/tokens.test.ts` (C-05 and C-39's type half) | not reddened — the suite that reads the changed document, run to show the change is invisible to it | 24 passed → 24 passed |

TC4 as the ticket words it — `git diff --stat` names one file — cannot pass on the branch, which
also carries `docs/tickets/T0.29.md` and this report. The command that measures what AC4 says is
`git diff origin/main...HEAD --stat -- docs/design-spec.md`, and that is the one the row records.

TC2's "script over the file" does not exist in the repository: C-01 to C-04 appear nowhere but
§17 and this ticket. C-03 and C-04 were checked by a throwaway script written for this run, which
resolves every `§n` and `§n.m` reference in the body against the headings the document carries and
every `` `--name` `` in prose against the declarations of §2, §3, §5 and §6. Both came back clean,
and clean again with the changelog comment read in — the new line's twenty-odd section references
all resolve, and the one undeclared property it turns up, `--r-s`, is v2.18's line and predates
this change. The missing script is filed; see *Open questions*.

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `e22c317` | opus | no | PASS |

Pass 1 was first called on `fable`, the model `.claude/agents/reviewer.md` pins, and came back
*You're out of usage credits* — a refusal, not a review, so it is not a row.
`review-model.mjs` read it as `credits` and named `opus`, the chain's next; the pass ran there, in
a fresh session with the same one-line message. No Must. Four Shoulds:

1. **`src/app/globals.css:75` still carries the pre-v2.22 comment.** The stylesheet copies §2's
   line, and the ticket's Rules held the diff to the document, so the two now disagree on when the
   shimmer fires — and `tokens.test.ts` strips CSS comments before it compares, so nothing catches
   it. Out of scope; filed as **Match the stylesheet's hero-gradient comment** (Backlog, Fix, E0.1).
2. **`docs/design-spec.md:159` says the same thing the ticket just corrected.** §2's accent table
   reads `Passed checks, handover-ready, meter at 100, diff additions → --success`, and §8.23 says
   handover-ready fires whatever the percentage reads. The objective calls the `--hero-gradient`
   comment "the one line in v2.21 that contradicts its own body"; this row is a second, twelve
   lines below it. It is not one of the five replacements, and the Rules say a check that does not
   pass is reported and never patched in the spec, so it is reported: filed as **Cut "meter at 100"
   from §2's accent table** (Backlog, Content, E0.1), which cuts v2.23.
3. **TC4 cannot pass as worded.** Recorded above with the command that measures AC4.
4. **TC2's script does not exist.** Recorded above; filed as **Run design-spec C-01 to C-04 as a
   test** (Backlog, Technical, E0.1).

## Changed since this ticket was cut

Nothing. `version-drift.mjs` read the cited design-spec v2.21 against the repository's v2.21 at
claim, and `origin/main` did not move during the run.

One thing the ticket assumed and this run confirmed rather than took: `scripts/fonts/check_fonts.py`
already reads the ğĞşŞİıĳĲ coverage per *face* across a face's subset files, and says so in its own
docstring — "Per *face*, not per file: §3 splits every face into a Latin and a Latin Extended
subset". So C-39's old sentence was the only thing out of step with §3 and with the check that
implements it, and no code follows from the new wording.

## Open questions

- **The stylesheet's copy of the §2 comment** — owner: *Match the stylesheet's hero-gradient
  comment* (Backlog). Two copies of one sentence with no check between them; the follow-up makes
  them agree, and the question of whether `tokens.test.ts` should compare comments at all is left
  open with it.
- **§2's accent table on `meter at 100`** — owner: *Cut "meter at 100" from §2's accent table*
  (Backlog). Reported, not patched, per the ticket's Rules.
- **C-01 to C-04 run by hand** — owner: *Run design-spec C-01 to C-04 as a test* (Backlog). Four
  conformance lines that say `(grep)` and `(script)` and have neither, so every version of the
  document is checked by a script the run writes and throws away.
