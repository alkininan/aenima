# T1.4 — Opportunity page and key

Branch `t1-4`. `product-spec v1.6 §2, §13 · build-log OQ9`.

**This ticket is not finished.** Its schema half — `drizzle/0016_opportunity_keys.sql` — is a
human's to apply (§5 step 6), and until it is applied `/o/<key>` resolves nothing and AC1 and AC2
are unobserved. Everything else is built, reviewed and green. The report below is the record as it
stands at the stop.

**Addendum round, 2026-09-20 — the collision is settled and the migration is `0016`.** The
board answered open question 5: T3.1's migration keeps `0015`, this one becomes `0016`. T3.1
merged to `main` at `ed0c5b8` earlier in the same run, so `main` was merged in again and the
renumbering done on top of it — file, journal entry and every reference on the branch. The
journal entry is **restamped as well as re-indexed**, which is the half a rename does not do: see
the Addendum section below. The ticket stops at Decision once more, for the `apply` it has always
been waiting on.

**Re-claimed 2026-09-20,** four days after the first stop, and stopped again at the same place.
The board had the task at Ready, but its thread holds no answer to the migration question — only
the pipeline's own two comments — so there was nothing new to build and the ticket returns to
Decision where it belongs. What this round did add: `origin/main` merged in after 20 commits of
drift, one conflict settled, the tree re-verified green, a fourth reviewer pass on the merged
tree, and two open questions the first stop could not have known about — the spec-version drift
and, more pressingly, a migration-number collision with T3.1 (open question 5).

## ACs implemented

| # | Criterion | Where | Its test |
|---|---|---|---|
| 1 | `opportunity.key` NOT NULL, unique per workspace, shaped, trigger-assigned from `key_prefix` + a per-product counter from 1 with no gaps | `drizzle/0016_opportunity_keys.sql`, `src/db/schema/tables.ts:188` | `src/db/opportunity-key.db.test.ts` — "numbers from one, without gaps", "counts per product, not per workspace". **Waiting on the apply.** |
| 2 | A client-supplied key is ignored, not honoured | `app.assign_opportunity_key()`, `drizzle/0016` | same file — "ignores a key the client supplies", "keeps its own count after an insert tried to name itself". **Waiting on the apply.** |
| 3 | `/o/<key>` renders key, title, summary, product and the items, each linking to `/i/<key>` | `src/app/o/[key]/page.tsx`, `OpportunityHeader.tsx`, `ItemsSection.tsx`, `ItemLine.tsx`, `src/db/queries/opportunity.ts` | `OpportunityHeader.dom.test.tsx` (all four), `ItemLine.dom.test.tsx` (both), `ItemsSection.dom.test.tsx` "lists every item…", `opportunity.test.ts` "carries the opportunity's own name and its product" |
| 4 | An unknown key and a key in another workspace are the same 404, one code path | `src/db/queries/opportunity.ts:105`, `src/app/o/[key]/page.tsx:42` | `opportunity.test.ts` — "returns null for a key it cannot see, as for one that does not exist" |
| 5 | An opportunity with no items renders a stated line, not an empty region | `src/app/o/[key]/ItemsSection.tsx:29` | `ItemsSection.dom.test.tsx` — "says so in words when nothing is being worked on yet"; `opportunity.test.ts` — "returns an empty item list rather than failing when nothing is linked", which is the input that branch reads |
| — | **Rules, not criteria.** "Status is derived: item lines carry a stage from `deriveStage`, never a stored one" and `OpportunityPageDetail.items`' creation order | `src/db/queries/opportunity.ts` | `opportunity.test.ts` — "derives each item's stage and asks for none", "asks for the items in creation order, on the embed" |
| 6 | The item page's opportunity line is a link; an unlinked item renders no line | `src/app/i/[key]/ItemHeader.tsx:58`, `src/db/queries/item.ts` | `ItemHeader.dom.test.tsx` — "links the opportunity to its own page, by key", "renders no lineage line at all for an unlinked item" |
| AA1 | The branch carries exactly one migration, numbered `0016`, and the journal's `idx` matches | `drizzle/0016_opportunity_keys.sql`; `drizzle/meta/_journal.json` `idx: 16` | `src/db/migrations.test.ts` — "names every migration file, and every entry has a file", "gives each entry the index its tag already carries, contiguously from zero" |
| AA2 | `0015` on main is untouched | nothing on the branch writes it: `git diff origin/main -- drizzle/0015_refinement_round.sql` is empty | `src/db/migrations.test.ts` — "keeps 0015 as T3.1's and 0016 as T1.4's, the collision as it was settled", "stamps them in index order, so none is passed over in silence" |
| AA3 | The merged tree is green | the merge commit `db6d0c0` | the Stop gate: `pnpm lint && pnpm typecheck && pnpm test` |
| 7 | Every string on the two surfaces comes from `src/i18n` | `src/i18n/en.ts` `opportunity` block | `ItemsSection.dom.test.tsx` "labels the region from the dictionary", `ItemLine.dom.test.tsx` "names the type and the derived stage from the dictionary", `ItemHeader.dom.test.tsx` "labels the line from the dictionary" |

## Tests written, each observed red first

Baseline before this ticket: **1317 passed** (94 files).

Each mutation below was applied to the source by a script, the named test file run, and the source
restored before the next — so every count is that file's own and no two mutations overlap. Pass 1's
new tests (`ItemsSection.dom.test.tsx` ×3, `opportunity.test.ts` ×1) are in the same table; they
were written against the source as reviewed and are M18–M20 and M4.

| test · file | reddened by | red → green |
|---|---|---|
| "filters on the workspace and the key, in one request" · `src/db/queries/opportunity.test.ts` | M1 the read stops naming its tenant: `.eq("workspace_id", workspaceId)` removed | 1 failed / 8 passed → 9 passed |
| "names both the workspace and the product it is reading" · `src/db/queries/opportunity.test.ts` | M2 `listOpportunities` stops naming its product: `.eq("product_id", productId)` removed | 1 failed / 8 passed → 9 passed |
| "fetches the items and their artifacts with the opportunity" · `src/db/queries/opportunity.test.ts` | M3 the embed drops the artifacts, so stage has nothing to derive from | 1 failed / 8 passed → 9 passed |
| "derives each item's stage and asks for none" · `src/db/queries/opportunity.test.ts` | M23 the stage hardcoded instead of derived from the artifacts | 1 failed / 8 passed → 9 passed |
| "asks for the items in creation order, on the embed" · `src/db/queries/opportunity.test.ts` | M4 the `.order` on the embed removed, so the item list is whatever order came back | 1 failed / 8 passed → 9 passed |
| "carries the opportunity's own name and its product" · `src/db/queries/opportunity.test.ts` | M5 the product's name dropped from the mapping | 1 failed / 8 passed → 9 passed |
| "reports a missing summary as null rather than as empty" · `src/db/queries/opportunity.test.ts` | M6 a missing summary becomes an empty string rather than null | 1 failed / 8 passed → 9 passed |
| "returns null for a key it cannot see, as for one that does not exist" · `src/db/queries/opportunity.test.ts` | M7 no row returns an empty detail instead of null, so a stranger's key resolves | 1 failed / 8 passed → 9 passed |
| "filters on the workspace and the key, in one request"; "carries the opportunity's own name and its product"; "reports a missing summary as null rather than as empty"; "returns an empty item list rather than failing when nothing is linked" · `src/db/queries/opportunity.test.ts` | M8 an opportunity with no items throws, as an implementation assuming items would | 4 failed / 5 passed → 9 passed |
| "links the opportunity to its own page, by key" · `src/app/i/[key]/ItemHeader.dom.test.tsx` | M9 the lineage rendered as a `<span>` again — the header as it was before this ticket | 1 failed / 2 passed → 3 passed |
| "renders no lineage line at all for an unlinked item" · `src/app/i/[key]/ItemHeader.dom.test.tsx` | M10 the lineage line rendered unconditionally, so an unlinked item grows an empty one | 1 failed / 2 passed → 3 passed |
| "labels the line from the dictionary" · `src/app/i/[key]/ItemHeader.dom.test.tsx` | M11 the label hardcoded in the JSX instead of read from the dictionary | 1 failed / 2 passed → 3 passed |
| "names the opportunity by the key people say out loud" · `src/app/o/[key]/OpportunityHeader.dom.test.tsx` | M12 the key dropped from the header | 1 failed / 3 passed → 4 passed |
| "renders no summary line when there is none" · `src/app/o/[key]/OpportunityHeader.dom.test.tsx` | M13 the `summary === null` branch removed, so a missing summary paints an empty line | 1 failed / 3 passed → 4 passed |
| "renders the summary when there is one" · `src/app/o/[key]/OpportunityHeader.dom.test.tsx` | M14 the summary never rendered at all | 1 failed / 3 passed → 4 passed |
| "renders no summary line when there is none"; "places the opportunity in its product" · `src/app/o/[key]/OpportunityHeader.dom.test.tsx` | M15 the product line dropped, so nothing places the opportunity | 2 failed / 2 passed → 4 passed |
| "links to the item by the key people say out loud" · `src/app/o/[key]/ItemLine.dom.test.tsx` | M16 the line links by title instead of by key | 1 failed / 1 passed → 2 passed |
| "names the type and the derived stage from the dictionary" · `src/app/o/[key]/ItemLine.dom.test.tsx` | M17 the taxonomy and the derived stage dropped from the line | 1 failed / 1 passed → 2 passed |
| "says so in words when nothing is being worked on yet" · `src/app/o/[key]/ItemsSection.dom.test.tsx` | M18 the empty-state sentence replaced with nothing — the empty region AC5 forbids | 1 failed / 2 passed → 3 passed |
| "lists every item when there are some, and says nothing about emptiness" · `src/app/o/[key]/ItemsSection.dom.test.tsx` | M19 only the first item rendered, so a list silently loses its tail | 1 failed / 2 passed → 3 passed |
| "labels the region from the dictionary" · `src/app/o/[key]/ItemsSection.dom.test.tsx` | M20 the region's heading unbound, so the section has no accessible name | 1 failed / 2 passed → 3 passed |
| "carries the opportunity the item came out of, by key and title" · `src/db/queries/item.test.ts` | M21 the item's opportunity embed drops the key it is now linked by | 1 failed / 27 passed → 28 passed |
| "reports an unlinked item as null rather than as empty" · `src/db/queries/item.test.ts` | M22 an unlinked item reports an empty opportunity instead of null | 1 failed / 27 passed → 28 passed |
| the six trigger tests · `src/db/opportunity-key.db.test.ts` | the absent column, observed against the live database: `column "key" does not exist`, from a transaction running the file's own inserts before `drizzle/0016`. **Never green and cannot be until the migration is applied.** | `6 skipped → 6 skipped`; green owed at the apply |
| "shows the opportunity as a link to its own page" · `e2e/item.spec.ts` (pass 2, finding 1) | the assertions it carried before this ticket — `closest("a") !== null` expected `false` and `main a[href^="/o/"]` expected 0 — run against the page as built: `Expected: false / Received: true` | `1 failed → 1 passed` (chromium, `/dev/item`) |

Whole suite after: **1338 passed, 6 skipped** (100 files). `pnpm lint` and `pnpm typecheck` clean.

### The addendum round's four, same discipline

Baseline on the merged tree before them: **1613 passed, 6 skipped** (115 files). Each mutation was
applied on its own and reverted before the next; `src/db/migrations.test.ts` is 4 tests, so the
counts below are that file's.

| test · file | reddened by | red → green |
|---|---|---|
| "names every migration file, and every entry has a file" · `src/db/migrations.test.ts` | M24 the file renamed back to `drizzle/0015_opportunity_keys.sql` while the journal still says `0016` — a tag with no file, which is what a rename without a journal edit leaves behind | `1 failed / 3 passed → 4 passed` |
| "gives each entry the index its tag already carries, contiguously from zero" · `src/db/migrations.test.ts` | M25 the last entry's `idx` set back to `15`, two tags on one index — the collision as it would have landed | `2 failed / 2 passed → 4 passed` (it also reddens the fourth) |
| "stamps them in index order, so none is passed over in silence" · `src/db/migrations.test.ts` | M26 the last entry's `when` set back to `1788982026724`, behind the `0015` that is now applied — the silent skip `apply.mjs`'s `blockedOf` refuses | `1 failed / 3 passed → 4 passed` |
| "keeps 0015 as T3.1's and 0016 as T1.4's, the collision as it was settled" · `src/db/migrations.test.ts` | M27 entry 15's tag set to `0015_opportunity_keys` — the collision settled the other way round, against the board's answer | `2 failed / 2 passed → 4 passed` (it also reddens the first) |

Whole suite after: **1617 passed, 6 skipped** (116 files). `pnpm lint` and `pnpm typecheck` clean.
`pnpm typecheck` was red first for a reason worth recording: `/o/[key]` is this branch's own new
route and `.next/types` had been generated for a tree without it, so `AppRoutes` did not carry it.
T3.1's `scripts/run/route-types.mjs` — which arrived in this very merge — stamps the route files
the types were generated from, read `unstamped`, and regenerated. Preflight's older check had only
asked whether `.next/types` existed, and it did.

**On the one row that is not green.** The migration cannot be applied from this checkout — the
`DATABASE_URL` in `.env.local` is not the table's owner (`must be owner of table opportunity`),
which is the credential boundary §5 step 6 describes working exactly as intended. Trialling the
DDL inside a rolled-back transaction was attempted for that reason and refused at the same wall.
So `0016` has been read against `0005_item_keys.sql` statement for statement rather than executed,
and the review confirmed that reading independently.

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `583a0a6` | fable | no | FINDINGS |
| 2 | `d0aa0cc` | fable | no | FINDINGS |
| 3 | `d12218e` | fable | no | PASS |
| 4 | `ce5a94f` | fable | no | PASS |
| 5 | `db6d0c0` | fable | no | FINDINGS |
| 6 | `e738e7b` | fable | no | PASS |

Every pass ran on `fable`, the model `.claude/agents/reviewer.md` pins; none was refused, so the
chain's `opus` was never reached, and none stopped at its turn limit. Passes 1–3 predate T0.22's
chain and T0.23's `resumed` column, which arrived on this branch with the re-claim merge; their
rows are read from the pin as it stood at each commit, not assumed.

**Pass 1** (`docs/reviews/T1.4.md`, commit `583a0a6`): FINDINGS — four Musts, four Shoulds.

1. **Must — no red-first record anywhere.** *Done:* this report, and the mutation table above.
2. **Must — AC5 has no test; `noItems` is asserted nowhere.** *Done:* the section lifted out of
   `page.tsx` into `src/app/o/[key]/ItemsSection.tsx` and given `ItemsSection.dom.test.tsx`, both
   arms plus the region's label.
3. **Must — the query documents "creation order" and never calls `.order`,** so the list can
   reshuffle between reads. *Done:* `.order("created_at", { ascending: true, referencedTable:
   "item" })` added, and the test recorder widened to keep the options object so the assertion can
   see it is the **embed** that is ordered.
4. **Must (the reviewer noted its own doubt) — the summary claims §4's subtitle slot and wraps,**
   where §4 says "One line; it truncates rather than wraps" and `ItemHeader` reads it that way.
   *Done:* `truncate` added and the comment rewritten to quote the rule. The reviewer was right and
   the doubt was misplaced: §4 is explicit, and two headers disagreeing about one slot is the drift
   a slot exists to prevent.
5. **Should — build-log OQ9 still reads as open.** *Done:* struck through and closed in
   `docs/build-log.md`, in item 11's form, with the shared-key-shape property recorded.
6. **Should — the `listOpportunities` test names no criterion.** *Kept,* with a comment saying it
   is the layer's rather than the ticket's. Recorded here as incidental coverage.
7. **Should — `opportunity.backToList` duplicates `item.backToList` verbatim.** *Kept,* with the
   reason in the comment: two surfaces, either sentence free to change.
8. **Should — the `/dev` fixture now links from a public page into a protected one.** *Noted* in
   the fixture's comment.

**Pass 2** (commit `d0aa0cc`): FINDINGS — two Musts, three Shoulds. Pass 1's four were confirmed
settled.

1. **Must — `e2e/item.spec.ts:184` asserts the opposite of AC6.** The test is named "shows the
   opportunity as text, with no link to a page that does not exist", it drives `/dev/item`, and
   this diff gave that fixture an opportunity; both assertions are false now. *Done:* the
   assertion inverted with the thing it guarded — `closest("a")` is `true` and
   `main a[href="/o/soc-2"]` has count 1 — with the comment rewritten to say why it used to read
   the other way. Observed red with the old assertions and green with the new (chromium).
   AGENTS.md's "remove the old path in the same change", applied to a test that *was* the old path.
2. **Must — the red-first record is per file, not per test:** seven rows, six tests seen red,
   twenty-one passing. *Done:* redone as 23 scripted mutations, one at a time, each naming the
   tests it reddens — the table above. Every new and every changed vitest test is now named by at
   least one row.
3. **Should — the ticket's Tests section is one file short of what exists.** *Done:*
   `ItemsSection.dom.test.tsx` and the `e2e/item.spec.ts` change added to `docs/tickets/T1.4.md`.
4. **Should — the missing-summary test counts `<p>` elements rather than asserting the text's
   absence.** *Done,* both, with a comment: the count is the stronger assertion, because a `<p>`
   around a null summary is empty and so invisible to `queryByText` while still painting the
   slot's line box.
5. **Should — ACs 1–2 stay unobserved behind the unapplied migration.** §5 step 6 working, not a
   gap. Open question 1 below.

**Pass 3** (commit `d12218e`, cold): **PASS** — no Must stands. Five Shoulds. The reviewer ran the
seven files the ticket names and reproduced the table above file by file (9·3·4·2·3·28 = 49
passed, 6 skipped), and read `drizzle/0016` line by line against `0005`.

1. **ACs 1–2 unobserved behind the unapplied migration;** the verdict does not assert the trigger
   works. *Recorded:* open question 1 below, and the diff is gated on `drizzle/`, so `apply` then
   `merge` stay the human's words either way.
2. **`ItemLine.tsx`'s comment claims the stretched link keeps the taxonomy selectable;** the
   overlay covers the whole row. *Done:* comment rewritten to `ItemRow`'s real reason — no nested
   interactives — and to say outright that the overlay covers the taxonomy.
3. **The embed is ordered on `created_at` alone, which is not a total order;** `0016`'s own
   backfill tie-breaks on `(created_at, id)` for that reason. *Done:* a second `.order("key", …)`
   on the embed, and the existing test extended to assert both keys. Observed red with the second
   order removed: `1 failed / 8 passed → 9 passed`.
4. **`CountRow` and `toArtifacts` are a verbatim copy of `item.ts`'s.** *Done:* exported from
   `item.ts` and imported here, so PostgREST's count shape has one reading.
5. **Three `opportunity.test.ts` tests name no criterion in the AC table.** *Done:* named in the
   AC table above — two under the Rules line they hold, one under AC5.

**Shoulds 2, 3 and 4 were fixed after the PASS was written**, so they are in the tree and not in
any verdict. Each is small and none changes a criterion: a comment, a second sort key with its own
red-first row, and an import replacing a copied helper. Said here rather than left implied.

**Pass 4** (commit `ce5a94f`, cold, after the re-claim merged `origin/main` in): **PASS** — no
Must stands. Four Shoulds. The reviewer ran the seven vitest files the ticket names — `6 passed |
1 skipped` files, `53 passed | 6 skipped` tests — and observed the six skips behind their stderr
banner rather than reading them off this report. The four tests over pass 3's 49 are main's,
merged in. It read the migration against `0005` statement for statement, checked `item`'s single
composite FK to `opportunity` so the reverse embed cannot be ambiguous, confirmed `0001`'s grants
are table-level so the new column is readable through PostgREST, and checked that the re-claim
merge left the ticket's hunks intact in all seven of its files the merge overlapped.

1. **ACs 1–2 remain unobserved; the db suite has never run.** *Recorded:* open question 1,
   unchanged — this round did not apply the migration and could not.
2. **"Changed since this ticket was cut" was false:** the repo is at `product-spec` v1.8, not
   v1.6. *Done:* corrected below, with the check that matters — §2 and §13 are byte-identical, so
   nothing built against v1.6 is wrong at v1.8.
3. **No per-pass reviewer-model table, which guidelines §5 step 8 now requires.** *Done:* the
   table above. The rule arrived with the re-claim merge; it was absent at `41047a7`.
4. **`docs/schema.md`'s migration list stops at `0013`; this migration owes a paragraph in
   `0005`'s form.** *Not done at pass 4, deliberately* — the paragraph would have to name the
   migration by number and the number was what open question 5 put to the human. **Done in the
   addendum round**, once the answer settled it at `0016`: see open question 4.

**Pass 5** (commit `db6d0c0`), the addendum round's first: FINDINGS — two Musts, two Shoulds.

1. **Must — `src/db/migrations.test.ts` has no red-first record and the ticket does not name it.**
   The addendum said `TA1 → AA1 · TA2 → AA2 · TA3 → AA3` and nothing defined TA1–TA3, so AA1 and
   AA2 rested on tests never seen red. *Done:* the three named in the ticket's Addendum against the
   file's own test names, and M24–M27 above, one mutation each.
2. **Must — the report contradicted the tree on the one thing the round changed.** It still said
   `drizzle/0015_opportunity_keys.sql`, and open question 5 still put the collision to the human as
   open — the very question the addendum quotes them answering. §5 step 8 mirrors the report to the
   board, so the person doing the `apply` would have read a filename that does not exist and a
   settled question re-asked. *Done:* every reference follows the file, open question 5 closed in
   the past tense with the restamping it turned on, AA1–AA3 in the AC table, this table's row 5.
3. **Should — `docs/schema.md` can now take `0016`'s paragraph.** *Done:* open question 4.
4. **Should — the log records no addendum round.** *Done:* `docs/log/T1.4.md`'s closing section.

**Pass 6** (commit `e738e7b`): **PASS** — no Must, three Shoulds. It read every hunk of
`origin/main...HEAD`, ran the ticket's seven files plus `migrations.test.ts` (7 passed | 1 skipped
files, 57 passed | 6 skipped tests), and checked the six skips behind their banner rather than off
this report. It confirmed `git diff origin/main HEAD -- drizzle/0015_refinement_round.sql` empty and
`0016` against `0005` statement for statement.

1. **Should — AC1 and AC2 are still unobserved and the six trigger tests have never been green.**
   *Recorded, not fixable here:* open question 1. It is the ticket's shape, not a defect — the diff
   adds a migration, so the guard will not let the run merge its own work either.
2. **Should — `src/db/queries/opportunity.ts` overclaims "creation order".** The second `.order` is
   on `key`, which sorts as text, so ten items sharing a `created_at` come back `soc-10` before
   `soc-2`. What the column buys is *stability*, which is what pass 1 asked for, not chronology.
   *Fixed after the verdict:* both comments reworded to say stable-and-arbitrary rather than
   chronological. Comments only — no test changed, and `opportunity.test.ts` stayed at 9 passed.
3. **Should — `scripts/run/review-scope.mjs` cuts a path at `[key]` and counts prose precedents as
   cited files.** *Out of scope, filed:* the same Fix task as open question 6, which is the same
   script family reading the wrong thing.

## Changed since this ticket was cut

`version-drift.mjs` on `product-spec v1.6 §2, §13 · build-log OQ9`: **drifted** — the ticket
cites v1.6 and the repo is at v1.8.

The drift is in the document's header, not in what this ticket reads. Both cited sections were
regenerated from the repo with `spec-sections.mjs` and compared against the `## Cited` block in
`docs/tickets/T1.4.md`: §2 Object model and §13 Surface both match byte for byte. Nothing built
against v1.6 is wrong at v1.8, and the ticket file needed no patch.

The branch itself had drifted much further — 20 commits of `main` (T0.13, T0.19, T0.21, T0.22,
T0.23, T2.9, T2.10, T1.5) landed while this ticket waited at Decision. The re-claim merged
`origin/main` in. One conflict, `docs/build-log.md`, where this branch and T1.5 each added a line
to the Tickets done list — §5's own case, settled by regenerating the list with `log-index.mjs`,
which then reported `written: false`: the union was already what the directory holds. Seven other
of the ticket's files auto-merged. The merged tree is green: **1461 passed, 6 skipped** across 105
files, `pnpm typecheck` clean once `next typegen` had seen the branch's `/o/[key]` route, and
`pnpm lint` clean but for one pre-existing warning in `scripts/run/loosening.mjs` that came in
with the merge and is main's, not this ticket's.

## Open questions

1. **`drizzle/0016_opportunity_keys.sql` is unapplied, and the ticket is not done until it is.**
   Owner: whoever answers `apply` on the task's thread, and the run in the primary checkout that
   follows. Until then `/o/<key>` 404s every key, the six trigger tests skip, and
   `src/db/database.types.ts` carries one hand-written block. That run should regenerate and diff
   that file (open question 5's rule), and confirm `opportunity-key.db.test.ts` goes green.
2. **A migration's own db test cannot be green in the run that writes it.** Recorded as build-log
   open question 40, with the alternatives and an owner: decide at the next migration whether the
   skip-with-a-banner here is the house pattern or this ticket's exception.
3. **`soc-3` can name an item and an opportunity in the same product.** The two counters are
   independent and the two unique constraints are on different tables, which is what the ticket's
   "mirroring `item.key`" asks for — `/i/` and `/o/` tell them apart and a person saying the key
   does not. Built as specified, held by a test, and recorded in build-log open question 9 so the
   day it is judged not worth it, something says what changed.

4. **~~`docs/schema.md` owes this migration a paragraph, and cannot have one yet.~~ Closed in
   the addendum round.** The blocker was never the apply, it was the number: pass 4 would have had
   to name the migration by an index open question 5 was still putting to you. The answer settled
   it at `0016`, so the paragraph is written, in `0005`'s form and beside `0015_refinement_round`'s
   own — the house pattern is to document a migration on its branch, which T3.1 did before its
   apply. It carries the renumbering's real lesson too: the stamp moves with the index or the
   migration is passed over in silence. `0014_test_users` is still missing from that list and is
   not this ticket's to add.
5. **~~Two branches both call their migration `0015`, and only one can.~~ Answered, and done.**
   You settled it on the thread: T3.1's migration keeps `0015`, this one becomes `0016`. T3.1
   merged to `main` at `ed0c5b8`, `main` was merged in again, and the renumbering followed — file,
   journal entry and every reference on the branch.

   **The half a rename would have missed.** The entry was restamped as well as re-indexed:
   `1788982026724` → `1789926529521`. `scripts/run/apply.mjs`'s `blockedOf` refuses a migration
   stamped no later than the newest row in drizzle's ledger, because `migrate()` passes such a
   file over *in silence* and still returns cleanly — and T3.1's `0015`, now applied, is stamped
   `1789644802697`. So an `0016` keeping its old stamp would have been reported applied and never
   have run. `pnpm db:generate` restamps on regeneration, which is where the rule comes from; this
   migration is hand-written, so the stamp was moved by hand to match.

   What now holds it: `src/db/migrations.test.ts`, four assertions over `drizzle/` and the journal
   — one entry per file, `idx` matching the tag, stamps increasing with the index, and the two
   tags pinned at the indexes you chose.

6. **`report-check.mjs` and `review-scope.mjs` default to `main`, which in a worktree is whoever
   last moved the primary checkout.** Both take an optional base and fall back to the literal
   `main`. A scheduled run works in a worktree branched from `origin/main`, and nothing in the
   protocol updates the *local* `main` ref — the primary checkout owns it and a worktree cannot
   check it out. This round met it twice: local `main` sat at `c310b30` while `origin/main` was
   `ed0c5b8`, so the check refused the report over ten test files belonging to T0.24 and T3.1, and
   pass 5 had to widen its own scope by hand for the same reason. Both were right against
   `origin/main`. **The fix is one word** — default to `origin/main`, which is what every other
   script in `scripts/run/` compares against and what the guard's own `loosening.mjs` calls "the
   corpus is always `origin/main`'s". It is a harness change and not this ticket's; filed rather
   than done. Owner: a Fix task.

## Defaults this claim took

Both cheap to undo, both said in the claim's one `default` comment on the thread.

1. **Re-claiming a task the board had at Ready whose thread holds no answer.** The picker offered
   T1.4 as the only unblocked Ready task. Its thread says the migration question still stands, so
   the Ready is not a go the human gave on this thread — most likely a click. Treating it as the
   go to re-claim costs nothing: the run cannot apply the migration from a worktree either way,
   and the task ends this round at Decision, which is where the thread already said it was. The
   alternative — refusing to claim and reporting an idle run — would have left the branch 20
   commits stale and the collision undiscovered.
2. **Merging `origin/main` into `t1-4` rather than leaving the branch where it stopped.** T1.5's
   own re-claim set the precedent four days ago. A branch that sits at Decision while eight
   tickets land behind it stops being reviewable against what would actually merge, and the
   conflict it was always going to hit — two log lines — is cheaper to settle now, by the script
   §5 names for exactly this, than at merge time. Undoing it is one `git reset`.

**What the re-claim did not decide.** The migration-number collision with T3.1 (open question 5).
That one touches the schema's applied-migration ordering, which §4 names as expensive, and the
order is the human's — so it was named in that stop's comment rather than guessed at. It was
answered on the thread, and the addendum round below acts on the answer.

### The addendum round's two

1. **The journal entry is restamped as well as re-indexed.** The addendum says to renumber "the
   file and its journal entry"; it does not mention the `when`. Left alone it would have stayed
   `1788982026724`, four days behind the `0015` that is now applied, and
   `scripts/run/apply.mjs` would have refused it — `blockedOf`, which exists because `migrate()`
   passes such a file over in silence and returns cleanly. So the stamp had to move for the
   renumbering to mean anything, and `1789926529521` is simply now. This is not a free choice in
   the ordinary sense — there is one value that works and every other fails the same way — but it
   touches a migration's place in the order, which is the human's, so it is said rather than
   assumed. `pnpm db:generate` would have done the same thing as a side effect; this migration is
   hand-written, so it was done by hand.
2. **The base-ref defect is filed, not fixed.** `report-check.mjs` refused this report over ten
   files belonging to other tickets (open question 6). Passing `origin/main` explicitly is one
   word and it is what the check wanted; changing the script's own default is a harness change
   with its own tests, and this is a ticket about an opportunity key. Filed as *Compare run
   scripts against origin/main* at Backlog, Type Fix, the same Epic, carrying pass 6's Should 3
   about `review-scope.mjs` with it.
