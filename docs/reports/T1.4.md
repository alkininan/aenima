# T1.4 — Opportunity page and key

Branch `t1-4`. `product-spec v1.6 §2, §13 · build-log OQ9`.

**This ticket is not finished.** Its schema half — `drizzle/0015_opportunity_keys.sql` — is a
human's to apply (§5 step 6), and until it is applied `/o/<key>` resolves nothing and AC1 and AC2
are unobserved. Everything else is built, reviewed and green. The report below is the record as it
stands at the stop.

## ACs implemented

| # | Criterion | Where | Its test |
|---|---|---|---|
| 1 | `opportunity.key` NOT NULL, unique per workspace, shaped, trigger-assigned from `key_prefix` + a per-product counter from 1 with no gaps | `drizzle/0015_opportunity_keys.sql`, `src/db/schema/tables.ts:188` | `src/db/opportunity-key.db.test.ts` — "numbers from one, without gaps", "counts per product, not per workspace". **Waiting on the apply.** |
| 2 | A client-supplied key is ignored, not honoured | `app.assign_opportunity_key()`, `drizzle/0015` | same file — "ignores a key the client supplies", "keeps its own count after an insert tried to name itself". **Waiting on the apply.** |
| 3 | `/o/<key>` renders key, title, summary, product and the items, each linking to `/i/<key>` | `src/app/o/[key]/page.tsx`, `OpportunityHeader.tsx`, `ItemsSection.tsx`, `ItemLine.tsx`, `src/db/queries/opportunity.ts` | `OpportunityHeader.dom.test.tsx` (all four), `ItemLine.dom.test.tsx` (both), `ItemsSection.dom.test.tsx` "lists every item…", `opportunity.test.ts` "carries the opportunity's own name and its product" |
| 4 | An unknown key and a key in another workspace are the same 404, one code path | `src/db/queries/opportunity.ts:105`, `src/app/o/[key]/page.tsx:42` | `opportunity.test.ts` — "returns null for a key it cannot see, as for one that does not exist" |
| 5 | An opportunity with no items renders a stated line, not an empty region | `src/app/o/[key]/ItemsSection.tsx:29` | `ItemsSection.dom.test.tsx` — "says so in words when nothing is being worked on yet" |
| 6 | The item page's opportunity line is a link; an unlinked item renders no line | `src/app/i/[key]/ItemHeader.tsx:58`, `src/db/queries/item.ts` | `ItemHeader.dom.test.tsx` — "links the opportunity to its own page, by key", "renders no lineage line at all for an unlinked item" |
| 7 | Every string on the two surfaces comes from `src/i18n` | `src/i18n/en.ts` `opportunity` block | `ItemsSection.dom.test.tsx` "labels the region from the dictionary", `ItemLine.dom.test.tsx` "names the type and the derived stage from the dictionary", `ItemHeader.dom.test.tsx` "labels the line from the dictionary" |

## Tests written, each observed red first

Baseline before this ticket: **1317 passed** (94 files).

| test · file | reddened by | red → green |
|---|---|---|
| `src/db/queries/opportunity.test.ts` — 8 tests of `getOpportunityByKey` and `listOpportunities` | dropped `.eq("workspace_id", workspaceId)` from `getOpportunityByKey`, so the layer stopped naming its tenant | `1 failed / 7 passed → 8 passed` |
| `src/db/queries/opportunity.test.ts` — "asks for the items in creation order, on the embed" (review pass 1, finding 3) | removed the `.order("created_at", { ascending: true, referencedTable: "item" })` call the finding asked for | `1 failed / 8 passed → 9 passed` |
| `src/app/i/[key]/ItemHeader.dom.test.tsx` — 3 tests | put the opportunity title back in a `<span>` instead of a `<Link>`, which is what the header rendered before this ticket | `1 failed / 2 passed → 3 passed` |
| `src/app/o/[key]/OpportunityHeader.dom.test.tsx` — 4 tests | removed the `summary === null` branch, so a missing summary rendered an empty line | `1 failed / 3 passed → 4 passed` |
| `src/app/o/[key]/ItemLine.dom.test.tsx` — 2 tests | pointed the link at `itemHref(item.title)` instead of `itemHref(item.key)` | `1 failed / 1 passed → 2 passed` |
| `src/app/o/[key]/ItemsSection.dom.test.tsx` — 3 tests (review pass 1, finding 2) | replaced the empty-state sentence with `null`, which is the empty region the criterion forbids | `1 failed / 2 passed → 3 passed` |
| `src/db/opportunity-key.db.test.ts` — 6 tests of the trigger | **the absent column, observed against the live database**: `column "key" does not exist`, from a transaction that ran the test's own inserts before `drizzle/0015`. The file has never been green and cannot be until the migration is applied — see the open question below. | `6 skipped → 6 skipped` (the loud banner; green owed at the apply) |
| `src/db/queries/item.test.ts` — 2 existing tests updated for `opportunity: { key, title }` | the shape change itself: `opportunityTitle` no longer exists on `ItemPageDetail`, and `tsc` said so before the tests were touched | part of `1317 → 1338 passed` |

Whole suite after: **1338 passed, 6 skipped** (100 files). `pnpm lint` and `pnpm typecheck` clean.

**On the one row that is not green.** The migration cannot be applied from this checkout — the
`DATABASE_URL` in `.env.local` is not the table's owner (`must be owner of table opportunity`),
which is the credential boundary §5 step 6 describes working exactly as intended. Trialling the
DDL inside a rolled-back transaction was attempted for that reason and refused at the same wall.
So `0015` has been read against `0005_item_keys.sql` statement for statement rather than executed,
and the review confirmed that reading independently.

## Reviewer passes and findings

**Pass 1** (`docs/reviews/T1.4.md`, commit `583a0a6`): FINDINGS — four Musts, four Shoulds.

| # | Finding | What was done |
|---|---|---|
| 1 | Must — no red-first record anywhere | This report, and the table above. |
| 2 | Must — AC5 has no test; `noItems` is asserted nowhere | The section lifted out of `page.tsx` into `src/app/o/[key]/ItemsSection.tsx` and given `ItemsSection.dom.test.tsx`, both arms plus the region's label. |
| 3 | Must — the query documents "creation order" and never calls `.order`, so the list can reshuffle between reads | `.order("created_at", { ascending: true, referencedTable: "item" })` added, and the test recorder widened to keep the options object so the assertion can see it is the **embed** that is ordered. |
| 4 | Must (reviewer's own doubt noted) — the summary claims §4's subtitle slot and wraps, where §4 says "One line; it truncates rather than wraps" and `ItemHeader` reads it that way | `truncate` added and the comment rewritten to quote the rule. The reviewer was right and the doubt was misplaced: §4 is explicit, and two headers disagreeing about one slot is the drift a slot exists to prevent. |
| 5 | Should — build-log OQ9 still reads as open | Struck through and closed in `docs/build-log.md`, in item 11's form, with the shared-key-shape property recorded. |
| 6 | Should — the `listOpportunities` test names no criterion | Kept, with a comment saying it is the layer's rather than the ticket's. Recorded here as incidental coverage. |
| 7 | Should — `opportunity.backToList` duplicates `item.backToList` verbatim | Kept, with the reason in the comment: two surfaces, either sentence free to change. |
| 8 | Should — the `/dev` fixture now links from a public page into a protected one | Noted in the fixture's comment. |

**Pass 2**: see the verdict file.

## Changed since this ticket was cut

`version-drift.mjs` on `product-spec v1.6 §2, §13`: nothing drifted — the repo is at v1.6.

## Open questions

1. **`drizzle/0015_opportunity_keys.sql` is unapplied, and the ticket is not done until it is.**
   Owner: whoever answers `apply` on the task's thread, and the run in the primary checkout that
   follows. Until then `/o/<key>` 404s every key, the six trigger tests skip, and
   `src/db/database.types.ts` carries one hand-written block. That run should regenerate and diff
   that file (open question 5's rule), and confirm `opportunity-key.db.test.ts` goes green.
2. **A migration's own db test cannot be green in the run that writes it.** Recorded as build-log
   open question 40, with the alternatives and an owner: decide at the next migration whether the
   skip-with-a-banner here is the house pattern or this ticket's exception.
3. **`soc-3` can name an item and an opportunity in the same product.** The two counters are
   independent and the two unique constraints are on different tables, which is what the ticket's
   "mirroring `item.key`" asks for — `/i/` and `/o/` tell them apart and a person saying the key
   does not. Built as specified, held by a test, and recorded in build-log open question 9 so the
   day it is judged not worth it, something says what changed.
