# T0.32 — report

Current state is now the second generated section of `docs/build-log.md`. `log-index.mjs` writes
it from the repo alone: the version each of product-spec, design-spec and guidelines declares in
its own header, read through `version-drift.mjs`'s `parseHeaderVersion`, and the newest entry
under `docs/log/` with its date and commit. Phase and next ticket are gone. The narrative
paragraphs under it moved to the entries they summarized.

## ACs implemented

| AC | What holds it | Test |
|---|---|---|
| AC1 — Current state names the three documents at their header versions, and the newest `docs/log/` entry with its date and commit | `currentState()` and `versions()` in `scripts/run/log-index.mjs`; `**Specs:**` and `**Newest entry:**` in `docs/build-log.md` | TC1 — `log-index.test.mjs` "names product-spec, design-spec and guidelines at their header versions", "names the newest docs/log/ entry with its date and commit", "keeps the two lines the repo cannot derive" |
| AC2 — no "Phase" or "Next ticket" line remains | the two lines are not in `currentState()`'s output and are gone from the file | TC2 — "carries no Phase and no Next ticket line" |
| AC3 — the test fails when a version or the newest entry in the block disagrees with the repo | `generate()` rewrites both sections, so the committed file is a function of the repo | TC3 — "matches docs/log/ and the documents' own headers, section for section" |
| AC4 — every narrative sentence removed is present in a `docs/log/` entry | `T2.2.md`, `design-spec-v2.7-v2.12.md`, `design-spec-v2.13-v2.15.md`, and `T0.32.md` for the one clause no other file held | TC4 — the five tests under "the narrative that left Current state" |
| AC5 — `log-index.mjs` and `version-drift.mjs` read versions through one shared function | `log-index.mjs` imports `parseHeaderVersion` and defines no version pattern | TC5 — "answers exactly what parseHeaderVersion answers, header shape for header shape", "defines no version pattern of its own" |

## Tests written, each observed red first

Counts are `scripts/run/log-index.test.mjs` alone unless the row says otherwise. The eleven tests
marked *absent* were red before the generator existed, over `origin/main`'s tree: 7 failed / 15
passed → 29 passed.

| test | reddened by | red → green |
|---|---|---|
| names product-spec, design-spec and guidelines at their header versions | absent: no `**Specs:**` line in the block | 7 failed / 15 passed → 29 passed |
| names the newest docs/log/ entry with its date and commit | absent: no `**Newest entry:**` line in the block | 7 failed / 15 passed → 29 passed |
| carries no Phase and no Next ticket line | absent: the block still carried both lines | 7 failed / 15 passed → 29 passed |
| keeps v1.4's two rulings and where they landed | absent: `T2.2.md` did not carry the sentence | 7 failed / 15 passed → 29 passed |
| keeps the form language having been settled over two runs against the sign-in flow | absent: the v2.7–v2.12 entry did not carry it | 7 failed / 15 passed → 29 passed |
| keeps every v2.7–v2.12 clause, the half already there and the half moved in | absent for the moved half (`#08090C base`); the already-there half by removing `the resend cooldown, ` from the entry | 7 failed / 15 passed → 29 passed, then 1 failed / 28 passed → 29 passed |
| keeps v2.13–v2.15 coming out of the first surfaces rather than the sign-in flow | absent: the entry did not carry the framing | 7 failed / 15 passed → 29 passed |
| keeps the closed-and-matching claim, as a fact about the moment it stamped | absent: `docs/log/T0.32.md` did not exist, then the clause reworded to "both documents were finished" | 1 failed / 28 passed → 29 passed |
| keeps every v2.3–v2.6 item across T0.5 and T0.6 | green untouched — T0.5 and T0.6 already held every item, which is the honest answer; `48h fields, ` removed from `docs/log/T0.5.md` | 1 failed / 28 passed → 29 passed |
| keeps the two lines the repo cannot derive | green untouched — both lines were already in the block; the `**Repo:**` line deleted from `docs/build-log.md` | 2 failed / 27 passed → 29 passed |
| matches docs/log/ and the documents' own headers, section for section (extended from T0.10's) | green untouched — main's `generate` ignores the extra `root` key; `product-spec v1.8` → `v1.7` in the block, and separately `[T0.31` → `[T0.30` on the newest-entry line | 2 failed / 27 passed → 29 passed, each edit |
| refuses a document whose header carries no version, by name | the `if (!version) throw` removed from `versions()` | 1 failed / 28 passed → 29 passed |
| takes the newest by stamp, ties by id in natural order | `newest()`'s `at(-1)` → `at(0)` | 4 failed / 25 passed → 29 passed |
| says null for an empty directory rather than guessing | `newest()`'s `?? null` → a fabricated entry | 2 failed / 27 passed → 29 passed |
| prints the intro, the specs, the newest entry and the standing lines | `...STANDING` dropped from `currentState()` | 3 failed / 26 passed → 29 passed |
| says so rather than inventing an entry when the directory is empty | `"none yet"` → `""` | 1 failed / 28 passed → 29 passed |
| answers exactly what parseHeaderVersion answers, header shape for header shape | `CURRENT_DOCS` reordered | 3 failed / 26 passed → 29 passed |
| defines no version pattern of its own | `versions()` given its own `/\bv(\d+\.\d+)\b/` instead of the import | 1 failed / 28 passed → 29 passed |
| reads every .md, sorts it, and writes both sections into the log (extended from T0.10's) | `...STANDING` dropped from `currentState()` | 3 failed / 26 passed → 29 passed |
| both scripts, AC5's shared function | `parseHeaderVersion`'s `return match[1]` → `` `${match[1]}-mutated` ``, over `log-index.test.mjs` and `version-drift.test.mjs` together | 8 failed / 34 passed → 42 passed |
| both scripts, AC5's shared header window | `HEADER_LINES` 6 → 0 in `version-drift.mjs`, same two files | 11 failed / 31 passed → 42 passed |

Suite at close: `pnpm vitest run --project node` — 81 files, 1392 tests, all passing; `pnpm lint`
clean bar two pre-existing warnings in `apply.mjs` and `loosening.mjs`; `pnpm typecheck` clean.

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `29c05e3` | opus | yes | FINDINGS |
| 2 | `f314805` | opus | no | PASS |

Pass 1 was called first on `fable`, the model `.claude/agents/reviewer.md` pins, and came back
*You're out of usage credits* rather than a review; `review-model.mjs` answered `stop: false`,
`model: opus`, and the pass ran there. It then stopped at its 30-turn limit; `review-model.mjs`
answered `resume: true` and the same session was continued once, which is the `yes` above. Pass 2
was invoked without a model, started again at `fable`, met the same refusal, and ran on `opus`.

**Pass 1 — two Musts, both fixed.**

1. `.claude/commands/ticket-done.md` step 1 still told a session to set the next-ticket line and
   mark a phase complete. Correct until this diff and a hand edit to a generated block after it —
   the old writer left in place, which AGENTS.md forbids. Steps 1 and 2 are now §5 step 8's path.
2. The clause "both complete and closed, and the code matches" was held by no file, and the test
   named for it asserted only the design spec's own closure line. The clause is recorded once in
   `docs/log/T0.32.md`, as a fact about the moment Current state last stamped by hand, with the
   reason it is not carried forward; the test greps it there.

**Pass 1 — eight Shoulds.** Six fixed: the equality test's marker dates itself (1), TC5's test
asserts the three versions literally rather than comparing the parser to itself (3), the TC1
helper reads headers through `parseHeaderVersion` instead of copying its regex and window (4), the
docstring says what the measurement supports (5), SKILL.md step 8 names both sections (6), and
`ordered`, `link` and `stamp` stop being exports nothing imports along with `versions`' unused
second parameter (8). Two are report content: the mutation rows for the tests green on an
untouched tree (2), which are in the table above, and that AC1's "and commit" cannot render for
any entry the pipeline writes, since SKILL.md tells every run to leave the commit off (7) — the
code is right, `stamp()` prints a commit where an entry has one, and only the migrated entries do.

**Pass 2 — PASS, eight Shoulds.** Four fixed after the verdict, and said so here because they are
the one change the PASS did not read: the marker cited T0.8 where that describe block is T0.10's
(1), `.claude/commands/ticket-done.md` gave the entry a title shape with no ID (5), the
v2.7–v2.12 grep covered only the clauses this ticket moved in and now covers the whole sentence
(6), and the closed-and-matching test no longer rests on the design spec's closing line, which may
be reworded at v2.23 without AC4 changing (7). Each is a comment, a doc line, or strictly more
assertions; none changes what the code does. The rest are recorded: guidelines §5 step 8 still
describes one generated section (2) — an open question below, since patching a versioned document
the Spec line does not cite is another ticket's; the report's mutation rows (3), which are above;
`docs/build-log.md` naming the second-newest entry (4), which is the open question below; and that
the design spec's closing line is not "a spec's own header comment" as AC4 words it (8 of pass 1's
lesser half) — so the `docs/log/` branch of AC4 is the one actually met for that clause.

## Changed since this ticket was cut

`version-drift.mjs` reports no drift: the ticket cites product-spec v1.8 and the repo holds v1.8.

The ticket's Objective says the board cites design-spec v2.21; the repo holds v2.22, which T0.29
landed the evening before this ticket was claimed. The generated block reads the repo, so it says
v2.22 — the Objective's number was already one behind when the ticket was written, which is the
drift it was cut to remove.

## Open questions

1. **`docs/log/T0.31.md`'s stamp is local time written with a `Z`, so the generated Newest entry
   names it rather than this ticket.** `_2026-09-23T06:58:00Z_` against a commit that landed at
   `03:54:17Z` — three hours out, and CLAUDE.md says store timestamps in UTC. The script is right
   and the stamp is wrong. Correcting a committed entry is outside this ticket, so it is filed at
   Backlog as a Fix; until it lands, AC1's line points a fresh chat one entry back. Reading the
   stamps for *newest* is what made a three-hour error visible after the list had already sorted
   on it for a ticket — nothing before this needed the ordering to be right at the top.
2. **`docs/guidelines.md` §5 step 8 still describes one generated section.** Lines 519–520 and
   699 say `log-index.mjs` regenerates "the build log's list from the directory". The README and
   SKILL.md were corrected in this diff; guidelines is a versioned document the ticket's Spec line
   does not cite, and patching it cuts a new version — §4's "the human owns a spec upstream".
   Filed at Backlog. Owner: whoever takes that task; the action guidelines prescribes is still
   right, so nothing is broken meanwhile.
3. **`parseHeaderVersion`'s six-line header window is under-tested in `version-drift.mjs`.**
   Narrowing `HEADER_LINES` from 6 to 1 reddens nothing in either script: every document's version
   sits on its first line today, and the fixture that checks a version is not found below the
   header uses eight blank lines. Setting it to 0 reddens eleven tests, so the window is shared and
   load-bearing — it is the boundary that is unpinned, not the value. Pre-existing and not this
   ticket's, recorded because AC5 made the two scripts depend on it together. Owner: T-next.
4. **AC1's "and commit" is dead text under the entry convention.** SKILL.md tells every run to
   leave the commit off the entry's stamp, because the commit is the board row's. So the Newest
   entry line renders without one for every entry the pipeline writes; only the migrated entries
   carry one. The code prints a commit where an entry has it. Nothing to change unless the
   convention does.
