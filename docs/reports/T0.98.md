# T0.98 — report

Smoke A: `scripts/run/README.md`. The first ticket claimed, drafted, built, reviewed and
closed by `/ticket` (T0.8) from a one-line body. Epic E0.2 Pipeline · Type Technical.

## Files added

`scripts/run/README.md` · `scripts/run/readme.test.mjs` · `docs/tickets/T0.98.md` ·
`docs/reports/T0.98.md`. Changed: `docs/build-log.md` (one line under Tickets done).

## ACs implemented

| AC | Where | Test |
|---|---|---|
| AC1 ten step sections `0 Preflight` … `9 Close`, in order, one paragraph each | `scripts/run/README.md` | TC1 — `readme.test.mjs` "has the ten steps … in order" and "has exactly one paragraph under each step" |
| AC2 every non-test, non-`cli.mjs` script named by file name | `scripts/run/README.md` steps 0, 1, 2, 3, 6 | TC2 — `readme.test.mjs` "names every step script by file name" |

## Tests written, each observed red first

`scripts/run/readme.test.mjs` — 3 tests. First run before the README existed: the file
failed to load (`ENOENT` on `README.md`), one file failed, no tests ran. Then, with the
README in place and all three green, three mutations of `README.md`, each restored
byte-identical afterwards (`diff` against the backup: identical):

| mutation | test that went red |
|---|---|
| `## 9 Close` renamed to `## 9 Closing` | "has the ten steps of guidelines §5 as sections, in order" — 1 failed, 2 passed |
| a blank line inserted inside step 4's paragraph | "has exactly one paragraph under each step" — 1 failed, 2 passed |
| `` `branch.mjs` `` replaced by "the branch script" | "names every step script by file name" — 1 failed, 2 passed |

Each mutation failed exactly the test that names it and no other.

## Reviewer passes and findings

**Pass 1** — FINDINGS, one Must, three Should.

1. Must — the build-log line was in the working tree, not in the commit the report claimed
   it in. Fixed: committed as `build log: T0.98` on the branch, the shape T0.7 used.
2. Should — two `SignInForm.dom.test.tsx` cases time out under the full parallel suite and
   pass alone; out of scope → Backlog task, Type Fix (see open questions).
3. Should — `join(import.meta.dirname)` with one argument. Fixed: `import.meta.dirname`.
4. Should — "exports a pure function" overstated `branch`, `merge-detect`,
   `migration-check`, which spawn git with the runner injected. Fixed: the README now says
   "a function — its effects, where it has any, injected".

_(pass 2 to follow)_

## Changed since this ticket was cut

Nothing. The task arrived with no Spec; the run set `guidelines v1.3 §5` at claim and
`version-drift.mjs` reports no drift against the repo header.

## Open questions

- Branch base. `origin/main` is at `bb41b25`; T0.8 (`7136df3`), which holds the `/ticket`
  skill and every script the README describes, is on `t0-8` and not yet merged. The run
  branched `t0-98` off `t0-8` (`AENIMA_RUN_BASE`, the override `branch.mjs` exists for) so the
  PR's diff against `main` will show T0.8 too until T0.8 lands. Owner: the human merging T0.8
  first, then T0.98.
- Load sensitivity, not this ticket's: two `SignInForm.dom.test.tsx` cases (`reachCodeStep`,
  `findByRole` at its default timeout) went red under the full parallel `pnpm test` — once for
  the reviewer, once for the Stop gate — and pass 20/20 alone. The Stop hook runs the suite
  exactly that way, so a red there can refuse a close this ticket did not cause. Filed as a
  Backlog task, Type Fix, Epic E0.2, per §5 step 5. Owner: that task.
- The guard hook refused the Bash heredoc that first wrote the README because the README's
  step 6 named the migrate command literally; the file went in through the Write tool and the
  sentence now says "the migrate command". A guard that reads a command string cannot tell a
  mention from an invocation — noted for the guard's owner, not changed here.
