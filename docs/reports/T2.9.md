# T2.9 — report

Applicability stability: the pack's three applicability conditions — `list-rendering-surface`,
`network-dependent-surface`, `user-to-user-or-location` — now carry probes, §4's "condition may
carry probes the way a check does" (v1.8, this ticket — v1.7 at the first round, renumbered in the
addendum round because T0.19 took v1.7 on `main` first), rendered under the condition in the
prompt's CONDITIONS block and decided by the protocol's one new paragraph: by the probes and by
nothing else, holding when the artifact answers yes to at least one. Measured the way the ticket
asked: eleven runs of `score:file` on `sample-juno-feature.md` after, against T2.8's arm P before.
**All three conditions held one state across the eleven, and the score's spread went from 0.3
to 0.0.** Pack 1.1.0 → 1.2.0, protocol 1.2.0 → 1.3.0, one re-baseline. Epic E2.1 Scoring engine ·
Priority Should · Type Fix. Branch `t2-9` off `origin/t2-8` at `fdd8cbd`, because the probe
mechanism this builds on is T2.8's and T2.8 is at Review, not on `main` (see *Changed since this
ticket was cut*). Claimed by the scheduled run on 2026-09-14 in its own worktree; sent back from
Review on 2026-09-16 to be brought up to `main` and closed, which the *Addendum round* below does.

**What the measurement says, in one table.** Same document, same bytes (`soc-13`, one
`content_hash`), same model, same effort pin; the only thing that changed between the arms is
what the scorer reads to decide the conditions.

| | arm P — T2.8, pack 1.1.0, protocol 1.2.0 | arm C — this ticket, pack 1.2.0, protocol 1.3.0 |
|---|---|---|
| runs | 11 · 2026-09-14 09:17–09:29 UTC (`score:spread 2026-09-14T09:16:22Z 2026-09-14T09:30:07Z`) | 11 · 2026-09-14 12:56–13:07 UTC (`score:spread 2026-09-14T12:54:49Z 2026-09-14T13:08:00Z`) |
| score | min 4.8 · max 5.1 · median 5.1 · **spread 0.3** | min 4.8 · max 4.8 · median 4.8 · **spread 0.0** |
| denominator | 99 in 8 · 105 in 3 | **105 in 11** |
| `list-rendering-surface` | **held in 3 of 11** | held in 11 of 11 |
| `network-dependent-surface` | held in 11 of 11 | held in 11 of 11 |
| `user-to-user-or-location` | held in 11 of 11 | held in 11 of 11 |
| checks holding one state | 19 of 20 (`prd-15` not asked 8 / fail 3) | **20 of 20** (`prd-15` fail 11) |
| cost | $0.060 and ~63 s per scored run | $0.73 over 11 calls, $0.05–$0.09 and ~72 s per scored run; no call refused |

**The condition settled, and it settled to yes.** Arm P leaned the other way — the model decided
the feature renders a list in three runs of eleven — and the probe's first question names what a
list surface is ("a feed, a list, a table, search results, a history, an inbox, a gallery … Name
the screen") and what it is not ("a fixed set of choices, one item's detail view, a form, a prompt
or a notification"). Read against that, the scorer finds the same thing every run: the place
picker's venue search is search results drawn from data, and `prd-15`'s note names it — "The
place-picker venue search is a data-drawn list surface, but no empty-results or first-use state is
described for it." Whether yes is the *correct* answer for this document is the golden set's
question, not this ticket's (open question 1); what this ticket owed was one answer, and it has
one. The other two conditions held every run in both arms on this document, and carry probes all
the same: open question 22 in the build log is the safety layer entering one run's denominator
and not the next's on a different document, and a condition that can wobble is a Must asked or not
asked on the scorer's mood.

**Nothing else moved.** `schema.ts`'s conditions half is the same object of booleans, `readAnswer`
and `run.ts` are untouched, the stored run's shape is what it was, and the denominator is still
arithmetic in `src/packs/scoring.ts`. The five checks' probe sets from T2.8 are unchanged, so arm
P remains a measurement of the shipped pack's checks and arm C differs from it in the conditions
alone.

## Addendum round — bring t2-9 up to main and close

T2.8 merged into `main` on 2026-09-15 (pull request #11, `ec531f3`), and T0.21 (`cbdc9aa`)
replaced the gates round one closed under; pull request #13 still targeted `t2-8` and GitHub
read it as `CONFLICTING`. The board's addendum asked for the merge, the retarget and the close,
and nothing else: the measurement stands and is not re-run. Claimed by the scheduled run on
2026-09-16.

| Item | Where |
|---|---|
| Merge `origin/main` into `t2-9` | merge commit `79ca9f0`, `origin/main` at `46c7f7c`; two files conflicted, `docs/product-spec.md` and `docs/build-log.md` |
| `docs/product-spec.md`: main's text kept, this ticket's change re-applied | main's v1.7 changelog line (T0.19, the comment path) kept whole; this ticket's line re-applied after it as **v1.8**, and the title is v1.8. The §4 sentence merged without conflict and is unchanged. The addendum's standing decision says the record "stays at v1.7"; v1.7 is T0.19's on `main`, so the number is the next free one — the renumbering T0.12 and T0.13 did when `main` took theirs first. A default, said in the claim's one comment |
| The build-log list regenerated, never hand-merged | main's side of the list taken, then `node scripts/run/log-index.mjs` (`written: true`, 42 entries); open question 22's answer, which merged without conflict, now names v1.8 |
| The v1.7 citations this ticket owns | the code comments citing §4 in `src/packs/{types,feature-prd,feature-prd.test,validate,validate.test}.ts` and `src/lib/scoring/prompt{,.test}.ts` (`e2a034e`), the ticket file's Build line, AC4 and TC4, this report and the log entry. Comments only: the fingerprint pin still passes, so nothing the scorer reads moved |
| Pull request #13 targets `main` | `gh pr edit 13 --base main`. `t2-8` was merged but its branch is still on origin, so GitHub had not retargeted it on its own as round one expected |
| The ticket file | an `## Addendum` section with the board's addendum verbatim, its AA1–AA3 and TA1–TA3, and how this round reads the number |

## Build items, as shipped

| Item | Where |
|---|---|
| Probes on the three conditions — questions the artifact answers yes or no, a yes meaning the condition holds: for the list surface, a data-drawn set of items of one kind that could be empty, and scrolling, sorting, filtering, paging or a no-results state; for the network surface, a refusable permission, and a step that needs the network at the moment of use; for the safety surface, seeing another person, acting on another person, and the person's location | `src/packs/feature-prd.ts` `probes` on `LIST_SURFACE`, `NETWORK_DEPENDENT`, `SAFETY_SURFACE`; version `1.2.0`; the header says why |
| A condition may carry probes the way a check does — plain text, no points, no id, no verdict of its own; an empty one refused at load | `src/packs/types.ts` `ApplicabilityCondition.probes`; `src/packs/validate.ts` |
| The scorer is told once how a probed condition is decided — by its probes and by nothing else, holding when the artifact answers yes to at least one — and reads each probe under its condition | `src/lib/scoring/prompt.ts` `PROTOCOL` (one paragraph), `renderPack`'s CONDITIONS block, `PROTOCOL_RELEASE` 1.3.0, `FINGERPRINT_PACK`'s `f-condition` carries a probe so a change to how condition probes render moves the digest |
| product-spec §4 records that a condition may carry probes, decided by its probes and by nothing else | `docs/product-spec.md` v1.8 (v1.7 at the first round): the H1, one sentence in §4, one changelog line naming open question 22 |
| Measured with `pnpm score:spread`, before and after | the table above; `docs/log/T2.9.md` |

## Files added and changed

Added: `docs/tickets/T2.9.md` · `docs/reports/T2.9.md` · `docs/log/T2.9.md` · `docs/reviews/T2.9.md`
(the addendum round's verdict, written by the reviewer).

Changed: `docs/product-spec.md` (v1.8) · `src/packs/types.ts` (`ApplicabilityCondition.probes`) ·
`src/packs/feature-prd.ts` (the three probe sets, 1.2.0, header) · `src/packs/feature-prd.test.ts`
· `src/packs/validate.ts` and `validate.test.ts` (the empty-condition-probe rule) ·
`src/lib/scoring/prompt.ts` (protocol paragraph, `renderPack`, release, fingerprint fixture) ·
`src/lib/scoring/prompt.test.ts` (the new pin, the rendering test) · `docs/build-log.md` (open
question 22 answered; Tickets done regenerated).

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC1 each of the three conditions holds one state across 11 runs on `sample-juno-feature.md`, or the report says which did not and why | TC1 | `pnpm score:spread` over arm P (`2026-09-14T09:16:22Z`–`09:30:07Z`) and arm C (`2026-09-14T12:54:49Z`–`13:08:00Z`): the table above. All three held in 11 of 11. None did not. |
| AC2 the pack version and `PROTOCOL_VERSION` each move exactly once, so the cache re-baselines once | TC2 | `src/lib/scoring/prompt.test.ts` "pins the fingerprint of everything the scorer reads": `1.2.0+6476334ca2123595` → `1.3.0+1ef28b5d617ff4a0`, observed red then pinned; `src/packs/feature-prd.test.ts` pins the pack at `1.2.0`. `findRunForVersion` keys on both (`run.ts`), and each moved once on this branch, in one commit. Arm C's eleven runs carry one protocol and one pack version (`score:spread` printed no two-version warning). |
| AC3 a condition's probes reach the scorer beneath the condition, a condition without probes renders as before, and an empty probe is refused at load | TC3 | `src/lib/scoring/prompt.test.ts` "renders a condition's probes beneath it, and nothing beneath a condition without"; `src/packs/feature-prd.test.ts` "carries probes on all three conditions, at 1.2.0"; `src/packs/validate.test.ts` "rejects an empty condition probe" |
| AC4 product-spec §4 says a condition may carry probes, at v1.8 (v1.7 at the first round) | TC4 | `node scripts/run/version-drift.mjs "product-spec v1.6 §4, §5 · build-log OQ22"` → `cited 1.6, repo 1.8, drifted: true` (repo 1.7 at the first round); the sentence is `docs/product-spec.md` §4, "A condition may carry probes the way a check does (§5) …" |
| AA1 `t2-9` merges cleanly into current main and the pull request targets `main` | TA1 | `git merge-tree --write-tree --name-only origin/main HEAD` exits 0 with no conflicted file; `gh pr view 13 --json baseRefName` → `main`, head `2ae25f6` at review |
| AA2 the full suite and the gate are green on the merged tree | TA2 | `pnpm lint && pnpm typecheck && pnpm test` on the merged tree: 95 files, 1375 tests passed, one lint warning in `scripts/run/loosening.mjs`, which is main's; TC2–TC4 re-run against v1.8; `scripts/run/log-index.test.mjs` 12 passed; the gate's green for the pushed commit is recorded at close by step 9 |
| AA3 the close follows §5 step 9 as it reads on main today | TA3 | `node scripts/run/gated.mjs` and the reviewer's `PASS` in `docs/reviews/T2.9.md`, then step 9 — the board's record of this close |

## Tests written, each observed red first

The three new tests and the changed expectation were written before the code they test and run
together against the tree without it; the count is the three files' together
(`src/lib/scoring/prompt.test.ts`, `src/packs/feature-prd.test.ts`, `src/packs/validate.test.ts`),
34 passing before this ticket. The last row is the existing pin, which the protocol edit turned red
on its own — that red is TC2's observation.

| test | holds | reddened by | red → green |
|---|---|---|---|
| `src/lib/scoring/prompt.test.ts` "renders a condition's probes beneath it, and nothing beneath a condition without" | AC3 | `renderPack` before it printed a condition's probes — the field did not exist on `ApplicabilityCondition` and nothing rendered beneath a condition | 4 failed / 33 passed → 1 failed / 36 passed (the pin, last row, still red) |
| `src/packs/feature-prd.test.ts` "carries probes on all three conditions, at 1.2.0" | AC3, AC2 | the pack at 1.1.0 with no probes on any condition | 4 failed / 33 passed → 1 failed / 36 passed (the pin, last row, still red) |
| `src/packs/feature-prd.test.ts` "carries probes on the five sufficiency checks and on no other" (expectation moved 1.1.0 → 1.2.0) | AC2 | the pack still at 1.1.0 | 4 failed / 33 passed → 1 failed / 36 passed (the pin, last row, still red) |
| `src/packs/validate.test.ts` "rejects an empty condition probe" | AC3 | `validatePack` without the condition-probe rule, which returned no problem for `["Does it render a list?", ""]` | 4 failed / 33 passed → 1 failed / 36 passed (the pin, last row, still red) |
| `src/lib/scoring/prompt.test.ts` "pins the fingerprint of everything the scorer reads" (TC2) | AC2 | the protocol's condition paragraph, `renderPack`'s condition-probe lines and the fixture's condition probe: the digest moved from `1.2.0+6476334ca2123595` to `1.3.0+1ef28b5d617ff4a0` against the old pin | 1 failed / 36 passed → 37 passed |
| TA1 `git merge-tree --write-tree --name-only origin/main HEAD` (addendum round) | AA1 | run at claim as `git merge origin/main` on `t2-9` at `6ec997b`, with `origin/main` at `46c7f7c`; `gh pr view 13` → base `t2-8`, `mergeable: CONFLICTING` | `CONFLICT (content)` in `docs/build-log.md` and `docs/product-spec.md` → exit 0, no conflicted file; base `main` |
| TC4 `version-drift.mjs "product-spec v1.6 §4, §5 · build-log OQ22"` (addendum round) | AC4 | run at claim on `main` at `46c7f7c`, where v1.7 is T0.19's | `cited 1.6, repo 1.7` → `cited 1.6, repo 1.8, drifted: true` |
| TA2 `scripts/run/log-index.test.mjs` (addendum round) | AA2 | main's `docs/build-log.md` at `46c7f7c` put back over the regenerated list, with `docs/log/T2.9.md` present | 2 failed / 10 passed (*matches docs/log/ entry for entry*, *has one list line per file*) → 12 passed |
| TA2 the full suite on the merged tree | AA2 | not reddened: the round changes no code the suite reads beyond comments, so there is no mutation that belongs to it; the row records the observed run | `pnpm lint && pnpm typecheck && pnpm test` exit 0, 95 files, 1375 tests passed |
| TA3 the close under step 9 | AA3 | not a check that runs before the close: the task sat at Review under the pre-T0.21 gate with pull request #13 against `t2-8` | Review → the board's record of this close |

## Reviewer passes and findings

### Addendum round (2026-09-16)

**Pass 1 — PASS**, at `2ae25f6` against `origin/main` at `46c7f7c`; verdict in
`docs/reviews/T2.9.md`. No Must, six Shoulds. The reviewer checked the merged diff against every
Build bullet and the Rules, matched AC1–AC4 with TC1–TC4 and AA1–AA3 with TA1–TA3, ran the three
named test files (37 passed) and `scripts/run/log-index.test.mjs` (12 passed), TC4 (cited 1.6,
repo 1.8), `git merge-tree` (exit 0), `gh pr view 13` (base `main`, head `2ae25f6`),
`gated.mjs` and `report-check.mjs` (both ok).

The first invocation ended before it read anything — the reviewer's pinned model, Fable, out of
usage credits (HTTP 429), the case the board's Ready task *T0.22 Reviewer model fallback* covers.
It was invoked again with the same one-line message on Opus, the fallback `.claude/settings.json`
names; that pass reached its 30-turn limit without a verdict and was resumed with one line asking
it to continue and write the verdict file.

1. Should — the report and the log entry still described round one: v1.7, the pull request
   against `t2-8`, no AA rows and no section for this round, and the first four red-first rows
   ending at 36 passed while the pin was still red. Settled: this report and `docs/log/T2.9.md`.
2. Should — v1.8 departs from the words of the addendum's standing decision, "stays at v1.7";
   the reviewer reads the renumber as correct since v1.7 is T0.19's on `main`, and says the Owner
   should see it. Said in the claim's one `default` comment on the board row.
3. Should — `src/packs/feature-prd.test.ts:116–118` asserts the pack version a second time under
   T2.8's comment, which the new test at `:140` already covers. Not taken: a test edited after the
   PASS lands unreviewed, and the duplicate costs nothing but a line. Recorded.
4. Should — `src/packs/validate.ts:78` validates only the first occurrence of each condition id,
   so a hand-edited pack could carry a second copy of a condition with empty or different probes
   that loads silently and never reaches the prompt. The Feature PRD pack shares one constant per
   condition and is unaffected. Not taken after the PASS; open question 6.
5. Should — `docs/build-log.md` split the code span `list-rendering-surface` across a line break.
   Settled: the span is on one line.
6. Should (outside this ticket) — `scripts/run/review-scope.mjs:24` reads only `## Tests`, so the
   addendum's `scripts/run/log-index.test.mjs` was not in the reviewer's scope and was run by hand.
   Filed at Backlog as *Scope reviews to addendum-named tests* (Fix, E0.2 Pipeline, Medium),
   https://app.notion.com/p/3dd79dafd42e8158a000da177b1b9658.

Settlements 1 and 5 change this report, the log entry and the build log only, after the verdict.

### Round one (2026-09-14)

**Pass 1** — two Musts, five Shoulds; verified the diff against every Build bullet, ran the three
named test files (37 passed) and TC1 off the ledger while the arm was still being scored.

1. Must — AC1 claimed eleven runs and the ledger held three at the time of reading. The arm was in
   flight; it finished at 13:07:57Z with eleven, and the table above carries the exact window
   beside arm P's.
2. Must — no red-first record was visible for the four new or changed tests. The record is the
   table above, written where step 8 puts it.
3. Should — build-log open question 22 still read "Candidates, none chosen" while the spec's v1.7
   line cited it as the cause. Rewritten: the answer, this ticket, and the after-arm figures, with
   the three candidates kept as they stood.
4. Should — T2.8 assigned three things to "the next ticket that moves `PROTOCOL_VERSION`" and this
   ticket moved it without carrying them. Correct under the ticket's own Rules and CLAUDE.md's
   scope rule, and "T-next" was false as written: re-pointed to a named Backlog task (open question
   2).
5. Should — TC4 as written invoked `version-drift.mjs` with no argument and would have printed an
   empty list. The invocation is now in the ticket file, the board body and AC4's row.
6. Should — the ticket file's Cited section cut open question 22 off mid-sentence. Completed.
7. Should — the branch is off `origin/t2-8`, and a pull request against `main` before T2.8 merges
   reviews T2.8 twice. The pull request is opened against `t2-8`; GitHub retargets it to `main`
   when `t2-8` merges and its branch is deleted. Said under *Changed since this ticket was cut*.

**Pass 2** — no Must, one Should, taken; verified every hunk of `fdd8cbd...HEAD` against the
Build bullets and the Rules, the criteria both ways, all five red-first rows, TC1 off the ledger
for both arms cell for cell, and that both halves of the cache key moved in one commit.

1. Should — the sentence introducing the red-first table said 33 tests passed before this ticket;
   the three files hold 34 at `fdd8cbd`, and 33 is the count after the changed expectation went
   red. Corrected to 34.

## Defaults taken under §4

- **Where this builds.** The ticket's Rules say a probe is the cure and say nothing about where the
  probe mechanism comes from; it is T2.8's, at Review with `merge` said and the guard not yet
  letting it through (the run's step 0 found why — a pipeline defect filed at Backlog, *Read board
  Status as select*). Branching off `t2-8` rather than `main` costs nothing if T2.8 merges as it
  stands and one rebase if it does not. Said in one `default` comment on the board row.
- **A condition may carry probes, and §4 says so.** T2.8 named this a spec question for the next
  ticket; the ticket's own Rules line answers it — "the condition oscillating is the scorer having
  no settled answer, which is what a probe addresses" — and §4 of the guidelines puts the record
  in the spec, not in a comment. One sentence in §4 at v1.7, in the same `default` comment.
- **A probed condition holds on any yes.** The protocol says so because §4's conditions are
  disjunctions — "user-to-user visibility, interaction, or location" — so each probe asks about one
  surface and a yes to any of them is the condition. The alternative, every probe answered yes,
  would read a feature with location but no interaction as having no safety surface.
- **The five checks' probes stay as T2.8 shipped them.** Any change un-measures arm P and the
  ticket's Rules keep the checks' probes out of scope; the rule-shaped probes T2.8's reviewer
  found ride with open question 2.
- **An empty condition probe is refused at load.** The same rule T2.8 wrote for a check's probes,
  one loop further.

## Changed since this ticket was cut

**At the addendum round's claim** `version-drift.mjs` reported `cited 1.6, repo 1.7, drifted:
true` — v1.7 being T0.19's comment path on `main`, not this ticket's line. This ticket's §4
change is v1.8 on top of it (*Addendum round*). T2.8 merged into `main` on 2026-09-15, and T0.21
replaced step 9's gates with `loosening.mjs`'s measurement, so this round closes under step 9 as
`main` reads it today. The expectation below that GitHub would retarget the pull request on its
own did not hold — `t2-8` is still on origin — and the retarget was made by hand.

**At round one:**

`version-drift.mjs` reported nothing drifted at claim: product-spec v1.6 was the cited and the
repo's version. This ticket itself moved it to v1.7, and every later ticket cut against v1.6 §4 or
§5 will see that drift reported — the sentence is additive and changes no other line of either
section.

The branch is off `origin/t2-8` at `fdd8cbd`, not `origin/main` (guidelines §5 step 3), so
`git diff main...HEAD` carries T2.8's six source files until T2.8 merges. The pull request is
opened against `t2-8` so it reviews this ticket alone; when `t2-8` merges with a merge commit and
GitHub deletes its branch, the pull request retargets to `main` on its own, and the board's Commit
for this ticket is an ancestor of `main` from that merge on, which is what `merge-detect.mjs`
reads.

## Open questions

1. **Settled is not the same as right — and this time the settled answer flipped.** Arm P decided
   the feature renders a list in 3 of 11; arm C in 11 of 11, on the venue search. The probe's own
   example list contains "search results", and a places-API search is one. If the Owner reads
   §7.2 row 15's "list-rendering surfaces" as feeds and tables and not a picker's results, the
   example comes out of the first probe and the condition is re-measured. The golden set (build-log
   *On the horizon*, the scorer eval harness) is what grades it. Owner: the eval-harness ticket.
2. **The deferred protocol rules did not ride with this move.** T2.8 assigned three things to the
   next ticket that moves `PROTOCOL_VERSION`: build-log open question 29's quoting rule and open
   question 23's passing-check citation (T2.8 report, open question 5), and the five rule-shaped
   probes plus the `prd-16` and `prd-19` shapes (T2.8 report, open question 6). This ticket was
   that move and carried none of them — its Rules keep the checks' probes unchanged, and CLAUDE.md
   forbids widening a ticket — so "T-next" is now a Backlog task drafted by this run, *Carry the
   deferred protocol rules* (Epic E2.1, Type Fix, as §5 step 5 types a finding's task), which lists all three and pays the next
   re-baseline once. Owner: that task.
3. **Every stored meter re-scores a second time today.** Both halves of the cache key moved again
   (pack 1.2.0, protocol 1.3.0), so every scored item calls the provider on its next open. On this
   document the number moves from 5.1 to 4.8 — the list condition now holds, `prd-15` is asked and
   fails, the denominator is 105. That is §5's re-baseline working as written, and this report is
   the explanation. Owner: none needed.
4. **The other two conditions were stable on this document before their probes and after.** Their
   probes are measured only in the sense that they did not unsettle anything; the safety layer's
   wobble in open question 22 was on a different document (`soc-10`/`soc-11`), which
   `sample*.md` gitignoring means this run could not re-score. A document written with an
   ambiguous safety surface is the measurement that would show the third probe set doing work.
   Owner: the eval-harness ticket, whose golden set carries such a document.
5. **~~Found in this run's step 0, not this ticket's~~ — settled by T0.16 (`1544c5f`), which reads
   the board's Status as the select it is.** Found in round one's step 0: the run reads every task's Status as null
   because the board's column is a `select` and the reader expects a `status` type, so a `merge`
   said on a task at Review is refused by the guard with "at no status". Filed at Backlog as *Read
   board Status as select* (Type Fix, Epic E0.2). Until it is fixed, T2.8's and this ticket's merges
   are by hand or by that fix. Owner: that task.
6. **A second copy of a condition id is not validated** (addendum round, reviewer Should 4).
   `validatePack` checks the probes of the first `appliesWhen` carrying each condition id, and
   `packConditions` keeps that first copy, so a later check carrying the same id with empty or
   different probes loads without complaint and its probes never reach the prompt. No pack does
   this today — the Feature PRD pack shares one constant per condition. The fix is to refuse a
   condition id that appears with a different `when` or `probes`. Owner: the next ticket that
   touches `src/packs/validate.ts` (E2.1).
