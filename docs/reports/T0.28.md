# T0.28 — Number task IDs across the phase

`next-id.mjs` counted within the epic. Two epics in one phase counted past each other, so an
ID that already named a task was answered to a second one: E0.4 Design conformance was answered
T0.1, the scaffold ticket's, and E0.1 Foundation is answered T0.7, E0.2's Setup. An ID names a
branch and a file under `docs/` on main, so the second task overwrites the first's record rather
than colliding anywhere a person would see it. R-0062 stopped at claim on the first of these.

The Epic now gives the phase and nothing else. The number is the lowest in that phase nobody
holds, and holding it is a task on the board whatever its epic and whatever its status, or a
branch or a file under `docs/` that still carries it — so a number outlives the task it was given
to, and no epic owns a block.

## ACs implemented

| AC | What holds it | Test |
|---|---|---|
| AC1 A claim in an epic with no numbered task answers the lowest free number in its phase: with T0.1–T0.27 and T0.96 taken, T0.28 | `nextId` walks up from 1 over a `Set` of every used number in the phase, rather than `Math.max(...used) + 1` over the epic's | `scripts/run/next-id.test.mjs` — "answers the lowest free number in the phase for an epic holding none", "fills a hole the board left rather than counting past it", "starts at 1 in a phase with no numbered task anywhere" |
| AC2 A claim in an epic whose next number another epic holds skips it: an E0.1 claim answers T0.28, not T0.7 | `nextId` takes `taskNames` as the whole board and filters on the phase alone; the epic is read only by `phaseOf` | `scripts/run/next-id.test.mjs` — "skips a number another epic in the phase holds", "counts only the phase's own numbers"; `scripts/run/pick-next.test.mjs` — "prints every task's name, blocked and Done ones included", "prints them when asked among a few, which reads the same board" |
| AC3 A number carried only by a branch or a docs file, with no task on the board, is not answered | `repoTaken` reads `git branch -a` and the `docs/` tree and hands the numbers to `nextId` as `takenIds`; the CLI composes the two | `scripts/run/next-id.test.mjs` — "does not answer a number carried only by a branch or a docs file"; `branchIds` (3), `docsIds` (3), `repoTaken` (4), "counts what the checkout carries, not only the names it is handed" |
| AC4 `docs/guidelines.md` §7 states the rule, and its version is bumped | §7 carries the rule as its own paragraph; the header is v1.20, and §5 step 1 names the read | `scripts/run/next-id.test.mjs` — "states under §7 that a number is the phase's and never used twice", "carries the bumped version in its header" |

## Tests written, each observed red first

Seventeen tests in `next-id.test.mjs` and two in `pick-next.test.mjs`. The counts are that file's
own, run alone.

| test | reddened by | red → green |
|---|---|---|
| `nextId` "answers the lowest free number in the phase for an epic holding none" (TC1) | the shipped per-epic count: `Math.max(...used) + 1` over the epic's names alone | 14 failed / 5 passed → 19 passed |
| `nextId` "skips a number another epic in the phase holds" (TC2) | the same — it answered T0.7, E0.2's Setup | 14 failed / 5 passed → 19 passed |
| `nextId` "does not answer a number carried only by a branch or a docs file" (TC3) | `nextId` took no third argument; the call threw on arity in spirit and answered T0.28 in fact | 14 failed / 5 passed → 19 passed |
| `nextId` "fills a hole the board left rather than counting past it" | the per-epic count answered T3.5 over a board holding 1, 3, 4 | 14 failed / 5 passed → 19 passed |
| `nextId` "starts at 1 in a phase with no numbered task anywhere" | — (held under both rules; kept as the floor) | 14 failed / 5 passed → 19 passed |
| `nextId` "counts only the phase's own numbers" | the per-epic count answered T3.1 too; kept as the phase filter's own test | 14 failed / 5 passed → 19 passed |
| `nextId` "refuses to invent a phase when the epic name carries none" | — (unchanged behaviour, kept as the seam) | 14 failed / 5 passed → 19 passed |
| `nextId` "reads a two-digit number as one number, not as its first digit" | `idOf`'s `\b` removed, so `T0.98` reads as 9 and the answer moves from T0.9 to T0.10 | 1 failed / 23 passed → 24 passed |
| `phaseOf` (1) and `idOf` (1) | — (carried from the shipped suite, unchanged) | 14 failed / 5 passed → 19 passed |
| `branchIds` (3): a ticket branch, a renamed one, a branch that is not a ticket's | `branchIds` did not exist — `TypeError: branchIds is not a function` | 14 failed / 5 passed → 19 passed |
| `docsIds` (3): a docs file's name, a two-digit number, a document that is not a ticket's | `docsIds` did not exist | 14 failed / 5 passed → 19 passed |
| `repoTaken` (4): branches and docs together, git refusing, no docs tree, a docs tree that cannot be read | `repoTaken` did not exist; the three `unread` arms were then reddened by the `{ ids, unread }` shape not existing | 14 failed / 5 passed → 19 passed, then 1 failed / 23 passed → 24 passed |
| `next-id.mjs as the command` "counts what the checkout carries, not only the names it is handed" | the fixture's own unborn branch: with `git init` and no commit, `git branch -a` lists nothing, so the answer was T0.2 rather than T0.4 — the composition, not the parts | 1 failed / 23 passed → 24 passed |
| `next-id.mjs as the command` "carries the epic's refusal through, with nothing unread" | the CLI emitted `nextId`'s value alone, with no `unread` key | 1 failed / 23 passed → 24 passed |
| `docs/guidelines.md` "states under §7 that a number is the phase's and never used twice" (TC4) | `git checkout origin/main -- docs/guidelines.md` | 2 failed / 19 passed → 21 passed |
| `docs/guidelines.md` "carries the bumped version in its header" (TC4) | the same revert: `parseHeaderVersion` read 1.19 | 2 failed / 19 passed → 21 passed |
| `readPick` "prints every task's name, blocked and Done ones included" | `pick-next.mjs` restored to `HEAD`'s copy, which emits no `names` | 3 failed / 21 passed → 24 passed |
| `readPick` "prints them when asked among a few, which reads the same board" | the same restore | 3 failed / 21 passed → 24 passed |
| `readPick` "says so, and picks nothing, when the token is not in .env.local" | the same restore, on the added `names: []` arm | 3 failed / 21 passed → 24 passed |

Whole suite at close: **118 files, 1696 tests, all passing** (1691 before the pass-1 fixes).

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `7177853` | opus | no | FINDINGS |

Pass 1 was first called on `fable`, the model `.claude/agents/reviewer.md` pins, and came back
*You're out of usage credits* — a refusal, not a review. `review-model.mjs` read it as `credits`
and named `opus`, the chain's next; the pass ran there, in a fresh session with the same one-line
message.

**Pass 1 — one Must, six Shoulds.**

- **Must 1 — no test on the branch was recorded as observed failing first.** True as the reviewer
  read it: `docs/reports/T0.28.md` did not exist yet, so the record existed only in this session.
  Fixed by this report's table above, one row per test.
- **Should 1** — "reads a two-digit number as one number" asserted `T0.1` and `T0.2`, which
  reading `T0.98` as 98, 9 or 8 all satisfy: a test that cannot fail for the reason its name
  gives. Fixed — with T0.1–T0.8 taken the answer is T0.9, and reading 98 as 9 moves it to T0.10.
- **Should 2** — dropping `repoTaken()` from the CLI left all 21 tests green: `nextId` and
  `repoTaken` were covered, their composition was not. Fixed — two tests spawn the script against
  a real checkout, and the first of them found the unborn-branch fixture bug above.
- **Should 3** — two tests were tagged `TC1 → AC1` for a criterion they make no claim about.
  Fixed: both now carry what they are for.
- **Should 4** — a failed `git branch` and a `catch` around `readdirSync` each narrowed the taken
  set with no word in the output, and a narrowed read can answer a number a `docs/` file still
  carries: the repeat this ticket exists to stop. Fixed — `repoTaken` returns `{ ids, unread }`,
  the CLI prints `unread`, and a checkout with no `docs/` tree at all is not an unread one.
- **Should 5** — the skill and §5 step 1 asked for "every task name on the board" and no script
  printed one. Fixed — `pick-next.mjs` already reads every row, so `readPick` now returns `names`
  and the skill reads `next-id` from it. No second pass over the board.
- **Should 6** — §7 ended with the history the Build asked to keep to one sentence. Fixed: the
  rule stands alone and the history lives in `next-id.mjs`'s docstring and in the log.

## Changed since this ticket was cut

Nothing. `version-drift.mjs` over the Spec line — guidelines v1.19 §5 step 1, §7 — reported the
repo at v1.19, no drift. This ticket takes it to v1.20.

## Open questions

None blocking.

