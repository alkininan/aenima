# T0.14 — report

Ignore worktrees in lint: `eslint .` from a checkout walked into `.claude/worktrees/` and linted
other sessions' generated `.next/types`, so a gate run from the primary went red while any
worktree held a build. One line in `eslint.config.mjs` — `.claude/worktrees/**` in the
`globalIgnores` block, mirroring `.gitignore:57` — and one test that loads the root config as the
gate does. Epic E0.2 Pipeline · Priority Must · Type Fix. Branch `t0-14` off `origin/main` at
`0870c08`. Claimed by the scheduled run on 2026-09-13 in its own worktree; the Spec line cites no
versions, so nothing to drift.

**Said first.** This run read no comments: `NOTION_TOKEN` is in `.env.local` but the API answers
401 to it, and the value is 17 characters long where an internal integration's token is about
fifty — a placeholder, not a token. Preflight went on without threads (skill step 0b). Open
question 1.

## Build items, as shipped

| Item | Where |
|---|---|
| `.claude/worktrees/**` added to `globalIgnores`, with a two-line comment naming `.gitignore:57` | `eslint.config.mjs:24-26` |
| The check the Build asks for: should `.next/**` also be unanchored? **No, and it stays anchored.** Every worktree Claude Code makes lands under `.claude/worktrees/` (guidelines §5, *Worktrees*), which the new line now covers whole — source and build output alike — so there is no nested `.next` left for an unanchored pattern to reach. Prettier needs nothing: it reads `.gitignore` by default, and `.claude/worktrees/` is there. The Rules line asks for one line and no other change, and one line is what this is. If a worktree ever lands outside `.claude/worktrees/`, that is a change to where Claude Code puts them, and the pattern follows it then | this table; open question 2 |
| A test of the config as the gate loads it, from the repo root | `scripts/eslint-ignores.test.mjs` |

## Files added and changed

Added: `scripts/eslint-ignores.test.mjs` · `docs/tickets/T0.14.md` · `docs/reports/T0.14.md` ·
`docs/log/T0.14.md`.

Changed: `eslint.config.mjs` (three lines: the pattern and its comment) · `docs/build-log.md`
(Tickets done regenerated).

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC1 with a worktree under `.claude/worktrees/` holding a `.next` folder, `pnpm lint` from the primary is green | TC1 | `scripts/eslint-ignores.test.mjs` "TC1 → AC1 ignores another session's worktree, build output included" — a config-level proxy for the ticket's procedure: it asks `ESLint.isPathIgnored` about `.claude/worktrees/other-session/.next/types/routes.d.ts` and `…/src/app/page.tsx` rather than creating a worktree. The ticket's procedure was also run once by hand, from this checkout (the run's worktree, so its own `.claude/worktrees/` stands in for the primary's): a `t014-probe/.next/types/probe.ts` with `var`, `any` and an unused binding made `eslint .` red before the change (2 errors, 1 warning) and `pnpm lint` exit 0 after it; the probe was removed |
| AC2 a lint error in the repository's own code is still reported | TC2 | `scripts/eslint-ignores.test.mjs` "TC2 → AC2 still reports a violation in the repository's own code" — `isPathIgnored` is false for `scripts/run/probe.ts` and `lintText` of a `var` declaration at that path reports `no-var`. By hand as well: `scripts/t014-planted.ts` with a `var` made `eslint .` exit 1 after the change; removed, exit 0 |

## Tests written, each observed red first

One test file is in the diff: `scripts/eslint-ignores.test.mjs`. TC1 was run before the config
change and went red on its own; TC2 was reddened by a mutation applied to `eslint.config.mjs`
with `sed`, the file run, the line removed and the diff checked back to the three intended
lines. The count is the file's.

| test | reddened by | red → green |
|---|---|---|
| "TC1 → AC1 ignores another session's worktree, build output included" | the config as claimed, without `.claude/worktrees/**` — the first assertion, `isPathIgnored` of `.claude/worktrees/other-session/.next/types/routes.d.ts` (line 20), is what failed: `expected false to be true` | 1 failed / 1 passed → 2 passed |
| "TC2 → AC2 still reports a violation in the repository's own code" | M1 `"scripts/**"` added to `globalIgnores` — the first assertion, `isPathIgnored` of `scripts/run/probe.ts` (line 30), is what failed: `expected true to be false` | 1 failed / 1 passed → 2 passed |

On the reviewer's note about `isPathIgnored`: it is true both for an ignored path and for one no
config's `files` pattern matches, so TC1 alone cannot tell the two apart. TC2's first assertion
is the control — a `.ts` path outside the ignores is not ignored — and the by-hand run above is
the end-to-end observation the test stands in for.

## Reviewer

One pass, no Must. Three Shoulds, all about what this report has to say, and all said above:

1. The `.next/**` check had no recorded answer → the Build items table, second row.
2. No red-first record existed at review time, since step 8 follows step 5; the reviewer asked
   that the rows name which assertion went red under each mutation → the table above does.
3. TC1 as implemented is a config-level proxy for the ticket's worktree procedure, and the report
   should say so → the AC1 row does, with the by-hand run beside it.

## Changed since this ticket was cut

Nothing. The Spec line cites `docs/guidelines.md §5`, `eslint.config.mjs` and `.gitignore:57`
without versions; `version-drift.mjs` had nothing to compare. All three were as the ticket
describes them at `0870c08`.

## Open questions

1. **`NOTION_TOKEN` is a placeholder** — owner: the human step from T0.11 (Build 3). The value in
   `.env.local` is 17 characters and the API answers 401, so no run since T0.11 merged has read a
   thread or could verify a `merge` or an `apply`. The `setup` comment on T0.11's board row says
   where the real token goes. Until it is there, a `merge` or `apply` reply on any task is not
   seen, and T0.13 at Decision is not assessed.
2. **Where worktrees land** — owner: none needed today. The ignore covers `.claude/worktrees/`
   because that is where Claude Code puts every worktree; `.next/**` stays anchored on that
   ground. If that location ever changes, `eslint.config.mjs` and `.gitignore:57` change together.
