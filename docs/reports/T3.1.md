# T3.1 — report

Author-critic loop. Epic E3.1 Authoring loop · Priority High · Type Feature · Spec `product-spec
v1.8 §6, §5, §13 · T2.3's fabrication guard · T2.2's seam`, no version drifted. Branch `t3-1` off
`origin/main` at `d79f49a`, reused from origin at `ab650d5`.

Three claims. The scheduled run of 2026-09-17 at 10:49 UTC (session `150c7be8`) wrote the ticket
pack, built objection intake and the scope wall, and stopped at Decision on three pieces of schema
no spec defined. The board's addendum answered all three by accepting the defaults, and the
scheduled run of 2026-09-17 at 11:18 UTC (session `212c9b1c`) built the addendum round and stopped
at Decision again, in step 6, as the addendum said it would: this diff adds
`drizzle/0015_refinement_round.sql`, and a run's credential cannot apply a migration. Three schema
choices in 0015 went to you with that stop (*Questions this stop puts to you*).

**This claim is the run after `apply`.** You answered `apply` on 2026-09-20 at 11:59 UTC; the
scheduled run of 2026-09-20 at 16:21 UTC (session `316c0ea5`) applied `0015_refinement_round` from
this worktree through T0.24's `apply.mjs` and carried the ticket on from where it stopped — the
first ticket to be picked back up from a granted `apply`. What waited on it is done: the nine
database tests reddened and run against Postgres, `database.types.ts` regenerated and confirmed,
and the recorded end-to-end run with its rounds stored.

## Addendum round — the three answers built

| Item | Where |
|---|---|
| AA1 a section is a `##` heading block, its id the heading's slug, a numeric suffix on collision in document order | `src/lib/authoring/sections.ts` — `parseSections` (lossless: the sections join back to the body byte for byte; text before the first `##` is the `_preamble` section; a `##` inside a code fence is code), `slugOf` (letters of every script, NFC), `spliceSection` (one section replaced, a dropped trailing line break put back and nothing else) |
| AA2 the open question is a round row marked `surfaced`; no open-question table | `refinement_outcome`'s `surfaced` (`src/db/schema/enums.ts`, `drizzle/0015_refinement_round.sql`); the loop writes it on the third objection (`src/lib/authoring/loop.ts`); `refinement_round_shape` allows it at round 3 and only there |
| AA3 one append-only row per round, unique on (artifact, section, check, round number); the count is the highest round number | `refinement_round` and `refinement_round_key` (`src/db/schema/tables.ts`, migration 0015); `roundCount` and `nextMove` (`src/lib/authoring/rounds.ts`) |
| AA4 preflight regenerates stale route types | `scripts/run/route-types.mjs` — a digest of the route files' paths, the Next config and the Next version, stamped in `.next/aenima-route-types` after a successful `next typegen`; missing, unstamped or differently stamped types are regenerated. `.claude/skills/ticket/SKILL.md` step 0, `scripts/run/README.md` 0 Preflight, `docs/guidelines.md` §5 step 0 (v1.17) |

## Build items, as shipped

| Item | Where |
|---|---|
| The two agents through T2.2's seam at the generation tier: the critic meters as a new purpose, `critique`; the author as `draft` | `src/lib/authoring/run.ts` (`seamAgents`), `src/lib/ai/types.ts` (`Purpose`) |
| What the critic sees: the checks in play, rendered in the pack's words with probes and the interview bank's critic test, and the section under refinement — no parameter exists for anything else | `src/lib/authoring/prompt.ts` (`CRITIC_PROTOCOL`, `renderChecks`, `criticRequest`) |
| An objection is four fields or it is discarded; bound to a check the pack holds; evidence verified with T2.3's `quoteOccursIn` against the section the critic read; scoped to that section or discarded | `src/lib/authoring/objection.ts` (first claim), the scope door in `refineSection` |
| Scope enforced: the author receives only the scoped section, its revision is spliced into the document, the whole document is parsed again and `checkRevisionScope` refuses a revision that touched another section; a refused round is spent and cuts no version | `refineSection`, `spliceSection`, `src/lib/authoring/scope.ts` (first claim) |
| Two rounds, then the human: `revised` (new version), `held` (the author returned the section unchanged — no version), `refused` (the wall) each spend a round; the third objection writes the `surfaced` row with the check, the evidence, the critic's reason and the author's latest position, and the section keeps its latest draft; objections on a surfaced check write nothing more | `nextMove`, `refineSection` |
| Round state stored, not inferred: every pass reads the ledger back before deciding; each round written before the loop moves on; a revised round and the version it cuts in one transaction, with `artifact.version.added` on the item and `refinement.<outcome>` on the round | `src/db/queries/refinement.ts` (`readRounds`, `writeRound`), `refineArtifactSection` |
| The checks in play: the conditions of the artifact's newest scoring run; for an artifact never scored, the checks no condition governs | `readLatestConditions`, `refineArtifactSection` |
| The author's first draft of a section, refused unless it reads as the one `##` block asked for | `draftSection` |
| The recorded end-to-end run | `scripts/refine-file.ts`, `pnpm refine:file` |
| The ledger documented | `docs/schema.md` (invariant 1, Tables, Enums, Policy summary, migration 0015, commands) |
| `database.types.ts` in step with 0015 | `src/db/database.types.ts` — `refinement_round` and `refinement_outcome` were written by hand in the generator's shape, since the generator reads a live database; this run regenerated the file against the project after `apply` and the block came back identical |

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC1 an objection naming a check id absent from the loaded pack is discarded, and the round continues as if it had not been made | TC1 | `objection.test.ts` › `bindObjections — TC1 → AC1` (4) · `loop.test.ts` › "TC1 → AC1, TC5 → AC5: the author sees only the objection that binds and quotes the section" |
| AC2 a revision whose diff touches a section outside the objection's scope is refused, and the round counts as spent | TC2 | `scope.test.ts` › `checkRevisionScope — TC2 → AC2` (7) · `loop.test.ts` › `refineSection — TC2 → AC2` (2: a section grown, a heading renamed; the refused round is round 1, the next objection is round 2 on the same version) |
| AC3 a section objected to three times keeps the author's latest draft and gains one open question naming the check, the evidence and both positions | TC3 | `loop.test.ts` › `refineSection — TC3 → AC3` (2) · `rounds.test.ts` › `nextMove — the two-round cap` (5) |
| AC4 the critic's input contains no turn of the conversation — asserted over the assembled request | TC4 | `loop.test.ts` › `refineSection — TC4 → AC4`, over every request the critic was sent: no conversation turn, no author position, no other section · `prompt.test.ts` › `criticRequest` |
| AC5 an objection whose evidence does not appear in the artifact is dropped before the author ever sees it | TC5 | `objection.test.ts` › `verifyObjections — TC5 → AC5` (3) · `loop.test.ts` › the doors test above |
| AC6 round state survives a reload: the count and the objections are read from the ledger | TC6 | `loop.test.ts` › `refineSection — TC6 → AC6` (2: two rounds from an earlier session surface the next objection with no author call and the stored position; a round written in one session is round 1 of the next's round 2) · `refinement.db.test.ts` › `rounds are history, and workspace-isolated` (2) and the key's two, all nine run against Postgres |
| AC7 two sections refine independently | TC7 | `loop.test.ts` › `refineSection — TC7 → AC7` · `rounds.test.ts` › "counts only its own section and check" |
| AA1 a `##` block parses to a section whose id is the heading slug, with a suffix on collision | TA1 | `sections.test.ts` › `parseSections — TA1 → AA1` (11), `slugOf` (3), `spliceSection` (3) |
| AA2 a surfaced round row is what AC3 creates; no open-question table exists | TA2 | `rounds.test.ts` › `the open question — TA2 → AA2` (2: the outcome, and no schema table named for a question) · `loop.test.ts` TC3 (the row AC3 writes is `surfaced`) · `refinement.db.test.ts` › `the open question — TA2 → AA2` (3, run against Postgres) |
| AA3 two rounds on one (artifact, section, check) are two rows, and the count is the highest round number | TA3 | `rounds.test.ts` › `roundCount — TA3 → AA3` (4) · `refinement.db.test.ts` › `the round ledger's key — TA3 → AA3` (2, run against Postgres) |
| AA4 preflight regenerates stale route types | TA4 | `route-types.test.mjs` (12) · run by hand in this worktree: `unstamped` → regenerated, then `current` |

## Tests written, each observed red first

Each mutation was applied to the source, the test file run alone, and the source restored and
compared byte for byte with the original before the next (a script in the run's scratchpad did the
apply, the run and the restore). The count is the file's at the time. The rows name each file by
its base name; the files are `src/lib/authoring/sections.test.ts` (18 tests),
`src/lib/authoring/loop.test.ts` (22), `src/lib/authoring/run.test.ts` (8),
`src/lib/authoring/prompt.test.ts` (4), `scripts/run/route-types.test.mjs` (12) and
`src/lib/authoring/rounds.test.ts` (11). The counts are of the file when the mutation ran: the
first build's rows count 17, 17, 7, 11 and 8 or 10; the rows marked *review* were added for a
review pass's findings, after the file had grown. `src/lib/ai/call.test.ts` gained one purpose in
an existing test, and its red is the typechecker's. Where one mutation reddened several rows it is named on each; a
second mutation that reddened a row alone is in brackets. `src/lib/authoring/objection.test.ts`
(13) and `src/lib/authoring/scope.test.ts` (9) are the first claim's, recorded then and unchanged
since — `scope.ts` changed only in a comment. `src/db/refinement.db.test.ts` (9) is the run after
`apply`'s: the file skipped, loudly, until 0015 was applied, and each of its nine tests was
reddened then. Its mutations are lettered D and are code-side or test-side by necessity — the
run's credential cannot drop a constraint or disable a trigger (the capability boundary), so
where the guarantee is the database's the mutation removes the *path to it* instead: a code guard
that refuses before Postgres does, an error rethrown without the constraint's name, or the
statement aimed at a table that carries no such guarantee.

| test | reddened by | red → green |
|---|---|---|
| `sections.test.ts` › parseSections › parses a ## block to a section whose id is the heading's slug | A1 id is the raw heading | `12 failed / 5 passed → 17 passed` |
| `sections.test.ts` › parseSections › suffixes a second heading that slugs the same, in document order | A3 no suffix at all | `2 failed / 15 passed → 17 passed` |
| `sections.test.ts` › parseSections › moves past a suffix a heading already took rather than colliding with it | A2 a taken suffix is not skipped | `2 failed / 15 passed → 17 passed` |
| `sections.test.ts` › parseSections › keeps a section's id when a section is added after it | A4 suffixes assigned from the end of the document | `10 failed / 7 passed → 17 passed` |
| `sections.test.ts` › parseSections › is lossless | A5 lines re-ended with a bare newline | `1 failed / 16 passed → 17 passed` |
| `sections.test.ts` › parseSections › reads ### and deeper as part of the ## section above them | A6 ### read as a heading | `1 failed / 16 passed → 17 passed` |
| `sections.test.ts` › parseSections › holds the text before the first ## heading as the preamble | A7 preamble dropped | `4 failed / 13 passed → 17 passed` |
| `sections.test.ts` › parseSections › gives no preamble to a body that opens on a ## heading | A8 an empty preamble pushed | `10 failed / 7 passed → 17 passed` |
| `sections.test.ts` › parseSections › does not read a ## line inside a fenced code block as a heading | A9 fences not read | `2 failed / 15 passed → 17 passed` |
| `sections.test.ts` › parseSections › closes a fence only on the same character at least as long | A10 any fence closes any fence | `1 failed / 16 passed → 17 passed` |
| `sections.test.ts` › parseSections › needs a space after ## | A11 no space needed after ## | `2 failed / 15 passed → 17 passed` |
| `sections.test.ts` › slugOf › keeps letters of every script | A12 ASCII letters only (A13 combining marks dropped: same 1 failed) | `1 failed / 16 passed → 17 passed` |
| `sections.test.ts` › slugOf › reads one heading typed two ways as one id | A14 no NFC | `1 failed / 16 passed → 17 passed` |
| `sections.test.ts` › slugOf › slugs a heading with no letters or digits to the empty-slug id | A15 empty slug left empty | `1 failed / 16 passed → 17 passed` |
| `sections.test.ts` › spliceSection › replaces one section and leaves every other byte as it was | A16 splice replaces the other sections | `2 failed / 15 passed → 17 passed` |
| `sections.test.ts` › spliceSection › puts back a trailing line break the revision dropped | A17 trailing break not put back | `1 failed / 16 passed → 17 passed` |
| `sections.test.ts` › spliceSection › refuses to splice a section the document does not have | A18 unknown section spliced silently | `1 failed / 16 passed → 17 passed` |
| `rounds.test.ts` › roundCount › reads two rounds on one (artifact, section, check) as two rows counting to the highest round number | R9 count is the lowest round number | `4 failed / 4 passed → 8 passed` |
| `rounds.test.ts` › roundCount › takes the highest round number, not the row count and not the last row read | R1 count is the number of rows | `1 failed / 7 passed → 8 passed` |
| `rounds.test.ts` › roundCount › is 0 when the ledger holds no round for the key | R4 count starts at 1 | `2 failed / 6 passed → 8 passed` |
| `rounds.test.ts` › roundCount › counts only its own section and check | R3 count ignores the key | `1 failed / 7 passed → 8 passed` |
| `rounds.test.ts` › nextMove › revises twice | R5 cap of three revisions | `3 failed / 5 passed → 8 passed` |
| `rounds.test.ts` › nextMove › surfaces the third objection with the author's latest position | R6 surfaces with the first position | `1 failed / 7 passed → 8 passed` |
| `rounds.test.ts` › nextMove › spends a refused or held round like a revised one | R7 only revised rounds spend the cap | `2 failed / 6 passed → 8 passed` |
| `rounds.test.ts` › nextMove › writes nothing more once the question is open | R8 a surfaced question is surfaced again | `1 failed / 7 passed → 8 passed` |
| `loop.test.ts` › TC2 › refuses a revision that grows a section outside its scope, and counts the round as spent | L2 a refused round is not recorded | `2 failed / 15 passed → 17 passed` |
| `loop.test.ts` › TC2 › refuses a revision that renames its own heading | L1 the scope wall never refuses | `2 failed / 15 passed → 17 passed` |
| `loop.test.ts` › TC3 › surfaces the third objection as one open question, keeping the author's latest draft | L3 a third objection is revised again (L4 surfacing puts the original back: 1 failed / 16 passed) | `2 failed / 15 passed → 17 passed` |
| `loop.test.ts` › TC3 › writes nothing more for a check whose question is already open | L7 the count recomputed from this session's rounds | `3 failed / 14 passed → 17 passed` |
| `loop.test.ts` › TC4 › never puts a turn of the conversation, or the author's position, into a request the critic is sent | L5 the conversation reaches the critic (L6 the critic reads the whole document: 1 failed / 16 passed) | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › TC6 › reads the count from the ledger after a reload, not from the document | L7 | `3 failed / 14 passed → 17 passed` |
| `loop.test.ts` › TC6 › carries a round written in one session into the next | L7 | `3 failed / 14 passed → 17 passed` |
| `loop.test.ts` › TC7 › leaves another section's count untouched by the rounds spent on one | L8 rounds keyed by check alone, across sections (rounds.ts) | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › doors › TC1 → AC1, TC5 → AC5: the author sees only the objection that binds and quotes the section | L9 the critic's drafts reach the author unadmitted | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › doors › discards an objection scoped to a section the critic was not shown | L10 no scope door | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › doors › sends the author only the scoped section of the document | L11 the author is sent the whole document | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › round ends › records a revision that changed nothing as held, cutting no version | L12 an unchanged revision recorded as revised | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › round ends › stops on a call that did not come back, keeping the rounds already written | L13 a failed call reads as settled | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › round ends › stops on a revision with no position, writing no round for it | L14 a blank position is recorded | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › round ends › says so when the section is not in the document | L15 a missing section refines nothing silently | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › draftSection › takes a draft that reads as the one section asked for | L18 draft keeps no trailing line break | `1 failed / 16 passed → 17 passed` |
| `loop.test.ts` › draftSection › refuses a draft that grows a second section or loses its heading | L16 any draft accepted (L17 another heading accepted: 1 failed / 16 passed) | `1 failed / 16 passed → 17 passed` |
| `run.test.ts` › refineArtifactSection › shows the critic the checks the newest scoring run left in play, and meters it as critique | W1 the scoring run's conditions ignored | `1 failed / 6 passed → 7 passed` |
| `run.test.ts` › refineArtifactSection › leaves conditioned checks out for an artifact that was never scored | W2 every base check in play whatever held | `2 failed / 5 passed → 7 passed` |
| `run.test.ts` › refineArtifactSection › meters the author's revision as draft and hands the round to the ledger with the artifact's item and product | W4 the author meters as critique (W8 item written as product: 1 failed / 6 passed) | `1 failed / 6 passed → 7 passed` |
| `run.test.ts` › refineArtifactSection › answers with a write failure, not an exception, when the ledger refuses a round | W5 a refused write escapes as an exception | `1 failed / 6 passed → 7 passed` |
| `run.test.ts` › refineArtifactSection › refines nothing for content with no markdown body, and calls no model | W6 content of another shape refined anyway | `1 failed / 6 passed → 7 passed` |
| `run.test.ts` › refineArtifactSection › refines nothing for an artifact with no versions or no pack | W7 no pack check | `1 failed / 6 passed → 7 passed` |
| `run.test.ts` › markdownBody › reads a string body and nothing else | W9 any body accepted | `1 failed / 6 passed → 7 passed` |
| `prompt.test.ts` › renderChecks › renders the checks in play in pack order, with probes and the bank's critic test beneath | P1 checks rendered in the order the ids arrive | `1 failed / 3 passed → 4 passed` |
| `prompt.test.ts` › renderChecks › renders only the checks in play, and nothing for an id the pack does not hold | P2 every check rendered, in play or not | `2 failed / 2 passed → 4 passed` |
| `prompt.test.ts` › criticRequest › puts the protocol and the checks in the cached context, and the section alone in the input | P3 the section written into the cached context as well — green against the first version of the test, which asserted nothing about the context's text; red once the test asserted the context holds none of the section | `1 failed / 3 passed → 4 passed` |
| `prompt.test.ts` › revisionRequest › names the objection's check in the rubric's words beside the critic's reason | P4 the objection names its check by id alone | `1 failed / 3 passed → 4 passed` |
| `rounds.test.ts` › the open question — TA2 → AA2 › is a round row marked surfaced: the outcome exists on the round ledger | Q1 no surfaced outcome (enums.ts) | `1 failed / 9 passed → 10 passed` |
| `rounds.test.ts` › the open question — TA2 → AA2 › has no table of its own: no table in the schema is named for a question | Q2 an open_question table beside the ledger (tables.ts) | `1 failed / 9 passed → 10 passed` |
| `route-types.test.mjs` › plan › regenerates types that exist but were stamped from another tree | RT1 a stale stamp trusted | `2 failed / 9 passed → 11 passed` |
| `route-types.test.mjs` › plan › regenerates types nobody stamped, which is the directory T3.1 met | RT2 the unstamped arm removed | `1 failed / 10 passed → 11 passed` |
| `route-types.test.mjs` › plan › regenerates when there are no types at all | RT3 the missing arm removed | `1 failed / 10 passed → 11 passed` |
| `route-types.test.mjs` › plan › leaves types stamped from this tree alone | RT4 always regenerate | `2 failed / 9 passed → 11 passed` |
| `route-types.test.mjs` › routeInputs › moves when a route is added | RT5 route files not in the inputs | `1 failed / 10 passed → 11 passed` |
| `route-types.test.mjs` › routeInputs › does not move for a file that makes no route, or for a route file's contents | RT6 every file counted as a route (RT7 contents in the inputs: 1 failed / 10 passed) | `1 failed / 10 passed → 11 passed` |
| `route-types.test.mjs` › routeInputs › moves when the Next config or the installed Next version changes | RT8 Next config not in the inputs | `1 failed / 10 passed → 11 passed` |
| `route-types.test.mjs` › ensureRouteTypes › runs next typegen on stale types and stamps them with this tree's digest | RT10 no stamp written after generating | `1 failed / 10 passed → 11 passed` |
| `route-types.test.mjs` › ensureRouteTypes › runs nothing when the stamp is this tree's | RT4 always regenerate | `2 failed / 9 passed → 11 passed` |
| `route-types.test.mjs` › ensureRouteTypes › stamps nothing when generation fails | RT9 stamp written before generation succeeds | `2 failed / 9 passed → 11 passed` |
| `route-types.test.mjs` › ensureRouteTypes › keeps the stamp outside the directory typegen writes | RT11 stamp inside the types directory | `1 failed / 10 passed → 11 passed` |
| `sections.test.ts` › spliceSection › keeps the section's own run of trailing line breaks, so a section returned unchanged reads unchanged *(review)* | B1 only a single dropped line break put back, as before the review | `1 failed / 17 passed → 18 passed` |
| `loop.test.ts` › how a round ends › records a section returned with only its trailing blank line dropped as held *(review)* | B1 (in `sections.ts`) | `1 failed / 20 passed → 21 passed` |
| `loop.test.ts` › how a round ends › stops on a position longer than a round can hold, writing no round for it *(review)* | C1 an over-long position recorded | `1 failed / 20 passed → 21 passed` |
| `loop.test.ts` › doors › discards an objection whose reason or evidence is longer than a round can hold *(review)* | C2 over-long objections acted on | `1 failed / 20 passed → 21 passed` |
| `loop.test.ts` › doors › refines nothing under the preamble, which is not a ## section *(review)* | C3 the preamble refined | `1 failed / 20 passed → 21 passed` |
| `rounds.test.ts` › nextMove › surfaces with the position of the latest draft the document kept, not a refused one *(review)* | D1 the surfaced position is the latest round's, refused or not | `1 failed / 10 passed → 11 passed` |
| `run.test.ts` › leaves a failure that is not a ledger write to throw, rather than reporting it as one *(review)* | E1 every throw reported as a write | `1 failed / 7 passed → 8 passed` |
| `call.test.ts` › still accepts every purpose that does route on a tier (`critique` added) *(review)* | `critique` taken out of `Purpose` in `src/lib/ai/types.ts` | `pnpm typecheck`: TS2322 at `call.test.ts:272` and `run.ts:35` → clean; `107 passed` across the files run |
| `loop.test.ts` › doors › refines nothing under a heading whose id is longer than a round can store *(review 2)* | F1 a section id of any length refined | `1 failed / 21 passed → 22 passed` |
| `route-types.test.mjs` › routeInputs › moves when a metadata file is added, since Next makes a route of it *(review 2)* | G1 metadata files not counted | `1 failed / 11 passed → 12 passed` |
| `objection.test.ts` › bindObjections › discards an objection naming a check the loaded pack does not have | M1 `bindObjections` returns every objection, bound or not | `4 failed / 9 passed → 13 passed` |
| `objection.test.ts` › bindObjections › keeps an objection bound to a layered check, since the id space is one space | M8 binding reads `pack.checks` only, not the layers | `1 failed / 12 passed → 13 passed` |
| `objection.test.ts` › bindObjections › binds on the exact id, never a near miss | M1 | `4 failed / 9 passed → 13 passed` |
| `objection.test.ts` › bindObjections › carries on as if the discarded objection had not been made | M1 | `4 failed / 9 passed → 13 passed` |
| `objection.test.ts` › verifyObjections › drops an objection whose evidence is not in the artifact | M2 `verifyObjections` returns every objection, verified or not | `2 failed / 11 passed → 13 passed` |
| `objection.test.ts` › verifyObjections › verifies the way T2.3's guard does: a re-wrap and emphasis are not content | M3 a raw `includes` in place of `quoteOccursIn` | `1 failed / 12 passed → 13 passed` |
| `objection.test.ts` › verifyObjections › drops it before the author sees it: admitObjections never returns it | M2 | `2 failed / 11 passed → 13 passed` |
| `objection.test.ts` › admitObjections › discards one whose checkId is missing or blank | M4 the four-field guard removed and binding returning everything | `8 failed / 5 passed → 13 passed` |
| `objection.test.ts` › admitObjections › discards one whose reason is missing or blank | M5 the guard reading null as absent and blank as present | `2 failed / 11 passed → 13 passed` |
| `objection.test.ts` › admitObjections › discards one whose evidence is missing or blank | M4 | `8 failed / 5 passed → 13 passed` |
| `objection.test.ts` › admitObjections › discards one whose scope is missing or blank | M5 | `2 failed / 11 passed → 13 passed` |
| `objection.test.ts` › admitObjections › admits a complete, bound, verified objection with its prose fields trimmed | M6 the prose fields kept untrimmed | `1 failed / 12 passed → 13 passed` |
| `objection.test.ts` › admitObjections › does not tidy a padded check id into a binding | M7 the check id trimmed before binding | `1 failed / 12 passed → 13 passed` |
| `scope.test.ts` › checkRevisionScope › lets a revision through that changes only the scoped section | S8 the scope never taken out, every touched section outside | `3 failed / 6 passed → 9 passed` |
| `scope.test.ts` › checkRevisionScope › refuses a revision that also changes a section outside the scope, naming it | S1 `checkRevisionScope` always lets the revision through | `4 failed / 5 passed → 9 passed` |
| `scope.test.ts` › checkRevisionScope › refuses a whitespace-only change outside the scope — a change is a change | S2 text compared with surrounding whitespace ignored | `1 failed / 8 passed → 9 passed` |
| `scope.test.ts` › checkRevisionScope › refuses a revision that adds or removes another section | S3 a section that only exists after not read as touched | `1 failed / 8 passed → 9 passed` |
| `scope.test.ts` › checkRevisionScope › refuses a revision that reorders the sections outside the scope | S5 the move comparison removed | `1 failed / 8 passed → 9 passed` |
| `scope.test.ts` › checkRevisionScope › lets the scoped section move, since where it sits is the author's to change | S7 the scoped section left in both lists and filtered from the result afterwards | `1 failed / 8 passed → 9 passed` |
| `scope.test.ts` › checkRevisionScope › refuses a list with two sections under one id rather than guessing which changed | S9 a duplicate id overwrites instead of throwing | `1 failed / 8 passed → 9 passed` |
| `scope.test.ts` › sectionsTouched › reads nothing as touched when nothing changed | S10 every section before the revision read as touched | `7 failed / 2 passed → 9 passed` |
| `scope.test.ts` › sectionsTouched › does not read one removed section as every later section moving | S6 the move comparison over absolute positions | `1 failed / 8 passed → 9 passed` |
| `refinement.db.test.ts` › the round ledger's key — TA3 → AA3 › holds two rounds on one (artifact, section, check) as two rows, counting to the highest round number | D1 the round number bound as the literal `1`, so round 2 is written on round 1's key | `3 failed / 6 passed → 9 passed` |
| `refinement.db.test.ts` › the round ledger's key — TA3 → AA3 › refuses a second row with the same round number on the same key | D2 the round insert's error caught and rethrown as `could not store the round`, so the constraint's own name never reaches the caller | `3 failed / 6 passed → 9 passed` |
| `refinement.db.test.ts` › the open question — TA2 → AA2 › is round 3 marked surfaced, carrying no version and no stray sections | D3 `readRounds` selecting the literal `'held'` in place of the `outcome` column | `1 failed / 8 passed → 9 passed` |
| `refinement.db.test.ts` › the open question — TA2 → AA2 › cannot be written before round 3, and there is no round 4 | D4 a code-side guard in `writeRound` refusing the same three pairings before the insert, so the refusal stops being the database's (D1 and D2 redden it too, from the other side) | `1 failed / 8 passed → 9 passed` |
| `refinement.db.test.ts` › the open question — TA2 → AA2 › has no table of its own in the database | D5 the catalog pattern widened from `%question%` to `%round%`, which matches a table that does exist — the negative check a query returning nothing needs | `1 failed / 8 passed → 9 passed` |
| `refinement.db.test.ts` › a round's outcome and what it carries › cuts the revised version in the same transaction, attributed to the author, with both ledger rows | D6 the `artifact.version.added` ledger write removed, leaving the round's own row alone | `1 failed / 8 passed → 9 passed` |
| `refinement.db.test.ts` › a round's outcome and what it carries › writes a refused round with the sections it strayed into, and refuses one that names none | D7 `outside_sections` stored as an empty array rather than the sections given | `1 failed / 8 passed → 9 passed` |
| `refinement.db.test.ts` › rounds are history, and workspace-isolated › refuses UPDATE and DELETE, service role included | D8 the two statements aimed at `item`, a mutable table — which is what shows the refusal is the append-only trigger's and not this role being unable to write at all | `1 failed / 8 passed → 9 passed` |
| `refinement.db.test.ts` › rounds are history, and workspace-isolated › lets a member of one workspace read none of another's rounds | D9 `actAs` not called, leaving the read on the connection role, which bypasses RLS | `1 failed / 8 passed → 9 passed` |

## Tests that cover a Build line or a default rather than an AC

The first review pass counted tests no Criteria row names. Each covers a Build line or a default,
so a later ticket that retires one knows what it answered for:

| Tests | Build line or default |
|---|---|
| `run.test.ts` (8) | Build "The two agents … through T2.2's seam at the generation tier" — `critique` and `draft` metering; default 6, the checks in play; the not-refinable paths; a write refused by the ledger versus a bug |
| `loop.test.ts` › how a round ends (6) | Build "Two rounds, then the human" — `held`, a provider stop keeping its rounds, a blank or over-long position, a missing section; default 8 |
| `loop.test.ts` › `draftSection` (2) | Build "The loop's shape" — *author drafts* |
| `loop.test.ts` › doors (6, beyond TC1/TC5) | Build "Scope is enforced": only the scoped section reaches the author; defaults 1 and 3; a section id too long to store |
| `prompt.test.ts` › `renderChecks` (2), `revisionRequest` (1) | Build "What the critic sees" — the checks in play in the pack's words; the objection's check named to the author |
| `rounds.test.ts` › `nextMove` (5) | Build "Two rounds, then the human" and AC3; default 5 |
| `objection.test.ts` › `admitObjections` (6) | Build "An objection is four fields, or it is discarded" |
| `scope.test.ts` › `sectionsTouched` (2) | Build "Scope is enforced, not advised" — what counts as touching a section |
| `call.test.ts` (1, changed) | the `critique` purpose routes on a tier |
| `route-types.test.mjs` (12) | AA4 |

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `0c8c5df` | opus | yes | FINDINGS |
| 2 | `b39f99b` | opus | yes | FINDINGS |

Each pass was called first on the pinned `fable`, which refused for credits (*"You're out of usage
credits"*, HTTP 429); `review-model.mjs` gave `opus`, and the pass ran there with the same one-line
message. Both stopped at their 30-turn limit and were resumed once. `docs/reviews/T3.1.md` holds
pass 2's verdict. No third pass: pass 2's one Must is pass 1's Must 2, which the reviewer says no
code change settles before `apply`, and pass 2's three Shoulds were fixed after it, below.

**Pass 1 — two Musts, eight Shoulds.**

1. *Must — three schema choices went out as cheap defaults* (`held`, `revised_version_id`,
   `outside_sections`, and `_preamble` as a stored id). Settled: the three are questions under the
   `apply` stop (ticket file and *Questions this stop puts to you*), and the preamble is never
   refined, so never stored.
2. *Must — the Done line and the database tests are not observed at this commit.* Stands: open
   question 6, owner T-next, the run after `apply`.
3. *Should — a raw NUL byte in `route-types.mjs` made git treat it as binary.* Fixed: the escape.
4. *Should — an unchanged section with a blank line dropped recorded as `revised`.* Fixed:
   `spliceSection` keeps the section's run of trailing line breaks.
5. *Should — reason, evidence and position lengths capped only by the database; the pass limit read
   as `write`.* Fixed: over-long objections discarded, an over-long position an `answer` stop, and
   only a ledger write reported as `write`.
6. *Should — tests naming no criterion.* Fixed: *Tests that cover a Build line or a default*.
7. *Should — `writeRound` does not check it revises the newest version.* Open question 5, owner T3.2.
8. *Should — a surfaced question could carry a refused revision's position.* Fixed: the latest kept
   or held draft's position.
9. *Should — `critique` missing from `call.test.ts`'s purposes.* Fixed.
10. *Should — `schema.ts`'s comment and the report disagreed about the cut-off call.* Fixed.

**Pass 2 — one Must, three Shoulds.** It found pass 1's findings settled but for Must 2 and found
no defect in the loop's logic, tracing the wall, the cap across `held`, `refused` and a reload, the
pass limit, and the SQL against the Drizzle table by hand.

1. *Must — the database half and the Done line are still not observed.* Pass 1's Must 2; stands as
   open question 6.
2. *Should — a heading whose slug is past 200 characters reaches the database only after two paid
   calls.* Fixed: `refineSection` refuses a section id longer than `SECTION_ID_MAX` before any call.
3. *Should — `route-types.mjs` did not count metadata route files.* Fixed: the favicon, icons,
   social images, sitemap, robots and manifest are inputs too.
4. *Should — two counts for `nextMove` in this report.* Fixed.

## The recorded end-to-end run — the Done line, on record

`pnpm refine:file ../sample-juno-feature.md --heading "Date & Meet"`, 2026-09-20 from 16:47 to
17:07 UTC, against the seed workspace's own key with the rounds stored in `refinement_round` —
the table the `apply` created two hours earlier. No `--memory`: the same `draftSection` and
`refineArtifactSection` an item page would call, the same seam, every call metered to `ai_usage`.
It ran to a natural settle rather than stopping on anything.

**What it produced.** One section drafted and stored as `soc-14` v1, then twelve rounds over six
checks, ending with the section at version `a0cd2a13`:

| check | rounds | end |
|---|---|---|
| `prd-4` | held, held, surfaced | open question |
| `prd-13` | held, held, surfaced | open question |
| `prd-19` | revised, revised, surfaced | open question |
| `prd-5` | revised | settled |
| `prd-14` | revised | settled |
| `prd-17` | revised | settled |

Three open questions surfaced, four revisions passed the scope wall, and 10 critic calls and 10
author calls cost **$3.54**. The full log is 309 lines and stays in the run's scratchpad.

**The third disagreement, surfacing.** `prd-19` is the shape the ticket describes end to end — two
revisions inside scope, then a third objection the author cannot answer, and the round becomes the
open question rather than a fourth attempt:

> critic: DM-14 binds reward settlement to "the following day" and "that morning" without saying
> what the day is counted from (the date window closing, or both confirmations being submitted) or
> at what hour the run happens, so two developers would build different settlement schedules.
>
> evidence: when settlement runs the following day, then each person receives +3 curated cards
> that morning and sees them in their drop

The author's position, stored on the surfaced row beside the critic's: *"Both ambiguous lines are
now bound to a single implementable reading … to replace the placeholders with final rules I would
need the list of structured report categories with the serious subset marked, and the intended
confirmation hour and timezone basis."* The section keeps its latest draft, as AC3 requires: no
work is thrown away, and what the meter reads is the document as it actually stands.

**`held` earned its place in the enum, twice.** `prd-4` is the check that asked for an evidence
artefact nobody supplied. The author returned the section unchanged three times running, saying so
each time — *"inventing a source would be a fabricated fact … To close prd-4 I would need at least
one real artifact with a citable source"* — and the two `held` rounds spent the cap without cutting
a version. That is the case the `apply` stop put to you as question 1: without `held` those rounds
would have been recorded as `revised`, cutting versions identical to the last and re-running §5's
checks on text that did not change, or not recorded at all, which would have let the author hold
its ground forever because no round was ever spent. It is the outcome §1 law 3 wants too — the
ledger now says *why* this section carries an open question, in the author's own words.

**A revision inside the wall.** `prd-5`'s objection was that an assumption's detection signal
compared a population against itself and named no number, so nothing could falsify it. The author
rewrote that one signal, gave it a trip-wire, flagged the figure as proposed — *"Every other
sentence in the section is unchanged"* — and `checkRevisionScope` agreed: the revision was spliced
in, the whole document parsed again, nothing outside `date-meet` touched, and a new version cut.

**The first attempt, three days earlier, found a defect before it drafted anything.** The author's
draft came back `schema-invalid`, cut off mid-JSON: the generation tier runs at the provider's
default effort, the model thinks by default, thinking counts against `max_tokens`, and the first
ceiling was 4,000. That call spent 4,000 output tokens and returned about 2,500 characters of JSON.
Truncated JSON reads as a flaky provider rather than as a ceiling. The ceilings are now 12,000 for
the critic and 16,000 for the author (`src/lib/authoring/schema.ts`), in the scorer's proportion,
and no call in this run was cut off. That run then stopped on its second critic pass, which the
provider refused for credit — *"Your credit balance is too low"* — and the loop reported it as the
`provider` stop with the round already written standing, which is the shape `loop.test.ts` pins.

**One section is still the whole document.** As in the first attempt, the author wrote a complete
feature PRD under the one `##` heading it was asked for, so the section under refinement and the
whole draft were the same text and the scope wall had nothing beside it to protect. Open question 4
still owns that; it is T3.2's interview to settle.

## Questions this stop puts to you

Three things in migration 0015 are schema the addendum did not name, and the first review pass
found them listed below as cheap defaults — guidelines §4 counts schema as expensive, and an enum
value cannot be dropped once applied. They are now questions, set out in the ticket file under
*Questions this stop puts to you* and named in the claim's `migration` comment; **`apply` takes all
three as 0015 has them**:

1. **`held`**, a fourth outcome: the author returns the section unchanged, the round is spent, no
   version is cut.
2. **`revised_version_id`**: the version a `revised` round cut.
3. **`outside_sections`**: the sections a `refused` revision strayed into.

## Defaults taken

Said in the claim's one `default` comment on the board row. None is schema or stored data:

1. **The preamble is not refined.** Text before the first `##` is a section of the list the scope
   wall compares, under `_preamble`, so a revision cannot change it unseen; the loop refuses to
   refine it, so no round stores that id. A `##` inside a code fence is not a heading.
2. **The critic is shown the section alone**, and evidence is checked against the section's text.
   The Build lists "the artifact" among what the critic sees and in the same paragraph scopes its
   reading to the section under refinement; the section is the artifact's text the critic reads. A
   quote found in the section is in the artifact, so AC5 holds as written.
3. **An objection scoped to any section but the one under refinement is discarded** — the author's
   call receives only that section, so it could not be acted on — and so is one whose reason or
   evidence is longer than a round holds.
4. **A surfaced check stays closed on that section**: later objections on it write nothing, since
   the question is the human's. The count never resets, as AA3's key implies.
5. **The surfaced question carries the author's position for the section as it stands** — the
   latest `revised` or `held` round's — and a refused revision's only when no round's draft was kept.
6. **The checks in play** are the conditions of the artifact's newest scoring run; for an artifact
   never scored, only the checks no condition governs.
7. **A metering purpose, `critique`**, for the critic's calls, beside `draft` for the author's.
8. **A section's trailing line breaks are the section's**: a revision is spliced in with the run of
   line breaks the section ended in, so one returned unchanged but for a blank line is `held`.
9. **The route-type stamp** lives at `.next/aenima-route-types`, outside the directory typegen
   writes.

## Changed since this ticket was cut

`product-spec` is v1.8 in the repo and on the Spec line, so nothing this ticket cites has moved.
`main` itself has: T0.24 landed while this ticket waited at Decision, taking `guidelines` to
v1.16, so this round is **v1.17** rather than the v1.16 it was written as. `origin/main` is merged
into the branch at `cfc1175` and that header was the one conflict — both changes are in the file,
T0.24's above v1.15 and AA4's above it; §5 step 0's preflight line carries both the apply-from-any-checkout
sentence and the route-type one. The build-log index is regenerated after the merge, which is how
§5 settles two pull requests each adding a list line.

## What the `apply` unblocked, and what it found

The previous claim listed four things waiting on the word. All four are done, and the migration
itself was applied from this worktree — `apply.mjs --ref origin/t3-1`, T0.24's child process in
the primary checkout reading the branch's `drizzle/` out of git — which is the first time the
pipeline has acted on a granted `apply` at all.

1. **The recorded end-to-end run**, above: twelve rounds, three open questions surfaced, $3.54.
2. **`src/db/refinement.db.test.ts` runs against Postgres.** Nine tests, each reddened by its own
   mutation (the D rows) and green after: the key holds two rounds as two rows and refuses a third
   on the same number, the open question is round 3 and only round 3 with no table of its own, a
   revised round cuts its version in the same transaction with both ledger rows, a refused round
   carries the sections it strayed into, and the table refuses UPDATE and DELETE to the service
   role and shows one workspace none of another's rounds.
3. **`src/db/database.types.ts` regenerated** against the project through the Supabase MCP
   server's `generate_typescript_types`. The `refinement_round` block written by hand before the
   table existed came back **identical** — every column's optionality on Row, Insert and Update,
   and all five relationships by constraint name — as did `refinement_outcome` under `Enums` and
   `Constants`; `PostgrestVersion` is still `14.5` and nothing else in the file moved. The file's
   header records the confirmation and retires the hand-written note.
4. **A fresh review pass** over the whole ticket, in the table above.

## Open questions

1. **The first claim's open question 4 — binding reads the pack, not the checks in play** —
   stands as a default: AC1 says "absent from the loaded pack", and the critic is shown only the
   checks in play, so an objection bound to a pack check outside them is admitted. Cheap either way.
2. **The first claim's open question 5 — scope not checked against the artifact's sections** — is
   answered: the loop discards an objection scoped to any section but the one the critic was shown.
3. **The first claim's open question 6 — stale route types** — is answered by AA4.
4. **One section can be the whole document.** The live draft put a complete PRD under one `##`
   heading, fifteen `###` subsections deep, so the section under refinement and "the whole draft"
   were the same text and the scope wall had nothing beside it to protect. Whether the author
   should draft to the rubric's sections as `##` blocks is T3.2's interview's to settle, not this
   ticket's; recorded so it is decided rather than inherited. Owner: T3.2.
5. **`writeRound` does not check the version it revises is still the newest** (review pass 1,
   Should 7). A version added while the loop runs would be replaced by a body built on the older
   one. Nothing writes an artifact version concurrently today; T3.2's chat panel is the first thing
   that will. Owner: T3.2.
6. **~~The Done line and the database tests are not met at this commit~~ — closed by this claim**
   (review pass 1, Must 2, carried by pass 2). The migration is applied, all nine database tests
   were reddened and run against Postgres, and the end-to-end run reached a natural settle with
   three open questions surfaced. Nothing here waits any more.
7. **Thinking on the generation tier is unbounded by anything but the ceiling.** The settled run
   spent 7,000–7,700 output tokens on a typical author turn and 2,600–3,900 on a critic pass, and
   twenty calls came to $3.54 — most of it reasoning nobody reads. `ResolvedRequest.effort` is null
   off the pinned path by T2.7's choice, which the build log records as an open question; the
   loop's calls are now its heaviest users by a wide margin. Owner: Phase 3.
8. **A `refine:file` run leaves an item and its versions in the seed workspace, and they cannot be
   deleted.** `soc-14` and its rounds join `soc-10` and `soc-11` under the build log's open question
   24: `refinement_round` is the eighth append-only table, so this run's twelve rounds are permanent
   in that workspace the way its scoring rows are. The script already reuses one item per file, so
   re-running this document adds versions rather than items; what has no answer yet is a supported
   reset. Owner: the same place open question 24 lands.
