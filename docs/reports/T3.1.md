# T3.1 — report, so far

Author-critic loop. Epic E3.1 Authoring loop · Priority High · Type Feature · Spec `product-spec
v1.8 §6, §5, §13 · T2.3's fabrication guard · T2.2's seam`, no version drifted. Branch `t3-1` off
`origin/main` at `d79f49a`. Claimed by the scheduled run on 2026-09-17 (session `150c7be8`) in its
own worktree.

**Stopped at Decision in step 4, said first.** The ticket needs three things no spec defines, and
each is schema or stored data: what a section of an artifact is, where an open question lives, and
what a `refinement_round` row is keyed on. `docs/tickets/T3.1.md` § *Stopped at Decision* sets out
all three with the default the next run takes on the word "default"; the board row carries the same
in one decision comment. The reviewer has not read this diff — a Decision exit is a step-4 exit, and
step 5 is the next run's, on the whole ticket.

## Built before the stop

The two pieces none of the three answers moves.

| Item | Where |
|---|---|
| Objection intake: an objection is four non-blank fields or it is discarded; it binds to a check id in the loaded pack's whole id space, base and layers, by exact match; its evidence is verified against the artifact text with T2.3's `quoteOccursIn`; what is left keeps the critic's order | `src/lib/authoring/objection.ts` (`admitObjections`, `bindObjections`, `verifyObjections`, `Objection`, `ObjectionDraft`) |
| The scope wall over a list of sections: a section is touched when its text changes (exactly — whitespace counts), it appears, it disappears, or its order among the sections both sides share moves; a revision is refused when it touches any section but the scoped one, which may itself move; a list with two sections under one id throws | `src/lib/authoring/scope.ts` (`checkRevisionScope`, `sectionsTouched`, `Section`) |

Not built, waiting on the answer: the critic's request (AC4), the author's and critic's calls
through the seam, the round cap over stored rounds (AC3, AC6, AC7), how content divides into
sections, the migration and `docs/schema.md`, the recorded end-to-end run.

## ACs implemented so far

| AC | Test | Where |
|---|---|---|
| AC1 an objection naming a check id absent from the loaded pack is discarded, and the round continues as if it had not been made | TC1 | `src/lib/authoring/objection.test.ts` › `bindObjections — TC1 → AC1` (4) |
| AC2 a revision whose diff touches a section outside the objection's scope is refused | TC2 | `src/lib/authoring/scope.test.ts` › `checkRevisionScope — TC2 → AC2` (7) — *the round counts as spent* half needs stored rounds and waits |
| AC5 an objection whose evidence does not appear in the artifact is dropped before the author ever sees it | TC5 | `src/lib/authoring/objection.test.ts` › `verifyObjections — TC5 → AC5` (3) |
| AC3, AC4, AC6, AC7 | — | not built — Decision |

## Tests written, each observed red first

Each mutation was applied to the source, the test file run, and the source restored and compared
byte for byte with a copy before the next. The count is the file's: `objection.test.ts` holds 13
tests, `scope.test.ts` 9.

| test | reddened by | red → green |
|---|---|---|
| bindObjections › discards an objection naming a check the loaded pack does not have | M1 `bindObjections` returns every objection, bound or not | `4 failed / 9 passed → 13 passed` |
| bindObjections › keeps an objection bound to a layered check, since the id space is one space | M8 binding reads `pack.checks` only, not the layers | `1 failed / 12 passed → 13 passed` |
| bindObjections › binds on the exact id, never a near miss | M1 | `4 failed / 9 passed → 13 passed` |
| bindObjections › carries on as if the discarded objection had not been made | M1 | `4 failed / 9 passed → 13 passed` |
| verifyObjections › drops an objection whose evidence is not in the artifact | M2 `verifyObjections` returns every objection, verified or not | `2 failed / 11 passed → 13 passed` |
| verifyObjections › verifies the way T2.3's guard does: a re-wrap and emphasis are not content | M3 a raw `includes` in place of `quoteOccursIn` | `1 failed / 12 passed → 13 passed` |
| verifyObjections › drops it before the author sees it: admitObjections never returns it | M2 | `2 failed / 11 passed → 13 passed` |
| admitObjections › discards one whose checkId is missing or blank | M4 the four-field guard removed and binding returning everything | `8 failed / 5 passed → 13 passed` |
| admitObjections › discards one whose reason is missing or blank | M5 the guard reading null as absent and blank as present | `2 failed / 11 passed → 13 passed` |
| admitObjections › discards one whose evidence is missing or blank | M4 | `8 failed / 5 passed → 13 passed` |
| admitObjections › discards one whose scope is missing or blank | M5 | `2 failed / 11 passed → 13 passed` |
| admitObjections › admits a complete, bound, verified objection with its prose fields trimmed | M6 the prose fields kept untrimmed | `1 failed / 12 passed → 13 passed` |
| admitObjections › does not tidy a padded check id into a binding | M7 the check id trimmed before binding | `1 failed / 12 passed → 13 passed` |
| checkRevisionScope › lets a revision through that changes only the scoped section | S8 the scope never taken out, every touched section outside | `3 failed / 6 passed → 9 passed` |
| checkRevisionScope › refuses a revision that also changes a section outside the scope, naming it | S1 `checkRevisionScope` always lets the revision through | `4 failed / 5 passed → 9 passed` |
| checkRevisionScope › refuses a whitespace-only change outside the scope — a change is a change | S2 text compared with surrounding whitespace ignored | `1 failed / 8 passed → 9 passed` |
| checkRevisionScope › refuses a revision that adds or removes another section | S3 a section that only exists after not read as touched | `1 failed / 8 passed → 9 passed` |
| checkRevisionScope › refuses a revision that reorders the sections outside the scope | S5 the move comparison removed | `1 failed / 8 passed → 9 passed` |
| checkRevisionScope › lets the scoped section move, since where it sits is the author's to change | S7 the scoped section left in both lists and filtered from the result afterwards | `1 failed / 8 passed → 9 passed` |
| checkRevisionScope › refuses a list with two sections under one id rather than guessing which changed | S9 a duplicate id overwrites instead of throwing | `1 failed / 8 passed → 9 passed` |
| sectionsTouched › reads nothing as touched when nothing changed | S10 every section before the revision read as touched | `7 failed / 2 passed → 9 passed` |
| sectionsTouched › does not read one removed section as every later section moving | S6 the move comparison over absolute positions | `1 failed / 8 passed → 9 passed` |

S4 — a section that disappeared not read as touched — reddened the removal half of *adds or
removes* and *one removed section* together (`2 failed / 7 passed → 9 passed`); it is recorded for
completeness and is not the row's mutation.

## Reviewer passes and findings

None yet: the review is step 5, and this claim stopped in step 4.

## Changed since this ticket was cut

Nothing: `product-spec` is v1.8 in the repo and on the Spec line.

## Open questions

1. **What a section is** — Decision on the board; see the ticket file.
2. **Where an open question lives** — Decision on the board; see the ticket file.
3. **What a `refinement_round` row is keyed on** — Decision on the board; see the ticket file.
4. **Binding reads the pack, not the checks in play.** AC1 says "absent from the loaded pack", so an
   objection bound to a check whose condition did not hold for this artifact is admitted. The Build
   also gives the critic "the check ids in play"; whether binding should narrow to those is the next
   run's to settle with the critic's request, and is cheap either way.
5. **An objection's scope is not yet checked against the artifact's sections.** A scope naming no
   section passes intake today; it cannot be checked until a section is defined.
6. **Stale route types in a fresh worktree.** This worktree arrived with `.next/types` naming a
   `src/app/o/[key]` route that is not on `main`, so `pnpm typecheck` failed before any code was
   written; step 0's `test -d .next/types || pnpm next typegen` skips a directory that exists but is
   stale. Regenerated here by hand.
