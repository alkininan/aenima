# T0.38 Design law checks — report

Built over two claims. The first (2026-09-24) built AC1, AC2, AC4, AC5 and C-33's scan, then
stopped at Decision on C-34 (product-spec had no vocabulary list) and on C-33's §12 defaults
(two readings). The reply was `default`. The second claim died partway on 2026-09-25 with the
spec patch uncommitted; this run recovered it (branch kept as `t0-38-stale-0714`), carried the
first claim's commit and the dead run's uncommitted diff forward, and finished the ticket.

## ACs implemented

- **AC1** C-01–C-04 — `scripts/design/spec-checks.mjs`, run as a script
  (`node scripts/design/spec-checks.mjs`, exit 1 on an offender) and tested in
  `scripts/design/spec-checks.test.mjs` (TC1). Passes over `docs/design-spec.md` v2.23.
- **AC2** C-06 and C-39's literal clause — `literalOffenders` in `scripts/design/laws.mjs`,
  tested in `scripts/design/laws.test.mjs` (TC2). What it found, fixed: `0 0 #0000` →
  `0 0 transparent` (4), `z-index: -1` → `calc(var(--z-content) - 1)` (2), the field label's
  13/18 and 17/22 → the `type-ui-label` class and §8.2's transform-only float, `z-[1]` →
  `z-[calc(var(--z-content)+1)]`, `animate-pulse` → `.shimmer` (2), the secondary button's
  hover border `rgba(120,126,136,.72)` → `border-glass-border-hover`, the OTP boxes'
  `rounded-[22px]`/`rounded-[27px]` → `rounded-pill` (§8.2).
- **AC3** C-33 and C-34 — `copyOffenders`, `defaultOffenders` and `vocabularyOffenders` in
  `laws.mjs` (TC3). C-33's defaults half checks every §12 default whose surface exists
  against the dictionary key that is that surface (`DEFAULTS`), verbatim by shape. What it
  found, fixed to §12's words: "Try again" → "Retry", "Back to your workspace" → "Back to
  dashboard", "{n}m/h/d/w ago" → "{n} m/h/d/w ago", "Reopen this gap" → "Reopen", the
  check list's unclear chip "Unclear" → "Must · unclear" / "Should · unclear" (§8's Unclear
  state, C-32), and the accepted gap chip "Accepted" beside a bare tag → "Must · {accepter}"
  (§8.9), the accepter the stamp already names. C-34 reads product-spec v1.10 Appendix G, fails with no Appendix G, fails
  when a term C-34 names has no row, and greps the UI strings for each listed word.
- **AC4** C-35–C-38 — allowlists in `laws.mjs`, each use read by where it sits (TC4). What it
  found, fixed: the `@supports not (backdrop-filter …)` fallback moved from `.glass` to
  `.glass-blur`, the only class that blurs (T0.37 had already split the two on main); the
  activity feed's agent actor no longer `text-agent`, since C-36 keeps `--agent` to proposal
  cards, the spinner, the caret and a chart's agent series.
- **AC5** C-50 — `scrollbarClashes` (TC5). What it found, fixed: `.scroll-thin`'s standard
  properties moved inside `@supports not selector(::-webkit-scrollbar)`.

## Tests written, each observed red first

| test | reddened by | red → green |
|---|---|---|
| `scripts/design/spec-checks.test.mjs` C-02, C-04 (TC1) | `About 4 series, see §9.9.` planted under §9 of the real `docs/design-spec.md` | 2 failed / 3 passed → 5 passed |
| `scripts/design/spec-checks.test.mjs` C-01 ×2, C-03 (TC1) | footer set to `v2.21`, `` `--made-up` `` planted under §9 | 3 failed / 2 passed → 5 passed |
| `scripts/design/laws.test.mjs` C-06/C-39 repo scan, C-36, C-37, C-50 (TC2, TC4, TC5) | the tree before the first claim's `src/` fixes (the fix diff reverse-applied) | 4 failed / 11 passed → 15 passed |
| `scripts/design/laws.test.mjs` C-33 scan, C-35, C-38 dot grid (TC3, TC4) | `"Send Code!"` in `en.ts`; `text-danger dot-grid` on `CARD_BASE` | 3 failed / 12 passed → 15 passed |
| `scripts/design/laws.test.mjs` C-37 "modals, sheets and the pipeline strip carry the unblurred class…" (TC4) | `MODAL_BASE`'s `glass` → `glass-blur` | 2 failed / 18 passed → 20 passed |
| `scripts/design/laws.test.mjs` C-38 page ground (TC4) | `bg-surface-1` on `loading.tsx`'s `<main>` | 1 failed / 14 passed → 15 passed |
| `scripts/design/laws.test.mjs` C-33 "hold §12's defaults verbatim wherever their surface exists" (TC3) | `en.ts` as it stood before this claim — "Try again", "Back to your workspace", "{n}m ago" ×4 | 1 failed / 20 passed → 21 passed |
| the same, its three pass-1 rows (TC3) | `en.ts` at f769aa0 — "Reopen this gap", no `checkUnclear.must`/`.should` | 1 failed / 19 passed → 20 passed |
| `scripts/design/laws.test.mjs` C-33 "reads a default by its shape…" (TC3) | `shapeOf` mutated to drop the `{}` for an interpolation | 4 failed / 17 passed (with the three below) → 21 passed |
| `scripts/design/laws.test.mjs` C-34 "finds a row for every term…" (TC3) | `docs/product-spec.md` at `origin/main` (no Appendix G); then `"Your move lane"` planted in `en.ts` | 2 failed / 19 passed (each, with the defaults test) → 21 passed |
| `scripts/design/laws.test.mjs` C-34 "reads C-34's own term list…" (TC3) | `c34Terms`'s pattern mutated to stop at the first comma | 4 failed / 17 passed (with the others above) → 21 passed |
| `scripts/design/laws.test.mjs` C-34 "reads Appendix G's table…" (TC3) | a dash read as the word `—` | 4 failed / 17 passed (with the others above) → 21 passed |
| the same, its two pass-2 rows (TC3) | `en.ts` at c414186 — no `gapAcceptedBy.must`/`.should` | 1 failed / 19 passed → 20 passed |
| `src/app/i/[key]/GapList.dom.test.tsx` "keeps an accepted gap visible…", "still says Must or Should…" | `GapList.tsx` at c414186 (the chip "Accepted") | 2 failed / 11 passed → 13 passed |
| `scripts/design/laws.test.mjs` "reads a colour, a radius, a duration, a shadow and a z-index…" (TC2) | `utilityLiteral` returning null | 2 failed / 18 passed → 20 passed |
| `scripts/design/laws.test.mjs` "reads a font size or a line height…" (TC2, C-39) | `utilityLiteral` passing `text-[…]` and `leading-[…]` | 1 failed / 19 passed → 20 passed |
| `scripts/design/laws.test.mjs` "passes a token, one of §4's rungs, and a calc or color-mix…" (TC2) | `utilityLiteral` flagging any `var(`, `calc(` or `color-mix` | 2 failed / 18 passed → 20 passed |
| `scripts/design/laws.test.mjs` "reads a shadow as literal only when…" (TC2) | `isLiteralShadow` always true | 3 failed / 17 passed → 20 passed |
| `scripts/design/laws.test.mjs` "reads class strings from the syntax tree…" (TC2) | `stringValues` fed the text with its `+` chains joined away | 2 failed / 18 passed → 20 passed |
| `scripts/design/laws.test.mjs` "does not read the token layer as a component…" (TC2) | `componentRules` returning every rule, `:root` and `@theme` included | 2 failed / 18 passed → 20 passed |
| `scripts/design/laws.test.mjs` C-50 "finds the clash when the standard properties leave §4's gate" (TC5) | `scrollbarClashes` returning none | 1 failed / 19 passed → 20 passed |
| e2e item "names an agent actor in --n-primary, never --agent" (TC4, C-36) | `text-agent` restored on the actor in `ActivityFeed.tsx` | 1 failed → 1 passed |

The rows that read "21 passed" were measured while `laws.test.mjs` held a 21st test (each
default listed once), dropped before f769aa0 as asserting nothing a reader needed; the file
holds 20.

Existing tests updated to the fixed strings and tokens: `src/components/ui/variants.test.ts` (expects
`rounded-pill`), `e2e/form-language.spec.ts`'s two OTP radius assertions, `src/app/i/[key]/CheckList.dom.test.tsx`
(the chip's words by tag), `src/app/i/[key]/GapList.dom.test.tsx` (the accepted chip), `src/lib/relative-time.test.ts` (a comment's freshness wording), `e2e/item.spec.ts` (the Reopen button, the freshness readout, the
actor's colour pinned to `--n-primary`) and `e2e/list.spec.ts` (the freshness readouts).
Playwright, this claim: `pnpm e2e` 132 passed, 1 skipped (3.4 m) at f769aa0's tree less the pass-1
fixes; `e2e/item.spec.ts` 36 passed after pass 1's fixes and again after pass 2's.

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | f769aa0 | fable | no | FINDINGS |
| 2 | c414186 | fable | no | FINDINGS |
| 3 | 121c552 | fable | no | FINDINGS |

Pass 1 — Musts: (1) `DEFAULTS` left out §12's reopen = "Reopen", whose surface exists; fixed.
(2) §12's gap chips "Must · unclear" / "Should · unclear" on the check list's chip; fixed.
(3) the red-first record and this report did not cover the last commit's tests and e2e edits;
this report. Shoulds: (4) the report described T0.37's glass split and stopped at v2.22; this
report. (5) `NAMES` exempted nothing; the name is now lowercased before the scan. (6)
`html { font-size: 16px }` filed under `C06_NAMED` though it is C-39's; recorded. (7)
`steeredWord` missed "quizzes"; `zes` added. (8) `CAPITALISED` carries bare "Product" and "Teams";
recorded. (9) the actor's colour asserted only as not `--agent`; now pinned to `--n-primary`.

Pass 2 — Musts: (1) §8.9's accepted gap chip "Must · {accepter}" unlisted; fixed. (2) the row's
chip "Must · {check id}" unlisted; not fixed here, because §12's shape on the row brings
§8.27's one-chip rule with it (the row still shows two), which is layout rather than a string —
an open question below. (3) seven detector tests had no red-first row; rows above. Shoulds:
(4) the "21 passed" counts; explained above. (5) `dictionaryShapes` reads only the first string
under a key, so a two-branch function (`versionCount`) is checked by half; recorded. (6)
`literalOffenders` does not read inline `style` objects; recorded — no component carries one
today. (7) the sentence-case scan drops the word after an interpolation that opens a string;
recorded.

Pass 3 — Must: (1) the C-37 unblurred-class test had no red-first row that could have reddened
it (row 3's tree has no `.glass-blur`, so the test passes there); the row is added above, after
the pass. Three passes is the maximum, so it stands as an open question even though the row now
exists. Shoulds: (2) `gapAccepted` had no renderer left; removed. (3) `DEFAULTS` reads a surface
as a dictionary key; its comment now says so, and that the degraded pages' two strings have keys
and no route yet. (4) "quiz" was the draft's own word, not the specs'; dropped from Appendix G.

## Changed since this ticket was cut

design-spec v2.21 → v2.23. v2.22's §8.2 moved the floated label to a transform-only float,
which the code had not followed; C-39's literal clause found it. v2.23 changed §6's morph
trigger and no §17 line. product-spec v1.9 → v1.10, this ticket's own patch (Appendix G).

## Open questions

- Pass 3's Must — the C-37 unblurred-class test's red-first row — was fixed after the third
  pass, which is the last; no pass has read the fix. Owner T-next.
- Appendix G is a draft for your read in review, as the Decision said: it lists only the words
  the specs already steer away from (violation; test; lane, swimlane) and a dash for the
  other twelve terms. *archive* is left off *park* because Appendix G's own rule excludes a word
  that is another concept's term, and §3 gives archiving to the handed-over state.
- §12's row freshness ladder has "{n} y ago" and bounds days under 100 and weeks under 52;
  `src/lib/relative-time.ts` stops at weeks and bounds days at 7, so a readout can exceed §12's
  8 characters ("120 w ago"). That is logic, not a string, and outside this ticket — owner T-next.
- The row's gap chip reads the bare check id where §12 and §8.9 say "Must · {check id}", and
  `ItemRow.tsx` shows two chips where §8.27 keeps one at ≤112 wide. The two go together; a row
  ticket's — owner T-next.
- Pass 1's Shoulds 6 and 8 and pass 2's Shoulds 5–7 stand as recorded.
