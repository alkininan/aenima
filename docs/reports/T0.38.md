# T0.38 Design law checks — report (in progress)

Stopped at Decision on 2026-09-24, before review: C-34 needs a list product-spec does not have,
and C-33's "the §12 defaults appear verbatim" has two readings. Everything else is built, fixed
and green; this file holds the step-4 record so the run that carries the ticket on can write
the step-8 report from it.

## ACs built so far

- **AC1** C-01–C-04 — `scripts/design/spec-checks.mjs`, run as a script
  (`node scripts/design/spec-checks.mjs`, exit 1 on an offender) and as
  `scripts/design/spec-checks.test.mjs`. Passes over `docs/design-spec.md` v2.22.
- **AC2** C-06 and C-39's literal clause — `scripts/design/laws.mjs` `literalOffenders`, tested
  in `scripts/design/laws.test.mjs`. What it found, fixed: `0 0 #0000` → `0 0 transparent` (4),
  `z-index: -1` → `calc(var(--z-content) - 1)` (2), the field label's 13/18 and 17/22 → the
  `type-ui-label` class and §8.2's transform-only float with the 17/13 scale, `z-[1]` →
  `z-[calc(var(--z-content)+1)]`, `animate-pulse` → `.shimmer` (2), the secondary button's hover
  border `rgba(120,126,136,.72)` → `border-glass-border-hover`, the OTP boxes'
  `rounded-[22px]`/`rounded-[27px]` → `rounded-pill` (§8.2).
- **AC3** C-33's scan (sentence case, no `!`, never test/fail/violation) — `copyOffenders`,
  passes. C-33's defaults half and C-34: **not built — the Decision.**
- **AC4** C-35–C-38 — allowlists in `laws.mjs`, each use read by where it sits. What it found,
  fixed: `.glass` carried `backdrop-filter` everywhere; split into `.glass` (unblurred: modals,
  sheets, the pipeline strip) and `.glass-blur` (toasts), the `-webkit-` twin added; the
  activity feed's agent actor name no longer `text-agent`.
- **AC5** C-50 — `scrollbarClashes`. What it found, fixed: `.scroll-thin`'s standard properties
  moved inside `@supports not selector(::-webkit-scrollbar)`.

## Tests written, each observed red first

| test | reddened by | red → green |
|---|---|---|
| spec-checks C-02, C-04 (TC1) | `About 4 series, see §9.9.` planted under §9 of the real `docs/design-spec.md` | 2 failed / 3 passed → 5 passed |
| spec-checks C-01 ×2, C-03 (TC1) | footer set to `v2.21`, `` `--made-up` `` planted under §9 | 3 failed / 2 passed → 5 passed |
| laws C-06/C-39 repo scan, C-36, C-37, C-50 (TC2, TC4, TC5) | the tree before this ticket's `src/` fixes (the fix diff reverse-applied) | 4 failed / 11 passed → 15 passed |
| laws C-33, C-35, C-38 dot grid (TC3, TC4) | `"Send Code!"` in `en.ts`; `text-danger dot-grid` on `CARD_BASE` | 3 failed / 12 passed → 15 passed |
| laws C-38 page ground (TC4) | `bg-surface-1` on `loading.tsx`'s `<main>` | 1 failed / 14 passed → 15 passed |

Existing tests updated to §8.2's token: `variants.test.ts` "steps down below 768…" (expects
`rounded-pill`), and `e2e/form-language.spec.ts`'s two OTP radius assertions (the computed radius
equals `--r-pill`). `pnpm exec playwright test e2e/form-language.spec.ts`: 26 passed, the floated
label's geometry among them.

## Reviewer passes and findings

None yet — the review runs once the Decision is answered and the remaining ACs are built.

## Changed since this ticket was cut

design-spec v2.21 → v2.22. v2.22's §8.2 moved the floated label to a transform-only float, which
the code had not followed; C-39's literal clause found it.

## Open questions

- C-34: product-spec has no vocabulary list of synonyms (the Decision).
- C-33: which §12 defaults must appear (the Decision).
