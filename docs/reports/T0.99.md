# T0.99 — report

Smoke B: the Stop gate's release message reworded. The first ticket `/ticket` (T0.8) took
through a Decision: the board body named "the agreed wording" that nothing recorded, the
previous run asked, the human answered `default`, and this run read that answer, set Ready,
claimed and built. Epic E0.2 Pipeline · Type Technical · Priority Should.

## Files changed

`scripts/hooks/gate.mjs` (one line: the `RELEASE_MESSAGE` constant) ·
`scripts/hooks/gate.test.mjs` (the two release assertions pin the wording as a literal; the
now-unused `RELEASE_MESSAGE` import dropped, import block reformatted by prettier) ·
`docs/tickets/T0.99.md` · `docs/reports/T0.99.md` · `docs/build-log.md` (one line under
Tickets done).

## ACs implemented

| AC | Where | Test |
|---|---|---|
| AC1 on the third red and every stop after it, exit 0 and the agreed wording on stderr and in `systemMessage`, byte for byte | `scripts/hooks/gate.mjs:41` — the one constant both call sites (`:68`, `:88`) read | TC1 — `gate.test.mjs` "releases on the third red rather than refusing a fourth time" and "stays released without running the suite again", both asserting the literal |

The `systemMessage` half of AC1 is covered by construction, not by a test: `main()` at
`gate.mjs:250` writes `JSON.stringify({ systemMessage: stderr })` from the same `stderr` value
the tests assert on, and `JSON.stringify` leaves the em dash unescaped. The ticket's TC1 scopes
the observation to `stderr`; the reviewer's finding 1 below records the gap.

## Tests written, each observed red first

No new test file; the two existing release tests changed from `toBe(RELEASE_MESSAGE)` (the
constant compared with itself — green whatever it said) to `toBe(AGREED_WORDING)`, a literal.
Observed red against the old constant before `gate.mjs` changed:

```
× releases on the third red rather than refusing a fourth time
× stays released without running the suite again
AssertionError: expected 'gate released after three attempts — …' to be 'gate released after three reds — rest…'
Expected: "gate released after three reds — restate the ticket rather than push harder at the code"
Received: "gate released after three attempts — see the three-failed-corrections rule"
Tests  2 failed | 25 passed (27)
```

Then the constant changed and the same file ran `Tests 27 passed (27)`.

## Reviewer passes and findings

**Pass 1** — FINDINGS, no Must, three Should. No second pass.

1. Should — AC1 names two emission paths, TC1 observes one (`stderr`). Not fixed: the ticket's
   own Tests line scopes TC1 to `stderr`, and the `systemMessage` path forwards the same value
   (`gate.mjs:250`). Recorded above under ACs implemented. The reviewer's cheap test, if the
   board wants it observed: spawn `gate.mjs` with a `.claude/.gate-count` at `MAX_RED` for the
   session and read stdout — that path releases before any step runs, so it never spawns pnpm.
2. Should — the red-first observation was a claim in the ticket, not yet a record. Fixed: the
   failing assertion output is quoted above.
3. Should — the agreed wording says what happened and what to do next, not where the rule lives
   (AGENTS.md build principles; build-guide §6), which the ticket's Rules ask of a *refusal*.
   Not actionable here: the wording was fixed byte for byte by the Decision answer, and a
   release is not a refusal. Recorded for the board.

## Changed since this ticket was cut

Nothing. `version-drift.mjs` reports guidelines v1.3 and build-guide v2.2, both at the cited
version.

## Open questions

- Branch base, as T0.98: `origin/main` is at `bb41b25`; T0.8 (`7136df3`), which holds the
  `/ticket` skill and the run scripts, is on `t0-8` and not merged. The run branched `t0-99`
  off `origin/t0-8` (`AENIMA_RUN_BASE`), so the PR's diff against `main` shows T0.8 too until
  T0.8 lands. `migration-check.mjs` ranges `origin/main...HEAD` and reported no migration, which
  is also true of T0.8's diff. Owner: the human merging T0.8 first.
- `docs/reports/T0.7.md` lines 147–148 quote the old wording. Left as is: that is a transcript
  of what T0.7 observed, not a live reference.
