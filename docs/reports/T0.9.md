# T0.9 — report

Run fixes: what three live runs taught. The guard reads commands instead of text, a run leaves a
marker the guard and the next preflight read, a run that dies partway is recovered by the next
one, the sign-in test no longer reads a clock, §4 stops only when a wrong guess is expensive and
speaks in plain sentences, the reviewer runs only the ticket's tests, and a report is refused
without its red-first record. Guidelines v1.4, build-guide v2.3. Structural. Epic E0.2 Pipeline ·
Priority Must · Type Technical. Branch `t0-9` off `origin/main` at `c51954a`.

## Files added and changed

Added: `scripts/run/claim.mjs` · `scripts/run/release.mjs` · `scripts/run/stale.mjs` ·
`scripts/run/review-scope.mjs` · `scripts/run/report-check.mjs`, each with its `.test.mjs` beside
it (`release.mjs` is covered by `claim.test.mjs`, which is where the marker's life is one story) ·
`docs/tickets/T0.9.md` · `docs/reports/T0.9.md`.

Changed: `scripts/hooks/guard.mjs` and `guard.test.mjs` (the parser; rule (f); rule (b) names
T0.10) · `scripts/run/comments.mjs` and `comments.test.mjs` (`compose` replaces
`decisionComment`) · `scripts/run/cli.mjs` (the "pure function" line) · `scripts/run/README.md`
(the new scripts; commit under 9) · `.claude/skills/ticket/SKILL.md` (0c, the marker, §4 in
step 4, review scope, `report-check`, commit under 9, every comment through `compose`) ·
`.claude/agents/reviewer.md` (step 5 scoped; step 4 names the record) · `.claude/skills/ticket/SKILL.md`
step 0c again after review pass 2 (the skill hands over rows only; `stale.mjs` reads the marker
itself) · `scripts/run/stale.mjs` after pass 2 (the CLI reads the marker; `--atomic` on the
remote rename) · `scripts/hooks/guard.mjs` after passes 1 and 2 (below) · `.claude/settings.json`
(the SessionEnd hook) · `.gitignore` (`.claude/.run-active`) ·
`src/app/sign-in/SignInForm.dom.test.tsx` · `docs/guidelines.md` (v1.3 → v1.4) ·
`docs/build-guide.md` (v2.2 → v2.3) · `docs/build-log.md` (the ticket line; open questions
31–39 brought up to date).

## Build items, as shipped

| Item | Where |
|---|---|
| 1 Guard parser: commands split on `;` `&&` `\|\|` `\|` `&` newline and `\`-continuation, quotes honoured, heredoc bodies data, `$(…)`, backticks and `sh -c` read as the commands they run; rules on argv; `gh pr merge` refused while the marker exists | `guard.mjs` `parse`, `program`, `target`, `decide` rule (f) |
| 2 Marker: `.claude/.run-active` written at claim, removed on Review, Decision, and SessionEnd; a session removes only its own | `claim.mjs`, `release.mjs`, `settings.json` SessionEnd, `.gitignore` |
| 3 Stale recovery: absent or three-hour-old marker → branch kept as `t<id>-stale-<HHMM>` (WIP committed first), one comment, re-claim from `origin/main` | `stale.mjs` `assess`, `recover`; `SKILL.md` step 0c |
| 4 §4: stop only when expensive; every comment in plain sentences from one composer | `docs/guidelines.md` §4; `comments.mjs` `compose`; `SKILL.md` |
| 5 Reviewer scope: named tests plus touched test files, nothing else | `review-scope.mjs`; `reviewer.md` step 5; guidelines §5 |
| 6 Sign-in test: synchronous queries, no clock | `SignInForm.dom.test.tsx` |
| 7 Commit under 9 Close in the skill and the README; `cli.mjs` no longer says "pure" | `SKILL.md`, `README.md`, `cli.mjs` |
| 6 and 7, the board: the two Fix rows the fixture runs filed are closed — "Raise SignInForm dom test timeout" renamed **Sign-in dom test reads no clock**, "Reconcile skill step 8 with §5" renamed **Skill commit moved under 9 Close**, each Commit `666f152`, Status Done, set on 2026-09-09 at close | the board, Tasks |
| 8 Red-first record: step 4 keeps it, `report-check.mjs` refuses a report without it | `report-check.mjs`; `SKILL.md` steps 4 and 8; `reviewer.md` step 4 |
| 9 Guidelines v1.4: header, §3 one row, §4, §5, §2 and §8 name T0.10; build-guide v2.3: §2 twice and the hooks paragraph | `docs/guidelines.md`, `docs/build-guide.md` |

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC1 the guard reads commands, not text — the five cases decide as listed | TC1 | `guard.test.mjs` "TC1 — the guard reads commands, not text" (7) and `parse` (3); the 33 tests from T0.7 and T0.8 carry over unchanged, rule (b)'s owner aside |
| AC2 marker present between claim and exit, absent after each of three exits | TC2 | `claim.test.mjs` (8): present after claim; absent after Review, after Decision, and after the SessionEnd hook run as a child process with the hook's JSON; another session's marker left alone |
| AC3 absent → stale; fresh → not; old → stale | TC3 | `stale.test.mjs` `assess` (9), including the three-hour edge and an unreadable start |
| AC4 rename preserves the old head | TC4 | `stale.test.mjs` `recover` (4) over a temporary repository: head preserved, WIP committed above it and reachable, no branch → `renamed: null` |
| AC5 sign-in: 20 alone, 5 full, all green | TC5 | below |
| AC6 the reviewer runs only the named tests | TC6 | met on the fixture's review in its fresh session, not on this ticket's own — below, *TC6* |
| AC7 live fixture: stale-run comment and re-claim, then a default taken and Review reached | TC7, TC8 | below — both comments quoted from the board thread; the run reached Review at PR #5 without a Decision |

Not under an AC but tested: `review-scope.test.mjs` (5), `report-check.test.mjs` (7),
`comments.test.mjs` `compose` (7; `readThread`'s 11 unchanged).

## Tests written, each observed red first

Each mutation was applied to the source, the test file run, and the source restored and checked
byte-identical (`cmp`) before the next. The count is the file's: red under the mutation, then
green with the source restored.

| test | reddened by | red → green |
|---|---|---|
| `scripts/hooks/guard.test.mjs` "allows a heredoc that mentions db:migrate" and `parse` "attaches a heredoc body to the command that reads it" | M1 the lexer's `readHeredocs` call removed — bodies no longer consumed | 2 failed / 41 passed → 43 passed |
| `guard.test.mjs` "allows a quoted mention — echo "pnpm db:push" prints, it does not push" | M2 rule (a) matches any argv word (`cmd.argv.includes`) instead of the program | 1 failed / 42 → 43 |
| `guard.test.mjs` "refuses pnpm db:push itself, in every runner shape", "refuses pnpm db:push and names the policies it would drop", "refuses the underlying binary, not only the script alias", "refuses it and names the ticket that will lift the refusal", "reads the commands inside $(…), backticks and sh -c, because they run", "reads the executable's basename past assignments and wrappers" — and, re-run after pass 2 with the review's tests in the file, the six of those that go through `pnpm` | M3 `pnpm` dropped from `RUNNERS` | 6 failed / 37 → 43; re-run at pass 2: 12 failed / 38 → 50 |
| `guard.test.mjs` "sees the git push behind an assignment and a -C path", "sees git behind a leading assignment or a wrapper", "refuses a push that names main, in every refspec shape", "reads the executable's basename past assignments and wrappers" | M4 `program()` stops stepping over `FOO=1` assignments | 4 failed / 39 → 43; re-run at pass 2: 4 failed / 46 → 50 |
| `guard.test.mjs` "refuses gh pr merge while the run marker exists, and allows it without" | M5 rule (f) ignores the marker (`&& false`) | 1 failed / 42 → 43 |
| `scripts/run/claim.test.mjs` "is absent after the Review exit", "…the Decision exit", "…the error exit — the SessionEnd hook" | M6 `release()` no longer unlinks the marker | 3 failed / 5 → 8 |
| `claim.test.mjs` "is left alone by a session that did not write it" | M7 `release()` ignores who wrote the marker | 1 failed / 7 → 8 |
| `scripts/run/stale.test.mjs` "reads a marker older than three hours as stale", "treats exactly three hours as old", "reads a marker with no readable start as old" | M8 `assess()` never reads a marker as old | 3 failed / 10 → 13 |
| `stale.test.mjs` "reads an In progress task with no marker in this checkout as stale" | M9 `assess()` reads an absent marker as live | 1 failed / 12 → 13 |
| `stale.test.mjs` "renames the dead run's branch and preserves its head" | M10 `recover()` copies the branch (`branch -c`) instead of renaming it | 1 failed / 12 → 13 |
| `stale.test.mjs` "commits uncommitted work onto the stale branch first, so nothing is lost" | M11 `recover()` skips the WIP commit | 1 failed / 12 → 13 |
| `scripts/run/review-scope.test.mjs` "takes only test files from the diff" | M12 `touchedTests()` stops filtering to test files | 1 failed / 4 → 5 |
| `scripts/run/report-check.test.mjs` "refuses a row with an empty mutation or count cell" | M13 `checkReport()` stops checking cells | 1 failed / 6 → 7 |
| `scripts/run/comments.test.mjs` "writes every kind prefixed…", "names the kept branch on a stale run…" | M14 `compose()` drops the prefix on a stale comment | 2 failed / 16 → 18 |
| `guard.test.mjs` "refuses db:push behind a runner flag that takes a value" | the guard as committed at `666f152`: `target()` read the first non-flag after the runner as the script, so `pnpm -C . db:push` was `.` (review pass 1, Must 3) | 4 failed / 43 passed → 47 passed |
| `guard.test.mjs` "refuses drizzle-kit push behind --config, and allows a generate whose name says push" | the same guard: `drizzleKit()` read `operands(rest)[0]` as the verb, so `--config x push` was `x` | 4 failed / 43 → 47 |
| `guard.test.mjs` "refuses a production deploy that goes through a runner" | the same guard: rule (c) read `program()`'s executable, so `npx vercel --prod` was `npx` (Must 1) | 4 failed / 43 → 47 |
| `guard.test.mjs` "refuses gh pr merge with a repo flag between pr and merge" | the same guard: rule (f) read `operands(rest)`, which keeps a flag's value, so `gh pr -R x merge` was `pr x` (Must 2) | 4 failed / 43 → 47 |
| `guard.test.mjs` "steps over a shell reserved word to the command it introduces" | written against the parser at `66b3e21`, which read `then` and `do` as the executable (pass 2, Must 1); then M20, the reserved-word step disabled | 3 failed / 47 → 50; M20 1 failed / 49 → 50 |
| `guard.test.mjs` "reads $(…) and backticks inside an unquoted heredoc body, which the shell expands" | written against `66b3e21`, whose heredoc bodies were data whatever the delimiter (Must 2); then M21, the expansion of an unquoted body disabled | 3 failed / 47 → 50; M21 1 failed / 49 → 50 |
| `guard.test.mjs` "does not read a quoted argument to a runner's script as the script" | written against `66b3e21`, whose `invocation()` matched the script anywhere in the arguments (Must 3); then M22, the per-runner value-flag lists forgotten (which also reddens "refuses db:push behind a runner flag that takes a value"); then M23, the anywhere-match restored | 3 failed / 47 → 50; M22 2 failed / 48 → 50; M23 1 failed / 49 → 50 |
| `guard.test.mjs` `parse` "splits on every separator and keeps quoted runs whole", with "reads each line of a multi-line command as its own command", "refuses a push that names main, in every refspec shape", "reads force flags off the push itself, not off whatever follows it", "refuses every shape of shell write", "collects the write target and not the operands around it", "steps over a shell reserved word to the command it introduces" | M18 a separator no longer closes the command | 7 failed / 43 → 50 |
| `guard.test.mjs` "still finds a write target beside a heredoc" | M19 `writeTargets()` skips a command that carries a heredoc | 1 failed / 49 → 50 |
| `scripts/run/claim.test.mjs`, all eight: "records task, page, branch, a UTC start and the session", "is present between claim and exit", "is absent after the Review exit", "…the Decision exit", "…the error exit — the SessionEnd hook", "is left alone by a session that did not write it", "releases nothing when there is nothing, and says so", "reads an unreadable marker as none rather than throwing" | `scripts/run/claim.mjs` absent — the ticket's "missing file": the test file fails to load and no test runs | 1 file failed, 0 of 8 ran → 8 passed |
| `scripts/run/stale.test.mjs`, all thirteen: `assess` "reads an In progress task with no marker in this checkout as stale", "reads a fresh marker as a live run and claims nothing", "reads a marker older than three hours as stale", "treats exactly three hours as old, not fresh", "reads a marker with no readable start as old", "with a fresh marker naming one task, every other In progress task is stale", "with nothing In progress there is nothing live and nothing stale", "reads the id off the front of a name", "stamps HHMM in UTC"; `recover` "renames the dead run's branch and preserves its head", "commits uncommitted work onto the stale branch first, so nothing is lost", "says there was no branch when the run died before making one", "leaves a clean checked-out branch without a WIP commit" | `scripts/run/stale.mjs` absent | 1 file failed, 0 of 13 ran → 13 passed |
| `scripts/run/review-scope.test.mjs`, all five: "takes the Tests section and nothing after it", "names the test files the Tests section cites, once each, in order", "takes only test files from the diff, and nothing when git cannot diff", "is named ∪ touched, existing files only, with what the ticket cites and lacks", "asks git for the diff against the base it is given" | `scripts/run/review-scope.mjs` absent | 1 file failed, 0 of 5 ran → 5 passed |
| `scripts/run/report-check.test.mjs`, all seven: "finds the Tests written section and stops at the next heading", "reads the table, header then rows, skipping the rule line", "accepts a report whose every test carries its mutation and its count", "refuses a report with no red-first section at all — T0.8's", "refuses a row with an empty mutation or count cell", "refuses a section with no table, or a table missing the two columns", "refuses when a test file in the diff is not in the record" | `scripts/run/report-check.mjs` absent | 1 file failed, 0 of 7 ran → 7 passed |
| `scripts/run/comments.test.mjs` `compose`: "writes every kind prefixed, in two to four sentences, with no labels", "says what it stopped on, where the gap lives in words, and what default takes", "names the two readings it cannot pick between", "leaves a migration in the diff and says whose move applying it is", "names the kept branch on a stale run, or says there was none to keep", "says what it chose when the choice was cheap, and keeps going" | M25 `compose()` returns the bare kind for every known kind | 6 failed / 12 → 18 |
| `comments.test.mjs` `compose` "refuses a kind it does not know rather than posting something unshaped" | M24 `compose()` returns undefined for an unknown kind instead of throwing | 1 failed / 17 → 18 |
| `src/app/sign-in/SignInForm.dom.test.tsx` "does not dress an outage up as a bad code" | the red itself, reproduced before the change: three full suites run at once, the file took 19.9s and `findByText` gave up at its one-second clock — `TestingLibraryElementError: Unable to find an element with the text: Sign-in is unavailable right now.` | 1 failed / 19 passed under load → 20 passed, 20/20 alone and 5/5 in the full suite |

Three further mutations of the sign-in file are recorded because they changed what shipped.
M15 replaced the first draft's `settle()` helper (an empty async `act`) with a plain resolved
promise: 20 passed. M16 removed every wait so the queries ran straight after the click: 20
passed. M17 made the wait a real macrotask: 20 passed. So the `act` flush the draft leaned on
adds nothing — userEvent already dispatches inside `act`, and the transition has committed by the
time `user.click` resolves. The helper was removed and the file says so: the only thing the old
polls added was a one-second real clock, and that is what went red.

## TC5 — the sign-in file

Twenty consecutive runs alone, all `20 passed`, 2.50s–2.57s each. Full suite, sequential, nothing
else running: runs 3, 4, 5 and 6 `1013 passed (1013)` in 43–45s; runs 1 and 2 each had one red
outside this file while a test of mine ran alongside, and the file itself was green in all five.
The two reds in runs 1 and 2 were not captured by name at the time and are not reproduced since:
the reviewer's own full run at the first review pass was `1013 passed` in 44.8s, and the Stop
gate's runs at each turn's end have been green. Open question 2 stays open on that basis rather
than on further runs. Before the change, with three suites running at once, the file took 38s
on its first run alone (green) and went red inside one of the three suites (the quote above).

## The five comment texts

Composed by `scripts/run/comments.mjs`; the sentences that need judgment are the skill's.

- **Decision** — `⟡ I've stopped on the release wording. The body asks for "the agreed wording"
  and that isn't written down anywhere, not in the ticket and not in the specs. If you say
  "default" I'll use build-guide §6's own sentence.`
- **Clarifying** — `⟡ Thanks, I read that, but it still fits two readings: the sentence as it
  stands, or the sentence with the rule's location added. If you say "default" I'll keep it as it
  stands.`
- **Migration** — `⟡ This change adds a migration, drizzle/0013_activity_trigger.sql, and applying
  it to the shared database is your call. I've left it in the diff and stopped here. Once you've
  applied it, say so on this thread and the next run picks the ticket back up.`
- **Stale run** — `⟡ This run stopped partway on 9 September. I've kept the branch as
  t0-97-stale-1607 in case anything on it is worth salvaging, and started again from main.` With
  no branch to keep: `⟡ This run stopped partway on 9 September before it made a branch, so
  there's nothing to salvage. I've started again from main.`
- **Chose the default** — `⟡ The body asks for a friendlier release message but doesn't say what
  friendlier means, and nothing else does either. A wrong guess here costs nothing to change, so I
  went with "…" and kept going. Say the word if you'd rather something else.`

## TC7 and TC8 — the fixture

"T0.97 Smoke C" was created on the board at In progress with no marker in any checkout and no
branch anywhere, body *Make the gate's release message friendlier*, and run by the human from a
fresh Code tab with `t0-9` checked out, on 2026-09-09. The run ended at Review — PR #5, Commit
`180ef7d` — without a Decision. Both comments below are quoted from the board thread, byte for
byte; `scripts/run/comments.mjs` composed them from the sentences the run supplied.

**TC7 — the stale run.** Preflight 0c handed `stale.mjs` the one In progress row and a null
marker and read back `markerState: "absent"`, `live: null`, the row under `stale` with
`reason: "no run marker in this checkout"`. `stale.mjs --recover T0.97` found no `t0-97`
branch — `renamed: null`, `head: null`, `wip: false` — and the run posted:

> ⟡ This run stopped partway on 9 September before it made a branch, so there's nothing to salvage. I've started again from main.

It then claimed the same row without touching its Status: `claim.mjs` wrote the marker at
`2026-09-09T17:21:33Z` and `branch.mjs` made `t0-97` off `t0-9` (`AENIMA_RUN_BASE`, this
branch not being on `main`; the run said so in its ticket file and report).

**TC8 — the default.** Step 4 met the gap — "friendlier" is defined nowhere in the ticket or
build-guide §6 — read the gate's output as a developer surface under §4, and posted one comment
instead of stopping:

> ⟡ The ticket asks for a friendlier release message and does not say what friendlier means; that is not written down in the ticket or in build-guide §6, and the gate's output is a developer surface, not copy a product user sees. A wrong guess here costs nothing to change, so I went with plain sentences that say what happened and where the rule lives: "Three reds in this session, so the gate has stepped aside. That usually means the ticket needs restating rather than the code pushing harder (build-guide §6)." and kept going. Say the word if you'd rather something else.

It kept building: the constant changed, two tests observed red first and recorded, one reviewer
pass with no Must, `report-check` passed, PR #5 opened, Status set to Review and Commit to
`180ef7d`, the marker released and the primary checkout returned to `main`. Under v1.3's §4
the same body stopped as Smoke B; under v1.4's it did not.

## TC6 — the reviewer's scope

Not observable in this ticket's own cold review, on any of its three passes: the subagent
definition a session registers is the one on disk at session start (build-log 35), and the
session that rewrote `reviewer.md` step 5 ran under the definition from before it — so each pass
ran the full suite as that definition told it to, and then the scoped list beside it (pass 1:
the seven files `review-scope.mjs` names, `missing: []`, 114 tests, 3.07s, green). The
observation that meets AC6 is the fixture's: T0.97 was run from a fresh Code tab with `t0-9`
checked out, its session loaded the rewritten reviewer, and its report records — "The reviewer
ran `review-scope.mjs` against `t0-9` (this ticket's base): `gate.test.mjs` only, nothing
missing; 28 passed." TC6 as written ("this ticket's own cold review") named the one session
that could not show it.

## Reviewer passes and findings

**Pass 1 — four Must, seven Should.** The three code Musts were one hole of one shape in the
new parser: a flag that takes a value, sitting where the rule expected its operand, was read
as the operand. Each was refused by the v1.3 text rule and let through by the v1.4 parser.

1. Must — rule (c) read `program()`'s executable, so `npx vercel --prod`, `pnpm exec vercel
   deploy --prod` and `pnpm dlx vercel --prod` were allowed. Fixed: `invocation()` finds
   `vercel` behind a runner and rule (c) reads what follows it.
2. Must — rule (f) kept a flag's value among `operands(rest)`, so `gh pr -R owner/repo merge 3`
   was allowed with the marker present. Fixed: `ghWords()` steps over `-R`/`--repo` and their
   value.
3. Must — `target()` took the first non-flag after a runner as the script, so `pnpm -C . db:push`,
   `pnpm --dir . db:push` and `pnpm --filter aenima db:push` were allowed; `drizzleKit()` read
   the verb at `[0]`, so `drizzle-kit --config x push` was allowed. Fixed: `invocation()` finds
   the script or binary among the arguments wherever a value put it, and the drizzle-kit verb
   is the first operand that is one of drizzle-kit's own commands — so `generate --name push`
   stays a generate. Four tests, each observed red first (the table above); 47 in the file.
4. Must — the report claimed a build-log ticket line the diff did not contain. Fixed: the line is
   under Tickets done, before T0.97's.

Shoulds, recorded and not changed here:

5. A `#` comment is prose that still matches — `git push origin t0-9 # not -f` is refused as a
   force-push; and `bash -lc '…'` is not read as nested because only the exact token `-c` is
   looked for. Build-log 34's line, one case further in. Owner: none, unless a run is seen
   crossing it.
6. The SessionEnd trap assumes the Bash env's `CLAUDE_CODE_SESSION_ID` equals the hook payload's
   `session_id`; `claim.test.mjs` sets both by hand and the fixture exited through Review, so the
   hook path has not run live. If they differ, an error exit leaves the marker and the next run
   within three hours exits as "a run is in progress". One observation settles it. Open question 5.
7. AC6 could not be observed exactly as specified — the caveat under *TC6*.
8. The red-first record is per cluster, not per test: `parse` "splits on every separator",
   `claim.test.mjs` "records task, page, branch", `stale.test.mjs` "reads a fresh marker as a
   live run" and their neighbours have no row of their own, and `report-check.mjs` checks only
   that a touched file is named in the section. The tool enforces less than SKILL step 4 asks.
   Owner: T0.10, when `report-check` grows a per-test comparison, or the skill's step 4 says
   "per file".
9. The skill reads the marker — `$(cat .claude/.run-active …)` in step 0c — where item 2 says
   only scripts do. `stale.mjs` could read it itself when `input.marker` is absent. Owner: T0.10,
   which moves runs to worktrees and touches 0c anyway.
10. §3's new row "In progress → Ready | M" sits beside §4's "The pipeline never sets a task to
    Ready without a comment from you"; the row is defensible (the human's go was given at
    Backlog → Ready) and §4 should name the exception in one clause. Owner: the next guidelines
    version.
11. The TC5 section argued with itself and left two reds unnamed. The sentence is fixed above;
    the reds stay unnamed — open question 2.

**Pass 2 — five Must, five Should.** Three more Musts of pass 1's shape — a word sitting where
the parser expected its operand — and two about the record.

1. Must — a shell reserved word became the executable: `if true; then pnpm db:push; fi`,
   `for x in a; do pnpm db:migrate; done` and `while true; do …; done` were allowed, and v1.3
   refused all three. Fixed: `RESERVED` is stepped over at the start of a simple command.
2. Must — an unquoted heredoc body was data whatever its delimiter, while bash expands it:
   `cat <<EOF\n$(pnpm db:push)\nEOF` runs the push. Fixed: the delimiter's quoting is recorded
   and an unquoted body's `$(…)` and backticks are read as the nested commands they are; a
   quoted delimiter keeps the whole body literal. The ticket's "heredoc bodies as data" is now
   true exactly as far as the shell makes it true.
3. Must — pass 1's fix matched the script anywhere in a runner's arguments, so
   `pnpm vitest run -t "db:push"` and `pnpm exec grep "db:push" f` were refused — a session
   testing this guard types the first. Fixed: the script is read at its position with each
   runner's value-taking options stepped over (`RUNNER_VALUE_FLAGS`, per runner, because `-w`
   is a value for npm and a boolean for pnpm). `pnpm --filter aenima db:push` stays refused.
   Three tests, each observed red first, then five mutations (the table above); 50 in the file.
4. Must — the two Backlog Fix rows were closed on the board in this session but the report did
   not say so. Said now, under *Build items* and in the build-log line.
5. Must — the record was per cluster where the ticket's Tests line says per test. Every test in
   the six files now has a row: the neighbours named with their counts re-run at pass 2, and
   the ticket's "missing file" used for the four new source files, one row per file naming
   every test in it. The pass-1 Should 8 that deferred this to T0.10 is withdrawn; what stays
   for T0.10 is `report-check.mjs` enforcing per-test rather than per-file.

Shoulds, each taken here because it was one line, except the one that is a document:

6. Build-log 34 now names the shapes the probes crossed: `echo '…' | sh`, `bash -lc`, a `#`
   comment, and a runner option that takes a value and is not in the parser's list.
7. `recover()`'s remote rename pushes `--atomic`, so a rejected create cannot leave origin
   with neither copy. The temp-repo test has no origin; the remote path stays unexercised —
   open question 7.
8. The report no longer announces a pass it does not contain.
9. The skill no longer reads the marker: step 0c hands `stale.mjs` the rows and the script
   reads `.claude/.run-active` itself (`readMarker` from `claim.mjs`); a `"marker"` field on
   stdin is still honoured so `assess` can be driven from a test. Ticket item 2 holds as written.
   The three lines are CLI plumbing over an unchanged `assess` and carry no test of their own.
10. §3's row "In progress → Ready | M" beside §4's "never sets a task to Ready without a comment
    from you" — the defaults list below says why the row was cheap; the clause §4 should carry
    is open question 6, for the next guidelines version.

**Pass 3 — one Must, uncertain, and five Should.** The protocol's last pass; the loop stops
here whatever stands (§5 step 5).

1. Must, uncertain — guidelines v1.4 said two things about the pipeline setting Ready: §3's
   new row and §4's bold sentence, in one diff, with the reconciling clause deferred to a later
   version. Under the ticket's own rule — apply the new §4 to this ticket's choices — a spec
   that says both things is the gap §4 exists for. Taken after the third pass without a fourth
   review, because it is one clause in a document this ticket rewrites: §4 now reads, after the
   bold sentence, *"The one exception is §3's return of a task to Ready whose Ready you already
   gave: a preflight that recovers two stale runs claims one and returns the other, and your go
   at Backlog → Ready is not withdrawn by a run dying."* Open question 6 closes with it.
2. Should — `recover()` reports `wip: true` without reading the commit's status; with no git
   identity or a failing hook the dirty tree carries onto the renamed branch and the result
   still says the work was kept. Recorded, not changed: open question 8.
3. Should — three shapes v1.3 refused now pass: `eval "…"`, `xargs pnpm db:push`,
   `yarn workspace aenima db:push`. Named in build-log 34 as the edge, beside `echo … | sh`
   and `bash -lc`; not closed. Open question 3 carries them.
4. Should — a command found inside an unquoted heredoc body is refused with the rule's own
   reason, which does not tell the session that quoting the delimiter keeps the body literal.
   Recorded: open question 9.
5. Should — the TC6 paragraph said T0.97's review ran in this session; it ran in the fixture's
   fresh session, which is the only one that could have loaded the rewritten reviewer. The
   paragraph and the AC6 row now say so and quote the T0.97 report. Open question 5 narrowed.
6. Should — `scripts/run/branch.mjs` still said T0.9 moves runs to worktrees. Now T0.10.

## Defaults taken under the new §4

Each cheap to undo, each said once here rather than stopped on.

- **SessionEnd is the trap.** It is the only hook the runtime fires on termination; it cannot
  block, which is fine for cleanup. A hard kill fires nothing, which the three-hour age covers.
- **The reviewer runs only the named tests** — not lint, not typecheck. Both are the gate's.
- **Recovery commits a dirty tree onto the stale branch** before renaming, and renames a pushed
  copy on origin the same way, so salvage is complete.
- **Several stale tasks:** each recovered and commented; `pick-next` orders them, the first is
  re-claimed, the rest return to Ready (§3 gained that row).
- **Guard rule (b) names T0.10** as the owner of the answered-migration path; the test that
  pinned "T0.8" now pins "T0.10".
- **Build-guide's three stale lines** (§2 twice, the hooks paragraph) patched, v2.2 → v2.3;
  guidelines §2, §5 and §8 now say T0.10 where they said T0.9.
- **The parser reads `$(…)`, backticks and `sh -c "…"`** as nested commands and steps over
  `sudo`/`time`/`nohup`/`nice` — a few lines past the ticket's list, in the direction that
  refuses more, and build-log open question 34 says where it stops.
- **`<HHMM>` is UTC**, as every stored timestamp is (CLAUDE.md).
- **T0.9's board row stayed at Backlog until close.** A row at In progress with no marker is
  what the fixture tests, and the fixture run must not recover T0.9 from under itself.
- **§3 gained "In progress → Ready | M"** for the second of two stale tasks recovered at one
  preflight. It is in a spec, which §4 calls expensive, and was cheap anyway: the human's go was
  given at Backlog → Ready and a run dying does not withdraw it, so the row records a return,
  not a decision the pipeline made. Decision → Ready still needs the human's comment.

## Changed since this ticket was cut

Guidelines v1.3 → v1.4 and build-guide v2.2 → v2.3 are this diff. Product-spec v1.6 and
design-spec v2.17 are unchanged. The board renamed the ticket that was `T0.9 Schedule and
telemetry` to `T0.10` and cut this one as `T0.9 Run fixes`; the repo's references followed.

## Open questions

1. **Product-spec §11 still describes the product's Decision section in three labelled lines**
   (Question · Where · Default, written into the ticket body), while guidelines §4 now has the
   board speak in plain sentences. This ticket changed the board's comment voice and left §11
   alone: one is the product's ticket body, the other the dev board's thread. If the product's
   Decision section should read the same way, that is a spec patch and a version bump. Owner:
   the human, as a spec patch, or a Content task under E0.2.
2. **Two unnamed reds in the first two full-suite runs**, one test each, outside the sign-in
   file, while a single-file vitest run of mine overlapped them. Runs 3–6 were clean, and so
   were the reviewer's full run and the gate's since; the names were not captured. If it recurs
   it is a Fix task, not this ticket's. Owner: E0.2, on recurrence.
3. **`bash file.sh`, `python -c`, `node -e`, `eval`, `xargs`, `yarn workspace`** and an editor
   still run what the guard cannot read (build-log 34, which names each). The parser reads
   command lines and is not a shell; the line is drawn there deliberately. Owner: none, unless a
   run is seen crossing it.
4. **The stale-run marker is per checkout.** A run in a worktree writes its marker there and the
   primary's preflight cannot see it; today every run is in the primary checkout. Owner: T0.10,
   with worktrees.
5. **The SessionEnd release has not run live.** `CLAUDE_CODE_SESSION_ID` is set in a Bash
   environment (observed: this session's id, and a subagent's Bash carries the host's); what is
   unobserved is only its equality with the SessionEnd payload's `session_id`, which the hook
   prefers. Ending a session with a marker present and reading the hook's emitted JSON is one
   observation. Owner: T0.10, before the schedule relies on it.
6. Closed at pass 3 — §4 carries the clause (pass 3, Must 1).
7. **`recover()`'s remote path is untested.** The temporary repository has no origin, so the
   `--atomic` rename of a pushed stale branch has only been read, not run. Owner: T0.10, whose
   worktree move touches `recover` anyway.
8. **`recover()` says `wip: true` without reading the commit's status** (pass 3, Should 2). One
   line — `wip = g([...commit...]).status === 0` — and a `detail` when it is false, with a
   test that breaks the commit. Owner: T0.10, with 7.
9. **A refusal for a command inside an unquoted heredoc names the rule, not the fix** (pass 3,
   Should 4): a session writing a README with backticks around `pnpm db:migrate` is refused as
   if it were applying a migration, and nothing tells it `<<'EOF'` keeps the body literal. The
   nested token would carry an origin through `parse()` and the reason gain one sentence. Owner:
   E0.2, Type Fix, when a run is seen hitting it.
