# T0.13 — report

Restrict Vercel's database role: production's connection on Vercel runs as `aenima_pipeline`,
the restricted role every local and pipeline connection has used since T0.10, so the admin
credential is on no path at all — not a run's and not the deployed app's. Guidelines v1.13.
Technical. Epic E0.2 Pipeline · Priority High · Type Technical. Branch `t0-13` off
`origin/main` at `b53c4c0`, with `origin/main` merged in at `0870c08` when the run resumed and
again at `6526f96` in the addendum round and at `cbdc9aa` in the re-close round. Claimed by a scheduled run on 2026-09-13, stopped under
§4 for the human's step the same day, resumed by a scheduled run on 2026-09-14 on the human's
answer, sent back from Review on 2026-09-15 for a rebase before merge, rebuilt by a scheduled
run the same evening, and sent back again on 2026-09-16 to close under T0.21's step 9.

**What the run did not do, said first.** The change itself — the Production `DATABASE_URL` on
Vercel and the redeploy — was the human's, made by hand on 2026-09-13 (no Vercel CLI on the
machine, no login, a public surface). The run's part was the stop, the checks from the outside
once the change was live, and the sentence in guidelines §5. The signed-in `/app` load, the one
path that touches the database, is not observable by a run (the OTP goes to a mailbox) and is
recorded as the human's word, quoted under AC2. No code, no schema, no migration, no test file.
The addendum round left the task at Review with the word waiting; the re-close round closes it
under §5 step 9 as main now reads, which merges a diff that adds no migration and weakens no
restraint on the reviewer's PASS.

## Addendum round — rebase before merge

PR #14 conflicted with `main` in `docs/guidelines.md` and `docs/build-log.md`: six pull requests
merged to `main` after round one's base — T0.16, T0.14, T2.8, T0.17, T0.12 and T0.20 — four of
which (T0.16, T0.17, T0.12, T0.20) bumped the guidelines header, and all six added a line to the
build log's list. The human's `merge` was verified by the guard and GitHub refused it. A
first attempt at this round, earlier on 2026-09-15, was refused at checkout because `t0-13` was
still checked out in another worktree; the human removed that worktree and replied.

| Item | Where |
|---|---|
| Merge `origin/main` into `t0-13` | Merge commit on the branch, `origin/main` at `6526f96` |
| `docs/guidelines.md`: keep main's text, re-apply the one §5 sentence | main's side taken whole, then the capability-boundary paragraph edited exactly as round one edited it: *So the credential a run is handed cannot change schema.* ends its own sentence, the *Production is the same role* sentence follows, and the Deploys sentence says why the production variable is the human's to set. Main had rewritten the paragraph's opening (*Two database credentials*); that stays as main has it |
| Bump the header if the number moved | It had: `main` is at v1.11, so the header is v1.12 with the change named. Round one's v1.7 line was on this branch only and is gone; the v1.12 entry says that number never reached main. The ticket file's AC3 and TC3 now name v1.12 |
| The build-log list regenerated, never hand-merged | main's `docs/build-log.md` taken whole, then `node scripts/run/log-index.mjs` (`written: true`, 38 entries); the T0.13 entry's timestamp moved to this round, so the list places it last |
| Re-run TC3 against the new number | Quoted under AC3 |

## Re-close round — under T0.21

The rebase round left the pull request clean at `a935d08` and the task at Review. Two pull
requests merged to `main` after it — T2.10 and T0.21 — and T0.21 bumped the guidelines header to
v1.12 and added a build-log line, so `t0-13` conflicted again in the same two files. T0.21 also
replaced the gated paths with a measurement (`scripts/run/loosening.mjs`), which is why the board
sent the task back: under step 9 as it now reads, this diff is the run's own to merge.

| Item | Where |
|---|---|
| Merge `origin/main` into `t0-13` | Merge commit `4cadfc8`, `origin/main` at `cbdc9aa` |
| `docs/guidelines.md`: main's text kept, the one §5 sentence re-applied | main's side taken whole (`git checkout --theirs`), then the capability-boundary paragraph replaced exactly as the rebase round replaced it; main had not touched that paragraph since, so the diff against `origin/main` is the same eleven lines |
| The header | v1.13 with the change named; the entry now says this change held v1.7 and then v1.12 on its branch, and that v1.12 is T0.21's. AC3 and TC3 in the ticket file name v1.13 |
| The build-log list regenerated | main's `docs/build-log.md` taken whole, then `node scripts/run/log-index.mjs` (`written: true`, 40 entries) |
| The ticket file | An `## Addendum — re-close under T0.21` section with the board's addendum verbatim; its three criteria are AB1–AB3 and TB1–TB3 there, since AA1–AA3 already name the rebase round's; §5 as it reads on `origin/main` at `cbdc9aa` inlined under Cited for AB3 |

## Files added and changed

Added: `docs/reports/T0.13.md` · `docs/log/T0.13.md` · `docs/tickets/T0.13.md` · `docs/reviews/T0.13.md`
(the reviewer's verdict, written by the reviewer).

Changed: `docs/guidelines.md` (header v1.12 → v1.13; §5 the capability-boundary paragraph) ·
`docs/build-log.md` (Tickets done regenerated by `log-index.mjs`).

Against `origin/main` the diff is those six files and nothing else.

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC1 Vercel's Production `DATABASE_URL` names `aenima_pipeline` and production has been redeployed since, confirmed by the human on the thread | TC1 | The human's reply on the task thread, 2026-09-13 17:20 UTC, verbatim: *done — /app loads signed in*. It answers the pipeline's comment of 16:53 UTC, which asked for exactly that: the Production value set to the `aenima_pipeline` string, a redeploy, and a reply that it is done and that `/app` loads signed in |
| AC2 After the redeploy `/sign-in` returns 200, `/app` unauthenticated redirects to `/sign-in`, and the human confirms `/app` loads signed in | TC2 | Observed 2026-09-14 15:28 UTC, 2026-09-15 20:15 UTC and 2026-09-16 09:13 UTC; the last is quoted below. The signed-in load: the human's word under AC1 |
| AC3 docs/guidelines.md is v1.13; §5's capability-boundary paragraph says the production connection is `aenima_pipeline` and the admin credential is on no path at all; the header comment names the change | TC3 | `version-drift.mjs "guidelines v1.13 §5"` → `drifted: false`; `spec-sections.mjs` for guidelines §5 returns the sentence once; both quoted below. The number is v1.13 — see the re-close round |
| AA1 `t0-13` merges cleanly into main and §5 still carries the sentence once | TA1 | `git merge-tree --write-tree --name-only origin/main HEAD` → exit 0, no conflicted file; the §5 count is TC3's |
| AA2 TC3's two checks pass against the header's current number | TA2 | TC3 as run below, at v1.13 |
| AA3 the build-log list is the generated one | TA3 | `pnpm test scripts/run/log-index.test.mjs` → 12 passed |
| AB1 `t0-13` merges cleanly into current main, and §5 carries the sentence once | TB1 | TA1's command re-run after this round's merge, at `faa6096`: exit 0, no conflicted file; TC3's count is 1 |
| AB2 `loosening.mjs` over `origin/main...HEAD` reports `ok: true` with no reasons, and `gated.mjs` reports `ok: true` with no reasons | TB2 | Both quoted below, with the same detector refusing a scratch commit that drops the Stop hook |
| AB3 the close follows §5 step 9 as it reads on main today | TB3 | `gated.mjs` quoted below; the verdict is `docs/reviews/T0.13.md`, written by the reviewer for this round. The gate on the pushed tree and the merge come after this file is committed, so they are on the board: Done with a Releases row, or Review with one `gated` comment |

TC2, as run in the re-close round:

```
$ curl -sI https://aeni.ma/sign-in
HTTP/2 200
date: Wed, 16 Sep 2026 09:13:18 GMT

$ curl -sI https://aeni.ma/app
HTTP/2 307
date: Wed, 16 Sep 2026 09:13:18 GMT
location: /sign-in
```

The run's own deploy check at preflight (`health.mjs`, `main` at `cbdc9aa`) answered the same:
`/sign-in` 200, `/app` 307, `ok: true`. The addendum round saw the same two answers on
2026-09-15 20:15 UTC.

TC3 and TA1, as run after the merge:

```
$ node scripts/run/version-drift.mjs "guidelines v1.13 §5"
{ "cited": [ { "doc": "guidelines", "cited": "1.13", "repo": "1.13", "sections": ["5"], "drifted": false } ], "drifted": false }

$ echo '{"cited":[{"doc":"guidelines","sections":["5"]}]}' | node scripts/run/spec-sections.mjs | grep -o 'on no path at all' | wc -l
1

The sentence, as §5 now holds it: Production is the same role: Vercel's Production `DATABASE_URL`
names `aenima_pipeline`, set by hand and redeployed on 2026-09-13 (T0.13), so the admin credential
is on no path at all — not a run's and not the deployed app's — and `.env.migrate` in the primary
checkout is its only home.

$ git merge-tree --write-tree --name-only origin/main HEAD      # at faa6096
322f7a0a63e1cfed6e6af8e1564209cb5d5c9434
(exit 0)
```

TB2, as run at `faa6096`, and the same detector over a scratch commit — `4cadfc8` with the
`Stop` hook deleted from `.claude/settings.json`, built with `git commit-tree` and never on a
branch:

```
$ node scripts/run/gated.mjs
{ "range": "origin/main...HEAD",
  "files": ["docs/build-log.md", "docs/guidelines.md", "docs/log/T0.13.md",
            "docs/reports/T0.13.md", "docs/reviews/T0.13.md", "docs/tickets/T0.13.md"],
  "reasons": [], "gated": [], "ok": true }

$ node scripts/run/loosening.mjs
{ "ok": true, "reasons": [] }

$ node scripts/run/loosening.mjs origin/main...620543c   # the scratch commit
{ "ok": false,
  "reasons": [ { "rule": "the Stop * hook that runs scripts/hooks/gate.mjs no longer carries main's command",
                 "ungate": "restore it — a guard nothing invokes refuses nothing" } ] }
```

## Tests written, each observed red first

No test file was added or changed: the ticket's Rules say no code change, and its tests are a
quoted reply, observed commands, and one existing test the addendum names by what it checks. The
record below is of those, red first where a red can be observed.

| test | reddened by | red → green |
|---|---|---|
| TB1 `git merge-tree --write-tree --name-only origin/main HEAD` (re-close round) | run at claim, on the branch as the addendum round left it (`a935d08`) against `origin/main` at `cbdc9aa` | exit 1, `CONFLICT (content)` in `docs/build-log.md` and `docs/guidelines.md` → exit 0, no conflicted file |
| TB2 `loosening.mjs` over the diff | a scratch commit, `4cadfc8` with the `Stop` hook deleted from `.claude/settings.json` | `ok: false`, one reason (*the Stop hook … no longer carries main's command*) → `ok: true`, no reasons, over `origin/main...HEAD` |
| TB3 the close under step 9 | not a check that runs before the close: the rebase round's close, under the old step 9, left the task at Review with the word waiting | Review at `a935d08` → the board's record of this close |
| TC3 `version-drift.mjs "guidelines v1.13 §5"` (re-close round) | run after main's side of `docs/guidelines.md` was taken whole, before the sentence was re-applied | `drifted: true` (cited 1.13, repo 1.12) → `drifted: false` (repo 1.13) |
| TC3 guidelines §5 carries the sentence once (re-close round) | the same point: main's side taken whole | `0` → `1` in the §5 body `spec-sections.mjs` returns |
| TA3 `scripts/run/log-index.test.mjs` (re-close round) | main's `docs/build-log.md` at `cbdc9aa` put back over the regenerated list | 2 failed / 10 passed → 12 passed |
| TA1 `git merge-tree --write-tree --name-only origin/main HEAD` | run before the merge, on the branch as round one left it | exit 1, `CONFLICT (content)` in `docs/build-log.md` and `docs/guidelines.md`; `gh pr view t0-13` → `mergeable: CONFLICTING`, `mergeStateStatus: DIRTY` → exit 0, no conflicted file |
| TA2 / TC3 `version-drift.mjs "guidelines v1.12 §5"` (addendum round) | run before the merge, with the branch's header at v1.7 | `drifted: true` (cited 1.12, repo 1.7) → `drifted: false` (repo 1.12) |
| TA2 / TC3 guidelines §5 carries the sentence once | main's side of the conflicted file taken whole: `grep -c "on no path at all"` over main's `docs/guidelines.md` | `0` → `1` in the §5 body `spec-sections.mjs` returns |
| TA3 `scripts/run/log-index.test.mjs` | main's `docs/build-log.md` put back over the regenerated list, with `docs/log/T0.13.md` present | 2 failed / 10 passed (*matches docs/log/ entry for entry*, *has one list line per file*) → 12 passed |
| TC2 `curl -sI` on `/sign-in` and `/app` | not reddenable from outside — an unauthenticated route never touches the database, so a wrong `DATABASE_URL` would not change either answer; the row records the observed values, and the signed-in load is the human's word | `200` and `307 location: /sign-in`, observed 2026-09-14 15:28 UTC, 2026-09-15 20:15 UTC and 2026-09-16 09:13 UTC; the same two answers `docs/log/deploy.md` recorded at the first deploy |
| TC1 the human's reply, quoted | not a command: the run stopped at Decision on 2026-09-13 and the thread held no answer (red) until the reply of 17:20 UTC (green) | no reply → *done — /app loads signed in* |

## Reviewer passes and findings

### Round one (2026-09-14)

**Pass 1.** No Must, three Should, all settled then. The ticket file's AC3 and TC3 said v1.6 and
were brought to that round's number; the new §5 sentence hung "cannot change schema" on the
Vercel change and the two conclusions were split; the report's TC3 quote was cut short and was
made whole. Round one predates the verdict file, so it left none.

### Addendum round (2026-09-15)

**Pass 1 — PASS**, at `836ad01`; verdict in `docs/reviews/T0.13.md`. No Must, five Should. The
reviewer re-ran TA1 (merge-tree exit 0; the §5 paragraph identical to round one's), TA2
(`drifted: false` at v1.12; the sentence once in §5), TA3 (12 passed, and `log-index.mjs` itself
wrote nothing), TC2 (200; 307 to `/sign-in`), matched Criteria and Tests both ways, and read the
red-first record row by row.

The first invocation of the reviewer ended before it read anything — the session's default model
was out of usage credits (HTTP 429) — and left no verdict file. It was invoked again with the same
one-line message on Opus, the fallback `.claude/settings.json` names.

1. Should (the reviewer marked it unsure) — §5 says the admin credential is on no path at all,
   not the deployed app's, but the evidence covers Vercel's Production `DATABASE_URL` only;
   nothing names Preview, and every pushed branch gets a preview build. Out of this ticket's scope,
   which is Production: filed at Backlog as *Check Preview's database role on Vercel* (Fix, E0.2,
   Medium), https://app.notion.com/p/3dc79dafd42e81228400c4f1369dc8de. Not settled here.
2. Should — the log entry said five tickets landed ahead, each bumping the header. Settled: six,
   four of them bumping it; this report's addendum section names them.
3. Should — the ticket's AC3 and TC3 still said v1.7, its resumed note called v1.6 "the one AC3
   names", and this report paraphrased AC3. Settled: AC3 and TC3 say v1.12, the note says what AC3
   named when cut, and the AC3 row quotes AC3.
4. Should — TA1–TA3 were only mapped, never defined, and TA3's file was unnamed. Settled: the
   ticket's addendum round lists the three tests with `scripts/run/log-index.test.mjs` by path.
5. Should — the header jumps from v1.8 to v1.6 with nothing saying why. Settled: the v1.12 entry
   says this change held v1.7 on its branch and that number never reached main.

The four settled Shoulds change docs only, after the verdict: the ticket file, this report, the
log entry and one line of the guidelines header comment. The §5 paragraph the review read is
unchanged.

### Re-close round (2026-09-16)

**Pass 1 — PASS**, at `a7e25cc` against `origin/main` at `cbdc9aa`; the verdict in
`docs/reviews/T0.13.md` replaces the addendum round's. No Must, three Should. The reviewer checked
both inlined copies of §5 against the file at `b53c4c0` and `cbdc9aa`, found the §5 paragraph
identical to the one the addendum round reviewed at `a935d08`, re-ran TB1 (merge-tree exit 0, and
the old tip's conflict against `cbdc9aa`), TC3 (`drifted: false` at v1.13; the sentence once),
TB2 (`gated.mjs` and `loosening.mjs` both `ok: true`, and the scratch commit `620543c` refused
with the Stop-hook reason), `scripts/run/log-index.test.mjs` (12 passed) and TC2 (200; 307 to
`/sign-in`), and matched Criteria and Tests both ways through AB3.

The first invocation ended before it read anything — the session's default model out of usage
credits (HTTP 429), as in the addendum round — and left no verdict file. It was invoked again with
the same one-line message on Opus, the fallback `.claude/settings.json` names.

1. Should — the AB3 row names this round's verdict while this section had no entry for it.
   Settled: this section.
2. Should — the AC2 row named only the 2026-09-14 and 2026-09-15 checks over a quote from
   2026-09-16, and `docs/reviews/T0.13.md` was listed as changed where against `origin/main` it
   is added. Settled: the row names all three, and the file is under *Added*.
3. Should (outside this round's settlement rule) — §8 Seed still ends T0.21's line with *this
   version*, which stops being true once v1.13 lands. Filed at Backlog as *Give T0.21 its version
   in §8* (Fix, E0.2, Medium), https://app.notion.com/p/3dd79dafd42e81dcab02db066c0b7ae8.

The two settled Shoulds change this report only, after the verdict.

## Changed since this ticket was cut

- **Guidelines v1.5 → v1.12.** T0.11 (v1.6) while the task waited at Decision; T0.16 (v1.8),
  T0.17 (v1.9), T0.12 (v1.10), T0.20 (v1.11) and T0.21 (v1.12) while it waited at Review.
  `version-drift.mjs "guidelines v1.5 §5"` reports `drifted: true` (cited 1.5, repo 1.12 at
  claim). Between them they rewrote most of §5 — the self-merge, the deploy check, the picker's order, the mirrors, the comment
  cap — and opened the capability-boundary paragraph with *Two database credentials*. None of it
  touches the sentence this ticket adds; the change was re-applied to §5 as it now reads, and the
  cited v1.5 text stays inlined in the ticket file. T0.21 also rewrote step 9 and the gated set,
  which is what the re-close round closes under; that §5 is inlined in the ticket file too.
- The ticket's Priority on the board is High; round one's report said Should, the scale before
  T0.17.

## Open questions

1. **The signed-in load is the human's word, not the run's observation.** A run cannot make a
   session (the OTP goes to a mailbox). A signed-in probe — a seeded session or a health route
   that touches the database without a user — would let a run observe AC2 itself. Not asked for
   by this ticket; recorded for whichever ticket next needs a run to see past sign-in. Owner:
   T-next.
2. **Vercel's Preview environment may still name the admin role.** The review's first Should: if
   it does, §5's *on no path at all* is true of production only. Owner: *Check Preview's database
   role on Vercel*, at Backlog.
3. ~~This run leaves the task at Review rather than merging it.~~ Settled by the re-close round:
   the board's second addendum asks for the close under step 9 as T0.21 wrote it.
4. **The detector lets a test deletion through on any added file that names its criteria.** Found
   while reddening TB2: a scratch commit deleting `scripts/run/log-index.test.mjs` (it names AC2
   and TC2) measured `ok: true`, because `testsLoosened` reads criteria from every added file and
   this diff adds `docs/tickets/T0.13.md`, which names AC1–AC3 and TC1–TC3. AC and TC numbers
   repeat across every ticket, so a test deletion rides ungated on any new ticket file. This
   diff deletes nothing, so its own `ok: true` stands; TB2 was reddened with the Stop hook
   instead. Owner: *Scope deleted-test replacements to tests* (Fix, E0.2, Medium), at Backlog,
   https://app.notion.com/p/3dd79dafd42e819fa708e3830cbb216a.

Round one's first open question — the guard refusing every `merge` because `notion.mjs` read the
board's Status as a status property — was settled by T0.16, which reads the select; this task's
own `merge` then passed the guard and reached GitHub, which refused it on the conflicts this round
settles.
