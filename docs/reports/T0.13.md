# T0.13 — report

Restrict Vercel's database role: production's connection on Vercel runs as `aenima_pipeline`,
the restricted role every local and pipeline connection has used since T0.10, so the admin
credential is on no path at all — not a run's and not the deployed app's. Guidelines v1.7.
Technical. Epic E0.2 Pipeline · Priority Should · Type Technical. Branch `t0-13` off
`origin/main` at `b53c4c0`, with `origin/main` merged in at `0870c08` when the run resumed.
Claimed by a scheduled run on 2026-09-13, stopped under §4 for the human's step the same day,
resumed by a scheduled run on 2026-09-14 on the human's answer.

**What the run did not do, said first.** The change itself — the Production `DATABASE_URL` on
Vercel and the redeploy — was the human's, made by hand on 2026-09-13 (no Vercel CLI on the
machine, no login, a public surface). The run's part was the stop, the checks from the outside
once the change was live, and the sentence in guidelines §5. The signed-in `/app` load, the one
path that touches the database, is not observable by a run (the OTP goes to a mailbox) and is
recorded as the human's word, quoted under AC2. No code, no schema, no migration, no test file.

## Build items, as shipped

| Item | Where |
|---|---|
| 1 The human's step: stop under §4 before any file change, one `decision` comment naming what to set and where, Decision; resume on the answer | The thread on the board row (comments of 2026-09-13 16:53 and 17:20 UTC, and the pipeline's `resolved` note of 2026-09-14 12:45 UTC); branch `t0-13` reused with its ticket pack |
| 2 Confirm the deployed app serves, from the outside | `curl -sI https://aeni.ma/sign-in` and `curl -sI https://aeni.ma/app`, quoted under AC2 |
| 3 Record it | `docs/guidelines.md` §5 *The capability boundary* — the production connection is the same role, the admin credential is on no path at all, `.env.migrate` in the primary checkout is its only home; header v1.6 → v1.7 with the change named |
| 4 Anything the change revealed | Nothing: both routes answer as at the first deploy (`docs/log/deploy.md`), and the human's reply reports the signed-in load without a fault |

## Files added and changed

Added: `docs/reports/T0.13.md` · `docs/log/T0.13.md`.

Changed: `docs/guidelines.md` (header v1.6 → v1.7; §5 the capability-boundary paragraph) ·
`docs/tickets/T0.13.md` (the resumed note under the header) · `docs/build-log.md` (Tickets done
regenerated by `log-index.mjs`).

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC1 Vercel's Production `DATABASE_URL` names `aenima_pipeline` and production has been redeployed since, confirmed by the human on the thread | TC1 | The human's reply on the task thread, 2026-09-13 17:20 UTC, verbatim: *done — /app loads signed in*. It answers the pipeline's comment of 16:53 UTC, which asked for exactly that: the Production value set to the `aenima_pipeline` string, a redeploy, and a reply that it is done and that `/app` loads signed in |
| AC2 After the redeploy `/sign-in` returns 200, `/app` unauthenticated redirects to `/sign-in`, and the human confirms `/app` loads signed in | TC2 | Observed 2026-09-14 15:28 UTC, quoted below. The signed-in load: the human's word under AC1 |
| AC3 `docs/guidelines.md` is at the next version; §5's capability-boundary paragraph says the production connection is `aenima_pipeline` and the admin credential is on no path at all; the header names the change | TC3 | `version-drift.mjs "guidelines v1.7 §5"` → `drifted: false`; `spec-sections.mjs` for guidelines §5 returns the new sentence; both quoted below. The number is v1.7, not the v1.6 AC3 names — see *changed since this ticket was cut* |

TC2, as run:

```
$ curl -sI https://aeni.ma/sign-in
HTTP/2 200
content-type: text/html; charset=utf-8
date: Mon, 14 Sep 2026 15:28:05 GMT
server: Vercel

$ curl -sI https://aeni.ma/app
HTTP/2 307
location: /sign-in
server: Vercel
x-matched-path: /app
```

TC3, as run after the edit:

```
$ node scripts/run/version-drift.mjs "guidelines v1.7 §5"
{ "cited": [ { "doc": "guidelines", "cited": "1.7", "repo": "1.7", "sections": ["5"], "drifted": false } ], "drifted": false }

$ echo '{"cited":[{"doc":"guidelines","sections":["5"]}]}' | node scripts/run/spec-sections.mjs | grep -o 'Production is the same role[^.]*\.'
Production is the same role: Vercel's Production `DATABASE_URL` names `aenima_pipeline`, set by hand and redeployed on 2026-09-13 (T0.
```

## Tests written, each observed red first

No test file was added or changed: the ticket's Rules say no code change, and the three tests are
a quoted reply and two observed commands. The record below is of those, red first where a red
can be observed.

| test | reddened by | red → green |
|---|---|---|
| TC3 `version-drift.mjs "guidelines v1.7 §5"` | run before the edit, with the header still at v1.6 | `drifted: true` (cited 1.7, repo 1.6) → `drifted: false` (repo 1.7) |
| TC3 `spec-sections.mjs` guidelines §5 contains the new sentence | run before the edit: `grep -c "on no path at all"` over the returned §5 body | `0` → `1` (the sentence *the admin credential is on no path at all* is in the returned body; the fuller quote is above) |
| TC2 `curl -sI` on `/sign-in` and `/app` | not reddenable from outside — an unauthenticated route never touches the database, so a wrong `DATABASE_URL` would not change either answer; the row records the observed values, and the signed-in load is the human's word | `200` and `307 location: /sign-in`, observed 2026-09-14 15:28 UTC; the same two answers `docs/log/deploy.md` recorded at the first deploy |
| TC1 the human's reply, quoted | not a command: the run stopped at Decision on 2026-09-13 and the thread held no answer (red) until the reply of 17:20 UTC (green) | no reply → *done — /app loads signed in* |

## Reviewer passes and findings

_Filled at step 5._

## Changed since this ticket was cut

- **Guidelines v1.5 → v1.6 (T0.11, merged to `main` as `0870c08` while this task waited at
  Decision).** `version-drift.mjs "guidelines v1.5 §5"` reports `drifted: true` (cited 1.5,
  repo 1.6). T0.11 rewrote §5's step 0 (every task's thread read over the API, the four shapes a
  reply takes), steps 1, 3, 6 and 9, added *The board's token* to the capability boundary, and
  changed the hard-boundaries paragraph (the guard reads the word `merge` or `apply` from the
  board rather than refusing any merge while a marker exists). None of it touches the sentence
  this ticket adds; the change was built against §5 as it now reads, and the cited v1.5 text
  stays inlined in the ticket file. AC3's "v1.6" is therefore taken: the header went to v1.7, said
  in one `default` comment on the thread.
- The branch was cut off `b53c4c0`, before T0.11; `origin/main` was merged into it on resume so
  the tree the gate runs carries T0.11's scripts and tests.

## Open questions

1. **This task's own `merge` will be refused until the board's Status is read as a select.**
   `scripts/run/notion.mjs` reads `Status` as a `status`-type property and the board's is a
   `select`, so every task's Status arrives as null: this run's preflight found three human
   `merge` replies (T0.14, T0.12, T2.8) shaped as `assess`, and the guard refused all three
   `gh pr merge` attempts with *the guard read the thread of … at no status and did not find
   "merge"*. Nothing was posted on those threads, so the word stays live for the run after the
   fix. Three Backlog tasks already name the fix (*Read board Status as select*, *Read Status as a
   select property*, *Read the board's select Status*, all E0.2, Fix, filed 2026-09-14 by idle
   runs); one of them at Ready unblocks every merge, this one included. Owner: that task.
2. **The signed-in load is the human's word, not the run's observation.** A run cannot make a
   session (the OTP goes to a mailbox). A signed-in probe — a seeded session or a health route
   that touches the database without a user — would let a run observe AC2 itself. Not asked for
   by this ticket; recorded for whichever ticket next needs a run to see past sign-in. Owner:
   T-next.
