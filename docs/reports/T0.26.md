# T0.26 — Applied migrations merge themselves

## ACs implemented

- **AC1 — a diff adding a migration whose thread carries a consumed *apply* for that file
  self-merges on a reviewer PASS.** `gatedDiff` takes a migration off the gated list when its tag
  is among the thread's spent applies, and the bookkeeping beside it — the journal and
  `db:generate`'s snapshot — goes with the migrations it records (`scripts/run/gated.mjs`,
  `coveredBy`, `isBookkeeping`). The tags reach it from two places and one source: the guard's
  door through `verify`'s own fetch (`permission.mjs`, `guard.mjs` `judge`), and the skill's step
  9 through `consumedApplies` from a command line. Tested by `gated.test.mjs` "lets the migration
  and its journal entry through once the apply is spent", "lets a snapshot through with the
  migration it records", "lets a migration through on the tag its thread says is applied" (end to
  end over a real repository), `comments.test.mjs` "reads the tag the run's applied note names,
  once the word granted it", `consumedApplies` "answers with the tags the claimed task's thread
  says are spent", `permission.test.mjs` "hands back the tags the thread's applied note names,
  beside the word", `guard.test.mjs` "hands the thread's spent applies to the reviewer's door".
- **AC2 — a diff adding a migration with no *apply* on its thread is gated, as today.** Every
  reader defaults to no tags and every failure path answers with none, so a caller that has read
  no thread gates exactly as before. Tested by `gated.test.mjs` "gates both files while no apply
  has been spent", "defaults to gated when nothing says an apply was spent", "keeps a snapshot
  gated while its migration is still waiting", "leaves a weakening gated whatever the thread says
  about migrations"; `comments.test.mjs` "reads nothing off a thread the human has not said the
  word on", "reads nothing off an applied note that stands before the word", "reads nothing off a
  reply that only mentions applying in passing"; `consumedApplies`' six refusal cases;
  `permission.test.mjs` "hands back no tags when the thread has no spent apply on it", "hands
  back no tags when the thread could not be read at all".
- **AC3 — an *apply* for a different migration file does not ungate this one.** Matching is by
  tag, and the bookkeeping waits for all of them. Tested by `gated.test.mjs` "does not let one
  migration's apply through for another's", "keeps the journal gated while any migration beside
  it is still waiting", "does not ungate a journal that stands on its own", "reads a migration's
  tag off its path, and nothing else's".
- **AC4 — a database test for a migration not on `origin/main` skips with a warning; the same
  test with the migration on `origin/main` and the column absent fails.**
  `src/test/migration-gate.ts` splits the rule (`gateOf`) from the observation (`landedOnMain`),
  and every case that cannot prove the migration is still waiting runs the tests. Tested by
  `migration-gate.test.ts` "skips only while the migration is not on origin/main", "does not skip
  once the migration is on origin/main and the schema is behind it", "says nothing at all once
  the schema object is there", the four `landedOnMain` cases, and the two `migrationGate` cases.
  Observed in the product as well: with `0015` and `0016` both on main, the two suites now run
  their 15 tests against Postgres rather than printing a banner.
- **AC5 — T3.1's and T1.4's test files use the shared helper and no longer carry their own skip
  path.** Tested by `migration-gate.test.ts` "leaves the unapplied-migration banner to the
  helper", asserted over the two files' own source.

## Tests written, each observed red first

| test · file | reddened by | red → green |
|---|---|---|
| `appliedMigrations` — six cases · `scripts/run/comments.test.mjs` | the function absent from `comments.mjs` | 6 failed / 58 passed → 64 passed |
| the consumed-apply gate — eight cases · `scripts/run/gated.test.mjs` | `migrationTag` and the ungating absent from `gated.mjs` | 4 failed / 15 passed → 19 passed |
| "lets a migration through on the tag its thread says is applied" · `scripts/run/gated.test.mjs` | `gatedDiffOf` dropping `applied` on its way to `gatedDiff` | 1 failed / 19 passed → 20 passed |
| `consumedApplies` — no marker, a marker with no page, a thread that will not load · `scripts/run/gated.test.mjs` | those returns handing back tags instead of none | 3 failed / 26 passed → 29 passed |
| `consumedApplies` — "answers with the tags … says are spent" · `scripts/run/gated.test.mjs` | the reader returning `[]` rather than `appliedMigrations` of the thread | 1 failed / 28 passed → 29 passed |
| `consumedApplies` — "when the token is not there" · `scripts/run/gated.test.mjs` | the token check dropped, a literal standing in for it | 1 failed / 28 passed → 29 passed |
| `consumedApplies` — "when the board file cannot be read" · `scripts/run/gated.test.mjs` | the board error swallowed and the default prefix used | 1 failed / 28 passed → 29 passed |
| `consumedApplies` — "when the thread carries no spent apply" · `scripts/run/gated.test.mjs` | tags read off every comment on the thread rather than off the `applied` note — the question answering for the answer | 1 failed / 28 passed → 29 passed |
| "lets a snapshot through with the migration it records" · `scripts/run/gated.test.mjs` | `isBookkeeping` narrowed back to the journal alone | 1 failed / 28 passed → 29 passed |
| "keeps a snapshot gated while its migration is still waiting" (with four others) · `scripts/run/gated.test.mjs` | `coveredBy` covering the bookkeeping on *any* spent apply rather than on all of them | 5 failed / 24 passed → 29 passed |
| the thread's applies reaching the second door — four cases · `scripts/run/permission.test.mjs` | `verify` not returning `applied`, and `reviewed` not passing it to the diff | 4 failed / 23 passed → 27 passed |
| "hands the thread's spent applies to the reviewer's door" · `scripts/hooks/guard.test.mjs` | `judge` calling `reviewed` without the tags | 1 failed / 115 passed → 116 passed |
| `diffGating` and `diffsLoosened` — five cases · `scripts/run/loosening.test.mjs` | the corpus and the rule absent from `loosening.mjs` | 5 failed / 39 passed → 44 passed |
| `gateOf`, `landedOnMain`, `migrationGate` — nine cases · `src/test/migration-gate.test.ts` | `src/test/migration-gate.ts` absent | no tests (module not found) → 9 passed |
| "leaves the unapplied-migration banner to the helper" · `src/test/migration-gate.test.ts` | the two `.db.test.ts` files still carrying their own skip path; and, after review pass 2 keyed it to each file's own migration, a banner restored to `refinement.db.test.ts` | 1 failed / 9 passed → 10 passed |
| "carries a version at or past the one §7's rule arrived in" · `scripts/run/next-id.test.mjs` | the guidelines header set back to v1.19 | 1 failed / 26 passed → 27 passed |
| `src/db/opportunity-key.db.test.ts`, `src/db/refinement.db.test.ts` | restoring either file's inline skip path — the assertion above is what reads them | 1 failed / 9 passed → 10 passed |

Whole suite: 1734 passed at the first green, 1743 after review pass 1, 1743 after pass 2 —
pass 2 added no test, only the records three of `consumedApplies`'s cases were still owed.

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `027431c` | opus | yes | FINDINGS |
| 2 | `974dbb8` | opus | no | FINDINGS |
| 3 | `ec36bf8` | opus | no | PASS |

Pass 1 was called first on the pinned model, `fable`, and came back an API error rather than a
review — out of usage credits, which `review-model.mjs` reads as `credits` and answers with the
next model of the configured chain. On `opus` it stopped at its 30-turn limit, was resumed once,
and wrote its verdict.

**Must 1 — `consumedApplies` is the run's half of AC1 and nothing tested it.** Five returns, none
exercised: the guard's door was tested twice over and the command line, which is what step 9
actually reads before it merges, not at all. Fixed with a describe over `consumedApplies`, one
case per return, each observed red by a mutation that stops it failing closed.

**Should 2 — a migration written the way CLAUDE.md prescribes carries a third file `coveredBy`
could never cover.** `pnpm db:generate` writes `drizzle/meta/<nnnn>_snapshot.json`, and the
journal was the only bookkeeping the coverage knew, so the prescribed diff would stay gated with
its apply fully spent — AC1 failing on the shape AC1 is about. Fixed: `isBookkeeping` names the
journal and the snapshots together, both answering to the migrations in their own diff, with a
`DIFF_CORPUS` entry beside it.

**Should 4 — the new loosening tests named criteria they do not cover.** They measure the
detector rather than the gate; AC2 and AC3 live in `gated.test.mjs`. Fixed: the describe now says
what it is, Build 1's last sentence.

**Should 5 — a stale quote in the detector's comment.** `loosening.mjs` quoted `reviewed`'s diff
line as it read before the tags were threaded through it. Fixed.

**Should 3 — `next-id.test.mjs`'s assertion is rewritten and no Criteria line accounts for it.**
Recorded rather than fixed; see the open questions.

**Should 6 — the tag half of "consumed" comes from a sentence the run composes.** Recorded; see
the open questions.

**Should 7 — no report on the branch.** This file.

### Pass 2

Called first on the pinned model again and refused for credits again, so it ran on `opus`. Two
Musts, both fair.

**Must 1 — the rule moved in code and in guidelines, and four other places still stated the old
one.** `gated.mjs`'s own contract paragraph, sitting directly above `coveredBy`; the skill at
step 9, which is what the run reads at the moment the new gate fires, and again in its closing
paragraph; `.claude/agents/reviewer.md`, which is the next reviewer's own definition of the door;
and `docs/build-guide.md`. Each now carries the qualifier guidelines settled on — *adds no
migration your `apply` is still owed on* — and build-guide is at **v2.8**, since
`version-drift.mjs` reads that header. Its `# ` line had said v2.5 through three bumps and now
says what the header says.

**Must 2 — three `consumedApplies` cases had no red-first record while the report claimed every
case had one.** True: the table covered four of seven. The three are recorded above, each
observed red by its own mutation — the token check dropped, the board error swallowed, and the
tags read off every comment on the thread rather than off the `applied` note.

**Should — `isBookkeeping` hardcoded `drizzle/`.** Fixed; it reads `MIGRATIONS_DIR` like the rest
of the module.

**Should — the AC5 assertion keyed on the literal `drizzle/00`.** Fixed: it reads the migration
each file names to the gate and asserts no banner of that file's own mentions it, so the check
follows the file rather than a literal kept in step by hand.

**Should — the five detector tests name no criterion.** Recorded rather than relabelled; see the
open questions.

### Pass 3 — PASS

Refused for credits on the pinned model a third time, so it ran on `opus`. No Must stands. It
traced AC1 end to end through the two runs rather than reading it off the tests — run A stopping
at Decision, the human's `apply`, run B posting the `applied` note at step 0 before it branches,
and step 9 reading the claimed task's page from the marker — and confirmed the branch that opens
the reviewer's door is precisely the one where `verify("merge")` answers no because the run's own
note consumed the word. It audited all seven of `verify`'s returns and all five of
`consumedApplies`' for the empty list, checked that `...weakened` is spread outside the coverage
filter so a weakening survives a spent apply, and reconciled the red-first counts against what
the files hold. Four Shoulds, none fixed: a Should is recorded, and the three-pass limit means a
code change after the PASS would be a diff no reviewer read.

1. **`docs/guidelines.md` §4 says the spending is read "not off anything a run says about it" and
   then names the run's own `applied` note as half of it.** The same gap as open question 2
   below, now visible in the prose as well as in the mechanism. Owner `T-next`, with the fix
   named there.
2. **`gated.mjs`'s `ungate` copy still opens "a migration is applied by hand, so this one waits
   for you either way".** The sentence corrects itself in its second clause and only posts while
   the migration *is* gated, so nothing it says is false in the situation it appears in — but
   "either way" is the old rule in the one place the change did not reach, because it is output
   rather than comment. Owner `T-next`: two words.
3. **The AC5 assertion binds to a banner that names a path, so a reworded inline skip would pass
   it.** It reads the migration each file hands the gate and asserts no `process.stderr.write` in
   that file mentions it; a file that reinstated a skip while describing the migration some other
   way would slip through. Tighter would be to assert the file's only gate on the unapplied case
   *is* `migrationGate`'s answer. Owner `T-next`.
4. **`landedOnMain` trusts the local `origin/main`, which is only as fresh as the last fetch.**
   A stale ref answers "not landed" for a migration that has landed, and skips — the one thing
   the rule exists to make impossible. It holds in the pipeline, where step 0 fetches; it does not
   hold in a person's stale checkout. Owner `T-next`: the helper's doc comment should price the
   observation as being as fresh as the ref, or the helper should fetch.

## Changed since this ticket was cut

- `docs/guidelines.md` moved v1.20 → **v1.21** in this diff, which is the ticket's own Build 3.
  Nothing it cites moved underneath it: `version-drift.mjs` reads no drift for §4 or §5, and the
  ticket's Cited transcription matches the pre-diff text word for word.
- `scripts/run/next-id.test.mjs` is another ticket's file and this change reddened it. T0.28
  asserted the guidelines header read `1.20` exactly — true of the afternoon it was written and
  of no later one — so every version bump reddens it and the only move available is to retype the
  number, which arms the trap again. It now asserts the rule that assertion was reaching for: the
  header carries a version, and it has never gone back behind the one §7's rule arrived in. The
  new assertion was observed red against a header set to v1.19.

## Open questions

1. **A relaxed assertion in another ticket's test has no criterion of its own — owner `T-next`.**
   Review pass 1's Should 3. The change is recorded here and in `docs/log/T0.26.md`, and the new
   assertion reddens on a real regression, but this ticket's Criteria never contemplated touching
   T0.28's test and nothing in the ticket accounts for it. The general question is worth one line
   somewhere: a ticket that bumps a document's version has to be allowed to move the assertions
   that pinned the old one, and the honest way to allow it is for those assertions not to pin a
   number in the first place. A sweep for the rest of them — any test asserting a spec version
   exactly — would say how many traps are left.
2. **The tag half of a consumed apply comes from a sentence the run composed — owner `T-next`.**
   Review pass 1's Should 6. The human's `apply` is verified by the guard over the API and cannot
   be fabricated; the tag beside it is read out of the run's own `applied` note, whose file field
   the skill fills from `apply.mjs`'s output. Two steps of misbehaviour would be needed to exploit
   it — a run composing an `applied` note for a migration it did not apply, on a thread that has
   some earlier granted `apply` — and the guard refuses an unprefixed comment and a comment of a
   kind already posted in the claim, so neither step is free. The tighter shape is for `apply.mjs`
   to be the thing whose output reaches the thread rather than a sentence composed around it;
   that is a change to how `applied` is written, not to this gate.
3. **The detector's own five tests answer to no criterion — owner `T-next`.** Review pass 2's
   last Should, and pass 1's Should 4 from the other side. They measure that `loosening.mjs`
   would catch a gate which stopped honouring AC2 and AC3, which is Build 1's last sentence —
   "the loosening detector's other rules are untouched" — rather than a criterion of its own.
   Labelling them TC2 and TC3 would claim they test the gate, which is what pass 1 objected to;
   leaving them unlabelled is what pass 2 objects to. The real gap is that a ticket which narrows
   a restraint has no criterion for *the narrowing being measured*, and that belongs in how
   tickets are cut rather than in this one.
4. **`appliedMigrations` takes the earliest human `apply` on the thread as the grant.** A later
   one does not narrow the window. That is deliberate — a thread can spend more than one apply,
   and the note that matters may precede a second question — but it means the window only ever
   widens as a thread grows. Worth revisiting if a ticket ever carries three migrations across
   two rounds.
