# T1.5 — report

Row roving and freshness: two of the three things T1.2 left on the list surface. Design-spec §11's
arrow keys walk the rows across the three buckets, and the row reads the scoring clock the item
page has read since T2.4 — "scored 6h ago", and "scored 6h ago — retrying" with the `--warning`
dot while a retry is queued. The third, park, split out to T1.6 Park move by the board's addendum
of 2026-09-16, and is not in this diff. Epic E1.1 Spine · Priority Medium · Type Enhancement ·
Spec `product-spec v1.6 §13 · design-spec §11 · build-log OQ6`. Branch `t1-5` off `origin/main`
at `0870c08`, with `origin/main` merged in at `9a1a881`.

Three claims. The scheduled run on 2026-09-14 (session `748b808b`, re-claiming a run that died
partway at session `87db13bc`, whose branch is kept as `t1-5-stale-1427` and whose walker was
salvaged from it byte for byte, then reddened and re-greened) built the walker and the row clock
and stopped at Decision on where a parked item is stored. The claim of 2026-09-16 at 13:24 UTC
merged `origin/main` into `t1-5` and stopped on the same question. The claim of 2026-09-16 at
14:10 UTC is the addendum round below, and the one that reviews and closes.

## Addendum round — park splits out

The board's addendum answered the twice-asked question by splitting park out: the walker and the
row clock close now, and T1.6 decides where park is stored and carries the migration. It asked for
the review this ticket had not had and a close under §5 step 9, and named three criteria.

| Item | Where |
|---|---|
| Park dropped from Build, Criteria and Tests | `docs/tickets/T1.5.md`: Build 1, AC1, AC2, TC1, TC2 and the two park Rules out; AC3, AC4, TC3 and TC4 kept at their numbers so the thread and this record still point at the right lines; the ticket renamed to the board's name; an `## Addendum` section with the reply verbatim. The board body's Build, Criteria and Tests say the same |
| AA1 the diff touches no migration and no schema file | TA1 — nothing under `drizzle/` or `src/db/schema/`, no `docs/schema.md`, no `src/db/database.types.ts`; `migration-check.mjs` reads `waiting: false` |
| AA2 the walker and the row clock keep their tests green on merged main | TA2 — `origin/main` (`f4efa56`) is an ancestor of `HEAD`; the three unit files `41 passed`, `e2e/list.spec.ts` `23 passed`, the two cases this ticket added among them |
| AA3 nothing in the diff references park | TA3 — no line the diff adds or removes under `src/` or `e2e/` names park. One unchanged context line in `ItemRow.tsx`, T1.2's §8 note on the idle chip, is `main`'s |
| The "Park?" chip | left as T1.2 renders it on `main` — a default, below |
| Build-log open question 6 | answered for the walker and the row, park named as T1.6's |
| Open question 2 of the first round — the embed never read against PostgREST | read, and it holds: see *Open questions*, 2 |

## Build items, as shipped

| Item | Where |
|---|---|
| 2 Arrow keys walk the rows: one client island around the buckets, the row's link as the stop, Down and Up across buckets in visual order wrapping at the ends, Home and End to the first and last, over `nextRovingIndex`; focus on the overflow trigger and every other key left alone. The rows stay Server Components — the stop's attribute name lives in a plain module so both sides of the boundary read the string | `src/app/app/RowWalker.tsx`, `src/app/app/row-link.ts`, `src/app/app/ItemRow.tsx` (the attribute on the link), `src/app/app/page.tsx` and `src/app/dev/primitives/Composites.tsx` (the wrap) |
| 3 The row reads the scoring clock: the list read embeds each item's newest `scoring_run(scored_at, artifact(next_scoring_attempt_at))`, ordered newest-first and capped at one *on the embed* — one request as before, no cap on the list (open question 7's rule) — and the row says "scored 6h ago", or "scored 6h ago — retrying" with the `--warning` dot while §5's queue holds a retry, per §10. The mapper still scans for the newest run, for `versionRange`'s reason. A row nothing has scored keeps last activity, "updated 3h ago", and shows a retry only beside a run, as the item page does | `src/db/queries/item.ts` (`ITEM_ROW`, the two embed clauses, `ItemListRow.scoredAt` and `nextScoringAttemptAt`), `src/app/app/page.tsx` (`toRowData`), `src/app/app/ItemRow.tsx` (`ItemRowData.scoredAt`, `retrying`, the readout and the dot), `src/app/dev/list-fixture.ts` (§10's own example on `soc-12`) |
| 1 Park | split out to T1.6 Park move |

## Files added and changed

Added: `src/app/app/RowWalker.tsx` and `RowWalker.dom.test.tsx` · `src/app/app/row-link.ts` ·
`src/app/app/ItemRow.dom.test.tsx` · `docs/tickets/T1.5.md` · `docs/reports/T1.5.md` ·
`docs/log/T1.5.md` · `docs/reviews/T1.5.md` (the reviewer's verdict, written by the reviewer).

Changed: `src/db/queries/item.ts` and `item.test.ts` · `src/app/app/ItemRow.tsx` ·
`src/app/app/page.tsx` · `src/app/dev/list-fixture.ts` · `src/app/dev/primitives/Composites.tsx` ·
`e2e/list.spec.ts` · `docs/build-log.md` (open question 6 answered; Tickets done regenerated).

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC3 with focus on a row's link, Down and Up move to the next and previous row's link across buckets and wrap; Home and End reach the first and last; other keys and focus elsewhere in the row are untouched | TC3 | `src/app/app/RowWalker.dom.test.tsx` (5, over the real buckets and fixture, so the link's stop is proven wired) · `e2e/list.spec.ts` "walks the rows with the arrow keys, across buckets, wrapping at the ends" on `/dev/primitives` |
| AC4 the row's readout shows the newest run's clock, and the retry state per §10 when a retry is scheduled | TC4 | `src/db/queries/item.test.ts` (4: the embed, its order and cap, the mapping, the newest-of-many, the null) · `src/app/app/ItemRow.dom.test.tsx` (4: scored, retrying, unscored, a flag with no run) · `e2e/list.spec.ts` "shows the newest run's clock, warning-dotted while a retry is queued" — the dot read as a computed colour, `--warning` and `--prime`, never red |
| AA1 the diff touches no migration and no schema file | TA1 | `git diff --name-only origin/main...HEAD` against `drizzle/`, `src/db/schema/`, `docs/schema.md`, `src/db/database.types.ts` — no match; `node scripts/run/migration-check.mjs` — `waiting: false` |
| AA2 the walker and the row clock keep their tests green on merged main | TA2 | `git merge-base --is-ancestor origin/main HEAD` holds; `pnpm vitest run` over the three unit files — `41 passed`; `playwright test e2e/list.spec.ts --project chromium` against a dev server on this tree — `23 passed` |
| AA3 nothing in the diff references park | TA3 | `git diff -U0 origin/main...HEAD -- src e2e`, `+` and `-` lines only, `grep -ic park` — `0` |

## Tests written, each observed red first

Each mutation was applied to the source, the test file run, and the source restored from a copy and
checked against git before the next. The count is the file's, or the three files' where a row says
so. Four test files are in the diff: `src/app/app/RowWalker.dom.test.tsx`,
`src/app/app/ItemRow.dom.test.tsx`, `src/db/queries/item.test.ts`, `e2e/list.spec.ts` (the
Playwright cases ran against a dev server on a spare port, `-g` on their names). The rows down to
the e2e ones are the first round's, on 2026-09-14; the TA rows and the rows marked *merged main*
are the addendum round's, on the tree with `origin/main` merged in.

| test | reddened by | red → green |
|---|---|---|
| RowWalker › renders one link per row, in bucket order | first round: `RowWalker.tsx` moved out of the tree — the import fails to resolve, which proves the import and not the order; addendum round, on the reviewer's Should 1: the harness rendering Flowing before Your move | `1 file failed, no test ran → 5 passed`; `1 failed / 4 passed → 5 passed` |
| RowWalker › steps down and up across buckets, wrapping at both ends | the `data-row-link` attribute removed from `ItemRow`'s link, so the walker finds no stop | `2 failed / 3 passed → 5 passed` |
| RowWalker › jumps to the ends with Home and End | the same | `2 failed / 3 passed → 5 passed` |
| RowWalker › leaves other keys alone | first round: `RowWalker.tsx` moved out of the tree, which proves the import and not the claim; addendum round, on the reviewer's Should 1: the walker reading `j` as ArrowDown | `1 file failed, no test ran → 5 passed`; `1 failed / 4 passed → 5 passed` |
| RowWalker › does not walk from the overflow menu's trigger | the walker's guard that focus is on a row's link removed | `1 failed / 4 passed → 5 passed` |
| listItemsForWorkspace › asks for each item's newest scoring run in the same request, newest first, one only | the `scoring_run` embed, its order and its cap removed from the query; both clocks mapped to null | `3 failed / 29 passed → 32 passed` |
| listItemsForWorkspace › carries the newest run's clock and the retry queued on its artifact | the same | `3 failed / 29 passed → 32 passed` |
| listItemsForWorkspace › takes the newest run whatever order the rows arrive in | the same | `3 failed / 29 passed → 32 passed` |
| listItemsForWorkspace › reads no clock for an item nothing has scored | the mapper falling back to `item.updated_at` as the clock when there is no run | `1 failed / 31 passed → 32 passed` |
| ItemRow freshness › reads the newest run's clock once the item has been scored | the row always reading last activity, the dot always `--prime` | `2 failed / 2 passed → 4 passed` |
| ItemRow freshness › marks a queued retry with the warning dot and says so, never red | the same; and the row always reading the run's line, scored or not | `2 failed / 2 passed → 4 passed`; `3 failed / 1 passed → 4 passed` |
| ItemRow freshness › keeps last activity while nothing has scored the item | the row always reading the run's line, scored or not | `3 failed / 1 passed → 4 passed` |
| ItemRow freshness › shows no retry without a run to put it beside | the retry shown from the flag alone, without a run behind it | `1 failed / 3 passed → 4 passed` |
| e2e list › walks the rows with the arrow keys, across buckets, wrapping at the ends | the `RowWalker` wrap on `/dev/primitives` replaced by a plain `div` | `2 failed → 2 passed` (of the 2 selected; the file's 23 pass) |
| e2e list › shows the newest run's clock, warning-dotted while a retry is queued | the fixture's `soc-12` left unscored, no retry | `2 failed → 2 passed` (of the 2 selected; the file's 23 pass) |
| TA1 no migration or schema file in the diff | `drizzle/0015_probe.sql` written and added intent-to-add, the diff read against the working tree: the path check matched it and `migration-check.mjs origin/main` read `waiting: true`; the file removed and unstaged | `1 match, waiting: true → 0 matches, waiting: false` |
| TA2 the walker, merged main — `RowWalker.dom.test.tsx` | the `data-row-link` attribute removed from `ItemRow`'s link | `2 failed / 39 passed → 41 passed` (the three unit files) |
| TA2 the row clock, merged main — `src/db/queries/item.test.ts` | the list mapper reading no clock: `scoredAt` and `nextScoringAttemptAt` always null | `2 failed / 39 passed → 41 passed` (the three unit files) |
| TA2 the row readout, merged main — `ItemRow.dom.test.tsx` | the row always reading last activity, the dot always `--prime` | `2 failed / 39 passed → 41 passed` (the three unit files) |
| TA2 the two e2e cases, merged main — `e2e/list.spec.ts` | the `RowWalker` wrap on `/dev/primitives` replaced by a plain `div`, and `soc-12` left unscored with no retry | `2 failed → 2 passed` (of the 2 selected; the file's 23 pass) |
| TA3 no line the code diff adds or removes names park | one comment naming park appended to `src/app/app/row-link.ts`, the diff read against the working tree; the file restored from a copy and checked against git | `1 match → 0 matches` |

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `3e5f6c6` | opus | yes | PASS |

The pass was first called on the pinned model, `fable`, and refused before it read anything — *out
of usage credits*, HTTP 429 — so `review-model.mjs` named the chain's next model and the same
one-line message went to `opus` in a fresh session. That session stopped at its 30-turn limit with
no report; it was resumed once, and wrote the verdict. The verdict, `docs/reviews/T1.5.md`, is the
reviewer's: no Must, five Shoulds.

1. **Two red-first rows proved the import, not the assertion** — "renders one link per row, in
   bucket order" and "leaves other keys alone" were reddened by moving `RowWalker.tsx` away.
   *Done in the record:* each reddened again by a mutation aimed at its claim, above.
2. **A test name and a comment are stale for the fixture this diff changed** —
   `e2e/list.spec.ts`'s "a row renders no meter at all while nothing is scored" and `ItemRow.tsx`'s
   "Nothing is scored until Phase 2"; and design-spec §10's row meters, which "appear when the
   scores do", are owned by nothing. *Filed* as a Backlog Fix, "Show row meters once scores exist"
   (E1.1 Spine, https://app.notion.com/p/3dd79dafd42e819c8140d6bda10a5698), with the two lines.
3. **A count in a comment is out of date** — `src/db/queries/item.ts`'s note on `ITEM_ROW` counts
   three differences from `ITEM_TREE`, each for a bucket rule; the `scoring_run` embed is a fourth,
   for the row's readout. *Filed* in the same Backlog task, since the row's meters would change
   that read again.
4. **The walk is driven in a browser only on the client-rooted `/dev/primitives`**, not on
   `/dev/list`, where Server Component rows sit inside the client walker as they do on `/app`. The
   reviewer checked the attribute server-side and found all four links carry it. *Recorded*, not
   built: the case would be a second copy of a passing walk, and no Criteria line asks for it.
5. **The report was ahead of the tree at `3e5f6c6`** — the log entry and the regenerated list.
   *Done* at close, in this commit.

Every Should is recorded; none changed code after the pass, so the code that merges is the code the
reviewer read.

## Changed since this ticket was cut

At the 14 September claim, nothing: `version-drift.mjs` reported product-spec v1.6 as cited and as
in the repo. design-spec and build-log OQ6 carry no version on the Spec line.

Since the 16 September claims, product-spec has drifted from the cited v1.6 to v1.8 in the repo.
§13, the one section cited, reads the same byte for byte at both versions (the section's text at
`0870c08` and at `origin/main` `f4efa56` hash alike), and so does design-spec §11; v1.7's and
v1.8's notes name §4, §5, §8, §11 and §17 of the product spec, none of which this ticket cites.

## Open questions

1. **Where the parked state lives** — the Decision two claims stopped on, and no longer this
   ticket's. The addendum moved park whole to T1.6 Park move (Backlog), which owns the storage
   and the migration; the "Park?" chip renders and does nothing until it lands. Owner: T1.6.
2. **~~The embed's runtime shape is proven by types and by the recording stand-in, not against
   PostgREST.~~ Read against it.** A read-only probe on 2026-09-16 ran the list's own select —
   `ITEM_ROW` with the embed's order and cap — over the service role against the shared database:
   PostgREST accepted it; of 15 items, 5 carry exactly one run in the embed and 10 none, and no
   item carries more than one; the two items with several runs (`soc-9`, 3; `soc-13`, 46) each
   carry the newest `scored_at` of all their runs, checked against an uncapped read of
   `scoring_run`; and the nested `artifact` arrives as a to-one object with
   `next_scoring_attempt_at`, as the types say. Still no database test drives
   `listItemsForWorkspace` — none does for any list read today. Owner: none; settled.
3. **Build-log open question 6** — answered in this diff for the walker and the row, with park
   named as T1.6's. Owner: T1.6 closes the rest.
4. **A pipeline defect met in the first round's step 0** — `notion.mjs` read Status as a
   `status`-type property where the board holds a `select`. Filed then as its own Backlog task;
   settled since: T0.16 made `notion.mjs` read the select. Owner: none.

## Defaults this claim took

Said in the claim's one comment on the board.

- **"Drop the Park chip … from Build, Criteria and Tests" read as scope, not as the row.** The chip
  T1.2 renders on `main` stays; removing it would change what a product user sees and put park in
  the diff AA3 keeps clear of it. T1.6 is where it starts doing something.
- **"The diff" in AA3 read as the code it changes** — lines added or removed under `src/` and
  `e2e/`. Unchanged context lines are `main`'s, and the ticket, this report, the log entry and
  build-log open question 6 cannot say park moved to T1.6 without naming it.
- **AC3, AC4, TC3 and TC4 kept at their numbers** rather than renumbered, so the thread's comments
  and the first round's record still point at the right lines.
