# T1.5 — report, so far

Park move and row roving: the three things T1.2 left on the list surface. Two of them are in this
diff — design-spec §11's arrow keys walking the rows, and the row reading the scoring clock the
item page has read since T2.4 — and the third, park, is stopped on a Decision: where a parked item
is recorded is not written down, and a column is not a thing a run invents (CLAUDE.md). Epic E1.1
Spine · Priority Should · Type Enhancement · Spec `product-spec v1.6 §13 · design-spec §11 ·
build-log OQ6`, no version drifted. Branch `t1-5` off `origin/main` at `0870c08`. Claimed by the
scheduled run on 2026-09-14 (session `748b808b`) in its own worktree, re-claiming a run that died
partway at session `87db13bc`; that run's branch is kept as `t1-5-stale-1427` and its walker was
salvaged from it, byte for byte, then reddened and re-greened here.

**What is not in this diff, said first.** Build 1 — park: the mutation, the `item.parked` and
undo activity rows, §12's toast, the .40 row and the parked list. The ticket says where the state
lives is not written down anywhere, and neither `docs/schema.md` nor product-spec §13 names a
place. The decision comment on the board row names the default the next run would take on the
word "default": a nullable `parked_at` on `item`. AC1 and AC2 are therefore not implemented, TC1
and TC2 not written. The reviewer has not read this diff yet — a Decision exit is a step-4 exit,
and step 5 is the next run's, on the whole ticket.

## Build items, as shipped

| Item | Where |
|---|---|
| 2 Arrow keys walk the rows: one client island around the buckets, the row's link as the stop, Down and Up across buckets in visual order wrapping at the ends, Home and End to the first and last, over `nextRovingIndex`; focus on the overflow trigger and every other key left alone. The rows stay Server Components — the stop's attribute name lives in a plain module so both sides of the boundary read the string | `src/app/app/RowWalker.tsx`, `src/app/app/row-link.ts`, `src/app/app/ItemRow.tsx` (the attribute on the link), `src/app/app/page.tsx` and `src/app/dev/primitives/Composites.tsx` (the wrap) |
| 3 The row reads the scoring clock: the list read embeds each item's newest `scoring_run(scored_at, artifact(next_scoring_attempt_at))`, ordered newest-first and capped at one *on the embed* — the ordered `limit(1)` shape the query's own note names, one request as before, no cap on the list (OQ7's rule) — and the row says "scored 6h ago", or "scored 6h ago — retrying" with the `--warning` dot while §5's queue holds a retry, per §10. The mapper still scans for the newest run, for `versionRange`'s reason. A row nothing has scored keeps last activity, "updated 3h ago", and shows a retry only beside a run, as the item page does — a default, said on the board | `src/db/queries/item.ts` (`ITEM_ROW`, the two embed clauses, `ItemListRow.scoredAt` and `nextScoringAttemptAt`), `src/app/app/page.tsx` (`toRowData`), `src/app/app/ItemRow.tsx` (`ItemRowData.scoredAt`, `retrying`, the readout and the dot), `src/app/dev/list-fixture.ts` (§10's own example on `soc-12`) |
| 1 Park | not built — Decision, see above |

## Files added and changed

Added: `src/app/app/RowWalker.tsx` and `RowWalker.dom.test.tsx` · `src/app/app/row-link.ts` ·
`src/app/app/ItemRow.dom.test.tsx` · `docs/tickets/T1.5.md` · `docs/reports/T1.5.md`.

Changed: `src/db/queries/item.ts` and `item.test.ts` · `src/app/app/ItemRow.tsx` ·
`src/app/app/page.tsx` · `src/app/dev/list-fixture.ts` · `src/app/dev/primitives/Composites.tsx` ·
`e2e/list.spec.ts`.

## ACs implemented

| AC | Test | Where |
|---|---|---|
| AC3 with focus on a row's link, Down and Up move to the next and previous row's link across buckets and wrap; Home and End reach the first and last; other keys and focus elsewhere in the row are untouched | TC3 | `src/app/app/RowWalker.dom.test.tsx` (5, over the real buckets and fixture, so the link's stop is proven wired) · `e2e/list.spec.ts` "walks the rows with the arrow keys, across buckets, wrapping at the ends" on `/dev/primitives` |
| AC4 the row's readout shows the newest run's clock, and the retry state per §10 when a retry is scheduled | TC4 | `src/db/queries/item.test.ts` (4: the embed, its order and cap, the mapping, the newest-of-many, the null) · `src/app/app/ItemRow.dom.test.tsx` (4: scored, retrying, unscored, a flag with no run) · `e2e/list.spec.ts` "shows the newest run's clock, warning-dotted while a retry is queued" — the dot read as a computed colour, `--warning` and `--prime`, never red |
| AC1 one tap on "Park?" parks the item, activity row, §12's toast with undo for 8 s | TC1 | not built — Decision |
| AC2 undo within the toast's clock unparks it with its own activity row | TC2 | not built — Decision |

## Tests written, each observed red first

Each mutation was applied to the source, the test file run, and the source restored from a copy and
checked against git before the next. The count is the file's. Four test files are in the diff:
`src/app/app/RowWalker.dom.test.tsx`, `src/app/app/ItemRow.dom.test.tsx`,
`src/db/queries/item.test.ts`, `e2e/list.spec.ts` (the two Playwright cases ran against a dev
server on a spare port, `-g` on their names).

| test | reddened by | red → green |
|---|---|---|
| RowWalker › renders one link per row, in bucket order | `RowWalker.tsx` moved out of the tree — the import fails to resolve | `1 file failed, no test ran → 5 passed` |
| RowWalker › steps down and up across buckets, wrapping at both ends | the `data-row-link` attribute removed from `ItemRow`'s link, so the walker finds no stop | `2 failed / 3 passed → 5 passed` |
| RowWalker › jumps to the ends with Home and End | the same | `2 failed / 3 passed → 5 passed` |
| RowWalker › leaves other keys alone | `RowWalker.tsx` moved out of the tree | `1 file failed, no test ran → 5 passed` |
| RowWalker › does not walk from the overflow menu's trigger | the walker's guard that focus is on a row's link removed | `1 failed / 4 passed → 5 passed` |
| listItemsForWorkspace › asks for each item's newest scoring run in the same request, newest first, one only | the `scoring_run` embed, its order and its cap removed from the query; both clocks mapped to null | `3 failed / 29 passed → 32 passed` |
| listItemsForWorkspace › carries the newest run's clock and the retry queued on its artifact | the same | `3 failed / 29 passed → 32 passed` |
| listItemsForWorkspace › takes the newest run whatever order the rows arrive in | the same | `3 failed / 29 passed → 32 passed` |
| listItemsForWorkspace › reads no clock for an item nothing has scored | the mapper falling back to `item.updated_at` as the clock when there is no run | `1 failed / 31 passed → 32 passed` |
| ItemRow freshness › reads the newest run's clock once the item has been scored | the row always reading last activity, the dot always `--prime` | `2 failed / 2 passed → 4 passed` |
| ItemRow freshness › marks a queued retry with the warning dot and says so, never red | the same; and the row always reading the run's line, scored or not | `2 failed / 2 passed → 4 passed`; `3 failed / 1 passed → 4 passed` |
| ItemRow freshness › keeps last activity while nothing has scored the item | the row always reading the run's line, scored or not | `3 failed / 1 passed → 4 passed` |
| ItemRow freshness › shows no retry without a run to put it beside | the retry shown from the flag alone, without a run behind it | `1 failed / 3 passed → 4 passed` |
| e2e list › walks the rows with the arrow keys, across buckets, wrapping at the ends | the `RowWalker` wrap on `/dev/primitives` replaced by a plain `div` | `2 failed → 2 passed` (of the 2 selected; the file's 23 pass) |
| e2e list › shows the newest run's clock, warning-dotted while a retry is queued | the fixture's `soc-12` left unscored, no retry | `2 failed → 2 passed` (of the 2 selected; the file's 23 pass) |

## Reviewer passes and findings

None yet. A Decision exit leaves step 5 to the run that carries the ticket on after the answer;
that run reviews the whole diff, park included.

## Changed since this ticket was cut

Nothing: `version-drift.mjs` reports product-spec v1.6 as cited and as in the repo. design-spec
and build-log OQ6 carry no version on the Spec line.

## Open questions

1. **Where the parked state lives** — the Decision this run stopped on. The default named on the
   board: a nullable `parked_at timestamptz` on `item`, written by the park move and cleared by
   undo; the row at .40 while set; the parked list as §13's first-class destination. A column is
   a migration, so the answer walks through step 6 as well: after "default", the next run writes
   `drizzle/00xx_item_parked.sql` and stops for "apply". Owner: T1.5's next round.
2. **The embed's runtime shape is proven by types and by the recording stand-in, not against
   PostgREST.** `pnpm typecheck` accepts `scoring_run(scored_at, artifact(next_scoring_attempt_at))`
   over the generated relationships and `referencedTable` on `order` and `limit`; `item.test.ts`
   asserts what was asked. No database test drives `listItemsForWorkspace` — none does today, the
   `.db.test.ts` files use raw SQL — so the first real read of the embed is `/app` itself. Worth
   one look at a scored workspace before merge. Owner: the reviewer's pass, or T1.5's next round.
3. **Build-log open question 6** is answered by this diff for the row's clock and the walker, and
   half-answered for park; the log entry at close should say so and OQ6 be rewritten then.
   Owner: T1.5's close.
4. **A pipeline defect met in step 0, filed as its own Backlog task** ("Read the board's select
   Status", E0.2 Pipeline, Fix): `notion.mjs` reads Status as a `status`-type property and the
   board holds a `select`, so every task reads as no status, `threads.mjs` shapes the three human
   `merge` replies at Review (T0.14, T0.12, T2.8) as `assess`, and the guard refuses the merge.
   Nothing was posted on those three threads — a ⟡ note would consume the word. Owner: that task.
