# T0.25 — Review scope against origin/main

Branch `t0-25` off `origin/main` at `ce2b4a0`. Claimed by the scheduled run on 2026-09-21 in its
own worktree. The reviewer's list and the report check now read the ticket's own branch and
nothing else. With no base given, both scripts compare against `origin/main`, the ref every other
run script uses, where before they used a local `main` that a worktree never moves. A test path
under a dynamic route directory is kept whole. A test named by its bare file name is read as a
precedent in prose, not as a citation. And the test files an `## Addendum…` section names are in
scope alongside `## Tests`. Run over every ticket file on disk, the script now reports nothing
missing. Before, it reported eight names in T1.4 and T2.10, and every one of them was on disk.

## ACs implemented

| AC | What was built | Its test |
|---|---|---|
| AC1 `report-check.mjs` and `review-scope.mjs` default to `origin/main`, so a worktree run reads only its own branch's files | `review-scope.mjs` exports `BASE = "origin/main"`, with the reason in its comment; `touchedTests`, `reviewScope` and the CLI default to it, and `report-check.mjs` imports the same constant for its CLI, so the default is one line in one file | `review-scope.test.mjs` › "review scope with no base, in a worktree whose local main is behind origin/main" (two tests); `report-check.test.mjs` › "report-check with no base, in a worktree whose local main is behind origin/main" |
| AC2 A path with a dynamic segment such as `src/app/o/[key]/…` is resolved whole, not cut at the bracket | `SEGMENT`: a directory is a plain name or a Next.js dynamic segment whole — `[key]`, `[...rest]`, `[[...slug]]` | `review-scope.test.mjs` › "keeps a dynamic route segment whole rather than cutting the path at its bracket", "reports nothing missing when every cited file is on disk, T1.4's included" |
| AC3 A prose precedent named in a ticket is not counted as a cited file | `PATH_LIKE` requires at least one directory: a ticket cites a test by its repo-relative path, and prose points at a precedent by its file name. The comment records the convention and what it cannot see | `review-scope.test.mjs` › "does not count a precedent named in prose as a cited file", "reports nothing missing when every cited file is on disk, T1.4's included" |
| AC4 The test files an `## Addendum` section names are read, not only `## Tests` | `addendumSections` returns every `## Addendum…` section, whatever follows the word in the heading. `namedTests` is the union of those sections and `## Tests`, in order of first mention. `## Cited` and every other section are still not read | `review-scope.test.mjs` › "reads every Addendum section as well as Tests, and nothing after them", "puts an addendum's test in the reviewer's list" |

The Build line's other half, removing from the skill the workaround of passing `origin/main` by
hand, had no text to remove. `.claude/skills/ticket/SKILL.md` step 5 and step 8 already call
both scripts with no base; the hand-passed `origin/main` existed only in the runs' own commands.
Once this lands, the commands the skill already gives are correct as written. What the skill did
carry was the same defect one step over: the reviewer's own `git diff main...HEAD`
(`.claude/agents/reviewer.md` step 2). It now reads `origin/main...HEAD` and says why, and its
step 5 names Addendum sections. `scripts/run/README.md` step 5 and step 8 say the same.

## Tests written, each observed red first

Two reds per test. The first is the code as it stood before this ticket (`ce2b4a0`), with the new
tests in place: `review-scope.test.mjs` went 7 failed / 5 passed, and `report-check.test.mjs`
went 1 failed / 15 passed. The CLI tests were red for exactly T1.4's reason: the diff against the
stale local `main` carried `other.test.mjs`, which in the fixture stands for another ticket's
merged test. The second red is one targeted mutation of the finished code, restored after each
run. The counts in the table are both files together, 28 tests.

| test · file | reddened by | red → green |
|---|---|---|
| "reads only the branch's own test files" · `scripts/run/review-scope.test.mjs` | before: the CLI's `base ?? "main"` read `other.test.mjs` as touched; M1 `BASE` back to the literal `"main"` | 7 failed / 5 passed → 12 passed; M1 3 failed / 25 passed → 28 passed |
| "defaults to origin/main in the library too, for any caller that passes no base" · `scripts/run/review-scope.test.mjs` | before: `touchedTests` and `reviewScope` defaulted to `"main"`; M1 | 7 failed / 5 passed → 12 passed; M1 3 failed / 25 passed → 28 passed |
| "accepts a report that records the branch's own tests and nobody else's" · `scripts/run/report-check.test.mjs` | before: refused with "other.test.mjs is in the diff and not in the record"; M1; M5 the report-check CLI alone back to `base ?? "main"` | 1 failed / 15 passed → 16 passed; M1 3 failed / 25 passed → 28 passed; M5 1 failed / 27 passed → 28 passed |
| "keeps a dynamic route segment whole rather than cutting the path at its bracket" · `scripts/run/review-scope.test.mjs` | before: `/OpportunityHeader.dom.test.tsx` and the rest; M2 the dynamic alternative dropped from `SEGMENT` | 7 failed / 5 passed → 12 passed; M2 3 failed / 25 passed → 28 passed |
| "reports nothing missing when every cited file is on disk, T1.4's included" · `scripts/run/review-scope.test.mjs` | before: six names in `missing`, as T1.4's pass 7 found; M2; M3 the directory made optional again | 7 failed / 5 passed → 12 passed; M2 3 failed / 25 passed → 28 passed; M3 2 failed / 26 passed → 28 passed |
| "does not count a precedent named in prose as a cited file" · `scripts/run/review-scope.test.mjs` | before: `item-key.db.test.ts` and `item.test.ts` in `named`; M3 `(?:${SEGMENT}/)*`, a bare name a citation again | 7 failed / 5 passed → 12 passed; M3 2 failed / 26 passed → 28 passed |
| "reads every Addendum section as well as Tests, and nothing after them" · `scripts/run/review-scope.test.mjs` | before: `## Tests` alone; M4 `namedTests` reads `## Tests` alone | 7 failed / 5 passed → 12 passed; M4 2 failed / 26 passed → 28 passed |
| "puts an addendum's test in the reviewer's list" · `scripts/run/review-scope.test.mjs` | before: `log-index.test.mjs` absent from `files`; M4 | 7 failed / 5 passed → 12 passed; M4 2 failed / 26 passed → 28 passed |

The five tests that were already in `review-scope.test.mjs` pass unchanged. So do the fifteen in
`report-check.test.mjs` and `scripts/run/readme.test.mjs`: 31 passed across the three files. As
evidence beyond the fixtures, `namedTests` was run over all 26 files under `docs/tickets/`. Before
the change, T1.4 had two bare names and four cut paths in `missing`, and T2.10 had two cut paths.
After it, nothing is missing anywhere, and T1.4's list gains `src/db/migrations.test.ts`, which
its own `## Addendum` names.

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `7368562` | fable | no | PASS |

**Pass 1** (commit `7368562`): **PASS**, with no Must and three Shoulds. It read every hunk of
`origin/main...HEAD` (seven files), and ran the two files `review-scope.mjs` lists for this
ticket: 2 files, 28 tests, nothing `missing`. It noted that the `reviewer.md` and README edits
fall outside what the ticket names but belong to the defect the Objective describes, and counted
them as accounted for.

1. **Should: the red-first record did not exist yet at the reviewed commit.** *Done:* the table
   above, one row per new test, both reds.
2. **Should: `scripts/hooks/guard.mjs:1017` still says the reviewer's diff is `main...HEAD`.**
   *Recorded, not fixed:* it is one word in a comment, the rule beside it is about `merge` the
   verb and is unchanged, and `origin/main...HEAD` rests on `merge-base` just the same. A guard
   edit made after the verdict would be unreviewed code in the file that judges every run. Open
   question 1.
3. **Should: the skill held no workaround to remove, and step 5 could name Addendum sections.**
   *Done for the first half:* the paragraph under *ACs implemented* says so. *Recorded for the
   second:* `SKILL.md` step 5 says "the tests the ticket names", which stays true now that the
   script reads more of the ticket. The reviewer's own definition, which the reviewer reads, is
   where the list is spelled out, and it now names both sections.

## Changed since this ticket was cut

`version-drift.mjs` over the Spec line: nothing cited carries a version, `drifted: false`. Main
moved from `ed0c5b8` to `ce2b4a0` between the cut and the claim. That was T1.4's merge, made by
this run's step 0, and it is what put T1.4's ticket file, its `[key]` routes and its addendum on
disk for the tests above to read.

## Open questions

1. **`scripts/hooks/guard.mjs:1017`'s comment names `main...HEAD`.** It should say
   `origin/main...HEAD`. Owner: the next ticket that edits the guard for a reason of its own. It
   is one word, and it is not worth a guard diff by itself.
2. **The prose rule is a convention, not a parse.** A precedent written out as a full path, such
   as "as `src/db/item-key.db.test.ts` does", is read as cited and run: one test too many, never
   one missed. The comment on `PATH_LIKE` says so. Owner: none unless it bites. The corpus
   measurement that chose the rule is in *Tests written*.

## Defaults taken under §4

- **What counts as prose.** The ticket says "a prose precedent" and does not define it. I read it
  as "a test named by its bare file name". Every citation in every ticket file carries a
  directory, and T1.4's two precedents are the only bare names in any Tests section. Choosing it
  alone was cheap: it is a developer surface, and it is one regex to change back.
- **The reviewer's own diff.** `.claude/agents/reviewer.md` step 2 moved from `main...HEAD` to
  `origin/main...HEAD`, and step 5 now names Addendum sections. The Build asked for the skill to
  stop carrying AC1's workaround; this was the one place the skill still named the literal
  `main`. Choosing it alone was cheap: it is a developer surface, one line to revert, and
  `gated.mjs` read the diff as ungated.
