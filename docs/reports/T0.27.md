# T0.27 — Extra-high effort for runs

Branch `t0-27` off `origin/main` at `1243eed`, claimed by the scheduled run on 2026-09-22 in its
own worktree. A `/ticket` run and every review pass now ask for `xhigh` effort, one step below
max. The run's effort comes from the skill's frontmatter and each review's from the reviewer's.
The scheduled task's form picks a model and has no effort control. Claude Code reads an `effort`
from both kinds of frontmatter and lets it override the session's effort for as long as the skill
or subagent is active. The model-config table lists `xhigh` for Fable 5.1 and for Opus 5, so the
level holds on the reviewer's pinned model and on its fallback. Nothing in the repository's
settings changed. Guidelines §5 says where the effort is set, at v1.19.

## ACs implemented

| AC | What was built | Its test |
|---|---|---|
| AC1 The `/ticket` skill's frontmatter sets `effort: xhigh` | `effort: xhigh` added to `.claude/skills/ticket/SKILL.md`'s frontmatter, after `disable-model-invocation`. `.claude/settings.json` carries no `effortLevel` and no `env.CLAUDE_CODE_EFFORT_LEVEL`, so the frontmatter is the one place a run's effort is set | `scripts/run/effort.test.mjs` › "sets xhigh in the /ticket skill's frontmatter", "sets no effort in the repository's settings" |
| AC2 The reviewer's frontmatter sets `effort: xhigh`; its model, fallback and turn cap are unchanged | `effort: xhigh` added to `.claude/agents/reviewer.md`'s frontmatter. `model: fable`, `maxTurns: 30` and `.claude/settings.json`'s `fallbackModel: ["opus"]` are untouched, and `readChain` still reads `fable` then `opus` | `scripts/run/effort.test.mjs` › "sets xhigh in the reviewer's frontmatter", "leaves the reviewer's model, fallback and turn cap as they were" |

Guidelines §5's opening paragraph gains one sentence. It says a run and every review pass run at
`xhigh`, and names the two frontmatters that set it. It also says why the effort is not in a
repository setting: that would reach every session opened in the repository. The header moves
from v1.18 to v1.19. No test and no script pins the old number (`version-drift.mjs` reads v1.19).

## Tests written, each observed red first

All four tests are in one file, `scripts/run/effort.test.mjs`. The first three were written
before either field existed. Against the tree at `1243eed`, the file went 2 failed / 1 passed:
the two `effort` tests were red, and the third test was green because it checks values the
ticket leaves alone. Adding the skill's field made it 1 failed / 2 passed, and adding the
reviewer's made it 3 passed. The table's mutations were each made on the finished tree and
restored after the run. The fourth test came from review pass 1. Every count in the table is for
the file at four tests.

| test · file | reddened by | red → green |
|---|---|---|
| "sets xhigh in the /ticket skill's frontmatter" · `scripts/run/effort.test.mjs` | before: no `effort` line in `SKILL.md` (2 failed / 1 passed at three tests); M1 the `effort: xhigh` line removed from `.claude/skills/ticket/SKILL.md` | 1 failed / 3 passed → 4 passed |
| "sets no effort in the repository's settings" · `scripts/run/effort.test.mjs` | M2 `effortLevel: "xhigh"` written into `.claude/settings.json`; M3 `env.CLAUDE_CODE_EFFORT_LEVEL: "xhigh"` written into it instead | M2 1 failed / 3 passed → 4 passed; M3 1 failed / 3 passed → 4 passed |
| "sets xhigh in the reviewer's frontmatter" · `scripts/run/effort.test.mjs` | before: no `effort` line in `reviewer.md` (1 failed / 2 passed once the skill had its field); M4 the `effort: xhigh` line removed from `.claude/agents/reviewer.md` | 1 failed / 3 passed → 4 passed |
| "leaves the reviewer's model, fallback and turn cap as they were" · `scripts/run/effort.test.mjs` | M5 `model: fable` → `model: opus`; M6 `maxTurns: 30` → `maxTurns: 40`; M7 `.claude/settings.json`'s `fallbackModel` `["opus"]` → `["sonnet"]` | each 1 failed / 2 passed → 3 passed at three tests; 4 passed at four |

The whole `scripts/run/` suite passes with the new file in it: 32 files and 509 tests at three
tests in this file.

## Reviewer passes and findings

| pass | commit | model | resumed | verdict |
|---|---|---|---|---|
| 1 | `1254a9f` | fable | no | FINDINGS |

**Pass 1** (commit `1254a9f`): **FINDINGS**, one Must and one Should. The reviewer read the
five-file diff against `origin/main`. It ran the one file `review-scope.mjs` lists, which had
3 tests, all passing. It confirmed that `xhigh` is a level the model-config table lists for both
the pinned model and the fallback.

1. **Must: no red-first record on file.** The report did not exist yet at the reviewed commit.
   *Done:* the table above has one row per test, and every row names its mutations and counts.
2. **Should: the Build's "nothing in settings" had no test.** *Done:* the test "sets no effort in
   the repository's settings" asserts that `.claude/settings.json` holds neither `effortLevel`
   nor `env.CLAUDE_CODE_EFFORT_LEVEL`. It is reddened by M2 and M3.

## Changed since this ticket was cut

`version-drift.mjs` over the Spec line: the Spec cites Claude Code's docs, which carry no version,
so the result is `drifted: false`. Main moved from `11c946a` to `1243eed` between the cut and the
claim. That was T0.18's merge, made by this run's step 0, and it took guidelines from v1.17 to
v1.18. So this run cut its branch before inlining, and the ticket file quotes §5 as it stands on
main, not as it stood in the worktree's stale checkout.

## Open questions

1. **This run cannot observe the effort it sets.** This session loaded the skill from before the
   field existed. The first run after the merge will be the first at `xhigh`. The Runs row
   records the model, not the effort, so checking the level means reading that session's own
   status line once. Owner: the human, on the next run.
2. **`.claude/settings.local.json` is not checked.** It is per-machine and gitignored, so it is
   not "the repository's settings" the Build names. An effort set there would still reach every
   session on this machine. Owner: none, unless a run is seen at a level it did not ask for.

## Defaults taken under §4

- **Where the guidelines line goes.** The Build says "one line in the run section". I put one
  sentence, wrapped over three lines, at the end of §5's opening paragraph, next to "fresh
  session, Fable". I could choose this alone because it is a document the pipeline owns, one
  edit to move.
- **Where the test lives, and what it parses with.** The test is a new file,
  `scripts/run/effort.test.mjs`. A flat `key: value` reader for the frontmatter sits in the test
  itself, since nothing but the test needs one. It reads the file paths from `review-model.mjs`,
  which already names the reviewer's definition and the settings file. This is a developer
  surface.
- **What "unchanged" pins.** The model, cap and fallback are pinned to their values at the cut:
  `fable`, `30` and `["opus"]`. A later ticket that changes any of them edits this test in the
  same diff. That is the point of the check, and it costs one line.
