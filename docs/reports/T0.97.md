# T0.97 — report

Smoke C: the Stop gate's release message made friendlier. Smoke B's ticket shape under §4 as it
now reads: the body did not say what "friendlier" means and nothing in build-guide §6 does, but a
hook's output is a developer surface, so the run took a default, said so in one comment on the
board, and kept building instead of stopping. The task was also this run's first stale-run
recovery: the row was In progress with no marker in this checkout and no branch anywhere, so it
was claimed again with one stale comment and no human. Epic E0.2 Pipeline · Type Technical ·
Priority Should.

## Files changed

`scripts/hooks/gate.mjs` (one line: the `RELEASE_MESSAGE` constant) ·
`scripts/hooks/gate.test.mjs` (the release literal updated and renamed `RELEASE_WORDING`, one
new test pinning the register) · `docs/tickets/T0.97.md` · `docs/reports/T0.97.md` ·
`docs/build-log.md` (one line under Tickets done).

The wording, byte for byte, in the constant, the test literal and the ticket:

    Three reds in this session, so the gate has stepped aside. That usually means the ticket needs restating rather than the code pushing harder (build-guide §6).

## ACs implemented

| AC | Where | Test |
|---|---|---|
| AC1 on the third red and every stop after it, exit 0 and the wording above on stderr and in `systemMessage` | `scripts/hooks/gate.mjs` `RELEASE_MESSAGE`; both emission sites in `main()` read the one `stderr` value `decide()` returns | TC1 — `gate.test.mjs` "releases on the third red rather than refusing a fourth time" and "stays released without running the suite again", `toBe(RELEASE_WORDING)` as a literal |
| AC2 complete sentences, no dash-joined fragment, none of `violation` `error` `fail`, names build-guide §6 | the wording itself | TC2 — `gate.test.mjs` "releases in plain sentences that say where the rule lives" |

## Tests written, each observed red first

All in `scripts/hooks/gate.test.mjs`, run with `pnpm vitest run scripts/hooks/gate.test.mjs`
(28 tests in the file after the change). Order of work: TC2 was written first against the old
constant; then the constant changed while the test literal still held T0.99's wording, which
reddened TC1; then the literal was updated.

| test | reddened by | red → green |
|---|---|---|
| TC2 "releases in plain sentences that say where the rule lives" | written against T0.99's wording, which ends without a full stop, contains ` — ` and does not name build-guide §6 (`expected false to be true` on the full-stop assertion) | 1 failed / 27 passed → 28 passed |
| TC1 "releases on the third red rather than refusing a fourth time" | `RELEASE_MESSAGE` in `gate.mjs` changed to the new wording while the test's literal still pinned T0.99's (`expected 'Three reds in this session…' to be 'gate released after three reds…'`) | 2 failed / 26 passed → 28 passed |
| TC1 "stays released without running the suite again" | the same mutation, same assertion | 2 failed / 26 passed → 28 passed |

The diff against `main` also carries T0.9's test files, because this branch is based on `t0-9`
(see open question 2): `scripts/hooks/guard.test.mjs`, `scripts/run/claim.test.mjs`,
`scripts/run/comments.test.mjs`, `scripts/run/report-check.test.mjs`,
`scripts/run/review-scope.test.mjs`, `scripts/run/stale.test.mjs`,
`src/app/sign-in/SignInForm.dom.test.tsx`. Those are T0.9's work and T0.9's report holds their
record; this ticket does not touch them.

## Reviewer

One pass. No Must; two Shoulds, both recorded here and neither changing the diff:

1. AC1 names two surfaces and TC1 pins one — `result.stderr`. The `systemMessage` half lives in
   `main()` (`gate.mjs`, the `JSON.stringify({ systemMessage: stderr })` line), which `decide()`'s
   unit tests do not exercise and never have. It cannot diverge from stderr by construction, since
   both sites read the one value; that is the reason it is a Should. Open question 1.
2. The red-first record did not exist at review time, because §5 orders Report after Review. The
   table above is that record.

The reviewer ran `review-scope.mjs` against `t0-9` (this ticket's base): `gate.test.mjs` only,
nothing missing; 28 passed.

## Changed since this ticket was cut

Nothing. `guidelines v1.4` and `build-guide v2.3` match the repo headers.

## Open questions

1. The `systemMessage` path of the release has no test of its own; AC1's claim for it rests on
   `main()` reusing the `stderr` value. A test would have to drive `main()` with a stdin payload.
   Owner: T-next, if the gate's `main()` is ever given a seam.
2. Branch base. `t0-9` is not on `main`, so this branch is `t0-9` plus one commit and the PR
   carries T0.9's commit with it; `migration-check` and `review-scope` diff against `origin/main`
   and list T0.9's files. Merging T0.9's PR first makes this PR the one-commit change it is. Same
   shape as Smoke B on `t0-8`. Owner: the human merging.
3. The default taken here is a judgment about register, recorded on the board thread. If the
   wording should be different, it is one constant and one literal.
