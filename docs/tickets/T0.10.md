# T0.10 — Schedule: nobody types /ticket

Structural. Claimed by hand in a Code tab session on 2026-09-09 · Epic E0.2 Pipeline · Priority
Must · Type Technical · Spec `guidelines v1.4 §2, §5 · docs/reports/T0.9.md open questions 4, 5,
7, 8`. Branch `t0-10` off `origin/main` at `ca6f332`. The board row stays at Backlog until close:
the live fixture's scheduled run must claim T0.96 and nothing else, and a Ready T0.10 would be the
top Must. Telemetry, the mirror sentinel and the migration-apply path are T0.11, created at close.

## Objective

A Desktop scheduled task runs `/ticket` on a cadence; two runs can never overlap; a pipeline
session cannot alter the database schema or deploy, by capability rather than by parsing;
overlapping PRs stop conflicting on the build log. After this merges, the human sets tasks Ready,
leaves the machine awake, and reads Review and Decision in the morning.

## Build

Read first, fully: docs/guidelines.md v1.4, docs/reports/T0.9.md, .claude/skills/ticket/*,
scripts/run/claim.mjs, release.mjs, stale.mjs, scripts/hooks/guard.mjs, .worktreeinclude,
.env.local (names only). Then the current docs, not memory: the Desktop scheduled-tasks page and
the Desktop page. Report anything the docs say that contradicts this pack before planning around
it.

1. **Marker moves to shared ground.** Scheduled sessions may each get a worktree, and a marker
   inside one worktree is invisible to another, so the never-overlap rule would silently fail.
   Marker path becomes `$(git rev-parse --git-common-dir)/aenima-run-active`, which every
   worktree of this repo shares and git never tracks. claim, release, stale and the guard's
   gh-pr-merge rule all read it there. Test: two temp worktrees of one temp repo see the same
   marker. *Approved addition:* the gate's `.gate-count` moves to the same place, so an idle run
   in a fresh worktree inherits the last green fingerprint.

2. **Scheduled task.** Create it the way the docs support: prompt `/ticket`, folder
   ~/dev/aenima, hourly, permission mode that never prompts (the hooks are the boundary), Notion
   connector available to it. Confirm from the docs whether it runs in a worktree; if it does,
   confirm .worktreeinclude carries .env.local. Write down in guidelines §5 exactly how to turn
   it on and off, and what it costs when idle. If a run is live (fresh marker), preflight exits
   within seconds; verify the Stop gate short-circuits on an unchanged tree so an idle run is
   cheap. *Approved addition:* find out whether Desktop removes a run's worktree after it ends;
   if not, preflight prunes worktrees whose branch is merged or older than three days. Measure
   the pnpm install cost in a fresh worktree; it goes in §5's idle-cost line.

3. **Database capability.** A Postgres role that can read and write rows and cannot change
   schema: LOGIN, no CREATE on schema public, no table ownership, DML on all current and future
   tables in public, membership in `authenticated` and `anon` if the RLS tests need SET ROLE.
   Exact SQL for the Supabase SQL editor, that step is the human's, stop until confirmed. Then
   .env.local's DATABASE_URL becomes that role's URL for everyone; the admin URL moves to a new
   gitignored file .env.migrate that `pnpm db:migrate` loads explicitly and that .worktreeinclude
   does NOT carry. Run the full suite under the restricted role; if any test needs DDL, list it
   and stop. Remove the `db:migrate` text rule from the guard only if the capability boundary is
   proven; otherwise keep both. *As planned and approved:* the role is a member of
   `service_role` (every public table has RLS forced and every policy is `TO authenticated`, so a
   plain DML role would be denied every row; Postgres 17 lets no non-superuser grant BYPASSRLS);
   `service_role` holds no privilege on `auth.users`, so migration `0014_test_users.sql` adds
   `app.seed_user` and `app.delete_user` for the eight db test files that seed users; the guard
   rule stays.

4. **Deploy capability.** If `vercel whoami` succeeds on this machine, the human runs
   `vercel logout`; deploys come from main through the Vercel Git integration only. The guard
   rule stays as belt and braces.

5. **Build log without a shared append point.** Each ticket's entry becomes docs/log/<id>.md;
   docs/build-log.md's Tickets done section becomes a one-line link list generated by a script
   from that directory, so two PRs never edit the same lines. Migrate the existing entries once.
   Skill step 8 writes the per-ticket file.

6. **docs/guidelines.md → v1.5:** §5 the scheduler (on, off, cadence, idle cost), the shared
   marker, the capability boundary, per-ticket logs; §3 unchanged; §8 T0.10 and T0.11 as they
   now are. build-guide → v2.4 §2: "set Ready; the timer does the rest".

## Rules

- Capability over parsing: a pipeline session must be unable to migrate or deploy even if every
  guard rule were deleted. The guards remain as the second layer. *Approved wording:* the
  credential a run is handed cannot change schema and the admin credential is on no path a run
  follows; a worktree session can still read any file on the disk, which is why rule (b) stays.
- Never print a secret. Names of env vars only.
- Where the docs are silent, take the default under §4 and say so; stop only for the two human
  steps above (the SQL, the logout).
- Vercel's own DATABASE_URL moving to a restricted role is real and out of scope: a Backlog
  Technical task under E0.2, not under T0.11.
- T0.9's open questions 5, 7 and 8 are reassigned to T0.11 at close.

## Criteria

- AC1 marker shared across worktrees.
- AC2 log index generated from docs/log/ matches the migrated entries.
- AC3 guard gh-pr-merge rule reads the new marker path.
- AC4 under the restricted role, `pnpm db:migrate` fails on permissions, in the primary
  checkout, with the error quoted.
- AC5 full suite green under the restricted role.
- AC6 live, unattended: fixture "T0.96 Smoke D", Backlog, body "Add a `pnpm board:ids` script
  that prints the keys of .claude/board.json, with a test". The human sets it Ready and leaves.
  The scheduled task claims it within its cadence and it reaches Review with a PR, and a second
  scheduled run afterwards exits on an empty queue. The human reports what the board shows; the
  report quotes it.

## Tests

- TC1 → AC1: `scripts/run/claim.test.mjs` "the marker is shared across worktrees" (4), each
  reddened by a named mutation.
- TC2 → AC2: `scripts/run/log-index.test.mjs` "the committed build log is the generated one" (2)
  and the script's own tests (10).
- TC3 → AC3: `scripts/hooks/guard.test.mjs` "TC3 — rule (f) reads the shared marker" (2).
- TC4 → AC4: `src/db/capability.db.test.ts`, error 42501 on create table, alter table, create
  policy and a read of `drizzle.__drizzle_migrations`, each rolled back; and the literal
  `pnpm db:migrate` under the restricted URL, run by the human because the guard refuses the
  session, error quoted in the report.
- TC5 → AC5: `pnpm test` under the restricted URL, count quoted.
- TC6 → AC6: the board, quoted from the human's report.
- Not under an AC: `scripts/run/prune.test.mjs` (9), each reddened by a named mutation.

## Done

`pnpm lint && pnpm typecheck && pnpm test`; TC1–TC6 observed and quoted; then the cold review,
three-pass cap.

## Report

Written at close: `docs/reports/T0.10.md`, mirrored into the board row.

## Cited

**guidelines v1.4 §2 — Documents.** Seven sub-pages: `product-spec` · `design-spec` · `CLAUDE` ·
`AGENTS` · `build-guide` · `build-log` · `schema`. The page is headed, verbatim: **Machine-written
mirrors of the repo documents.** *Each page is refreshed from `main` at the start of every
pipeline run and headed with the commit it mirrors. The repo is the only editing surface; nothing
typed here survives the next run.* Nobody types here. The refresh itself is T0.10's; no run before
it touches a mirror.

**guidelines v1.4 §5 — Run protocol.** One run is `/ticket`, typed by a human from `~/dev/aenima`:
fresh session, Fable, auto mode, hooks as the boundary. T0.10 puts it on a schedule. Everything
countable in the steps below is a script under `scripts/run/` with a test; the skill holds the
judgment and nothing else. [Steps 0–9 as in v1.4.] The marker is the run's footprint and nothing
more. It is written at claim and removed on every exit — Review, Decision, and error through the
SessionEnd hook; a hard kill leaves it behind, which is what the three-hour age is for. Only
scripts read it; the skill never reasons about it. A stale run recovers by default because the
branch is preserved: a wrong guess costs nothing. Hard boundaries, enforced by hooks not prose: no
schema push, no migration apply, no writes to `.env` or `.env.*` (`.env.example` is tracked and
excepted), no production deploy, no force-push, no push to main in any refspec shape, no merge
with main checked out, no `gh pr merge` while a run marker exists. The guard reads commands, never
text: prose inside a heredoc or a quoted string matches no rule.

**docs/reports/T0.9.md — open questions 4, 5, 7, 8.** 4. The stale-run marker is per checkout. A
run in a worktree writes its marker there and the primary's preflight cannot see it; today every
run is in the primary checkout. Owner: T0.10, with worktrees. 5. The SessionEnd release has not
run live. Owner: T0.10, before the schedule relies on it. 7. `recover()`'s remote path is
untested. Owner: T0.10, whose worktree move touches `recover` anyway. 8. `recover()` says
`wip: true` without reading the commit's status. Owner: T0.10, with 7.
