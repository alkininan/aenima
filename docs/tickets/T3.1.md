# T3.1 — Author-critic loop

Claimed by the scheduled run on 2026-09-17 (session `150c7be8`) in its own worktree · Epic E3.1
Authoring loop · Priority High · Type Feature · Spec `product-spec v1.8 §6, §5, §13 · T2.3's fabrication
guard · T2.2's seam`, no version drifted. Branch `t3-1` off `origin/main` at `d79f49a`. Board row:
https://app.notion.com/p/3cf79dafd42e815d9b88e34d26d1ffb4.

The board's body carried all seven sections; they are transcribed below as the board holds them.
The two non-spec citations are code, not documents: **T2.2's seam** is `src/lib/ai/index.ts`
(`runGeneration`, `AiRequest`, the closed `Purpose` union in `src/lib/ai/types.ts`) and **T2.3's
fabrication guard** is `quoteOccursIn` in `src/lib/scoring/evidence.ts`, which `run.ts` applies to
every failure quote before it is written.

## Objective

The author drafts a section, the critic tests it against the pack's checks, each objection carries
the check it binds to and the section it may change, and no section is refined more than twice
before the disagreement surfaces as an open question the human owns.

## Build

**The two agents.** Both run through T2.2's seam at the generation tier. The author drafts and
revises one section at a time; the critic tests what the author produced and never rewrites.

**What the critic sees.** The artifact, the pack, the section under refinement and the check ids in
play — never the conversation, the author's reasoning, or how confident anyone sounded. §6's
blindness is what stops the judge being anchored by the drafting context. Scoping it to the section
under refinement rather than the whole draft is this ticket's decision: the critic's judgment is
unchanged, its reading is bounded, and §5's cached results stay valid for every section it did not
look at.

**An objection is four fields, or it is discarded.** `check_id` (binding to a real check in the
loaded pack, unbound objections dropped — §6's structural cure for nitpicking), `reason`, `evidence`
quoted from the artifact and verified against it the way T2.3's fabrication guard verifies a failure
quote, and `scope`: the section id it may change.

**Scope is enforced, not advised.** The author's revision call receives only the scoped section, and
a revision whose diff touches any other section is refused and counted as a failed round. A
wandering fix invalidates check results cached against sections nobody asked to re-run, which is
§5's cache and the reason this is a wall rather than a request.

**Two rounds, then the human.** A section may be refined at most twice per objection. On a third
disagreement the round ends: the author's latest draft stays in the document, and the objection
becomes an **open question** on the item naming the check it fails, its evidence and both positions.
The meter therefore reads the document as it actually stands, and no work is thrown away. The open
question is the §6 surfacing, not a gap — gaps are what scoring produces.

**Round state is stored, not inferred.** A `refinement_round` row per (artifact version, section,
check) holding the count, the objections and their outcomes — append-only like every other ledger,
so "why does this section have an open question" is answerable later. Needs a migration; the run
stops at Decision for the human's *apply*.

**The loop's shape.** author drafts → critic tests the section → no objection: the section settles →
objection: author revises within scope → critic re-tests → second objection: one more revision →
third: open question, section keeps its draft.

## Rules

- The critic detects and cites; it never rewrites. The author writes; it never judges its own work
- An objection without a bound check id is discarded silently — that is the design, not a failure
- Evidence is verified against the artifact before an objection is shown, as T2.3 verifies a failure
  quote
- Deterministic work is code: round counting, scope diffing, objection binding and the two-round cap
  are functions with tests, never a model's judgement
- Welcoming, never alarming: "this section was unclear", never "failed"
- Agent-authored text is visibly attributed in violet until the human confirms it

## Criteria

- AC1 An objection naming a check id absent from the loaded pack is discarded, and the round
  continues as if it had not been made
- AC2 A revision whose diff touches a section outside the objection's scope is refused, and the
  round counts as spent
- AC3 A section objected to three times keeps the author's latest draft and gains one open question
  naming the check, the evidence and both positions
- AC4 The critic's input contains no turn of the conversation — asserted over the assembled request,
  not over its output
- AC5 An objection whose evidence does not appear in the artifact is dropped before the author ever
  sees it
- AC6 Round state survives a reload: the count and the objections are read from the ledger, never
  recomputed from the document
- AC7 Two sections refine independently — a round spent on one leaves the other's count untouched

## Tests

- TC1 → AC1 · TC2 → AC2 · TC3 → AC3 · TC4 → AC4 · TC5 → AC5 · TC6 → AC6 · TC7 → AC7, each reddened
  by a named mutation. The model-facing paths use the seam's recorded fixtures rather than live
  calls; one end-to-end run on `sample-juno-feature.md` records what the loop actually produced.

## Done

`pnpm lint && pnpm typecheck && pnpm test`, and one recorded end-to-end run: a section drafted,
objected to, revised in scope, and a third disagreement surfacing as an open question — quoted in
the report.

## Report

Written at close: `docs/reports/T3.1.md`, mirrored into the board row.

## Stopped at Decision — 2026-09-17

The ticket asks for three things the specs never define, and each one is schema or stored data, so
under guidelines §4 a guess is not the run's to take. The one decision comment on the board row asks
all three and names the default the next run takes on the word "default".

1. **What a section is.** §6 says an objection's scope is "which section it may change", and the
   Build keys scope, the critic's input and round state on a section id. `artifact_version.content`
   is `{ body: string }` today — `renderArtifact` in `src/lib/scoring/prompt.ts` says no ticket has
   defined a document model and that Phase 3 would — and no spec, `docs/schema.md` included, says
   how a section is identified. *Default:* a section is a `## ` heading block of the markdown body,
   its id the heading's slug, and the text before the first heading a section of its own. That
   leaves stored content, `renderArtifact` and the scoring protocol's fingerprint as they are.
2. **Where an open question lives.** AC3 gives the item "one open question naming the check, the
   evidence and both positions", T3.2's decide-later writes one too and T3.4 renders both — but no
   table, column or schema.md section names it, and the Build says it is not a gap. *Default:* this
   ticket stores none of its own; the open question is the `refinement_round` row whose outcome is
   *surfaced*, carrying the check, the evidence, the critic's reason and the author's position, and
   T3.2 decides where a decide-later is stored when it gets there.
3. **What a `refinement_round` row is keyed on.** "A row per (artifact version, section, check)
   holding the count" cannot also be append-only once a round is refused: a refused revision cuts no
   new version, so the next round tests the same version and would need a second row on the same
   triple — or an UPDATE of the count, which append-only forbids. *Default:* one append-only row per
   round — the version it tested, the section, the check, the round number, the objection, the
   outcome — unique on (artifact, section, check, round number), the count read as the highest round
   number since the section last settled.

Built before the stop, because none of the three answers moves them: objection intake (AC1, AC5)
and the scope wall over a list of sections (AC2). Everything else — the critic's request (AC4), the
two agents' calls, the cap over stored rounds (AC3, AC6, AC7), the migration, the end-to-end run —
waits for the answer.

## Addendum — 2026-09-17

Sent back from Decision with the three schema answers in the board row's body, and the task set
Ready. The body's addendum, verbatim:

> ## Addendum — the three schema answers (2026-09-16)
>
> All three defaults are accepted as asked. They are decisions now, not assumptions, and the ticket owns them.
>
> **1. A section is a `##` heading block**, its id the heading's slug. Stored content and scoring are unchanged — sectioning is a parse over the stored body, not a new storage shape. Two headings that slug the same get a numeric suffix in document order, so ids stay stable while the document grows.
>
> **2. An open question is a round row marked surfaced**, not a new table. No open-question table is created here. T3.2's Open Questions section and T3.4's rendering both read surfaced rounds, and the ticket says so where they are cited.
>
> **3. One append-only row per round**, unique on (artifact, section, check, round number). Correcting the pack: "one row per artifact version, section and check" cannot be append-only, because a refused revision cuts no new version — the round number is what makes each attempt its own row. AC6 reads the count as the highest round number for that key.
>
> **What the next run does:** carry on from `ab650d5` with these three settled — the round ledger and its migration, then the rest of AC2, AC3, AC4, AC6 and AC7, the review, and the recorded end-to-end run. The migration stops the run once more for the human's *apply*; that stop is expected and is not a Decision to re-ask.
>
> **Also in scope, small:** preflight regenerates the route types whenever they are stale, not only when the folder is missing — this worktree arrived with types naming a route that is not on main and typecheck failed before anything was touched. Fix it in `scripts/run/`'s preflight rather than leaving it as a follow-up.
>
> **AA1** a `##` block parses to a section whose id is the heading slug, with a suffix on collision · **AA2** a surfaced round row is what AC3 creates; no open-question table exists · **AA3** two rounds on one (artifact, section, check) are two rows, and the count is the highest round number · **AA4** preflight regenerates stale route types · TA1 → AA1 · TA2 → AA2 · TA3 → AA3 · TA4 → AA4

This round builds the addendum's criteria with the Criteria still open above: the round ledger and
its migration (AA2, AA3), sectioning (AA1), the rest of AC2, AC3, AC4, AC6 and AC7, and the stale
route types (AA4). Claimed again by the scheduled run on 2026-09-17 (session `212c9b1c`), branch
`t3-1` reused from origin at `ab650d5`; `product-spec` still v1.8, no drift.

## Questions this stop puts to you — 2026-09-17

The addendum named what a round row holds — "the version it tested, the section, the check, the
round number, the objection, the outcome" — and not the values of the outcome or where a round
records what it produced. Migration 0015 answers three things that are schema, so under guidelines
§4 they are not the run's to settle, and the run is stopping for `apply` anyway. **Answering `apply`
takes all three as 0015 has them.** A reply that changes any of them sends the ticket back to
Ready, the next run edits 0015 before it is applied, and stops for `apply` again.

1. **A fourth outcome, `held`.** The author returns the section unchanged — it has nothing new to
   say without a fact nobody has given it. `held` spends the round and cuts no version. Without it,
   an unchanged revision has to be recorded as `revised` (cutting a version identical to the last,
   which re-runs §5's checks on text that did not change) or not recorded at all (which lets the
   author hold its ground forever, since no round is spent). *Default: `held`, as in 0015.*
2. **`revised_version_id`** — the version a `revised` round cut, null otherwise. It is what makes
   "which edit answered this objection" a join rather than a guess by timestamp. *Default: kept.*
3. **`outside_sections`** — the sections a `refused` revision touched outside its scope, null
   otherwise. It is the ledger's answer to "why was this round spent". *Default: kept.*

The preamble — text before the first `##` heading — is not among them: the review found it was
reaching stored data as a section id, and it no longer does. The loop refuses to refine it, so
`section_id` only ever holds a `##` heading's slug.

## Cited

### product-spec §6 — Authoring engine

#### 6. Authoring engine

Documents are created by interview, not by template-filling. Two agents run every session:

- The **author** conducts: drafts from whatever exists, asks the next unanswered question in plain speech, one at a time.
- The **critic** runs behind every answer, blind: it sees only the artifact and the rubric — never the conversation, the author's confidence, or the owner's pushback. It detects and cites; it never rewrites. Every objection must bind to a rubric check ID; unbound objections are discarded (the structural cure for nitpicking). An objection carries four things — the check id it binds to, the reason, the quoted evidence, and its **scope**: which section it may change. A returned section is corrected, not improved. Without a scope the author opens the section, notices two adjacent weaknesses, fixes those too, and a one-check correction becomes a rewrite nobody asked for — which also invalidates the check results cached against the sections it touched. The two-round cap governs attempts; scope governs blast radius, and both are needed.

Session flow, identical at every entry state:

1. **Collect.** "Got anything on this already — a doc, a thread, or just what's in your head?" Upload, pick from connected sources, or keep talking. Skipped when intake already routed material.
2. **Silent test.** The critic runs all checks against everything collected, including the braindump itself. The meter jumps to its true starting score: "14 of 20 already answered from your doc."
3. **Gap interview.** Only the leftovers are asked. The problem block always opens first (everything downstream inherits a wrong problem); after that, whichever question closes the most remaining rubric weight goes next. Each answer is echoed back in doc-language for a one-tap confirm before it's written. A single enthusiastic paragraph is credited against every check it touches.
4. **Close — whenever.** Sessions persist; the meter holds. "Decide later" is always legal and lands in an auto-rendered Open Questions section with the check it blocks.

Hard limits from the self-correction research: **two refinement rounds per section, maximum** — after two, the disagreement surfaces to the human as an open question. Author and critic are separated so the judge is never anchored by the drafting context.

The screen: item page, conversation on one side, the draft document assembling live on the other, meter on top. More material captured upstream (meetings, Slack, braindumps) → fewer questions asked → the system visibly rewards being fed.

### product-spec §5 — Scoring engine

#### 5. Scoring engine

Each active artifact is scored against its rubric: 0–100, weights zero-sum per rubric, each check tagged **Must** (blocks handover) or **Should** (advisory).

- **Checks are binary with evidence.** A check passes or fails, and a failure quotes the exact gap ("MN-2: 'nearby' — same venue, or within 100 m? Two readings possible."). No vibes-based partial credit.
- **Event-driven re-scoring.** Notion webhooks where available, short-interval polling where not (Figma), debounced so an edit burst triggers one run, plus a nightly full sweep as backstop. Timestamps show freshness ("scored 4 min ago"); a queued retry shows "scored 6 h ago — retrying" during provider outages, never an error banner.
- **Stability.** Results cache per artifact version; only checks whose artifact changed re-run. The scoring model is pinned per workspace and never juggled for cost. Switching AI provider or editing a rubric triggers a quiet re-baseline pass so numbers never wobble without explanation.
- **Rubrics are versioned** like documents. Every scoring run stamps provider + model + rubric version + **protocol version**. Rubric edits ship through skill packs (below) with the same re-baseline.
- **The protocol is versioned like the rubric, and is part of the cache key.** The protocol is the instruction layer wrapped around a rubric — how to answer, what counts as a quote, that a check is binary rather than partial. It changes verdicts as surely as a check's wording does, so a run scored under one protocol is not comparable to a run scored under another and must not be served from the cache to it. Its version is **computed from the assembled context, never declared**: an edit to the protocol, or to any renderer that shapes what the model is sent, moves the number by itself rather than waiting for someone to remember to bump it. A stamp a human maintains is a stamp that is eventually wrong.
- **Checks are expensive and few; probes are free and infinite.** A rubric stays ~10–20 checks; behind each check sits an open-ended probe library of follow-up questions. Any new check — including ones promoted from the probe library by the learning loop — must take its points from an existing check. This zero-sum budget is what keeps the standard holdable in a human head.

**Negotiation protocol.** Talking the AI into a state change is legal and structured. Persuasion resolves into exactly three typed moves; rhetoric that maps to none of them changes nothing:

1. **"Doesn't apply here"** → applicability re-check. If the condition genuinely fails, the check leaves the denominator.
2. **"Already covered"** → user points at evidence; the pinned scorer re-runs the check against it. Pass → closed with the evidence linked. Fail → the exact remaining gap is shown.
3. **"We accept this risk"** → never closes the gap; converts it to an accepted gap stamped with the accepter's name, routed through the Decider if handover-blocking.

Patterns change the law, not the verdict: when a workspace repeatedly accepts or excludes the same check, the agent proposes a rubric change (versioned, Owner-approved) — it never quietly loosens.

### product-spec §13 — Surface

#### 13. Surface

**Primary view: a prioritized list, not a board.** Status is derived, so a drag-to-move board would be decoration. Three buckets:

- **Your move** — anything awaiting a human: sign-offs, exclusion confirms, triage items, walkthrough answers, stalled packets (>48 h). Always on top.
- **At risk** — score regressed this week, a handover-blocking gap older than 5 days, or time-in-stage past ~1.5× the learned baseline. Sorted by blocking-gap age 40%, regression size 30%, staleness ratio 20%, handover proximity 10% (weights tunable).
- **Flowing** — everything else, by recent activity.

A thin pipeline strip on top shows counts per stage. Each row carries per-stage readiness meters. Idle items dim relative to their stage baseline and receive a one-tap, reversible **park** suggestion; the parked list ("graveyard") is a first-class, guilt-free destination — killing ideas cheaply is the system working. Retroactive imports keep historical gaps in a separate **foundation gaps** view with one-tap "adopt," never polluting the buckets.

**Chat is everywhere and is a full action surface.** It is also the command palette: `Cmd/Ctrl+K` focuses it from any page. Globally aware (whole workspace), page-aware only for pronoun resolution ("park this" needs no name on an item page; "how's Juno doing?" works anywhere and navigates you there while answering). It performs every permitted move — park, confirm exclusions, log decisions, push gaps, draft patches — and respects the role matrix: a Developer asking for a rubric change gets "that's an Owner action — want me to request it?" It answers ceremony questions in librarian mode and captures decision moments ("so we're dropping video for V1 because of capacity") with an offer to log them: decision, reason, date, who.

**Notifications route by role.** Owner: daily digest + spend alerts. Product: triage, exclusion confirms, regressions. Developer: packets and walkthrough follow-ups only. Viewer: nothing by default. The **daily digest reports deltas, never state dumps:** new blocking findings, resolved findings, regressions, aging sign-offs, ready-buffer line, glossary proposals — in the same neutral voice throughout.
