# T1.4 — Opportunity page and key

`product-spec v1.6 §2, §13 · build-log OQ9`. Branch `t1-4` off `origin/main`. Type Feature,
Epic E1 the spine, Priority Low.

> Drafted by pipeline · 2026-09-16 · the board row carried Objective, Rules and Done; Build,
> Criteria and Tests are expanded here from the Name, the Objective, the cited sections and the
> open question the Spec names.

## Objective

An opportunity page a person can say the URL of: `/o/<key>` with a `key_prefix` counter assigned
by trigger, mirroring `item.key` — never a uuid route. Until it ships the item header shows the
opportunity's title as plain text.

## Build

1. **`drizzle/0016_opportunity_keys.sql`** — `opportunity.key`, the same two-part name
   `item.key` carries and by the same mechanism: added nullable, backfilled in creation order
   per product from `product.key_prefix`, then `NOT NULL`; `UNIQUE (workspace_id, key)` as the
   backstop on the MAX+1 race; the `item_key_shape` regex as a CHECK; and
   `app.assign_opportunity_key()` on `BEFORE INSERT`, overwriting `NEW.key` unconditionally so a
   client-supplied key is ignored rather than honoured. Hand-written, like 0001–0014.
2. **`src/db/schema/tables.ts`** — the column in the Drizzle DSL, so the schema file and the
   database agree.
3. **`src/db/database.types.ts`** — `key` on `opportunity`'s Row, Insert and Update, in the shape
   the generator gives a `NOT NULL` column with no default. Hand-written for one round: the
   generator reads the live database, and the migration is not applied yet.
4. **`src/db/queries/opportunity.ts`** — `getOpportunityByKey(workspaceId, key)`: the
   opportunity, its product, and the items that belong to it with the artifacts `deriveStage`
   needs, in one request. Null for a key that does not exist and for a key in a workspace the
   caller cannot see — the same answer, as `getItemByKey` gives.
5. **`src/app/o/[key]/page.tsx` + `OpportunityHeader.tsx` + `ItemLine.tsx`** — §4's page frame as
   the item page holds it: the back link, the header, one titled section, and the reserved 380
   chat column. The header carries the key in mono-readout, the title in display-xl, the summary
   under it and the product as §3's eyebrow. The section lists the items, each a link to
   `/i/<key>`.
6. **`src/app/i/[key]/ItemHeader.tsx`** — the opportunity line becomes a link to `/o/<key>`.
   `opportunityTitle: string | null` becomes `opportunity: { key, title } | null`: after the
   migration a linked opportunity always has a key, and two nullable fields could say otherwise.
   `src/db/queries/item.ts` embeds `opportunity(key, title)` to feed it.
7. **`src/i18n/en.ts`** — an `opportunity` block for the new page's strings.

## Rules

- `src/lib/routes.ts` keeps segments short so a URL can be read out; a uuid defeats that
  entirely. `opportunityHref` is already there and is what the page and the header use.
- Artifacts are immutable and status is derived: the item lines on this page carry a stage
  computed by `deriveStage`, never a stored one.
- Every query names `workspace_id`. RLS narrows it again as the user.
- Every user-facing string goes through `src/i18n`. No bare strings in JSX.
- No hardcoded hex, radius, duration or font size — design tokens only.
- Keys come from the database, never the client, exactly as `item.key` and
  `artifact_version.version_no` do.

## Criteria

1. `opportunity.key` exists and is `NOT NULL`, unique per workspace, shaped
   `^[a-z][a-z0-9]{1,7}-[0-9]+$`, and assigned by a trigger from the product's `key_prefix` plus a
   per-product counter that starts at 1 and leaves no gaps.
2. A key supplied by the client on insert is ignored, not honoured.
3. `/o/<key>` renders the opportunity's key, title, summary and product, and lists the items that
   belong to it, each linking to `/i/<key>`.
4. An unknown key and a key in another workspace are the same 404, by one code path — never a
   redirect, never a 500.
5. An opportunity with no items renders a stated empty line rather than an empty region.
6. The item page's opportunity line is a link to `/o/<key>`; an unlinked item still renders no
   line at all.
7. Every string the two surfaces render comes from `src/i18n`.

## Tests

- `src/db/opportunity-key.db.test.ts` — the trigger, against a real Postgres, as
  `item-key.db.test.ts` holds `item.key`: per-product counters, no gaps, and a supplied key
  overwritten. **It skips loudly while `drizzle/0016` is unapplied** — the schema half of this
  ticket is a human's to apply (§5 step 6), and a hard failure there would redden every run in
  the repo until they did.
- `src/db/queries/opportunity.test.ts` — `getOpportunityByKey` against the recording PostgREST
  stand-in `item.test.ts` uses: one request, `workspace_id` and `key` both filtered, null for no
  row, and the item tree mapped with its derived stage.
- `src/app/o/[key]/OpportunityHeader.dom.test.tsx` — the header renders the summary when there is
  one and omits the line when there is not.
- `src/app/o/[key]/ItemsSection.dom.test.tsx` — the empty-state sentence when nothing is linked
  (AC5), every item's link when something is, and the region's label.
- `src/app/o/[key]/ItemLine.dom.test.tsx` — an item line links to `/i/<key>`.
- `src/app/i/[key]/ItemHeader.dom.test.tsx` — the opportunity line is a link to `/o/<key>`, and
  an unlinked item renders no line.
- `e2e/item.spec.ts` — the lineage assertion inverts with the thing it guarded: the line was
  asserted *not* to be an anchor while `/o/<key>` was unbuildable, and is now asserted to point at
  `/o/soc-2`.

## Done

`pnpm lint && pnpm typecheck && pnpm test` all pass. New logic has tests.

## Report

Written at close into `docs/reports/T1.4.md`.

## Addendum — renumber to 0016 (2026-09-20)

From the board, verbatim:

> Answering the collision the run found: **T3.1's migration keeps `0015`; this one becomes
> `0016`.** T3.1's `apply` was granted first and its table is the one Phase 3 is blocked on.
> Renumber the file and its journal entry on this branch after T3.1 lands on main, not before —
> rebasing onto a main that already carries `0015` makes the right number obvious and the journal
> consistent in one step.
>
> **What the next run does:** claim this task, merge current main in (which will carry T3.1's
> `0015`), renumber this branch's migration to `0016` with its journal entry, re-run the gate,
> then stop for the human's *apply* as any migration does. The *apply* already asked for on this
> thread covers the renumbered file — it is the same schema change with a different index, and
> re-asking is not needed.
>
> **AA1** the branch carries exactly one migration, numbered `0016`, and the journal's `idx`
> matches · **AA2** `0015` on main is untouched · **AA3** the merged tree is green · TA1 → AA1 ·
> TA2 → AA2 · TA3 → AA3

This round builds AA1–AA3 and nothing else. The seven original Criteria were built, reviewed and
green at `41047a7`; 3–7 are observable and 1–2 wait on the apply, unchanged by the renumbering.

The addendum's tests, named:

- **TA1 → AA1** · `src/db/migrations.test.ts` — "names every migration file, and every entry has a
  file" and "gives each entry the index its tag already carries, contiguously from zero". The
  journal read against `drizzle/`'s own listing: one entry per file, one file per entry, and each
  entry's `idx` the number its tag already carries.
- **TA2 → AA2** · `src/db/migrations.test.ts` — "stamps them in index order, so none is passed over
  in silence" and "keeps 0015 as T3.1's and 0016 as T1.4's, the collision as it was settled".
  `0015_refinement_round` pinned at `idx: 15` and its own stamp, and the stamps strictly increasing
  with the index — which is what `scripts/run/apply.mjs`'s `blockedOf` needs, and the half of a
  renumbering that a rename alone does not do.
- **TA3 → AA3** · the Stop gate, `pnpm lint && pnpm typecheck && pnpm test` over the merged tree.

## Cited

## 2. Object model

```
Workspace (one per account; billing, AI config, members)
└── Product (Sociera, Aurenza, Juno … — isolation boundary, permission boundary)
    └── Opportunity (a problem or outcome, e.g. "new users don't return after week 1";
        holds an evidence pile that outlives individual bets)
        └── Item (a unit of work moving through stages)
            ├── Artifacts: brief, PRD, tech spec, design links, backlog
            ├── Per-stage readiness scores + check results with evidence
            ├── Gaps (open / accepted / excluded), decisions log, evidence links
            └── Pushed tasks, ceremony packet, prompt packs
```

- The **dashboard** is the workspace-level view: essential state of every product at a glance.
- An item may be **unlinked** from any opportunity; that shows as a small advisory gap, never a block. Opportunities are mostly born from intake: the router files fragments as evidence or proposes new opportunities when fragments cluster.
- There are no teamspaces. Products are the only grouping and the visibility unit for roles.
- Every mutating action — human or agent — records its actor, timestamp, and trigger. The agent is a first-class actor.

---

## 13. Surface

**Primary view: a prioritized list, not a board.** Status is derived, so a drag-to-move board would be decoration. Three buckets:

- **Your move** — anything awaiting a human: sign-offs, exclusion confirms, triage items, walkthrough answers, stalled packets (>48 h). Always on top.
- **At risk** — score regressed this week, a handover-blocking gap older than 5 days, or time-in-stage past ~1.5× the learned baseline. Sorted by blocking-gap age 40%, regression size 30%, staleness ratio 20%, handover proximity 10% (weights tunable).
- **Flowing** — everything else, by recent activity.

A thin pipeline strip on top shows counts per stage. Each row carries per-stage readiness meters. Idle items dim relative to their stage baseline and receive a one-tap, reversible **park** suggestion; the parked list ("graveyard") is a first-class, guilt-free destination — killing ideas cheaply is the system working. Retroactive imports keep historical gaps in a separate **foundation gaps** view with one-tap "adopt," never polluting the buckets.

**Chat is everywhere and is a full action surface.** It is also the command palette: `Cmd/Ctrl+K` focuses it from any page. Globally aware (whole workspace), page-aware only for pronoun resolution ("park this" needs no name on an item page; "how's Juno doing?" works anywhere and navigates you there while answering). It performs every permitted move — park, confirm exclusions, log decisions, push gaps, draft patches — and respects the role matrix: a Developer asking for a rubric change gets "that's an Owner action — want me to request it?" It answers ceremony questions in librarian mode and captures decision moments ("so we're dropping video for V1 because of capacity") with an offer to log them: decision, reason, date, who.

**Notifications route by role.** Owner: daily digest + spend alerts. Product: triage, exclusion confirms, regressions. Developer: packets and walkthrough follow-ups only. Viewer: nothing by default. The **daily digest reports deltas, never state dumps:** new blocking findings, resolved findings, regressions, aging sign-offs, ready-buffer line, glossary proposals — in the same neutral voice throughout.

---

### build-log — open question 9

9. **Opportunities have no key column, so `/o/<key>` cannot be built.** `opportunity` carries `id`,
   `workspace_id`, `product_id`, `title` and `summary` — nothing to put in a URL a person can say.
   **When the opportunity page ships, mirror `item.key`:** a `key_prefix` counter assigned by
   trigger, the same discipline as `artifact_version.version_no`. **Not a uuid route.**
   `src/lib/routes.ts` keeps its segments short for one stated reason — "`/i/soc-12` is a URL a
   person can read out" — and `/o/6f3c…` defeats it entirely. Until then the item header shows the
   opportunity's **title as plain text**, no href: it fixes the thing that mattered (an item that
   shows its product but not its opportunity hides why it exists) and needs no migration.
