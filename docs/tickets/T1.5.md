# T1.5 — Row roving and freshness

Claimed by the scheduled run on 2026-09-14 (session `748b808b`, re-claiming a run that died at
session `87db13bc`) in its own worktree · Epic E1.1 Spine · Priority Medium · Type Enhancement ·
Spec `product-spec v1.6 §13 · design-spec §11 · build-log OQ6`. Branch `t1-5` off `origin/main` at
`0870c08`; `origin/main` merged in at `9a1a881` on 2026-09-16. Board row:
https://app.notion.com/3cf79dafd42e81ffab85eb4bc67abb9a.

The board's body held an Objective, one Rule and the Done line; Build read "To be written when the
ticket is cut" and Criteria and Tests were empty. The run expanded them from the Name, the
Objective, the Spec line and the cited sections, and the board carries the same text under a
`Drafted by pipeline · 2026-09-14 · confirm or edit in Notion` callout. The Cited section holds
every cited section from the repo.

**Sent back as an addendum on 2026-09-16, and renamed from "Park move and row roving".** Park —
the old Build 1, AC1 and AC2, TC1 and TC2, and the two Rules that were about it — moved to T1.6
Park move, which owns where a parked item is stored and the migration. What stays is numbered as it
was, so the thread, the report and the red-first record still point at the right lines. The
`## Addendum` section below is the reply verbatim and the criteria this round is closed against.

## Objective

Two of the three things T1.2 left on the list surface: arrow keys walk the rows over the
`roving.ts` that is already there, and the row's freshness reads the scoring clock the item page
already reads.

## Build

1. *(Park — split out to T1.6 Park move by the addendum.)*
2. **Arrow keys walk the rows** (design-spec §11) over `nextRovingIndex` in `src/lib/roving.ts`:
   with focus on a row's link, Down and Up move focus to the next and previous row across the
   three buckets in visual order, wrapping at the ends as the menus do; Home and End jump to the
   first and last. One client island around the list; the rows stay Server Components.
3. **The row's freshness reads the scoring clock** the item page reads since T2.4 —
   `scoring_run.scored_at` for the timestamp and `artifact.next_scoring_attempt_at` for the retry —
   so the dot and the mono-readout say "scored 6 h ago", and "scored 6 h ago — retrying" with the
   `--warning` dot per design-spec §10. Build-log open question 6 records that the row was left on
   last activity on purpose and ties the change to open question 7's read.

## Rules

- The rows stay Server Components; the walker is the list's one client island
- A queued retry is the system working: the `--warning` dot, never red (design-spec §10)

## Criteria

- AC3 With focus on a row's link, ArrowDown and ArrowUp move focus to the next and previous row's
  link across buckets and wrap at the ends; Home and End reach the first and last row; other keys
  and focus elsewhere in the row are untouched
- AC4 The row's readout shows the newest run's clock, and the retry state per §10 when a retry is
  scheduled
- AA1 The diff touches no migration and no schema file
- AA2 The walker and the row clock keep their tests green on merged main
- AA3 Nothing in the diff references park

## Tests

- TC3 → AC3 · `src/app/app/RowWalker.dom.test.tsx` (DOM test over the walker) plus one Playwright
  case on `/dev/primitives` in `e2e/list.spec.ts`, "walks the rows with the arrow keys, across
  buckets, wrapping at the ends"
- TC4 → AC4 · `src/db/queries/item.test.ts` (the embed and the mapping) ·
  `src/app/app/ItemRow.dom.test.tsx` (the readout and the dot) · the Playwright case in
  `e2e/list.spec.ts`, "shows the newest run's clock, warning-dotted while a retry is queued"
- TA1 → AA1 · `git diff --name-only origin/main...HEAD` names nothing under `drizzle/` or
  `src/db/schema/`, nor `docs/schema.md` or `src/db/database.types.ts`, and
  `node scripts/run/migration-check.mjs` reads `waiting: false`
- TA2 → AA2 · `git merge-base --is-ancestor origin/main HEAD` holds, and on that tree
  `pnpm vitest run src/app/app/RowWalker.dom.test.tsx src/app/app/ItemRow.dom.test.tsx src/db/queries/item.test.ts`
  passes, and so do the two Playwright cases above
- TA3 → AA3 · the lines the diff adds or removes under `src/` and `e2e/` — `git diff -U0
  origin/main...HEAD -- src e2e`, `+` and `-` lines only — carry no match for `park`, any case.
  The ticket, the report and the log entry name what was split out, and have to

## Done

`pnpm lint && pnpm typecheck && pnpm test && pnpm e2e`

## Report

Written at close: `docs/reports/T1.5.md`, mirrored into the board row.

## Addendum

2026-09-16 — sent back from Decision to be split and closed. The addendum on the board, verbatim:

> **Addendum — park splits out (2026-09-16)**
>
> Answering the question asked twice on this thread: **split park out.** The arrow-key walker and
> the row's scoring clock are finished, tested on merged main and mergeable now; park needs stored
> state and a migration, and holding two done pieces behind an unanswered schema question is the
> wrong trade. Where park is stored is decided in T1.6, which owns the migration.
>
> **What this ticket is now:** the walker and the row freshness only. Drop the Park chip, its
> mutation and its undo toast from Build, Criteria and Tests; leave the `roving.ts` and
> scoring-clock work as it stands on `17204ac`. Run the review it has not had yet, then close
> under §5 step 9 — no migration in the diff, so it self-merges on a PASS.
>
> **AA1** the diff touches no migration and no schema file · **AA2** the walker and the row clock
> keep their tests green on merged main · **AA3** nothing in the diff references park · TA1 → AA1
> · TA2 → AA2 · TA3 → AA3

This round builds AA1–AA3 and nothing else; the walker and the row clock are as they stand on
`17204ac`. Two readings this round takes, said in the claim's one comment on the board:

- **"Drop the Park chip … from Build, Criteria and Tests" is scope, not the row.** The "Park?" chip
  T1.2 renders on `main` stays as it is — T1.6 is where it starts doing something. Removing it
  here would be a change to what a product user sees, and a diff that references park.
- **"The diff" in AA3 is the code the diff changes** — the lines it adds or removes under `src/`
  and `e2e/`. The unchanged lines git prints around a hunk are `main`'s, and the ticket, the report
  and the log entry cannot say park moved to T1.6 without naming it.

## Cited

### product-spec §13 — Surface

#### 13. Surface

**Primary view: a prioritized list, not a board.** Status is derived, so a drag-to-move board would be decoration. Three buckets:

- **Your move** — anything awaiting a human: sign-offs, exclusion confirms, triage items, walkthrough answers, stalled packets (>48 h). Always on top.
- **At risk** — score regressed this week, a handover-blocking gap older than 5 days, or time-in-stage past ~1.5× the learned baseline. Sorted by blocking-gap age 40%, regression size 30%, staleness ratio 20%, handover proximity 10% (weights tunable).
- **Flowing** — everything else, by recent activity.

A thin pipeline strip on top shows counts per stage. Each row carries per-stage readiness meters. Idle items dim relative to their stage baseline and receive a one-tap, reversible **park** suggestion; the parked list ("graveyard") is a first-class, guilt-free destination — killing ideas cheaply is the system working. Retroactive imports keep historical gaps in a separate **foundation gaps** view with one-tap "adopt," never polluting the buckets.

**Chat is everywhere and is a full action surface.** It is also the command palette: `Cmd/Ctrl+K` focuses it from any page. Globally aware (whole workspace), page-aware only for pronoun resolution ("park this" needs no name on an item page; "how's Juno doing?" works anywhere and navigates you there while answering). It performs every permitted move — park, confirm exclusions, log decisions, push gaps, draft patches — and respects the role matrix: a Developer asking for a rubric change gets "that's an Owner action — want me to request it?" It answers ceremony questions in librarian mode and captures decision moments ("so we're dropping video for V1 because of capacity") with an offer to log them: decision, reason, date, who.

**Notifications route by role.** Owner: daily digest + spend alerts. Product: triage, exclusion confirms, regressions. Developer: packets and walkthrough follow-ups only. Viewer: nothing by default. The **daily digest reports deltas, never state dumps:** new blocking findings, resolved findings, regressions, aging sign-offs, ready-buffer line, glossary proposals — in the same neutral voice throughout.

---

### design-spec §11 — Keyboard & focus

#### 11. Keyboard & focus

`Cmd/Ctrl+K` focus chat · `Esc` closes the most recently opened layer (last-opened-wins, per §4 — not the highest rung) · `Enter` confirms focused proposal, `Cmd/Ctrl+Enter` sends in composer · arrow keys walk menus/selects/list rows · `/` focuses search in list views. Every interactive element reachable by Tab in visual order; focus ring per §6; focus trapped inside modals; on close, focus returns to the opener.

### design-spec §10 — System states & degraded pages

Cited by Build 3 and AC4 ("the retry state per §10"); inlined so the reviewer reads the line the row renders.

#### 10. System states & degraded pages

- **No AI key:** meters render hollow tracks + "connect AI to activate scoring" (ui-footnote) — never zeros, never red. **That is the item page, where the line stands beside the track and says what the emptiness means.** A list row has no room for the line, so the hollow track there is an unlabelled stub repeated once per row — noise that explains nothing. **Row micro-meters do not render at all without a key**; the space goes to the content, and the meters appear when the scores do.
- **Provider outage / retry:** freshness shows `--warning` dot + mono-readout "scored 6 h ago — retrying"; no banners.
- **404/500/offline:** empty-state pattern on dot grid, Æ mark 32, one line, one action ("back to dashboard" / "retry"). 500 may show a mono-readout incident id.
- Full-page loads: skeleton screens mirroring the target layout; never a centered spinner page.

### build-log — open question 6

6. **What T1.2 left on the list surface, and where each goes.** The "Park?" chip renders and does
   nothing — park is a mutation plus an activity row plus §13's undo toast, so it lands with the
   negotiation moves in **Phase 2**; it renders now because adding it later would shift every idle
   row's layout after the fact. Arrow-key walking of list rows (§11) is unwired, and
   `src/lib/roving.ts` is already there for whoever does it — **T1.3 or a later pass**. The row's
   freshness is *last activity*, not *scored at*: §8's dot and §10's "scored 6 h ago — retrying"
   both want a scoring clock, which arrives in **Phase 2**. **Half-answered by T2.4:** the item page
   now reads that clock — `scoring_run.scored_at` for the timestamp, `artifact.next_scoring_attempt_at`
   for the retry — and renders both states. The *row* still shows last activity, because §13's list
   is a workspace-wide ranking and giving every row its newest run is a second read across the whole
   workspace, not a column on the one it already makes. Decide it with the list's pagination question
   (open question 7), which is the same read.
