# T0.96 — Smoke D

Claimed by a `/ticket` run · 2026-09-13 · Epic E0.2 Pipeline · Priority Should · Type Technical ·
Spec `guidelines v1.5 §1` (set at claim — the row's Spec was empty; no drift against the repo
header). Branch `t0-96`, a scheduled run in a Desktop worktree (`primary: false`), off `HEAD` =
`6bd10e6` rather than `origin/main`: `.claude/settings.local.json` sets `AENIMA_RUN_BASE=HEAD`, the
override `branch.mjs` keeps for a run whose own scripts are not yet on main — T0.10's `prune.mjs`,
`log-index.mjs`, `docs/log/` and guidelines v1.5 are on this worktree's HEAD and not on `main`, and
steps 0, 8 and 9 of this run use them. So the branch is T0.10's three commits plus this ticket's,
the pull request carries T0.10's files until T0.10 merges, and this ticket's own diff is
`git diff 6bd10e6...HEAD`: `package.json`, `scripts/board-ids.mjs`, `scripts/board-ids.test.mjs`,
`docs/tickets/T0.96.md`, and at close `docs/reports/T0.96.md`, `docs/log/T0.96.md`,
`docs/build-log.md`. Same shape as Smoke B on `t0-8` and Smoke C on `t0-9`.

Drafted by pipeline · 2026-09-13 · confirm or edit in Notion. Original body: *Add a `pnpm
board:ids` script that prints the keys of .claude/board.json, with a test.*

## Objective

A developer at any checkout can list the board identity's keys with one command, `pnpm
board:ids`, without opening the file. Smoke D: a scheduled run adds a package script and its test
end to end — claim, branch, red-first tests, review, report, pull request — on a change small
enough that the protocol is the whole of the work.

## Build

`scripts/board-ids.mjs`: a pure `boardIds(text)` that returns the top-level keys of a JSON
document in the order `JSON.parse` yields them (AC3), and a CLI that reads `.claude/board.json` from the repository root and
prints one key per line. A `board:ids` entry in `package.json` that runs it.
`scripts/board-ids.test.mjs` covering the three criteria.

## Rules

The shape of `scripts/run/*`: export the function, run the CLI only under `isMain` so a test's
import does not execute it. Read the file relative to the repository root, not the working
directory, so the command works from a worktree or a subdirectory. Keys only, never values: the
file holds ids and the prefix, and this command is a listing of what is configured, not of what
it is set to. No new dependency.

## Criteria

AC1 `pnpm board:ids` from the repository root prints every top-level key of `.claude/board.json`,
one per line, in file order, and exits 0.

AC2 The output contains no value from the file — no id and not the prefix — keys only.

AC3 `boardIds(text)` is a pure function of the JSON text: it returns the keys in the order
`JSON.parse` yields them (file order; integer-like keys would come first, and board.json has
none), and a document that is not a JSON object is refused with an error naming `board.json` rather than
printing nothing.

## Tests

TC1 → AC1 `scripts/board-ids.test.mjs` "prints the keys of the committed board.json, one per
line, in order": the CLI's stdout over the real file equals the file's keys joined by newlines,
exit 0, and `package.json` has the `board:ids` script pointing at it.

TC2 → AC2 `scripts/board-ids.test.mjs` "prints no value": none of the committed file's values
appears in the output.

TC3 → AC3 `scripts/board-ids.test.mjs` "refuses a document that is not an object, naming the
file": `boardIds("[]")` and `boardIds("nope")` throw with `board.json` in the message.

## Done

`pnpm lint && pnpm typecheck && pnpm test`

## Report

Written at close — `docs/reports/T0.96.md`.

## Cited

### guidelines §1

## 1. Structure

```
dev  (teamspace)
└── Admin  (page — canonical home; build your own pages as linked views of these)
    ├── Guidelines   page   · mirror of docs/guidelines.md
    ├── Documents    page   · six sub-pages, mirrors of the repo docs
    ├── Tasks        database
    ├── Epics        database
    ├── Roadmap      database  (phases)
    ├── Releases     database
    └── Runs         database
```

Everything under Admin is created once and refreshed by the pipeline. Views on other pages are
yours; hide or show what you like there.

---
