# T0.15 — Read the board's select Status

Claimed by the scheduled run on 2026-09-14 (session `5cf9de31`) in its own worktree · Epic E0.2
Pipeline · Priority Must · Type Fix · Spec empty — nothing to compare, nothing drifted. Branch
`t0-15` off `origin/main` at `0870c08`. Board row:
https://app.notion.com/3db79dafd42e81e2a095d299e9cbc941. The body below is the board's, verbatim,
as the run before this one drafted it from its own preflight going red (guidelines §5 step 1).

> ⟡ Drafted by pipeline · 2026-09-14 · confirm or edit in Notion

## Objective

On 14 September a run found three human merge replies at Review (T0.14, T0.12, T2.8) that the
pipeline could not act on: the guard read each task at no status. Fix the board client so the
guard and the preflight read a task's Status: `scripts/run/notion.mjs` `task()` reads
`props.Status.status.name`, but the Tasks data source holds Status as a select property
(`props.Status.select.name`), so every row comes back with Status null. `threads.mjs` then shapes
a human `merge` at Review as `assess` and `permission.mjs` refuses `gh pr merge` with "at no
status". Read the select shape (and keep the status shape if the property is ever converted), add
the case to `notion.test.mjs` and `threads.test.mjs`, and confirm
`node scripts/run/permission.mjs merge` finds the word on a claimed Review task.

## Build

What the line above asks for, as written. The run that filed this has no more than that.

## Rules

As the repository's: CLAUDE.md, AGENTS.md, docs/guidelines.md.

## Criteria

- AC1 Fix the board client so the guard and the preflight read a task's Status:
  `scripts/run/notion.mjs` `task()` reads `props.Status.status.name`, but the Tasks data source
  holds Status as a select property (`props.Status.select.name`), so every row comes back with
  Status null. `threads.mjs` then shapes a human `merge` at Review as `assess` and
  `permission.mjs` refuses `gh pr merge` with "at no status". Read the select shape (and keep the
  status shape if the property is ever converted), add the case to `notion.test.mjs` and
  `threads.test.mjs`, and confirm `node scripts/run/permission.mjs merge` finds the word on a
  claimed Review task.

## Tests

- TC1 → AC1 — `scripts/run/notion.test.mjs`, `scripts/run/threads.test.mjs`; the live half is
  `permission.mjs`'s `verify("merge")` read against a Review task carrying a human `merge`.

## Done

`pnpm lint && pnpm typecheck && pnpm test`

## Report

Written by the run at close — `docs/reports/T0.15.md`, mirrored into the board row.

## Cited

The board row's Spec property is empty, so `spec-sections.mjs` had nothing to inline. The reading
the body names is the repository's constitution — CLAUDE.md, AGENTS.md — and docs/guidelines.md,
whose §4 ("The two words are verified in code") and §5 ("The board's token") are the sections this
fix serves: the guard and the preflight read the board over the API, and a Status the client
cannot see is a word the guard cannot find. The shape the API actually returns for the Tasks data
source's Status, read on 2026-09-14 with the integration token:

```json
{ "id": "xjQi", "type": "select", "select": { "id": "…", "name": "In progress", "color": "orange" } }
```
