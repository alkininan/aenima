# T0.31 Read bundled shell -c flags

## Objective

Found while reviewing T0.18 Guard the token. T0.18's reviewer, pass 2, Should 1. `parse()` in
`scripts/hooks/guard.mjs` reads the command string of `sh -c "…"` as the commands it runs, but only
when `-c` is its own word: a bundled flag — `bash -lc '…'`, `sh -ec '…'`, `bash -xc '…'` — hides the
string, so every Bash rule misses what it runs. On main today that carries `git push --force` past
rule (d) and `pnpm db:push` past rule (a); since T0.18 it carries a write to the Notion API past
rule (i) too. Read a shell's bundled short flags ending in `c` as `-c`, with a test per rule that
the shape used to pass.

## Build

What the line above asks for, as written. The run that filed this has no more than that.

## Rules

As the repository's: CLAUDE.md, AGENTS.md, docs/guidelines.md.

## Criteria

- **AC1** T0.18's reviewer, pass 2, Should 1. `parse()` in `scripts/hooks/guard.mjs` reads the
  command string of `sh -c "…"` as the commands it runs, but only when `-c` is its own word: a
  bundled flag — `bash -lc '…'`, `sh -ec '…'`, `bash -xc '…'` — hides the string, so every Bash rule
  misses what it runs. On main today that carries `git push --force` past rule (d) and
  `pnpm db:push` past rule (a); since T0.18 it carries a write to the Notion API past rule (i) too.
  Read a shell's bundled short flags ending in `c` as `-c`, with a test per rule that the shape used
  to pass.

## Tests

- **TC1 → AC1**

## Done

`pnpm lint && pnpm typecheck && pnpm test`

## Report

## Cited

The ticket cites no spec section. Its Rules name the repository's own documents — `CLAUDE.md`,
`AGENTS.md`, `docs/guidelines.md` — as a whole, and the Spec property on the board is empty.
