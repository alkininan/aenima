# T0.99 — Smoke B

Claimed by a `/ticket` run · 2026-09-03 · Epic E0.2 Pipeline · Priority Should · Type Technical ·
Spec `guidelines v1.3 §5 · build-guide v2.2 §6` (no drift against the repo headers).
Branch `t0-99` off `origin/t0-8` (`AENIMA_RUN_BASE`: T0.8, which holds this skill and the run
scripts, is not on `main` yet), so this ticket's own diff is `git diff origin/t0-8...HEAD`.

Drafted by pipeline · 2026-09-03 · confirm or edit in Notion. Original body: *Change the gate's
release message to the agreed wording.*

The Decision on this ticket was resolved on the board with the answer `default`, which takes the
Default line of the question. The agreed wording is therefore, byte for byte:

    gate released after three reds — restate the ticket rather than push harder at the code

## Objective

The Stop gate's release message — the one line it prints when three reds in a session release it
(build-guide §6) — reads the wording that was agreed, on both paths it is emitted: stderr and the
`systemMessage` field. For the session that hits the release and the human reading its transcript.

## Build

Set `RELEASE_MESSAGE` in `scripts/hooks/gate.mjs` to the agreed wording and update the assertion
in `scripts/hooks/gate.test.mjs` that pins it. The wording was not recorded anywhere when the
ticket was cut; the Decision comment on the board asked, and the answer `default` fixed it to the
line above (build-guide §6's own sentence).

## Rules

The gate refuses in law-6 language: what happened and where the rule lives, never "violation".
The message is emitted twice on release, from one constant — change the constant, not a call
site. Nothing else in `scripts/hooks/gate.mjs` changes.

## Criteria

AC1 On the third red in a session, and on every stop after it, the gate exits 0 and the text it
emits on stderr and in `systemMessage` is the agreed wording, byte for byte.

## Tests

TC1 → AC1 `scripts/hooks/gate.test.mjs`: the released decision's `stderr` equals the agreed
wording; observed red against the old constant before the change, green after.

## Done

`pnpm lint && pnpm typecheck && pnpm test`

## Report

Written at close — `docs/reports/T0.99.md`.

## Cited

Inlined verbatim by `scripts/run/spec-sections.mjs` at claim.

### guidelines v1.3 §5 — Run protocol

## 5. Run protocol

One run is `/ticket`, typed by a human from `~/dev/aenima`: fresh session, Fable, auto mode,
hooks as the boundary. T0.9 puts it on a schedule. Everything countable in the steps below is a
script under `scripts/run/` with a test; the skill holds the judgment and nothing else.

```
0  Preflight    assess Decision comments (§4) · mark merged Review tasks Done (merge-base
                --is-ancestor) and write Release rows · stop if a task is already In progress
1  Claim        top Ready by Priority (Must first), then oldest · set In progress · assign ID and
                Epic if missing · compare Spec versions against repo headers, note drift
2  Inline       read every cited section · write docs/tickets/<id>.md — the pack the reviewer reads
3  Branch       branch t<id> off origin/main · plan mode before any file changes · a primary
                checkout is returned to main on exit, success or not
4  Build        smallest complete implementation · new logic has tests observed failing first
5  Review       reviewer subagent, fresh context, reads the ticket file and the diff, not the
                author's summary · findings are tagged Must or Should · fix every Must and
                re-invoke, three passes maximum · a Must still standing after the third becomes
                an open question, Shoulds are recorded · out-of-scope findings → Backlog tasks
                (Type Fix, Epic inherited)
6  Migration    if the diff adds a migration file: stop → Decision (§4). Acting on the answer
                is T0.9's; until then a human applies it
7  Gate         Stop hook runs pnpm lint && pnpm typecheck && pnpm test in the cwd it is
                handed, not the project dir; red cannot close
8  Report       write docs/reports/<id>.md → mirror into the body Report section:
                ACs implemented (each with its test) · tests written (each observed red then
                green) · open questions · update build-log
9  Close        commit, push branch, open the PR → Review · never merge · Runs row written by
                the session-end script (T0.9)
```

One run, one task. The run exits; the next scheduled run takes the next task. Chaining inside a
session is not done: a session that built three things reasons worse about the fourth.

Hard boundaries, enforced by hooks not prose: no schema push, no migration apply, no writes to
`.env` or `.env.*` (`.env.example` is tracked and excepted), no production deploy, no force-push,
no push to main in any refspec shape, no merge with main checked out.

---

### build-guide v2.2 §6 — Done means

## 6. Done means

`pnpm lint && pnpm typecheck && pnpm test` all pass, and `pnpm e2e` for anything that touches a
surface. New logic has tests, and each of them has been observed failing. A structural ticket also
needs the fresh-context review above. Report back as: **ACs implemented, tests written, open
questions.**

**The first sentence is enforced, not requested.** The `Stop` hook runs those three commands and
refuses the stop while any is red, handing back the last 40 lines of whatever failed; red cannot
close. Two things keep it honest. It fingerprints the working tree — the commit plus the contents
of every changed and untracked file — and skips the run when nothing has moved since the last
green, because `Stop` fires at the end of every turn and the suite costs about seventy seconds
against a hosted Postgres. And three reds in one session release it, which is the
three-failed-corrections rule wired in: a gate that kept refusing would loop a session against a
ticket that cannot be satisfied. A released gate is the signal to restate the ticket, not to push
harder at the code.

Its hook `timeout` is 600s against a ~76s gate, and the gap is deliberate: a hook that times out
does not refuse, it is simply ignored, so a ceiling near the measurement turns one slow run against
a hosted Postgres into a silent release. The measurement sets the expectation; it is not the
budget.

`pnpm e2e` is not in the gate. It needs a running server, and a hook that starts one would be
deciding on a session's behalf what the session is doing; run it yourself for surface work.
